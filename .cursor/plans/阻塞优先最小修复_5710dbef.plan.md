---
name: 阻塞优先最小修复
overview: 按阻塞问题优先，先修复会影响交付/上线判断的缺陷，再做最小范围补强，保持现有架构与页面大纲不变。
todos:
  - id: fix-audit-chain
    content: 修复 security_logger 跨重启哈希链续接并验证 smoke 通过
    status: in_progress
  - id: add-readme-delivery
    content: 新增 README 覆盖运行、部署、环境变量和故障排查
    status: pending
  - id: document-model-handoff
    content: 明确模型交付策略与就绪校验命令
    status: pending
  - id: minimal-metrics-traffic
    content: 补齐 dashboard 流量趋势接口与前端 range 联动（最小实现）
    status: pending
  - id: normalize-api-shape
    content: 以兼容方式统一错误体与分页元信息
    status: pending
isProject: false
---

# 阻塞优先最小修复清单

## 目标与边界
- 目标：最小改动下把项目从“有条件通过”提升到可交付状态。
- 边界：不改整体架构、不重做前后端页面大纲、不新增大功能。

## P0 阻塞项（必须先完成）

### 1) 修复审计日志完整性跨重启断链
- 文件：`[c:\Users\yyl\Desktop\ai安全\ai-security-guardian\src\audit\security_logger.py](c:\Users\yyl\Desktop\ai安全\ai-security-guardian\src\audit\security_logger.py)`
- 最小改动：
  - 初始化时从现有 `security.log` 末条恢复 `last_hash`。
  - 仅在完整性开启时恢复；读取失败时降级为 `genesis`（不中断主流程）。
- 验收：
  - 连续两次启动+写日志后 `verify_integrity()` 通过。
  - `tests._phase8_smoke` 的完整性校验不再失败。

### 2) 补齐交付文档（README）
- 新文件：`[c:\Users\yyl\Desktop\ai安全\ai-security-guardian\README.md](c:\Users\yyl\Desktop\ai安全\ai-security-guardian\README.md)`
- 最小内容：
  - 项目简介与模块结构。
  - 本地启动（Web/API、`main.py`）步骤。
  - 环境变量说明（引用 `.env.example` 关键项）。
  - Docker/Compose 启动命令（默认与 `full-chain`）。
  - 常见故障排查（Redis、模型缺失、权限、健康检查）。
- 验收：
  - 新人可按 README 在新机器完成最小启动。

### 3) 明确模型交付策略（避免“可运行但无检测能力”）
- 文件：
  - `[c:\Users\yyl\Desktop\ai安全\ai-security-guardian\README.md](c:\Users\yyl\Desktop\ai安全\ai-security-guardian\README.md)`
  - `[c:\Users\yyl\Desktop\ai安全\ai-security-guardian\.env.example](c:\Users\yyl\Desktop\ai安全\ai-security-guardian\.env.example)`
- 最小改动：
  - 在文档中明确“模型文件是否随仓库交付”。
  - 若不随仓库交付，提供获取路径/生成命令与最小校验命令。
- 验收：
  - 运行前可明确判断模型是否就绪，不再依赖隐式降级。

## P1 非阻塞高价值项（阻塞清零后）

### 4) 把 Dashboard 的流量趋势从占位改为可展示
- 文件：
  - `[c:\Users\yyl\Desktop\ai安全\ai-security-guardian\web\app.py](c:\Users\yyl\Desktop\ai安全\ai-security-guardian\web\app.py)`
  - `[c:\Users\yyl\Desktop\ai安全\ai-security-guardian\web\static\js\dashboard.js](c:\Users\yyl\Desktop\ai安全\ai-security-guardian\web\static\js\dashboard.js)`
- 最小改动：
  - `/api/metrics/traffic` 返回基础时间序列（哪怕是由现有告警聚合）。
  - 前端 range 切换真实带参请求，不再只清空图表。

### 5) 统一 API 响应风格（只做兼容层，不重构）
- 文件：`[c:\Users\yyl\Desktop\ai安全\ai-security-guardian\web\app.py](c:\Users\yyl\Desktop\ai安全\ai-security-guardian\web\app.py)`
- 最小改动：
  - 保持旧返回兼容，新增统一字段（例如 `error/code/message`）优先在新增与错误分支补齐。
  - 分页接口统一暴露 `X-Total-Count/X-Limit/X-Offset` 或在 body 增加 `meta`（二选一）。

## 验证顺序（最小回归）
- 1) `python -m pytest -q`
- 2) `python -m tests._smoke_phase7`
- 3) `python -m tests._phase8_smoke`
- 4) 手动验证：`/login` -> `/dashboard` -> `/alerts` -> `/settings` 关键操作一遍。

## 交付判定
- 满足以下即可判定“阻塞已清”：
  - 审计完整性校验稳定通过。
  - README 足以支撑本地与 Docker 启动。
  - 模型交付路径明确且可校验。
- 其后再进入 P1 优化，避免范围失控。